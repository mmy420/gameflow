# PowerShell 脚本用 CRLF —— 目标是 Windows
*.ps1   text eol=crlf
*.psd1  text eol=crlf
*.psm1  text eol=crlf
*.md    text eol=lf
*.json  text eol=lf

# fixture 是二进制样本，绝不可做行尾转换 —— 转了就不是同一个归档了
tests/fixtures/** binary
