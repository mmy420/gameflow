# GameFlow · Windows 游戏资源批处理工作流 · 详尽设计规格（SPEC v1）

> 一个 **批次驱动、三执行体、状态机落盘** 的 Windows 游戏资源归档流水线：把「资源页 → 网盘转存 → 下载 → 解压 → 校验 → 删源包 → MTool 加载翻译」这条重复的人工链条，拆成可重跑、可中断、每一步都有显式状态的作业。
>
> **状态**：设计规格已成稿，**尚未落地**。开工第一件事是 §11 的实测（不做它，本文每一节都建在沙上）。
>
> **目标平台**：Windows。本文在 macOS 上撰写——因此凡 Windows 特有的行为，要么有官方文档依据，要么明确标 `【待测】`。
>
> **核心设计判断**：三个目标彼此不对称——A 巡检要的是**无人值守**，B 作业要的是**密码与提权**，C 前台要的是**独占桌面**。上游方案的两阶段拆分不够：需要密码的步骤和需要提权的步骤**都不能**放进无人值守的计划任务（S4U 登录类型不存储密码，解不开 DPAPI 密码库；`MpCmdRun` 官方明写需提权）。这一刀是被官方文档逼出来的，不是偏好。详见 §1.2。
>
> **最高风险**：删除源包。C3（源包与本体同目录）+ C4（解压后自动删除）意味着**不可撤销、无回收站兜底**，判错一次即永久数据丢失。§6.8 的 12 条与条件、§3 的 `verified` + `consumed_by` gate、§9.7 的诚实局限，都是围绕这一件事设计的。

---

## 关于本文档

**可信度标注是硬要求。** 每一条技术断言后面都带来源等级，读的时候请当真：

| 标注 | 含义 | 你可以怎么用 |
|---|---|---|
| `【官方】` | 官方文档 / 手册原文，附 URL | 可直接依赖 |
| `【实测】` | 本轮在 macOS 上实际跑出来的，**并注明能否外推到 Windows** | 外推标记为「可外推」的可依赖；标「需复验」的进 §11 |
| `【社区】` | 论坛 / 博客 / 源码阅读的共识，无官方背书 | 当作强假设，别当事实 |
| `【待测】` | 未核实。**全部收录在 §11**，每条附「不测的后果」与「阻塞哪一节」 | 开工前先打掉高优先级的那几条 |

**「遗留异议」块**：文中若干处有 `⚠️ 遗留异议` 引用块。那是章节起草者对某条锁定决策提出、而主控**未予采纳**的保留意见，连同理由一并留在原地。它们不是待办，是**已知张力的存档**——将来若要推翻对应决策，先读它。

**本文档如何产生**：需求来自 Codex 会话「规划 Windows 游戏资源工作流」（`01a08eba-1b68-7b33-9a7e-242a5be1c22a`，2026-09-10），该会话确立了批次驱动与两阶段拆分的方向。在此之上做了六条技术线的事实核查（7-Zip 命令行语义 / Windows 下载完成检测 / PowerShell 工程 / Codex on Windows 能力边界 / MTool 与引擎识别 / 资源站归档的真实形态），一轮对抗式事实仲裁，分节起草，再经一致性与保真度两轮审查、一轮主控裁决修订。

仲裁推翻了上游方案中的若干断言，其中影响最大的三条：

1. **Codex 内置浏览器没有「按任务/按游戏」的下载目录**——官方配置参考里零命中。上游「Codex 将下载目标定到 `games\<游戏名>\`」这个能力不存在，改为 `_inbox` 统一落地 + 执行体 A 认领归位（**C3 因此是终态保证，不是落地瞬间的保证**）。
2. **「改后缀」在绝大多数情况下根本不需要**——7-Zip 默认按内容嗅探（`-t*:r` + 前 8 MB 签名扫描），伪装扩展名、前置垃圾字节、图种、SFX 全部无需改名即可处理；传具体 `-t<类型>` 反而会**关掉嗅探**、把本可自动处理的情况变成硬失败。唯一硬例外是分卷 `.NNN` 序列重建。
3. **游戏目录名的真实约束不是「非 ASCII」而是「必须能在系统 ANSI 代码页内无损表示」**——日文 Windows 上日文路径完全正常；失败的是老引擎（KiriKiri / NScripter / RPG Maker 2000–VX）用 ANSI API 读自己的资源时，路径里出现当前代码页表示不了的字符。

完整对照见 §1.6，全部锁定决策与被否决的替代方案见 §12 决策台账。

---

## 目录

- [§1 概述、定性、核心设计判断、非目标与合规边界](#1-概述定性核心设计判断非目标与合规边界)
  - [1.1 一句话定性与现状人工链条](#11-一句话定性与现状人工链条)
  - [1.2 核心设计判断](#12-核心设计判断)
  - [1.3 用户约束回执](#13-用户约束回执)
  - [1.4 非目标](#14-非目标)
  - [1.5 合规与边界](#15-合规与边界)
  - [1.6 本 SPEC 相对上游 Codex 会话改动了什么](#16-本-spec-相对上游-codex-会话改动了什么)
  - [1.7 端到端范例：一个典型批次长什么样](#17-端到端范例一个典型批次长什么样)
- [§2 总体架构：三执行体、部署形态、目录布局与 MAX_PATH 预算](#2-总体架构三执行体部署形态目录布局与-max_path-预算)
  - [2.1 三执行体职责矩阵](#21-三执行体职责矩阵)
  - [2.2 数据流与控制流](#22-数据流与控制流)
  - [2.3 部署形态（E1 主力机约束下的具体安排）](#23-部署形态e1-主力机约束下的具体安排)
  - [2.4 完整目录布局](#24-完整目录布局)
  - [2.5 MAX_PATH 预算实算](#25-max_path-预算实算)
  - [2.6 为什么 `_stg` 在库级而不在游戏目录下](#26-为什么-_stg-在库级而不在游戏目录下)
  - [2.7 `_inbox` 的存在理由与认领归位](#27-_inbox-的存在理由与认领归位)
  - [2.8 与 Git 的边界](#28-与-git-的边界)
- [§3 数据模型：批次、状态、事件、清单、规则、密码库](#3-数据模型批次状态事件清单规则密码库)
  - [3.0 六类文件与它们的真相层级](#30-六类文件与它们的真相层级)
  - [3.1 JSON 与序列化硬规则](#31-json-与序列化硬规则)
  - [3.2 批次清单 `D:\GameLib\batches\<batch-id>.json`](#32-批次清单-dgamelibbatchesbatch-idjson)
  - [3.2A `dirname` 生成算法（全 SPEC 唯一权威）](#32a-dirname-生成算法全-spec-唯一权威)
  - [3.3 `games\<dirname>\.gameflow\events.jsonl`：唯一真相](#33-gamesdirnamegamefloweventsjsonl唯一真相)
  - [3.4 `games\<dirname>\.gameflow\state.json`：派生快照](#34-gamesdirnamegameflowstatejson派生快照)
  - [3.5 `games\<dirname>\.gameflow\manifest.json`：来源与产物档案](#35-gamesdirnamegameflowmanifestjson来源与产物档案)
  - [3.6 规则文件（`D:\GameFlow\rules\`，进 Git，人是唯一写者）](#36-规则文件dgameflowrules进-git人是唯一写者)
  - [3.7 密码库 `%LOCALAPPDATA%\GameFlow\vault.clixml`](#37-密码库-localappdatagameflowvaultclixml)
  - [3.8 「谁写谁读」矩阵](#38-谁写谁读矩阵)
  - [3.9 编码与落盘协议](#39-编码与落盘协议)
  - [3.10 本节产生的【待测】项（全部进 §11）](#310-本节产生的待测项全部进-11)
- [§4 状态机：状态集、迁移表、停机态手册、幂等与崩溃恢复](#4-状态机状态集迁移表停机态手册幂等与崩溃恢复)
  - [4.0 三条贯穿全节的原则](#40-三条贯穿全节的原则)
  - [4.1 完整状态图](#41-完整状态图)
  - [4.2 完整迁移表](#42-完整迁移表)
  - [4.3 停机态手册（runbook，共 17 个）](#43-停机态手册runbook共-17-个)
  - [4.4 幂等](#44-幂等)
  - [4.5 崩溃恢复](#45-崩溃恢复)
  - [4.6 并发与互斥](#46-并发与互斥)
  - [4.7 重跑语义（`-Retry`）](#47-重跑语义-retry)
  - [4.8 `ABANDONED` 的语义与清理策略](#48-abandoned-的语义与清理策略)
  - [4.9 本节产生的待 Windows 实测项](#49-本节产生的待-windows-实测项)
  - [4.10 本节引用的一手来源](#410-本节引用的一手来源)
- [§5 执行体 A：巡检（Sweep）](#5-执行体-a巡检sweep)
  - [5.0 A 的铁律与写白名单](#50-a-的铁律与写白名单)
  - [5.1 A 的一次运行](#51-a-的一次运行)
  - [5.2 认领归位（inbox claim）](#52-认领归位inbox-claim)
  - [5.3 下载完成判定 G1–G6](#53-下载完成判定-g1g6)
  - [5.4 归档识别](#54-归档识别)
  - [5.5 卷集归组与连续性检查](#55-卷集归组与连续性检查)
  - [5.6 只读预检](#56-只读预检)
  - [5.7 头加密（`-mhe`）分支](#57-头加密-mhe分支)
  - [5.8 ZIP 文件名编码风险检测](#58-zip-文件名编码风险检测)
  - [5.9 对账报告](#59-对账报告)
  - [5.10 A 的调度](#510-a-的调度)
- [§6 执行体 B：作业（Work）](#6-执行体-b作业work)
  - [6.1 B 的一次运行做什么](#61-b-的一次运行做什么)
  - [6.2 7-Zip 调用约定（**契约级；全 SPEC 唯一封装，§5 / §7 / §8.8.7 / §10 全部引用本小节**）](#62-7-zip-调用约定契约级全-spec-唯一封装5--7--887--10-全部引用本小节)
  - [6.3 密码链 P0–P6 与等价展开](#63-密码链-p0p6-与等价展开)
  - [6.4 解压与递归解包](#64-解压与递归解包)
  - [6.5 分卷 `.NNN` 重建（C7 的唯一硬例外）](#65-分卷-nnn-重建c7-的唯一硬例外)
  - [6.6 逐条目校验](#66-逐条目校验)
  - [6.7 内容判定与 `EXTRACTED_NO_CONTENT`](#67-内容判定与-extracted_no_content)
  - [6.8 12 条删除判据 D1–D12（**本小节是 D1–D12 的唯一权威**）](#68-12-条删除判据-d1d12本小节是-d1d12-的唯一权威)
  - [6.9 Defender 预检](#69-defender-预检)
  - [6.10 MOTW（Mark of the Web）](#610-motwmark-of-the-web)
  - [6.11 文件名编码修复](#611-文件名编码修复)
  - [6.12 护栏（**本小节是护栏阈值语义的权威；默认值一律登记在 §3.2.3 的 `guards`**）](#612-护栏本小节是护栏阈值语义的权威默认值一律登记在-323-的-guards)
  - [6.13 不进自动流程的形态（**本小节是这一判定的唯一权威；§5.4.4 引用本小节**）](#613-不进自动流程的形态本小节是这一判定的唯一权威544-引用本小节)
- [§7 执行体 C：前台（会话模型、三类网盘适配、MTool、人工接力点清单）](#7-执行体-c前台会话模型三类网盘适配mtool人工接力点清单)
  - [7.0 下载这一步不归 C：主路径是人工排队（用户裁定，2026-09-14）](#70-下载这一步不归-c主路径是人工排队用户裁定2026-09-14)
  - [7.1 会话模型](#71-会话模型)
  - [7.2 C 的输入输出契约](#72-c-的输入输出契约)
  - [7.3 下载目标的现实](#73-下载目标的现实)
  - [7.4 资源页解析](#74-资源页解析)
  - [7.5 三类网盘适配器（E2）](#75-三类网盘适配器e2)
  - [7.6 MTool](#76-mtool)
  - [7.7 人工接力点清单](#77-人工接力点清单)
  - [7.8 C 的前置条件](#78-c-的前置条件)
- [§8 Codex 操作面与 PowerShell 工程约定](#8-codex-操作面与-powershell-工程约定)
  - [8.1 五个固定操作（做成 Codex skills）](#81-五个固定操作做成-codex-skills)
  - [8.2 脚本入口约定](#82-脚本入口约定)
  - [8.3 沙盒配置：双根问题](#83-沙盒配置双根问题)
  - [8.4 ExecutionPolicy](#84-executionpolicy)
  - [8.5 免审批：rules / prefix_rule 对 PowerShell 的语义空白](#85-免审批rules--prefix_rule-对-powershell-的语义空白)
  - [8.6 为什么阶段一调度用 Windows 任务计划，不用 Codex 的 Scheduled tasks](#86-为什么阶段一调度用-windows-任务计划不用-codex-的-scheduled-tasks)
  - [8.7 `AGENTS.md` 该写什么](#87-agentsmd-该写什么)
  - [8.8 PowerShell 工程约定（契约级）](#88-powershell-工程约定契约级)
  - [8.9 本节产生的待实测项](#89-本节产生的待实测项)
- [§9 安全、隐私与诚实局限；前置条件与 Preflight 探测](#9-安全隐私与诚实局限前置条件与-preflight-探测)
  - [9.1 密码的暴露面（诚实清单）](#91-密码的暴露面诚实清单)
  - [9.2 Computer Use 的截图会进上下文](#92-computer-use-的截图会进上下文)
  - [9.3 DPAPI vault 的局限](#93-dpapi-vault-的局限)
  - [9.4 MOTW 不是安全边界（明确声明）](#94-motw-不是安全边界明确声明)
  - [9.5 头加密包的护栏空洞（无法消除的盲区）](#95-头加密包的护栏空洞无法消除的盲区)
  - [9.6 RAR4 的不可区分性](#96-rar4-的不可区分性)
  - [9.7 删除的不可逆性](#97-删除的不可逆性)
  - [9.8 恶意内容与误报](#98-恶意内容与误报)
  - [9.9 威胁模型与明确不防的](#99-威胁模型与明确不防的)
  - [9.10 合规边界](#910-合规边界)
  - [9.11 前置条件与 `Preflight.ps1`](#911-前置条件与-preflightps1)
  - [9.12 为什么这一节不能跳过](#912-为什么这一节不能跳过)
- [§10 落地路线、测试策略与验收](#10-落地路线测试策略与验收)
  - [10.1 里程碑 M0–M4](#101-里程碑-m0m4)
  - [10.2 测试策略](#102-测试策略)
  - [10.3 验收清单](#103-验收清单)
  - [10.4 阈值标定](#104-阈值标定)
- [§11 待 Windows 实测清单](#11-待-windows-实测清单)
  - [11.1 里程碑清账映射](#111-里程碑清账映射)
  - [11.2 已从清单中消解的三条](#112-已从清单中消解的三条)
- [§12 决策台账](#12-决策台账)
  - [12.1 决策表](#121-决策表)
  - [12.2 待用户拍板项](#122-待用户拍板项)

---

## §1 概述、定性、核心设计判断、非目标与合规边界

### 1.1 一句话定性与现状人工链条

**GameFlow 是一个批次驱动、三执行体、状态机落盘的 Windows 游戏资源归档流水线。** 它把「资源页 → 网盘 → 下载 → 解压 → 校验 → 删源包 → MTool 翻译」这条今天靠人一步步盯着做的链条，拆成可重跑、可中断、每一步都有显式状态的作业。

姿态是**确定性优先**：不猜密码、不猜类型、不猜规则，不自适应、不学习、不做加权评分。所有分支要么由显式规则决定，要么停下来落一个具名状态等人。

#### 1.1.1 现状：七步人工流程

| # | 现状人工动作 | 这一步的本质 | 机器能不能独立做完 | 归属 |
|---|---|---|---|---|
| 1 | 从资源网站拿到一个游戏的资源页 | 网页读取 + 语义抽取（标题、密码提示、链接） | 能读，但有登录/风控 | 执行体 C（§7） |
| 2 | 资源页给出多个云盘链接 | 链接归集 | 能 | 执行体 C（§7） |
| 3 | 到各云盘「保存到我的网盘」并下载 | **凭据与人机验证**（登录、验证码、会员、限速） | **不能**——按 1.5 人工接管 | 执行体 C（§7） |
| 4 | 本地建以游戏名命名的文件夹 | 字符串规范化 + 文件系统约束 | 能，但有 ACP 与 MAX_PATH 陷阱（§2、§3.2A） | 执行体 A（§5） |
| 5 | 下载压缩包到该文件夹 | **等待**（分钟到小时级）+ 完成判定 | 能，但「大小不变」判据会稳定产出错误结论（§5.3，G1–G6） | 执行体 A（§5） |
| 6 | 解压（有密码、有嵌套、有时要改后缀），并删掉源包 | 确定性文件处理 + **不可逆删除** | 能，但这是全项目风险最高的一步（1.2.4） | 执行体 B（§6） |
| 7 | 如需要，用 MTool 加载翻译 | GUI 注入 + 一次性规则确认 | 部分能；新引擎/多 exe 歧义必须停下来问人 | 执行体 C（§7） |

#### 1.1.2 为什么这条链值得自动化

**不是因为单次操作难。** 单看任何一步人做起来都不费劲。它值得自动化，是因为四件事在**批量**下同时成立：

1. **状态没有落脚处。** 一次处理 6 个游戏时，「哪个下完了、哪个缺密码、哪个还差一卷」全存在人的短期记忆和一堆浏览器标签页里。E1 说这是主力机、会被打断——**打断一次，状态就没了**。这是本项目要解决的第一个问题，也是 1.2.3 选择事件溯源的直接原因。
2. **等待占绝对主导。** 第 5 步分钟到小时级、第 6 步分钟级。人在等待期内只能轮询，而轮询恰恰是机器该做的事（执行体 A）。
3. **错误代价高度不对称。** 第 1–5 步做错重来一次即可；第 6 步的删除是**永久数据丢失**（C3+C4，见 1.2.4）。而人在批量重复劳动里判断力下降最快的地方，正好就是第 6 步。把它交给一组**可枚举、可审计、与条件**的判据，比交给注意力更可靠。
4. **需要人的部分很少，但必须留在人手里。** 七步里真正无法自动化的只有第 3 步的凭据/验证码、第 7 步遇到新引擎时的一次性规则确认，其余全是确定性文件处理。**这个比例本身就是「值得做」的论据**——如果一半步骤都需要人，这条流水线就不该存在。

> 反过来说，本 SPEC **不追求把第 3 步和第 7 步也自动化掉**。为了流程完整性去绕验证码或猜 MTool 该点哪个 exe，是把项目从「省事」变成「不可信」。非目标见 1.4，合规边界见 1.5。

---

### 1.2 核心设计判断

本节是全篇的锚：后面每一节的结构都可追溯到这五条判断之一；任何与这五条冲突的实现改良，都应先回到这里论证。

#### 1.2.1 （a）为什么必须是批次驱动，而不是单游戏

C2 是用户原话提炼出的硬约束：「实际表现上往往不会是仅处理一个游戏而是一组游戏」。但「批次」在本 SPEC 里不是一个 `for` 循环的语法糖，它是三件事：

**其一，时间尺度天然不对齐。** 下载小时级、解压分钟级、翻译加载人分钟级。以单游戏串行推进，整批耗时 = 各环节之和；批次驱动让「A 在下载」与「B 在解压」自然重叠，而这个重叠**不需要任何并发编程**——三个执行体各自扫描批次清单，各干各的。

**其二，失败隔离是刚需，不是优化。** C5 明说压缩包「通常有密码」——**缺密码是常态而不是异常**。若一个条目缺密码会阻塞整批，这套流水线在真实批次上将永远处于阻塞态。因此有不变量：

> **I4** 每个游戏条目独立推进；A 缺密码不阻塞 B。

I4 的实现形态是：**每个条目有自己的 `.gameflow\`（`events.jsonl` / `state.json` / `manifest.json`，schema 见 §3），批次清单只持有条目的 id 与来源信息，不持有状态**——批次条目上**没有 `state` 字段**，只有一个 `abandoned` 标记（§3）。批次层面不存在「批次状态」这种东西，只有一份由各条目状态**派生**出来的对账视图（§5.9 的报告）。

```
批次 2026-09-12-vn（6 个条目）        ← 一份清单、一个 batch-id
  g0001 ─► DOWNLOAD_STABLE ─► EXTRACTING ─► …
  g0002 ─► WAITING_FOR_PASSWORD          ┤ 停在这里，不阻塞任何人
  g0003 ─► AWAITING_DOWNLOAD（分卷 3/5，等 §5.3 的 G6）
  g0004 ─► COMPLETE
  g0005 ─► WAITING_FOR_MANUAL_INSTALL
  g0006 ─► TRANSLATION_NOT_APPLICABLE
  ▲ 唯一横切这些泳道的东西 = §5.9 的对账报告，而它是只读派生物
```

**其三，批次清单是某些失败模式唯一的检测锚。** 「A 卷下完了、B 卷还没开始下」这种情况，从文件系统上看和「一个完整的单卷包」完全一样——尺寸静默、能独占打开、`7z l` 报出的 stderr 与缺中间卷、与单文件截断**一字不差**（证据见 1.2.4 的 T10）。唯一可靠的自动检测点，是**执行体 C 在解析资源页时把「一共几个下载链接 / 几个分卷」写回批次清单的 `expected_parts`**（取值域「≥1 或 null」，「明确无分卷」用 `1` 表达，见 §3），由 A 在 G6 里比对（§5.3）、由 B 在 D11 里复核（§6.8）。**这条能力只有在批次语义下才存在。**

**单游戏是批次大小为 1 的特例，没有第二套代码路径。** 不提供「快速处理一个包」的旁路——旁路会绕过 §6.8 的删除判据，而那正是最不能绕的地方。

#### 1.2.2 （b）为什么必须是三执行体，而不是两阶段

上游把项目切成「阶段一 = 后台 / 阶段二 = Computer Use」。方向是对的，但**阶段一内部还必须再切一刀**——这一刀不是架构美学，是被两条官方文档逼出来的。

**逼迫一：无人值守计划任务解不开密码库。** Windows 任务计划 S4U 登录类型，官方逐字：

> `TASK_LOGON_S4U` (2)：…**When an S4U logon is used, no password is stored by the system and there is no access to either the network or encrypted files.**
> 【官方】https://learn.microsoft.com/en-us/windows/win32/taskschd/principal-logontype

密码库是 DPAPI 加密的（`%LOCALAPPDATA%\GameFlow\vault.clixml`，绑定运行身份，§3）。「no access to encrypted files」直接意味着 **S4U 任务打不开 vault**；不用 S4U 则要么在计划任务里存明文凭据（暴露面），要么要求一个已登录的交互会话——那就不是「无人值守」了。**结论：需要 vault 密码的步骤，天然属于「人在场的那一半」。**

**逼迫二：Defender 预检需要提权。** MS Learn 的 `MpCmdRun` 页面页首逐字：

> **You need to run MpCmdRun in an elevated Command Prompt**（a Command Prompt window you opened by selecting Run as administrator）。
> 【官方】https://learn.microsoft.com/en-us/defender-endpoint/command-line-arguments-microsoft-defender-antivirus

这与「用 `-RunLevel Limited` 跑一个受限的定时任务」直接冲突。**结论：Defender 预检也属于「人在场的那一半」。**

于是切分不是选出来的，是算出来的：

```
                需要 vault 密码，或需要提权？
                 │                       │
                否│                      │是  → 不能进无人值守计划任务
                 ▼                       ▼
        需要占用前台桌面？          需要占用前台桌面？
         否 ─► A 巡检              否 ─► B 作业
         是 ─► C 前台              是 ─► C 前台
```

**A = 巡检（Sweep，无人值守、只读预检与认领归位）· B = 作业（Work，人触发，密码与提权都在这里）· C = 前台（Foreground，Computer Use）。** 三者的触发方式、部署形态与逐条职责表是 §2 的权威内容，本节不复述。

**A + B 合起来正好等于用户要的「阶段一（不使用 Computer Use，含人工环节）」，完全满足 C1。** 这个切分顺带把上游那个悬空项——「阶段一需要人工做的一部分具体指什么」——**定义死了**：恰好四项（录密码、提权确认、登记新网盘客户端的临时后缀、设 Defender 排除），逐条见 §12 `D-14`。

**A 的写集合白名单**（写进不变量，§5 与 §8 反复引用）：

> **I5** 执行体 A 的写集合**恰为三处**：① `games\<dirname>\`（含 `.gameflow\`）；② **新建** `_reports\<batch-id>-<seq>.md`（**不得覆盖已存在的报告**）；③ 把 `_inbox\` 中**已认领**的文件移入 ①（同卷 rename，不构成删除语义）。
> 除此之外 A 不写任何路径，**不删除任何文件**，不回收 `_stg\`（只在报告里列出孤儿，回收由 B 做）。
> 补充：**A 永不触碰密码、永不提权。**

> 白名单是**枚举式**的，不是「除了 X 都不许」式的——原措辞（「永不写 `games\` 以外」）与分配给 A 的「认领归位」「出对账报告」自相矛盾。由此定案：**A 直接写对账报告**（§5.9），孤儿 `_stg\` 的回收留给 B（§6）。

**A 还必须是无状态、扫一遍就退出的批处理，不得设计成常驻守护进程。** 理由同样来自官方文档（`unified_exec` 在 Windows 默认关闭、Computer Use 独占前台），逐字引文与出处见 1.6 的 U8。叠加 E1：在一台会被打断的主力机上，任何长跑进程都会被用户关掉、被睡眠、被重启。**能被随时杀死而不损坏任何东西的批处理，才是这台机器上唯一稳妥的形态。**

> **给后续各节的判别式**：任何新增步骤归 A / B / C，只问两个问题——「要不要 vault 密码或提权？」「要不要占鼠标键盘？」。不要按「这看起来像后台活」来分。

#### 1.2.3 （c）为什么必须状态机落盘 + 事件溯源

E1 是本项目最强的环境约束：**这是主力机，会被打断。** 具体形态有三种，每一种都会让内存里的状态蒸发：

- 用户要用电脑 → 把执行体 C 关掉（C 占着鼠标键盘）；
- 睡眠 / 重启 / 断电 → 正在跑的 `7z x` 直接消失；
- 下载是小时级、大包解压是分钟级 → **中途死掉不是异常路径，是常规路径**。

因此状态不能放在三个地方：**内存**（进程一死就没）、**长跑守护进程**（1.2.2 已否决）、**模型的会话上下文**（会话结束即失忆，且会把确定性逻辑塞进不确定的载体）。状态只能落盘。

落盘的形式选**追加事件 + 派生快照**，而不是「就地改一个 state.json」：

```
A / B / C ──append──► .gameflow\events.jsonl   ← 唯一真相（I2）
                            │（纯函数 replay）
                            ▼
                      .gameflow\state.json     ← 派生快照，可删可重建
                            ├─► §6.8 的删除判据只读它（I3，不现场重判）
                            └─► §5.9 的对账报告只读它
崩溃点可落在任何位置（半行 JSONL / 快照损坏 / 解压到一半 / 删除前一刻），
四种落点的恢复动作都是确定的 —— 协议见 §3.9 与 §4.5，本节不复述。
```

> **I1** 任何停机态都**不删除任何文件**。
> **I2** 状态只能由 `events.jsonl` 追加事件推进；`state.json` 是派生物，任何时候可删掉重建。
> **I3** 删除源包由 `state.json` 里**已落盘**的 `verified:true` + `consumed_by` gate 住，**不现场重新判断**。

这里有一条容易被误解的工程事实需要提前说清楚：**Windows 上不存在真正的原子文件替换**（`ReplaceFile` 自称是「把几个步骤合进一个函数」并列举了三种部分失败的中间态，`MoveFileEx` 全文没有 atomic 字样；官方原文与逐条对照见 §8.8）。所以本 SPEC **不写「原子」，只写「可恢复」**：真正的崩溃安全来自 I2，不来自追求单次替换的原子性。相应地 `.gameflow\` 必须与游戏库同卷——`File.Replace` 跨卷会抛异常（§8.8）。

I3 是幂等的来源，也是它存在的全部理由：**删除动作读的是历史，不是现场。** 若删除前重跑一遍判据，「跑判据」与「删文件」之间就有一个窗口，崩溃落在窗口里就无法判断删过没有；读已落盘的 `verified:true` 则让重跑无害——要么状态里写着「已验证已消费」（继续删剩下的），要么没写（从头验证）。

#### 1.2.4 （d）为什么删除源包是本项目风险最高的操作

C3（源包与本体同目录）+ C4（解压后自动删除源包）合起来，构成一个在别的项目里少见的组合：

| 因素 | 后果 |
|---|---|
| 删除在游戏目录里就地发生（C3） | 删掉的就是**唯一一份**源包，旁边没有备份副本 |
| 脚本删除走 API，不经 Explorer | **没有回收站兜底**（这是 Windows 上 shell 删除与 API 删除的行为差异）【工程事实，§6.8 会给出所用 API】。本 SPEC 用尺寸门控的 `.gameflow\trash\` 自建一个**只覆盖小包**的后悔窗口，见本节末 |
| 上游那一份在网盘上 | 可能已过期、被清理、被和谐，或要重新转存并重新排队——**重下的代价是小时级，且不保证还能下到** |
| 自动、批量、无人复核（C4） | 一个错误判据会在整批 N 个条目上重复执行 |

**结论：判错一次即永久数据丢失。** 这不是「要小心」这种程度的提醒，它决定了 §6.8 的判据必须写成什么样。

而「7z 说成功了」这句话本身有大量已知的失效方式。下表把每一个已核实的陷阱，映射到 §6.8 那 12 条判据中的对应项——**这张表是 D1–D12 必要性的证明，不是判据本体**；每条判据的可执行定义只在 §6.8 有一份，本表不复述。

| # | 已知陷阱 | 可信度 | 对应判据 |
|---|---|---|---|
| T1 | `exit 1` = 命令「完成了」但有文件被静默跳过 | 【官方 exit code 表 + 实测(macOS)；退出码属 7-Zip 跨平台公共层，可外推到 Windows】 | D2 / D4 |
| T2 | `exit 0` 仍可能伴随 `Warnings:`（如尾部垃圾）——「有 Warnings」与「exit 1」是两条独立信号 | 【实测(macOS)，可外推】 | D3 |
| T3 | `7z t a.7z b.7z` 的第二个参数是 a.7z 的**内部文件过滤器**，不是第二个包；结果 exit 0 而一个包都没测 | 【实测(macOS)；命令行解析跨平台，可外推】 | D2 |
| T4 | `exit 0` 且零文件的四种情形：过滤器无匹配 / 多归档参数误用 / 只含空目录 / 尾部垃圾 | 【实测(macOS)，可外推】 | D5 |
| T5 | 解出来了但落盘不全（条目缺失、size 不符） | 【实测(macOS)。注意：Windows 没有信号也没有 `ulimit -f`，磁盘满是 `ERROR_DISK_FULL` 会显式报错，所以「工具报成功、落盘却截断」在 Windows 上远没有 POSIX 演示那么容易发生——本 SPEC 据此把全量 CRC 重读降为默认关闭（§6.8）】 | D6 |
| T6 | ZipCrypto 的 **1/256 假通过**：密码错也会「成功」解出垃圾文件 ⇒「解出来几个文件」永远不能当密码正确的证据 | 【实测；属算法性质，与平台无关，可外推】 | D7（AES / ZipCrypto 的判别归 B，见 §6.3） |
| T7 | 还有没解开的嵌套容器就删了源包（C6 是常态） | 【实测：「7z 能打开它」≠「它是要递归解包的容器」——Windows 上每个 `.exe/.dll` 都是 PE 归档，`.docx/.apk/.jar` 都是 `Type=zip`】 | D8 |
| T8 | 产物里含 symlink / junction / reparse point，「解出来了」其实指向别处 | 【实测(macOS)：7-Zip 确实拦路径穿越但**完全静默**（exit 0、零 warning、零 stderr），流水线拿不到信号；且 `-snld20` 实测真的创建了逃逸链接并 exit 0。**Windows 上 `\` 与盘符语义不同，落地位置不可外推 → §11 待测**】 | D9 |
| T9 | 只解出说明/密码类文件，游戏本体其实在另一个还没下完的包里 | 【社区 + 上游经验】 | D10（否则落 `EXTRACTED_NO_CONTENT`） |
| T10 | 卷集看着齐，其实少一卷 | 【实测(macOS)：缺中间卷 / 缺末卷 / 单文件截断三种情况 stderr **一字不差**，可外推】 | D1 / D11 |
| T11 | 判据过了但状态没落盘就删，崩溃后无法判断删过没有 | 【工程推论 + §8 落盘协议】 | D12（+ I3 gate） |
| T12 | staging→落地的移动失败或体积不一致 | 【工程推论】 | D12 |

三条由此推出的硬规则：

1. **12 条是「与」，不是「或」，也不是打分。** 任一不满足 → 保留所有压缩包，落对应停机态，写日志（I1）。没有加权、没有「凑够 10 条也行」。
2. **删除前必须把每个源包的 SHA-256 写进 `manifest.json`。** 零成本、纯保险：万一误删，至少能确认重下的是不是同一个包（§3.5）。
3. **要删掉 §6.8 里的任何一条判据，必须先在上表里指出它对应的陷阱为什么不会发生。** 这是对「以后有人觉得判据太啰嗦」的防御。

> 编号书写约定（供全篇统一）：删除判据写 `D1`–`D12`（无连字符）；§12 决策台账写 `D-NN`（带连字符）。两者不同。

**两段式删除的定案（尺寸门控）。** `retention.trash_days = 3`、`retention.trash_max_bytes = 2 GiB`（两个字段与默认值登记在 §3）。**小于**阈值的源包先移入 `games\<dirname>\.gameflow\trash\`，`trash_days` 天后由执行体 B 清理；**大于等于**阈值的直接删除。

**为什么是尺寸门控而不是一刀切：误判概率最高的恰恰是小包**——纯文本汉化补丁既是压缩比 133x 触 `ratio_warn` 的那一类（合法但告警），也最容易命中 `EXTRACTED_NO_CONTENT` 的边界；而与磁盘余量护栏（§6.12）的冲突只在**大包**上成立。同卷移动是元数据操作，磁盘代价有硬上限（阈值 × 批次条目数）。**C4 语义仍然满足**：源包在游戏目录里自动消失、不需要任何人工动作，改变的只是物理删除的时刻；设 `retention.trash_days = 0` 即回到字面意义的 C4。

这条**仍列入 §12「待用户拍板」**，并在那里显式签收代价：「误删无法恢复，只能靠 `manifest.json` 的 SHA-256 确认重下的是不是同一个包，而重下不保证还能下到。」

#### 1.2.5 （e）「不猜」是硬原则

密码不猜、类型不猜、规则不猜。这三句话不给判别标准就会退化成口号——「等价展开 4 个候选」和「字典试 4 个候选」在代码里长得一模一样。判别标准是三个问题，**三个都是「是」才叫不猜，任一为「否」就是猜**：

- **Q1 来源**：候选是否来自一个**有明确语义**、有限、可枚举的来源（人写的声明、密码库条目、归档自身的字节、正则精确抽取）？
- **Q2 规模**：候选集大小是否是与输入无关的常数，不会因为「多试几次成功率更高」而增长？
- **Q3 兜底**：全部失败之后，是否有一个**具名停机态**接住，而不是继续扩大候选集？

| 做法 | Q1 | Q2 | Q3 | 判定 |
|---|---|---|---|---|
| P0–P4 取到的候选，各做 ≤4 种**等价展开** | 是 | 是（候选数 × 4 封顶） | 是（`WAITING_FOR_PASSWORD` / `PASSWORD_ENCODING_SUSPECT`） | **不算猜** |
| 字典爆破 / 从游戏名·域名·文件名推导密码 / 把 README 里「读懂」的一段话当密码 | 否（后者是模型语义理解，不是正则抽取） | 否（越试越可能成） | — | **猜** |
| 不给 `-t`，让 7-Zip 按内容嗅探判类型 | 是（归档自身的签名字节） | 是（一次调用出答案） | 是（`WAITING_FOR_RULE`） | **不算猜** |
| 对候选类型各试一次 `-tzip`/`-trar`/`-t7z`，或把未知文件批量改成 `.zip`/`.rar` 再试 | 否 | 否 | — | **猜**（且实测会把本可自动处理的包变成硬失败） |
| 按批次条目**显式声明**的规则重建分卷 `.NNN` | 是（人写的声明） | 是（一组一次） | 是（`WAITING_FOR_RULE`） | **不算猜** |
| MTool 多 exe 时自动选 `Game.exe` | 否（启发式） | — | — | **猜** |
| MTool 多 exe → 候选集 + 人确认一次写进 profile | 是 | 是 | 是（`WAITING_FOR_MTOOL_RULE`） | **不算猜** |

**为什么「等价展开 ≤4 个」不是暴力破解。** 展开集是固定的四项、去重后 ≤4（逐项定义见 §6.3）：这四个字节串不是四个不同的密码，而是**同一个语义对象在四种编码假设下的四种字节表示**——资源站上所谓「乱码密码」正是正常中文的 UTF-8 字节被按 GBK 显示出来的结果【实测：密码是字节不是字符，两种编码互不通用；字节层事实，与平台无关，可外推】。展开集与「试了多少次」无关，也不随失败增长；用尽即停 `WAITING_FOR_PASSWORD`。

配套的一条硬规则（来自实测，最容易被「顺手优化」破坏）：**密码是字节，不是字符。** 尾随空格有意义（`'pass '` ≠ `'pass'`，所以展开集里「原样」与「strip」两项都要留）；密码库存储必须字节精确，UI 与日志**不得做任何 Unicode 规范化**——NFKC 会把全角 `ｐａｓｓ１２３` 折成 `pass123`，直接破坏密码【实测，字节层事实，可外推；落盘纪律见 §3 与 §9】。

**同一把尺子适用于类型与规则**：

- **类型不猜**：主路径不改名、不给具体 `-t`。7-Zip 的默认打开模式就是 `-t*:r` + 前 8 MB 签名扫描窗口【官方：随 7-Zip 官方 tarball 分发的手册 `MANUAL/cmdline/switches/type.htm`】【实测(macOS, 7zz 25.01)，样例与逐项结果见 1.6 的 U2；归档识别属跨平台公共层，可外推到 Windows，§11 仍列一次性复验】。嗅探不出来就停 `WAITING_FOR_RULE`，**不做「三种类型各试一次」的探测**。合法的 `-TypeSpec` 取值（含 `none` = 不传 `-t`）与各自的适用场合由 §6.2.1 唯一定义，本节不复述。
- **规则不猜**：分卷 `.NNN` 重建是本项目唯一必须物理改名的场景（§6.5），且必须由批次条目**显式声明**才执行；MTool 的目标 exe 不写「多 exe 时选 `Game.exe`」这种自动规则——论坛真实反例里正确答案是 `開始游戏.exe`，选它会卡在加载界面【社区】。

**唯一被允许的「学习」形态**：人确认一次 → 结果固化成一条显式规则文件（`source-profiles.json` / `mtool-profiles\<item-id>.json`，schema 见 §3.6）。**这是人写的规则，不是机器学的权重**：可读、可 diff、可删除、可进 Git，同样输入产生完全相同的输出。任何「随运行自动调整阈值 / 自动扩大候选集 / 按历史成功率排序」的机制都在 1.4 的非目标里。

---

### 1.3 用户约束回执

| 约束 | 含义 | 本 SPEC 如何满足 | 在哪一节 |
|---|---|---|---|
| **C1** | 阶段一不用 Computer Use（可含人工环节）；阶段二用 | A + B = 阶段一（B 需要人在场，但**不碰 GUI**）；C = 阶段二 | §2 §5 §6 §7；论证见 1.2.2 |
| **C2** | 工作对象是一组游戏 | 批次为唯一工作单位；单游戏 = 批次大小 1，无旁路；条目独立推进（I4） | §3 §5；论证见 1.2.1 |
| **C3** | 原始压缩包与游戏本体同目录 | **终态保证**：下载先落 `_inbox`，由 A 按批次清单认领归位到 `games\<dirname>\`，与本体同级 | §2 §5；成因见 U1 |
| **C4** | 解压完成后自动删除压缩包 | 受 D1–D12 十二条**与**条件 gate（§6.8）；由 `state.json` 里已落盘的 `verified:true` + `consumed_by` gate（I3）。物理删除分两段：小于 `retention.trash_max_bytes` 的源包先入 `.gameflow\trash\`、`retention.trash_days` 天后由 B 清理，其余直接删——**游戏目录里的源包即时消失、无需任何人工动作，C4 语义不变** | §6.8；论证见 1.2.4 |
| **C5** | 压缩包通常有密码 | P0→P6 优先级链 + ≤4 等价展开 + 具名停机态兜底（清单见 §4） | §6.3；判别标准见 1.2.5 |
| **C6** | 多重嵌套 | `max_depth = 5`（计数口径：成功解开一个白名单容器 = depth+1）+ 容器白名单 + 扩展名排除表（`container-types.json`，schema 见 §3.6）+ 按内容嗅探 | §6.4；见 U9 U13 |
| **C7** | 有时需要改文件名后缀 | **重新界定**：主流程不改名、不给具体 `-t`；唯一硬例外是分卷整组补齐 `.NNN`，且必须批次显式声明；原始文件永不改名（改的是 staging 里的副本） | §6.5；见 U3 |
| **E1** | 主力机、会被打断 | A 脱离前台、无状态、扫完退出；C 短会话、可中断续跑；事件溯源保证任意点被杀都能恢复 | §2 §5 §7；论证见 1.2.3 |
| **E2** | 三类网盘（百度 / 夸克·阿里 / 123·蓝奏·直链） | `netdisk-profiles.json` 三套规则（临时物模式表 + 页面规则，schema 见 §3.6）；海外盘只留扩展位 | §7 §3.6 |
| **E3** | 已装 7-Zip **或** WinRAR/Bandizip（未指明哪个）、Windows 版 Codex、MTool | `Preflight.ps1` 一次性探测；归档引擎硬性要求 **7-Zip CLI ≥ 25.01（建议 ≥ 26.02）**，只探到 GUI 工具时给出补齐路径 | §9 |

---

### 1.4 非目标

本节明列**不做**的事，目的和 §1.2 一样是防御性的：防止实现阶段「自作主张改良」回到已被论证否决的方向。

| # | 明确不做 | 原因 / 去向 |
|---|---|---|
| N1 | **不做无人值守的全自动。** 不追求「贴一个链接，回来就全好了」 | 需密码/提权的步骤在官方约束下进不了无人值守任务（1.2.2），登录与验证码必须人工接管（1.5）。人在环内是设计目标，不是妥协 |
| N2 | **不做通用下载器。** 不实现下载引擎、不做断点续传、不管理下载队列 | 下载由用户既有的浏览器/网盘客户端完成，本项目只做**完成判定**（§5.3 的 G1–G6）与认领归位。写下载器等于把 E2 三类网盘的私有协议全逆一遍 |
| N3 | **不做翻译质量保证。** 不评估、不校对、不比较机翻质量 | MTool 是注入式工具，产物落盘位置未知【社区】；完成判据只到「翻译已加载」（§7） |
| N4 | **不做游戏启动器。** 不管理存档、不做游戏库前端、不记录游玩时长 | 流水线终点是 `COMPLETE`（可玩状态），之后与本项目无关 |
| N5 | **不做跨机同步。** 不做云备份、不做多机状态合并 | 密码库是 DPAPI 加密、绑定运行身份，跨机原理上不可解密（「重装/换机 = 密码全丢」这条代价写在 §9）。且库根**不得**放在 OneDrive 同步目录下——有社区证据表明会导致 MTool inject 失败【社区】 |
| N6 | **不做 GUI。** 所有呈现是 Markdown 报告（`_reports\<batch-id>-<seq>.md`，结构见 §5.9） | 「呈现层不吝啬」指的是报告要真正好读（分组、排序、异常项在最前、每条给出下一步动作），不是写一个窗口程序。Markdown 可 diff、可进聊天、可存档 |
| N7 | **不做自适应 / 学习 / 加权评分。** 没有滚动阈值、没有 ML 判定、没有任何「历史**改变判定结果**」的机制 | 用户品味明确拒绝；且 1.2.5 的三问判别式在结构上就排除了这类机制 |
| N7b | **唯一例外：密码候选的*尝试顺序*可按命中次数排序**（§3.7.4、§6.3.5）。它改变的是**先试谁**，不改变**试哪些**（候选集仍由 key 匹配决定）、也不改变**试几次**（上限仍是 `候选数 × 4`） | 判别标准是 1.2.5 的三问：结果是否可能不同？否——同一候选集穷举完的结论完全一样，只是期望尝试次数更少。是否可解释？是——计数器可读、可 diff、可手动清零。是否可复现？是——给定同一份计数器，顺序唯一。**硬护栏：排序只在 key 匹配出的那个候选列表*内部*生效，绝不跨 key 借用高频密码**——那就是 §6.3 硬禁的字典爆破换了身外衣 |
| N8 | **不做「伪装扩展名 → 真实类型」映射表。** `dat-as-rar` / `bin-as-zip` 这类规则整张删除 | 7-Zip 自己就知道真实类型（见 U2）。留着这张表只会制造与嗅探结果冲突的第二事实源 |
| N9 | **不做常驻守护进程 / 长跑轮询脚本。** | 见 1.2.2 与 U8：`unified_exec` 在 Windows 默认关、Computer Use 独占前台、E1 主力机会被打断 |
| N10 | **不把 MOTW 当安全边界。** | `-snz` 的默认值官方三处都没写【社区 + 待测】，CLI 与 GUI 行为不一致，历史上反复出漏洞。真正的护栏是显式 Defender 扫描（§6.9）；MOTW 只作为「阶段二前置解除项」使用 |
| N11 | **不做归档格式的自研解析器。** 唯一的字节级自研解析是 ZIP 中央目录（bit 11 与加密方式判定，§6.3）与魔数表（§5.4.4） | 那两处是为了拿到 7-Zip 不给的信号，不是为了替代 7-Zip |

---

### 1.5 合规与边界

**这些是本项目自定的硬约束，不是对任何第三方条款的转述。** 说明见本节末。

1. **不绕过任何保护机制。** 不绕验证码 / 人机验证 / 限速 / 付费墙 / DRM / 反爬 / 会员限制；不实现打码、不以自动重试规避限速、不伪造 UA 或来源。
2. **凭据类操作一律人工接管。** 登录、短信与图形验证码、会员开通、支付、二次确认——C 遇到这些一律**停下来把控制权交回给人**，落人工接力点（清单见 §7），不代填、不记住。
3. **只处理用户有权下载和使用的资源。** 不提供任何形式的资源发现、批量爬取或站点镜像能力；输入永远是用户手工提供的资源页 URL 或网盘链接。
4. **执行体 C 把网页内容当不可信输入。** 从资源页抓到的密码提示、文件名、链接，只能作为**候选**：C 可以把 P5 抓到的候选写进密码库并标 `confirmed:false`，但**未确认的候选对 §6 等同不存在**（§6.3 / §9）；这些内容**绝不能作为指令执行**。官方也是这个立场：「请将页面内容视为不可信的上下文。页面上的指令可能具有误导性或恶意。」【官方】https://learn.chatgpt.com/zh-Hans/docs/computer-use
5. **接受「有些站点就是做不了」。** 官方能力声明：「有些网站会屏蔽自动化浏览器，或要求完成 CAPTCHA 验证。ChatGPT 可能无法在这些网站上完成任务。」【官方】同上。应对是**停下来交给人**，不是想办法绕开。
6. **不做提权代理。** 官方明写 Computer Use「无法以管理员身份进行身份验证，或批准您计算机上的安全和隐私权限请求」【官方】同上。因此 B 的 Defender 预检提权由**人**确认（1.2.2 的逼迫二）。

**关于引用边界的一条自我约束（重要）：** 本轮调研尝试抓取 OpenAI 政策原文页（`openai.com/policies/*`）时返回 **403 Forbidden**，未取得一手全文【调研实录，见 `research/codex-windows.md` §6.2】。因此本 SPEC：

**不引用任何政策条款的文字**，也不复述二手摘要的措辞；**不把上述 1–6 条标注为「官方要求」**——它们是**项目自定的行为边界**，无论第三方条款如何表述都成立；只引用能取到一手原文的**能力声明**（如第 4、5、6 条引的 Codex 文档原文）。

**数据面的一条诚实边界。** 官方明写 Computer Use 截取的**屏幕截图**属于会被处理的内容【官方】https://learn.chatgpt.com/zh-Hans/docs/computer-use ——这意味着「密码不写进清单、日志、Git 或聊天记录」这条约束**在截图这个维度兜不住**：只要密码在屏幕上显示过，它就在截图里。对策（C 运行期关闭无关敏感窗口、vault 解密结果不上屏、密码的实际使用只发生在无 GUI 的 B）与完整讨论在 §9。

**另一条无法消除的暴露面。** 7-Zip 只有 `-p` 一种密码输入方式（管道、文件重定向、环境变量三条路径均实测证伪）【官方：随 tarball 分发的手册 `password.htm` 全文只有 `-p{password}` 一种语法】【实测(macOS)；属命令行接口设计，可外推到 Windows】。于是**密码会短暂出现在进程命令行中，同机的其他进程与系统审计可以看到，无法消除**。这是工具的硬限制而非实现缺陷，必须写进 §9 的诚实局限，而不是被藏起来。

---

### 1.6 本 SPEC 相对上游 Codex 会话改动了什么

上游是 2026-09-10 的 Codex 会话（CONTEXT 记录了全文）。它的**架构方向是对的**（批次驱动、两阶段、状态机、先做阶段一），本 SPEC 全部继承。下表列出**被核实推翻或收紧的具体断言**——每行给出原断言、核实结果、本 SPEC 怎么写。`U1`–`U15` 针对上游会话；**`U16`–`U19` 是本轮定案相对起草契约的收紧**，它们同样改变了可实现的契约，故一并登记。**行号可被其他章节引用。**

| # | 原断言（上游 / 起草契约） | 核实结果（可信度） | 本 SPEC 怎么写 | 章节 |
|---|---|---|---|---|
| **U1** | 「Codex 将下载目标定到 `games\<游戏名>\`」；「内置浏览器可下载到指定目录」 | **能力不存在。** 官方浏览器文档只给出「设置 > 浏览器」里的**全局**下载位置或「每次询问」；112 KB 官方配置参考里 `download_dir`/`download_path`/`downloads_dir` 类键 **0 命中**【官方 https://learn.chatgpt.com/zh-Hans/docs/browser + 本机 grep 实测】 | 全局下载目录固定为 **`D:\GameLib\_inbox`**（唯一落地目录），A 按批次清单**认领归位**到 `games\<dirname>\`。**C3 因此是终态保证，不是落地瞬间的保证** | §2 §5 §7 |
| **U2** | 用 `7z x -tzip` / `-trar` / `-t7z` **显式指定类型**来代替改后缀 | **会适得其反。** 给具体类型**关闭内容嗅探**；实测 `-tzip` 打一个内容是 7z 的 `fake.jpg` → exit 2 硬失败，不给 `-t` 反而正常打开【实测(macOS)，可外推】 | **永不传具体类型名。** 合法的 `-TypeSpec` 取值与各自的适用场合由 §6.2.1 唯一定义（含 `none` = 不传 `-t`，A 的只读预检与 G5 必须用它） | §6.2.1 |
| **U3** | 改后缀是主流程的一步：`原文件 → 文件头/压缩测试 → 指定规则 → 临时副本改名 → 再次测试 → 解压` | **绝大多数情况不需要改名。** 7-Zip 默认 `-t*:r` + 前 8 MB 签名扫描；伪装扩展名、前置垃圾（≤8 MB）、图种、SFX 全部无需改名即可 `l/t/x`【官方 `type.htm` + 实测(macOS)】。**唯一硬例外是分卷**：伪装命名的分卷组 `7z t` exit 2 完全打不开，只改首卷也不行，必须整组补齐 `.NNN`；且 `.NNN` 之前的扩展名无所谓（`game.mp4.001` 正常工作）【实测(macOS)】 | 六步改名流程从主流程**降级为例外分支**，只处理分卷一种形态，且必须由批次条目显式声明。C7 因此被重新界定（见 1.3） | §6.5 |
| **U4** | 目录名直接用游戏名 | **真实约束不是「非 ASCII」，而是「必须能在系统 ANSI 代码页内无损表示」**——老引擎（KiriKiri / NScripter / RM2000-VX）用 ANSI API 打开自己的资源，日文 Windows 上日文路径完全正常【社区；本 SPEC 据此定案】 | 完整规范化链 + **一道 ACP 可表示性 strict 往返检查**：能无损往返就用规范化标题（保住可读性），不能就降级 ASCII slug，真实标题写进 `manifest.json`，降级原因必须记录。**算法本体（逐字映射表、裁剪长度、哈希位数、往返用的 API）只在 §3.2A 有一份，本节不复述** | §3.2A |
| **U5** | staging = `games\<游戏名>\.gameflow\staging\<attempt-id>` | **吃掉 39 字符路径预算**，配合日文长名 + 多重嵌套会真的撞破；建目录实际上限是 **248**（MAX_PATH − 12），不是 260【算术 + 官方 MAX_PATH 语义】 | 改用**库级短中转区** `D:\GameLib\_stg\<8hex>\`（25 字符，布局与路径预算见 §2），**必须与游戏库同卷**。这不违反 C3——C3 约束的是源包的**终态**位置，中转区解完即清空。附带收益：Defender 排除项简化成一个固定目录 | §2 §6 |
| **U6** | 阶段一是**一个**后台阶段 | **阶段一内部必须再切一刀。** S4U 官方明写不存密码、访问不了加密文件 → 解不开 DPAPI vault；`MpCmdRun` 官方明写需提权 → 与受限计划任务冲突【两条均为官方，逐字引文与 URL 见 1.2.2】 | 切成 **A（巡检，无人值守）** + **B（作业，人触发）**。A + B = C1 的阶段一，并定义了「阶段一需要人工做的一部分」= 四项（§12 `D-14`） | §2 §5 §6 |
| **U7** | 阶段一由 Codex 桌面 App 的 Scheduled tasks 调度 | **依赖面过大且与品味冲突。** Codex 计划任务要求「机器开机 + 桌面 App 常驻 + 项目目录在盘上 + 沙盒能写到库目录」【官方】；Windows 任务计划只要求用户登录着。用一个 LLM turn 去驱动一个纯确定性文件处理器，是把确定性逻辑塞进不确定载体 | 阶段一调度用 **Windows 任务计划**（`MultipleInstances = IgnoreNew`，不用 S4U）。Codex 侧只保留 **5 个固定操作做成 skills**，其配置形态与目录由 §8 定义 | §8 §2 · `D-10` |
| **U8** | `Watch-Downloads.ps1` 常驻监测下载 | **不可行。** Windows 上 `features.unified_exec` 默认关闭（官方逐字 "enabled by default except on Windows"）；Computer Use 在 Windows 上独占前台、无法与用户共用会话（「锁屏继续」是 macOS 专属）【官方】；叠加 E1 | A 必须**无状态、可重跑、扫一遍就退出**，由外部调度器反复调用。脚本清单里没有 `Watch-*.ps1` | §5 §2 |
| **U9** | `max_depth = 4` | 真实资源站形态（改名 → 外壳 zip → 分卷 rar → 7z → 同名文件夹）就有 4~5 层【社区素材】 | **`max_depth = 5`**，可配置。并钉死计数口径：**「成功解开一个白名单容器 = depth+1」**，不是「调用了几次 7z」（`.tar.gz` 不同工具一步或两步）；用 `-t*` 强制每次只开一层让计数与现实一致；**没有例外**（Split 外壳同样计层） | §6.4 |
| **U10** | 批次清单是 `batch.yaml`，模板 `batch.template.yaml` | PowerShell 内置 JSON（`ConvertTo-Json`/`ConvertFrom-Json`），YAML 需要外部模块 —— 零依赖优先【工程事实】 | **全部改 JSON**：`batches\<batch-id>.json`、`templates\batch.template.json`、`rules\*.json`。`ConvertTo-Json` 的深度陷阱与「写后回读校验」纪律见 §8.8；人类注释走 `"_note"` 字符串字段 | §3 §8.8 |
| **U11** | 「下载文件无临时后缀且连续多次扫描大小不变后，才标记为可解压」 | **这个判据会稳定产出错误结论**，六种独立机理都能击穿（预分配、限速停顿、0 字节最终名占位、无临时后缀的缓慢合并、完成后后缀残留、先改名后扫描且可被 `IAttachmentExecute::Save()` 删掉）；逐条机理与出处见 §5.3【调研，多为官方源码/文档级证据】 | `DOWNLOAD_STABLE` = **六重与条件 G1∧G2∧G3∧G4∧G5∧G6**，逐条定义与采样参数见 §5.3。两个异常必须分开处置：`IOException` = 被占用（继续等）；`UnauthorizedAccessException` = 权限问题（**不能当成还在下载**，否则永久卡死） | §5.3 §4 |
| **U12** | 删除判据：「7z 测试通过 + 解压目标存在 + 不再有可识别嵌套包 + 预期内容或人工确认存在 + manifest/state 已写入」（5 项） | **已知陷阱多于 5 项**，且每一项都能让「看起来成功」与「其实没解出来」重合 | **12 条与条件 D1–D12**（权威定义在 §6.8，必要性证明见 1.2.4 的 T1–T12 表，本节不复述判据内容）。删除前把每个源包的 SHA-256 写进 `manifest.json`；删除由已落盘的 `verified:true` gate 住（I3） | §6.8 |
| **U13** | 「递归识别内部压缩包并继续解压」 | **「7z 能打开它」≠「它是要递归解包的容器」**：Windows 上每个 `.exe/.dll` 都是 PE「归档」（游戏目录里少说几十个），`.docx/.xlsx/.apk/.jar/.epub` 都是 `Type = zip`，`.msi/.doc` 是 Compound【实测(macOS) + Windows PE 事实】 | 递归必须用**容器白名单 + 扩展名排除表**（`rules\container-types.json`，键名与取值见 §3.6，判定顺序见 §6.4），**绝不能用「7z 能不能打开」当判据**。这是扩展名唯一真正有用的场合：不是识别要解什么，而是排除不该解什么 | §6.4 |
| **U14** | 每游戏子目录 `00_source / 01_game / 02_translation / 03_tools` | 已被用户 C3 推翻（源包与本体同目录） | 游戏目录 = 源包 + 本体平铺 + 一个 `.gameflow\`（只存状态与日志，**绝不存密码、账号、带 token 的 URL**） | §2 §3 |
| **U15** | 状态机含 `WAITING_FOR_MTOOL`，隐含「都能翻译」 | **MTool 不支持 Unity / Godot / Unreal**（官网与 Patreon 两个独立来源的引擎清单都没有）【社区，两个独立来源】；且 `UNKNOWN` **不等于**不支持翻译（Enigma Virtual Box 单 exe 打包的 MV 判 UNKNOWN 但 MTool 能处理） | 增加终态 **`TRANSLATION_NOT_APPLICABLE`**（视同完成）；`UNKNOWN` 引擎停 `WAITING_FOR_MTOOL_RULE` 等人工，**不判失败**。相关迁移见 §4 | §4 §7 |
| **U16** | I5：「A 永不写 `games\<dirname>\` 以外的路径，永不删除」 | **与分配给 A 的职责自相矛盾**：A 同时被要求「认领归位」（从 `_inbox\` 移文件）与「出对账报告」（写 `_reports\`）【起草轮四节独立提出】 | I5 改写为**枚举式白名单**（三处写集合，见 1.2.2），定案 **A 直接写对账报告**；A 仍不删除任何文件、不回收 `_stg\` | §2 §5 §8 |
| **U17** | 起草契约的停机态清单（15 个） | **两类真实失败没有落点**：① 路径预算超限且 dirname 已降级仍不够——人工动作是「改配置」而非「补规则」，塞进 `WAITING_FOR_RULE` 会误导人；② `events.jsonl` 不可解析若只当运行期状况，条目会**永久静默停住** | 新增 **`PATH_BUDGET_EXCEEDED`**、**`JOURNAL_CORRUPT`** 两个停机态（后者是落盘态）；`BLOCKED_RESOURCE` 覆盖面补全为「磁盘余量 / 体积与压缩比护栏 / 深度上限 / 路径穿越 / 产物含 reparse point、symlink、junction」 | §4 |
| **U18** | Defender 预检的时点未定，起草稿一度把它排在**落地并删源包之后** | 那样「A 级不删源包留证」结构上不可能成立，「只排 `_stg\` + `-DisableRemediation`」这套组合也失去支点（扫描对象必须还在 `_stg\`），并破坏 I1 | 预检**钉死在 `VERIFYING` 之内、落地（`staging.promoted`）之前**；`THREAT_*` 解除后回到 `VERIFYING` | §4 §6.9 §9 |
| **U19** | `retention.trash_days` 默认 `0`（两段式删除不启用），理由是 C4 要求自动删除、保留期与磁盘护栏打架 | **只对大包成立**：误判概率最高的是小包（压缩比 133x 的纯文本补丁、`EXTRACTED_NO_CONTENT` 边界），磁盘冲突只在大包上真实，且同卷移动是元数据操作 | 改为**尺寸门控的两段式删除**：`trash_days = 3` + 新增 `trash_max_bytes = 2 GiB`，小于阈值先入 `.gameflow\trash\`，其余直接删。C4 语义不变，见 1.2.4 | §3 §6.8 §12 |

> 上游**未被推翻**、本 SPEC 完整继承的判断（列出以免误读为「全盘改写」）：可重复执行 + 带人工确认点的半自动工作流；Windows 原生而非 WSL；PowerShell 负责稳定部分 / Codex 负责 GUI 部分；批次清单为核心、每游戏独立推进；密码只引用本机私有密码库；`.gameflow` 不存敏感数据；先阶段一再阶段二的落地顺序；以及第 1 轮合规边界的实质内容（约束保留，但按 1.5 改成项目自定而非「官方要求」）。


### 1.7 端到端范例：一个典型批次长什么样

这一节把 §1 的抽象说法落成一个具体场景。它就是用户 2026-09-14 描述的理想输入 / 输出，逐字对应到 SPEC 的字段与状态。

#### 1.7.1 你提供什么

| 你给的东西 | 落到哪个字段 | 备注 |
|---|---|---|
| 一组游戏的云盘分享链接 | `items[].cloud_links[].url` | 每个游戏可以有多个（主源 + 镜像），用 `role` 区分 |
| 每个链接的网盘提取码 | vault 的 `link:` 键 → `cloud_links[].code_ref` | **提取码永不内联进批次文件**（§3.2） |
| 每个游戏的解压密码 | vault 的 `site:` / `title:` / `item:` 键 → `password_ref` | 同一上传者多个等价密码 → 一个 key 下多个候选（§3.7.2） |
| 目标库根 | `library_root` | 必须是已登记库根之一；**批内统一**（§2.4.3 LR2） |
| 每个游戏的目录名 | `items[].title` → 由 §3.2A 推导出 `dirname` | 你只给标题，目录名算法自己推（带 ACP 可表示性检查） |
| 压缩包可能的后缀名 | `items[].extraction` | **绝大多数情况留空即可**——7-Zip 按内容嗅探（U2）。只有**分卷伪装**（一组文件要补回 `.001/.002/.003`）才必须显式声明 |
| 「已登录各网盘」 | 不是字段，是前置条件 | 见 §7.0 与各 netdisk profile 的 `needs_login` |

#### 1.7.2 你做什么（两分钟，每批一次）

1. 把网盘客户端的下载目录设成 `<library_root>\_inbox`（**只设一次**，不是每个游戏一次）。
2. 把本批次的游戏一次性转存 + 排进下载队列。
3. 走开。

#### 1.7.3 机器做什么

```
A（计划任务，每 10 分钟一轮，无人值守）
   扫 _inbox → G1–G6 判下载完成 → 认领归位到 games\<dirname>\
   → 7z l -slt 只读预检（不带密码）→ 卷集连续性 → 写状态 → 出对账报告

你（看报告，确认没有停机态需要处理）→ 跑一次 B

B（你或 Codex 触发，需要密码与提权）
   密码链 P0→P6（按 §3.7.4 的命中次数排序试）→ 7z t → 7z x 到 _stg
   → 递归解包 → 逐条目校验 → Defender 预检 → 落地 → D1–D12 → 删源包
```

#### 1.7.4 你拿到什么

| 你要的输出 | SPEC 里的形态 |
|---|---|
| 各游戏在目标路径下完成下载并解压 | 每条目状态 `COMPLETE`（或 `TRANSLATION_NOT_APPLICABLE`）；游戏本体在 `games\<dirname>\` |
| 压缩包已清除 | D1–D12 全过后删除；小于 `trash_max_bytes` 的先进 3 天 `trash\`（§6.8、§12.2） |
| 密码库更新了本次用到的解压密码 | 每个候选的 `hit_count` / `miss_count` / `last_used_at` 已更新（§3.7.4）；**新密码不会被自动写入**——vault 的写入永远是人的动作或 C 的 `confirmed:false` 候选 |
| 失败的游戏与其状态 | 对账报告的「⛔ 等你做事」区，按停机态分组，每组给出具体人工动作（§5.9、§4.3 停机态手册） |

#### 1.7.5 这个范例里**不需要**执行体 C

因为链接和密码都由你直接提供，`资源页解析`（§7.4）整个跳过；下载由你排队（§7.0）。**C 只在该游戏需要 MTool 翻译时才登场**，且那是一个独立的、可以推迟到任何时候的会话。

换句话说：**你描述的这个「第一阶段」= A + B + 两分钟人工，完全不占用前台桌面。**

---

---

## §2 总体架构：三执行体、部署形态、目录布局与 MAX_PATH 预算

本节定义 GameFlow 的**骨架**：谁在跑、谁写什么、装在哪、路径预算怎么算。本节不定义任何算法细节——下载完成判定在 §5.3，归档识别与只读预检在 §5.4/§5.6，密码链与解压递归、删除判据在 §6，状态机与不变量在 §4/§1.2.2，全部数据 schema 在 §3。本节只保证：读完这一节，能在一台空的 Windows 机器上把目录建对、把任务计划配对、并且知道任何一条路径会不会撞破 260。

---

### 2.1 三执行体职责矩阵

上游会话把项目切成「阶段一 = 后台 / 阶段二 = Computer Use」。调研证明**阶段一内部还必须再切一刀**，理由是两条互相独立的官方硬事实：

- 计划任务的 S4U 登录类型：「**no password is stored by the system and there is no access to either the network or encrypted files**」【官方】 <https://learn.microsoft.com/en-us/windows/win32/taskschd/principal-logontype> → 无人值守任务打不开 DPAPI 密码库。
- `MpCmdRun` **要求提权运行**【官方】 <https://learn.microsoft.com/en-us/defender-endpoint/command-line-arguments-microsoft-defender-antivirus> → 与 `-RunLevel Limited` 的受限计划任务直接冲突。

**需要密码的步骤和需要提权的步骤，都不能放进无人值守计划任务。** 于是阶段一拆成「巡检 A」与「作业 B」，阶段二保持为「前台 C」。A + B 合起来才是用户要求的 C1「阶段一（不使用 Computer Use，含人工环节）」；「阶段一需要人工做的一部分」由此有了确切定义 = **执行体 B 的触发与裁决**。

#### 2.1.1 速查表

| 代号 | 名称 | 触发方式 | 无人值守 | Computer Use | 提权 | 碰密码 | 有删除权 |
|---|---|---|---|---|---|---|---|
| **A** | 巡检 Sweep | Windows 任务计划，定时 | ✅ | ❌ | ❌ | ❌ | ❌ |
| **B** | 作业 Work | 人 / Codex 在交互会话触发 | ❌ | ❌ | ✅ | ✅ | ✅（唯一） |
| **C** | 前台 Foreground | 人显式触发，短会话 | ❌ | ✅ | ❌ | ✅（仅 P5 写回 vault，标 `confirmed:false`，§9） | ❌ |

#### 2.1.2 执行体 A · 巡检（`Invoke-Sweep.ps1`）

| 维度 | 定义 |
|---|---|
| **输入（只读）** | `batches\<batch-id>.json`；`_inbox\` 的目录项与文件元数据；`_inbox\claims.json`（人手写的认领补充映射，§5.2.2 M3）；`games\<dirname>\` 的文件元数据；`games\<dirname>\.gameflow\events.jsonl`；`rules\netdisk-profiles.json`、`rules\source-profiles.json`、`rules\container-types.json` |
| **输出（写）** | ① `games\<dirname>\.gameflow\events.jsonl`（**只追加**）② `games\<dirname>\.gameflow\state.json`（由 events 重建覆盖）③ `games\<dirname>\.gameflow\manifest.json` 的来源/文件快照字段 ④ `_reports\<batch-id>-<seq>.md`（**只新建，不覆盖既有报告**） |
| **副作用** | 唯一的破坏性文件操作是**认领归位**：把 `_inbox\<file>` **移动**到 `games\<dirname>\<file>`（同卷 rename，不是复制，不是删除）。除此之外 A 不改动任何已存在文件的字节。 |
| **前置条件** | `7z.exe` 在 PATH 且版本 ≥ 25.01（§9 Preflight 核验）；`D:\GameLib` 存在且各子目录可写；`_inbox`、`_stg`、`games` 同卷；批次 JSON 可解析。**不要求** vault 可解、**不要求**提权。 |
| **失败姿态** | **永不阻塞、永不删除、永不猜。** ① 抢**批次锁** `batches\<batch-id>.lock` 失败 → 运行期状况 `LOCKED_BY_ANOTHER_RUN`，**不落盘、不进状态机**（§4.6.4），直接退出等下一周期；抢不到某个**条目锁** `games\<dirname>\.gameflow\.lock` → 只跳过该条目，其余继续。② 单条目异常 → 该条目落对应停机态，其余条目继续推进（不变量 **I4**）。③ 无法认领的 `_inbox` 文件 → 原地不动，进报告的「未认领」清单。④ 整体异常 → 按 §8.2.3 的退出码退出 + Windows 事件日志一条，等下周期重来。 |
| **可并发** | **与自身不可并发**（`MultipleInstances = IgnoreNew`【官方】 <https://learn.microsoft.com/en-us/powershell/module/scheduledtasks/register-scheduledtask> ＋ 两层文件锁双保险）。**与 B 不可并发**（都写 `.gameflow\`；真正的互斥靠**条目锁**，两者必须抢同一个句柄，§4.6.1）。**与 C 的下载/浏览阶段可并发**；与 C 的状态写入阶段争条目锁，A 让路。 |

> A 的写集合 = **§1.2.2 不变量 I5 的枚举式白名单三处**：`games\<dirname>\`（含 `.gameflow\`）· **新建** `_reports\<batch-id>-<seq>.md`（不得覆盖已存在的报告）· 把 `_inbox\` 中**已认领**的文件移入前者。除此之外 A 不写、不删，**也不回收孤儿 `_stg\`**——只在报告里列出来，回收由 B 做（§6）。

#### 2.1.3 执行体 B · 作业（`Invoke-Work.ps1 -BatchId <slug>`）

| 维度 | 定义 |
|---|---|
| **输入（只读）** | `games\<dirname>\.gameflow\state.json`（条目必须已在 `PREFLIGHT_OK`）；源包本体；`%LOCALAPPDATA%\GameFlow\vault.clixml`（DPAPI）；`rules\` 下的 `container-types.json`、`source-profiles.json`、`engine-rules.json`、`sevenzip-warnings.json`、`defender-stdout.json`、`threat-allowlist.json` |
| **输出（写）** | ① `_stg\<8hex>\` 的解压产物（临时；分卷重建副本落**同级**的 `_stg\<8hex>-p\`，§6.5）② `games\<dirname>\` 的游戏本体 ③ `events.jsonl` 追加 ④ `manifest.json`（源包 SHA-256、逐条目对账结果、引擎判定、`stage1_snapshot`）⑤ `state.json` ⑥ `rules\threat-allowlist.json` 的追加（**唯一由脚本写的规则文件**，且只在人工放行后，§6.9） |
| **副作用** | ① **删除源包**——B 是三个执行体中**唯一**有删除权的，由 §6.8 的 D1–D12 判据 gate 住；小于 `retention.trash_max_bytes` 的源包先移入 `games\<dirname>\.gameflow\trash\`，`retention.trash_days` 后再由 B 清理（两段式删除的完整语义与默认值见 §3 与 §6.8，**该默认值仍待用户拍板，见 §12**）② `Add-MpPreference -ExclusionPath D:\GameLib\_stg\`（一次性、显式、写日志、附 `Remove-MpPreference` 回滚）③ 调用 `MpCmdRun -Scan -DisableRemediation`（提权）④ 进程命令行上短暂出现明文密码（`-p` 是 7-Zip 唯一可用的密码通道，同机进程可见，不可消除，见 §9） |
| **前置条件** | ① 会话**已提权**（UAC 在会话启动时确认一次，不是每条命令一次）② vault 的创建身份 == 当前运行身份（否则 DPAPI 解不开）③ `_stg` 与 `games` **同卷** ④ 磁盘余量过 §6.12 的空间护栏 ⑤ 目标路径长度已过 §2.5 的预算闸门 |
| **失败姿态** | **任一判据不满足 = 保留所有源包 + 落具名停机态**（不变量 **I1**）。密码尝试失败 → **清空 `_stg\<8hex>\`**（理由见 §6.3）。B 崩溃 → 留下孤儿 `_stg\<8hex>\`，由下一次 B 启动时按「attempt-id 不在任何 `state.json` 里」清理（§4.5.3）。**绝不**在失败后改写或删除原包。 |
| **可并发** | **与自身、与 A、与 C 均不可并发。** 同一条目同一时刻只允许一个写状态的执行体实例持有条目锁。抢锁失败 → 干净报错退出，让人重试（B 是人触发的，有人看着，不需要像 A 那样静默重排）。 |

#### 2.1.4 执行体 C · 前台（Computer Use，短会话）

| 维度 | 定义 |
|---|---|
| **输入（只读）** | `batches\<batch-id>.json` 的 `source_pages[]` / `cloud_links[]`；`rules\netdisk-profiles.json`；`rules\mtool-profiles\<item-id>.json`；各条目 `state.json`（挑 `READY_FOR_TRANSLATION` 的） |
| **输出（写）** | ① 下载文件落进 `_inbox\`——注意**不是 C 亲手写的**，是浏览器或网盘客户端写的，C 只是点了那个按钮 ② `events.jsonl` 追加（一律 `state.entered`，§3.3）③ `rules\mtool-profiles\<item-id>.json`（人工确认一次后写死）④ P5 从资源页抓到的密码候选写回 vault 并标 `confirmed:false`；**未确认的候选对 §6 等同不存在**（§9） |
| **副作用** | ① **独占前台鼠标键盘**（见 2.3）② MTool 往游戏根目录注入 `version.dll` 等文件 |
| **前置条件** | ① 目标条目已 `SCANNED`。按 §4 M09，`SCANNED` = Defender 裁决已落盘为放行 **且** 整树 `Unblock-File` 已完成 **且** `manifest.stage1_snapshot` 已写 —— 即 **MOTW 由 B 在上游解除，不是 C 的收尾**。这条不能省：带 MOTW 的 exe 首次运行会弹 SmartScreen 前台模态框，Computer Use 会在那里卡死【社区 + 待测 · §11：Windows 上实机确认弹框形态】；而解除动作本身安全——`Unblock-File` 官方明文幂等、对未标记文件无副作用【官方】 <https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.utility/unblock-file>。**MOTW 不得作为安全边界**（§9） ② 网盘已登录（登录、验证码、会员、人机验证一律由人接管）③ 游戏根目录已判定（= 含引擎标记文件的那一层，**不等于** `games\<dirname>\`，判定规则见 §7.6） |
| **失败姿态** | **随时可被人中断**（E1）。中断 = 直接退出，不留半状态：每个人工接力点前后各是一次完整的事件追加。歧义（多 exe、新引擎、界面变化）→ `WAITING_FOR_MTOOL_RULE`，**不猜**。C **不做**解压、不做改名、不做删除——这三件事在 B 手里。 |
| **可并发** | **与自身不可并发**（物理上只有一套鼠标键盘）。**与 B 不可并发**。**与 A 可并发**（A 无 GUI，不抢焦点），仅在写状态的瞬间与 A 争条目锁。 |

#### 2.1.5 读写集合总览（谁碰得到什么）

| 路径 | A | B | C | 说明 |
|---|---|---|---|---|
| `D:\GameFlow\scripts\`、`lib\` | R | R | R | 代码，运行期只读 |
| `D:\GameFlow\rules\*.json` | R | R | R | 规则，**只有人改**；两个例外：`threat-allowlist.json` 由 B 在人工放行后追加（§6.9），`mtool-profiles\<item-id>.json` 由 C 在人工确认后写死 |
| `D:\GameLib\batches\<id>.json` | R | R | R | 由 `New-Batch.ps1` 生成，运行期只读 |
| `D:\GameLib\batches\<id>.lock` | **RW** | **RW** | – | 批次锁（§4.6.1） |
| `D:\GameLib\_inbox\` | **R + 移出** | – | 间接写（浏览器落地） | A 是唯一的搬运工；`claims.json` 人写、A 只读 |
| `D:\GameLib\_stg\<8hex>\`、`…-p\` | – | **RW + 清空** | – | B 独占；A 只在报告里列孤儿 |
| `D:\GameLib\games\<dirname>\`（本体） | **W**（归位）| **RW + 删源包** | R + MTool 注入 | |
| `…\.gameflow\events.jsonl` | **A**ppend | **A**ppend | **A**ppend | 唯一真相（**I2**） |
| `…\.gameflow\state.json` | **W** | **W** | **W** | 派生快照，可删可重建 |
| `…\.gameflow\manifest.json` | W（部分字段） | **W** | – | |
| `…\.gameflow\.lock` | **RW** | **RW** | **RW** | 条目锁 = 真正的并发单位（**I4**） |
| `…\.gameflow\trash\` | – | **RW** | – | 两段式删除的暂存区（§6.8） |
| `D:\GameLib\_reports\` | **W**（新建） | – | – | |
| `%LOCALAPPDATA%\GameFlow\vault.clixml` | – | R | **RW** | A 永远碰不到 |

---

### 2.2 数据流与控制流

```
 ┌─ 阶段二 · C 前台（独占鼠标键盘）────────────────────────────────────────────────┐
 │  资源页 URL / 网盘链接 ─解析标题·密码提示·网盘链接→ 「保存到我的网盘」→ 点下载     │
 │      读 rules\netdisk-profiles.json     ← 登录 / 验证码 / 会员 / 人机验证由人接管   │
 └──────┬──────────────────────────────────────────────────────────────────────────┘
        │  浏览器或网盘客户端把字节写进那个**全局的**下载目录（见 2.7）
        ▼
   ╔══════════════════════════════════════════════════════════════════════════════╗
   ║  D:\GameLib\_inbox\                                                          ║
   ║   写者：Codex 内置浏览器 / 网盘桌面客户端（不受我们控制）                        ║
   ║   读者：A（唯一）      A 只往外搬，从不往里放                                   ║
   ╚══════┬═══════════════════════════════════════════════════════════════════════╝
          │  Windows 任务计划，每 10 分钟一次，扫一遍就退出
          ▼
 ┌─ 阶段一 · A 巡检（无 GUI，不抢焦点，用户可正常用机）───────────────────────────────┐
 │  ① 稳定性判定  G1∧G2∧G3∧G4∧G5∧G6      就地做，绝不移动一个还在写的文件（§5.3）    │
 │  ② 认领        按批次清单匹配到**唯一**条目；匹配不唯一 → 不动，进报告（§5.2）     │
 │  ③ 归位        move  _inbox\x  →  games\<dirname>\x      ◀── C3 从这一刻起成立   │
 │  ④ 落状态      append state.entered(DOWNLOAD_STABLE) → .gameflow\events.jsonl   │
 │  ⑤ 只读预检    7z l -slt（`-TypeSpec none`，不带密码，§6.2.1/§6.2.7）：卷集连续性 ·│
 │                条目清单 · 路径穿越 · 体积/压缩比预算 · 容器类型 · 路径闸门（§2.5） │
 │                              →  PREFLIGHT_OK   或   具名停机态                   │
 │  ⑥ 出报告      新建 _reports\<batch-id>-<seq>.md                                │
 └──────┬──────────────────────────────────────────────────────────────────────────┘
        │  人读报告，决定这一轮跑哪个批次；缺密码的先补进 vault
        ▼
 ┌─ 阶段一 · B 作业（已提权的交互式会话；机器很忙，但屏幕还是用户的）─────────────────┐
 │  ⑦⑧ 密码链 P0→P6（§6.3，读 vault.clixml）→ 每个源包单独一次 7z t，exit==0（§6.2）│
 │  ⑨⑩ 解压 7z x（`-TypeSpec *`，每次只开一层）→ _stg\<8hex>\；白名单容器 + 内容嗅探 │
 │       depth ≤ 5；逐条目 exists + size 对账（§6.4）                                │
 │  ⑪ Defender    MpCmdRun -Scan -DisableRemediation（提权）→ A/B/C 三级处置        │
 │                **必须在 ⑫ 之前**：扫描对象还在 _stg\ 里（§6.9 / §9.8）           │
 │  ⑫ 落地        _stg\<8hex>\  →  games\<dirname>\      同卷移动，体积一致          │
 │  ⑬ 删源包      D1–D12 全部为真才删；任一为假 → 保留所有包 + 停机态（§6.8，I1）     │
 │  ⑭ 解 MOTW + 写清单   整树 Unblock-File · manifest.stage1_snapshot  →  SCANNED    │
 └──────┬──────────────────────────────────────────────────────────────────────────┘
        │  人显式触发（E1：这一段时间用户不能用这台电脑）
        ▼
 ┌─ 阶段二 · C 前台（独占鼠标键盘）────────────────────────────────────────────────┐
 │  ⑮ 定位游戏根目录（含引擎标记文件的那一层，§7.6）→ MTool 加载翻译                  │
 │  ⑯ 人确认加载结果 → COMPLETE / MTOOL_FAILED / WAITING_FOR_MTOOL_RULE             │
 └─────────────────────────────────────────────────────────────────────────────────┘
```

图中四条**不可逾越的线**：

1. **`_inbox` 的字节不是我们写的。** 我们只负责判断它写完了没有，然后把它搬走。任何「我们知道下载器在干什么」的假设都会被 §5.3 的六种机理击穿。
2. **`_stg` 只有 B 能进。** A 连读都不读它（孤儿目录只按名字列进报告），C 完全不知道它存在。
3. **删除只发生在 ⑬ 这一格。** 上游的每一格都可以重跑，⑬ 之后不可撤销（C4，判据见 §6.8）。
4. **Defender 预检（⑪）必须早于落地（⑫）。** 这是契约，不是顺手排的：只有产物还在 `_stg\` 里时，「排除 `_stg` 让解压不被打断」+「`-DisableRemediation` 忽略排除目录、做一次显式全量扫描」这套组合才成立（论证见 2.6）；反过来排，`THREAT_*` 硬停时源包已经在 ⑬ 被删掉了，「留证」不可能成立，直接破坏 I1。对应的迁移表口径见 §4（M08/M09、H20/H21）。

---

### 2.3 部署形态（E1 主力机约束下的具体安排）

E1 是硬约束：**这是用户的主力机，会被打断**。这一条把「两阶段拆分」从架构偏好变成了可用性要求。

#### 2.3.1 A：Windows 任务计划，每 10 分钟

**为什么是 Windows 任务计划而不是 Codex 桌面 App 的 Scheduled tasks**（本 SPEC 定案）：

| 维度 | Windows 任务计划 | Codex Scheduled tasks |
|---|---|---|
| 依赖面与沙盒 | 用户登录即可；无沙盒 | 开机 **+ 桌面 App 常驻** + 项目在盘上；`workspace-write` 下写不到 project 外，还要额外配 `writable_roots`【官方】 |
| E1 韧性 | 用户关不掉 | 主力机上桌面 App 随时可能被关 |

**为什么不是常驻守护进程**：Codex 在 Windows 上 `features.unified_exec` **默认关闭**——官方配置参考对该 feature 的逐字描述是「Use the unified PTY-backed exec tool (stable; **enabled by default except on Windows**)」【官方 config-reference】。「起一个长跑进程、后续 turn 回来 poll」这条路在 Windows 上不是默认能力；hook 也不行，官方明写会话结束时「cancels unfinished background hooks and discards output」【官方 hooks】。→ **A 必须是无状态、可重跑、扫一遍就退出的批处理**，绝不设计成守着 `_inbox` 轮询的长跑进程。

##### G2 的采样模型（本节与 §5 的统一契约）

G2 需要「跨 ≥ `sample_count` 次采样、总时长 ≥ `quiet_window_seconds`」的观测。本 SPEC 采用**混合模型**：

- **`_inbox` 里尚未认领的文件：只能做运行内采样。** 这不是偏好，是**结构性限制**——未认领的文件没有 `item_id`（§3.3 的事件公共字段必填），也没有任何一个 I5 白名单允许 A 写入的目录，**A 无处存放它们的跨运行采样点**。于是一轮巡检内连做 `sample_count` 次快照，间隔 `sample_interval_seconds`。
- **已认领（已归位到 `games\<dirname>\`）的条目：本轮采样 ∪ 最近 N 条 `download.gate` 事件。** 这些条目有 `.gameflow\`，采样点作为 `download.gate` 事件落进 journal（§3.3），G2 因此能看到跨运行的更长历史，抗「限速长停顿」的能力比纯运行内强一个量级。
- **求值口径以 §5.3.3 为准**（跨度怎么数、N 取多少、mtime 与 size 双静默的细节）；本节只定模型与参数来源。参数全部登记在 `rules\netdisk-profiles.json` 与 §3 的 `guards`（**默认值的唯一权威是 §3**）：`sample_count = 3`、`sample_interval_seconds = 40`、`quiet_window_seconds = 120`。

> 起草轮那句「**跨运行采样是本节定下的契约**、否决运行内采样」**作废**：它在本 SPEC 里结构上不可实现（理由同上第一条）。凡按旧契约写的下游文字一律以本小节为准。

**间隔 = 10 分钟**，理由是四条约束的交集：

| 约束 | 方向 | 数值含义 |
|---|---|---|
| 一轮采样自己要花 `sample_count × sample_interval_seconds` | 间隔必须远大于采样耗时 | 3 × 40 = 120 秒；10 分钟 = 5× 余量 |
| `IgnoreNew` 会**静默丢弃**重叠触发 | 间隔应大于单轮实际耗时 | 单轮 ≈ 120 秒采样 + 每包一次 `7z l -slt`；**本轮无实测**，见下方【待测】 |
| 限速停顿最长 6 秒就能骗过 3×1s 的判据【实测·§5.3】 | 采样间距要远大于停顿尺度 | 40 秒 ≈ 6.7× 余量；120 秒窗口 ≈ 20× |
| 用户感知延迟 | 间隔越小越好 | 下载完成到 `DOWNLOAD_STABLE` 最坏 ≈ 10 分钟 + 120 秒；相对于几 GB 的下载本身是噪声 |

**三个时间参数必须一起看**（三者不同源，混用会得出相反结论）：

| 参数 | 值 | 作用 |
|---|---|---|
| 巡检间隔 | **10 分钟** | 任务计划的触发周期 |
| `max_run_seconds` | **900** | **软预算**：A 自己在超出时收尾退出（§5.1.2）。A 无状态，丢掉的只有本轮采样 |
| `ExecutionTimeLimit` | **30 分钟** | **硬断路器**：软预算失灵（卡在某个 IO 上）时由任务计划杀掉，不是正常路径 |

注意 `max_run_seconds`(900) **大于**间隔(600)：**重叠触发会发生，并被 `IgnoreNew` 静默丢弃**。这在本设计里是可接受的——① A 无状态可重跑，被丢弃的那次触发没有任何数据损失；② 真正的互斥由两层文件锁保证（§4.6.1），`IgnoreNew` 只是第一道。但它留下一个必须实测的后果：

> **【待测 · §11】单次巡检的实际耗时**（满库、真实包数量下，一轮从启动到退出的墙钟时间）。
> **不实测会怎样**：若常态耗时 > 600 秒，后续触发会被 `IgnoreNew` **静默丢弃**【官方 Register-ScheduledTask】——没有错误、没有事件日志、没有任何提示，实际巡检周期悄悄退化成 900+ 秒，用户看到的只是「报告怎么更新得这么慢」。**这是本节最难发现的一类故障**，也是唯一需要靠实测才能确认间隔选对了的一条。

配套设置（细则在 §8.8.10）：`LogonType Interactive`（S4U 不可用，见 2.1）· `RunLevel Limited` · `MultipleInstances IgnoreNew` · `-NoProfile -NonInteractive` · `ExecutionTimeLimit` 30 分钟。

#### 2.3.2 B：人 / Codex 在已提权的交互会话触发

- 入口固定为 `Invoke-Work.ps1 -BatchId <slug>`（完整参数清单是 §8.2.3 的契约表，本节不重列）。**参数只接收无空格无歧义的 token**——Codex 的 Windows 原生 agent 跑的是 `powershell.exe -NoProfile`，即 Windows PowerShell 5.1，5.1 仍把参数拼成一个字符串【官方 + 仲裁】。一切路径由脚本自己从配置解析，绝不从命令行接收自由格式的路径或标题。
- **提权在会话级完成一次**（以管理员身份启动那个 PowerShell 窗口 / 那个 Codex 终端），不是每条 `MpCmdRun` 弹一次 UAC。这是 B 不能进计划任务的直接后果，也是「阶段一含人工环节」的具体所指。
- B 是**长跑但可重入**的：解压 10 GB 包要几分钟到几十分钟，中途被杀留下的孤儿 `_stg\<8hex>\` 由下次启动清理（§4.5.3），已落盘的 `events.jsonl` 决定从哪继续。

#### 2.3.3 C：人显式触发、短会话、可中断续跑

- 单位是「批次的一个片段」，不是整个批次：做完一个网盘、或做完一个游戏的 MTool 加载，就是一个可以停下的点；每个人工接力点前后各追加一次事件 → 中断后重跑不会重做已完成的部分。
- MTool 幂等：进入前若游戏根目录已存在 `version.dll` → 判定已处理，**不重复注入**。

#### 2.3.4 「A 在跑的时候能用电脑，C 在跑的时候不能」

这不是程度差别，是**机制差别**：

| | A（巡检） | B（作业） | C（前台） |
|---|---|---|---|
| 占用什么 | 磁盘 IO + 少量 CPU | 大量 CPU + 磁盘 IO + 磁盘空间 | **鼠标、键盘、活动桌面** |
| 有没有窗口 | 无（`-NonInteractive`，不创建可见窗口） | 有一个终端窗口，用户可以最小化 | 有，而且它在替用户点 |
| 用户能同时用机吗 | **能**，通常察觉不到 | **能**，但机器会明显变慢 | **不能** |
| 依据 | 无 GUI 调用，不抢焦点【设计】 | 纯 CLI【设计】 | Computer Use 在 Windows 上**独占前台**、无法与用户共用同一会话，「锁屏继续」是 macOS 专属【官方】 <https://learn.chatgpt.com/zh-Hans/docs/computer-use> |

**给用户的操作契约**：A 装完就忘了它（它每 10 分钟醒一次，报告攒在 `_reports\`）；B 想起来就跑一次（要盯着，但可以边跑边干别的）；C 是**要腾出一段时间的事**，像跑一次备份，跑之前先把手上的活停了。C 的每一次会话都应该短到「去倒杯水回来就差不多了」，长了就该拆。

---

### 2.4 完整目录布局

```
D:\GameFlow\                        # 代码与规则，进 Git
  scripts\
    Preflight.ps1                   # 环境探测（§9）
    Invoke-Sweep.ps1                # 执行体 A
    Invoke-Work.ps1                 # 执行体 B
    New-Batch.ps1                   # 建批次（dirname 生成算法 = §3.2A）
    Get-BatchReport.ps1             # 重新生成 / 回填对账报告
    lib\                            # 共享模块（含唯一的 7z 封装，§6.2.1）
  rules\
    source-profiles.json            # 来源站点/上传者 → 密码引用、已知伪装、预期层数、
                                    #   filename_patterns[]、密码抽取正则（§6.3.2 引用此处）
    netdisk-profiles.json           # 三类网盘的临时物模式、页面规则、采样参数
    container-types.json            # 递归解包白名单 + 扩展名排除表（键名见 §3.6.3）
    engine-rules.json               # 引擎特征 → 判据
    sevenzip-warnings.json          # 7-Zip 警告文本的分类表（§6.2.3）
    defender-stdout.json            # MpCmdRun stdout 的解析规则（§6.9.3）
    threat-allowlist.json           # 人工放行过的 SHA-256（§6.9.4）；唯一由 B 追加的规则文件
    mtool-profiles\<item-id>.json
  templates\batch.template.json
  tests\fixtures\
  .agents\skills\                   # Codex 的 5 个固定操作
  AGENTS.md
  gameflow.config.json              # 本机配置：已登记库根集合（§2.4.3）。进 Git 但
                                    #   每台机器的值不同 —— 视为机器级配置，改它要走 §12

D:\GameLib\                         # 已登记库根之一。根名压到 10 字符是 MAX_PATH 预算需要
                                    #   下面这整棵子树在**每个**库根里各有一份
  _inbox\                           # 唯一下载落地目录
    claims.json                     # 人手写的认领补充映射，A 只读（§5.2.2 M3）
  _stg\<8hex>\                      # 库级短中转区，必须与游戏库同卷；产物直接落这里
  _stg\<8hex>-p\                    # 分卷重建副本（与上者**同级**，§6.5）
  _reports\<batch-id>-<seq>.md
  batches\<batch-id>.json
  batches\<batch-id>.lock           # 批次锁（§4.6.1）
  games\<dirname>\
    <原始压缩包…>                    # C3：源包与本体同目录
    <游戏本体…>
    .gameflow\
      events.jsonl                  # append-only，唯一真相
      state.json                    # 派生快照，可从 events.jsonl 完全重建
      manifest.json                 # 来源、SHA-256、引擎判定、stage1_snapshot
      trash\                        # 两段式删除的暂存（小包先移进来，§6.8）
      .lock                         # 条目锁 = 真正的并发单位（§4.6.1）

%LOCALAPPDATA%\GameFlow\vault.clixml   # DPAPI 密码库，绑定运行身份，不在库内、不在 Git 内
```

> 测试库根由 `-Profile test` 从配置解析（§8.2.3），**不从命令行接收路径**；它必须与生产库根**等长**（10 字符），否则 2.5 的预算表在测试环境里不等价，测出来的闸门触发频率没有意义。

#### 2.4.1 逐目录的存在理由

| 路径 | 为什么它必须单独存在 |
|---|---|
| `D:\GameFlow\` 与 `D:\GameLib\` 分离 | 代码进版本控制、数据不进（2.8）；沙盒后果见 2.8.4 |
| `.agents\skills\` | Codex 的 5 个固定操作。**`.codex\` 不是 skills 目录**——它是本地环境配置，actions 跑在集成终端而非 agent 沙盒，权限模型不同【官方】 |
| `scripts\lib\` | A/B 共用同一套落盘协议、锁、编码约定、7z 调用封装（§6.2.1 / §8）。两份实现迟早会漂移，漂移点恰好是删除判据。 |
| `rules\` 与 `scripts\` 分离 | 规则是**人改的静态声明**，脚本是执行器。用户品味明确要求「规则显式声明、阈值写死可调」——把 `container-types.json` 塞进代码里就意味着改规则要改代码。唯一的写例外是 `threat-allowlist.json`（B 在人工放行后追加），它因此也是 Git 里第一个要人工复核的文件。 |
| `rules\mtool-profiles\<item-id>.json` | MTool 的「选哪个 exe」**不能写成自动规则**（论坛真实反例里正确答案是 `開始游戏.exe`，选 `Game.exe` 会卡加载界面）【社区】。所以每个游戏一个文件，人工确认一次后固化。 |
| `_inbox\` | 见 2.7。Codex 内置浏览器没有「按任务的下载目录」这个能力。`claims.json` 放在这里而不是 `rules\`，因为它是**这一批落地文件**的补充映射，不是跨批次的长期规则。 |
| `_stg\<8hex>\` 与 `_stg\<8hex>-p\` | 见 2.6。库级、短、同卷。`<8hex>` 就是 attempt-id，孤儿目录靠「不在任何 state.json 里」识别并清理。分卷重建副本用**同级**的 `-p` 目录而不是子目录，是为了不给通道一再加前缀（算术见 2.5.4）。 |
| `_reports\<batch-id>-<seq>.md` | `<seq>` 单调递增、**只新建不覆盖**：报告是给人看的历史，覆盖掉上一次就没法回答「上周那次为什么停在缺密码」。用户品味的「呈现层不吝啬」落在这里。**只存批次对账报告**——Preflight 输出、标定记录这类不属于任何批次的产物落 `%LOCALAPPDATA%\GameFlow\`（§9.11.3），不进 `_reports\`。 |
| `games\<dirname>\` 里源包与本体同级 | C3 的直接落实（批次目录本身是 C2 的工作单位，单游戏 = 批次大小 1）。代价是游戏目录里会有一段时间同时躺着 `.part1.rar` 和 `Game.exe`——这是用户明确要的形态，不是疏漏。 |
| `.gameflow\` 里的三件套 + `trash\` | `events.jsonl` 是唯一真相（**I2**），`state.json` 是随时可删可重建的派生物，`manifest.json` 是给人和给下游用的静态快照。三者分离的价值在崩溃恢复：只要 `events.jsonl` 完整，另外两个都能重算。`trash\` 与它们放在一起，是因为两段式删除的暂存必须与它的账本同卷、同生命周期（§6.8）。 |
| vault **不在库内** | ① 库不进 Git 但会被用户备份/搬盘，密码库跟着走是净风险 ② DPAPI 绑定**运行身份**，跟着文件搬到别的机器也解不开，放在 `%LOCALAPPDATA%` 才符合它的语义 ③ A 根本没有读它的需要，物理隔离比约定更可靠 |

#### 2.4.2 为什么库根叫 `D:\GameLib` 而不是 `D:\GameLibrary`

先把账算清楚，别夸大：`D:\GameLibrary\games\` = 21 字符，`D:\GameLib\games\` = 17 字符，**省了 4 个字符，就这么多**。它不是这套预算里最重要的一步（最重要的是 2.6 的 `_stg` 搬家，省 96 字符；其次是目录名降级，省 46 字符），但它入选有三条理由：

1. **它是白拿的。** 换个目录名的成本是零，而在最紧的那一档（日文长标题、`dirname` 顶到 60 字符）上，可用内部路径从 158 涨到 162——2.5% 的余量，代价为零的东西没有理由不拿。
2. **它把库根压进了「≤20 字符」的调研建议线内**【调研 windows-download §6.3】，而且压得很宽裕（10 字符），给未来加一级子目录留了口子。
3. **它没有短到伤可读性。** 调研原文建议的 `D:\GL` 再省 5 个字符，但这个名字要出现在资源管理器地址栏、对账报告、Defender 排除项确认、以及用户三个月后回来找「那个游戏库在哪」的时刻。**`GameLib` 是「够短且仍然自解释」的那个点。**

> 库根是**配置项**，不是硬编码，硬约束只有两条：① 长度 ≤ 20 字符 ② 与 `_stg` 同卷，由 Preflight 核验。**但库根不得放在 OneDrive 同步目录或 `Program Files` 下**——前者有社区证据导致 MTool inject 失败【社区】，后者写不进 `version.dll`【社区】。

---

#### 2.4.3 多库根：允许多个，但一个批次只能落在一个里

用户可以登记多个库根（例如按盘、按题材分）。`gameflow.config.json`：

```json
{
  "schema_version": 1,
  "library_roots": [
    { "id": "main", "path": "D:\\GameLib", "_note": "默认" },
    { "id": "adv",  "path": "E:\\GameAdv" },
    { "id": "test", "path": "D:\\GameTst", "test_only": true }
  ],
  "default_library_root": "main"
}
```

**四条规则**：

| # | 规则 | 为什么 |
|---|---|---|
| **LR1** | 批次的 `library_root` 必须**精确匹配**（大小写不敏感）已登记集合里的某个 `path`。不做路径规范化后的等价判断（不解析 `..`、不跟随 junction、不补全 8.3 短名） | 「看起来等价」的两个路径在 NTFS 上可能不是同一个目录。宁可要求逐字相同，也不要一个会在 junction 上出错的智能匹配 |
| **LR2** | **一个批次只能落在一个库根里。** 要跨库根就拆成两个批次 | `_stg` 必须与游戏库**同卷**（`File.Replace` 跨卷抛异常，§8.8），而 `_stg` 是库根下的目录。批内混用库根 = 批内混用卷 = 落地移动随机退化成「复制+删除」并可能中途失败 |
| **LR3** | **每个库根各有一份完整子树**（`_inbox` / `_stg` / `_reports` / `batches` / `games`）。它们之间没有任何共享状态 | 共享 `_inbox` 会让认领归位变成跨卷移动（见 LR2）。共享 `batches` 会让批次锁的作用域含糊 |
| **LR4** | **每个库根都要独立满足**：MAX_PATH 预算（§2.5，根名长度直接进预算表）、同卷约束、Defender 排除项（`<root>\_stg` 各排一条）、NTFS（§9 的 MOTW 逻辑） | 这四条是按库根算的，不是按机器算的。`Preflight.ps1` 必须**逐个库根**核验并分别报告（§9.11） |

> `library_roots[].path` 的根名长度**不要求相同**，但每个都必须自己通过 §2.5 的预算闸门。根名长一个字符，那个库根留给游戏内部路径的余量就少一个字符——`E:\GameAdv`（10 字符）与 `D:\GameLib`（10 字符）等价，而 `E:\MyGameLibrary`（16 字符）会让该库根的降级触发得更频繁。

### 2.5 MAX_PATH 预算实算

#### 2.5.1 天花板到底是几

三个数，别混：

| 数值 | 含义 | 来源 |
|---|---|---|
| **260** / **259** | `MAX_PATH`（**含结尾 NUL**）/ 文件路径实际可用字符数 | 【官方】「the maximum length for a path is MAX_PATH, which is defined as 260 characters … the maximum path on drive D is `D:\<some 256-character path string><NUL>`」<https://learn.microsoft.com/en-us/windows/win32/fileio/naming-a-file> |
| **248** | **建目录的实际上限** | 【官方】「When using an API to create a directory, the specified path cannot be so long that you cannot append an 8.3 file name (that is, **the directory name cannot exceed MAX_PATH minus 12**)」同上 |

**本 SPEC 一律用 248 当硬天花板**，不用 259。理由很直白：解压出来的每一个文件都有一个必须先被创建的父目录，最深的那个父目录才是真正会撞墙的东西。用 259 去算，会在「文件名放得下、但装它的目录建不出来」这个位置炸掉，而且报的错是 7-Zip 的「拒绝访问 / 找不到路径」，极难归因。

**闸门取 240**，即在 248 之下再留 8 个字符。这 8 个字符**没有一手依据**，是工程余量：吸收落盘协议的 `.tmp-<attemptId>` 尾巴、吸收 `.NNN` 分卷重建时的名字变化、吸收我们自己算错一两个字符的风险，并且 240 是个能口算的整数。【设计取值，非技术断言】

#### 2.5.2 为什么不去赌长路径开关

`LongPathsEnabled` 需要**两个条件同时满足**：注册表 `HKLM\SYSTEM\CurrentControlSet\Control\FileSystem\LongPathsEnabled = 1` **且**应用的 manifest 声明 `longPathAware`；而且注册表值「will be cached by the system (per process) after the first call … **will not be reloaded during the lifetime of the process** … a reboot might be required」【官方】 <https://learn.microsoft.com/en-us/windows/win32/fileio/maximum-file-path-limitation>。

社区证据显示 `longPathAware` 存在于 Windows PowerShell 5.1 的 `powershell.exe`，但 pwsh 7.2.2 当时没有；长路径上 `Remove-Item` 抛 `Win32Exception` code 3【社区】。.NET Framework 4.6.2+ 还需要 AppContext 开关 `Switch.System.IO.UseLegacyPathHandling=false` / `BlockLongPaths=false`，5.1 是否默认设好**无法在 macOS 上核实**【待测 · §11】。**⇒ 从源头压短路径，不赌开关。** `LongPathsEnabled` 的实际值由 Preflight 探测并写进 `state.json` 当上下文（解释失败用），**不作为放宽预算的依据**。

#### 2.5.3 逐段字符数（算术，可完全外推）

以下全部是纯字符计数，与操作系统无关，macOS 上算的和 Windows 上算的一模一样【实测·算术，可外推到 Windows】。

| 片段 | 字面 | 字符数 | 累计 |
|---|---|---|---|
| 库根 | `D:\GameLib` | 10 | 10 |
| ＋ `games\` | `D:\GameLib\games\` | +7 | **17** |
| ＋ 游戏目录名 `<dirname>` | 变长 `N` | +N | 17+N |
| ＋ 分隔符 | `\` | +1 | **18+N** |
| ＋ `.gameflow\` | `…\<dirname>\.gameflow\` | +10 | 28+N |
| 中转区（**绝对路径，与 dirname 无关**） | `D:\GameLib\_stg\<8hex>\` | — | **25** |
| 中转区·分卷重建副本 | `D:\GameLib\_stg\<8hex>-p\` | — | **27** |
| 落地目录（`_inbox`） | `D:\GameLib\_inbox\` | — | 18 |

#### 2.5.4 两条通道的余量

设 `E` = 归档内部最长条目路径（含内部所有目录层级和文件名）。

```
通道一 · 解压期（staging）
    25  +  E   ≤ 240      ⇒   E ≤ 215
    分卷重建副本：27 + E ≤ 240   ⇒   E ≤ 213

通道二 · 落地后（games）  ◀── 这一条才是紧的
    18  +  N  +  E   ≤ 240      ⇒   E ≤ 222 − N
```

| `dirname` 形态 | `N` | 落地前缀 `18+N` | 可用内部路径 `E` |
|---|---|---|---|
| 规范化标题，顶到上限 | 60 | 78 | **162** |
| 规范化标题，典型日文长度 | 24 | 42 | **198** |
| ASCII slug 降级 `g0042-<8hex>` | 14 | 32 | **208** |

**这张表给出三条契约：**

- **C-PATH-1**：路径长度预检必须按**落地路径**算（`18+N+E`），不能按 staging 路径算。staging 反而是最宽松的那一条（215，即使是 `-p` 副本也有 213，**都宽于通道二在最好情况下的 208**），拿它做闸门等于没做。产物直接落 `_stg\<8hex>\`、不再有工作树子目录，这两个数才成立。
- **C-PATH-2**：`E > 222 − N` 时，第一动作是**把 `dirname` 降级成 ASCII slug**（这是 §3.2A 已经规定的降级触发条件之一，本节只是给出它的算术判据），降级把 `N` 从最坏 60 压到 14，多出 46 字符。降级原因必须记录，真实标题写进 `manifest.json`。
- **C-PATH-3**：降级后仍 `E > 208` → 落停机态 **`PATH_BUDGET_EXCEEDED`**（§4.3）。它与 `WAITING_FOR_RULE` 分开是因为人工动作不同：这里要人**改配置**（换更短的库根 / 人工指定更短的目录名），而不是**补一条规则**。

#### 2.5.5 游戏内部路径能有多长

**本轮没有实测样本。** macOS 上没有 Windows 游戏本体，六份调研里也没有人量过一个真实 RPG Maker / Unity 包的最长内部路径。下面给的是**结构长度**，即「还没算文件名就已经花掉的字符」，来源是引擎特征表【调研 mtool §引擎判定表，官方/文档级】：

| 引擎 | 典型内部目录前缀 | 前缀字符数 | 备注 |
|---|---|---|---|
| RPG Maker MV/MZ | `www/data/` · `www/audio/bgm/` · `www/img/characters/` | 9 / 14 / 19 | 素材名常含日文，可以很长 |
| Unity | `<Name>_Data/` · `<Name>_Data/StreamingAssets/` | len(Name)+6 / +22 | Addressables 之下还有更深的层级 |
| Wolf RPG · KiriKiri | `Data/` · `data.xp3` | 5 / 8 | KiriKiri 是单文件封装，路径压力最小 |

再叠加一条**已知会额外多一层**的事实：解压常多出一层 `GameName/GameName_v1.02/`【契约 §7.6 / 调研 mtool】。这一层按游戏名长度算，很容易是 25~50 字符，**直接从 `E` 里扣**。

一个可以照着核对的最坏组合（**估算，不是实测**）：

```
落地前缀（N=60）                        78
＋ 解压多出的一层 GameName_v1.02\        26
＋ Unity: <Name>_Data\StreamingAssets\   40   （Name=18）
＋ 再一层资源分类目录                     20
＋ 文件名（日文素材名）                    40
                                       ────
                                        204   ← 过 240 闸门，但只剩 36 字符余量
换成上游的 staging 通道（121 前缀，见 2.6）：247   ← 破 240 闸门，逼近 248 建目录上限
```

**⇒ 结论**：`_stg` 搬到库级之后，最坏组合还有 36 字符余量；不搬，同一个包在解压过程中就会撞墙。**这不是理论风险，是把已知的结构长度加起来就得到的数。**

> **【待测 · §11】** 真实 RPG Maker MV / Unity 游戏包的**实测最长内部条目路径**（用 `7z l -slt` 取 `Path` 字段的最大长度，三五个真实包即可）。
> **不实测会怎样**：`E ≤ 222 − N` 这条闸门的**触发频率完全未知**。如果真实素材的 `E` 中位数是 80，闸门几乎永不触发，降级机制形同虚设也无所谓；如果中位数是 180，那么**多数日文长标题游戏都会被强制降级成 ASCII slug**，用户会看到一库的 `g0042-a1b2c3d4` 而不是游戏名——那属于设计失败，需要重新考虑库根是不是该压到 `D:\GL`。这个数字直接决定用户体验，必须实测。

---

### 2.6 为什么 `_stg` 在库级而不在游戏目录下

上游设计是 `games\<dirname>\.gameflow\staging\<attempt-id>\`；本 SPEC 改成 `D:\GameLib\_stg\<8hex>\`。三条独立理由，任何一条单独成立都足够。

**理由一 · 路径预算：省 96 字符。** 上游那一段是 `.gameflow\`(10) + `staging\`(8) + `<attempt-id>\`(21) = **39 字符**：

```
上游 staging 绝对前缀 = 21 (D:\GameLibrary\games\) + N + 1 + 39 = 61 + N   →  N=60 时 121，余量 240−121 = 119
本设计 staging 绝对前缀 = 25（与 N 完全无关）                               →  余量 215，省 96
```

库级 staging 的前缀**与游戏目录名完全无关**，这一点很关键：解压是路径压力最大的时刻（内部嵌套还没被摊平），恰好在这一刻我们把前缀从 `61+N` 压到了 25。119 这个数字在真实素材上是会破的——见 2.5.5 的最坏组合。

**理由二 · Defender 排除项从「每游戏一条通配」简化成「一条固定路径」。**

解压过程会被实时保护打断：官方明写「If real-time protection is turned on, **files are scanned before they're accessed and executed**」且「Scan archive files」默认启用【官方】 <https://learn.microsoft.com/en-us/defender-endpoint/configure-advanced-scan-types-microsoft-defender-antivirus>。命中签名时 Defender 会在 7-Zip 还在写的时候隔离文件，7-Zip 那边表现为「拒绝访问 / 找不到文件 / CRC 失败」这类**误导性错误**【社区】。所以中转区必须排除。

| | 游戏目录下的 staging | 库级 `_stg` |
|---|---|---|
| 排除项写法与数量 | `D:\GameLib\games\*\.gameflow\staging\`，**依赖通配语义**（`*` 能否跨/只跨一层，本轮无一手证据【待测 · §11】）；退化方案是每游戏 1 条、随库线性增长，且通配项的 `-CheckExclusion` 结果对未来新建的目录不具代表性 | `D:\GameLib\_stg\`，不依赖通配，**恒定 1 条**，`MpCmdRun -CheckExclusion -Path D:\GameLib\_stg\` 一次说清【官方】 |
| 回滚与安全面 | 通配项误删风险高，且可能意外覆盖 `games\` 下别的东西 | 一条 `Remove-MpPreference`；**精确到一个只存在几分钟的临时目录** |

注意这条排除**不会削弱预检**：`MpCmdRun -Scan -DisableRemediation` 官方明写会**忽略排除目录**【官方】——所以「排除 `_stg` 让解压不被打断」和「移入 `games\` 前对 `_stg` 做一次显式全量扫描」这两件事可以同时成立，**这正是整套组合的支点，也是 2.2 第 4 条线（Defender 必须早于落地）的理由**。`games\` 本身**绝不排除**，实时保护完整保留。三级处置的细节归 §6.9 / §9.8。

**理由三 · `File.Replace` 跨卷抛异常，所以必须同卷。**

官方原文：「The backup file, replaced file, and replacement file **must all reside on the same volume**」【官方】 <https://learn.microsoft.com/en-us/windows/win32/api/winbase/nf-winbase-replacefilew>；.NET 侧同约束 <https://learn.microsoft.com/en-us/dotnet/api/system.io.file.replace>。本项目的落盘协议（写 tmp → `Flush($true)` → `File.Replace(tmp, dst, dst.bak)`，**保留 backup 仅作诊断**，见 §8）因此强制要求 `.gameflow\` 与它的 tmp 同卷；`_stg\<8hex>\` → `games\<dirname>\` 的移动也必须同卷，否则会退化成「跨卷复制 + 删除」——耗时按 GB 计、失败中间态多一倍、删除那一步一旦失败就留下双份数据。把 `_stg` 钉在库根下，**同卷性变成结构保证而不是运维约定**：Preflight 对 `D:\GameLib` 做一次 `Get-Volume` 就同时覆盖 `_inbox`、`_stg`、`games`、`batches`、`_reports` 五个目录；staging 若分散在每个游戏目录下，这个假设**会随时间悄悄失效**。

> `_stg\<8hex>\` 的 `<8hex>` 就是 attempt-id（格式见 §4.4.3）。B 崩溃留下的孤儿目录，判据是「这个 8hex 不出现在任何 `games\*\.gameflow\state.json` 里」——确定性判据，不做时间猜测、不做「超过 N 小时就算陈旧」。

---

### 2.7 `_inbox` 的存在理由与认领归位

#### 2.7.1 这个能力不存在

上游设计里有一句「Codex 将下载目标定到 `games\<游戏名>\`」。**这个能力不存在。**

官方 `docs/browser`（zh-Hans）原文：「浏览器下载的文件默认保存在系统的『下载』文件夹中。在**设置 > 浏览器**中，您可以选择其他下载位置、将下载位置恢复为系统默认位置，或开启**询问下载文件的保存位置**。」【官方】 <https://learn.chatgpt.com/zh-Hans/docs/browser> 在 112 KB 的官方全量配置键参考里搜下载目录相关键，**零命中**（`download_dir` / `download_path` / `downloads_dir` 全部 0）；所有 `download` 相关的键都是 `allow | deny` 权限开关，不是路径【实测·对官方文档全文做的 grep，与运行平台无关，可完全外推】。

⇒ 实际能力是：**一个全局的、在 GUI 里设的、对所有任务共享的下载目录**，或者「每次下载都弹框问存哪」（那就不是自动了）。三条可行替代里只有一条是后台友好的：**把全局下载目录固定到 `D:\GameLib\_inbox`，让执行体 A 按批次清单把文件归位。** 另外两条（每次弹保存框由 C 手打路径 / 走网盘桌面客户端）都把落地这件事拽回前台，与 E1 冲突。

#### 2.7.2 认领归位（概念，细节在 §5.2）

`_inbox` 是一个**混杂的、无结构的、我们不控制写入者的**目录：一个批次的十几个游戏、几十个分卷、加上用户自己随手下的东西，全都堆在一起。「认领归位」就是把这堆东西**确定性地**分派回各自的游戏目录。

```
_inbox\  ──►  ① 稳定性判定（G1..G6，就地做，绝不移动还在写的文件；§5.3）
              ② 认领（M0→M3，§5.2.2；_inbox\claims.json 是人手写的补充映射）
                     ├─ 恰好匹配 1 个条目 → 认领成功
                     └─ 匹配 0 个 / ≥2 个 → 不动，进报告的「未认领」/「歧义」清单
              ③ 归位：move  _inbox\x  →  games\<dirname>\x   （同卷 rename）
              ④ 落 state.entered(DOWNLOAD_STABLE)
```

三条设计约束，全部服务于「确定性、人在环内」：

1. **匹配规则由批次清单显式声明**，不做模糊匹配、不做相似度打分、不从文件名反推游戏名。匹配不到就是匹配不到，进报告等人。
2. **必须先稳定、后认领。** 顺序反过来会搬走一个还在写的文件——那是不可恢复的错误（下载器会继续往原路径写，产生第二个残缺文件）。
3. **分卷整组认领。** 卷集按命名分组后作为一个原子单位归位，不允许一半在 `_inbox` 一半在 `games\`。

#### 2.7.3 C3 是终态保证，不是落地瞬间的保证

用户约束 C3 的原文是「原始压缩包与游戏本体在同一个文件夹下」。由于 2.7.1 的能力缺失，**这个约束在文件落地的那一瞬间无法成立**——它落在 `_inbox`。本 SPEC 给出 C3 的精确措辞：

> **C3（精确措辞）**：对任一批次条目，**从它进入 `DOWNLOAD_STABLE` 那一刻起**，直到其源包按 §6.8 的 D1–D12 判据被删除为止，该条目的**全部**源包都位于 `games\<dirname>\` 之内。
>
> 在 `DOWNLOAD_STABLE` 之前，源包可能位于 `_inbox\`。此时条目仍停在 `AWAITING_DOWNLOAD`，**不产生任何状态迁移**——即「文件躺在 `_inbox` 里」这件事在状态机上是**不可见**的，它只出现在对账报告里。

这条措辞把「归位」焊进了状态定义：**`DOWNLOAD_STABLE` = 稳定 且 已归位**。于是 C3 变成一个可以直接断言的不变量——凡是状态 ≥ `DOWNLOAD_STABLE` 的条目，去 `games\<dirname>\` 一定能找到它的源包。这也让「未认领」成为一个**永远不会静默通过**的状态：文件再稳定，认领不了就永远推不到 `DOWNLOAD_STABLE`，永远出现在报告的异常清单里。（两段式删除会让源包在删除前先移进 `.gameflow\trash\`，仍在 `games\<dirname>\` 之内，C3 不受影响；语义见 §6.8。）

---

### 2.8 与 Git 的边界

#### 2.8.1 两棵树

| | `D:\GameFlow\`（脚本、共享模块、规则 JSON、批次模板、fixture、`AGENTS.md`、`.agents\skills\`） | `D:\GameLib\`（游戏本体、源包、状态、日志、报告、批次实例） |
|---|---|---|
| 进 Git | **是**——这些是**可重建的意图**，要 diff、要回滚、要在两台机器之间同步 | **否**——体积按几十 GB 计；`events.jsonl` 高频追加；内容是二进制；且真实游戏标题本身是隐私 |

**`D:\GameLib` 不得作为 Git 工作区**——不是「不建议」，是不得。一次误 `git add .` 就会把几十 GB 二进制塞进 `.git`，而且 `games\<dirname>\` 里的目录名就是游戏标题。

#### 2.8.2 `.gameflow\` 的禁列表

`.gameflow\` 只存**状态、日志与待清理的源包**（`trash\`）。以下三类**绝不写入**：**密码明文**（只能出现 `password_ref` 这类引用名与「已尝试 N 个候选、全败」这类计数与结论）、**网盘账号 / cookie / session**（登录态归 Codex 内置浏览器的独立 profile 管，我们既不读也不存【官方：内置浏览器使用独立配置方案】）、**带 token 的下载 URL**（批次清单里存资源页 URL 与分享链接，不存换取出来的临时直链）。密码一律只存在 `%LOCALAPPDATA%\GameFlow\vault.clixml`（DPAPI，绑定运行身份），键空间与写入规则见 §3.7 与 §9。

对应地，**报告 `_reports\*.md` 会包含**游戏标题、绝对路径、威胁名、失败原因——它是给人看的，就应该包含这些。SPEC **不做**自动脱敏：把报告发给别人之前的脱敏责任在人，这是一条**诚实局限**（§9），不是待办。

#### 2.8.3 进 Git 的东西里也有雷

1. **`rules\source-profiles.json` 存的是 `password_ref`，不是密码。** 它可以写「来源站点 X 的上传者 Y 用的是密码库里的 `ref-0017`」，**不能**写那个密码本身；`filename_patterns[]` 与密码抽取正则进 Git，抽出来的**值**不进。
2. **`rules\threat-allowlist.json` 含 SHA-256 与放行理由**，是唯一由脚本（B）追加、也是唯一记录「人放行过一个 Defender 命中」的地方。进 Git 是故意的（要可追溯），但每次 diff 都应当被人读一遍。
3. **`rules\mtool-profiles\<item-id>.json` 进 Git 是故意的**——它是规则不是运行数据，但含游戏标题与 exe 名。缓解：文件名用 `item-id`（受限字符集 slug）而不是标题；若仓库要公开，这个目录是第一个要检查的地方。
4. **`.gitignore` 必须包含**（最小集）：`vault*`、`*.clixml`、`*.local.json`、`tests/fixtures/*.bin` 之类真实样本包。测试 fixture 用**自己造的**小包。

#### 2.8.4 与 Codex 沙盒的交叉

`D:\GameFlow` 是 Codex 的 project root，`D:\GameLib` 在 project 之外 → 必须配 `sandbox_workspace_write.writable_roots`，否则 B 的第一次写文件就失败【官方：键存在于 config-reference；**实际边界属 §11 待测**】。官方另有一条：`<writable_root>/.codex` 在作为目录存在时会被**递归置为只读**【官方 config-reference】——我们的状态目录叫 `.gameflow`，正好不撞。这条不是巧合，是选名字时就避开的。


---

## §3 数据模型：批次、状态、事件、清单、规则、密码库

本节是 GameFlow 的**数据契约唯一权威**：`events.jsonl` 的事件 schema、`state.json` 与 `manifest.json` 的字段、批次清单字段、所有 `rules\*.json` 的键名与默认值，一律以本节为准。§4/§5/§6/§7/§8 不再自列 schema，只保留各自的**语义与判定顺序**。凡本节写死的字段名，实现时**不得增删或改名**；确有需要走 §12 决策台账。

反过来，下列内容**不在本节**，本节只引用不重复：

| 内容 | 权威节 |
|---|---|
| 7-Zip 调用封装（`-TypeSpec` 白名单、超时、stdin 重定向） | §6.2.1 |
| 删除判据 D1–D12 的逐条定义 | §6.8 |
| 归档魔数表（含偏移） | §5.4.4 |
| 不进自动流程的形态（NSIS / Inno / setup.exe / 镜像 / 未知 MZ） | §6.13 |
| 护栏阈值的**语义**与求值时机（默认值仍登记在本节 §3.2.3） | §6.12 |
| 状态名、停机态手册、迁移编号 `M01`/`H07`/`R02` | §4 |
| 不变量 I1–I5 | §1 |
| PowerShell 工程约定（cmdlet 编码差异、传参、互斥） | §8.8 |

阅读约定：JSON 示例中 `//` 开头的是**本文档的讲解注释，不是文件内容**——真实文件里不允许出现 `//`（§3.1）。示例里的中文注释在落地文件中一律改写成 `"_note"` 字段。

### 3.0 六类文件与它们的真相层级

```
                    rules\*.json                 %LOCALAPPDATA%\GameFlow\vault.clixml
                 （人是唯一写者，只读消费）        （DPAPI，绑定运行身份，库外）
                          │                                   │
  batches\<batch-id>.json │                                   │
  （人建；C 回写 expected）│                                   │
          │               │                                   │
          └───────────────┼───────────────────────────────────┘
                          ▼
        ┌────────────────────────────────────────────────────────────┐
        │   执行体 A 巡检   ·   执行体 B 作业   ·   执行体 C 前台       │
        └────────────────────────────────────────────────────────────┘
                          │  append-only，整行一次写
                          ▼
   games\<dirname>\.gameflow\events.jsonl   ◀── 唯一真相（不变量 I2）
                          │  fold（按文件行序，不按 seq 排序）
                          ▼
   games\<dirname>\.gameflow\state.json     ◀── 派生快照，任何时候可删可重建
                          │                       │
                          │                       └──▶ 删除源包的 gate（不变量 I3）
                          ▼
   games\<dirname>\.gameflow\manifest.json  ◀── 来源 / SHA-256 / 引擎判定 / 阶段一文件快照
                          │
                          ▼
   _reports\<batch-id>-<seq>.md             ◀── 纯派生物，随时可重新生成
```

| 档 | 文件 | 丢了会怎样 |
|---|---|---|
| **真相** | `events.jsonl` | 该条目的历史不可恢复；`state.json` 在时可继续跑，但失去审计能力 |
| **派生** | `state.json`、`_reports\*.md` | 重建即可，不是事故 |
| **档案** | `manifest.json` | 无法确认重下的包是不是同一个；不影响流程推进 |
| **输入** | `batches\*.json`、`rules\*.json`、`vault.clixml` | 流水线停在对应停机态等人补 |

---

### 3.1 JSON 与序列化硬规则

#### 3.1.1 为什么是 JSON

三条决定性理由，按硬度排序：

1. **零依赖是硬约束。** PowerShell 5.1 与 7.x 都内置 `ConvertTo-Json`/`ConvertFrom-Json`【官方】；YAML 需 PSGallery 第三方模块 `powershell-yaml`【官方】。装模块这件事落在「任务计划以某个身份反复触发」的路径上——模块搜索路径、`-Scope` 安装位置、PSGallery 信任状态都随运行身份变化，是典型的「手动跑好好的、计划任务跑不起来」来源。
2. **YAML 的隐式标量转型与「确定性、静态」正面冲突**【社区】（`Yes`/`No`/`On`/`Off` → bool，`0755` 视实现），而清单里最多的内容恰是最容易触发它的中日文标题（`：` `:` `#` `- ` 都会改变解析结果）。
3. **PSD1 被「只导入前 500 个键值对」直接否决**【官方 `Import-PowerShellDataFile`】：一批 30 个游戏 × 约 12 字段 ≈ 360 键，加嵌套就顶到天花板；且只有 PowerShell 读得懂，而 Codex 是本项目的一等公民（§8）。

代价与补偿：不能写注释 → **人类注释一律走 `"_note"`（`string` 或 `string[]`），禁止 `//` 与 `/* */`**（5.1 的 `ConvertFrom-Json` 可能容忍，`Test-Json` 会拒【社区】）；人不手写结构（`New-Batch.ps1` 生成骨架）；多行文本用字符串数组；无锚点 → 用 `defaults` + §3.2.4 的显式浅合并。

#### 3.1.2 序列化硬规则（全项目强制，违反即缺陷）

| # | 规则 | 依据 |
|---|---|---|
| J1 | **所有 `ConvertTo-Json` 必须显式写 `-Depth 10`**，禁止裸调用 | 默认 `-Depth 2`【官方】 https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.utility/convertto-json ；本项目清单第 4 层（`items[].extraction.volume_rebuild.members`）就会被静默截断 |
| J2 | 数组字段写出前强制 `@(...)` 包裹；读回后一律 `@($obj.items)` 归一化 | `-AsArray` 是 7.x 才有【官方】；单元素数组在 5.1 下会退化成对象，`.items.Count` 变成属性数 |
| J3 | **所有时间戳自己格式化成字符串**：`[DateTimeOffset]::UtcNow.ToString('o')`，绝不把 `[datetime]` 对象交给 `ConvertTo-Json` | 5.1 用 JavaScriptSerializer 输出 `"\/Date(…)\/"`，7.x 用 Json.NET 输出 ISO【官方】——5.1 写的文件 7.x 读不回来 |
| J4 | 所有 id 类字段是**显式生成的受限字符集 slug**，不用原始标题 | `ConvertFrom-Json` 不加 `-AsHashtable` 时把大小写不同的键当同一个键并**静默只留最后一个**；空字符串键**直接抛错**【官方】 |
| J5 | 大整数一律 `[long]`（Int64），不碰 BigInteger | `ConvertTo-Json` 序列化 BigInteger 是 7.5 才支持【官方】；Int64 上限约 9.2×10^18 字节，远超任何硬盘 |
| J6 | **写后立刻回读并比对关键字段**（`schema_version` + 顶层键集合 + 本次改动的字段） | 「超 `-Depth` 是否告警」在 5.1 文档里自相矛盾且无定论；即便告警，`-NonInteractive` 计划任务里没人看得见 Warning 流 |
| J7 | 每个文件顶层必须有 `"schema_version": 1`（整数） | 格式一定会演进，重放与迁移逻辑需要按版本分支。**`rules\mtool-profiles\<item-id>.json` 同样适用**（§7.6 的 `"schema"` 拼写不成立） |

---

### 3.2 批次清单 `D:\GameLib\batches\<batch-id>.json`

#### 3.2.1 `batch_id` 与 `item.id` 格式规范（全 SPEC 唯一定义）

```
batch_id 正则：^[0-9]{4}-[0-9]{2}-[0-9]{2}-[a-z0-9](?:[a-z0-9-]{0,23}[a-z0-9])?$
形状：YYYY-MM-DD-<slug>    例：2026-09-12-vn-a    长度 11..35
约束：batch_id 必须与文件名去掉 .json 后**字节级相等**

item.id 正则：^[a-z0-9](?:[a-z0-9-]{0,30}[a-z0-9])?$          长度 1..32
生成规则：  "g" + <4 位十进制序号> + "-" + <ASCII slug>        例：g0042-nekopara
序号来源：  持 batches\ 目录独占句柄，扫描库内已存在的全部 item.id，取最大序号 +1
唯一性：    **库内全局唯一**，不是批次内唯一
```

字符集这么窄不是洁癖：Codex 的 Windows 原生 agent 跑 `powershell.exe -NoProfile`（即 5.1），**5.1 会把参数数组重新拼成一个字符串再交给外部程序**【官方】 https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.core/about/about_parsing 。所以脚本入口只接收 `-BatchId <slug>` 这类无空格无歧义 token（§8.2.2），一切路径由脚本自己从配置解析。§8.2.2 的入口校验与 §10 的测试批次名**直接引用上面两条正则，不得另立**（测试批次名形如 `2026-01-01-t-m1`）。

`item.id` 必须全局唯一的理由是硬的：`rules\mtool-profiles\<item-id>.json` 是一个**全局命名空间**（§3.6.4、§7.6），两个批次里出现同名 id 会互相覆盖对方的 MTool 规则。`New-Batch.ps1` 发现 id 已存在时**直接报错退出，不自动改名**。

#### 3.2.2 标题与目录名

| 字段 | 语义 | 谁写 |
|---|---|---|
| `id` | 上面的 slug。是 `mtool-profiles` 文件名、事件 `item_id`、报告行首标识 | `New-Batch.ps1` |
| `title` | **真实标题**，可含日文/中文/全角。NFC 规范化后存储，**不做 NFKC** | 人 / C |
| `dirname` | 落地目录名，即 `games\<dirname>\` 那一段。由 **§3.2A** 的算法产出 | `New-Batch.ps1` |
| `dirname_degraded` | 对象，记录是否降级成 ASCII slug 及其原因 | `New-Batch.ps1` |

```jsonc
"dirname_degraded": {
  "degraded": true,
  "reason": "acp_unrepresentable",   // none | acp_unrepresentable | path_budget | collision | empty_after_normalize
  "acp_at_decision": 936,            // 做判定时的系统 ACP（§9.11.2 PF-08），用于事后解释
  "normalized_candidate": "ネコぱら"  // 降级前的规范化结果，仅供人读
}
```

- `degraded=false` 时 `reason` 必须是 `"none"`；`reason` 是封闭枚举，不是自由文本。**无论走哪一支都必须记录**（§3.2A 的硬要求）。
- 「非 ASCII 会让老引擎起不来」这个说法的真实条件是「路径含当前系统 ANSI 代码页无法表示的字符」——老引擎（KiriKiri / NScripter / RM2000–VX）用 ANSI API 打开自己的资源，日文 Windows 上日文路径完全正常【社区】。所以判据是 ACP 往返，不是「是否 ASCII」。
- **不得断言目标机 ACP = 936**：用户的 Windows 区域设置未知。ACP 由 Preflight 探测（§9.11.2 PF-08）并写进 `state.json.env`。【待测】→ §11：不实测就只能保守地一律降级成 slug，代价是全库目录名不可读。

> ⚠ `dirname` 必须在建批次时算出，因此 **`New-Batch.ps1` 依赖 Preflight 已跑过并留下 ACP**。Preflight 未跑时 `New-Batch.ps1` 报错退出，不允许把 `dirname` 留成 `null`。

#### 3.2.3 完整 schema（带讲解注释的示例）

```jsonc
{
  "schema_version": 1,
  "batch_id": "2026-09-12-vn-a",
  "created_at": "2026-09-12T03:11:07.4210000Z",   // J3：字符串，自己格式化
  "created_by": "human",                           // human | codex
  "library_root": "D:\\GameLib",                   // 与本机配置不符时 A/B 拒绝处理整个批次
  "_note": "本批 3 个条目，来自同一资源站的三个上传者",

  // ── defaults：按 §3.2.4 的规则继承给每个 item ──────────────────
  "defaults": {
    "extraction": {
      "max_depth": 5,
      "prefix_scan_mb": 0,                         // 取值域 {0, 16, 64}；0 = 不传 -t（§6.2.1 的 none）
      "volume_rebuild": { "enabled": false }
    },
    "translation": { "mode": "mtool", "engine": null, "target_exe": null },

    // guards：护栏的**默认值登记处**；语义与求值时机见 §6.12
    "guards": {
      "ratio_warn": 200,
      "ratio_block": 1000,
      "ratio_block_min_declared_bytes": 5368709120,      // 5 GiB
      "max_total_uncompressed_bytes": 128849018880,      // 120 GiB
      "max_single_file_bytes": 42949672960,              // 40 GiB
      "max_entries_per_archive": 200000,
      "min_free_headroom_bytes": 2147483648,             // 2 GiB
      "space_safety_factor": 1.15,
      "poll_sec": 5,
      "runtime_factor": 3,
      "abs_cap_bytes": 128849018880,                     // 默认取 max_total_uncompressed_bytes
      "hdr_enc_factor": 20,
      "max_run_seconds": 900
    },

    // retention：两段式删除（§6.8.4 执行；仍列为 §12 待用户拍板项）
    "retention": { "trash_days": 3, "trash_max_bytes": 2147483648 }
  },

  // ── items ────────────────────────────────────────────────────
  "items": [
    {
      "id": "g0042-nekopara",
      "title": "ネコぱら Vol.1",
      "dirname": "ネコぱら Vol.1",
      "dirname_degraded": { "degraded": false, "reason": "none", "acp_at_decision": 932,
                            "normalized_candidate": "ネコぱら Vol.1" },

      "source": {
        "site": "example-site.org",                // 归一化为小写主机名，不带协议、不带路径
        "uploader": "ookkgg",                      // 原样保留，可含中文；source-profiles 的复合主键之一
        "pages": ["https://example-site.org/archives/12345"]   // 公开资源页，不含 token
      },

      // 网盘分享链接。**提取码/访问码永不内联**，只放 vault 键名（code_ref）
      "cloud_links": [
        { "netdisk": "baidu", "url": "https://pan.baidu.com/s/xxxxxxxx",
          "code_ref": "link:example-site.org|g0042-nekopara|1", "role": "primary",
          "transferred_at": "2026-09-12T03:40:11.0000000Z",   // ↓ 四个字段由 C 写，§7.1.5 的重入凭据
          "dispatched": true, "channel": "desktop_client", "note": null },
        { "netdisk": "quark", "url": "https://pan.quark.cn/s/yyyyyyyy",
          "code_ref": null, "role": "mirror",
          "transferred_at": null, "dispatched": false, "channel": null, "note": null }
      ],

      // ★ 由执行体 C 从资源页/网盘列表页写回。这是「缺分卷」唯一可靠的自动检测点
      "expected_parts": 3,
      "expected_files": [
        { "name": "ネコぱら.mp4.001", "bytes": 2147483648, "role": "archive", "part_index": 1 },
        { "name": "ネコぱら.mp4.002", "bytes": 2147483648, "role": "archive", "part_index": 2 },
        { "name": "ネコぱら.mp4.003", "bytes": 738197504,  "role": "archive", "part_index": 3 }
      ],
      "expected_source": "netdisk_listing",        // netdisk_listing | resource_page | human
      "expected_written_by": "C",
      "expected_written_at": "2026-09-12T04:02:55.0000000Z",

      "password_ref": "site:example-site.org|ookkgg",   // §3.7.2；"none" = 明确无密码

      "extraction": {
        "max_depth": 5,
        "prefix_scan_mb": 0,
        "volume_rebuild": {
          "enabled": false, "base_name": null, "members": [],
          "_note": "只有落 WAITING_FOR_RULE 且人工确认后才置 enabled=true"
        }
      },

      "translation": {
        "mode": "mtool",                           // mtool | none
        "engine": null,                            // null = 交给 §7.6 判定；非 null = 人工钉死
        "target_exe": null,                        // 相对「游戏根目录」，非 games\<dirname>\
        "profile_ref": "mtool-profiles\\g0042-nekopara.json"
      },

      "abandoned": false,                          // 见 §3.2.5：批次清单里**没有**运行期状态字段
      "_note": "站点公告说要改 2 次后缀解压 2 次；按 §6 主流程不需要改名，先直接解"
    }
  ]
}
```

#### 3.2.4 `defaults` 合并规则（写死，不得聪明）

```
Merge(defaults, item) -> effective_item:
    for key in ("extraction", "translation", "guards", "retention"):
        effective[key] = Copy(defaults[key])
        for k, v in item[key]:  effective[key][k] = v          # 逐键覆盖，v 为 null 也算覆盖
    for key in item 的其余顶层键:
        effective[key] = item[key]                             # 整体覆盖，不合并
    # 数组永不合并：item 给了 members/pages/cloud_links 就整体替换 defaults 的同名数组
```

只有一层对象参与键级合并，**嵌套对象（如 `extraction.volume_rebuild`）整体覆盖**。这是刻意的：多层深合并会让「某个值到底从哪来」变成要在脑子里跑算法才能回答的问题。`Get-BatchReport.ps1` 在报告里**打印合并后的有效值**，人不需要自己合并。

#### 3.2.5 字段表

**批次级**

| 字段 | 类型 | 必填 | 取值域 / 约束 |
|---|---|---|---|
| `schema_version` | int | ✅ | `1` |
| `batch_id` | string | ✅ | §3.2.1 正则；必须等于文件名去 `.json` |
| `created_at` | string | ✅ | ISO 8601 `o` 格式，UTC |
| `created_by` | string | ✅ | `human` \| `codex` |
| `library_root` | string | ✅ | 绝对路径，**必须是本机 `gameflow.config.json` 已登记库根之一**（精确匹配，大小写不敏感，不做路径规范化后的等价判断）；不在集合里时 A/B **拒绝处理整个批次**并在报告里报错。**一个批次只能落在一个库根里**（批内统一），跨库根请拆成两个批次 |
| `defaults` | object | ✅ | 可以是 `{}`，但键必须存在 |
| `items` | array | ✅ | 长度 ≥ 1；`id` 在数组内唯一 |
| `_note` | string \| string[] | ❌ | |

**条目级**（★ = 由执行体 C 写回，其余由人经 `New-Batch.ps1` 写）

| 字段 | 类型 | 必填 | 取值域 / 约束 |
|---|---|---|---|
| `id` | string | ✅ | §3.2.1 正则，库内全局唯一 |
| `title` | string | ✅ | NFC；可含任意 Unicode；**不做 NFKC** |
| `dirname` | string | ✅ | §3.2A 产出；**大小写不敏感**地在库内唯一 |
| `dirname_degraded` | object | ✅ | `{degraded, reason, acp_at_decision, normalized_candidate}` |
| `source.site` | string | ✅ | 小写主机名 |
| `source.uploader` | string \| null | ✅ | 可为 null（站点无上传者概念时） |
| `source.pages` | string[] | ✅ | 可为空数组 |
| `cloud_links[]` | array | ✅ | 每项 `{netdisk, url, code_ref, role, transferred_at★, dispatched★, channel★, note★}` |
| `cloud_links[].netdisk` | string | ✅ | 必须是 `netdisk-profiles.json` 已登记的 id（§3.6.2）；未登记 → `WAITING_FOR_RULE`，**不猜** |
| `cloud_links[].code_ref` | string \| null | ✅ | **只放 vault 键名**（`link:` 键，§3.7.2）。**提取码永不内联**——这是硬规则，§7.4.1 抓到提取码时写 vault 并回填键名 |
| `cloud_links[].role` | string | ✅ | `primary` \| `mirror` |
| `cloud_links[].transferred_at` ★ | string \| null | ✅ | 转存**成功确认后立即**写；§7.1.5 的重入凭据，**报告生成器不得改写或补齐** |
| `cloud_links[].dispatched` ★ | bool | ✅ | 下载已排进队列 |
| `cloud_links[].channel` ★ | string \| null | ✅ | `desktop_client` \| `browser`；取自 §3.6.2 的 `download_channel`，**不在运行期自适应切换** |
| `cloud_links[].note` ★ | string \| null | ✅ | C 写的机器备注（人的注释走 `_note`） |
| `expected_parts` ★ | int \| null | ✅ | **≥ 1 或 null**。**明确无分卷写 `1`，不写 `0`**（`0` 不是合法值） |
| `expected_files[]` ★ | array | ✅ | 每项 `{name, bytes, role, part_index}`；`role` ∈ `archive`\|`patch`\|`readme`；`bytes` 可为 null；`part_index` 为 1 基整数或 null，**冗余于数组序，两者冲突以 `part_index` 为准** |
| `expected_source` ★ | string \| null | ✅ | `expected_files` 非空时必填 |
| `expected_written_by` / `expected_written_at` ★ | string \| null | ✅ | C 写回时填 |
| `password_ref` | string \| null | ✅ | vault 键名，或字面量 `"none"`，或 null |
| `extraction.max_depth` | int | ✅ | 1..10，默认 5 |
| `extraction.prefix_scan_mb` | int | ✅ | **{0, 16, 64}**（与 §6.2.1 的 `-TypeSpec` 白名单 `*:s{N}m`, N ∈ {16,64} 对齐） |
| `extraction.volume_rebuild` | object | ✅ | 见 §3.2.6 |
| `translation.mode` | string | ✅ | `mtool` \| `none` |
| `translation.engine` | string \| null | ✅ | null = 交给 §7.6 判定 |
| `translation.target_exe` | string \| null | ✅ | 相对游戏根目录 |
| `translation.profile_ref` | string \| null | ✅ | `mtool-profiles\<item-id>.json` |
| `abandoned` | bool | ✅ | `true` = 人工放弃该条目（见下） |
| `_note` | string \| string[] | ❌ | |

**关于 `expected_parts` / `expected_files`（本节最重要的一条）**

「A 完成、B 还没开始下」是最隐蔽的失败模式：目录里现存文件全都稳定，尺寸、mtime、独占打开、归档头自校验**全部通过**，但整组卷根本没下全。这个失败模式在本地**没有任何可观测信号**——只有资源页/网盘列表页知道一共应该有几个文件。因此：

- `expected_parts` 是 `DOWNLOAD_STABLE` 判据 **G6**（§5.3）与删除判据 **D11**（§6.8）的输入。
- **执行体 C 必须在网盘转存/下载阶段把它写回批次清单**，这是 C 的一项硬职责（§7）。
- `expected_parts = null` 时，G6 只做「卷集命名连续 + 除末卷外等大」，D11 真空成立，且 `Get-BatchReport.ps1` **必须在该条目行上打出「无预期卷数，缺分卷不可自动检测」**。留白是允许的，装作没有这个洞不允许。

**批次清单里没有运行期状态字段（硬规则）**

条目级只有 `abandoned: true|false`，**没有 `state`**。运行期状态的唯一来源是 `events.jsonl` → `state.json`（不变量 I2）。

> **硬规则**：`Invoke-Sweep.ps1` / `Invoke-Work.ps1` / C 的 worklist 选择器一律从 `games\<dirname>\.gameflow\state.json`（或 `Replay(events.jsonl)`）取状态；批次字段只用于 `abandoned` 过滤（§5.1.2、§7.2.1 照此实现）。理由有三条：三份状态副本必然漂移，漂移之后没人知道该信哪份；执行体 A 受 I5 约束、根本没有回写批次文件的权限；**只要字段还在，早晚会有人把运行期状态写回去**——所以字段直接不给。
> `abandoned=true` 与状态机的 `ABANDONED` 终态是两回事：前者是人在清单上的声明（输入），后者由 `state.entered` 事件落盘（真相）。`Invoke-Work.ps1 -Abandon` 两者都写（§4.8）。

#### 3.2.6 `extraction.volume_rebuild`：唯一的受控改名规则

调研裁定：7-Zip 默认按内容嗅探，伪装扩展名、前置垃圾字节、图种、SFX **全部无需改名**即可 `l`/`t`/`x`【官方 type.htm + 实测 macOS 25.01，机制在公共层，可外推 Windows】。**唯一的硬例外是分卷**：伪装命名的分卷组 `7z t` exit 2 完全打不开，只改首卷也不行，必须整组补齐 `.NNN`；而 `.NNN` 之前的扩展名无所谓（`game.mp4.001` 正常工作）【实测 macOS，命名匹配逻辑在公共层，可外推】。

```jsonc
"volume_rebuild": {
  "enabled": true,
  "base_name": "g0042.bin",          // 重建后的基名；扩展名无所谓，默认 "<item.id>.bin"
  "members": [                        // **有序**：数组第 0 项 → .001，第 1 项 → .002 …
    "ネコぱら.mp4", "ネコぱら (1).mp4", "ネコぱら (2).mp4"
  ],
  "_note": "人工按站点公告确认的顺序；A/B 绝不自己推断顺序"
}
```

1. **默认 `enabled=false`。** 置 true 只能由人在落 `WAITING_FOR_RULE` 之后手工完成（§4.3）。
2. `members` 是**人工声明的有序列表**，执行体不得按大小/时间/名称自行排序——顺序错一位整组就解不开，而 7-Zip 给的错误（`Unexpected end of archive`）与「真的缺卷」一字不差【实测】。
3. **原始文件永不改名。** 重建在 `_stg\<8hex>-p\` 里对**副本**做（§6.5）。这条让「绝不把未知文件批量改成 .zip/.rar」成为结构性保证，而不是靠纪律。
4. 重建后的组用 `base_name` + `.001/.002/...`，与 `members` 一一对应；对应关系写进 `archive.identified` 事件的 `payload`，并落 `manifest.archives[].rebuilt_as`。

`prefix_scan_mb` 的语义：`0` = 走 §6.2.1 的 `-TypeSpec none`（不传 `-t`，用 7-Zip 默认 `-t*:r` + 8 MB 签名扫描窗口），失败后由 §6 做**一次**固定的 `-t*:s64m` 重试；`16`/`64` = 强制传 `-t*:s<N>m` 并**关闭**那次重试。**永远不允许出现具体类型名**（`-t7z`/`-tzip`/`-trar`）——给具体类型会关闭嗅探，把本来自动就能解开的包变成 exit 2 硬失败【实测 macOS，可外推】。调用形状与白名单的权威在 §6.2.1，本节只登记取值域。

---

### 3.2A `dirname` 生成算法（全 SPEC 唯一权威）

`dirname` 是**批次清单字段**，由 `New-Batch.ps1` 产出，既不属于 A 也不属于 B；A/B/C 一律只读，**任何执行体都不得改名已存在的 `games\<dirname>\`**。§2.5、§4 的 M01 前置断言、§8.8.9、§9.11.2、§10 的相关条目全部引用本小节。

```
输入： title（人填的真实标题）· seq4（item.id 里的 4 位序号）· acp（§9.11.2 PF-08 探测值）
       budget_e（§2.5 给出的「内部最长相对路径」预算）· libIndex（库内已存在 dirname 的集合）
输出： dirname + dirname_degraded{degraded, reason, acp_at_decision, normalized_candidate}
```

#### 3.2A.1 第一步：规范化链（九条，顺序不可换）

| # | 动作 | 细则 |
|---|---|---|
| N1 | **NFC** | `$s.Normalize([Text.NormalizationForm]::FormC)`。从网页抓来的日文名常是 NFD 分解形，建目录与查目录必须用同一形式【官方 .NET Normalize】 |
| N2 | **非法字符 → 全角同形字** | 只换 §3.2A.2 表里那九个，其余字符一律不动（保可读性） |
| N3 | **删除 `U+0000..U+001F`** | 直接删除，不替换 |
| N4 | **折叠空白** | `U+0009` `U+0020` `U+00A0` `U+3000` 的连续段 → 单个 `U+0020`；然后首尾 Trim |
| N5 | **去尾部的 `.` 与 ` `** | 循环到尾字符既不是 `.` 也不是空格为止（Windows 会静默吃掉它们，导致「建的目录名≠查的目录名」） |
| N6 | **保留设备名防御** | 取第一个 `.` 之前的部分（stem），**大小写不敏感**比对 `CON PRN AUX NUL CONIN$ CONOUT$ COM1..COM9 LPT1..LPT9`，以及上标变体 `COM¹ COM² COM³ LPT¹ LPT² LPT³`（`U+00B9` `U+00B2` `U+00B3`）。命中 → 整个名字前加 `_`。**`NUL.txt` 这类「保留名+扩展名」同样命中**【官方 naming-files；上标变体属 Win32 名称解析的已知行为，标【社区】】 |
| N7 | **裁剪到 60 字符** | 口径 = 60 个 **UTF-16 码元**（PowerShell `.Length`）。若切点落在代理对或组合序列中间，**退到前一个字素簇边界**。裁剪后**重跑 N5**（裁剪可能暴露出新的尾部空格或点） |
| N8 | **冲突 → 追加 SHA-1 前 8 位** | 按 §3.2A.4 的大小写不敏感规则与 `libIndex` 比对；冲突则主体先裁到 **51** 字符（再跑 N5），追加 `-` + `SHA1(UTF8(N1 输出))` 的**前 8 个十六进制小写字符**，`reason = "collision"`。SHA-1 在此只作短标识、不作安全用途【设计决策】 |
| N9 | **空串兜底** | 走完前八条仍为空串 → `dirname := "g" + seq4`，`reason = "empty_after_normalize"` |

#### 3.2A.2 全角同形字映射表（逐字写死）

| 非法字符 | 码位 | 替换为 | 码位 |
|---|---|---|---|
| `\` | U+005C | `＼` | U+FF3C |
| `/` | U+002F | `／` | U+FF0F |
| `:` | U+003A | `：` | U+FF1A |
| `*` | U+002A | `＊` | U+FF0A |
| `?` | U+003F | `？` | U+FF1F |
| `"` | U+0022 | `＂` | U+FF02 |
| `<` | U+003C | `＜` | U+FF1C |
| `>` | U+003E | `＞` | U+FF1E |
| `\|` | U+007C | `｜` | U+FF5C |

这九个是 Win32 的保留字符全集【官方 https://learn.microsoft.com/en-us/windows/win32/fileio/naming-a-file 】。**注意替换结果全是非 ASCII**，因此含这些字符的标题几乎必然在第二步被 ACP 往返判掉（ACP 不是 932/936/65001 时），这是**有意的**：宁可降级成可读性差但一定能开的 slug，也不要建一个老引擎打不开的目录。

#### 3.2A.3 第二步：ACP strict 往返检查

```powershell
# 【官方】Encoding.GetEncoding(Int32, EncoderFallback, DecoderFallback)
#         https://learn.microsoft.com/en-us/dotnet/api/system.text.encoding.getencoding
$enc = [System.Text.Encoding]::GetEncoding(
           $acp,
           [System.Text.EncoderExceptionFallback]::new(),
           [System.Text.DecoderExceptionFallback]::new())
try {
    $roundTrip = $enc.GetString($enc.GetBytes($candidate))
    $ok = ($roundTrip -ceq $candidate)        # -ceq = Ordinal 区分大小写比较
} catch [System.Text.EncoderFallbackException] { $ok = $false }
  catch [System.Text.DecoderFallbackException] { $ok = $false }
```

**必须用 `ExceptionFallback` 这一对**：`GetEncoding(int)` 的默认 fallback 是**替换**（不可表示的字符变 `?`），往返会「成功」但字符已经被悄悄改掉——那正是我们要检出的情况。`$ok == false` → `reason = "acp_unrepresentable"`，走第三步降级。

【待测】→ §11：目标机的 ACP 实际值（PF-08）与常见日文标题在该 ACP 下的往返率。不实测的后果：只能保守地一律降级，全库目录名不可读。

#### 3.2A.4 第三步：MAX_PATH 预算 · 第四步：库内去重 · 降级 slug

**第三步（预算）**：断言 `17 + len(dirname) + 1 + budget_e ≤ 248`（`D:\GameLib\games\` = 17 字符；建目录的实际上限是 **248 = MAX_PATH − 12**，不是 260；`budget_e` 与整条算术见 §2.5）。不满足 → `reason = "path_budget"`，走降级。降级后仍不满足 → 落停机态 **`PATH_BUDGET_EXCEEDED`**（§4.3），人工动作是换更短的库根或人工指定更短的 `dirname`；人工指定的名字仍要过 N2/N3/N5/N6 的合法性校验，但跳过 N7/N8 的自动裁剪，`reason` 仍记 `path_budget`。

**第四步（去重）**：库内 `dirname` 的唯一性判据是 **`ToUpperInvariant()` 后的 Ordinal 相等**（NTFS 默认大小写不敏感，`ネコぱら` 与 `ネコパラ` 是不同名字、`Game` 与 `game` 是同一个）。N8 与降级 slug 都用这一条比较；降级 slug 再撞 → `New-Batch.ps1` 报错退出，**不自动改名**。

**降级 slug 的形状**：

```
dirname := "g" + seq4 + "-" + tail
tail    := ASCII 化结果（把 N1 输出里的 [A-Za-z0-9] 段保留、其余折成单个 "-"、小写、去首尾 "-"、裁到 24 字符）
           若 ASCII 化结果为空  ->  tail := "h" + SHA1(UTF8(N1 输出)) 的前 8 位十六进制小写
例：  "NEKOPARA Vol.1"  ->  g0042-nekopara-vol-1
      "ネコぱら Vol.1"   ->  g0042-vol-1
      "ネコぱら"         ->  g0042-h3b1f9ac2
```

**不做假名/汉字→罗马字的自动转写**——那需要一本词典并且会猜错读音，与「不猜」的定性冲突；只保留标题里本来就有的 ASCII。真实标题始终留在批次清单的 `title` 与 `manifest.json` 里，报告一律**同时**打印 `dirname` 与 `title`，所以可读性损失只发生在文件管理器里。

**降级原因必须记录**（`dirname_degraded`），这是本算法的硬要求：事后没有这条记录，就无法回答「为什么这个游戏的目录是一串字母」。

---

### 3.3 `games\<dirname>\.gameflow\events.jsonl`：唯一真相

#### 3.3.1 事件 schema（唯一一套，闭集）

每行一个 **compact** JSON 对象，UTF-8 无 BOM，行尾 `\n`（单字节 LF，不是 CRLF）。

```jsonc
{
  "schema_version": 1,
  "seq": 187,                                  // 本文件内单调递增，从 1 开始
  "ts": "2026-09-12T04:18:02.7710000Z",        // [DateTimeOffset]::UtcNow.ToString('o')
  "actor": "B",                                // A | B | C | human
  "trigger": "manual",                         // scheduled | manual | codex
  "run_id": "20260912T041800Z-a3f19b2c",       // 一次脚本执行的标识
  "batch_id": "2026-09-12-vn-a",
  "item_id": "g0042-nekopara",
  "kind": "state.entered",                     // 封闭枚举，见 §3.3.2
  "from_state": "VERIFYING",                   // 仅 kind=state.entered 时非 null
  "to_state": "EXTRACTED",
  "payload": { "transition_id": "M08", "reason_code": null },
  "evidence": null                             // 见 §3.3.3
}
```

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `schema_version` | int | ✅ | `1` |
| `seq` | int | ✅ | 本文件内单调递增。**撕裂尾行被封口后允许出现一次重复**，见 §3.3.4 |
| `ts` | string | ✅ | UTC，`o` 格式。**不用于排序**，只用于人读与耗时统计（时钟可能回拨） |
| `actor` | string | ✅ | `A`（巡检）\| `B`（作业）\| `C`（前台）\| `human` |
| `trigger` | string | ✅ | `scheduled`（任务计划）\| `manual`（人在终端）\| `codex`（Codex skill）。同一入口脚本三条触发路径，不记就无法事后区分是谁动的 |
| `run_id` | string | ✅ | `<UTC yyyyMMddTHHmmssZ>-<8hex>`，例 `20260912T041800Z-a3f19b2c`。**报告 front-matter 用的也是这个形状**，不得改写成带冒号的完整 ISO |
| `batch_id` / `item_id` | string | ✅ | 冗余存放，让单个 `.gameflow` 目录可脱离库被阅读 |
| `kind` | string | ✅ | §3.3.2 的封闭枚举，**不是自由文本** |
| `from_state` / `to_state` | string \| null | ✅ | 只有 `kind="state.entered"` 时两者都非 null；其余事件一律 `null` |
| `payload` | object | ✅ | 形状由 `kind` 决定；可以是 `{}`，键不可省 |
| `evidence` | object \| null | ✅ | §3.3.3；断言外部事实的事件必填 |

三条全局约定：

1. **失败原因一律叫 `reason_code`**（封闭代码，不是自由文本），全 SPEC 不存在 `reason` 这个字段名。自由文本走 `payload.note` 或 `_note`。
2. **`state.entered` 是唯一能改变 `state.json.state` 的事件**（不变量 I2 的落地形式）。迁移信息全部进 payload：`{transition_id, reason_code}`，`transition_id` 取 §4 的迁移编号（`M08` / `H07` / `R02`…）。§4 **不另立具名迁移 kind**，`from_state`/`to_state`/`transition_id` 三者已经能表达全部 14 条主线迁移与全部停机迁移。
3. `actor="human"` 的事件**同样由脚本写入**（`Add-VaultEntry`、`Resolve-Halt`、`Set-VolumeRule` 等），代表「这一步的决定由人做出」。**人不手工编辑 `events.jsonl`**——手编会破坏 seq 连续性与尾行完整性判定。

#### 3.3.2 `kind` 闭集（30 个）

| `kind` | 典型 actor | 语义 | `payload` 关键键 | 改 state | evidence |
|---|---|---|---|---|---|
| `run.started` | A / B / C | 一次脚本执行开始 | `script`,`args_digest`,`script_version` | ❌ | ❌ |
| `run.ended` | A / B / C | 一次脚本执行正常结束 | `exit_code`,`items_touched` | ❌ | ❌ |
| `item.registered` | A | 首次为该条目建目录并落 `.gameflow` | `dirname`,`library_root` | ❌ | ❌ |
| `env.snapshot` | A / B | 记录本次运行的环境上下文 | `acp`,`beta_utf8`,`sevenzip_version`,`long_paths`,`fs_type`,`ps_version`,`mtool_build` | ❌ | ❌ |
| `download.claimed` | A | 从 `_inbox` 认领一个文件归位到 `games\<dirname>\` | `file`,`bytes`,`from`,`matched_by` | ❌ | ✅ |
| `download.gate` | A | 一次 `DOWNLOAD_STABLE` 六重判据采样 | `g1`..`g6`（各 `true`/`false`/`"skipped"`）,`sample_index`,`file`,`reason_code` | ❌ | ✅ |
| `volume.group.formed` | A | 卷集归组结果 | `group`,`members[]`,`contiguous`,`equal_size_except_last`,`count`,`expected_parts` | ❌ | ✅ |
| `archive.identified` | A | 只读预检（`7z l -slt`）结果 | `archive`,`type`,`offset`,`physical_size`,`sum_entry_size`,`entries`,`encrypted`,`encryption_method`,`header_encrypted`,`volumes`,`ratio`,`rebuilt_as` | ❌ | ✅ |
| `expected.declared` | C / human | 写回 `expected_parts` / `expected_files` | `expected_parts`,`expected_files[]`,`expected_source` | ❌ | ✅ |
| `password.attempted` | B | 一次密码候选尝试 | `source`（`P0`..`P5`）,`vault_key`,`candidate_index`,`variant_index`（0..3）,`exit` | ❌ | ✅ |
| `archive.tested` | B | 单个源包的 `7z t` 结果（D2/D3 的证据） | `archive`,`exit`,`warnings[]`,`entries` | ❌ | ✅ |
| `extract.attempt.started` | B | 一层解包开始 | `attempt_id`,`depth`,`archive`,`staging` | ❌ | ❌ |
| `extract.attempt.finished` | B | 一层解包结束 | `attempt_id`,`exit`,`files_reported`,`entries_listed`,`warnings[]`,`bytes_out` | ❌ | ✅ |
| `verify.entry.mismatch` | B | 逐条目 exists/size 对账不通过 | `path`,`expected_bytes`,`actual_bytes` | ❌ | ✅ |
| `verify.completed` | B | D1–D12 逐条判定结果（定义见 §6.8） | `d1`..`d12`（各 `true`/`false`/`"n/a"`）,`passed`,`manifest_sha256` | ❌ | ✅ |
| `archive.verified` | B | 把某源包标为可删（**I3 的唯一来源**） | `archive`,`sha256`,`bytes`,`consumed_by`,`criteria_ref` | ❌ | ✅ |
| `staging.promoted` | B | staging → 落地移动完成且体积一致（D12 前半） | `attempt_id`,`from`,`to`,`bytes`,`entries` | ❌ | ✅ |
| `staging.discarded` | B | attempt 的 staging 被回收 | `attempt_id`,`reason_code`（`crash_orphan`\|`superseded`\|`abandoned`）,`bytes_reclaimed` | ❌ | ✅ |
| `archive.deleted` | B | 源包已删除或已移入 trash | `archive`,`sha256`,`bytes`,`result`（`deleted`\|`trashed`\|`already_absent`\|`retained`） | ❌ | ✅ |
| `defender.scanned` | B | Defender 预检（时点见 §6.9：在 `VERIFYING` 之内、`staging.promoted` 之前） | `verdict`（`clean`\|`A`\|`B`\|`C`）,`targets[]`,`threats[]`,`raw_ref` | ❌ | ✅ |
| `scan.completed` | B | 引擎判定 + 内容清单快照写入 manifest | `engine`,`rule_rank`,`matched_by`,`game_root`,`content_files`,`container_marks[]` | ❌ | ✅ |
| `motw.cleared` | B | `Unblock-File` 已执行（阶段二前置条件） | `targets_count` | ❌ | ✅ |
| `mtool.injected` | C | 检出 `version.dll` | `target_exe`,`game_root`,`new_files[]` | ❌ | ✅ |
| `rule.required` | A / B / C | 需要人工声明规则 | `rule_kind`,`candidates[]`,`human_action` | ❌ | ✅ |
| `halt.entered` | A / B / C | 进入停机态（与同 `run_id` 内紧邻的 `state.entered` 配对） | `state`,`reason_code`,`human_action`,`action_command`,`resume_allowed[]`,`cleared_by_allowed[]` | ❌ | ❌ |
| `halt.cleared` | human | 人工解除停机 | `state`,`resolution`,`resume_to`,`resolved_by` | ❌ | ❌ |
| `state.entered` | A/B/C/human | **状态迁移** | `transition_id`,`reason_code` | **✅** | ❌ |
| `journal.torn_line_sealed` | A/B/C | 尾部半行被封口 | `bytes_discarded`,`tail_excerpt_sha256` | ❌ | ❌ |
| `state.rebuilt` | A/B/C | `state.json` 从日志重建完成 | `events_replayed`,`reason_code`（`missing`\|`unparsable`\|`stale`\|`forced`）,`divergence` | ❌ | ❌ |
| `note.human` | human | 人工备注 | `text` | ❌ | ❌ |

**`halt.entered` 的三个闭集字段**（P-2 的落地：进入停机的那一刻就写死「解除后回到哪」，不靠解除时推断）：`resume_allowed[]` 是允许的回归状态闭集，`cleared_by_allowed[]` ⊆ `{human, B, C}`；`halt.cleared` 写入前必须断言 `resume_to ∈ resume_allowed` 且 `resolved_by ∈ cleared_by_allowed`。取值由 §4.3 的停机态手册逐态给出，本节只定形状。

**§4 草稿里的旧 kind 名一律作废**，映射如下（实现时只认右列）：`item.planned`/`download.armed`/`verify.passed` 等 14 个具名迁移 → `state.entered` + `payload.transition_id`；`item.claimed` → `download.claimed`；`archive.extracted` → `extract.attempt.finished`；`verify.result` → `verify.completed` + `archive.verified`；`scan.result` → `defender.scanned`；`engine.detected` → `scan.completed`；`human.action` → 由 `actor="human"` 的事件本身承担（见 §3.4.1 `human_actions[]`）；`recovery.rebuilt` → `state.rebuilt`。

枚举必须封闭的理由：`Get-BatchReport.ps1`、状态重建、删除 gate 三个消费者都按 `kind` 分派；一旦允许自由文本，对账就要做模糊匹配，确定性当场失效。新增 `kind` 属于 schema 变更，要提 `schema_version`。

#### 3.3.3 `evidence`：外部事实的凭据

```jsonc
"evidence": {
  "cmd": "7z l -slt -bso1 -bse1 -p*** \"ネコぱら.mp4.001\"",  // 已脱敏，见下
  "exit": 0,
  "stdout_sha256": "9f2c…",          // 完整 stdout 的 SHA-256，用于事后比对
  "stdout_excerpt": "Type = Split\nVolumes = 3\n…",           // ≤ 2 KiB
  "overflow_file": null,             // 超长时：".gameflow\\evidence\\000187-archive.identified.txt"
  "files": ["ネコぱら.mp4.001"],      // 相对 games\<dirname>\ 的路径，绝不写绝对路径
  "observed_at": "2026-09-12T04:18:02.7010000Z"
}
```

1. **脱敏是写入前的强制步骤**：命令行文本里 `-p` 之后到下一个空白之前的一切一律替换为 `***`。`evidence.cmd` 里**永远不得出现密码明文**，`password_ref` 的**值**也不得出现（键名可以）。完整脱敏规则见 §9.1.4。
2. `stdout_excerpt` ≤ 2 KiB；整行序列化后 > 64 KiB 时，把完整 stdout 另存 `.gameflow\evidence\<6位seq>-<kind>.txt` 并把相对路径放进 `overflow_file`。JSONL 的可恢复性依赖「行不要太长」。
3. 所有路径**相对 `games\<dirname>\`**——日志因此可以跨机器阅读，也不泄露目录结构。
4. `evidence` 必填的 kind：`download.claimed` / `download.gate` / `volume.group.formed` / `archive.identified` / `expected.declared` / `password.attempted` / `archive.tested` / `extract.attempt.finished` / `verify.*` / `archive.verified` / `staging.promoted` / `staging.discarded` / `archive.deleted` / `defender.scanned` / `scan.completed` / `motw.cleared` / `mtool.injected` / `rule.required`。其余可为 `null`。

#### 3.3.4 写入协议（照着实现）

```
AppendEvent(itemDir, ev):
  1. fs = FileStream(itemDir\.gameflow\events.jsonl,
                     Append, Write, FileShare::None)          # 独占；失败 = LOCKED_BY_ANOTHER_RUN
                                                              # （运行期状况，不落盘、不进状态机）
  2. if fs.Length > 0 且最后一个字节 != 0x0A:                  # 上次崩溃留下半行
        写入单字节 0x0A 封口 + Flush($true)
        先追加一条 journal.torn_line_sealed 事件（同样走本协议）
  3. ev.seq = (扫描本文件全部可解析行得到的 max(seq)) + 1
  4. line  = ConvertTo-Json $ev -Compress -Depth 10            # -Compress 是硬要求
     断言 line 不含 0x0A / 0x0D；含则视为构造错误，抛异常，不写
  5. bytes = [UTF8Encoding]::new($false).GetBytes(line + "`n") # 组装成**一个** byte[]
  6. fs.Write(bytes, 0, bytes.Length)
     fs.Flush($true)                                          # 对应 Win32 FlushFileBuffers
     fs.Dispose()                                             # 立即关闭，不保持长开 StreamWriter
```

- **`-Compress` 是硬要求**：默认带缩进的多行 JSON 会彻底毁掉「一行一条」的可恢复性。
- **不保持长开的 `StreamWriter`**：进程被强杀时缓冲区内容全丢。每条事件开→写→刷→关。
- `Flush($true)` 是本项目唯一真正有断电保证的一步【官方】 https://learn.microsoft.com/en-us/dotnet/api/system.io.filestream.flush ——「clears all intermediate file buffers」，5.1 与 7.x 都有。
- 追加本身**不是原子的**，崩溃可能留下半行【社区】（Windows 上没有找到官方保证）→ 第 2 步的封口逻辑是必需的。【待测】→ §11：NTFS 上 < 4 KB 的单次写被强杀是否真会撕裂半行。

#### 3.3.5 读取与重放协议（本条是重放顺序的唯一权威）

```
ReadEvents(path) -> (events[], torn_tail: bool):
  lines = 按 0x0A 切分 UTF-8 字节流
  for i, line in lines:
      if line 为空: continue
      ev = TryConvertFromJson(line)
      if ev == null:
          if i 是最后一行: torn_tail = true; 记警告; break     # 丢弃尾部半行
          else:            落停机态 JOURNAL_CORRUPT（见下）
      events += ev
```

- **重放顺序 = 文件行序，绝不按 `seq` 排序。** `seq` 只用于人读与断裂检测；封口之后允许出现一次重复值，按 seq 排序会让这两条的先后变得不确定 ⇒ 重放结果不确定 ⇒ I2 失效。**§4.5.1 的 `StableSortBy` 与 §8.8.5 的「按 seq fold」不成立。**
- **中间行解析失败** = 真正的日志损坏（不是崩溃截断）。落**停机态 `JOURNAL_CORRUPT`**（§4.3）：不推进任何状态、不删除任何文件（I1），报告里以错误级标出并给出人工动作「把 `events.jsonl` 交给人检查」。它是**落盘的停机态**而不是运行期状况——运行期状况下次运行还是坏的，条目会永久静默停在原地。
- `from_state` 与快照当前状态不一致时**采用事件的 `to_state` 继续**（append-only 日志是真相，重建器没有资格否决它），但必须写 `state.rebuilt{divergence:true}` 并在报告里标红。
- **上次崩溃的探测器** = 有 `run.started` 而无配对 `run.ended`。纯 journal 推导、无时间猜测。

```
Rebuild(itemDir) -> state:
  s = EmptyState(schema_version)                  # 见 §3.4.1 的字段集合
  (events, torn) = ReadEvents(itemDir\.gameflow\events.jsonl)
  active_item = events 中最后一条 item.registered 的 item_id      # §4.8 的复活语义
  for ev in events:
      if ev.item_id != active_item: s.history += Summarize(ev); continue
      s = Apply(s, ev)                            # Apply 由 §3.3.2 表的「改 state」列 + payload 定义
  s.last_event_seq = events[-1].seq ;  s.rebuilt_at = now
  写回 state.json（§3.9.2）；追加 state.rebuilt 事件
```

`Apply` 必须**幂等、只依赖顺序、绝不读文件系统**：所有事件记的是「进入状态 X」「把 archive Y 标成 verified」这类**绝对赋值**，绝不记「计数 +1」。一旦重建时「看一眼盘上现在什么样」，I3 就破了，幂等随之破。

---

### 3.4 `games\<dirname>\.gameflow\state.json`：派生快照

#### 3.4.1 完整示例

```jsonc
{
  "schema_version": 1,
  "item_id": "g0042-nekopara",
  "batch_id": "2026-09-12-vn-a",
  "dirname": "ネコぱら Vol.1",

  "state": "EXTRACTED",
  "entered_at": "2026-09-12T04:18:02.7710000Z",
  "last_event_seq": 187,
  "rebuilt_at": "2026-09-12T04:18:02.9000000Z",

  // 最近一次运行；started 有而 ended 为 null = 上次在这个 item 上被打断（崩溃探测器）
  "last_run": { "run_id": "20260912T041800Z-a3f19b2c", "actor": "B", "trigger": "manual",
                "started_at": "2026-09-12T04:18:00.0000000Z", "ended_at": null },

  // 认领归位账本（fold 自 download.claimed）
  "claimed_files": [
    { "name": "ネコぱら.mp4.001", "bytes": 2147483648, "from": "_inbox\\ネコぱら.mp4.001",
      "claimed_at": "2026-09-12T03:55:02.0000000Z" }
  ],

  // ── 源包账本。删除源包的判据只读这里，不现场重判（不变量 I3）──────
  "archives": [
    {
      "name": "ネコぱら.mp4.001",         // 相对 games\<dirname>\；**没有 path 字段，见 §3.4.2**
      "sha256": "3b1f…",                  // 删除前必须已写入 manifest.json
      "bytes": 2147483648,
      "type": "Split",                    // 7z l -slt 的归档级 Type，内容嗅探结果
      "encrypted": false,
      "encryption_method": null,          // A 只抄 `Method` 原文；AES/ZipCrypto 判别归 B（§6.3.7）
      "volume_group": "vg-001",           // 同组卷共享；单文件包为 null
      "tested": { "exit": 0, "warnings": [], "entries": 214 },
      "verified": true,
      "verified_at": "2026-09-12T04:17:55.0000000Z",
      "consumed_by": "20260912T041640Z-7c2e",   // attempt_id
      "deleted_at": null
    }
    // … 同组另外两卷同形状，略
  ],

  // staging → 落地的落盘凭据（D12 前半）
  "promoted": [
    { "attempt_id": "20260912T041640Z-7c2e", "from": "_stg\\7c2ea1b4", "to": "ネコぱら Vol.1",
      "bytes": 6871947673, "entries": 1402, "promoted_at": "2026-09-12T04:18:01.0000000Z" }
  ],
  "manifest_sha256": "7d41…",             // 写 manifest 的那一刻的内容哈希；幂等跳过用

  "extract": {
    "depth_reached": 2,                   // 口径：成功解开一个白名单容器 = +1（Split 外壳也算）
    "max_depth_effective": 5,
    "staging": "_stg\\7c2ea1b4",          // 相对 D:\GameLib\；解完清空
    "attempts": [
      { "attempt_id": "20260912T041640Z-7c2e", "depth": 1, "archive": "ネコぱら.mp4.001",
        "exit": 0, "files_reported": 214, "entries_listed": 214, "warnings": [],
        "started_at": "…", "finished_at": "…" }
    ]
  },

  "defender": { "verdict": "clean", "scanned_at": "2026-09-12T04:17:40.0000000Z",
                "targets": ["_stg\\7c2ea1b4"], "threats": [], "raw_ref": null },

  "engine": {
    "id": "RPGMaker_MV",
    "rule_rank": 2,                       // engine-rules.json 里的 rank，先匹配先赢
    "matched_by": "www/js/rpg_core.js",   // 命中的那个文件，相对 game_root
    "game_root": "ネコぱら Vol.1\\NEKOPARA_v1.02",  // 相对 games\<dirname>\，可能多一层
    "container_marks": ["NWJS"],          // 只记录，不参与引擎裁决
    "mtool_supported": true,
    "target_exe": null
  },

  // 人工动作登记：fold 自所有 actor="human" 的事件（halt.cleared / note.human / expected.declared…）
  "human_actions": [
    { "seq": 154, "at": "2026-09-12T04:10:02.0000000Z", "kind": "halt.cleared",
      "summary": "录入 site:example-site.org|ookkgg 的密码后解除 WAITING_FOR_PASSWORD" }
  ],

  // 被复活语义挤下去的旧 item 的事件摘要（§4.8：同一 dirname 可有多个历史 item_id）
  "history": [
    { "item_id": "g0007-nekopara-old", "events": 42, "last_state": "ABANDONED",
      "last_seq": 96 }
  ],

  // ── 解释失败用的环境上下文。不是判据，是报告里的上下文 ─────────────
  "env": {
    "captured_at": "2026-09-12T04:16:30.0000000Z",
    "acp": 932,                 // 系统 ANSI 代码页；由 §9.11.2 PF-08 探测
    "beta_utf8": false,         // 「使用 Unicode UTF-8 提供全球语言支持」是否勾选
    "sevenzip_version": "26.02",
    "sevenzip_path": "C:\\Program Files\\7-Zip\\7z.exe",
    "long_paths": true,         // LongPathsEnabled；**只记录，不放宽预算**
    "fs_type": "NTFS",          // 库所在卷
    "ps_version": "7.4.6",
    "mtool_build": null         // 阶段二填；MTool 只有 build 标识且会自动更新
  },

  "halt": null,                 // 非停机态时为 null
  "_note": null
}
```

停机时 `halt` 的形状：

```jsonc
"halt": {
  "state": "WAITING_FOR_PASSWORD",
  "reason_code": "PWD_NOT_IN_CHAIN",                // 封闭代码，逐态取值见 §4.3
  "transition_id": "H07",
  "human_action": "vault 里没有 site:example-site.org|ookkgg，也没有 title: 条目；请录入密码后重跑作业",
  "action_command": "Add-VaultEntry -Key 'site:example-site.org|ookkgg'",
  "resume_allowed": ["DOWNLOAD_STABLE"],            // 闭集；解除时的目标状态只能取自这里
  "cleared_by_allowed": ["B"],                      // ⊆ {human, B, C}
  "since": "2026-09-12T04:19:10.0000000Z",
  "resolved_by": null,                              // 解除时写，∈ cleared_by_allowed
  "cleared_by": null                                // 解除者的标识（人名/脚本名），仅供人读
}
```

#### 3.4.2 字段表

| 字段 | 类型 | 说明 |
|---|---|---|
| `state` | string | §4 的状态名之一。**只由 `state.entered` 事件改** |
| `entered_at` | string | 进入当前状态的时间（该 `state.entered` 事件的 `ts`）。**字段名不是 `state_since`** |
| `last_event_seq` | int | 最后一条被 fold 进来的事件 `seq`；用于「快照是否落后于日志」的检测 |
| `last_run` | object \| null | fold 自 `run.started`/`run.ended`；**崩溃探测器的唯一输入** |
| `claimed_files[]` | array | fold 自 `download.claimed`；认领归位的幂等判据（同 `(name, bytes)` 再来一次即 no-op） |
| `archives[]` | array | 见下 |
| `promoted[]` | array | fold 自 `staging.promoted` |
| `manifest_sha256` | string \| null | 写 manifest 的幂等判据；与重算值一致则跳过重写 |
| `extract.depth_reached` | int | 口径：**成功解开一个白名单容器 = +1**（**分卷 Split 外壳同样计层，无例外**），不是「调了几次 7z」 |
| `extract.staging` | string \| null | `_stg\<8hex>`，相对 `D:\GameLib\`。**布局里没有 `w\` 子目录**；分卷重建副本落**同级**的 `_stg\<8hex>-p\`（§6.4） |
| `extract.attempts[]` | array | 每层一条，含 exit / `Files: N` / `l -slt` 条目数 / warnings |
| `defender` | object \| null | fold 自 `defender.scanned`；`verdict` ∈ `clean`\|`A`\|`B`\|`C` |
| `engine` | object \| null | §7.6 判定结果；`game_root` 是**相对路径**（字段名不是 `game_root_rel`）；`UNKNOWN` 不等于不支持翻译 |
| `human_actions[]` | array | fold 自所有 `actor="human"` 的事件（不需要专门的 `human.action` kind） |
| `history[]` | array | §4.8 复活语义：同一 `dirname` 下非活跃 `item_id` 的事件摘要 |
| `env` | object \| null | 见下 |
| `halt` | object \| null | 见上 |

**`archives[]` 每项**

| 字段 | 类型 | 说明 |
|---|---|---|
| `name` | string | 相对 `games\<dirname>\` |
| `sha256` | string \| null | **删除前必须非 null**，且必须已同步写进 `manifest.json` |
| `bytes` | long | 字段名是 `bytes`，不是 `size` |
| `type` | string \| null | `7z l -slt` 归档级 `Type`（内容嗅探结果）。字段名与语义跨平台一致【实测 macOS 25.01，可外推】 |
| `encrypted` | bool | A 在只读预检里读到的 `Encrypted = +` |
| `encryption_method` | string \| null | **`Method` 字段的原文，作为上下文**。A 不做 AES / ZipCrypto 判别——那由 B 纯字节解析中央目录完成（§6.3.7） |
| `volume_group` | string \| null | 同一卷集共享一个组 id |
| `tested` | object \| null | fold 自 `archive.tested`：`{exit, warnings[], entries}` |
| `verified` | bool | D1–D12（§6.8）全部通过才为 true |
| `verified_at` | string \| null | |
| `consumed_by` | string \| null | 消费它的 `attempt_id` |
| `deleted_at` | string \| null | 非 null 即已删（字段名不是 `deleted`） |

> **`archives[]` 永远不引入 `path` 字段。** 删除源包时必须 `Join-Path <该 item 的游戏根> a.name` 现拼，§6.8.3 照此实现。理由：自拼绝对路径会让删除动作读的字段与落盘 gate 读的字段不是同一个，直接破坏 I3；而且库根一旦搬迁，落盘的绝对路径会指向别的机器上的目录。

**`attempt_id` 格式** = `<UTC 紧凑 ISO>-<4hex>`，例 `20260912T041640Z-7c2e`。**不用 ULID**——`_stg\<8hex>` 的目录名与 attempt 的映射靠这个形状维持，换成 ULID 就对不上了（§6.1.2 照此实现）。

**`env` 各字段的用途**：全部是**解释失败的上下文**，不是判据。举例：中文密码的 4 种等价展开全部失败时，报告要同时显示「已试的 4 种展开」与「当前 ACP = 932」，人才能一眼看出「这台机器是日文区域，GBK 换算本来就不成立」；`sevenzip_version` 落后时报告顶部长期提示「7-Zip 版本落后，存在已知解析器 RCE」但**不拒绝运行**（硬下限 25.01，建议 ≥ 26.02，理由是解析不可信归档的内存安全漏洞）【官方 changelog】。

#### 3.4.3 `verified` + `consumed_by` 如何 gate 住删除（不变量 I3）

```
CanDelete(archive) :=
      archive.verified        == true
  AND archive.consumed_by     == <本次落地所对应的 attempt_id>
  AND archive.deleted_at      == null
  AND state.json 已经 Flush($true) 落盘（即该判定读的是磁盘上的值，不是内存里的）
```

**删除是一个只读 `state.json` 的纯函数。** D1–D12（定义见 §6.8）只在 `verify.completed` 那一刻求值一次，结果经 `archive.verified` 事件落盘；执行体 B 在删除的那一刻**不重新跑** `7z t`、不重新数条目、不重新对账。三条理由，每条都能单独否掉「现场重判」：

1. **幂等性**——重跑是本项目的基本假设。现场重判会让同一个包在两次运行里得到不同答案（Defender 隔离了一个文件、磁盘满了、密码被从 vault 删了），于是已验证落地的内容会因一次无关的环境变化而被判成未验证。
2. **一半判据在删除时刻已无法求值**——D4/D5 是 `7z x` 那一刻的输出，D11/D12 依赖 staging，而 staging 解完就清空了。现场重判只能重判 D2/D6，等于**偷偷放宽判据**。C3+C4 意味着删除不可撤销、无回收站兜底，判错一次即永久数据丢失。
3. **崩溃语义**——「验证通过」与「已删除」之间必然有窗口。验证结果不落盘，崩在窗口里就无法知道验证过没过；落盘之后恢复逻辑只需读 `archives[].verified/deleted_at`。

对应地，`archive.verified` 事件必须**自带全部重建所需的信息**（`sha256` / `bytes` / `consumed_by` / `criteria_ref`），否则删掉 `state.json` 重建之后 `verified` 会丢，I2 与 I3 就互相拆台了。

---

### 3.5 `games\<dirname>\.gameflow\manifest.json`：来源与产物档案

```jsonc
{
  "schema_version": 1,
  "item_id": "g0042-nekopara",
  "batch_id": "2026-09-12-vn-a",

  "title": "ネコぱら Vol.1",
  "title_form": "NFC",
  "dirname": "ネコぱら Vol.1",
  "dirname_degraded": { "degraded": false, "reason": "none", "acp_at_decision": 932,
                        "normalized_candidate": "ネコぱら Vol.1" },

  "source": {
    "site": "example-site.org",
    "uploader": "ookkgg",
    "pages": ["https://example-site.org/archives/12345"],
    "netdisk_hosts": ["pan.baidu.com", "pan.quark.cn"],   // 只存 host，**不存带 token 的 URL**
    "profile_id": "example-site.org|ookkgg"               // 命中的 source-profiles 条目
  },

  // 每个源包的档案。sha256 在**删除之前**必须写进这里
  "archives": [
    { "name": "ネコぱら.mp4.001", "sha256": "3b1f…", "bytes": 2147483648,
      "type": "Split", "volume_group": "vg-001",
      "rebuilt_as": "g0042.bin.001",                      // §3.2.6 的分卷重建副本名；未重建则 null
      "claimed_from": "_inbox\\ネコぱら.mp4.001", "claimed_at": "2026-09-12T03:55:02.0000000Z",
      "netdisk": "baidu", "deleted_at": "2026-09-12T04:20:31.0000000Z",
      "consumed_by": "20260912T041640Z-7c2e" }
  ],

  "engine": {
    "id": "RPGMaker_MV", "rule_rank": 2, "matched_by": "www/js/rpg_core.js",
    "game_root": "ネコぱら Vol.1\\NEKOPARA_v1.02",
    "container_marks": ["NWJS"], "mtool_supported": true,
    "rules_version": 1, "decided_at": "2026-09-12T04:19:40.0000000Z"
  },

  // ★ 阶段一结束时的文件清单快照，用于识别阶段二 MTool 的增量。**键名就叫 stage1_snapshot**
  "stage1_snapshot": {
    "captured_at": "2026-09-12T04:21:05.0000000Z",
    "root": "games\\ネコぱら Vol.1",
    "hash_policy": "none",              // none | content_files_only | all
    "file_count": 1402,
    "total_bytes": 6871947673,
    "files": [
      { "path": "NEKOPARA_v1.02\\Game.exe", "bytes": 1284096,
        "mtime": "2026-08-02T11:04:19.0000000Z" }
      // …按 path 序数序（Ordinal）排序的全量文件列表
    ]
  },

  "_note": null
}
```

1. **`archives[].sha256` 必须在删除源包之前写入并 Flush。** 零成本的保险：误删之后能确认重下的是不是同一个包。删除动作本身受 D12 约束（§6.8）。
2. **`source.netdisk_hosts` 只存主机名。** `.gameflow\` 绝不存密码、账号、或带 token 的下载 URL（§2.4 的硬约束）；完整分享链接留在批次清单，提取码只以 vault 键名（`code_ref`）的形式出现。
3. **`stage1_snapshot` 是阶段二完成判据的基线。** MTool 是注入式的：往游戏根目录写 `version.dll` 等，产物 `.bin` 落盘位置未知【社区】，所以完成判据**不能依赖具体产物文件名**（`与工具一同启动.bat` 是中文名，几乎肯定随界面语言变化）。正确做法是阶段一在 `SCANNED → READY_FOR_TRANSLATION` 之前拍快照，阶段二用**快照差集**识别增量，再叠加 `version.dll` 存在性这条机器可判定的主判据（§7.6）。
4. **`hash_policy` 默认 `none`**：快照只记 `path` + `bytes` + `mtime`（`LastWriteTimeUtc`，`o` 格式）。给一个 40 GB 的游戏全量算 SHA-256 要几分钟，而识别增量不需要内容哈希。源包的 SHA-256 是**单独一条强制规则**（第 1 点），与快照策略无关。
5. **快照时点**必须在 `Unblock-File` 之后（MOTW 解除是阶段二的前置条件，忘了会让 Computer Use 撞上 SmartScreen 的前台模态框卡死）【官方：`Unblock-File` 幂等、对未标记文件无副作用】。【待测】→ §11：`Unblock-File` 删除 `Zone.Identifier` 备用数据流是否会改动主数据流的 `LastWriteTime`；若会改，快照必须改在它之前，否则阶段二的差集会把整库都算成「变了」。
6. `files[]` 按 `path` 的**序数序（Ordinal）**排序，路径分隔符统一 `\`，相对 `root`。排序固定是为了让两次快照可以逐行 diff。

---

### 3.6 规则文件（`D:\GameFlow\rules\`，进 Git，人是唯一写者）

共七个文件，共同约定：顶层 `schema_version` + `_note`（J7）；**只有人能改**（§3.8 矩阵；唯一例外见 §3.6.5 的 `threat-allowlist.json`）；改动进 Git 因此可回滚可 diff。

| 文件 | 内容 | 消费者 |
|---|---|---|
| `source-profiles.json` | 站点/上传者 → 密码引用、文件名模式、密码抽取正则、已知伪装、预期层数 | A（归组/报告）、B（密码链 P1/P3/P4）、C（P5） |
| `netdisk-profiles.json` | 下载客户端临时物模式 + 三类网盘的页面规则 + 下载判定采样参数 | A（G1/G2/G4）、C（转存与下载） |
| `container-types.json` | 递归白名单 + 扩展名排除表 + content_files 排除表 + 手工安装形态 | A（预检）、B（递归解包、D8/D10） |
| `engine-rules.json` | 引擎特征 → 判据 | B（判定）、C（MTool） |
| `sevenzip-warnings.json` | `7z` 输出 Warning 的白/黑名单（D3 的输入） | B |
| `defender-stdout.json` | `MpCmdRun -DisableRemediation` 的 stdout 解析规则 | B |
| `threat-allowlist.json` | 人工放行的 SHA-256 白名单 | B |
| `mtool-profiles\<item-id>.json` | 单条目的 MTool 规则（目标 exe 等），**全局命名空间** | C |

另有 `_inbox\claims.json`（§3.6.6），它在**数据库**里不在 `rules\` 里，但同样「人写、执行体只读」。

#### 3.6.1 `source-profiles.json`

```jsonc
{
  "schema_version": 1,
  "_note": "主键是 (site, uploader)。资源站的规则按上传者固定，不是按包随机的。",
  "profiles": [
    {
      "id": "example-site.org|ookkgg",       // = site + "|" + uploader，复合主键
      "site": "example-site.org",
      "uploader": "ookkgg",
      "password_ref": "site:example-site.org|ookkgg",
      "filename_patterns": [                 // A 认领归位时的候选匹配模式（glob，大小写不敏感）
        "*ネコぱら*", "[Oo]okkgg_*"
      ],
      "known_decoy_ext": [".jpg", ".png"],   // 站点公告的伪装扩展名
      "expected_layers": 2,                  // 站点公告「需要改 2 次后缀解压 2 次」
      "volume_pattern": "none",              // none | 7z-NNN | rar-partN | rar-rNN | zip-zNN
      "_note": "站点公告原文：下载后把 jpg、png 后缀改成 rar 解压，需要改 2 次后缀解压 2 次"
    }
  ],

  // ★ 密码抽取正则（密码链 P3/P4 的唯一定义处；§6.3.2 的 PW-RX-* 引用本表，不另立）
  "password_patterns": [
    { "id": "PW-RX-01", "regex": "(?im)^\\s*(?:解压)?密\\s*码\\s*[:：]\\s*(?<pw>\\S+)\\s*$" },
    { "id": "PW-RX-02", "regex": "(?im)^\\s*pass(?:word)?\\s*[:：]\\s*(?<pw>\\S+)\\s*$" },
    { "id": "PW-RX-03", "regex": "(?im)^\\s*パスワード\\s*[:：]\\s*(?<pw>\\S+)\\s*$" }
  ],
  "password_file_globs": ["密码.txt", "password.txt", "README*", "*.nfo", "*.url"]
}
```

| 字段 | 用途 | **硬约束** |
|---|---|---|
| `password_ref` | 密码链 P1 的查表结果 | 只写 vault 键名，永不写密码 |
| `filename_patterns[]` | A 把 `_inbox` 里的文件匹配到 item 的候选模式之一（另一路是 `expected_files[].name` 精确匹配，**精确优先**） | 匹配到**多个** item → 不猜，落 `rule.required` |
| `password_patterns[]` | P3/P4 **只做正则精确抽取** | ❌ 绝不把 README 里「读懂」的一段话当密码；`(?<pw>…)` 命名组是唯一取值来源 |
| `known_decoy_ext` | **仅供报告与人工核对** | ❌ **绝不驱动改名，绝不驱动 `-t<type>`**——7-Zip 自己就知道真实类型；照站点公告写死类型会把本来能自动解开的包变成硬失败【实测】 |
| `expected_layers` | 与 `extract.depth_reached` 对照 | 只在报告里标注差异，**不阻断**；阻断只由 `max_depth`（§6.12）负责 |
| `volume_pattern` | 归组时的命名模式提示 | 只影响 A 的归组策略，不影响类型判定 |

这张表是「伪装扩展名 → 真实类型」映射表的**替代品**——那张映射表整张被删掉了，因为 7-Zip 按内容嗅探，扩展名只是第一猜测与警告来源【官方 type.htm】。

#### 3.6.2 `netdisk-profiles.json`

**顶层采样参数（`DOWNLOAD_STABLE` 的时间维度，全 SPEC 唯一登记处）**

| 键 | 定值 | 用途 |
|---|---|---|
| `sample_count` | **3** | G2 的最少采样次数 |
| `quiet_window_seconds` | **120** | G2 的总时长下限（限速停顿 6 秒就能骗过 3×1 秒判据，所以窗口必须以分钟计） |
| `sample_interval_seconds` | **40** | 两次采样的间隔 |
| `grace_seconds_after_final_name` | **8** | G4 的宽限期 |
| `fallback_quiet_seconds` | **300** | `confidence = "unregistered"` 的网盘：G2 窗口放大到这个值 |

采样是**运行内**进行的（一次 A 运行里睡够 `sample_count × sample_interval_seconds`），**已认领**文件可叠加跨运行的历史 `download.gate` 事件作为补充样本；未认领文件只能运行内采样——因为 `_inbox` 里还没有 item 的文件既没有 `item_id`（事件 schema 必填），也没有一个 I5 允许 A 写入的落点。语义与流程见 §5.3。
配套的两个**任务计划设置**（不是 JSON 字段，数值在此登记，由 §5.1 与 §2.3 消费）：**巡检间隔 = 10 分钟**、**`ExecutionTimeLimit` = 30 分钟**；脚本自身的硬超时 `max_run_seconds = 900` 登记在 `guards`（§3.2.3）。

```jsonc
{
  "schema_version": 1,
  "sample_count": 3, "quiet_window_seconds": 120, "sample_interval_seconds": 40,
  "grace_seconds_after_final_name": 8, "fallback_quiet_seconds": 300,

  // 下载客户端 → 临时物模式（形状相同的条目共 9 条，此处示三条）
  "clients": [
    { "id": "chromium", "display": "Chrome / Edge",
      "temp_suffixes": ["*.crdownload"], "temp_name_globs": ["Unconfirmed *.crdownload"],
      "sidecar_pairs": [], "zero_byte_placeholder": false, "preallocates": false,
      "no_temp_in_target": false, "known_false_complete": [],
      "confidence": "official", "evidence": "Chromium 源码 kCrdownloadSuffix / kUnconfirmedFormatSuffix",
      "registered_by": "spec", "registered_at": "2026-09-12" },

    { "id": "baidu", "display": "百度网盘 PC 客户端",
      "temp_suffixes": ["*.baiduyun.p.downloading", "*.baiduyun.downloading"],
      "known_false_complete": [
        { "pattern": "*.baiduyun.p.downloading", "note": "已知下载完成后后缀仍残留，需人工确认" }
      ],
      "confidence": "community" },

    { "id": "quark", "display": "夸克网盘", "temp_suffixes": [], "temp_name_globs": [],
      "confidence": "unregistered", "registered_by": null, "registered_at": null,
      "_note": "【待测】§11：首次使用后由人工观察一次并登记；登记前 G1 对本客户端一律判 skipped" }
  ],

  // 网盘 → 类别与页面规则（语义由 §7.5 定义，本节固定形状）
  "netdisks": [
    { "id": "baidu", "class": "baidu", "host_patterns": ["pan.baidu.com"],
      "client_ids": ["baidu"], "needs_login": true, "requires_transfer": true,
      "download_channel": "desktop_client", "landing_dir": "_inbox",
      "page_rules": { "share_url_rx": "^https://pan\\.baidu\\.com/s/", "access_code_in_url": true },
      "manual_relay_points": ["R-01", "R-02", "R-04"], "confidence": "community" }
    // … quark / aliyun / pan123 / lanzou 同形状
  ]
}
```

**`netdisks[]` 字段**：`id`（`cloud_links[].netdisk` 的取值域）· `class` ∈ `baidu`\|`quark-ali`\|`direct` · `host_patterns[]` · `client_ids[]`（指向 `clients[].id`）· `needs_login` · `requires_transfer`（是否必须先「保存到我的网盘」）· `download_channel` ∈ `desktop_client`\|`browser`（**不在运行期自适应切换**）· `landing_dir`（固定 `_inbox`，见 §2.4）· `page_rules`（对象，键由 §7.5 定义）· `manual_relay_points[]`（指向 §7.7 的接力点编号）· `confidence`。

**`confidence` 是有执行语义的字段，不是注释**（`clients[]` 与 `netdisks[]` 共用同一套取值）：

| 取值 | G1（无同组临时物）怎么用 |
|---|---|
| `official` | 正常求值，`true`/`false` |
| `community` | 正常求值；命中 `known_false_complete` 时不判失败，转人工确认分支 |
| `observed` | 人工观察后登记（`registered_by:"human"` + `registered_at`），等同 `official` |
| `unregistered` | **G1 一律记 `"skipped"`**，且 G2 的窗口放大到 `fallback_quiet_seconds`；报告里给该条目打标 |

这条规则是对「阿里/夸克/123/蓝奏四家查不到权威来源」的**诚实处理**：不知道人家的临时后缀长什么样，就不能把「我没看见临时物」当成证据。代价是延迟而不是正确性——这是有意的取舍。【待测】→ §11：四家 PC 客户端的实际临时后缀与落盘目录。

#### 3.6.3 `container-types.json`：递归白名单 + 扩展名排除表

「7z 能打开它」**不等于**「它是要递归解包的容器」：Windows 上每个 `.exe`/`.dll` 都会被 PE handler 认成归档（handler 表逐字 `PE  exe dll sys  M Z`），`.docx/.xlsx/.apk/.jar/.epub` 全是 `Type = zip`【实测 macOS 25.01 的 handler 表；签名表跨平台共用，可外推】。所以递归必须用**类型白名单 + 扩展名排除表**。

```jsonc
{
  "schema_version": 1,

  // 允许递归的归档级 Type（7z l -slt 的 Type 值，大小写敏感地照抄 7-Zip 的拼写）
  "recurse_into_types": [
    "7z", "zip", "Rar", "Rar5", "tar", "gzip", "bzip2", "xz", "zstd", "Split", "Cab", "arj", "lzh"
  ],

  // 明确不递归的 Type（命中即当普通文件；写出来是为了让报告能解释「为什么没解它」）
  "never_recurse_types": [
    "PE", "Mub", "MachO", "ELF", "Compound", "Nsis", "Iso", "Udf",
    "NTFS", "FAT", "HFS", "Ext", "SquashFS", "wim", "vhd", "vhdx", "vmdk", "qcow", "dmg",
    "Hash", "Base64"
  ],

  // 命中即不递归，**即使 Type 在白名单里**。三份草稿的并集，理由见下表
  "excluded_extensions": [
    ".docx", ".xlsx", ".pptx", ".xls", ".ppt", ".doc", ".epub",
    ".apk", ".ipa", ".jar", ".xpi", ".appx", ".msix", ".crx", ".vsix", ".whl", ".nupkg",
    ".exe", ".dll", ".sys", ".msi", ".iso"
  ],

  // 形态层面就不进自动流程的 Type（判别与处置的权威在 §6.13，本表只登记键与取值）
  "manual_install_types": ["Nsis", "Iso", "Udf", "Compound"],

  // content_files 计数时要排除的「说明类」文件（D10 用）
  "content_file_exclusions": {
    "extensions": [".txt", ".md", ".nfo", ".url", ".html", ".htm", ".log"],
    "names": ["Thumbs.db", "desktop.ini", ".DS_Store"],
    "small_image": { "extensions": [".jpg", ".jpeg", ".png", ".gif", ".bmp"], "max_bytes": 524288 }
  }
}
```

| 排除组 | 为什么 |
|---|---|
| `.docx/.xlsx/.pptx/.xls/.ppt/.doc/.epub` | 文档与电子书本身就是 ZIP 或 Compound。解开会把它拆成碎片，**原文档在产物里不复存在**；若同时命中删源包规则即永久损坏 |
| `.apk/.ipa/.jar/.xpi/.appx/.msix/.crx/.vsix/.whl/.nupkg` | 它们本身就是**交付物**（安卓版游戏、插件、包）。解开等于毁掉可安装/可运行文件，并丢掉 manifest 与签名 |
| `.exe/.dll/.sys` | 每个 PE 都是「归档」。游戏目录里少说几十个；解开得到资源节碎片、depth 计数立刻打爆、产物目录被污染 |
| `.msi/.iso` | 安装器与镜像形态，走 `WAITING_FOR_MANUAL_INSTALL`（§6.13） |

`content_file_exclusions.small_image.max_bytes` = **524288（512 KiB）**，全 SPEC 只有这一个取值与这一条键路径（§6.7 引用，不另立 `content.small_image_bytes`）。
`recurse_into_types` / `never_recurse_types` 的**大小写**必须与目标机 `7z.exe i` 的 handler 表逐字一致：【待测】→ §11（不核对则每个包都可能落 `WAITING_FOR_RULE`，流水线整体停摆）。在核对完成之前，全 SPEC 一律照抄本表的拼写。

**排除表的适用范围（必须写死，否则会和 SFX 打架）**

```
排除表只作用于 **_stg\ 里的解压产物**。
对「批次清单声明的源包」（state.json 的 archives[]）一律不适用 —— 源包走纯内容嗅探。
```

理由：资源站发的源包本来就可能是 SFX `.exe`（`Offset > 0`），或被伪装成 `.mp4`/`.jpg`。对源包套排除表会把 SFX 直接排掉；对产物不套排除表，游戏目录里几十个 `.dll` 会被逐个当成归档去解。**扩展名唯一真正有用的场合：不是识别要解什么，而是排除不该解什么。**

```
ShouldRecurse(file, is_source_archive) -> bool:              # 完整算法见 §6.4
    if not is_source_archive and ext(file) in excluded_extensions:  return false
    t = (7z l -slt file).Type                     # 不给具体 -t，保留嗅探（§6.2.1 的 TypeSpec=none）
    if t == null:                                  return false     # 不是归档
    if t in manual_install_types:                  return false     # → §6.13
    if t in never_recurse_types:                   return false
    if t not in recurse_into_types:                return false     # 白名单之外一律不递归
    return true
```

第三、四条不是冗余：`never_recurse_types` 是**给报告用的解释性清单**（能说出「它是 PE，所以没解」），`recurse_into_types` 才是判据。7-Zip 未来新增 handler 时，新类型自动落到「白名单之外」→ 不递归，这是安全的默认方向。

#### 3.6.4 `engine-rules.json`

本文件只定义 **schema 与匹配语义**；判据表的内容（哪个引擎看哪个文件）的权威是 **§7.6.3**（完整 19 行，含 `UNKNOWN` 兜底），本文件是它的机器可读形式。

```jsonc
{
  "schema_version": 1,
  "rules_version": 1,
  "root_search_depth": 2,          // 从 games\<dirname>\ 往下找「游戏根目录」的最大层数
  "_note": "rank 升序，先匹配先赢。容器标记不参与引擎裁决。",

  "ordered_rules": [
    { "rank": 3, "engine": "RPGMaker_VXAce",
      "match_any": ["RGSS3*.dll", "*.rgss3a", "Data/*.rvdata2"],   // ★ 按版本段前缀，不照抄 SteamDB
      "aux": ["Game.ini"], "exe_convention": ["Game.exe"], "exe_exclude": ["unins*.exe"],
      "mtool": "supported", "confidence": "official" },
    { "rank": 16, "engine": "Unity",
      "match_any": ["UnityPlayer.dll", "UnityEngine.dll", "*_Data/globalgamemanagers"],
      "mtool": "unsupported", "confidence": "official" }
    // … 完整 19 条（含 UNKNOWN 兜底）见 §7.6.3
  ],

  "container_marks": [
    { "id": "NWJS",     "match_any": ["nw.dll", "nw.pak"] },
    { "id": "Electron", "match_any": ["LICENSE.electron.txt"] }
  ],

  "unknown_policy": "WAITING_FOR_MTOOL_RULE"
}
```

- **`rank` 升序求值，先匹配先赢**，匹配到即停。匹配是**大小写不敏感**的相对 `game_root` 路径 glob。
- `mtool` ∈ `supported` \| `unsupported` \| `unknown`。`unsupported` → `TRANSLATION_NOT_APPLICABLE`（终态，视同完成）。
- **`UNKNOWN` 不等于不支持翻译**（Enigma Virtual Box 单 exe 打包的 MV 会判 UNKNOWN 但 MTool 能处理）【社区】→ 落 `WAITING_FOR_MTOOL_RULE`，**不判失败**。
- `container_marks` 只作为**属性**记进 `state.json.engine.container_marks`（**键名与取值大小写以此为准**，`container_attrs` 的拼写不成立），**不参与引擎裁决**——看到 `nw.dll` 只说明外壳是 NW.js，还要看 `www/js` 才知道是 MV 还是 MZ。
- RGSS 一律按**版本段前缀**（`RGSS1*`/`RGSS2*`/`RGSS3*`）匹配，**不照抄 SteamDB 的正则**：它只枚举 5 个具体 DLL 名，漏掉 `RGSS300.dll`/`RGSS202J.dll`，且把所有 RPG Maker 合并成一个标签【官方 SteamDatabase/FileDetectionRuleSets + 实测本机 grep 核对】。
- `target_exe` **不进本文件**。「多 exe 时选 `Game.exe`」不能写成自动规则（论坛真实反例里正确答案是 `開始游戏.exe`）【社区】→ 候选集 + `ambiguous` + 人工确认一次，结果写进 `rules\mtool-profiles\<item-id>.json`（顶层 `schema_version`（J7）、`item_id` 用完整 slug、`game_root` 用相对路径）。

#### 3.6.5 `sevenzip-warnings.json` / `defender-stdout.json` / `threat-allowlist.json`

```jsonc
// sevenzip-warnings.json —— D3「t 的输出无非白名单 Warnings」的唯一输入
{ "schema_version": 1,
  "benign": [ { "id": "W-01", "regex": "(?im)^\\s*Warnings:\\s*\\d+\\s*$",
                "_note": "计数行本身不是内容；真正的判据是下面 fatal 未命中且 exit==0" } ],
  "fatal":  [ { "id": "W-90", "regex": "(?i)can not open file", "reason_code": "WARN_CANNOT_OPEN" },
              { "id": "W-91", "regex": "(?i)there are some data after the end of the payload data",
                "reason_code": "WARN_TRAILING_DATA" } ] }

// defender-stdout.json —— MpCmdRun -DisableRemediation 的 stdout 解析规则
{ "schema_version": 1,
  "threat_line_rx": "(?im)^Threat\\s+(?<name>.+?)\\s+identified\\.?$",
  "clean_rx": "(?im)^Scan finished\\.?$",
  "verdict_map": [ { "name_rx": "(?i)^(Trojan|Backdoor|Ransom):", "verdict": "A" },
                   { "name_rx": "(?i)(HackTool|Crack|KeyGen|Patcher)", "verdict": "B" },
                   { "name_rx": ".*", "verdict": "B" } ],
  "_note": "【待测】§11：stdout 的确切文本。-DisableRemediation 模式下事件日志与 Get-MpThreatDetection 都看不到这次检出（官方明写），stdout 是唯一信号" }

// threat-allowlist.json —— 人工放行的 SHA-256
{ "schema_version": 1,
  "entries": [ { "sha256": "…", "path_hint": "NEKOPARA_v1.02\\Game.exe", "threat_name": "HackTool:Win32/Keygen",
                 "approved_by": "human", "approved_at": "2026-09-12T05:00:00Z", "reason": "汉化补丁误报" } ] }
```

三条契约：

1. `sevenzip-warnings.json` 的判定顺序：`exit != 0` → 直接失败；`exit == 0` 且命中任一 `fatal` → D3 不通过；其余 → 通过。**「有 Warnings」与「exit 1」是两条独立信号，必须分别检查**【实测】。
2. `defender-stdout.json` 的 `verdict_map` **有序，先匹配先赢**，最后一条必须是 `".*" → "B"`（解析不出来就当疑似误报软停，绝不当 clean 放行）。扫描失败按 C 级处理（§6.9）。
3. `threat-allowlist.json` 是 `rules\` 里**唯一允许执行体追加**的文件：B 在人工放行后追加一条（`approved_by` 恒为 `human`）。**它含哈希与威胁名，进 Git 前由人确认**（§9.10）。放行只对**阶段一产物**有效；MTool 的 `version.dll` 哈希随游戏实例变化，SHA-256 白名单原理上无效，只能目录级排除（§6.9.5 / §9.8.4），两者分开写。

#### 3.6.6 `_inbox\claims.json`：人工认领覆盖表

```jsonc
{ "schema_version": 1,
  "claims": [ { "file": "下载 (3).mp4", "item_id": "g0042-nekopara", "part_index": 2,
                "declared_by": "human", "declared_at": "2026-09-12T03:50:00Z",
                "_note": "客户端重名加了 (3)，自动匹配认不出来" } ] }
```

- **人写、A 只读**（A 对它是 `R`，不是 `W`——I5）。它是 A 认领归位的**最高优先级**输入：`claims.json` > `expected_files[].name` 精确匹配 > `source-profiles.filename_patterns[]`。
- 认领成功后 A **不删除也不改写** `claims.json`（I5：A 不删除任何文件）；重复认领靠 `state.json.claimed_files[]` 幂等（同 `(name, bytes)` 即 no-op）。过期条目由人清理。
- 一个文件被多条 claim 指向、或指向不存在的 `item_id` → 落 `rule.required`，**不猜**。

---

### 3.7 密码库 `%LOCALAPPDATA%\GameFlow\vault.clixml`

#### 3.7.1 为什么不放库内

- **目录布局的硬约束**：`.gameflow\` 只存状态与日志，绝不存密码、账号、带 token 的 URL（§2.4）。密码库进库内，这条约束就名存实亡。
- **库会被整卷搬走**：DPAPI blob 一旦离开原机器 + 原用户就永远解不开，而且是**静默失效**（文件还在、格式还对、就是解不开）【社区】。
- **库可能被同步/备份/分享**：密文跟着跑出去，用户还会误以为「备份了库就备份了密码」。
- **身份同域**：`%LOCALAPPDATA%` 天然与「当前登录用户」同域，和 DPAPI 的绑定关系一致。

#### 3.7.2 条目 schema

存储形态：`Export-CliXml` 写出的对象图，顶层是带 `entries` **数组**（不是 Hashtable）的对象。用数组的两个原因：保序（候选列表是**有序**的），以及避开 `ConvertFrom-Json` 的大小写合并陷阱（J4）。

```jsonc
// 这是 clixml 里那个对象的**逻辑形状**（真实文件是 PowerShell 的 CLIXML，不是 JSON）
{
  "schema_version": 1,
  "created_by_sid": "S-1-5-21-…-1001",       // 创建身份，用于自检（§3.7.3）
  "created_at": "2026-09-12T02:40:00.0000000Z",
  "entries": [
    { "key": "site:example-site.org|ookkgg",   // 见下方封闭键空间
      "kind": "site",                          // site | title | batch | item | link
      "candidates": [                          // ★ 有序候选列表，不是单值
        { "secure": "<SecureString>", "byte_exact": true, "confirmed": true,
          "added_at": "…", "added_by": "human", "note": "站点公告第一个密码",
          "hit_count": 7,                      // ★ 命中次数，仅用于排序（§3.7.4）
          "miss_count": 1,                     // ★ 被试过但失败的次数
          "last_used_at": "2026-09-14T03:11:02.0000000Z" },
        { "secure": "<SecureString>", "byte_exact": false, "confirmed": false,
          "added_at": "…", "added_by": "C", "note": "资源页抓取 https://…/12345，待人工确认",
          "hit_count": 0, "miss_count": 0, "last_used_at": null }
      ],
      "_note": "同一上传者有多个等价密码是常态" }
  ]
}
```

**`confirmed` 的执行语义（P5 的落地）**：执行体 C **可以**把资源页抓到的候选写进 vault，但必须标 `confirmed:false`；**未确认的候选对 §6 等同不存在**（B 的候选枚举一律过滤 `confirmed == true`）。人确认一次后置 `true`。「不得持久化」的禁令只适用于**网盘账号密码**，不适用于归档密码候选（§9.2 CU-3 照此改）。

**键空间（封闭，与密码链 P0→P6 一一对应）**

| 键形状 | 对应优先级 | 说明 |
|---|---|---|
| `site:<host>\|<uploader>` | P1 | `host` 小写；`uploader` 原样（可含中文）。`uploader` 为空时写 `site:<host>\|` |
| `title:<NFC 标题>` | P2 | 键做 **NFC 规范化**；**值一个字节都不动** |
| `batch:<batch-id>` | — | 整批共用密码时的便利键；由条目的 `password_ref` 显式指向（走 P0） |
| `item:<item-id>` | — | 单条目专用；同样走 P0 |
| `link:<site>\|<item-id>\|<index>` | — | 网盘**提取码**（`cloud_links[].code_ref` 指向它），不是压缩包密码 |

**键空间是封闭的**：`game:` / `uploader:` 这类写法不存在（§4.3、§5.9 的示例照此改）。批次条目的 `password_ref` 是上面这些键名之一，或字面量 `"none"`（明确声明无密码），或 `null`（未声明，按 P1→P6 依次查）。

> 区分 `"none"` 与 `null` 不是洁癖：裸 `-p`（值为空）会让 7-Zip 切进交互模式并表现成「密码错」【实测】，把「这个包没密码」误分流成 `CORRUPT`。**密码为空时不传 `-p`**，所以必须有一个字段能确定性地说出「这里确实不该传 `-p`」。

**`byte_exact` 的执行语义**

密码是**字节不是字符**：资源站上的「乱码密码」= 正常中文的 UTF-8 字节被按 GBK 显示，两者互不通用；尾随空格有意义（`'pass '` ≠ `'pass'`）；全角 `ｐａｓｓ１２３` ≠ `pass123`【实测，字节层事实，与平台无关】。

| `byte_exact` | 来源 | 运行时行为 |
|---|---|---|
| `true` | 人工逐字节确认，或从已验证成功的候选复制 | **只用原样**这一种形式，**不做等价展开**（§6.3.3 的 `expand()` 必须尊重这个标志） |
| `false` | 网页抓取 / 人工转述 / 从截图敲入 | 允许 ≤4 个等价展开：`[原样, strip(), utf-8 字节按 gbk 解, gbk 字节按 utf-8 解]` 去重（§6.3） |

尝试次数硬上限 = **候选数 × 4**（等价展开有限、确定、可枚举，**这不是暴力破解**）；`byte_exact` 候选只贡献 1 次实际尝试，但仍按 4 计入上限。用尽仍失败 → `WAITING_FOR_PASSWORD`；若失败集中在中文候选且 `env.acp` 显示编码疑点 → `PASSWORD_ENCODING_SUSPECT`。

**UI / 日志的规范化禁令（强制）**

```
存储、读取、比较、展示密码字节的任何环节，禁止：
  ❌ NFKC / NFKD（会把全角折成半角，直接破坏密码）
  ❌ Trim / TrimEnd（尾随空格有意义；strip 只作为等价展开的第 1 项，不是存储形式）
  ❌ 大小写折叠
  ❌ 把密码写进 events.jsonl / state.json / manifest.json / 报告 的任何字段
键名做 NFC 是允许且必需的（否则从网页抓来的 NFD 分解形标题会查不到条目）；值一个字节都不动。
```

#### 3.7.4 候选排序与使用统计（N7b 的落地，全 SPEC 唯一权威）

这是本 SPEC 里**唯一**允许「历史数据影响运行期行为」的地方。它被允许，是因为它通过了 §1.2.5 的三问：**结果不会不同**（同一候选集穷举完的结论完全一样）、**可解释**（计数器可读可 diff 可手动清零）、**可复现**（给定同一份计数器，顺序唯一）。

**排序规则（确定性，全序）**——对 §6.3.4 汇总出的候选集 `C`：

```
sort C by:
  0. priority_tier 升序   # ★ P0=0, P1=1, P2=2, P3=3, P4=4 —— 来源优先级永远压倒统计量
  1. confirmed     降序   # true 在前；false 的对 §6 等同不存在，实际不会进入尝试
  2. hit_count     降序   # 同一优先级层内部，命中多的先试
  3. last_used_at  降序   # 同命中次数时，最近用过的先试（null 排最后）
  4. added_at      升序   # 仍相同则按录入顺序，保证全序、无随机
```

**第 0 条是硬性的，不可与后面几条交换**。它保证「人显式写死的密码（P0）永远第一个试」——统计量只能在**同一优先级层内部**决定先后，永远不能把一个高命中的密码库候选排到用户手写的 P0 前面。

> 这条是 2026-09-14 补的。此前的排序键不含 `priority_tier`，而 §6.3.4 又把 P0…P4 并成一个集合，结果是 P0 可能被一个 `hit_count` 高的 P1 候选挤到后面——与 §6.3.1「优先级链顺序锁定」直接矛盾。**候选的「集合」可以合并，「顺序」不能丢掉来源。**

同一候选被多个来源命中时（例如既在批次 `password_ref` 里、又在 vault 的 `site:` 键里），`priority_tier` 取**最小值**（最高优先级），`hit_count` 等统计量取 vault 条目上的值——去重发生在合并时，不是排序时。

第 4 条是**必需**的：前四条可能全部相等（例如两个新录入、都没用过、同层的候选），没有它排序就不是全序，不同实现可能给出不同顺序，可复现性就破了。

**计数更新时点**（由执行体 B 在一次 `7z t` 探测后立即写，§6.3）：

| 事件 | 动作 |
|---|---|
| 候选 `c` 的某个等价展开使 `7z t` 返回 `exit == 0` | `c.hit_count += 1`；`c.last_used_at := now`；**本次探测立即停止**（不再试后面的候选） |
| 候选 `c` 的全部等价展开都失败 | `c.miss_count += 1`；`last_used_at` **不动**（它的语义是「最近一次*成功*」） |
| 该 key 下所有候选都失败 | 只写 `miss_count`，落 `WAITING_FOR_PASSWORD`，vault 不做任何其它改动 |

**四条硬护栏**（少一条这个功能就会烂掉）：

| # | 护栏 | 不守会怎样 |
|---|---|---|
| **V1** | **排序只在 key 匹配出的候选列表*内部*生效。绝不跨 key 借用高频密码，绝不构造「全局高频候选池」** | 那就是 §6.3.2 硬性禁止的字典爆破，只是穿了统计学的外衣。**这是四条里唯一一条一旦违反就改变安全性质的** |
| **V2** | 排序**不改变候选集**，也**不改变上限**（仍是 `候选数 × 4`，§6.3.4） | 排序一旦能增删候选，N7b 的「结果不会不同」就不成立，它就变成了真正的自适应 |
| **V3** | `hit_count` / `miss_count` **只影响顺序，不影响任何判定**。不得据此自动删除候选、不得据此跳过候选、不得设「命中率低于 X 就不试」 | 自动删除会让一个用了三次没中、第四次才对的密码永久消失，而用户根本不知道发生过什么 |
| **V4** | 计数器**可手动清零**（`Reset-VaultStats`），清零后行为回到「按 `added_at` 升序」这一确定基线 | 没有复位手段的统计量，出问题时无法二分定位 |

**它到底省了什么**：资源站的真实形态是「站点/上传者级统一密码」，一个 key 下常有 2–4 个等价候选。按命中排序后，一个批次里同源的十来个游戏，第二个开始基本都是**第一个候选就中**——省掉的是 `7z t` 的进程启动与归档打开开销，在大包上不是可以忽略的量。**它不省正确性，也不该省。**

#### 3.7.3 创建身份必须与运行身份一致；重装 / 换机 = 密码全丢

`ConvertFrom-SecureString` 在不指定 Key 时用 **DPAPI** 加密【官方】 https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.security/convertfrom-securestring ；`Export-CliXml` 对 `SecureString` 走同一条路径【社区】。「只有同一用户、同一机器能解密」微软该页并没有正面写，属 DPAPI 的众所周知特性【社区】。由此：

1. **vault 必须由将来运行流水线的那个账户创建**；管理员建的，另一个账户解不开。
2. **计划任务不得用 S4U**：S4U 不存储密码、访问不了加密文件【官方】，`SYSTEM`/`LocalService` 同样解不开该用户的 blob。本项目的切分让这条天然成立——A 跑在计划任务里且**永不触碰密码**，需要 vault 的 B/C 都在人的交互会话里跑。`created_by_sid` 就是给 B/C 启动自检用的（§9.11.2 PF-14）。
3. **vault 是缓存，不是唯一副本。** 重装系统、换机器、甚至重建同名用户，vault 都解不开：**文件还在、格式还对、备份也做了——就是解不开**，没有任何报错能提前预警。密码的原始来源是资源站的公开公告（可重查）与人工确认过的候选，后者必须由用户**自行在密码管理器里另存一份**。
4. GameFlow **不提供明文导出命令**（一条 `Export-VaultPlaintext` 会把 DPAPI 的保护意义抹平）。这条留作 §12 的用户拍板项。

【待测】→ §11：DPAPI blob 跨用户/跨机器不可解密的**实际表现**（抛异常还是返回垃圾）。不实测的后果：解不开时的错误信息可能误导成「密码错」，把条目错分流到 `WAITING_FOR_PASSWORD` 而不是「vault 身份不匹配」。

---

### 3.8 「谁写谁读」矩阵

`W` = 写 · `R` = 读 · `A` = 仅追加（append-only）· `N` = 只新建不覆盖 · `—` = 不访问 · `✗` = **明令禁止访问**

| 文件 | 执行体 A（巡检，计划任务） | 执行体 B（作业，交互） | 执行体 C（前台，Computer Use） | 人 |
|---|---|---|---|---|
| `batches\<batch-id>.json` | **R** | **R** | **R + W**：只写 `expected_*` / `cloud_links[]` 的四个 C 字段 / `translation.target_exe` | **W**（经 `New-Batch.ps1`；手工只改 `password_ref`、`abandoned`、`_note`） |
| `.gameflow\events.jsonl` | **A** | **A** | **A** | **A**（经命令，不手编） |
| `.gameflow\state.json` | **R + W** | **R + W** | **R + W** | **R**（可整份删除以触发重建） |
| `.gameflow\manifest.json` | **R + W**（来源、认领证据） | **R + W**（SHA-256、引擎、阶段一快照） | **R**（读 `stage1_snapshot` 做差集） | **R** |
| `_reports\<batch-id>-<seq>.md` | **N**（新建，**不得覆盖已存在的报告**） | **N** | — | **R**（也可用 `Get-BatchReport.ps1` 重新生成） |
| `_inbox\`（已认领文件） | **移出 → `games\<dirname>\`**（同卷 rename，不构成删除语义） | — | 下载落地（经客户端） | **W** |
| `_inbox\claims.json` | **R** | — | — | **W（唯一写者）** |
| `_stg\<8hex>\` | **R**（只在报告里列出孤儿） | **R + W + 删除/回收** | — | **R** |
| `rules\*.json` | **R** | **R**；`threat-allowlist.json` 人工放行后**追加**（唯一例外） | **R** | **W（唯一写者）** |
| `vault.clixml` | **✗ 永不访问** | **R** | **R + W**（P5 候选追加为 `confirmed:false`，需人工确认一次才启用） | **W** |

四条最硬的：

1. **A 的写集合恰为三处**（不变量 I5，§1）：① `games\<dirname>\`（含 `.gameflow\`）；② **新建** `_reports\<batch-id>-<seq>.md`；③ 把 `_inbox\` 中已认领的文件移入 ①。**除此之外 A 不写任何路径，不删除任何文件，不回收 `_stg\`**（只在报告里列出孤儿，回收由 B 做）。
2. **A 直接出对账报告**——它是无人值守的那个执行体；报告若要靠人另跑一个命令才有，「装完就忘了它」这条操作契约就不成立。报告的**去重/摘要（digest）机制留在 A**；`Get-BatchReport.ps1` 的职责是**重新生成 / 回填**，不是「唯一生成者」。
3. **A 对 vault 是 `✗`，不是 `—`。** 代码层面应做到 A 的模块根本不 import vault 模块。
4. **`rules\*.json` 只有人能写**（`threat-allowlist.json` 的人工放行追加除外）。规则被自动修改 = 自适应/学习，与「确定性、静态、人在环内」正面冲突。C 从资源页学到的东西写进 **vault 候选**（`confirmed:false`）或**批次清单的 expected 字段**，不写规则文件。

---

### 3.9 编码与落盘协议

> 这是本项目**最容易造成真实数据损坏**的一条：编码错一次 → 中文/日文游戏名乱码 → 建出乱码目录 → 全盘错位。PowerShell 层面的完整约定（cmdlet 版本差异表、`$OutputEncoding` 与外部程序通信、`-LiteralPath` 强制）见 **§8.8.2 / §8.8.9**；本节只定**数据文件自身的字节契约**。

#### 3.9.1 字节契约

- 本节定义的**全部文件**（`batches\*.json`、`rules\*.json`、`.gameflow\*.json`、`events.jsonl`、`_reports\*.md`）一律 **UTF-8 无 BOM**；`events.jsonl` 的行尾是单字节 `\n`。
- **唯一在 5.1 与 7.x 下字节级一致的读写方式是绕开 cmdlet 编码层**（`Out-File`/`>`/`Set-Content`/`Add-Content`/`-Encoding utf8` 在 5.1 下分别是 UTF-16LE / ANSI / 带 BOM，7.x 下又都是 utf8NoBOM【官方】）：

```powershell
$utf8NoBom = [System.Text.UTF8Encoding]::new($false)      # $false = 不发出 BOM
[System.IO.File]::WriteAllText($tmpPath, $json, $utf8NoBom)                  # 写
$text = [System.IO.File]::ReadAllText($path, $utf8NoBom)                     # 读
if ($text.Length -gt 0 -and $text[0] -eq [char]0xFEFF) { $text = $text.Substring(1) }  # 兜底剥 BOM
```

- 显式剥 BOM 是因为别人（编辑器、Codex）写出来的文件可能带 BOM，我们**必须读得进去**。

#### 3.9.2 落盘协议（`state.json` / `manifest.json` / 批次清单）

```
WriteJsonFile(path, obj):
  1. json = ConvertTo-Json $obj -Depth 10                     # J1；不要 -Compress（这几个文件要人读）
  2. tmp  = "<同目录>\<basename>.tmp-<run_id>"                # ★ 必须同目录、同卷
  3. fs = FileStream(tmp, Create, Write, FileShare::None)
     bytes = UTF8Encoding($false).GetBytes(json)
     fs.Write(bytes); fs.Flush($true); fs.Dispose()           # Flush(true) = FlushFileBuffers
  4. if (不存在 path):  [System.IO.File]::Move(tmp, path)
     else:              [System.IO.File]::Replace(tmp, path, "<path>.bak")   # ★ 保留 backup
  5. 回读 path 并比对 schema_version + 顶层键集合 + 本次改动的字段（J6）
```

**`File.Replace` 的第三个参数绝不传 `$null`**：Win32 `ReplaceFile` 的三种部分失败态（1175/1176/1177）里，**有备份名和没备份名时剩下的文件叫什么名字完全不同**【官方 https://learn.microsoft.com/en-us/windows/win32/api/winbase/nf-winbase-replacefilew 】——保留备份名，恢复逻辑才有确定的判据；不保留就要靠猜。逐条判读表见 §8.8.4。

**SPEC 全文不得出现「状态写入是原子的」**：`ReplaceFile` 自称「把几步合进一个函数」并列举了三种部分失败中间态，`MoveFileEx` 全文无 atomic 字样，`REPLACEFILE_WRITE_THROUGH` 官方写明「not supported」【官方】。**真正的崩溃安全来自不变量 I2**（`events.jsonl` 是唯一真相且只追加，`state.json` 是可随时删掉重建的缓存），不是来自追求单次替换的原子性——后者在 Windows 上根本做不到。

#### 3.9.3 崩溃恢复 `Recover`（确定性，无猜测）

**`.bak` 不是恢复源，只作诊断/取证。** 它按定义是**上一版** `state.json`，用它恢复会跳过 journal 重放、得到一个 `last_event_seq` 落后于日志的快照，而删除 gate（I3）读的正是这个快照里的 `verified` / `consumed_by`——恢复结果将依赖两个来源，违反「唯一真相」。**恢复路径唯一：`state.json` 不可解析就从 `events.jsonl` 全量重放。**（§8.8.4 的 2a 同此删除。）

```
Recover(itemDir, actor):
  0. 取得 events.jsonl 的独占句柄          # 恢复必须在锁内做；抢锁失败 = LOCKED_BY_ANOTHER_RUN
  # ── 第一段：重建。**所有执行体（A / B / C）都做** ────────────────────
  1. if state.json 存在且可解析:
         if state.last_event_seq < 日志最后一条 seq:  Rebuild()   # 快照落后于日志
     elif state.json 缺失:      Rebuild() -> state.rebuilt{reason_code:"missing"}
     else:                      Rebuild() -> state.rebuilt{reason_code:"unparsable"}
     # ReadEvents 中间行解析失败 -> 落 JOURNAL_CORRUPT（§3.3.5），到此为止，不做任何破坏性动作
  # ── 第二段：清理。**仅执行体 B 做**（A 受 I5 约束，永不删除）──────────
  2. if actor == B:
         删除本 item 目录下所有 *.tmp-*（`.bak` 保留，供诊断）
         回收孤儿 _stg\<8hex>\：attempt_id 不在 state.extract.attempts 里的
             -> 每回收一个写一条 staging.discarded{reason_code:"crash_orphan"}
     if actor == A:
         只在对账报告里列出上面这两类残留，**不动它们**
```

#### 3.9.4 同卷约束

`File.Replace` 在源与目标**位于不同卷时直接抛异常**【官方 https://learn.microsoft.com/en-us/dotnet/api/system.io.file.replace 】；Win32 侧同样要求备份、被替换、替换三个文件**全部同卷**【官方】。由此两条硬性布局约束（与 §2.4 一致，Preflight 的 PF-05 会拦）：

1. `.gameflow\` 必须与游戏库同卷——它本来就在 `games\<dirname>\` 下，自动满足；**但 `.tmp-*` 必须写在目标文件的同一个目录**，不能图省事写到 `%TEMP%`。
2. 库级中转区 `_stg\<8hex>\` **必须与游戏库同卷**：跨卷会让 staging→落地退化成「复制 + 删除」，代价与风险都上升，而 D12（移动成功且体积一致）会变成昂贵得多的检查。同卷也是 §6.12.5 余量公式成立的前提（staging 与落地占的是同一块空间，**不需要**预留两倍）。

#### 3.9.5 路径长度断言

每次建目录 / 建文件前，断言完整路径长度未超预算（§2.5 的实算表：`D:\GameLib\games\` = 17 字符，建目录实际上限 **248 = MAX_PATH − 12**，不是 260）。超限时的处置**不是等 API 报错**，而是：建批次阶段 → 触发 §3.2A 第三步的 `dirname` 降级；降级后仍超限 → 落停机态 **`PATH_BUDGET_EXCEEDED`**（§4.3）。

`env.long_paths` 只作为**上下文记录**，**不作为放宽预算的依据**——【待测】→ §11：`LongPathsEnabled=1` 时 `powershell.exe`/`pwsh.exe` 对 >260 路径的实际行为。不实测就只能按「从源头压短路径」走，这也是本 SPEC 的默认取舍。

---

### 3.10 本节产生的【待测】项（全部进 §11）

| 待测项 | 不测的后果 | 阻塞谁 |
|---|---|---|
| 目标机 ACP 实际值与常见日文标题的 ACP 往返率（PF-08） | 只能保守地一律降级，全库目录名不可读 | §3.2A、§2.5 |
| `Unblock-File` 是否改动主数据流的 `LastWriteTime` | `stage1_snapshot` 的时点定错 → 阶段二差集把整库都算成「变了」 | §3.5、§7.6 |
| `7z.exe i` 的 handler 名逐字拼写（大小写） | `recurse_into_types` 匹配不上 → 每个包都落 `WAITING_FOR_RULE`，流水线整体停摆 | §3.6.3、§5.4、§6.4 |
| 阿里/夸克/123/蓝奏四家客户端的临时后缀与落盘目录 | 这四家只能靠通用判据，`DOWNLOAD_STABLE` 更晚触发（延迟，不是误判） | §3.6.2、§5.3 |
| `MpCmdRun -DisableRemediation` 的 stdout 确切文本 | 写不出 `defender-stdout.json` 的解析器 → 全部条目按 C 级软停 | §3.6.5、§6.9 |
| NTFS 上 <4 KB 的单次写被强杀是否真会撕裂半行 | 封口逻辑无法验证；若从不撕裂则 `journal.torn_line_sealed` 是死代码 | §3.3.4 |
| DPAPI blob 跨用户/跨机器不可解密的实际表现 | 错误信息误导成「密码错」，条目错分流 | §3.7.3 |
| `LongPathsEnabled=1` 时 PS 对 >260 路径的实际行为 | 只能按「从源头压短路径」走（本 SPEC 的默认取舍，不阻塞设计） | §3.9.5、§2.5 |


---

## §4 状态机：状态集、迁移表、停机态手册、幂等与崩溃恢复

本节是 GameFlow 的**执行契约**。§5 / §6 / §7 三个执行体只负责实现本节定义的迁移：它们可以决定「怎么判」，但**不得新增状态、不得新增迁移、不得改变解除路径**。本节的编号（`M##` 主线迁移、`H##` 停机进入、`R##` 解除）是全文引用锚点，也是事件里 `transition_id` 的取值域。

**权威边界**：状态名、停机态名单、不变量 I1–I5 见 §1，本节原样使用。**事件 schema、`state.json` / `manifest.json` 字段名、规则文件键名一律以 §3 为准**（§4.2.1）；删除判据 D1–D12 见 §6.8；下载判据 G1–G6 见 §5.3；护栏阈值见 §6.12；7-Zip 调用封装见 §6.2.1；魔数表见 §5.4.4；不进自动流程的形态判别见 §6.13。本节只写**语义与判定顺序**，不重列这些定义。

### 4.0 三条贯穿全节的原则

- **P-1 状态只由事件推进。** `events.jsonl` 是唯一真相（I2）；`state.json` 是 fold 的结果，任何时刻可删可重建。**重建过程绝不读文件系统**——一旦重建时去"看一眼盘上现在是什么样"，幂等就没了（I3）。
- **P-2 停机态是一等公民，不是异常。** 每个停机态都必须在进入的那一刻，把「解除后回到哪」写死进事件（`resume_allowed`），而不是等解除时再推断。人和脚本读同一个字段。
- **P-3 停机不删文件（I1）。** 唯一例外是库级中转区 `D:\GameLib\_stg\<8hex>\` 下的内容——它是可重造的中间物，不是用户数据。删除动作必须带 `_stg\` 前缀断言，见 §4.5.3。

---

### 4.1 完整状态图

```
 图例   ══▶ 主线迁移(M##)      ──▶ 进入停机态(H##)     ◀╌╌ 解除(R##，人触发)
        〔A〕巡检  〔B〕作业  〔C〕前台  〔人〕人工     ◆ 终态

                         ● 〔人/C〕New-Batch.ps1
                         ║ M01
                         ▼
                  ┌─────────────┐
                  │   PLANNED   │
                  └──────╥──────┘
                         ║ M02 〔A〕item 目录 + .gameflow\ 就位
                         ▼
                  ┌──────────────────────┐   H01 ──▶ WAITING_FOR_VOLUMES  ◀╌╌ R01
                  │  AWAITING_DOWNLOAD   │   H02 ──▶ WAITING_FOR_PASSWORD ◀╌╌ R02
                  │  〔A〕每次巡检重入     │   H03 ──▶ BLOCKED_RESOURCE     ◀╌╌ R11
                  └──────────╥───────────┘   H27 ──▶ WAITING_FOR_RULE      ◀╌╌ R05
                             ║ M03 〔A〕G1∧G2∧G3∧G4∧G5∧G6 + 认领归位
                             ▼
                  ┌──────────────────────┐   H04 ──▶ WAITING_FOR_RULE           ◀╌ R05
                  │   DOWNLOAD_STABLE    │   H05 ──▶ WAITING_FOR_MANUAL_INSTALL ◀╌ R06
                  │                      │   H06 ──▶ FILENAME_ENCODING_RISK     ◀╌ R07
                  │                      │   H07 ──▶ WAITING_FOR_PASSWORD       ◀╌ R02
                  │                      │   H26 ──▶ BLOCKED_RESOURCE           ◀╌ R11
                  └──────────╥───────────┘   H28 ──▶ PATH_BUDGET_EXCEEDED       ◀╌ R16
                             ║ M04 〔A〕只读深预检全通过
                             ▼
                  ┌──────────────────────┐   H08 ──▶ BLOCKED_RESOURCE ◀╌╌╌╌╌╌╌╌ R11
                  │     PREFLIGHT_OK     │
                  │  （B 的入口，等人触发）│
                  └──────────╥───────────┘
                             ║ M05 〔B〕人/Codex 触发 Invoke-Work，取得 item 锁
                             ▼
                  ┌──────────────────────┐   H09 ──▶ WAITING_FOR_PASSWORD       ◀╌ R02
                  │      EXTRACTING      │   H10 ──▶ PASSWORD_ENCODING_SUSPECT  ◀╌ R03
                  │  （递归解包内循环在   │   H11 ──▶ EXTRACT_FAILED_AMBIGUOUS   ◀╌ R04
                  │    态内完成，不迁移） │   H12 ──▶ BLOCKED_RESOURCE           ◀╌ R11
                  │                      │   H13 ──▶ CORRUPT                    ◀╌ R12
                  │                      │   H14 ──▶ WAITING_FOR_RULE           ◀╌ R05
                  │                      │   H15 ──▶ WAITING_FOR_MANUAL_INSTALL ◀╌ R06
                  └──────────╥───────────┘   H29 ──▶ PATH_BUDGET_EXCEEDED       ◀╌ R16
                             ║ M07 〔B〕本 attempt 再无未处理白名单容器
                             ▼
                  ┌──────────────────────┐   H16 ──▶ CORRUPT              ◀╌╌╌╌ R12
                  │      VERIFYING       │   H17 ──▶ EXTRACTED_NO_CONTENT ◀╌╌╌╌ R08
                  │  D1–D12 逐条判定      │   H18 ──▶ BLOCKED_RESOURCE     ◀╌╌╌╌ R11
                  │  ★ Defender 预检在本  │   H19 ──▶ WAITING_FOR_RULE     ◀╌╌╌╌ R05
                  │    态内、落地之前完成  │   H20 ──▶ THREAT_SUSPECTED     ◀╌╌╌╌ R09
                  └──────────╥───────────┘   H21 ──▶ THREAT_CONFIRMED ◆（硬停）
                             ║ M08 〔B〕D1–D12 全通过 ∧ Defender 裁决已落盘为放行
                             ▼                  ∧ staging 已落地并 Flush
                  ┌──────────────────────┐
                  │      EXTRACTED       │  ← 入态后动作：受 gate 的源包删除（§4.4）
                  │  （Defender 已在上游   │     —— 删除发生在 Defender 放行**之后**，
                  │    VERIFYING 内放行）  │        这正是 I1「停机不删文件」成立的前提
                  └──────────╥───────────┘
                             ║ M09 〔B〕删除账已平 + 整树 Unblock-File + stage1_snapshot 已写
                             ▼
                  ┌──────────────────────┐
                  │       SCANNED        │
                  └───╥──────────────╥───┘
        M11 mode=none ║              ║ M10 mode≠none
                      ║              ▼
                      ║   ┌──────────────────────────┐  H22 ──▶ WAITING_FOR_MTOOL_RULE ◀╌ R13
                      ║   │ READY_FOR_TRANSLATION    │
                      ║   └───╥──────────────────╥───┘
                      ║  M13  ║                  ║ M12 〔C〕人显式触发前台会话
                      ║  引擎  ║                  ▼
                      ║  不支持║       ┌──────────────────────┐ H23 ──▶ MTOOL_FAILED ◀╌╌ R14
                      ║       ║       │      TRANSLATING     │ H24 ──▶ WAITING_FOR_MTOOL_RULE
                      ║       ▼       └──────────╥───────────┘
                      ║  ┌──────────────────────┐║ M14 〔C〕人确认翻译已加载
                      ║  │TRANSLATION_NOT_      ││
                      ║  │  APPLICABLE       ◆  ││
                      ║  └──────────────────────┘│
                      ▼                          ▼
                  ┌───────────────────────────────────┐
                  │            COMPLETE  ◆            │
                  └───────────────────────────────────┘

  任一非终态 ──H25──▶  ABANDONED ◆      （只由人显式声明，见 §4.8）
  任一停机态 ──R15──▶  ABANDONED ◆      （唯一例外：JOURNAL_CORRUPT，见 §4.3.17）
  任一状态   ──H30──▶  JOURNAL_CORRUPT  （重放器判定；resume_allowed = []）

  运行期状况（不落盘、不进状态机）：LOCKED_BY_ANOTHER_RUN   见 §4.6.4
```

图里没有画的三件事，在这里补齐：

1. **`EXTRACTING` 的递归是态内循环。** 「成功解开一个白名单容器 = depth+1」（计层口径见 §6.4，护栏阈值见 §6.12）产生的是**事实事件**（§3.3.2），不是迁移事件。状态机里看不到深度，深度只存在于 `state.json` 的 `extract.depth_reached` 与 journal 里。这样 `max_depth` 的调整不会动状态机。
2. **`AWAITING_DOWNLOAD` 是自反的。** A 每一轮巡检都会重新求值 G1–G6；不通过就什么都不做（**不写迁移事件**，采样仍按 §5.3 写 `download.gate`），通过才写 M03。见 §4.4.4。
3. **终态共四个**：`COMPLETE` · `TRANSLATION_NOT_APPLICABLE` · `THREAT_CONFIRMED` · `ABANDONED`。前两个视同完成，后两个视同关闭。共同性质：**任何执行体见到它们一律跳过，不做任何文件系统动作。** `JOURNAL_CORRUPT` 在行为上同样无回归路径，但它是重放判定的产物而不是流程终点，单列在 §4.3.17。

---

### 4.2 完整迁移表

#### 4.2.1 事件与字段：以 §3 为准

本节**不定义任何 schema**。事件 schema、`kind` 闭集、公共字段（含 `trigger`）、`state.json` 字段名，全部见 §3.3 与 §3.4。本节只规定三件事：

1. **状态变更的唯一通道是 `state.entered`**（§3.3.2）。迁移信息进 payload：`{from_state, to_state, transition_id}`，`transition_id` 的取值域 = 本节的 `M##` / `H##` / `R##` 编号（闭集）。本节表格里那一列 `#` 就是它。
2. **进入停机态 = `state.entered`（`transition_id = H##`）+ 一条 `halt.entered`**；解除 = `state.entered`（`transition_id = R##`）+ 一条 `halt.cleared`。`halt.entered` 必带 `reason_code`（闭集见表 B）、`resume_allowed[]`、`cleared_by_allowed[]`；`halt.cleared` 必带 `resolved_by`。这四个字段的 schema 落位在 §3.3.2 与 §3.4.1 的 `halt` 对象。
3. **事实事件不改状态**，只写 `state.json` 的派生字段。本节引用到的事实事件（`run.started` / `run.ended` / `download.gate` / `download.claimed` / `archive.tested` / `archive.verified` / `verify.completed` / `staging.promoted` / `staging.discarded` / `defender.scanned` / `note.human` / `state.rebuilt`）一律以 §3.3.2 的拼写与必填键为准。

> `run.started` 与 `run.ended` 成对出现：**有 started 无 ended = 上次在这个 item 上被打断**，这是 §4.5.1 崩溃探测器的唯一信号，也是本节对 §3 事件闭集的硬依赖。

#### 4.2.2 表 A · 主线迁移（13 行，M06 空置）

| # | from → to | 触发条件（可编程判据） | 执行体 | 前置断言（不满足则拒绝写事件） |
|---|---|---|---|---|
| M01 | ∅ → `PLANNED` | `New-Batch.ps1` 成功写入批次条目 | 人 / C | `item.id` 匹配 §3.2.2 的正则且批次内唯一；`dirname` 已过 **§3.2A** 的规范化链 + ACP strict 往返检查（不可表示或路径预算超限 → 降级 ASCII slug，**降级原因必须记录**）；`games\<dirname>\` 尚无其它活跃 item |
| M02 | `PLANNED` → `AWAITING_DOWNLOAD` | A 首次巡检到该条目 | A | `games\<dirname>\.gameflow\` 已建且与 `_stg` 同卷（`File.Replace` 跨卷**抛异常**【官方】https://learn.microsoft.com/en-us/dotnet/api/system.io.file.replace）；`events.jsonl` 可追加 |
| M03 | `AWAITING_DOWNLOAD` → `DOWNLOAD_STABLE` | `G1∧G2∧G3∧G4∧G5∧G6` 全部成立（判据与采样机制见 §5.3）——「文件大小连续 N 次不变」单独使用是**会稳定产出错误结论**的判据，六种独立机理都能击穿它【社区·浏览器/下载器源码与行为证据】；且认领归位（`_inbox` → `games\<dirname>\` 的同卷 `File.Move`）对全部预期文件成功，写 `download.claimed` | A | 归位后**复检文件仍存在**（G4）；G5 的 `7z l -slt` **不带密码**（无人值守的 A 永不触碰密码）；每个包**单独调用**一次 7z——`7z t a.7z b.7z` 的第二个参数是 a.7z **内部的文件名过滤器**，会 exit 0 而一个包都没测【实测·macOS 7zz 25.01；可外推：命令行参数语义是跨平台公共层】 |
| M04 | `DOWNLOAD_STABLE` → `PREFLIGHT_OK` | 只读深预检全通过：① 分卷先分组再嗅探 ② 首卷 `Type` ∈ 容器白名单（§3.6.3） ③ `l -slt` 的 `Path` 无穿越 ④ 无 reparse 声明 ⑤ `sum(Size)` / 条目数 / 压缩比 / 磁盘余量四项预算均在 §6.12 的护栏内 ⑥ 魔数形态不是安装器/镜像（§6.13） ⑦ ZIP 的 bit11 检查通过 ⑧ 最长内部条目路径在 MAX_PATH 预算内（§2.5） | A | **全程只读**：不改名、不复制、不带密码；调用必须走 §6.2.1 的封装并取 `-TypeSpec none`——带 `-t*` 只能看到 Split 外壳、拿不到内层 `Size` 表，体积预算与穿越预检**全部落空**（§6.2.7）；给具体类型（`-t7z`/`-tzip`）会关掉内容嗅探，把本可自动处理的情况变成 exit 2 硬失败【官方 type.htm（随官方 tarball 分发）+ 实测·macOS】；头加密包（`l` exit 255）不走这条，走 H07 |
| M05 | `PREFLIGHT_OK` → `EXTRACTING` | 人 / Codex 触发 `Invoke-Work.ps1 -BatchId <slug>`（入口契约见 §8.2.3），并取得该 item 的独占锁 | B | 新 `attempt_id` 已生成（格式见 §4.4.3）；`_stg\<8hex>\` 已建且为空；§6.12.5 的余量公式已复算 |
| M07 | `EXTRACTING` → `VERIFYING` | 本 attempt 的 staging 内**再无未处理的白名单容器**（按内容嗅探，非扩展名），且 `extract.depth_reached ≤ max_depth` | B | 每层解开都已写 §3.3.2 的解包事实事件；无任一 7z 调用 `exit != 0`；计层口径统一按 §6.4——**Split 外壳同样计层**，没有例外 |
| M08 | `VERIFYING` → `EXTRACTED` | `D1–D12` **全部**成立（§6.8）**且 Defender 裁决已落盘为放行**（`defender.scanned.level == clean`，或 B 级已经人放行，见 R09） | B | **Defender 预检钉死在本态之内、`staging.promoted` 之前**（§6.1.2 / §9.8）：扫描对象必须还在 `_stg\`，否则「只排 `_stg` + `-DisableRemediation`」这套组合失去支点；`archives[].verified:true` + `archives[].consumed_by` + `staging.promoted` **均已 `Flush($true)` 落盘**，再写本迁移（D12）。`FileStream.Flush(Boolean)` 清空所有中间文件缓冲、对应 Win32 `FlushFileBuffers`【官方】https://learn.microsoft.com/en-us/dotnet/api/system.io.filestream.flush |
| M09 | `EXTRACTED` → `SCANNED` | 三条**全部**成立：① 源包删除账已平——每个 `consumed_by` 非空的包都有删除事实且 `result ∈ {deleted, already_absent, retained}` ② 整树 `Unblock-File` 完毕 ③ `manifest.stage1_snapshot` 已写 | B | `Unblock-File` 官方明文幂等、对未标记文件无副作用【官方】；**MOTW 不得作为安全边界**；本步不再做任何威胁裁决（已在 M08 之前完成） |
| M10 | `SCANNED` → `READY_FOR_TRANSLATION` | 批次条目 `translation.mode != none` | B | 引擎判定事实已落盘；`engine.game_root` 指向含引擎标记文件的那一层，**不假定等于 `games\<dirname>\`**（判定表见 §7.6） |
| M11 | `SCANNED` → `COMPLETE` | 批次条目 `translation.mode == none` | B | 同 M10 的 manifest 完整性要求 |
| M12 | `READY_FOR_TRANSLATION` → `TRANSLATING` | 人显式触发前台会话（C），且 `mtool-profiles\<item-id>.json` 存在且 `target_exe` 非 `ambiguous` | C | MOTW 已 `Unblock-File`（**是前置条件不是收尾**）——忘了会让 Computer Use 撞上 SmartScreen 的前台模态框而卡死【社区】；若 `version.dll` 已存在 → 判定已处理，**不重复注入**，直接走 M14 |
| M13 | `READY_FOR_TRANSLATION` → `TRANSLATION_NOT_APPLICABLE` | `engine ∈ {Unity, Godot, Unreal}`——MTool 官网与 Patreon 两个独立来源的引擎清单都没有这三者【官方】 | B / C | `engine != UNKNOWN`——`UNKNOWN` 走 H22，**不判不支持** |
| M14 | `TRANSLATING` → `COMPLETE` | 人在前台确认翻译已加载 | C | **不得依赖产物文件存在性**作为判据——MTool 是注入式，产物 `.bin` 落盘位置未知【社区】【待测】；`mtool_build` 作为运行时字段记进 `state.json` |

> M06 号位空置：`EXTRACTING` 的递归层推进不是迁移（见 §4.1 补充 1）。编号保留空位，便于日后插入而不动既有引用。

#### 4.2.3 表 B · 停机进入迁移（H01–H30）

`halt.entered` 的 `reason_code` 是**闭集代码**，报告与 `-Retry` 都按它分支。同一停机态可由不同 `reason_code` 从不同状态进入，`resume_allowed` 因 `reason_code` 而异——所以它**写在事件里**，不靠解除时推断（P-2）。

| # | from → to | `reason_code`（可编程判据） | 执行体 | 前置断言 |
|---|---|---|---|---|
| H01 | `AWAITING_DOWNLOAD` → `WAITING_FOR_VOLUMES` | `VOL_SEQUENCE_GAP` 卷号 1..N 有空洞 · `VOL_TAIL_EQUAL_SIZE` 末卷与前卷字节数完全相等（疑似还有一卷） · `VOL_COUNT_MISMATCH` 实际卷数 ≠ 批次 `expected_parts` | A | G1–G4 已通过（下载看起来已停），否则应留在 `AWAITING_DOWNLOAD` 继续等；`expected_parts = null` 时本条不可判，报告必须打出「无预期卷数，缺分卷不可自动检测」（§3.2.5） |
| H02 | `AWAITING_DOWNLOAD` → `WAITING_FOR_PASSWORD` | `PWD_HEADER_ENCRYPTED_UNATTENDED`：G5 的 `7z l -slt` 返回 **exit 255**（头加密 `-mhe`：拿到密码前 `l` 直接 exit 255，`Size`/`Path`/条目数一个都拿不到）【官方 password.htm + 实测·macOS；可外推：头加密是 7z 格式层特性】 | A | A **永不触碰密码**；`resume_allowed=[DOWNLOAD_STABLE]`，语义级校验交给带密码的 B；头加密下四项预算全部失效，只剩 §6.12 的运行期监控 + 绝对上限 |
| H03 | `AWAITING_DOWNLOAD` → `BLOCKED_RESOURCE` | `PERMISSION_DENIED`：G3 的独占打开抛 `UnauthorizedAccessException`（权限/只读） | A | **必须与 `IOException` 分开处理**：`IOException` = 还被占用，继续等；把 `UnauthorizedAccessException` 当成"还在下载"会永久卡死【社区】；两种异常在 Windows 上的实际类型与 HResult【待测】 |
| H04 | `DOWNLOAD_STABLE` → `WAITING_FOR_RULE` | `RULE_VOLUME_RENAME_REQUIRED` 疑似分卷组但命名不成 `.NNN` 序列（`7z l` exit 2 且同前缀多文件、除末个外等大）——伪装命名的分卷组完全打不开，只改首卷也不行，必须整组补齐 `.NNN`；`.NNN` **之前**的扩展名无所谓（`game.mp4.001` 正常工作）【实测·macOS；可外推：7-Zip 靠文件名序列寻找同伴卷】 · `RULE_PREFIX_OVER_8MB` 单次 `-t*:s64m` 重试仍 exit 2（默认扫描窗口 8 MB【官方 type.htm】） · `RULE_UNKNOWN_CONTAINER` `Type` 既不在容器白名单也不在扩展名排除表（§3.6.3） | A | **绝不批量改名试错**；原始文件永不改名（改名只允许发生在 `_stg\` 的副本上，且须由批次条目显式声明） |
| H05 | `DOWNLOAD_STABLE` → `WAITING_FOR_MANUAL_INSTALL` | `FORM_INSTALLER` / `FORM_DISK_IMAGE` / `FORM_UNKNOWN_MZ`：按 §6.13 的判别规则命中"不进自动流程的形态"，魔数与偏移取 §5.4.4 的表 | A | **分卷必须先分组再做类型判定**——实测中间卷会被 `file` 误判成「DOS executable」【实测·macOS；Windows 上的误判文本可能不同，机理相同】；SFX 当归档解，**绝不执行** |
| H06 | `DOWNLOAD_STABLE` → `FILENAME_ENCODING_RISK` | `ZIP_BIT11_ZERO_NONASCII`：纯字节解析 ZIP 中央目录，general purpose bit 11 = 0 且文件名字段含 ≥ `0x80` 字节 | A | 判定**不依赖任何 7-Zip 开关**；只对 ZIP 生效——7z 头强制 UTF-16LE、RAR5 强制 UTF-8，格式层没有这个问题【社区】 |
| H07 | `DOWNLOAD_STABLE` → `WAITING_FOR_PASSWORD` | `PWD_NOT_IN_CHAIN`：预检读到 `Encrypted = +`，而密码链 P0–P4 全部未命中（P5 属 C，P6 即本态） | A | A 只判"有没有可用引用"，**不解密、不尝试** |
| H08 | `PREFLIGHT_OK` → `BLOCKED_RESOURCE` | `INSUFFICIENT_SPACE` 余量公式为负 · `BOMB_SUSPECTED` `ratio ≥ ratio_block` **且** `sum(Size) > 5 GiB` · `SIZE_CAP_EXCEEDED` 超总量或单文件上限 · `PATH_TRAVERSAL_DETECTED` `l -slt` 的 `Path` 含绝对路径/`..`/盘符（阈值与公式一律取 §6.12） | B | `ratio ≥ ratio_warn` **只记日志，绝不单独阻断**——实测合法的纯文本汉化补丁 7z 就有 133x，真炸弹 1029x，中间没有干净分界【实测】；`ratio` 公式是 `sum(条目 Size) / 归档级 Physical Size`，**不能用 `sum(Packed Size)`**（7z 默认 solid，一个 block 只有第一个条目带 Packed Size）【实测·macOS；可外推：字段语义是格式层】 |
| H09 | `EXTRACTING` → `WAITING_FOR_PASSWORD` | `PWD_ALL_CANDIDATES_FAILED`：P0–P4 给出的候选（含 ≤4 个等价展开）全部返回 exit 2 / 255 | B | **探测只用 `7z t`，不写盘**（§6.3.7 的 PW-4）；确需落盘的例外（分卷重建）在 `_stg\` 内做并即时清空——ZipCrypto 有 **1/256 假通过**，会解出垃圾文件，"解出几个文件"永远不能当密码正确的证据【实测（Info-ZIP/Python 验证；机理属 ZIP 格式层，可外推）】；AES（`compress_type==99`）有 HMAC，不受影响；尝试次数上限见 §6.3.4 |
| H10 | `EXTRACTING` → `PASSWORD_ENCODING_SUSPECT` | `PWD_CJK_ALL_EXPANSIONS_FAILED`：候选含非 ASCII 字节，4 种等价展开全败，且检出 ACP 相关疑点 | B | 事件必须带 `acp`、`beta_utf8` 与**已试展开的形态描述**（不写密码明文）；UI/日志**不得做任何 Unicode 规范化**——NFKC 会折全角（`ｐａｓｓ１２３ ≠ pass123`），尾随空格有意义（`'pass ' ≠ 'pass'`）【实测】 |
| H11 | `EXTRACTING` → `EXTRACT_FAILED_AMBIGUOUS` | `RAR4_AMBIGUOUS_FAILURE`：魔数判定为 RAR4（§5.4.4），且 `7z t/x` 失败 | B | 只对 **RAR4** 成立（WinRAR 的 exit 11 = 密码错**仅对 RAR5 有效**【官方·WinRAR 手册；本轮未留存 URL，见 §4.10】）；RAR5 失败按 H09 / H13 分流 |
| H12 | `EXTRACTING` → `BLOCKED_RESOURCE` | `STAGING_SIZE_CAP` 运行期 staging 体积超绝对上限 · `DEPTH_LIMIT` `depth > max_depth` · `INSUFFICIENT_SPACE` 解压途中余量跌破 headroom | B | 停机**不删 staging**（留给人看），但该 attempt 被标 `superseded`，回收由下一次运行做（§4.5.3） |
| H13 | `EXTRACTING` → `CORRUPT` | `TEST_FAILED_NO_PASSWORD` 非加密包 `7z t` **`exit ∈ {1, 2}`** 且已排除密码因素 · `TEST_FAILED_PASSWORD_CONFIRMED` 密码已在同组另一包上验证成功后仍失败（非 RAR4） · `WARNINGS_NOT_WHITELISTED` exit 0 但有非白名单 `Warnings:`（白名单见 `rules\sevenzip-warnings.json`） | B | 判据是 `exit == 0` **严格相等**（`1` = Warning 有文件被静默跳过，本项目一律当失败；`2` 含密码错；`255` 在加密包 stdin 不可用时真实含义是「需要密码」）【官方 exit_codes.htm + 实测·macOS】；**`exit 7` = 我们自己拼错参数 → 立即中止整个 B 运行、不落任何停机态**；**`exit 8` = 内存不足 → 按 §6.2.2 重试一次，仍失败落 `WAITING_FOR_RULE`，不是 `CORRUPT`**；"有 Warnings" 与 "exit 1" 是两条独立信号，必须分别检查【实测·macOS】 |
| H14 | `EXTRACTING` → `WAITING_FOR_RULE` | `RULE_UNKNOWN_CONTAINER` 内层解出未知容器 · `RULE_VOLUME_RENAME_REQUIRED` 内层是伪装命名的分卷组 · `RULE_OOM_RETRY_FAILED` `7z` exit 8 重试后仍失败 | B | 同 H04：改名只在 `_stg\` 的副本上、只补 `.NNN`、只按显式声明 |
| H15 | `EXTRACTING` → `WAITING_FOR_MANUAL_INSTALL` | `FORM_INSTALLER` / `FORM_DISK_IMAGE` / `FORM_UNKNOWN_MZ`：内层解出 §6.13 列举的形态 | B | 同 H05 |
| H16 | `VERIFYING` → `CORRUPT` | `ENTRY_COUNT_MISMATCH` D5 失败 · `ENTRY_RECON_FAILED` D6 失败 · `CRC_RECHECK_FAILED` D7 的 ZipCrypto 全量 CRC 重读失败（判据定义见 §6.8） | B | **exit 0 且零文件有四种情形**（过滤器无匹配 / 多归档参数误用 / 只含空目录 / 尾部垃圾），必须靠 `Files: N > 0` + 与 `l -slt` 条目数比对才能识别【实测·macOS】 |
| H17 | `VERIFYING` → `EXTRACTED_NO_CONTENT` | `NO_CONTENT_FILES`：D10 失败——`content_files == 0`（排除表与小图片阈值取 §3.6.3 的 `content_file_exclusions`） | B | 常见成因是"这一层只是密码说明包，真包还没下"，所以 `resume_allowed` 含 `AWAITING_DOWNLOAD` |
| H18 | `VERIFYING` → `BLOCKED_RESOURCE` | `REPARSE_POINT_IN_PRODUCT`：D9 失败，产物含 symlink / junction / reparse point | B | 7-Zip 对路径穿越的拦截**完全静默**（exit 0、零 warning、零 stderr），流水线拿不到信号，必须自己扫 `l -slt` 的 `Path`【实测·macOS】；**永远不要用 `-snld20`**——实测它真的创建了逃逸符号链接且 exit 0【实测·macOS；Windows 的 symlink/junction 语义不同，需复验】 |
| H19 | `VERIFYING` → `WAITING_FOR_RULE` | `PROMOTE_NAME_COLLISION`：staging 顶层名与 item 目录内已有的源包名相交，且同名目标已存在但 size 不同 | B | 碰撞检测必须在移动**之前**做；`-LiteralPath` 全程（规范化名可能含 `[ ]`） |
| H20 | `VERIFYING` → `THREAT_SUSPECTED` | `DEFENDER_LEVEL_B` 疑似误报 · `DEFENDER_SCAN_FAILED`（C 级，按 B 处理） | B | **预检在 `VERIFYING` 之内、落地之前**：产物还在 `_stg\`，源包**一个都没删**——这是 A/B 级都能"留证"的唯一时点（§9.8.2）；**不要"扫到就停"**（破解/汉化补丁误报是常态）；`-DisableRemediation` 下**事件日志和 `Get-MpThreatDetection` 都看不到这次检出，stdout 是唯一信号**【官方】https://learn.microsoft.com/en-us/defender-endpoint/command-line-arguments-microsoft-defender-antivirus；stdout 的具体格式【待测】——不实测就写不出解析器 |
| H21 | `VERIFYING` → `THREAT_CONFIRMED` | `DEFENDER_LEVEL_A`：明确恶意 | B | 硬停终态，`resume_allowed = []`；**不删任何文件**（I1）——由于本迁移在删除动作之前，源包与产物同时留在盘上，留证成立 |
| H22 | `READY_FOR_TRANSLATION` → `WAITING_FOR_MTOOL_RULE` | `ENGINE_UNKNOWN` · `EXE_AMBIGUOUS` 候选集 > 1 · `MTOOL_UI_CHANGED` | B / C | `UNKNOWN ≠ 不支持翻译`（Enigma Virtual Box 单 exe 打包的 RPG Maker MV 就判 UNKNOWN 而 MTool 能处理）【社区】；"多 exe 时选 `Game.exe`" **不得写成自动规则**（论坛真实反例里正确答案是 `開始游戏.exe`）【社区·论坛一手】 |
| H23 | `TRANSLATING` → `MTOOL_FAILED` | `MTOOL_LOAD_FAILED`：人在前台观察到翻译未加载 | C | 判据是**人的观察**，不是产物文件存在性 |
| H24 | `TRANSLATING` → `WAITING_FOR_MTOOL_RULE` | `MTOOL_UI_CHANGED`：前台流程与 profile 不符 | C | 结果要写回 `mtool-profiles\<item-id>.json`，人工确认一次形成新规则 |
| H25 | 任一非终态 → `ABANDONED` | `HUMAN_ABANDON` | 人 | 只由专用命令触发（§4.8）；`resume_allowed = []` |
| H26 | `DOWNLOAD_STABLE` → `BLOCKED_RESOURCE` | A 在只读预检里检出：`PATH_TRAVERSAL_DETECTED` · `INSUFFICIENT_SPACE` · `BOMB_SUSPECTED` · `SIZE_CAP_EXCEEDED`（与 H08 同一组 `reason_code`，只是执行体与时点不同） | **A** | A **只判不动**：不删、不改名、不解压；`resume_allowed=[DOWNLOAD_STABLE]`（清盘/放行后由 B 重走 M04） |
| H27 | `AWAITING_DOWNLOAD` → `WAITING_FOR_RULE` | `RULE_NETDISK_TEMP_SUFFIX_RESIDUE`：网盘客户端完成后残留临时后缀（典型是百度网盘），G1 因此永不成立（模式表见 §3.6.2） | A | 人工去后缀是文件系统动作，**不由 A 执行**（I5）；`resume_allowed=[AWAITING_DOWNLOAD]`，`cleared_by_allowed` 含 `A`——判据完全在 A 的职权内（下一轮重新求值 G1–G6） |
| H28 | `DOWNLOAD_STABLE` → `PATH_BUDGET_EXCEEDED` | `PATH_BUDGET_ENTRY_TOO_LONG`：`l -slt` 里最长内部条目路径 + 库根 + `dirname` 超出 §2.5 的 MAX_PATH 预算，且 `dirname` 已经是 §3.2A 的降级 ASCII slug | A | 与 `WAITING_FOR_RULE` 的区别是**人工动作不同**：这里要改配置（换更短库根 / 人工指定更短目录名），不是补一条规则；`resume_allowed=[DOWNLOAD_STABLE]` |
| H29 | `EXTRACTING` → `PATH_BUDGET_EXCEEDED` | `PATH_BUDGET_ENTRY_TOO_LONG`：内层解出的条目路径超预算（头加密包拿不到条目表，只能在这里才发现） | B | staging 保留给人看；`resume_allowed=[PREFLIGHT_OK]` |
| H30 | 任一状态 → `JOURNAL_CORRUPT` | `JOURNAL_UNPARSABLE`：重放时命中**不可解析的中间行**（尾部半行不算，见 §4.5.1），或 `events.jsonl` 整体不可读/缺失 | 重放器（A/B/C 任一） | 判定是 journal 字节的**纯函数**，每次重放同一结论——这正是它能落盘而不是运行期状况的理由；`resume_allowed = []`，**连 R15 都不适用**（见 §4.3.17） |

#### 4.2.4 表 C · 解除迁移矩阵（R01–R17，机器视角）

`halt.cleared` 的写入必须通过两道断言：`to_state ∈ resume_allowed`（取自对应的 `halt.entered`），且 `resolved_by ∈ cleared_by_allowed`。**不允许脚本自选目标状态。** 人的操作手册见 §4.3。

| # | 停机态 | `resume_allowed`（严格闭集） | `cleared_by_allowed` | 判据是否可自动复核 |
|---|---|---|---|---|
| R01 | `WAITING_FOR_VOLUMES` | `[AWAITING_DOWNLOAD]` | `B` | ✅ 全自动复核（重新数卷 + 等大校验） |
| R02 | `WAITING_FOR_PASSWORD` | H02/H07 → `[DOWNLOAD_STABLE]`；H09 → `[PREFLIGHT_OK]` | `B` | ✅ 复核 vault 是否命中；命中即试 |
| R03 | `PASSWORD_ENCODING_SUSPECT` | `[PREFLIGHT_OK]` | `B` | ⚠️ 人必须先提供新候选，脚本不自造 |
| R04 | `EXTRACT_FAILED_AMBIGUOUS` | `[PREFLIGHT_OK, AWAITING_DOWNLOAD]` | `B` | ❌ 人必须先选一支（换密码 / 重下） |
| R05 | `WAITING_FOR_RULE` | H04/H14 → `[PREFLIGHT_OK]`；H19 → `[VERIFYING]`；H27 → `[AWAITING_DOWNLOAD]` | `B`；H27 另含 `A` | ❌ 人必须先写规则（H27 是例外：人去掉后缀即可，判据自动复核） |
| R06 | `WAITING_FOR_MANUAL_INSTALL` | `[EXTRACTED]` | `B` | ❌ 人必须先装完并声明 |
| R07 | `FILENAME_ENCODING_RISK` | `[PREFLIGHT_OK, EXTRACTED, AWAITING_DOWNLOAD]` | `B` | ❌ 人必须先选处置 |
| R08 | `EXTRACTED_NO_CONTENT` | `[EXTRACTED, AWAITING_DOWNLOAD, PREFLIGHT_OK]` | `B` | ❌ 人必须先判定 |
| R09 | `THREAT_SUSPECTED` | `[VERIFYING, THREAT_CONFIRMED]` | `人`（裁决）+ `B`（写入） | ❌ 安全裁决永不自动。回到 `VERIFYING` 而不是 `SCANNED`：放行后还要走完 D1–D12 与落地 |
| R10 | `THREAT_CONFIRMED` | `[]` | — | — 硬停，只能 R15 |
| R11 | `BLOCKED_RESOURCE` | H03 → `[AWAITING_DOWNLOAD]`；H26 → `[DOWNLOAD_STABLE]`；H08/H12/H18 → `[PREFLIGHT_OK]` | `B` | ✅ 余量/体积类可自动复核；`PATH_TRAVERSAL_DETECTED`、`REPARSE_POINT_IN_PRODUCT` ❌ 必须人工放行 |
| R12 | `CORRUPT` | `[AWAITING_DOWNLOAD]` | `B` | ⚠️ 需要人先替换文件，之后判据自动复核 |
| R13 | `WAITING_FOR_MTOOL_RULE` | `[READY_FOR_TRANSLATION, TRANSLATION_NOT_APPLICABLE]` | `C` | ❌ 人必须先写 profile |
| R14 | `MTOOL_FAILED` | `[READY_FOR_TRANSLATION, TRANSLATION_NOT_APPLICABLE]` | `C` | ❌ 人必须先判断 |
| R15 | 任一停机态（`JOURNAL_CORRUPT` 除外） | `[ABANDONED]` | `人` | ❌ |
| R16 | `PATH_BUDGET_EXCEEDED` | H28 → `[DOWNLOAD_STABLE]`；H29 → `[PREFLIGHT_OK]` | `B` | ⚠️ 人必须先改配置；改完后预算算术可自动复核 |
| R17 | `JOURNAL_CORRUPT` | `[]` | — | — 无解除路径，只能人工重新立项（§4.3.17 / §4.8.3） |

> **A 只在一条路径上写 `halt.cleared`**：`RULE_NETDISK_TEMP_SUFFIX_RESIDUE`（R05 的 H27 分支）——它的解除判据就是重新求值 G1–G6，完全在 A 的职权内。其余一律由 B 或 C 写：A 无密码、无提权、不删除（I5）。A 对其它停机态的唯一贡献是在对账报告里标注「判据现在看起来已满足，可 `-Retry`」——把复核结果**呈现**给人，而不替人解除。

---

### 4.3 停机态手册（runbook，共 17 个）

> 读法：第 1 列是**机器怎么判定要停**，第 2 列是**你现在具体要做什么**，第 3 列是**做完后流水线从哪继续**，第 4 列是**谁把它从停机态里拽出来**。
> 全局保证，每张表都成立、不再重复：**停机不删任何文件；所有源包、分卷、已解出的产物原样保留。**

#### 4.3.1 WAITING_FOR_PASSWORD — 缺密码，或全部候选用尽

| 触发条件（可编程判据） | 你要做什么 | 做完回到 | 谁能解除 |
|---|---|---|---|
| `PWD_HEADER_ENCRYPTED_UNATTENDED`（H02）：A 的 `7z l -slt` 返回 exit **255**，头加密包的确定信号——无人值守拿不到任何条目信息【官方 password.htm + 实测·macOS】。 | 把密码写进 vault：`Add-VaultEntry -Key 'item:<item-id>'` 或 `'site:<host>|<uploader>'`（键空间闭集见 §3.7.2）。**不做任何 Unicode 规范化**——NFKC 会折全角，直接破坏密码；尾随空格有意义（`'pass ' ≠ 'pass'`）【实测】。 | `DOWNLOAD_STABLE`（由 B 用 `7z t -p` 补做语义级完成判定） | **B**（人先录密码，再 `-Retry`） |
| `PWD_NOT_IN_CHAIN`（H07）：`Encrypted = +` 且 P0–P4 全未命中。报告会列出**已查过的键**。 | 同上；或在批次条目里填 `password_ref`（P0，优先级最高）；或让 C 去资源页抓（P5：C 可以把候选写进 vault 并标 `confirmed:false`，**未确认的候选对 §6 等同不存在**，需人工确认一次）。 | `DOWNLOAD_STABLE` | **B**；P5 路径由 **C** 取回、仍由 B 推进 |
| `PWD_ALL_CANDIDATES_FAILED`（H09）：候选（含 ≤4 个等价展开）全部 exit 2 / 255，且尝试已达 §6.3.4 的上限。 | 看报告里的**已试候选数**与**卷集完整性结果**，判断是"密码不对"还是"包不全"。前者补 vault，后者走 `WAITING_FOR_VOLUMES` 的做法。 | `PREFLIGHT_OK` | **B** |

**不要做的事**：不要让脚本从游戏名/域名/文件名推导密码，不要上字典，不要把 README 里"读懂"的一段话当密码——硬禁令见 §6.3。

#### 4.3.2 PASSWORD_ENCODING_SUSPECT — 中文密码全败且检出编码疑点

| 触发条件（可编程判据） | 你要做什么 | 做完回到 | 谁能解除 |
|---|---|---|---|
| `PWD_CJK_ALL_EXPANSIONS_FAILED`（H10）：候选含非 ASCII 字节，`[原样, strip(), utf-8字节按gbk解, gbk字节按utf-8解]` 四种展开去重后全部失败。**密码是字节不是字符**：资源站上的「乱码密码」= 正常中文的 UTF-8 字节被按 GBK 显示，两者互不通用【实测】。 | 报告会同时显示**四种展开的字节形态**和**当前系统 ACP / Beta UTF-8 状态**。① 确认资源站上看到的"乱码密码"是不是被网页按错误代码页渲染过——若是，直接从原始字节重新录入；② 检查是否全角（`ｐａｓｓ１２３ ≠ pass123`）【实测】；③ 检查是否有尾随空格。把**字节精确**的新候选写进 vault（标 `byte_exact:true` 的候选 §6 只试原样一种形式）。 | `PREFLIGHT_OK` | **B**（人必须先提供新候选；脚本不自造第五种展开） |

> 为什么不自动扩大展开集：等价展开必须**有限、确定、可枚举**——这是它"不是暴力破解"的唯一依据。展开集一旦可变，这条界线就没了。

#### 4.3.3 EXTRACT_FAILED_AMBIGUOUS — RAR4 专用：工具层分不清密码错与数据损坏

| 触发条件（可编程判据） | 你要做什么 | 做完回到 | 谁能解除 |
|---|---|---|---|
| `RAR4_AMBIGUOUS_FAILURE`（H11）：魔数判定为 RAR4（§5.4.4）且解压失败。**WinRAR 官方文档明写 exit 11（密码错）只对 RAR5 有效**【官方·WinRAR 手册；本轮未留存 URL，见 §4.10】，所以在 RAR4 上这两种原因在工具层不可区分。 | 报告会同时给出**已试候选数**与**卷集完整性结果**。<br>① 卷集不完整 → 先补卷（当作 `WAITING_FOR_VOLUMES` 处理）；<br>② 卷集完整 → 大概率是密码，补 vault；<br>③ 两者都排除 → 重下该包。 | 选 ①③ → `AWAITING_DOWNLOAD`；选 ② → `PREFLIGHT_OK` | **B**（人必须先选一支） |

#### 4.3.4 WAITING_FOR_VOLUMES — 卷集不连续 / 末卷等大疑似还有一卷

| 触发条件（可编程判据） | 你要做什么 | 做完回到 | 谁能解除 |
|---|---|---|---|
| `VOL_SEQUENCE_GAP`（H01）：卷号 `1..N` 有空洞。 | 报告会精确列出**缺哪几号**。回网盘补下缺的卷，落到 `_inbox`（全局下载目录），A 下一轮自动认领归位。 | `AWAITING_DOWNLOAD` | **B**（判据全自动复核，人只需触发 `-Retry`；也可等 A 下一轮巡检提示） |
| `VOL_TAIL_EQUAL_SIZE`（H01）：末卷与前一卷字节数**完全相等**。分卷除末卷外等大，所以末卷等大意味着极可能还有下一卷没到。 | 回资源页确认总卷数；若确实只有 N 卷（恰好整除），在批次条目里显式填 `expected_parts = N` 关掉这条警报（**明确无分卷用 `expected_parts = 1` 表达，不用 0**）。 | `AWAITING_DOWNLOAD` | **B** |
| `VOL_COUNT_MISMATCH`（H01）：实际卷数 ≠ 批次声明的 `expected_parts`。 | 要么补下缺卷，要么改正 `expected_parts`（前提是你确认资源页写错了）。 | `AWAITING_DOWNLOAD` | **B** |

> 为什么这三条必须停人：缺中间卷、缺末卷、单文件截断三种情况的 7z stderr **一字不差**（都是 `Unexpected end of archive`）【实测·macOS；可外推：该文本由 7-Zip 自身产生】，工具层给不出区分信号，只能靠我们自己的卷集连续性 + 等大校验，而"补一卷"这个动作只有人能做。

#### 4.3.5 WAITING_FOR_RULE — 需要人工声明一条规则

| 触发条件（可编程判据） | 你要做什么 | 做完回到 | 谁能解除 |
|---|---|---|---|
| `RULE_VOLUME_RENAME_REQUIRED`（H04/H14）：`7z l` exit 2，且同目录同前缀多文件、除最后一个外字节数完全相等 → 疑似伪装命名的分卷组。**这是本项目唯一真正需要改名的情形**（C7）。 | 在批次条目里显式声明分卷重建规则（源文件名序 → `.001/.002/…`）。声明后 B 会把**整组复制到 `_stg\`** 再补 `.NNN`，**原始文件永不改名**。`.NNN` 之前的扩展名无所谓，`game.mp4.001` 正常工作——资源站教程里"先改 zip 再改 7z.001"中间那步多余【实测·macOS】。 | `PREFLIGHT_OK` | **B** |
| `RULE_PREFIX_OVER_8MB`（H04）：单次 `-t*:s64m` 重试后仍 exit 2。默认签名扫描窗口是前 8 MB，超出就需要显式放大。 | 在批次条目里声明 `prefix_scan_mb`（取值域 {0, 16, 64}，B 转成 `-TypeSpec *:s{N}m`）。**只允许通配形式**——给具体类型会关掉嗅探，把本来能自动处理的情况变成硬失败【实测·macOS + 官方 type.htm】。封装签名见 §6.2.1。 | `PREFLIGHT_OK` | **B** |
| `RULE_UNKNOWN_CONTAINER`（H04/H14）：`Type` 既不在 `container-types.json` 的 `recurse_into_types`，也不在 `excluded_extensions`（§3.6.3）。 | 看报告给出的 `Type` 与前 16 字节十六进制，决定：加进递归白名单 / 加进排除表 / 判为安装器（改走 `WAITING_FOR_MANUAL_INSTALL`）。**「7z 能打开它」不等于「它是容器」**——Windows 上每个 `.exe`/`.dll` 都是 PE 归档，`.docx/.apk/.jar` 都是 `Type = zip`【实测·macOS；可外推：handler 集合是 7-Zip 自带的】。 | `PREFLIGHT_OK` | **B** |
| `RULE_OOM_RETRY_FAILED`（H14）：`7z` exit 8（内存不足），按 §6.2.2 重试一次后仍失败。 | 关掉占内存的程序重试，或在批次条目里为该 item 降低并发/字典要求。**不要当成 `CORRUPT`**——包本身没问题。 | `PREFLIGHT_OK` | **B** |
| `RULE_NETDISK_TEMP_SUFFIX_RESIDUE`（H27）：网盘客户端下载完成后残留临时后缀，G1 永不成立（模式表见 §3.6.2）。 | 手动去掉残留后缀（或在 `netdisk-profiles.json` 里登记这个后缀模式，让 G1 认识它）。 | `AWAITING_DOWNLOAD` | **A**（下一轮巡检重新求值 G1–G6）或 **B**（`-Retry`） |
| `PROMOTE_NAME_COLLISION`（H19）：staging 顶层名与 item 目录里已有的源包重名，且同名目标已存在但体积不同。 | 人工看一眼是哪两个文件撞了，改批次条目里的落地规则，或先把冲突的源包挪走。 | `VERIFYING` | **B** |

#### 4.3.6 WAITING_FOR_MANUAL_INSTALL — 不进自动流程的形态

判别规则与形态清单以 **§6.13** 为准，魔数与偏移见 **§5.4.4**；本表只写人工动作。

| 触发条件（可编程判据） | 你要做什么 | 做完回到 | 谁能解除 |
|---|---|---|---|
| `FORM_INSTALLER`（H05/H15）：NSIS / Inno / `setup.exe`。 | 自己双击装到 `games\<dirname>\` 下（**不要装进 `Program Files`，也不要装进 OneDrive 同步目录**——前者写不进 `version.dll`，后者有社区证据导致 MTool inject 失败）【社区】。装完运行解除命令并声明安装落地目录。 | `EXTRACTED`，且该 item 被强制打上 `retention.keep_archives = true` | **B** |
| `FORM_DISK_IMAGE`（H05/H15）：ISO9660 等镜像。 | 自己挂载/解出到 `games\<dirname>\`，然后同上声明。 | 同上 | **B** |
| `FORM_UNKNOWN_MZ`（H05/H15）：offset 0 = `4D 5A` 且不是可识别的 SFX 归档。 | 自己判断这是什么。**流水线绝不执行任何 MZ 文件**——SFX 一律当归档解，不当程序跑。（真实 SFX `.exe` 的 `7z l -slt` 输出【待测】：Windows 上 `.exe` 是合法 PE，PE handler 可能抢先解资源节而不是尾随负载。） | 同上 | **B** |

> **为什么解除后源包不再自动删除**：D2/D4/D5 从未通过，12 条与条件里有 4 条根本没被求值过。C4 的自动删除只在 D1–D12 全通过时成立（§6.8），人工安装这条路上它不成立——所以 `keep_archives` 是**强制**的，不是可选的。要删自己删。

#### 4.3.7 FILENAME_ENCODING_RISK — ZIP 文件名可能落地成乱码

| 触发条件（可编程判据） | 你要做什么 | 做完回到 | 谁能解除 |
|---|---|---|---|
| `ZIP_BIT11_ZERO_NONASCII`（H06）：纯字节读 ZIP 中央目录，general purpose bit 11 = 0 且文件名字段含 ≥ `0x80` 字节。判定不依赖任何 7-Zip 开关。【社区：bit 11 = language encoding flag 是 PKWARE APPNOTE 的广泛共识，本轮未取到官方原文 URL】【实测·macOS：样本可判】 | 三选一，**必须显式选**：<br>① `accept_as_is` — 接受可能的乱码文件名（老游戏常见，很多引擎按字节打开自己的资源）；<br>② `manual_extract` — 你用 GUI 工具（有代码页选项【社区】）自己解到 `games\<dirname>\`，然后声明"已人工解压"；<br>③ `replace_archive` — 换一个包重下。 | ① → `PREFLIGHT_OK`；② → `EXTRACTED`（`keep_archives = true`）；③ → `AWAITING_DOWNLOAD` | **B** |

> 为什么不自动用 `-mcp=932` 修：官方 `method.htm` 明写 `-m` 只适用于 `a/h/d/rn/u`（**不含 `x/l/t`**），macOS 实测无效，社区说法自相矛盾——三方矛盾的东西不能进主路径。它是 `【待测】`，见 §11。

#### 4.3.8 EXTRACTED_NO_CONTENT — 解出来没有游戏内容

| 触发条件（可编程判据） | 你要做什么 | 做完回到 | 谁能解除 |
|---|---|---|---|
| `NO_CONTENT_FILES`（H17）：D10 失败，`content_files == 0`（排除表与小图片阈值见 §3.6.3）。 | 报告会列出解出来的全部文件名。最常见的真相是：**这一层只是"密码/说明包"，真正的游戏包还没下**。<br>① 真包没下 → 回资源页补下；<br>② 解出来的密码文件正是下一层要用的（P4）→ 声明"继续"，B 会把它接进密码链（回喂上限见 §6.7.2）；<br>③ 确认这就是全部内容（例如纯文本补丁）→ 声明"接受"。 | ① → `AWAITING_DOWNLOAD`；② → `PREFLIGHT_OK`；③ → `EXTRACTED` | **B** |

#### 4.3.9 THREAT_SUSPECTED — Defender 检出，疑似误报

三级分流（A/B/C 级）的判定口径见 §6.9 与 §9.8；本表只写人工动作。**注意时点**：本态发生在 `VERIFYING` 之内，产物还在 `_stg\`、源包一个都没删，所以"留证"是真的成立的。

| 触发条件（可编程判据） | 你要做什么 | 做完回到 | 谁能解除 |
|---|---|---|---|
| `DEFENDER_LEVEL_B`（H20）：检出且归入 B 级（疑似误报）。 | **别急着放行，也别急着删。** 破解/汉化补丁的误报是常态【社区】，但不是全部。① 看报告里的威胁名与命中文件；② 拿命中文件的 SHA-256 去第三方多引擎核对；③ 判为误报 → 放行（按 **SHA-256** 加白名单，阶段一产物哈希固定）；④ 判为真 → 升级。 | ③ → `VERIFYING`（放行后继续走完 D1–D12 与落地）；④ → `THREAT_CONFIRMED` | **人**裁决 + **B** 写入 |
| `DEFENDER_SCAN_FAILED`（H20，C 级）：扫描本身失败（超时、提权不足、`MpCmdRun` 不可用）。**按 B 处理，不按 clean 处理。** | 先修扫描本身：`MpCmdRun` **需提权**【官方】https://learn.microsoft.com/en-us/defender-endpoint/command-line-arguments-microsoft-defender-antivirus，确认这一步不是跑在 `-RunLevel Limited` 的计划任务里。受限权限下的失败形态【待测】。修好后重扫。 | `VERIFYING`（重扫通过时） | **人** + **B** |

> 两个必须知道的事实：① `-DisableRemediation` 模式下，**事件日志和 `Get-MpThreatDetection` 都看不到这次检出**，`stdout` 是唯一信号【官方·同上 URL】；② MTool 注入的 `version.dll` **哈希随游戏实例变化**，SHA-256 白名单对它原理上无效，只能目录级排除（`_stg\` + MTool 安装目录）【社区·Kaspersky 版主确认，一手】——这两条白名单机制**分开写、不要混**。

#### 4.3.10 THREAT_CONFIRMED — 人工确认为真，硬停

| 触发条件（可编程判据） | 你要做什么 | 做完回到 | 谁能解除 |
|---|---|---|---|
| `DEFENDER_LEVEL_A`（H21）：Defender 判定明确恶意；或从 `THREAT_SUSPECTED` 经人工确认升级。 | **流水线到此为止。** 它不会删你的文件（I1），也不会再碰这个 item。**源包与产物都还在**（本态在删除动作之前），你自己决定隔离/删除/上报。处理完把条目标成 `ABANDONED` 让它离开"需要你处理"清单。 | `resume_allowed = []`，**无回归路径**；只能 → `ABANDONED` | 无人能解除；只有**人**能 `-Abandon` |

> 为什么不给回归路径：安全裁决一旦做出就不该被一条 `-Retry` 推翻。要重来，新建一个 item.id 重走全流程（§4.8），历史不改写。

#### 4.3.11 BLOCKED_RESOURCE — 护栏拦截

覆盖五类：**磁盘余量不足 / 体积或压缩比护栏 / 深度上限 / 路径穿越 / 产物含 reparse point、symlink、junction**。阈值与公式一律取 §6.12，本表只写人工动作。

| 触发条件（可编程判据） | 你要做什么 | 做完回到 | 谁能解除 |
|---|---|---|---|
| `INSUFFICIENT_SPACE`（H08/H12/H26）：§6.12.5 的余量公式为负。 | 清盘，或把库根挪到更大的卷（注意：必须与 `_stg` 同卷）。判据全自动复核。 | A 检出 → `DOWNLOAD_STABLE`；B 检出 → `PREFLIGHT_OK` | **B**（人清完盘触发 `-Retry` 即可） |
| `BOMB_SUSPECTED`（H08/H26）：`ratio ≥ ratio_block` **且** `sum(Size) > 5 GiB`。`ratio ≥ ratio_warn` 只记日志不阻断——实测合法的纯文本汉化补丁 7z 就有 133x，真炸弹 1029x，**中间没有干净分界**【实测】。头部声明的大小**会撒谎**，真正的护栏是运行期 staging 体积监控 + 绝对上限 + 磁盘余量。 | 看报告里的 `sum(Size)` 与归档级 `Physical Size`，判断是不是真的异常。确认无害 → 在批次条目里为该 item 声明护栏例外（**只对这一个 item 生效，不要改全局阈值**）。 | 同上 | **B** |
| `SIZE_CAP_EXCEEDED` / `STAGING_SIZE_CAP`（H08/H12/H26）：超总量上限、单文件上限，或运行期 staging 体积超绝对上限。 | 同上：确认后为该 item 抬高上限。**头加密包只有这一道护栏**（体积/条目/穿越/压缩比四项预算全部失效）【官方+实测·macOS】，抬高前务必想清楚。 | 同上 | **B** |
| `DEPTH_LIMIT`（H12）：`extract.depth_reached > max_depth`。计数口径是"成功解开一个白名单容器 = depth+1"（§6.4），不是"调用了几次 7z"。 | 看报告里的解包链路（每一层的 `Type` 与文件名）。链路合理 → 为该 item 抬高 `max_depth`；链路是自嵌套循环 → 按炸弹处理，别抬。 | `PREFLIGHT_OK` | **B** |
| `PATH_TRAVERSAL_DETECTED`（H08/H26）：`l -slt` 的 `Path` 含绝对路径 / `..` / 盘符。**7-Zip 自己会拦，但完全静默**（exit 0、零 warning、零 stderr），所以必须我们自己扫【实测·macOS】。落地位置在 Windows 上必然与 macOS 不同【待测】。 | **默认答案是放弃这个包**。确实要处理的话，人工用 GUI 解到隔离目录自己检查。没有"放行后自动解"这条路。 | A 检出 → `DOWNLOAD_STABLE`；B 检出 → `PREFLIGHT_OK`（仅当人显式放行）；或 `ABANDONED` | **人**必须显式放行，`B` 写入 |
| `REPARSE_POINT_IN_PRODUCT`（H18）：D9 失败，产物含 symlink / junction / reparse point。**绝不使用 `-snld20`**（实测真的会创建逃逸符号链接且 exit 0）【实测·macOS；Windows 的 symlink/junction 语义不同，需复验】。 | 同上：默认放弃。 | 同上 | **人** + **B** |
| `PERMISSION_DENIED`（H03）：G3 的独占打开抛 `UnauthorizedAccessException`（不是 `IOException`）。 | 检查文件/目录 ACL、只读属性、是否被同步客户端接管。修好后重跑。 | `AWAITING_DOWNLOAD` | **B** |

#### 4.3.12 CORRUPT — 完整性测试失败且已排除密码因素

| 触发条件（可编程判据） | 你要做什么 | 做完回到 | 谁能解除 |
|---|---|---|---|
| `TEST_FAILED_NO_PASSWORD`（H13）：非加密包 `7z t` `exit ∈ {1, 2}`。（`exit 7` 是我们自己拼错参数 → 中止运行，不落停机态；`exit 8` 走 `WAITING_FOR_RULE`。） | 重下这个包。报告会给出**包名 + 已记录的 SHA-256**，重下后可以直接比对是不是同一个文件。 | `AWAITING_DOWNLOAD` | **B**（人替换文件后触发，判据自动复核） |
| `TEST_FAILED_PASSWORD_CONFIRMED`（H13）：同组另一个包已用同一密码验证成功，本包仍失败（且不是 RAR4）。 | 同上：重下本包。 | `AWAITING_DOWNLOAD` | **B** |
| `WARNINGS_NOT_WHITELISTED`（H13）：`exit == 0` 但 stdout 有非白名单 `Warnings:`（典型是尾部垃圾）。**"有 Warnings" 与 "exit 1" 是两条独立信号**【实测·macOS】。 | 看具体 warning 文本。确认无害 → 把该模式加进 `rules\sevenzip-warnings.json`；否则重下。 | `AWAITING_DOWNLOAD` | **B** |
| `ENTRY_COUNT_MISMATCH`（H16）：D5 失败——`Files: N` 为 0，或与 `l -slt` 的文件条目数不符。**exit 0 且零文件有四种成因**：过滤器无匹配 / 多归档参数误用 / 只含空目录 / 尾部垃圾【实测·macOS】。 | 报告会说明命中哪一种。「多归档参数误用」是我们自己的 bug（`7z t a.7z b.7z` 第二个参数是**内部过滤器**不是第二个包【实测·macOS】）→ 报 bug；其余重下。 | `AWAITING_DOWNLOAD` | **B** |
| `ENTRY_RECON_FAILED` / `CRC_RECHECK_FAILED`（H16）：D6 逐条目对账失败，或 D7 的 ZipCrypto 全量 CRC 重读失败。ZipCrypto 有 **1/256 假通过**，会解出垃圾文件【实测（Info-ZIP/Python 验证；机理属 ZIP 格式层，可外推）】——这就是为什么只有 ZipCrypto 包必须额外做全量 CRC（D7）。 | 换密码重试（1/256 假通过意味着"解出来了"不代表密码对），或重下。 | `AWAITING_DOWNLOAD` | **B** |

#### 4.3.13 WAITING_FOR_MTOOL_RULE — 新引擎 / 多 exe 歧义 / 界面变化

| 触发条件（可编程判据） | 你要做什么 | 做完回到 | 谁能解除 |
|---|---|---|---|
| `ENGINE_UNKNOWN`（H22）：引擎判定表（有序匹配、先匹配先赢，见 §7.6）没有命中。**`UNKNOWN` 不等于不支持翻译**——Enigma Virtual Box 单 exe 打包的 RPG Maker MV 就判 UNKNOWN，而 MTool 能处理【社区】。 | 自己开一次游戏看是什么引擎，把判定特征补进 `engine-rules.json`（NW.js / Electron 只作为**属性**记录，不参与引擎裁决）。 | `READY_FOR_TRANSLATION`，或确认 MTool 不支持 → `TRANSLATION_NOT_APPLICABLE` | **C** |
| `EXE_AMBIGUOUS`（H22）：`engine.game_root` 下有多个候选 exe。**"多 exe 时选 `Game.exe`" 不得写成自动规则**——论坛真实反例里正确答案是 `開始游戏.exe`【社区·论坛一手】。 | 从报告给的候选集里指一个，写进 `mtool-profiles\<item-id>.json`。**确认一次，以后这个 item 不再问。** | `READY_FOR_TRANSLATION` | **C** |
| `MTOOL_UI_CHANGED`（H22/H24）：前台流程与 profile 记录的步骤不符。 | 手动跑一遍 MTool，把新步骤更新进 profile。MTool 会自动更新（PC 发布页只有 build 标识）【官方】，**不要在任何地方写死 MTool 版本号**——它作为运行时字段记进 `state.json`。 | `READY_FOR_TRANSLATION` | **C** |

#### 4.3.14 MTOOL_FAILED — 翻译加载失败

| 触发条件（可编程判据） | 你要做什么 | 做完回到 | 谁能解除 |
|---|---|---|---|
| `MTOOL_LOAD_FAILED`（H23）：你在前台观察到翻译没加载上。**判据是人的观察，不是产物文件存在性**——MTool 的 `.bin` 落盘位置未知【社区】【待测】。 | 常见原因按顺序排查：① 库根落在 OneDrive 同步目录或 `Program Files` 下【社区】；② `target_exe` 选错（回 `WAITING_FOR_MTOOL_RULE` 改 profile）；③ 该引擎 MTool 本就不支持。 | ①② → `READY_FOR_TRANSLATION`；③ → `TRANSLATION_NOT_APPLICABLE` | **C** |

> 幂等提示：重试前**不必**清掉 `version.dll`。进入 MTool 前若 `version.dll` 已存在 → 判定已注入，不重复注入【社区】——该判据在真实注入流程中的可靠性【待测】。

#### 4.3.15 ABANDONED — 人工放弃（终态）

| 触发条件（可编程判据） | 你要做什么 | 做完回到 | 谁能解除 |
|---|---|---|---|
| `HUMAN_ABANDON`（H25/R15）：只由你显式声明，没有任何自动触发路径。 | 运行 `Invoke-Work.ps1 -BatchId <slug> -Abandon <item-id> -Reason <token>`（参数契约见 §8.2.3）。文件一个都不会被删（I1）。详见 §4.8。 | 终态，`resume_allowed = []` | 无人能解除；要重做必须**新建 item.id** |

#### 4.3.16 PATH_BUDGET_EXCEEDED — 路径预算超限且已降级仍不够

| 触发条件（可编程判据） | 你要做什么 | 做完回到 | 谁能解除 |
|---|---|---|---|
| `PATH_BUDGET_ENTRY_TOO_LONG`（H28 由 A 在只读预检算出；H29 由 B 在内层解包时撞上）：`库根 + games\ + dirname + 最长内部条目路径` 超出 §2.5 的 MAX_PATH 预算（建目录实际上限是 248 = `MAX_PATH−12`，不是 260【社区】），**且 `dirname` 已经是 §3.2A 的降级 ASCII slug**——也就是说自动手段已经用尽。 | **改配置，不是补规则**（这正是它与 `WAITING_FOR_RULE` 分开成态的理由）：① 把库根换到更短的路径（`D:\GL\` 这类），或 ② 在批次条目里人工指定一个更短的 `dirname`（仍须过 §3.2A 的合法性检查，且降级原因必须记录）。改完预算算术可自动复核。<br>`LongPathsEnabled=1` 下 PowerShell 对 >260 字符路径的实际行为【待测】——在实测之前**不要**靠它来解决本态。 | H28 → `DOWNLOAD_STABLE`；H29 → `PREFLIGHT_OK` | **B** |

#### 4.3.17 JOURNAL_CORRUPT — `events.jsonl` 不可解析

| 触发条件（可编程判据） | 你要做什么 | 做完回到 | 谁能解除 |
|---|---|---|---|
| `JOURNAL_UNPARSABLE`（H30）：重放时命中**不可解析的中间行**（尾部半行不算——那是崩溃截断，按 §4.5.1 规则 2 丢弃并封口），或 `events.jsonl` 缺失 / 整体不可读。**重放在损坏点封口**：该点之后的内容一律不参与 fold，只进 `history[]`。 | ① 先取证：`.gameflow\` 一个文件都不要删，按 §4.5.2 的判读表看 `state.json` / `.bak` / `.tmp-*` 的组合，确定损坏发生在哪一次写入附近；② 把损坏的 journal **另存**为取证副本（改名，不删除），③ 按 §4.8.3 **新建一个 `item.id` 重新立项**——旧 item 的历史不可信，但也不改写。<br>**不要**手工编辑 `events.jsonl` 让它"重新能解析"：那是改写历史，之后所有幂等 gate（I3）读到的都是伪造事实。 | 无。`resume_allowed = []`，**连 R15（→ `ABANDONED`）都不适用**——R15 要写一条事件，而事件流本身已不可信 | 无人能解除；只有**人**重新立项 |

> **为什么它是落盘停机态而不是运行期状况**：它的判定是 journal 字节的纯函数，每次重放都得同一结论，因此可以稳定出现在 `state.json` 与对账报告的「⛔ 等你做事」分区里。若做成运行期状况，这个 item 会**永久静默停住**——每一轮巡检都跳过它、每一份报告都看不见它，这是最难发现的一类故障。
> **诚实局限**：当 journal 仍可追加时，同时追加一条 `halt.entered` 让事件流与报告一致；当它**整体不可读**（IO 错误 / 文件缺失）时，没有任何可写的真相载体，此时只有 `state.json` 与报告承载这个状态——这是本 SPEC 里唯一一处状态不由 journal 承载的位置，必须写在这里而不是藏起来。

---

### 4.4 幂等

**幂等的定义（本项目口径）**：对同一个 item，把任意执行体的任意一步**重复跑任意次**，落盘状态与文件系统收敛到同一结果，且**不产生第二次破坏性动作**。

#### 4.4.1 唯一机制：由已落盘事实 gate，不现场重判（I3）

这是幂等的全部秘密，也是最容易被"优化"掉的一条：

```
错的写法（看似等价，实则破坏幂等）：
    if (现场跑一遍 D1..D12 都通过) { 删除源包 }
        ↳ 第二次跑：源包已被删，D2/D4 无从求值 → 判"失败" → 落 CORRUPT（假失败）
        ↳ 或者判据在两次运行间因外部因素翻转 → 该删的不删 / 不该删的删

对的写法：
    fact = state.archives[i]            # 从 events.jsonl fold 出来的已落盘事实
    if (fact.verified == true && fact.consumed_by == <attempt_id> && fact.deleted_at == null) {
        删除；追加删除事实事件{result: deleted | already_absent}
    }
        ↳ 第二次跑：fact.deleted_at != null → no-op
        ↳ 崩在"已删但没记"之间：gate 仍满足 → 再删一次 → 文件不在 → already_absent → 收敛
```

推广成规则：**任何会改变文件系统的动作，其触发条件只能引用 `state.json`（=journal 的 fold）里的字段，不能引用现场探测结果。** 现场探测只允许出现在"产生新事实"的那一步，且探测结果必须**先落 journal**、下一步才消费。

删除源包的路径以 `Join-Path <游戏根> archives[].name` 拼出——`archives[]` **没有 `path` 字段**（§3.4.1），自拼绝对路径会破坏 I3。

#### 4.4.2 各执行体动作的 gate 表

| 动作 | 执行体 | gate（已落盘事实） | 重复执行的结果 |
|---|---|---|---|
| 认领归位（`_inbox` → `games\`） | A | `claimed_files[]` 已含该 (name, size) | no-op |
| `7z t` 单包测试 | B | 该包的 `archive.tested` 已落盘且 (name, size, mtime) 未变 | 跳过；`-ForceRetest` 可强制重测（只重写事实，不改状态） |
| `7z x` 解包 | B | 无 gate —— 每次进入 `EXTRACTING` 生成**新** `attempt_id` 与**新** `_stg\<8hex>\` | 旧 attempt 的 staging 被标 `superseded` 并回收；不会两个 attempt 同时写同一个 staging |
| Defender 预检 | B | `defender.scanned` 已落盘且产物文件清单快照未变 | 跳过（注意：本步在 `VERIFYING` 之内、落地之前） |
| staging → 落地移动 | B | 无独立 gate —— **staging 目录本身就是进度记录**：移动会消费源，还留在 staging 里的就是还没移走的 | 断点续移；目标同名同 size → 跳过并删 staging 副本；同名不同 size → H19 停机 |
| 写 `staging.promoted` | B | staging 已空 + 落地体积 == 进入 `VERIFYING` 时记录的 staging 总体积 | 幂等（相同事实重复追加不改变 fold 结果，但应跳过以免刷屏） |
| **删除源包** | B | `archives[].verified == true` ∧ `consumed_by == <attempt_id>` ∧ `deleted_at == null` | 见 §4.4.1；**逐包立即追加事件，不要攒到最后批量写**。两段式删除（小于 `retention.trash_max_bytes` 的先移入 `trash\`）见 §6.8，对本 gate 无影响——`deleted_at` 在移入 trash 的那一刻就写 |
| MTool 注入 | C | `version.dll` 已存在 | 判定已处理，不重复注入 |
| 写 manifest | B | `manifest_sha256` 与重算值一致 | 跳过 |

#### 4.4.3 attempt-id 与 staging

- **格式**：`attempt_id = <UTC 紧凑 ISO>-<4hex>`，例如 `20260912T041205Z-7c2e`。全局唯一，**永不复用**。**不用 ULID**——它与 `_stg\<8hex>` 的映射关系对不上，也不满足"显式生成的受限字符集 slug"。
- **staging 路径**：`D:\GameLib\_stg\<8hex>\`（产物直接落这一层，**没有 `w\` 子目录**；分卷重建副本落**同级**的 `_stg\<8hex>-p\`）。`8hex` 与 `attempt_id` 的映射写在进入 `EXTRACTING` 的事件里。用 8 位十六进制而不是 `attempt_id` 本身，是 MAX_PATH 预算需要（`D:\GameLib\_stg\<8hex>\` = 25 字符；按上游设计放成 `games\<名>\.gameflow\staging\<attempt-id>\` 单这一段就吃 39 字符）【实测·字符计数】。
- **必须与游戏库同卷**——`File.Replace` 跨卷**抛异常**【官方】https://learn.microsoft.com/en-us/dotnet/api/system.io.file.replace；staging→落地跨卷会退化成复制+删除，而 .NET 文档自己点名 `Copy`+`Delete` **不原子**（崩溃会留下半写的目标文件）【官方·同上】。
- **一个 attempt 一个 staging，永不共用、永不追加复用。** 重跑就是新 attempt——这比"清理旧 staging 再用"少一个失败模式。

#### 4.4.4 下载稳定性采样窗口存在哪里

采样机制的权威定义在 **§5.3**，本节只说明它与状态机的关系：

- **未认领文件只能运行内采样**（一次巡检内完成 `sample_count` 次采样）。这是 **I5 白名单造成的结构性限制**：`_inbox` 里未认领的文件没有 `item_id`（事件 schema 必填），也没有任何 A 被允许写入的目录去存跨运行的采样点。
- **已认领文件**（item 目录已存在）的采样写 `download.gate` 事实事件，G2 取「本轮采样 ∪ 最近 N 条 `download.gate`」。
- 参数（`sample_count` / `sample_interval_seconds` / `quiet_window_seconds` 等）全部登记在 §3 的 `netdisk-profiles.json` 与 `guards`，本节不复制数值。

对状态机的影响只有一条：**采样不产生迁移事件**。`AWAITING_DOWNLOAD` 自反等待期间状态不变，直到 G1–G6 全成立才写 M03。未认领文件的采样窗口在跨运行时从零开始，**不影响正确性，只影响时延**——这条必须写进报告脚注，免得人以为流水线卡住了。

---

### 4.5 崩溃恢复

#### 4.5.1 从 events.jsonl 重建 state.json

```
Rebuild(itemDir) -> state:
  # 0. 必须在 item 锁内做（§4.6）。恢复期间不得有第二个进程在写。
  lines := ReadAllLines(itemDir\.gameflow\events.jsonl, UTF8NoBOM)
  events := []
  for i, line in lines:
      try: events += ParseJson(line)
      catch:
          if i == len(lines)-1:  记 tail_discarded = true            # 崩溃留下的半行，封口
          else:                  return HaltState(JOURNAL_CORRUPT)   # 中间坏行 = 不可信（H30）
  # 重放顺序 = 文件行序，**不排序**。seq 只用于人读与断裂检测：
  # §3.3.5 明确允许封口后出现一次重复 seq，按 seq 排序会静默重排历史 → I2 失效。

  s := EmptyState(schema_version)
  active_item := (events 中最后一条 to_state == PLANNED 的迁移所声明的 item_id)   # §4.8.3 的复活语义
  for e in events:
      if e.item_id != active_item: s.history += e; continue
      if e.kind == 'state.entered':
          if e.payload.from_state != s.state and s.state != null:
              记 divergence；**采用 e.payload.to_state 继续**          # journal 是真相，不回退
          s.state      := e.payload.to_state
          s.entered_at := e.ts
          s.halt       := (紧随其后的 halt.entered) ? {reason_code, resume_allowed, cleared_by_allowed} : null
      else:                       # 事实事件
          ApplyFact(s, e)         # 纯函数：无 I/O、无现场探测、无时间依赖
  if tail_discarded or divergence: 追加 state.rebuilt 事件
  return s
```

三条硬规则：

1. **重建只读 journal，绝不读文件系统。** 一旦重建时"看一眼盘上现在什么样"，I3 就破了，幂等随之破。
2. **只丢弃最后一行的解析失败。** 中间行坏掉说明不是崩溃截断而是文件被破坏，这时**不猜**：落停机态 `JOURNAL_CORRUPT`（§4.3.17），**不做任何破坏性动作**。
3. **`from_state` 不一致时采用 `to_state`。** append-only 日志是真相，重建器没有资格否决它。但必须记 `state.rebuilt{divergence:true}` 并在报告里标红——这是"有人手改过 journal"或"有 bug"的唯一信号。

**上次崩溃的探测器**：`run.started` 没有配对的 `run.ended`。这是纯 journal 推导的、确定的、无时间猜测的信号——比命名 Mutex 的 `AbandonedMutexException` 更可靠（后者有线程亲和性与会话可见性两个坑，见 §4.6.3）。

> §3.9.3 的 `Recover` 与本算法的分工（R26）：「从 journal 重建 state」**所有执行体**都做（就是本小节）；「清理 `*.tmp-*` / 回收孤儿 `_stg`」**只有 B** 做（§4.5.3）。A 跑 Recover 时只执行前半段，否则直接违反 I5。

#### 4.5.2 `state.json` / `.bak` / `.tmp-*` 的判读

落盘协议（完整写法见 §8.8）：同卷 tmp → `Flush($true)` → `File.Replace(tmp, state.json, state.json.bak)`，**保留 `.bak`，不传 `null`**。

`File.Replace` / Win32 `ReplaceFile` **不是原子操作**——官方 Remarks 自称「combines several steps within a single function」，并逐条列举三种部分失败中间态（1175 / 1176 / 1177），且 `REPLACEFILE_WRITE_THROUGH` 明写「This value is not supported」；备份文件、被替换文件、替换文件三者**必须同卷**。【官方】https://learn.microsoft.com/en-us/windows/win32/api/winbase/nf-winbase-replacefilew　`MoveFileEx` 全文没有 atomic 字样。【官方】https://learn.microsoft.com/en-us/windows/win32/api/winbase/nf-winbase-movefileexw

**所以 SPEC 里永远不要写"状态写入是原子的"，只写"可恢复"。** 真正的崩溃安全来自 I2，不是追求单次替换的原子性。断电中途是否会留下中间态，没有任何官方文档给出承诺【待测·只能靠虚拟机强制断电】。

| 盘上看到 | 含义 | 动作 |
|---|---|---|
| `state.json` 可解析，无 `.tmp-*`、无 `.bak` | 上一次写入完整收尾 | 直接用（仍可随时重建，成本是遍历 journal） |
| `state.json` 可解析 + `.bak` 存在 | Replace 成功；`.bak` 是设计保留的上一版 | 用 `state.json`；保留最近一份 `.bak`（成本极低） |
| `state.json` 可解析 + `.tmp-<attempt>` 存在 | tmp 写完但 Replace 未执行或失败（1177 下 replacement 仍在原名） | **丢弃 tmp**——tmp 与 state.json 谁更新不可判定 → 一律以 journal 重建为准 |
| `state.json` 缺失 / 不可解析 | 写入被截断，或 1176 的无备份分支 | **从 journal 重建**（§4.5.1） |
| `state.json` 与 `.bak` 都在但都不可解析 | 罕见；磁盘层面出事了 | 从 journal 重建；若 journal 也不可读 → `JOURNAL_CORRUPT`（§4.3.17） |
| `events.jsonl` 缺失、中间行坏、整体不可读 | 无可信真相 | 落 `JOURNAL_CORRUPT`：跳过该 item，报告点名，**不删、不移、不改写 journal** |

**`.bak` 存在意味着什么**：它意味着上一次 `File.Replace` 至少走到了"把旧 `state.json` 改名成 `.bak`"这一步。它的价值是**诊断**，不是恢复源——I2 已经规定 `state.json` 是纯派生物，恢复永远走 journal 重建这一条路。保留 `.bak` 的真实理由是：ReplaceFile 的三种失败态里，**有备份与无备份的残留文件命名完全不同**，保留备份让崩溃后的盘面布局是确定的、可判读的（上表就是这张判读表）。**「`.bak` 可解析就直接采用它恢复」这一支不采纳**——它会让恢复结果依赖两个来源，违反"唯一真相"；§3.9.3 与 §8.8.4 同此。

> `Flush($true)`（`FileStream.Flush(Boolean)`：「clears all intermediate file buffers」，对应 Win32 `FlushFileBuffers`）是整条链上**唯一有断电保证**的一步【官方】https://learn.microsoft.com/en-us/dotnet/api/system.io.filestream.flush。`File.Replace` 之前必须自己刷盘，因为 `REPLACEFILE_WRITE_THROUGH` 不可用。

#### 4.5.3 半写的 staging 怎么清理

```
ReclaimStaging(libraryRoot):                      # 只由 B 做；A 永不删除（I5）
  onDisk  := { 每个 D:\GameLib\_stg\<8hex>\ }
  live    := {}                                   # 仍被引用的 staging
  for item in 本批次全部 item:
      s := Rebuild(item)                           # 在该 item 的锁内
      if s.state == EXTRACTING 且本次运行未持有该 attempt:
          # 没有活跃持有者（我们拿到了 item 锁）=> 这就是上次崩溃的残留
          标记 s.current_attempt 为 crash_orphan
      live 中只保留「本次运行自己刚创建的 attempt」
  for stg in onDisk - live:
      断言 stg 的完整路径以 "D:\GameLib\_stg\" 为前缀        # 前缀断言，无条件执行
      递归删除 stg
      追加 staging.discarded{attempt_id, reason_code}
```

四条要点：

- **staging 不是用户数据，是可重造的中间物** —— I1 的"停机态不删任何文件"保护的是 `games\` 下的源包与产物，不是 `_stg\`。这条边界必须写清楚，否则实现者会不敢清理，盘迟早被半成品塞满。
- **拿到 item 锁 == 没有活跃持有者。** 文件锁在进程死亡时由 OS 自动释放，所以"我拿到了锁"本身就证明"上一个持有者已经不在了"。因此看到 `EXTRACTING` 而自己没在跑，就是崩溃残留——**不需要任何超时猜测**。这是选文件锁而不是 PID 检测的直接收益。
- **`_stg\` 前缀断言无条件执行**，哪怕路径是从事件里读出来的。递归删除是本项目唯一的批量删除动作，它必须有一条不依赖调用方正确性的护栏。
- **A 只观察不回收。** 按 I5 的白名单（A 的写集合恰为三处：`games\<dirname>\`、新建 `_reports\<batch-id>-<seq>.md`、把 `_inbox\` 中已认领的文件移入前者），A 在对账报告里**列出**孤儿 staging，回收由 B 做。
- **停机态下的 staging 保留到下一次运行。** H12 / H29 这类需要人看现场的停机，当次运行不清 staging；下一次运行发现该 attempt 已被 `superseded` 或该 item 进了新 attempt 时才回收。

#### 4.5.4 被强杀后残留的独占句柄

**不需要任何处理。** 进程终止时 OS 关闭其句柄表中的全部句柄，`FileShare.None` 的独占随之释放【社区（广泛共识，本轮未取到针对该场景的官方原文）】【待测·见 §11】——这正是选它做互斥的核心理由。所以本项目**没有"陈旧锁"这个概念**，也就不需要超时清理、不需要 PID 存活检测、不需要任何时间猜测。

需要处理的只有两类残留：

| 残留 | 判据 | 处理 |
|---|---|---|
| `.lock` 文件本身还在盘上 | 文件存在，但能 `FileShare.None` 打开 | **正常，不是异常**。锁文件是长期存在的空壳，锁语义完全来自句柄 |
| `_stg\<8hex>\` 里的半写文件 | §4.5.3 的 orphan 判定 | 整目录递归删除 |

锁文件里可以写 `{pid, host, started_at, run_id}` **供人排查**，但**绝不用它判存活**——Windows PID 复用很快，`Get-Process -Id` 命中的可能是完全无关的新进程【社区】。

#### 4.5.5 JSONL 追加的崩溃行为

写入协议（整行一次 `Write` + `Flush($true)` + 立即关闭、必须 `-Compress`、`-Depth 10`）的权威在 §8.8 与 §3.3；本节只声明两条状态机侧的容错规则：

1. **读取端逐行解析，最后一行解析失败就丢弃并记 warn**（§4.5.1 规则 2），不是致命错误；中间行失败 → `JOURNAL_CORRUPT`。
2. 追加**不是**原子的，崩溃可能留半行【社区（POSIX `O_APPEND` 的原子性讨论广为人知；Windows 上未找到官方保证）】【待测·见 §11】—— 这是我们容忍尾部半行的原因，**不是**可以不 flush 的借口。

---

### 4.6 并发与互斥

#### 4.6.1 两层文件锁，一个并发单位

```
D:\GameLib\batches\<batch-id>.lock              批次级   FileShare.None
D:\GameLib\games\<dirname>\.gameflow\.lock      item 级  FileShare.None   ← 真正的并发单位
```

```powershell
$lock = [System.IO.File]::Open($lockPath, 'OpenOrCreate', 'ReadWrite', 'None')
```

**两层都必须取**：A 与 B 只有争同一个句柄才谈得上互斥。B（§6.1.2 步骤 00）取批次锁、并在每个 item 的循环体内取/放条目锁，形状与 §5.1.2 第 4 步相同；**不存在库级的 `D:\GameLib\.lock`**。

选它的五条理由：

| 性质 | 为什么重要 |
|---|---|
| 进程死亡时 OS 自动释放【社区】【待测】 | 没有陈旧锁，不需要超时清理（§4.5.4） |
| 无线程亲和性 | 句柄属于进程不属于线程；PowerShell 跨 `begin/process/end` 或跨 runspace 不会炸（对照：`Mutex` 官方明写 enforces thread identity）【官方】https://learn.microsoft.com/en-us/dotnet/api/system.threading.mutex |
| 粒度自由 | item 级锁天然满足 I4「A 缺密码不阻塞 B」 |
| 跨会话生效 | 文件系统没有终端服务会话隔离【社区】；**计划任务（A）与交互会话（B/C）天然互斥**——这正是命名 Mutex 会栽跟头的地方（§4.6.3） |
| 5.1 / 7.x 行为一致，零依赖 | Codex 的 Windows 原生 agent 跑的是 `powershell.exe -NoProfile`，即 **Windows PowerShell 5.1**【官方·Codex 配置参考】 |

**抢锁失败一律不区分异常。** `IOException`（被锁）与 `UnauthorizedAccessException`（权限）在抢锁这一步都判为"拿不到锁"，统一停在运行期状况 `LOCKED_BY_ANOTHER_RUN`，以 §8.2.3 的退出码 **3（前置条件不满足）** 立即退出，**不等待、不重试**。区分它们需要核对 HResult 具体值【待测·仲裁已裁定本项目不必实测，因为我们选择不区分】，而"分不清就都当拿不到"更符合确定性偏好，也不会产生错误动作。

> 注意：G3 的独占打开**必须**区分这两个异常（H03 落 `BLOCKED_RESOURCE` / `PERMISSION_DENIED`），因为那里区分错了会永久卡死。**抢锁不区分、G3 必须区分**——这两条不矛盾，因为后果完全不同。

#### 4.6.2 为什么不用 lock 文件 + PID

| 问题 | 后果 |
|---|---|
| Windows PID 复用很快【社区】 | `Get-Process -Id <pid>` 命中的可能是完全无关的新进程 → 把死锁判成活锁，item 永久卡住 |
| 强杀后锁文件残留 → 需要"超时清理陈旧锁" | **引入时间猜测**：多久算陈旧？机器休眠算不算？这是典型的"聪明的猜测"，与本项目的确定性姿态直接冲突 |
| 要判"是不是我们的进程"就得再比进程名 + 启动时间 | 逻辑越堆越脆，每加一条都在加一个新的误判面 |

`FileShare.None` 把这三个问题**一次性消掉**：锁的生命周期由内核维护，没有任何一处需要我们猜。

#### 4.6.3 命名 Mutex：本项目不用它做互斥；若将来引入，必须 `Global\`

本 SPEC **不使用命名 Mutex**。崩溃信号由 journal 的 `run.started` / `run.ended` 配对给出（§4.5.1）。

但契约里要写清楚**如果**将来有人加一个机器级单例 Mutex，硬性规则是**必须 `Global\` 前缀**：

> 「If its name begins with the prefix `Global\`, the mutex is visible in **all terminal server sessions**. … **If you do not specify a prefix when you create a named mutex, it takes the prefix `Local\`.**」【官方】https://learn.microsoft.com/en-us/dotnet/api/system.threading.mutex

后果是致命的：A 由 Windows 任务计划触发（服务会话），B/C 在交互会话。**无前缀 Mutex 会让 A 和 B 互相看不见——互斥静默失效，而 A 与 B 并发恰恰是本项目最需要防的场景**（A 在扫描、B 在删源包）。不会报错，只会在某个倒霉的时刻撞车。

另外三条同样出自该官方文档：① Mutex **强制线程亲和性**（「a mutex can be released only by the thread that acquired it」），PowerShell 管道跨线程释放会抛 `ApplicationException`【社区】；② 反斜杠是 Mutex 名的**保留字符**，做不出"每个游戏目录一把锁"；③ 默认无 ACL。叠加计划任务侧的 `MultipleInstances = IgnoreNew`【官方】https://learn.microsoft.com/en-us/powershell/module/scheduledtasks/new-scheduledtasksettingsset，机器级单例这件事已经被覆盖，Mutex 没有必要。

#### 4.6.4 `LOCKED_BY_ANOTHER_RUN` 为什么不落盘

三条理由，第三条是决定性的：

1. **它描述的是"运行"，不是 item。** 抢锁失败时 item 的真实状态一个字都没变。把它落成 item 状态，会在对账报告里凭空长出一批假停机条目，而它们下一分钟就自己消失了——这恰恰是"需要你处理"清单最不能有的东西。
2. **它是瞬时的。** 落盘就必须有一条"解除"迁移，于是凭空多出 `H##` + `R##` 两条迁移和一个本不存在的人工动作。
3. **要写它就得先写 `events.jsonl`，而写 journal 本身必须在锁内做** —— 我们恰恰没拿到锁。绕开锁去写 journal，等于承认 journal 可以被无锁并发追加，整个崩溃恢复模型的前提就塌了。**这条不是取舍，是逻辑上不可能。**

> **运行期状况只剩这一个。** journal 不可解析的情形**不是**运行期状况——它按 R13 落成停机态 `JOURNAL_CORRUPT`（§4.3.17），否则条目会永久静默停住。二者的分界线很清楚：`LOCKED_BY_ANOTHER_RUN` 下一分钟自己消失，`JOURNAL_CORRUPT` 永远不会自己好。

---

### 4.7 重跑语义（`-Retry`）

#### 4.7.1 命令形状

```
Invoke-Work.ps1 -BatchId <slug> -Retry <item-id> [-Resume <STATE>] [-ForceRetest]
Invoke-Work.ps1 -BatchId <slug> -RetryAll
```

参数全部是**无空格无歧义 token**，完整入口契约（含退出码与全部参数）以 **§8.2.3** 为准，本节不重复论证。`-Resume` 只在 `resume_allowed` 有多个元素时**必填**；只有一个元素时**禁止传**（传了就是调用方在猜，直接报错退出）。

#### 4.7.2 `-Retry` 做什么

```
1. 取 item 锁（拿不到 → LOCKED_BY_ANOTHER_RUN，按 §8.2.3 退出）
2. 从 journal 重建 state（§4.5.1），回收孤儿 staging（§4.5.3）
3. 断言 state.state ∈ 可重跑集合（§4.7.4），否则原样退出并说明原因
4. 复核该停机态 reason_code 对应的**解除判据**：
     - 判据可自动复核（R01/R02/R11/R12 的一部分）→ 脚本自己跑一遍
     - 判据需人先动作（其余）→ 检查人的动作是否已留痕：
         vault 里有键？规则文件里有条目？`note.human` 事件已写？
5. 判据满足 → 追加 halt.cleared + state.entered{to_state ∈ resume_allowed, transition_id = R##}
             → 从该状态起正常推进
   判据不满足 → **一个事件都不写**，打印缺什么，退出
```

第 5 步的"不满足就什么都不写"很重要：`-Retry` 失败**不留痕**，人可以放心连按。只有真正发生了状态推进才写事件。

#### 4.7.3 `-Retry` 不做什么

- ❌ **不改写历史**：不删事件、不重写 `events.jsonl`、不改 `manifest.json` 里已记的 SHA-256。
- ❌ **不降低任何判据**：D1–D12 一条不减，G1–G6 一条不减，护栏阈值不临时放宽（要放宽就在批次条目里显式声明，那是一次人工动作，会写 `note.human`）。
- ❌ **不重新下载、不改名任何 `games\` 下的文件**。
- ❌ **不解除 `THREAT_CONFIRMED`、`JOURNAL_CORRUPT` 与 `ABANDONED`**（`resume_allowed = []`）。
- ❌ **不跨 item 传染**：`-RetryAll` 等价于对每个 item 逐个执行上述流程，**判据不满足的原样跳过**，不会因为一个 item 失败而中断其余（I4）。
- ❌ **不重复已完成的破坏性动作**：所有删除/移动都走 §4.4 的 gate。

#### 4.7.4 哪些状态可以重跑

| 分类 | 状态 | `-Retry` 的行为 |
|---|---|---|
| **不需要 `-Retry`**（正常运行自动接管） | `EXTRACTING` · `VERIFYING` · `TRANSLATING` | 拿到 item 锁就证明没有活跃持有者（§4.5.4），这三个"进行中"状态等价于"上次崩溃"。回收孤儿 staging 后从上一个稳定状态重来：`EXTRACTING`/`VERIFYING` → 新 attempt 从 `PREFLIGHT_OK` 重走；`TRANSLATING` → 回 `READY_FOR_TRANSLATION`（`version.dll` 已存在则直接判定已注入） |
| **判据可自动复核**（人只需触发，不需判断） | `WAITING_FOR_VOLUMES` · `WAITING_FOR_PASSWORD` · `BLOCKED_RESOURCE`（余量/体积/深度类） · `CORRUPT`（人已换文件后） · `WAITING_FOR_RULE`（仅 `RULE_NETDISK_TEMP_SUFFIX_RESIDUE`） | 脚本自己复核判据；满足就推进。A 会在对账报告里提前标注"判据现在看起来已满足，可 `-Retry`" |
| **必须人工先动作，再 `-Retry`** | `PASSWORD_ENCODING_SUSPECT` · `EXTRACT_FAILED_AMBIGUOUS` · `WAITING_FOR_RULE`（其余 reason_code） · `WAITING_FOR_MANUAL_INSTALL` · `FILENAME_ENCODING_RISK` · `EXTRACTED_NO_CONTENT` · `THREAT_SUSPECTED` · `BLOCKED_RESOURCE`（穿越/reparse 类） · `PATH_BUDGET_EXCEEDED` · `WAITING_FOR_MTOOL_RULE` · `MTOOL_FAILED` | 动作留痕（vault 键 / 规则文件条目 / `note.human` 事件）是判据的一部分；没留痕 = 判据不满足 = 不写任何事件 |
| **不可重跑** | `COMPLETE` · `TRANSLATION_NOT_APPLICABLE` · `THREAT_CONFIRMED` · `ABANDONED` · `JOURNAL_CORRUPT` | 终态或无回归路径。`-Retry` 打印原因并退出 |
| **不适用** | `PLANNED` · `AWAITING_DOWNLOAD` · `DOWNLOAD_STABLE` · `PREFLIGHT_OK` · `EXTRACTED` · `SCANNED` · `READY_FOR_TRANSLATION` | 非停机态；正常运行本来就会推进它们。`-Retry` 退化为"跑一次正常流程" |

---

### 4.8 `ABANDONED` 的语义与清理策略

#### 4.8.1 语义

- **唯一入口**：`Invoke-Work.ps1 -BatchId <slug> -Abandon <item-id> -Reason <token>`。**没有任何自动触发路径**——不会因为失败次数、超时、长期停机而自动放弃。
- **可从任一非终态进入**（H25），也可从任一停机态进入（R15，`JOURNAL_CORRUPT` 除外）。
- **终态**：`resume_allowed = []`。任何执行体见到 `ABANDONED` 一律跳过：不扫描、不解压、不扫毒、不翻译。
- **I1 依然成立：放弃不删任何文件。** 「放弃」是流水线不再管它，不是清理它。这两件事必须分开，否则一个误按的 `-Abandon` 就是数据丢失。
- **批次清单里只有 `abandoned: true|false`**（§3.2.5），**没有 `state` 字段**——运行期状态一律从 `games\<dirname>\.gameflow\state.json`（或 `Replay(events.jsonl)`）取，批次字段只用于 `abandoned` 过滤。
- **报告里单列分区**：`ABANDONED` 的条目**不进**"需要你处理"清单（那会让清单永远清不空），但必须进一个「已放弃（仍占用 X GB）」分区，附 `reason_code` 与放弃时间。

#### 4.8.2 清理策略：三档，默认最保守

| 档位 | 做什么 | 默认 | 谁执行 |
|---|---|---|---|
| `keep`（默认） | 什么都不做。目录、源包、已解出的产物全留 | ✅ | — |
| `purge-staging` | 立即回收该 item 的全部 `_stg\<8hex>\`（不等下一次运行） | | B |
| `purge-all` | **不提供。** 删除整个 `games\<dirname>\` 是你在资源管理器里的动作 | | 人（手动） |

不提供 `purge-all` 的理由：C3 + C4 已经让本项目有一条不可撤销的删除路径，判错一次就是永久数据丢失。再给一个"一键删整个游戏目录"的命令，等于把最危险的操作也自动化了。**放弃一个条目和删掉几十 GB 数据，不应该是同一个命令。**

注意即使在 `keep` 档下，`_stg\` 的孤儿 staging 仍会被下一次 B 运行按 §4.5.3 回收（`reason_code = abandoned`）——staging 从来不是用户数据。

#### 4.8.3 不提供"撤销放弃"，提供"重新立项"

`ABANDONED`（以及 `JOURNAL_CORRUPT`）**没有回归主线的迁移**。要重做，必须**新建一个 `item.id`**（新 slug），在批次里新增条目；旧条目永远保持原状，历史不改写。

理由是幂等：允许复活会让"已落盘的 `verified:true` + `consumed_by`"跨越两个生命周期，删除 gate（I3）就失去意义——第二次生命周期里的 `-Retry` 可能读到第一次的 `verified`，删掉第二次下载回来的包。

**新条目可以指向同一个 `dirname`**（目录名不变，`.gameflow\events.jsonl` 还是同一份，append-only 继续追加）。重建时按 `item_id` 分组，**取最后一条 `to_state == PLANNED` 的迁移所声明的 `item_id` 为活跃 item**，其余归入 `history[]`（§4.5.1）。这条规则是 §3.4.1 的 `state.json` schema 必须支持的契约。

---

### 4.9 本节产生的待 Windows 实测项

**权威清单在 §11**（含「不测的后果」与「阻塞哪一节」两列）。下表只登记本节的提出点，便于回查：

| # | 待验证 | 涉及本节何处 |
|---|---|---|
| S4-1 | 杀掉持锁进程后，另一进程能否**立刻** `FileShare.None` 打开同一文件 —— 整个「没有陈旧锁」前提的基础 | §4.5.4 · §4.6.1 |
| S4-2 | 强杀 `7z.exe` 后 `_stg\<8hex>\` 里残留文件的形态 | §4.5.3 · §4.4.2 |
| S4-3 | NTFS 上单次 <4 KB `Write` + `Flush($true)` 被强杀，`events.jsonl` 是否真会撕裂出半行 | §4.5.5 · §4.3.17 |
| S4-4 | `FileShare.None` 抢锁失败时抛的异常类型与 HResult | §4.6.1 · H03 |
| S4-5 | `MpCmdRun -Scan -ScanType 3 -DisableRemediation` 的 stdout 格式 | H20 · §4.3.9 |
| S4-6 | 受限权限（`-RunLevel Limited`）计划任务下 Defender 预检的失败形态 | H20 的 `DEFENDER_SCAN_FAILED` |
| S4-7 | `LongPathsEnabled=1` 下 PowerShell 对 >260 字符路径的实际行为；深层 staging 递归删除是否会失败 | §4.4.3 · §4.5.3 · §4.3.16 |
| S4-8 | 真实 SFX `.exe` 的 `7z l -slt` 输出（`Type` 与 `Offset`） | H05 · M04 |
| S4-9 | `traversal.zip` 里 `C:\...` 与 `back\slash\...` 在 Windows 上的落地位置 | H08 / H26 的 `PATH_TRAVERSAL_DETECTED` |
| S4-10 | Defender 实时保护是否会在解压中途隔离/锁住 staging 里的文件 | §4.4.2 · §4.5.3 |
| S4-11 | MTool 注入后 `version.dll` 是否稳定存在、名称是否随语言/版本变化 | M12 · §4.3.14 |
| S4-12 | `File.Replace` 在真实断电下的残留形态 | §4.5.2 |

### 4.10 本节引用的一手来源

7-Zip 行为（退出码、`type.htm` 的 `-t*:r` 默认与 8 MB 窗口、`password.htm` 的头加密、`method.htm` 的 `-m` 适用范围）与 Defender 行为的一手来源，统一见 **§6.2.1 / §6.9 / §9.8** 的来源表，本节不重复。本节独有的来源：

| 断言 | 来源 |
|---|---|
| `ReplaceFile` 非原子、三种部分失败态、`WRITE_THROUGH` 不支持、三文件必须同卷 | https://learn.microsoft.com/en-us/windows/win32/api/winbase/nf-winbase-replacefilew |
| `MoveFileEx` 全文无 atomic | https://learn.microsoft.com/en-us/windows/win32/api/winbase/nf-winbase-movefileexw |
| `File.Replace` 跨卷抛异常；`Copy`+`Delete` 不原子 | https://learn.microsoft.com/en-us/dotnet/api/system.io.file.replace |
| `FileStream.Flush(Boolean)` 清空中间文件缓冲 | https://learn.microsoft.com/en-us/dotnet/api/system.io.filestream.flush |
| Mutex 的 `Global\` / `Local\` 可见性、线程亲和性、保留字符、默认无 ACL | https://learn.microsoft.com/en-us/dotnet/api/system.threading.mutex |
| `MultipleInstances = IgnoreNew` | https://learn.microsoft.com/en-us/powershell/module/scheduledtasks/new-scheduledtasksettingsset |
| WinRAR exit 11 仅对 RAR5 有效 | 官方 WinRAR 手册；**本轮未留存 URL**——正文引用一律标 `【官方·WinRAR 手册；本轮未留存 URL】`，不得升格为带 URL 的官方引用 |
| 本节全部标 `【实测·macOS】` 的 7-Zip 行为 | 官方 7zz 25.01（arm64 macOS）实验，实验目录与可复现命令见事实仲裁附录 |


---

## §5 执行体 A：巡检（Sweep）

执行体 A 是流水线里**唯一无人值守**的部分：由 Windows 任务计划定时拉起，读一遍批次、看一遍磁盘、把能确定的事情落成状态、把不能确定的事情写进对账报告，然后退出。它不是流水线的大脑，而是流水线的**眼睛**——所有会造成不可逆后果的动作（`7z t` / `7z x`、递归解包、Defender 预检、删除源包）都在 §6 的执行体 B 里，由人或 Codex 在交互会话触发。

### 5.0 A 的铁律与写白名单

| 编号 | 铁律 | 结构性保证（不是靠纪律） |
|---|---|---|
| **A-R1** | **不碰密码** | A 的 7-Zip 调用**一个 `-p` 都不出现**——A 经 §6.2.1 的唯一封装发调用，且固定不传 `-Password`（§5.6.1）。DPAPI vault 路径不在 A 的配置读取列表里 |
| **A-R2** | **不提权** | 计划任务 `-RunLevel Limited`（§5.10.2）。A 不调用 `MpCmdRun`（官方明写需提权，Defender 预检归 §6.9） |
| **A-R3** | **不删除任何文件** | A 的文件系统写操作只有两种：`File.WriteAllText` 新建/替换自己的状态文件，与**同卷 `File.Move` 认领**。没有任何代码路径调用 `Remove-Item` / `File.Delete`；`*.tmp-*` 清理与孤儿 `_stg\` 回收**只由 B 做**（§3.9.3 的 Recover 拆两段，A 只跑「从 journal 重建 state」那一段） |
| **A-R4** | **不写白名单以外的路径** | A 的写集合**恰为不变量 I5 枚举的三处**（§4.3）：① `games\<dirname>\`（含 `.gameflow\`）；② **新建** `_reports\<batch-id>-<seq>.md`，**不得覆盖已存在的报告**；③ 把 `_inbox\` 中**已认领**的文件移入 ①（同卷 rename，不构成删除语义）。除此之外 A 不写任何路径 |

`_inbox\`（除 I5 ③ 的同卷移出）、`batches\`、`_stg\`、`D:\GameFlow\` 全部对 A **只读**。跨卷时 A 拒绝执行认领（§5.2.4）——跨卷 `File.Move` 在 .NET 上退化成「复制 + 删除源」，会撞破 A-R3。

`_stg\<8hex>\` 的孤儿目录：A **只在报告里列出**（§5.9.1 的 🔴 异常区），**不回收**。回收是 B 的动作（§6.1.2 / §4.5.3）。

---

### 5.1 A 的一次运行

#### 5.1.1 运行形态：扫一遍就退出

**A 不是常驻守护进程。** 这不是风格偏好，是平台事实推出来的：

- Windows 上 Codex 的 `features.unified_exec` **默认关闭**。官方配置参考逐字：`"Use the unified PTY-backed exec tool (stable; enabled by default except on Windows)."`【官方】（`config-reference` L798-801，本轮已逐字复核）
- Windows 上 Computer Use **独占前台**，无法与用户共用同一会话；「锁屏继续」是 macOS 专属。【官方】
- E1：这是用户的主力机，会被随时打断。

三条合起来：A 必须是**无状态、可重跑、每次扫描一遍就退出**的批处理，由外部调度器（Windows 任务计划，§5.10）反复调用。**任何「起一个 Watch-Downloads.ps1 守着轮询」的设计都是错的**——它会在用户关机、注销、被 Computer Use 抢走前台时静默死掉，而没有任何人发现。

「扫一遍就退出」允许一次运行内部有**有界的采样等待**（§5.3.3 的 G2 需要多次采样）。区别在于：采样窗口有硬上界 `max_run_seconds`（**900**），到点即结束本轮、未定案的条目原样留给下一轮，**不携带任何内存状态**。

#### 5.1.2 主循环（伪码）

```text
Invoke-Sweep.ps1 [-BatchId <slug>] [-Unattended]
# 参数契约与退出码见 §8.2.3。入口只接收受限字符集 slug，不接收自由格式路径或标题。
# -BatchId 缺省 = 处理全部活跃批次（abandoned 的批次条目除外）

run_id  = "<UTC yyyyMMddTHHmmssZ>-<8hex>"        # 格式以 §3.3.1 为准
trigger = "scheduled" | "manual" | "codex"       # §3.3.1 公共字段，写进 A 产生的每一条事件

for batch in 选定批次:
  # ── 0. 批次锁 ──────────────────────────────────────────────
  batch_lock = TryOpen("<LibRoot>\batches\<batch-id>.lock",
                       OpenOrCreate, ReadWrite, FileShare.None)
  if (batch_lock == null): 记「本批次被占用」; continue      # 运行期状况 LOCKED_BY_ANOTHER_RUN，不落盘（§4）
        # 不等待、不重试、不做陈旧锁清理（PID 复用不可信，超时清理引入时间猜测）

  # ── 1. 读批次与规则（只读） ────────────────────────────────
  manifest = ReadJson("<LibRoot>\batches\<batch-id>.json")         # 只读
  rules    = ReadJson("<GameFlow>\rules\{netdisk,source,container}-*.json")   # 只读
  # 批次清单里**没有**运行期状态字段（§3.2.5）：state 一律从条目日志取
  items = manifest.items
        | where { not $_.abandoned }                              # 批次字段只用于 abandoned 过滤
        | where { StateOf($_) 非终态 }                            # StateOf 见下；终态 = COMPLETE /
                                                                  #   TRANSLATION_NOT_APPLICABLE / ABANDONED
  # StateOf(item) := Replay("games\<dirname>\.gameflow\events.jsonl").state
  #                  （state.json 只是派生快照，可随时删掉重建，I2）

  # ── 2. 采样阶段（服务于 G2/G4，参数见 §5.3.3） ─────────────
  n = max(sample_count, ceil(quiet_window_seconds / sample_interval_seconds) + 1)   # = 4
  samples = []
  repeat n times:
        samples += SnapshotDir("<LibRoot>\_inbox", recurse=true, force=true)   # {path,length,mtimeUtc}
        samples += SnapshotDir(每个 item 的 games\<dirname>\)
        对**已认领**文件：Append(item, kind:"download.gate", payload:{g1..g6, sample_index, file})
        Sleep(sample_interval_seconds)   unless 已达 max_run_seconds

  # ── 3. 认领归位（§5.2） ────────────────────────────────────
  groups = GroupVolumes(_inbox 的文件, 命名族规则)        # §5.5：必须在任何类型判定之前
  for g in groups:                                        # 组是认领的最小单位，全组或全不动
        owner = ClaimMatch(g, manifest.items)             # M0→M1→M2→M3，见 §5.2.2
        if owner == null: 记入报告「待认领」区; continue   # 认不出来 = 不动
        if not (G1 ∧ G2 ∧ G3 ∧ G4)(g, samples): continue  # 还没下完就别搬
        MoveGroupSameVolume(g → games\<owner.dirname>\)    # I5 ③；跨卷拒绝，见 §5.2.4
        每成功移动一个文件 → Append(owner, kind:"download.claimed",
                                   payload:{file, bytes, from, matched_by})

  # ── 4. 逐条目推进（每个 item 独立，I4） ────────────────────
  for item in items:
        item_lock = TryOpen("games\<dirname>\.gameflow\.lock", …, FileShare.None)
        if (item_lock == null): 记「本轮跳过」; continue
        try:
            st = Replay("games\<dirname>\.gameflow\events.jsonl")      # §3.3.5
            # 末行撕裂 → 丢弃并封口（journal.torn_line_sealed），不是停机
            # **非末行**出现不可解析行 → Emit(JOURNAL_CORRUPT); continue   （§4 停机态）
            # state.json 缺失/不可解析 → 只跑 §3.9.3 Recover 的「重建」段，写 state.rebuilt
            switch st.state:
              AWAITING_DOWNLOAD:
                  if AllGates_G1toG6(item):  Emit(DOWNLOAD_STABLE)
                  elif 头加密(G5 exit 255):  Emit(WAITING_FOR_PASSWORD)   # §5.7
                  elif 卷集异常:              Emit(WAITING_FOR_VOLUMES)    # §5.5.4
                  else:                       留在 AWAITING_DOWNLOAD
              DOWNLOAD_STABLE:
                  r = ReadOnlyPreflight(item)                             # §5.6
                  if r.zip_encoding_risk:     Emit(FILENAME_ENCODING_RISK) # §5.8
                  elif r.manual_install:      Emit(WAITING_FOR_MANUAL_INSTALL)  # 判别见 §6.13.1
                  elif r.unknown_container:   Emit(WAITING_FOR_RULE)
                  elif r.resource_block:      Emit(BLOCKED_RESOURCE)       # §5.6.3 / §5.6.5
                  else:                       Emit(PREFLIGHT_OK)
              其余状态:  只刷新观测字段，不改状态（那些状态归 B / C 推进）
        finally:
            Flush($true); item_lock.Dispose()

  # ── 5. 出对账（§5.9） ──────────────────────────────────────
  digest = SHA256(规范化对账表)
  if digest != 上一份报告 front-matter 里的 digest:
        Write("<LibRoot>\_reports\<batch-id>-<seq+1>.md")    # I5 ②；从不覆盖、从不删除旧报告

  batch_lock.Dispose()
exit 0
```

状态迁移一律走 `state.entered` 事件（`payload.from_state` / `to_state` / `transition_id`），事件与 `payload` 键名以 §3.3 为准；上面伪码里的 `Emit(<状态>)` 是它的简写。

#### 5.1.3 退出码

以 §8.2.3 为准，A 用到其中五个：

| 退出码 | 含义 | 谁看 |
|---|---|---|
| `0` | 本次调用正常结束。**不代表所有条目成功**——「有条目落在停机态」是正常结果，出口是报告与 `events.jsonl` | 任务计划历史 |
| `2` | 参数不合法：slug 不匹配、未知参数、**批次不存在** | 人 / skill |
| `3` | 前置条件不满足：缺 `7z.exe`、`.gameflow` 与库不同卷、执行策略被 GPO 锁死、**写白名单路径不可写** | 人 / skill |
| `70` | 内部错误（未捕获异常）。此时必然已写了一条 `level=error` 事件 | 人 |
| `75` | 拿不到批次锁（`LOCKED_BY_ANOTHER_RUN`）。多批次模式下仅当**一个批次都没能处理**时返回 | 任务计划历史 |

A **不会**因为某个条目失败而返回非零。

#### 5.1.4 互斥

- 两层锁：批次级 `<LibRoot>\batches\<batch-id>.lock` + 条目级 `games\<dirname>\.gameflow\.lock`，都用 **`FileShare.None` 独占文件句柄**。进程死亡时 OS 自动释放，无陈旧锁问题；句柄属进程不属线程，无 Mutex 的线程亲和性坑；文件系统无会话隔离，计划任务与交互会话天然互斥。【官方 CreateFileW `dwShareMode=0` 语义】+【社区 PowerShell 写法】
- B 取同名的两把锁（§6.1.2 步骤 00 / 02），**A 与 B 因此争同一句柄**——锁名对不上等于互斥静默失效。
- **不使用 lock 文件 + PID 存活检测**（Windows PID 复用快；「超时清理陈旧锁」引入时间猜测，与确定性偏好冲突）。
- 若额外使用命名 Mutex，**必须 `Global\` 前缀**——默认 `Local\` 只在当前终端服务会话内可见，计划任务与交互会话互相看不见。【官方 .NET Mutex 文档】
- 第二道保险在调度侧：`MultipleInstances = IgnoreNew`（§5.10.2）。
- 【待测】杀掉持锁进程后能否**立刻**抢到 `FileShare.None`（「没有陈旧锁」这个前提的基础）。不测的后果：崩溃后的第一轮巡检可能整批空转，而表现与「另一实例在跑」完全一样。→ §11。

---

### 5.2 认领归位（inbox claim）

#### 5.2.1 为什么必须有这一步

Codex 的内置浏览器**没有「按任务/按游戏」的下载目录**——112 KB 的官方配置参考里 `download_dir` 一类的键 **0 命中**，只有一个 GUI 全局设置或「每次询问」。【官方】+【待测】§11 #5：需在目标机的「设置 > 浏览器」里目视确认还有没有别的选项。

所以上游设计里「Codex 将下载目标定到 `games\<游戏名>\`」**这个能力不存在**。取而代之的契约是：

```
全局下载目录 = D:\GameLib\_inbox        （唯一下载落地目录，D-02）
        ↓  执行体 A 按批次清单认领归位
D:\GameLib\games\<dirname>\             （C3 的终态保证）
```

**C3「源包与游戏本体同目录」是终态保证，不是落地瞬间的保证。**

`dirname` 由 `New-Batch.ps1` 按 **§3.2A 的生成算法**产出并写进批次清单；**A 既不生成也不改名**，只按清单建目录（`item.registered`）与移入文件。

#### 5.2.2 匹配优先级链（M0 → M3，先匹配先赢，命中即停）

归组（§5.5）先于匹配：**匹配的对象是「卷集组」，不是单个文件**。单文件视为大小为 1 的组。

| 编号 | 判据 | 输入 | 确定性 | 命中后 |
|---|---|---|---|---|
| **M0** | 组内**每一个**文件名（大小写不敏感、序数比较）都出现在某个 `item.expected_files[]` 中，且该 item 的 `expected_files` 没有被别的组同时命中 | 批次条目（§3.2.5） | 完全确定 | 认领，`matched_by:"M0"` |
| **M1** | 组的**分卷组名**（去掉卷号后缀后的 basename，见 §5.5.2）精确等于某个 `item.expected_files[]` 元素去掉卷号后缀后的 basename；且卷数 == `expected_parts`（若声明） | 批次条目 | 完全确定 | 认领，`matched_by:"M1"` |
| **M2** | 组名匹配 `source-profiles.json` 中该条目所属 profile 的 `filename_patterns[]`（**显式正则，人工登记**，schema 见 §3.6.1），且该 profile 在本批次内**只被一个 item 引用** | 站点/上传者档案 | 确定（规则是人写死的） | 认领，`matched_by:"M2"` |
| **M3** | 人工认领：`_inbox\claims.json`（人手写、A 只读，登记在 §2.4 布局树、schema 见 §3.6）给出 `{"file_or_group": "...", "item_id": "..."}` 的显式映射 | 人 | 完全确定 | 认领，`matched_by:"M3"` |
| — | 以上全不命中 | | | **不动**，进报告「待认领」区 |

**M2 的边界必须写死**：`filename_patterns[]` 是人在 `source-profiles.json` 里为某个站点/上传者登记的正则（例如 `^\[ookkgg\]_.+\.(mp4|png)$`）。A **不生成、不学习、不模糊匹配**任何模式。没有 profile、或 profile 在批次内被多个 item 引用（歧义）→ 视为不命中。

**禁止清单**（每一条都是「聪明的猜测」，都不做）：
- ❌ 用游戏标题与文件名做编辑距离 / 子串 / 模糊匹配
- ❌ 用文件大小或下载时间「就近」归属
- ❌ 用「批次里只剩这一个 item 没文件了，那就是它」做排除法推断
- ❌ 从压缩包内容（`7z l` 出来的条目名）反推属于哪个游戏
- ❌ 命中多个 item 时按某种优先级挑一个

**歧义（同一组命中 ≥2 个 item，或同一 item 被 ≥2 个组命中）一律判为不命中**，落「待认领」，报告里列出所有候选让人一眼裁决。

#### 5.2.3 归位动作：移动，不是复制

| 项 | 契约 | 理由 |
|---|---|---|
| 动作 | `[System.IO.File]::Move(src, dst)`（同卷 → NTFS 目录项重链接，不搬数据） | 复制会在 `_inbox` 留一份，日后要么占着几十 GB、要么需要谁去删——而 A 不能删（A-R3） |
| 目标存在同名文件 | **不覆盖、不改名、不合并**。整组停手，落报告「异常」区，人工处理 | 覆盖 = 数据丢失 |
| 组的原子性 | 逐文件移动，**移动前先检查全组目标名都不存在**；中途失败则**已移动的文件留在原地不回滚**（每个成功的移动各自写一条 `download.claimed`），落报告「异常」区 | 回滚要再移一次 = 又一次可能失败的写操作；把半成品状态如实报出来比假装原子更诚实。下一轮 A 会看到「组的一部分已在 item 目录、一部分还在 `_inbox`」并按 M0 继续补齐 |
| 时间戳 | 不修改 `LastWriteTimeUtc` / `CreationTimeUtc`（`File.Move` 天然保留） | G2 的下一轮判据依赖 mtime |
| 重命名 | **A 永不改名。** 分卷 `.NNN` 序列重建（C7 的唯一硬例外）在 §6.5 的 `_stg` 里对**副本**做，原始文件零修改 | A-R3 +「绝不把未知文件批量改成 .zip/.rar」由结构保证 |

#### 5.2.4 跨卷：A 拒绝执行

`_inbox` 与 `games\` 按 §2.4 的目录布局同在 `D:\GameLib\` 下，天然同卷。若 Preflight（§9）探测到二者**不同卷**（用户把下载目录设到了别的盘）：

- 跨卷 `File.Move` 在 .NET 上退化为「复制 + 删除源」——**删除源违反 A-R3**。
- 契约：A **不做跨卷认领**。整批认领跳过，报告顶部长期显示一条阻塞提示。
- 这**不是**一个条目级停机态（不是某个游戏的问题），所以不落状态机，只在报告里显示。

#### 5.2.5 「待认领」区与后缀残留

`_inbox` 里认不出归属的东西，A 的处理是**看一眼、记下来、什么都不做**：

- 不移动、不改名、不删除、不解压、不读内容（除魔数分诊的前 32 KB + ISO 偏移 32769 处 5 字节，见 §5.4.4）。
- 报告里给出：组名、文件数、总字节、首卷魔数分诊结果、以及**为什么没认出来**（无 `expected_files` 声明 / 命中多个候选 / profile 歧义 / 命中 `known_false_complete`）。
- 认不出来**不产生任何状态迁移**——没有 item 就没有状态机。它只是报告里的一行。

命中 `netdisk-profiles.json` 的 `known_false_complete`（已知典型：百度网盘客户端下载完成后 `.baiduyun.p.downloading` 后缀仍残留【社区】，多篇用户反馈一致）时，**A 绝不自动去后缀**（去错了就毁掉一个未完成文件）：

- 未认领 → 进「待认领」区，标注「疑似已完成但后缀残留，需人工确认后改名」。
- **已认领到某个 item** → G1 判假，条目**留在 `AWAITING_DOWNLOAD`**，**不落停机态**（`WAITING_FOR_RULE` 的 reason 闭集里没有这一类，且它的解除者按 §4 是 B，而这里该动手的是人与下一轮巡检）。作为补偿，报告的「⛔ 等你做事」区**必须**每轮为它打出一行固定标注（§5.9.1 的强制标注 ②）——否则就会复刻 §5.3.4 那条「永远卡在 `AWAITING_DOWNLOAD` 且没人知道」的失败模式。

---

### 5.3 下载完成判定 G1–G6

#### 5.3.0 为什么「文件大小连续 N 次不变」是会**稳定产出错误结论**的判据

不是「偶尔不准」，是**在真实的网盘下载场景里系统性地给出错误答案**。六种互相独立的机理，任何一种单独出现就能击穿它：

| 编号 | 机理 | 后果与证据 |
|---|---|---|
| **B1** | **aria2 / Motrix 预分配**（`--file-allocation` 默认 `prealloc`，官方手册推荐 NTFS 用 `falloc`：「almost instantly」） | 文件**从第 0 秒起就是最终尺寸**，实际落盘 0 字节；轮询尺寸的检测器**第一轮**就判「完成」。【官方 aria2 手册】+【实测 macOS 稀疏文件 `st_size = 536870912` / `st_blocks*512 = 0`，写入后 `st_size` 不变；Windows 对应 `SetEndOfFile`/`FSCTL_SET_SPARSE`/`SetFileValidData`，**可外推**】 |
| **B2** | **限速 / 暂停 / 重连造成的假稳定** | 只停 **6 秒**就骗过「连续 3 次 × 1 秒」判据，拿到 50% 的半截文件；网盘限速排队动辄停几分钟，**把 N 调大只是把失败概率往后推**。【实测 macOS `stall.py`：`t=4.0s 判 COMPLETE at size 3072`，`FINAL real size: 6144`；与 OS 无关，**可外推**】 |
| **B3** | **Firefox 在目标目录留 0 字节最终名占位**（源码：占位符在发起网络请求之前就建），真身在同名 `.part` | 目录里同时有 `游戏A.rar`（0 字节，永远稳定）和 `游戏A.rar.part`。【官方 Firefox 源码 `DownloadCore.sys.mjs`】 |
| **B4** | **IDM 无临时后缀缓慢合并**：分块在 `%APPDATA%\IDM\DwnlData\`，**目标目录里没有任何临时物** | 目标目录里凭空出现最终名文件并缓慢增长，全程零线索。【社区】+【官方 IDM FAQ 标题即 "It takes a very long time to rebuild temporary files"】 |
| **B5** | **百度网盘完成后后缀残留**：任务列表显示成功，`.baiduyun.p.downloading` 还在 | 「无临时后缀」既不是必要条件也不是充分条件。【社区，多篇用户反馈一致】 |
| **B6** | **Chromium 先改名后扫描**（源码注释明写「改名之后再打标，留下一个文件已是最终名但尚未被标记的小窗口」；`IAttachmentExecute::Save()` 注释：*"IAE::Save() will delete the file if it was found to be blocked by local security policy or if it was found to be infected"*） | **文件以最终名出现的那一刻，它还可能在下一秒被 Windows 附件服务/杀软删掉**。【官方 Chromium `download_file_impl.cc` / `quarantine_win.cc`】 |

补充两条同样能骗过尺寸判据的形态：网盘「秒传」（服务端命中哈希，尺寸从 0 直接跳到最终值）【社区】；以及**多文件批次里「A 卷完成、B 卷还没开始下」**——`游戏A.part2.rar` 在目录里**根本不存在**，只看「现有文件都稳定」就会判整个游戏就绪（逻辑必然，由 G6 兜住）。

#### 5.3.1 `DOWNLOAD_STABLE` 的定义与求值次序

```
DOWNLOAD_STABLE  ⟺  G1 ∧ G2 ∧ G3 ∧ G4 ∧ G5 ∧ G6
```

六重**与**条件，任一不成立就不是 `DOWNLOAD_STABLE`。求值顺序（廉价的先做、贵的后做、有副作用窗口的紧贴动作）：

```
        ┌── G1 无临时物 ─────────────┐  纯目录枚举，最廉价
        ├── G2 尺寸+mtime 双静默 ────┤  用采样阶段已有的快照，零额外 IO
        ├── G3 独占打开 ────────────┤  每文件一次 CreateFile
        ├── G4 宽限期 + 存在性复检 ──┤  紧贴认领动作之前
        │        ↓ 认领归位（§5.2）
        ├── G6 卷集连续 + 除末卷外等大 ┤  纯算术，抓缺卷比调 7z 快一个量级
        └── G5 7z l -slt 只读头自校验 ┘  唯一要起子进程的一条，最后做
```

> **顺序不是 G1..G6 的字面顺序**：G6 是纯算术，放在 G5 之前能在起 7z 之前就抓出缺卷；G4 的存在性复检必须**紧贴**认领动作，否则 B6 的删除窗口会漏进来。G1–G6 定义的是**合取关系**，求值次序由本节固定。

一轮六重判据的结果落一条 `download.gate` 事件（`g1..g6` 各为 `true`/`false`/`"skipped"`，§3.3.2）。

#### 5.3.2 G1 无同组临时物

- **判据**：以组为单位，对 `_inbox` 枚举（`Get-ChildItem -Force -Recurse`，`-Force` 是为了看见隐藏/系统属性的临时物），组的目录内不存在 `netdisk-profiles.json` 的 `temp_suffixes[]` / `temp_name_globs[]` / `sidecar_pairs[]` 任一模式命中的文件。
- **模式表是数据不是代码**，全部字段、取值域与预填内容以 **§3.6.2** 为准，本节不复制。两条必须记住的语义：
  - Chromium 对 dangerous 下载用**随机中间名** `Unconfirmed <random>.crdownload`（与游戏名毫无关系）——按「最终名 + `.crdownload`」匹配会**漏判**，所以 `temp_name_globs[]` 这一维不能省。【官方 Chromium 源码 `kUnconfirmedFormatSuffix`】
  - profile 的 `confidence` 为「未登记」档时：G1 只按通用后缀表判定、G2 的静默窗口按 §7.5 的放大值取、并在报告里打标。语义三档以 §3.6.2 为准。
- **阿里云盘 / 夸克网盘 / 123 云盘的临时后缀查不到任何权威或可交叉验证的资料**【待测】（§11 #19）。不实测的后果：这三家只能靠 G2/G3/G4 的通用判据，B4 型（合并期无临时物）失效模式无从检测。**首次使用某网盘客户端时人工观察一次并登记**——这正是「阶段一需要人工做的一部分」的第 ③ 项（D-14）。
- 命中 `known_false_complete` → 不判成功也不当异常，走 §5.2.5。
- **单独失效**：对 **B4（IDM）完全无效**——目标目录里本来就没有临时物，G1 恒真。对 B5（百度残留）方向相反：明明完成了却恒假。

#### 5.3.3 G2 尺寸与 mtime 双静默（采样模型）

**判据**：候选文件的 `Length` **和** `LastWriteTimeUtc`，在一组采样点上全部相等，且这组采样点满足「点数 ≥ `sample_count`」**与**「首末时间差 ≥ `quiet_window_seconds`」。

**锁定参数**（登记在 §3 的 `netdisk-profiles.json` / `guards`，本节只引用不另立默认值）：

| 参数 | 定值 |
|---|---|
| `sample_count` | **3** |
| `sample_interval_seconds` | **40** |
| `quiet_window_seconds` | **120** |
| `max_run_seconds` | **900** |
| 巡检间隔 | **10 分钟**（§5.10.4） |
| 任务计划 `ExecutionTimeLimit` | **30 分钟**（§5.10.2） |

由此推出运行内采样次数 `n = max(sample_count, ⌈quiet_window_seconds / sample_interval_seconds⌉ + 1) = 4`（t = 0/40/80/120 秒，首末差恰好 120 秒）。这是**推导值不是新参数**。

**两类文件，两种采样源**：

| 文件 | 采样源 | 为什么 |
|---|---|---|
| `_inbox` 中**未认领**的文件 | **只能运行内采样**（本轮 4 个采样点） | **结构性限制，不是设计选择**：未认领文件没有 `item_id`，而 `item_id` 是事件 schema 的必填字段（§3.3.1）；它也没有任何 A 被允许写入的目录（I5 白名单只到 `games\<dirname>\`）。因此 A 无法为它持久化任何跨运行的采样记录 |
| **已认领**到 item 的文件（含已归位的） | 本轮采样 **∪** 最近 `sample_count` 条 `download.gate` 事件里的采样点 | 已认领文件有 `.gameflow\events.jsonl` 可写，跨运行采样点因此可用。好处：某轮被 `max_run_seconds` 截断、或文件恰在本轮窗口边缘静止时，下一轮能直接接上，而不必重新等满 120 秒 |

**实现要点**：
- **必须同时看 mtime，不能只看 size**：B1 的预分配文件尺寸恒定，但往中间写数据时 mtime 会跳——mtime 是抓预分配的唯一廉价信号。
- **拒绝 0 字节文件**（`MinBytes = 1`），直接挡掉 B3 的 Firefox 占位。
- 跨运行采样点与本轮采样点**同等对待**，按 `ts` 排序后取首末差；两者之间的空档（巡检间隔）不做任何插值假设。
- **单独失效**：**B2 的长停顿**。停顿超过 `quiet_window_seconds` 就骗过它，而网盘限速排队常常就是几分钟。**把窗口调大只是把概率往后推，不能消除**——这就是为什么 G2 必须和 G3/G5 合取。

#### 5.3.4 G3 独占打开

- **判据**：
  ```powershell
  [System.IO.File]::Open($LiteralPath,
        [System.IO.FileMode]::Open,
        [System.IO.FileAccess]::ReadWrite,
        [System.IO.FileShare]::None)     # dwShareMode = 0
  ```
  成功即通过，`finally` 里立刻 `Dispose()`。
- **实现要点**：
  - `FileShare::None` = `CreateFileW` 的 `dwShareMode = 0`。官方原文：`"If this parameter is zero and CreateFile succeeds, the file or device cannot be shared and cannot be opened again until the handle to the file or device is closed."`【官方 CreateFileW】→ **打开成功就证明此刻全系统没有任何其他句柄开着这个文件**。
  - **必须用 `[System.IO.File]::Open`，不能用 `Test-Path` / `Get-Content`**——后者以共享方式读，开着的下载文件照样读得出来，判不出占用。
  - 一律 `-LiteralPath` / `.NET` API，不用 `-Path`（文件名里的 `[ ]` 会被当通配符）。
  - 分卷组内**逐个文件都要测过**，不是只测首卷。
- **异常区分（这一条写错会永久卡死）**：

  | 异常 | 真实含义 | 处置 |
  |---|---|---|
  | `System.IO.IOException`（典型 `ERROR_SHARING_VIOLATION`） | **文件被占用**，还有人开着它 | 判 G3 失败，**继续等**，下一轮再看。`download.gate` 记 `g3:false` |
  | `System.UnauthorizedAccessException` | **权限 / 只读属性 / ACL 问题**，跟「还在下载」毫无关系 | **绝不能当成还在下载**。记 `download.gate`（`g3:false` + `reason_code`，§3.3.2），落报告「异常」区；若已认领到 item，落 **`BLOCKED_RESOURCE`**，`reason_code = PERMISSION_DENIED`（§4 停机态手册 H03）。把它当成「还在等」会让这个条目**永远卡在 `AWAITING_DOWNLOAD`，每轮重试、永不推进、也永不报错** |

  > 这是本节最容易写错、且错了最难发现的一条：两种异常都表现为「打不开」，但一个该等、一个该停。**没有 `gate.G3_denied` 这种事件**——权限拒绝也走 `download.gate`，事件枚举是封闭的（§3.3.2）。
- **单独失效**：B3 的 0 字节占位文件**没有任何进程持有**，G3 单独会通过（由 G2 的 `MinBytes` 挡住）；B4 的 IDM 在两个分块之间可能短暂无句柄；B1 预分配后的空闲间隙同理。

#### 5.3.5 G4 宽限期与存在性复检

- **判据**：文件**以最终名**（即不匹配任何临时物模式）被采样首次看到之后，至少经过 `grace_seconds_after_final_name`（§3.6.2，默认 8 秒）；且**在执行认领 move 之前的瞬间**再做一次 `[System.IO.File]::Exists` + `(Length, LastWriteTimeUtc)` 与最后一次采样的一致性复检。
- **实现要点**：
  - 存在的唯一理由是 **B6**：Chromium 先改名到最终名、再调 `IAttachmentExecute::Save()` 打 MOTW 并送 AV 扫描，而这个调用**可以直接把刚出现的文件删掉**（`E_FAIL` 病毒感染 / `INET_E_SECURITY_PROBLEM` 被本地安全策略阻止 / 其他失败也可能删，crbug.com/153212）。【官方 Chromium 源码注释】
  - 「首次以最终名看到」用**采样序列**判定，不用文件系统的 `CreationTimeUtc`——NTFS 上 `MoveFile` 保留创建时间，`.crdownload` → 最终名的重命名会让 `CreationTimeUtc` 指向**临时文件创建的时刻**，从而**高估**已流逝时间，把宽限期变成零。这个方向的错误是不安全的。
  - `grace_seconds_after_final_name` 通常已被 `quiet_window_seconds` 覆盖，但**必须独立判定**：文件可能在 `.crdownload` 名下静止很久（B2 的停顿），最终名却是刚刚才出现的。
- **单独失效**：G4 只保证「这个名字稳定存在了 8 秒」，对内容完整性零信息量。单独用它等价于什么都不做。

#### 5.3.6 G5 只读归档头自校验

- **判据**：对组的**首卷**（§5.5.1）经 §6.2.1 的唯一封装发一次只读列举：`-Op 'l'`、`-TypeSpec none`、**不传 `-Password`**。要求 `ExitCode == 0` **严格相等**，且解析出的**文件条目数 > 0**。命令行形状、固定开关、禁用开关、超时与 `$LASTEXITCODE` 检查全部在 §6.2.1，本节不重复。
- **A 侧的两条固定取值**：
  - **不传 `-Password`**（A-R1）。数据加密的包 `l` 照样能列出全部条目并给出 `Encrypted = +`，判据成立；头加密包在此 exit 255 → §5.7。【实测 macOS 7zz 25.01，**可外推**：`l` 的密码/头加密行为在 `CPP/7zip/UI/Console` 公共代码里，与平台无关】
  - **`-TypeSpec none`**（= 不传 `-t`，走 7-Zip 默认的 `-t*:r`：内容嗅探 + 8 MB 签名扫描）。理由见 §5.6.1，与 §6.2.7 一致。
- **实现要点**：
  - **stdin 必须重定向**，否则加密包会挂在 `Enter password:` 变僵尸进程。macOS 上 `</dev/null` 得到 exit 255 而不是挂死【实测】；**Windows 的控制台密码读取可能走 `CONIN$` 而不是 stdin，重定向未必挡得住**【待测】（§11 #7）。不验证的后果：后台计划任务里挂死一个无输出的进程，是最难发现的故障模式。→ 封装层的超时（§6.2.1 的 `-TimeoutSec`）是这一条的兜底，**不是可选项**。
  - exit 0 也可能伴随 `Warnings:`（如 `.exe` 扩展名让 7-Zip 先试 PE handler 失败的良性回退 `Open WARNING: Cannot open the file as [PE] archive`）。**「有 Warnings」与「exit 1」是两条独立信号，必须分别检查**【实测】。A 阶段对 warning 只记录、不阻断；warning 黑白名单归 §6.2.3（它才决定删不删源包）。
  - 7-Zip stdout 在 Windows 上按什么编码输出（日文条目名的正确解码）**【待测】**。不验证的后果：`Path` 字段解析出乱码，§5.6.5 的路径穿越预检与条目对账会在日文包上系统性误判。A 把 `[Console]::OutputEncoding` 与 `chcp` 结果写进 `env.snapshot` 事件（§3.3.2）作为解释上下文。→ §11。
- **单独失效**：
  - **`l` 只读头，不解码数据**。实测把一个 ZIP 条目的方法号改成 97（WavPack）：`7zz l weird.zip → exit 0，条目照常列出`，而 `7zz t weird.zip → exit 2 Unsupported Method`【实测】。→「能 `l`」≠「数据完整」，真正的完整性由 §6.2 的 `7z t` 负责。
  - **头加密包 G5 恒失败**（假阴性），必须由 §5.7 单独分流，否则一个完全下完的包会被永远判成「没下完」。
  - `l` 成功之后，B6 的删除窗口仍然存在——所以 G4 的复检必须在 G5 之后、动作之前再做一次。

#### 5.3.7 G6 卷集连续与等大

判据、算法与「末卷恰好等大」的处置全部在 §5.5。此处只记合取关系：`G6` 为假时**不进入** `DOWNLOAD_STABLE`，落 `WAITING_FOR_VOLUMES` 或留在 `AWAITING_DOWNLOAD`（区分规则见 §5.5.4）。

**单独失效**：对**未分卷的单文件**，G6 是空条件、恒真，零约束力。

---

### 5.4 归档识别

#### 5.4.1 分诊顺序：先按命名分组，再判型

**这一步的顺序是硬契约，颠倒了会把流程卡死。**

分卷的**中间卷是数据碎片，没有归档魔数**。实测：`zip -s 64k` 切出的 8 卷里，`split.z04` 被 `file(1)` 判成 **"DOS executable (COM)"**，其余中间卷判成 `data`。【实测 macOS，机理与 OS 无关，**可外推**：中间卷的头 N 字节就是上一卷压缩流的续写，落到哪个字节完全由切分点决定】

如果写成「扫描目录，凡嗅探出可执行文件就报警 / 交给 Defender / 停下来问人」，这类误报会把**每一个分卷包**都卡死。

```
       ┌─────────────────────────────────────────────┐
       │ 1. 按命名族归组（§5.5）                       │  ← 纯字符串，不读一个字节
       │    .partN.rar / .rNN / .NNN / .zNN+.zip      │
       └──────────────────┬──────────────────────────┘
                          │  组内只把「首卷」送去判型
       ┌──────────────────▼──────────────────────────┐
       │ 2. 魔数分诊（读首卷前 32 KB + 偏移 32769）     │  ← 只做分诊，不做权威判定（§5.4.4）
       └──────────────────┬──────────────────────────┘
       ┌──────────────────▼──────────────────────────┐
       │ 3. 7z l -slt 的 Type 字段 = 权威类型判定      │  ← §5.6
       └─────────────────────────────────────────────┘
```

#### 5.4.2 魔数的职责边界

魔数是**分诊器**，回答三个问题：这东西值不值得喂给 7-Zip、它是不是 MZ/ISO 这类要停人的形态、RAR4 还是 RAR5。它**不是**类型判定的权威——权威是 `7z l -slt` 的 `Type` 字段。

理由：7-Zip 默认在文件**前 8 MB 内扫描签名**（默认 `-t*:r`），偏移 0 的魔数失配完全不代表它打不开。实测 1 KB / 3 MB 前缀垃圾的 7z 包 `l`/`x` 全部直接成功（只是 `Offset` 字段变了），9 MB 前缀才失败、`-TypeSpec *:s16m` 一个开关就恢复。【官方 type.htm】+【实测】

#### 5.4.3 「7z 能打开它」≠「要递归解它」

误报面与逐条理由（`.exe/.dll/.sys → Type = PE`、`.docx/.apk/.jar/.epub → Type = zip`、`.msi/.doc → Compound`、镜像各有 handler）**以 §3.6.3 为准**，本节不复述。一句话结论：**容器判定必须用「类型白名单 + 扩展名排除表」，绝不能用「7z 能不能打开」当判据**——这是扩展名唯一真正有用的场合：不是用来识别要解什么，而是用来排除不该解什么。【实测 macOS：`7zz` 自身 `l` exit 0、`Type = Mub`，`x` 真会把它拆成 `.x64`/`.arm64` 两片；`7zz i` 里 `PE exe dll sys / M Z` 一行确认 Windows 上每个 exe/dll 都是「归档」，**可外推且命中面更大**】

`rules\container-types.json` 的**键名、取值与全部清单以 §3.6.3 为准**（键：`recurse_into_types` / `never_recurse_types` / `excluded_extensions` / `content_file_exclusions` / `manual_install_types`），A 只读、本节不复制。A 侧的判定顺序：

```text
IdentifyTopLevel(首卷):                        # 对象是**批次清单声明的源包**
    t = (§5.6.1 的 l -slt).Type
    if t == null (l exit 2):                    -> §5.6.1 的 exit 2 分支
    if 命中 §6.13.1 的 R1/R2/R3:                -> WAITING_FOR_MANUAL_INSTALL   # 安装器/镜像/未知 MZ
    if t ∈ recurse_into_types:                  -> 走自动流程
    if t ∈ never_recurse_types:                 -> WAITING_FOR_MANUAL_INSTALL 或 WAITING_FOR_RULE（按 §6.13.1）
    else:                                       -> WAITING_FOR_RULE            # 白名单之外一律不猜、不兜底放行
```

> **`excluded_extensions` 对源包不适用**（§3.6.3 已写死适用范围）：源包走纯内容嗅探，否则伪装成 `.mp4` 的分卷组与 SFX `.exe` 会被扩展名直接排掉。排除表只作用于 `_stg\` 里的**解压产物**，那是 §6.4 的事。

- **`Type` 字符串的确切拼写与大小写必须在目标机用 `7z.exe i` 逐字核对**【待测】（§11 #1）。不核对的后果：白名单全部落空，**每个包都落 `WAITING_FOR_RULE`，流水线整体停摆**。建议照 §6.9.3 的手法把它变成运行期可检测条件：Preflight 跑一次 `7z.exe i`，与 `container-types.json` 的 Type 集合求差集，有差集就在报告顶部长期提示。

#### 5.4.4 魔数表（**全文权威**，含偏移）

本表是魔数与偏移的**唯一权威**，§6.13.1 等处一律引用本表，不再自列。

| 偏移 | Magic (hex) | 判定 | 本项目的处置 |
|---|---|---|---|
| 0 | `50 4B 03 04` | ZIP（本地头） | 自动 |
| 0 | `50 4B 05 06` | ZIP（空归档 / EOCD 直出） | 自动 |
| 0 | `50 4B 07 08` | ZIP（跨卷标记） | 自动（按卷集处理） |
| 0 | `52 61 72 21 1A 07 00` | **RAR4**（7 字节） | 自动。RAR4 的密码错与数据损坏在工具层不可区分，见 §6.3.8 的 `EXTRACT_FAILED_AMBIGUOUS` |
| 0 | `52 61 72 21 1A 07 01 00` | **RAR5**（8 字节，比 RAR4 多一字节） | 自动 |
| 0 | `37 7A BC AF 27 1C` | 7z | 自动 |
| 0 | `1F 8B 08` | gzip | 自动 |
| 0 | `42 5A 68` | bzip2 | 自动 |
| 0 | `FD 37 7A 58 5A 00` | xz | 自动 |
| 0 | `28 B5 2F FD` | zstd | 自动 |
| 0 | `4D 53 43 46` | CAB | 自动 |
| 0 | `4D 5A` | **MZ**（PE / SFX / 安装器） | **不自动**，按 §6.13.1 的 R3 分流 |
| **257** | `75 73 74 61 72` | tar（tar 在偏移 0 **没有**魔数） | 自动 |
| **32769** | `43 44 30 30 31` | ISO9660（"CD001"） | `WAITING_FOR_MANUAL_INSTALL` |

【实测】签名与偏移取自 7-Zip 自身的 handler 签名表（目标机应以 `7z.exe i` 的输出为准），并用真实样本 `xxd` 交叉验证；签名表是 7-Zip 跨平台共用的，**可外推到 Windows**。（本表不是官方文档原文，因此整体标【实测】，不标【官方】。）

补充分诊行（用于报告里给人看的提示，来源同上，均【实测】）：`D0 CF 11 E0 A1 B1 1A E1` @0 = Compound（MSI/DOC/XLS）；`4D 53 57 49 4D 00 00 00` @0 = WIM；`1F 9D` @0 = Z(compress)；`2D 6C 68` @**2** = LZH。

**「不进自动流程的形态」的判别方法（R1–R4，含 SFX / NSIS / Inno / setup.exe / 未知 MZ）以 §6.13 为权威**，A 与 B 共用同一套规则，本节不复述。A 在这里只多做一件事：把 `Offset > 0`（SFX / 带前缀容器的确定性标志）与首卷魔数一并记进 `archive.identified` 事件，供报告与 §6.13.1 的 R3a 使用。SFX 在 Windows 上是否会被 PE handler 抢先打开，是 §11 #8 的待测项（后果见 §6.13.2）——在它有结论之前，A 对 MZ 的保守分流会让**每一个真 SFX 都停在 `WAITING_FOR_MANUAL_INSTALL`**，这是已知的高频假阳性，必须在报告里写明原因，而不是让人以为包坏了。

---

### 5.5 卷集归组与连续性检查

#### 5.5.1 四种命名族与首卷位置

| 命名族 | 卷名形态 | **首卷（喂给 7-Zip 的那个）** | 谁产生 |
|---|---|---|---|
| **F1 `.NNN`**（7-Zip Split 容器） | `x.7z.001`, `.002`, …；`.NNN` **之前的扩展名无所谓** | **`.001`** | 7-Zip `-v` |
| **F2 `.partN.rar`** | `x.part1.rar`, `x.part2.rar`…（或 `part01`/`part001`，**补零位数随卷数且必须一致**） | **`.part1.rar`**（按实际补零位数） | WinRAR (RAR5 / 现代 RAR4) |
| **F3 `.rNN`**（旧式 RAR4） | `x.rar`, `x.r00`, `x.r01`, … | **`x.rar`** | WinRAR `-vn` |
| **F4 `.zNN` + `.zip`**（ZIP 真跨卷） | `x.z01`, `x.z02`, …, `x.zip` | **`x.zip`（最后那个！）** | WinZip / `zip -s` |

⚠ **F1 与 F4 的方向是相反的**：`.001` 从第一个开，**分卷 ZIP 的中央目录在最后那个 `.zip` 里**，必须从末尾那个 `.zip` 开。实测 7-Zip 两端都认（`7zz t span.z01 → exit 0, Volume Index = 0`；`7zz t span.zip → exit 0, Volume Index = 2`），但**规则要写对**——因为 §5.8 的 ZIP 编码检测必须解析**含中央目录的那一卷**，方向搞反就什么也解析不到。【实测 macOS 7zz 25.01，分卷装配逻辑跨平台，**可外推**】

【实测】F1 的 `.NNN` 之前的扩展名确实无所谓：`game.mp4.001/.002/.003` → `7z t` exit 0、`Type = Split`、`Volumes = 3`。而伪装命名的分卷组（三个 `.mp4`，实为 `.7z.001/002/003`）→ **exit 2 完全打不开，只改首卷也不行**。这正是 C7「改后缀」在真实资源站里的**唯一硬例外**，处置在 §6.5（对 `_stg` 里的**副本**整组补齐 `.NNN`，A 永不改名）。

#### 5.5.2 归组算法（确定性，纯字符串）

```text
GroupVolumes(files):
  # 一个文件最多属于一个组；先匹配先赢，顺序 F2 → F3 → F4 → F1
  # F2 在 F1 之前：x.part1.rar 若先被 F1 规则看成「无 .NNN」会漏掉整族
  for f in files (按序数比较排序):
      if  f 匹配 ^(?<stem>.+)\.part(?<n>\d+)\.rar$          -> 组键 = ("F2", stem, len(n))
      elif f 匹配 ^(?<stem>.+)\.r(?<n>\d{2})$                -> 组键 = ("F3", stem)
      elif f 匹配 ^(?<stem>.+)\.rar$ 且 存在 stem.r00        -> 组键 = ("F3", stem)   # 首卷
      elif f 匹配 ^(?<stem>.+)\.z(?<n>\d{2})$                -> 组键 = ("F4", stem)
      elif f 匹配 ^(?<stem>.+)\.zip$ 且 存在 stem.z01        -> 组键 = ("F4", stem)   # 末卷=首卷
      elif f 匹配 ^(?<stem>.+)\.(?<n>\d{3})$                 -> 组键 = ("F1", stem)
      else                                                   -> 单文件组
  归组结果落一条 volume.group.formed 事件（§3.3.2）
```

- **组键里的 `len(n)` 对 F2 是必要的**：`part1` 与 `part01` 是两套命名，混用会找不到同伴卷。同一 stem 出现两种补零位数 → 判为**卷集损坏**，落 `WAITING_FOR_VOLUMES`。
- 大小写：Windows 文件系统不区分大小写，组键比较用**序数不区分大小写**，但**报告与事件里原样保留磁盘上的拼写**。
- Unicode：stem 比较前统一 **NFC** 规范化（从网页抓来的日文名可能是 NFD 分解形）。

#### 5.5.3 连续性判据 V1–V4

| 编号 | 判据 | 说明 |
|---|---|---|
| **V1** | 序号从起点起 **`1..N` 无空洞**（F1 从 `001`，F2 从 `part1`，F3 从 `r00`，F4 从 `z01`） | 缺号即缺卷 |
| **V2** | **除末卷外，所有卷字节数完全相等** | 7-Zip / WinRAR 定长切分。【实测：`81920 / 81920 / 36683`】这是**在调 7z 之前**就能抓出缺卷和下载未完成的强而廉价的校验 |
| **V3** | **末卷字节数 < 其余卷字节数**（严格小于） | 见 §5.5.4 |
| **V4** | 若批次条目声明了 `expected_parts`（取值域「≥1 或 null」，§3.2.5），实际卷数 **==** 声明卷数 | 这是「A 卷完成、B 卷还没开始下」这个失败模式**唯一可靠的自动检测点**——文件根本不存在，前三条判据都看不见它 |

`expected_parts = null` 时 V4 **真空成立**，且报告**必须**为该条目打出「无预期卷数，缺分卷不可自动检测」（§5.9.1 强制标注 ①）。「明确只有一卷」用 `expected_parts = 1` 表达，**不用 0**。

**为什么必须自己做这件事**：7-Zip 帮不上忙。实测**缺中间卷、缺末卷、单文件被下载截断**三种情况的 stderr **一字不差**，全是 `Unexpected end of archive`；从 `.002` 开始打开则是 `Cannot open the file as archive`。【实测，跨平台文案，**可外推**】

#### 5.5.4 末卷恰好等大 → 高度怀疑还有一卷没下完

```
        卷1     卷2     卷3     卷4          实际总卷数 = 5，第 5 卷还没开始下
      ┌─────┬─────┬─────┬─────┐
      │ 80M │ 80M │ 80M │ 80M │  ← V1 通过（1..4 无空洞）
      └─────┴─────┴─────┴─────┘     V2 通过（除「末卷」外等大 —— 因为它们本来就全等大）
                            ↑        V3 **失败**（末卷不小于其余卷）
                     假的「末卷」
```

**判据 V3 的存在理由**：定长切分下，真正的末卷**几乎不可能恰好等于分卷大小**（只有原始数据恰好是分卷大小整数倍时才会，概率极低）。所以：

- `V1 ∧ V2 ∧ V3 ∧ V4` 全真 → G6 通过。
- `V1 ∧ V2` 真、**`V3` 假**（末卷恰好等大）→ **`WAITING_FOR_VOLUMES`**，报告里写「末卷与其余卷等大，高度怀疑还有第 N+1 卷未下载完成；若确认只有 N 卷，请在批次条目里显式声明 `expected_parts: N`」。
  - `expected_parts` 一旦声明且等于 N，V4 成立，此时 V3 的怀疑被**人工显式解除**，G6 通过。**这就是「人在环内」的落点：机器不猜，人一句话说清。**
- `V1` 假（有空洞）→ **`WAITING_FOR_VOLUMES`**。
- `V4` 假（声明了 `expected_parts` 但卷数不够）→ 留在 **`AWAITING_DOWNLOAD`**（还在下，不是异常）；若卷数**多于**声明 → `WAITING_FOR_VOLUMES`（多出来的是什么？不猜）。
- `V2` 假（中间某卷大小不同）→ **`WAITING_FOR_VOLUMES`**，报告里点名是哪一卷。

I1 在这里的体现：**以上每一条都不删除任何文件。**

---

### 5.6 只读预检

#### 5.6.1 调用形状（契约）

A 的**所有** 7-Zip 调用一律经 §6.2.1 的唯一封装发出，本节不重复命令行形状、固定开关、禁用开关与退出码语义。A 侧只锁死三件事：

| 项 | A 的固定取值 | 理由 |
|---|---|---|
| `-Op` | `'l'` | A 只列举、永不 `t`/`x`（那是 §6 的动作，会产生不可逆后果） |
| `-Password` | **不传** | A-R1。保证一旦有例外就不再是保证 |
| `-TypeSpec` | **`none`**（= 不传 `-t`，走默认 `-t*:r`） | 见下 |

**为什么 A 必须用 `-TypeSpec none`**：对 F1 的 `.001` 首卷，默认模式下 7-Zip 会把卷集识别成 `Split` 容器、透明拼接后**再打开内层归档**，一次调用同时给出卷集信息与真实容器信息。【实测：`Type = Split / Volumes = 3 / Total Physical Size = 200523`，随后一个归档级块 `Path = parts.7z / Type = 7z`】而 `-TypeSpec *`（强制只开一层）**只在 §6.4 的递归计层里用**——A 若用了它就**只能看到 `Split` 外壳，拿不到内层的 `Size` 表，体积预算、条目预算与路径穿越预检全部落空**。§6.2.7 已把这条写进封装契约。

- 解析规则：`-slt` 输出里可能有 **1~2 个归档级块**。若首块 `Type = Split`，它提供 `Volumes` / `Total Physical Size`；**最后一个归档级块**是真实容器，提供 `Type` / `Physical Size` / `Offset` / 条目表。【实测 macOS，输出层跨平台，**可外推**；具体块分隔符在 Windows 上的逐字形态属【待测】，→ §11】
- 首卷的选择严格按 §5.5.1（F4 用最后那个 `.zip`）。
- exit != 0 的处置：`255` → §5.7；`2` → 用 `-TypeSpec *:s64m` **重试一次**（前置垃圾字节超过默认 8 MB 扫描窗口；单次，不做阶梯）；仍失败 → `WAITING_FOR_RULE`（人工看一眼文件头，**绝不批量改名试错**）。
- 预检结果落一条 `archive.identified` 事件（§3.3.2）。

#### 5.6.2 能拿到什么

| 字段 | 出现位置 | A 的用途 |
|---|---|---|
| `Type` | 归档级 | **内容嗅探结果**，容器判定的唯一权威（§5.4.3） |
| `Physical Size` | 归档级 | 压缩比公式的**分母** |
| `Offset` | 归档级，仅 SFX / 有前缀时 | `Offset > 0` = SFX 或带前缀容器的**确定性标志**。**只是记录项**，判别归 §6.13.1 |
| `Tail Size` | 归档级，仅有尾部数据时 | 尾部垃圾字节数，记录 |
| `Volumes` / `Total Physical Size` | 归档级，分卷时 | 与 §5.5.3 的 V1–V4 交叉验证 |
| `Multivolume` / `Volume Index` | 归档级，ZIP 跨卷时 | 同上 |
| `Size` | **条目级，恒有** | 解压后大小 ★。体积预算与压缩比的**分子** |
| `Packed Size` | 条目级 | **solid 归档里大量为空** ⚠ 见 §5.6.3 |
| `Encrypted` | 条目级 | `+`/`-`，加密探测判据 |
| `Method` | 条目/归档级 | **原文记录**，不做判别，见 §5.6.4 |
| `CRC` | 条目级 | 目录条目为空 |
| `Attributes` | 条目级 | `D` 前缀 = 目录。Windows 上是 `A`/`D`/`R`/`H`/`S` 组合而非 macOS 的 `-rw-r--r--` |
| `Path` | 条目级 | 路径穿越预检与路径预算（§5.6.5）、条目对账 |
| `Symbolic Link` / `Hard Link` | 条目级（tar/wim 等） | 链接目标，穿越预检要看 |

【实测 macOS 7zz 25.01；字段名、solid 空值行为、`Encrypted`/`Offset`/`Volumes` 语义**全部与平台无关，可外推**，仅 `Attributes` 的具体字符串不同】

**字段名就是 `Size`**——不存在 `Uncompressed Size` / `Unpacked Size` 这种字段。

#### 5.6.3 体积预算与压缩比：A 只做「不解压就能算出来的那一半」

```
declared_total   = Σ(所有条目的 Size)                       # 分子
physical         = 真实容器块的 Physical Size                # 分母；缺失时退回 Total Physical Size
expansion_ratio  = declared_total / physical
```

> **绝不能用 `sum(Packed Size)` 当分母。** 7z 默认 solid，一个 block 里**只有第一个条目带 `Packed Size`**，其余为空。【实测：同一次 `l -slt` 输出里 `data/blob.bin` 有 `Packed Size = 200275`，同 Block 的 `data/compressible.txt` 是 `Packed Size = `（空）】照抄 `sum(Packed Size)/…` 的公式会算出一个荒谬的比值。

正确公式的威力：实测一个 4668 倍的解压炸弹，**完全不解压、只读头**就算出来了（`Physical Size = 44923`，`Σ Size = 209715200`）。【实测】

**护栏的阈值语义、磁盘余量公式与各项系数一律以 §6.12 为准，默认值以 §3.2.3 的 `guards` 为准**，本节不再复制数值。A 在 `DOWNLOAD_STABLE` 内用这三项输入（`declared_total` / `expansion_ratio` / 单条目最大 `Size`）求值 §6.12 的护栏；任一阻断项成立 → `BLOCKED_RESOURCE`（迁移 `DOWNLOAD_STABLE → BLOCKED_RESOURCE`，执行体 A，见 §4 迁移表）。仅触发「记录级」阈值时只在报告里标 `BOMB_WARN`，**不阻断**。

> **`ratio` 绝不能单独阻断。** 实测合法的纯文本汉化补丁 7z 就有 **133x**，而真炸弹是 **1029x**，中间没有干净分界。【实测】而且**头部声明的大小会撒谎**——真正的护栏是 §6.12.3 的运行期 staging 体积监控 + 绝对上限 + 磁盘余量。

#### 5.6.4 加密标志：A 只记事实，不做判别

- 任一条目 `Encrypted = +` → 数据加密。A **不尝试任何密码**，只把两样东西作为**上下文**写进 `archive.identified` 与 `state.json` 的 `archives[]`（§3.4.1）：`encrypted: true`，以及 `Method` 字段的**原文字符串**（落 `archives[].encryption_method`）。
- **AES 与 ZipCrypto 的判别不在 A**：由 B 按 §6.3.7 用纯字节解析 ZIP 中央目录做（D7 是否触发由那里决定）。`l -slt` 的 `Method` 串在 Windows 上对 ZIP AES 打印什么，本身就是【待测】项——把它当判据会在删除判据链上埋一个静默错误。
- 数据加密**不阻止** `DOWNLOAD_STABLE` 与 `PREFLIGHT_OK`——`l -slt` 拿得到全部条目，所有预检都成立。只有**头加密**才会打断（§5.7）。报告的「等你做事」区列出「需要密码」。

#### 5.6.5 路径穿越预检与路径预算

7-Zip 确实拦截路径穿越，但**完全静默**：构造含 7 种恶意路径的 zip 解到沙箱，`exit 0`、**零 warning、零 stderr**，没有任何文件逃出 `-o` 目录。【实测 macOS】

> **流水线拿不到「这个包试图穿越」的信号。** 所以必须在解压前自己扫 `l -slt` 的 `Path` 字段做判定。

拒绝条件（对每个条目的 `Path`，命中任一即拒）：

```
- 含 ".." 分量
- 以 "/" 或 "\" 开头
- 匹配 ^[A-Za-z]:            (盘符)
- 以 "\\" 开头               (UNC)
- 含 NUL 或其它控制字符 (U+0000..U+001F)
- Windows 保留设备名（CON/PRN/AUX/NUL/COM1-9/LPT1-9，含「保留名+扩展名」如 NUL.txt）
- 尾随点或空格（Windows 上会被静默改名）
- Symbolic Link / Hard Link 字段非空，且目标含 ".." 或为绝对路径
```

命中 → **`BLOCKED_RESOURCE`**（§4 已把「路径穿越 / 产物含 reparse point、symlink、junction」写进该态的说明），**不删任何文件**，报告里逐条列出可疑 `Path`。§6.6.4 的解压前自查用同一张表。

两条实现警告：
- 7-Zip 是**删除** `..` 分量而不是规范化解析（`sub/../../sneaky.txt` → `sub/sneaky.txt`，规范化应得 `../sneaky.txt`）。结果是安全的，但**落地路径与归档里声明的逻辑路径不同** → §6.6.1 做文件清单比对时要按 7-Zip 的规则算，**不能用 `Path.GetFullPath` / `normpath`**。【实测】
- **`C:\...` 与 `back\slash\...` 在 Windows 上的落地位置与 macOS 必然不同**（POSIX 下 `\` 是普通字符，Windows 下是路径分隔符 + 要剥盘符）【待测】（§11 #13）。不验证的后果：上面这张拒绝表是否覆盖了 Windows 的真实逃逸面，属于未知。样本 `lab7z/evil/traversal.zip` 可直接拷到目标机复验。

**路径预算（只记录，不阻断）**：A 顺便算出 `max(len(Path))` 与落地预算余量 `248 − len("<LibRoot>\games\<dirname>\") − max(len(Path))`（建目录实际上限是 MAX_PATH−12 = 248，见 §2.5），写进 `archive.identified` 并在报告里标出余量最小的三个条目。**A 不因此落停机态**：`PATH_BUDGET_EXCEEDED` 的落点在 `dirname` 生成期（§3.2A，降级仍不够时）与 B 的落地期（§6）；A 这里提供的是**提前量**，让人在解压之前就能看到「这个包解出来会撞破 248」。
【待测】真实 RPG Maker / Unity 包的最长内部条目路径分布（决定这道闸门的触发频率与降级策略是否够用）→ §11。

#### 5.6.6 `PREFLIGHT_OK` 的定义

```
PREFLIGHT_OK  ⟺  DOWNLOAD_STABLE
               ∧ 容器判定通过（§5.4.3 / §3.6.3 / §6.13.1）
               ∧ 路径穿越预检全部通过                        (§5.6.5)
               ∧ 体积 / 压缩比 / 磁盘余量护栏全部通过          (§5.6.3 → §6.12)
               ∧ ZIP 文件名编码检测通过（非 ZIP 时恒真）       (§5.8)
```

`PREFLIGHT_OK` 是 A 能推到的**最远状态**。再往前一步（`EXTRACTING`）需要密码与解压，那是 §6。

---

### 5.7 头加密（`-mhe`）分支

**这个分支必须显式存在，否则一个完全下完的头加密包会被永远判成「没下完」。**

7z 的 `-mhe=on` 把归档头本身也加密。后果是断崖式的：

```
$ 7zz l pw-hdr.7z </dev/null
Enter password:
$ echo $?
255
```
【实测 macOS 7zz 25.01。`l` 的密码交互在 `CPP/7zip/UI/Console` 公共代码里，**可外推到 Windows**。注意 exit 255 的官方语义是「用户中断」，但**加密包在 stdin 不可用时也返回 255，真实含义是「需要密码」**——这是本项目必须记住的语义重载】

**拿不到密码时，一个字段都拿不到**：`Type` / `Size` / `Path` / 条目数 / `Encrypted` **全部为空**。于是：

| 预检项 | 头加密包 |
|---|---|
| 体积预算 `Σ Size` | ❌ 失效 |
| 条目数预算 | ❌ 失效 |
| 压缩比护栏 | ❌ 失效（分子拿不到） |
| 路径穿越预检 / 路径预算 | ❌ 失效（`Path` 拿不到） |
| 容器白名单判定 | ❌ 失效（`Type` 拿不到） |
| ZIP bit11 检测 | 不适用（7z 格式头强制 UTF-16LE，没有这个问题） |
| G1 / G2 / G3 / G4 | ✅ 仍然有效 |
| G6（卷集连续 + 除末卷外等大） | ✅ 仍然有效（纯文件系统算术，与归档内容无关） |

**A 对头加密包的确定性处置**：

```text
if (G5 ExitCode == 255):
      # 不重试、不传哨兵密码、不做任何「探一探」的动作（A-R1）
      Emit(kind:"archive.identified", payload:{header_encrypted:true, exit:255,
                                               gates_passed:[G1,G2,G3,G4,G6]})
      if (G1 ∧ G2 ∧ G3 ∧ G4 ∧ G6):
            Emit(state: WAITING_FOR_PASSWORD)      # 「下完了，但我看不进去」
      else:
            留在 AWAITING_DOWNLOAD                  # 连文件都没齐，谈不上密码
```

报告里必须写清楚这句话：**「该包为头加密（`7z l` exit 255），巡检无法做体积、条目、压缩比与路径穿越预检；这些护栏将在执行体 B 拿到密码后、以运行期 staging 体积监控 + 绝对上限的形式补上（§6.12.3）。」** 不能假装预检做过了。

> A **绝不**为了「探一探」而给头加密包传一个哨兵密码。虽然实测哨兵法对不加密包无害，但那会让 A 出现 `-p` 参数，破坏 A-R1 这条结构性保证——**保证一旦有例外就不再是保证**。

---

### 5.8 ZIP 文件名编码风险检测

#### 5.8.1 为什么只做 ZIP

文件名乱码**只是 ZIP（和少量 TAR）的问题**，不是通用问题：

7z 头里**强制 UTF-16LE**、RAR5 **强制 UTF-8** → 格式层面没有这个问题；RAR4 同时存 OEM 名与 Unicode 名（后者存在时优先），只有极少数只有 OEM 名的老包有风险；TAR 无规范、纯字节流；**ZIP 是重灾区**——文件名是不是 Unicode 由 general purpose **bit 11** 决定，且默认不是。【实测】+【官方格式规范】

→ 编码逻辑只覆盖 ZIP 一条路径，**不做通用机制**。这符合「不要为了流程而流程」。

#### 5.8.2 判定：纯字节解析中央目录的 bit 11

**不依赖任何 7-Zip 开关。** 特别是 **`-mcp=932` 不可作为主路径**：官方 `method.htm` 明写 `-m` 只适用于 `a / h / d / rn / u`（**不含 `x` / `l` / `t`**）；macOS 实测 `-mcp=932` / `-mcp=65001` / 甚至非法值 `-mcp=99999` 对 `l` **全部零效果且不报错**，说明根本没被消费；社区说法自相矛盾。【官方】+【实测】→ 降级为【待测】候选手段（§11 #12），封装层默认硬拒（§6.2.1）。

三分支判定（对中央目录里的**每一个**条目）：

| 条件 | 判定 |
|---|---|
| `bit11 == 1`（`0x0800`） | 文件名是 UTF-8，**安全** |
| `bit11 == 0` 且文件名字节**全部 < 0x80** | 纯 ASCII，**安全** |
| `bit11 == 0` 且存在 **≥ 0x80** 的字节 | **风险** → `FILENAME_ENCODING_RISK` |

只要有**一个**条目落进第三档，整个归档就落 `FILENAME_ENCODING_RISK`。

#### 5.8.3 解析算法（伪码）

```text
ScanZipFilenameEncoding(path):            # path = 单文件 ZIP，或分卷 ZIP 的**最后那个 .zip**
  fs = File.Open(path, Open, Read, FileShare.Read)     # 只读，绝不写

  # 1. 从文件尾向前扫 EOCD 签名 50 4B 05 06
  #    ZIP 注释最长 65535，EOCD 定长 22 -> 最多回扫 65557 字节
  tail = 读取最后 min(65557, len) 字节
  eocd = tail 中**最后一个** 50 4B 05 06 的位置        # 取最后一个，防注释里出现同样的字节
  if eocd 不存在: return { applicable:false, reason:"NO_EOCD" }   -> WAITING_FOR_RULE

  this_disk    = u16 @ eocd+4
  cd_disk      = u16 @ eocd+6
  cd_entries   = u16 @ eocd+10
  cd_size      = u32 @ eocd+12
  cd_offset    = u32 @ eocd+16

  # 2. ZIP64：任一字段为 0xFFFF/0xFFFFFFFF 就必须走 ZIP64
  if 任一字段是全 F:
      定位 ZIP64 EOCD locator 50 4B 06 07（在 EOCD 之前 20 字节）
      -> ZIP64 EOCD 50 4B 06 06，从中重取 cd_entries(u64)/cd_size(u64)/cd_offset(u64)

  # 3. 分卷 ZIP：中央目录必须整个落在当前这一卷里
  if this_disk != cd_disk:
      return { applicable:false, reason:"CD_ON_OTHER_DISK" }      -> WAITING_FOR_RULE

  # 4. 遍历中央目录头 50 4B 01 02（定长 46 字节 + 三个变长域）
  p = cd_offset ; risky = [] ; n = 0
  while n < cd_entries:
      if u32 @ p != 0x02014B50: return { applicable:false, reason:"BAD_CD_SIG" }
      gpbf     = u16 @ p+8               # general purpose bit flag
      name_len = u16 @ p+28
      extra_len= u16 @ p+30
      cmt_len  = u16 @ p+32
      name_bytes = 字节 @ p+46 .. p+46+name_len
      utf8_flag  = (gpbf & 0x0800) != 0
      has_high   = name_bytes 中存在 >= 0x80 的字节
      if (not utf8_flag) and has_high:
            risky += { index:n, name_hex: hex(name_bytes[0..32]) }   # 只记 hex，不臆测解码
      p += 46 + name_len + extra_len + cmt_len ; n += 1

  return { applicable:true, entries:n, risky_count:len(risky), samples:risky[0..4] }
```

要点：
- **只读、按字节、不解码**。风险条目的文件名在报告里以 **hex 呈现**，事件与 `state.json` 里也存 hex——**不做任何猜测性解码**（猜 CP932 还是 GBK 正是这一整套机制要避免的事）。
- `bit 11 = 0x0800` 的语义与中央目录头的字段偏移取自 ZIP 格式规范 APPNOTE.TXT（`4.4.4 general purpose bit flag`、`4.3.12 central directory header`）【官方】；本轮**未在线复核该文档原文**，字段偏移按通行实现写出 → **解析器必须用 §10 的回归样本（`lab7z/sjis/sjis-legacy.zip` 与一个已知 bit11=1 的 UTF-8 ZIP）双向验证后才算可用**【待测】。不验证的后果：解析器可能在所有包上都报「安全」或都报「风险」，而这两种错误都不会自己暴露。→ §11。
- 分卷 ZIP 一定解析**最后那个 `.zip`**（§5.5.1 F4）。
- ZIP 之外的格式：`applicable = false, reason = "NOT_ZIP"`，判定通过。

#### 5.8.4 命中之后

落 **`FILENAME_ENCODING_RISK`**（停机态，I1：不删任何文件）。人工动作：确认该包的真实文件名代码页，把结论写进批次条目或 `source-profiles.json`，由 §6.11 决定用什么手段落地。

> **同一台机器上「解出来什么样」还取决于系统 ACP**【待测】（§11 #11）：Shift-JIS ZIP 在目标机 ACP 下的落地文件名形态（ACP 解码 / `U+EFxx` 转义 / `?` 替换）未知，macOS 用的是 `U+EF00+原字节`，Windows 大概率不同。不实测的后果：§6.11 所谓「96% 可逆的 GBK↔CP932 修复」是否成立完全悬空。A 把系统 ACP 与 Beta UTF-8 状态写进 `env.snapshot` 作为解释上下文——**但不据此做任何自动分支**。

---

### 5.9 对账报告

呈现层是这个项目**唯一不吝啬**的地方。引擎可以冷冰冰，报告必须让人**扫一眼就知道该干什么**。

**谁写**：**执行体 A**（I5 ②：新建 `_reports\<batch-id>-<seq>.md`，**不得覆盖已存在的报告**）。理由是 A 是无人值守的那一个——如果报告要靠人另跑一个命令才有，「装完就忘了它」这条操作契约就不成立。`Get-BatchReport.ps1` 用**同一套结构**做重新生成 / 回填（§8.1.4），不另立格式。

#### 5.9.1 结构契约（全 SPEC 的报告格式以本小节为准）

`D:\GameLib\_reports\<batch-id>-<seq>.md`，UTF-8 无 BOM（`[System.IO.File]::WriteAllText(path, text, UTF8Encoding($false))`——`Out-File`/`>` 会写成 UTF-16LE，PS 5.1 的 `-Encoding utf8` 带 BOM，两者都会让中文游戏名在后续读取里错位，详见 §8.8 的落盘协议）。

**Front-matter** 必须有：`batch_id` / `seq` / `run_id`（紧凑形，§3.3.1）/ `generated_utc` / `digest`（§5.1.2 第 5 步的去重指纹）/ `script_version` / `sevenzip_version` / `acp` / `beta_utf8`。
环境信息**只在 front-matter 出现一次**——§9.11.3 的「顶部环境条」与它二选一，本 SPEC 选 front-matter。

**Front-matter 之后的第一块**固定是**本次删除声明**（§8.8.11 的硬要求）：本轮有没有源包被删除、删了哪些、各自的 SHA-256 与字节数（数据来自 `archive.deleted` 事件）。**A 自己从不删除任何文件**，所以这一块在纯巡检轮里恒为「本轮无删除」——但它必须**存在且显式**，不能省略。

其后是五个分区，**顺序固定**，空分区写「（无）」而不是省略：

| 顺序 | 分区 | 收录 | 为什么排这个位置 |
|---|---|---|---|
| 1 | **⛔ 等你做事** | 所有停机态，**按停机态分组**，每组带具体动作；外加一个「非停机态但需要你动手」子块（见强制标注 ②） | 人打开报告就是为了看这个 |
| 2 | **🟡 待认领** | `_inbox` 里认不出归属的组 | 也需要人，但不属于任何条目 |
| 3 | **🔴 异常** | 跨卷阻塞、认领部分失败、G3 权限拒绝、**孤儿 `_stg\<8hex>\` 清单**（A 只列不回收，回收见 §6.1.2）、报告自身的写入问题 | 系统级问题，不是某个游戏的问题 |
| 4 | **⏳ 进行中** | `AWAITING_DOWNLOAD` / `DOWNLOAD_STABLE` / `PREFLIGHT_OK` / `EXTRACTING` … | 不需要动作，扫一眼 |
| 5 | **✅ 已完成** | `COMPLETE` / `TRANSLATION_NOT_APPLICABLE`。**逐条目列出，不折叠**，并对每个条目**原样打印 §9.7.2 的 HV-1..HV-5 诚实局限声明** | 这是「删了源包之后我们到底保证了什么」的唯一交付面 |

**三条强制标注**（缺一即缺陷）：

1. `expected_parts = null` 的条目：在「⏳ 进行中」与「⛔ 等你做事」区的该条目行上打出「**无预期卷数，缺分卷不可自动检测**」（§3.2.5 的硬要求；V4 真空成立并不等于卷数正确）。
2. 已认领但命中 `known_false_complete`（后缀残留）的条目：在「⛔ 等你做事」的「非停机态但需要你动手」子块里逐轮列出，给出「到客户端确认任务已完成后**人工**去掉后缀」这个动作（§5.2.5）。
3. 头加密条目：原样打印 §5.7 那句护栏失效声明。

顶部**长期提示条**（满足条件就一直显示，直到条件消失）：7-Zip 版本低于建议版本 26.02（硬下限 25.01）；`_inbox` 与 `games\` 不同卷；`netdisk-profiles.json` 里某个在用网盘的临时物模式仍未登记；`7z.exe i` 的 handler 名与 `container-types.json` 有差集（§5.4.3）。

#### 5.9.2 样例

```markdown
---
batch_id: 2026-09-12-vn-06
seq: 14
run_id: 20260912T112003Z-a91f4c2e
generated_utc: 2026-09-12T11:24:41Z
digest: 3f9c…a1
script_version: 1.0.0
sevenzip_version: "25.01"
acp: 936
beta_utf8: false
---

# 批次 2026-09-12-vn-06 · 对账 #14

## 本次删除声明

**本轮无删除。** 巡检（执行体 A）不删除任何文件；源包删除只由执行体 B 在 §6.8 的 D1–D12 全部通过后执行，并在此处逐条列出 SHA-256。

> ⚠ **7-Zip 25.01**：满足硬下限，但低于建议版本 26.02。26.02 修复了 CVE-2026-14266（XZ 解压堆溢出 RCE）。
> 本项目正是拿陌生归档去喂解析器，建议升级。（不阻断运行）

**8 个条目**： ⛔ 等你做事 3 · ⏳ 进行中 3 · ✅ 已完成 2 · 🟡 待认领 2 组 · 🔴 异常 0

---

## ⛔ 等你做事（3）

### WAITING_FOR_PASSWORD（2）

| 条目 | 目录 | 卡在哪 | 你要做的 |
|---|---|---|---|
| `g0031-mahoutsukai` | `games\魔法使いの夜\` | **头加密 7z**（`7z l` exit 255）。巡检拿不到任何条目信息 | 在 vault 里补 `site:zywz.example|dudulove` 这一条，然后跑「作业」 |
| `g0034-summer-pockets` | `games\Summer Pockets\` | 数据加密（`Encrypted = +`，`Method` 原文已记录）。密码库无命中 | 同上，或在批次条目里填 `password_ref` |

> `g0031` 的护栏说明：该包为头加密，巡检**无法**做体积预算、条目数预算、压缩比与路径穿越预检。
> 这些护栏将在执行体 B 拿到密码后，以运行期 staging 体积监控 + 绝对上限的形式补上（§6.12.3）。

### WAITING_FOR_VOLUMES（1）

| 条目 | 目录 | 卡在哪 | 你要做的 |
|---|---|---|---|
| `g0033-hanahira` | `games\はなひらっ\` | 4 卷齐全无空洞，但**末卷与其余卷等大**（4 × 209 715 200 B）。高度怀疑还有第 5 卷未下完 | ① 回网盘确认总卷数；② 若确实只有 4 卷，在批次条目里写 `expected_parts: 4` 解除怀疑；③ 若有第 5 卷，补下载即可，下一轮巡检自动继续 |

<details><summary>卷集明细</summary>

```
はなひらっ.part1.rar   209 715 200 B   ✔
はなひらっ.part2.rar   209 715 200 B   ✔
はなひらっ.part3.rar   209 715 200 B   ✔
はなひらっ.part4.rar   209 715 200 B   ⚠ 末卷不应与其余卷等大
V1 序号连续 ✔ ／ V2 除末卷外等大 ✔ ／ V3 末卷更小 ✘ ／ V4 expected_parts 未声明
```
</details>

### 非停机态，但需要你动手（1）

| 条目 | 情况 | 你要做的 |
|---|---|---|
| `g0037-shinshin` | 已认领的 `新月同人汉化.baiduyun.p.downloading` 命中 `known_false_complete`：疑似已完成但后缀残留。条目留在 `AWAITING_DOWNLOAD`，巡检**不会**自动改名 | 到百度网盘客户端确认任务已完成，**人工**去掉后缀；下一轮巡检自动继续 |

---

## 🟡 待认领（2 组）

`D:\GameLib\_inbox\` 里有 2 组文件没能对应到本批次的任何条目。**巡检没有碰它们**（不移动、不改名、不删除）。

| 组 | 文件数 / 总字节 | 首卷分诊 | 为什么没认出来 | 你要做的 |
|---|---|---|---|---|
| `[GAME@GAME]_1234567.mp4` 系列 | 3 / 1.83 GiB | `.001` 族，首卷魔数 `37 7A BC AF 27 1C`（7z） | 批次里没有任何条目声明过这些文件名；`source-profiles.json` 里该 profile 没有 `filename_patterns` | 在批次条目的 `expected_files` 里补上这三个名字，或在 `_inbox\claims.json` 里写一行显式认领 |
| `冬のまち.zip` | 1 / 742 MiB | `50 4B 03 04`（ZIP） | 同时命中 `g0038` 与 `g0039` 两个条目的 `expected_files`（歧义） | 修正其中一个条目的 `expected_files`，或用 `claims.json` 显式指定 |

---

## 🔴 异常（0）

（无）

---

## ⏳ 进行中（3）

| 条目 | 状态 | 观测 |
|---|---|---|
| `g0032-clannad` | `AWAITING_DOWNLOAD` | 2/3 卷到齐（`expected_parts: 3`，V4 未满足）。`.part3.rar.crdownload` 正在写，G1 未过 |
| `g0035-alia` | `DOWNLOAD_STABLE` | G1–G6 全通过。`Type = zip`，18 条目，Σ Size 3.21 GiB，`Physical Size` 3.09 GiB，ratio **1.04x**。⚠ **无预期卷数，缺分卷不可自动检测** |
| `g0036-tsukihime` | `PREFLIGHT_OK` | `Type = Rar5`，142 条目，Σ Size 12.4 GiB，ratio 1.31x，`Encrypted = -`，路径穿越预检通过，路径预算余量最小 31 字符。**可以跑「作业」了** |

---

## ✅ 已完成（2）

### `g0029-air` — COMPLETE
源包 2 个已删除（SHA-256 见「本次删除声明」历史报告 #11）。
> HV-1..HV-5（§9.7.2）原样打印：…

### `g0030-kanon` — TRANSLATION_NOT_APPLICABLE
引擎 Unity，MTool 不支持（§7.6）。源包 1 个已删除。
> HV-1..HV-5（§9.7.2）原样打印：…
```

#### 5.9.3 报告的三条硬规矩

1. **每个停机态都必须带「你要做的」**，而且是**具体动作**，不是状态名的同义反复。写不出具体动作的停机态，说明状态设计有问题。
2. **不写密码、不写带 token 的下载 URL、不写账号**。`.gameflow\` 与 `_reports\` 都不许出现这些（§9）。密码相关只写「密码库里缺 `site:<host>|<uploader>` 这一条」（键空间见 §3.7.2）。
3. **不覆盖旧报告、不删除旧报告**（A-R3 / I5 ②）。`<seq>` 单调递增；内容指纹（`digest`）相同就**不出新报告**，避免每 10 分钟刷一份一模一样的文件。

---

### 5.10 A 的调度

#### 5.10.1 用 Windows 任务计划，不用 Codex 的 Scheduled tasks

四条理由：① A 是纯确定性的文件处理器，用一个 LLM turn 驱动它等于把确定性逻辑塞进不确定的载体，还要烧 token；② Codex 计划任务要求「机器开机 **+ 桌面 App 常驻** + 项目目录在盘上 + 沙盒能写到 `D:\GameLib`」【官方】，Windows 任务计划只要求用户登录着；③ E1 主力机会被打断、桌面 App 可能被关掉，任务计划不会；④ Codex 计划任务「run unattended with your default sandbox settings」，workspace-write 下写不到 project 外的目录，还要额外配 `writable_roots`【官方】（§8.3）。

Codex 仍然保留「手动催一次巡检 / 批次对账 / 归档报告」这几个 skill（§8.5），它们调用的是**同一个入口脚本**，只是 `trigger` 不同。

#### 5.10.2 最小配置要点

```powershell
$action  = New-ScheduledTaskAction -Execute 'pwsh.exe' `
  -Argument '-NoProfile -NonInteractive -ExecutionPolicy Bypass -File "D:\GameFlow\scripts\Invoke-Sweep.ps1" -Unattended' `
  -WorkingDirectory 'D:\GameFlow'
$trigger = New-ScheduledTaskTrigger -Once -At (Get-Date) `
             -RepetitionInterval (New-TimeSpan -Minutes 10)
$princ   = New-ScheduledTaskPrincipal -UserId "$env:USERDOMAIN\$env:USERNAME" `
             -LogonType Interactive -RunLevel Limited
$set     = New-ScheduledTaskSettingsSet -MultipleInstances IgnoreNew `
             -ExecutionTimeLimit (New-TimeSpan -Minutes 30) `
             -StartWhenAvailable -DontStopOnIdleEnd
Register-ScheduledTask -TaskName 'GameFlow-Sweep' -Action $action `
  -Trigger $trigger -Principal $princ -Settings $set -Force
```

不带 `-BatchId` = 扫全部活跃批次（§8.2.3 的参数契约允许缺省），**一个任务管到底**；也可以按批次各注册一个，二者不冲突（批次锁按批次名互斥）。

| 设置 | 值 | 为什么 |
|---|---|---|
| `LogonType` | **`Interactive`** | 见 §5.10.3 |
| `RunLevel` | **`Limited`** | A-R2。A 不需要任何提权动作；`MpCmdRun` 官方明写需提权，所以 Defender 预检**不在 A 里**（§6.9.1）【官方 MS Learn】 |
| `MultipleInstances` | **`IgnoreNew`** | 第二道互斥保险（第一道是 §5.1.4 的 `FileShare.None` 文件锁）。重叠触发时新实例被直接丢弃 |
| `ExecutionTimeLimit` | **30 分钟** | 必须设。A 单轮上界是 `max_run_seconds`(900) + 余量；无限期挂着的任务会掩盖 §5.3.6 那个 stdin 挂死的故障 |
| `-NoProfile` / `-NonInteractive` | 必须 | 前者挡住 `$PROFILE` 的别名/函数让行为随环境漂移；后者让任何意外的 `Read-Host` 直接失败，而不是**永久挂起** |
| `-ExecutionPolicy Bypass` | 必须 | 只对该进程生效，不改系统策略。⚠ 若 `MachinePolicy`/`UserPolicy` 被 GPO 设过**这个参数无效**——Preflight 必须探测 `Get-ExecutionPolicy -List` 的全部作用域（§9），失败时脚本以退出码 `3` 结束 |

入口参数形状（只接收受限字符集 token、库根由脚本从配置解析而**不作为参数传入**）的理由见 §8.2.2 / §8.2.3：Codex 的 Windows 原生 agent 跑的是 PS 5.1，会把参数数组重新拼成一个字符串，带空格的路径或标题在最外层就会被拆坏【官方逐字复核 + 实测】。

#### 5.10.3 **不要用 S4U**

任务计划的「不存储密码。该任务将只能访问本地资源」= `TASK_LOGON_S4U`。官方原文：*"Use an existing interactive token to run a task. ... **When an S4U logon is used, no password is stored by the system and there is no access to either the network or encrypted files.**"*【官方 Microsoft Learn · Principal.LogonType】

两个后果：① 没有密码 → 无法派生该用户的 DPAPI master key → **解不开 `%LOCALAPPDATA%\GameFlow\vault.clixml`**；虽然 A 本来就不碰密码（A-R1），但**同一个入口脚本**也会被人手动触发，习惯性配成 S4U 会让日后任何读 vault 的路径都以极难排查的方式失败。② 「no access to network」→ 若库根落在网络位置，A 直接读不到。

同样不推荐：`SYSTEM` / `ServiceAccount`（解不开该用户的 DPAPI blob + 权限过大）、`InteractiveOrPassword`（官方自己说 *"This flag is not recommended for new tasks because it is less reliable"*）。

**DPAPI vault 的创建身份与计划任务的运行身份必须一致**（§3.7.3）——跨用户/跨机不可解密的实际表现属【待测】（§11 #20）。

#### 5.10.4 间隔：10 分钟（锁定值）

- **下界**：每轮内含 4 个采样点、跨度 `quiet_window_seconds`(120) 的采样窗口，加上遍历 `_inbox` 与全部 `games\` 目录的 IO（还会和 Defender 实时保护交互）。间隔小于 5 分钟时机器基本一直在扫盘，收益却是零——网盘下载的时间尺度是十分钟到小时。
- **上界**：B6 的 AV 删除窗口，以及用户「下完了怎么还没动静」的等待感。超过 30 分钟就该手动催一次而不是调间隔。
- **与 `IgnoreNew` 的关系**：单轮上界 `max_run_seconds`(900 秒) **大于**间隔（600 秒），所以重叠是可能发生的——`IgnoreNew` + 文件锁**两道**保险缺一不可。
- **手动催**：Codex skill「处理已下载文件」以 `trigger = codex` 调同一入口，抢不到锁就以退出码 `75` 立即返回，**不等待、不重试**。
- **【待测】单次巡检的实际耗时**。若它超过巡检间隔，`IgnoreNew` 会把后续触发**静默丢弃**——没有报错、没有事件，任务计划历史里只有一行「已忽略」，是最难发现的一类故障。→ §11。

`trigger`（`scheduled` / `manual` / `codex`）是事件的公共字段（§3.3.1），**A 产生的每一条事件都要带**，否则事后无法区分是谁动的。

---

> ### ⚠️ 遗留异议（起草者对某条锁定决策的保留意见，主控未采纳）
>
> **异议（针对契约把 G1–G6 写成扁平合取、不规定求值次序）。** 六条判据的**求值次序**在工程上不是自由的：G4 的存在性复检必须紧贴认领动作（否则 B6 的 AV 删除窗口漏进来），G6 是纯算术必须排在唯一要起子进程的 G5 之前（否则每个缺卷的分卷包都要白起一次 7z 并等超时）。本节已把次序固定为 `G1→G2→G3→G4→[认领]→G6→G5` 并写进 §5.3.1。建议主控把这个次序**提升为契约的一部分**，否则不同实现者会各写各的，而次序错误的失败模式（多等一个采样周期 vs. 文件在眼皮底下被删掉）严重程度差着量级。


---

## §6 执行体 B：作业（Work）

> 本节是全篇技术密度最高、也是风险最高的一节。B 是整条流水线里**唯一会删除文件**的执行体，而 C3（源包与本体同目录）+ C4（解压完成后自动删除源包）共同意味着：**删除不可撤销、没有回收站兜底、判错一次即永久数据丢失**。因此本节的所有判据一律取「严格相等 / 全部成立 / 任一不满足即保留一切」的形式，不接受任何「大概没问题」的分支。
>
> 可信度标注规则见文档头。`【实测】` 一律指本轮在 **macOS 26.6.2 / arm64 上用官方 7-Zip 25.01（`7zz`）** 跑出来的结果，每条都附「能否外推到 Windows」的判定。
>
> **权威边界（R29/R30）**：本节是 **7-Zip 调用封装（§6.2.1）**、**删除判据 D1–D12（§6.8）**、**护栏阈值语义（§6.12）**、**不进自动流程的形态（§6.13）** 的权威。所有 JSON schema、字段名、阈值默认值一律以 **§3** 为准，本节只写语义与判定顺序；状态名与迁移以 **§4** 为准；魔数表以 **§5.4.4** 为准。

---

### 6.1 B 的一次运行做什么

#### 6.1.1 为什么 B 不能无人值守

B 需要两样 A 永远拿不到的东西，两条都是官方硬约束：

- **归档密码**（vault，DPAPI 绑定运行身份，schema 见 §3.7）：Windows 任务计划的 **S4U 登录类型官方明写「no password is stored」「no access to encrypted files」** → 解不开 DPAPI 密码库。【官方，MS Learn 任务计划 LogonType】
- **提权**（Defender 预检）：`MpCmdRun.exe` 官方页首原文 *"**You need to run MpCmdRun in an elevated Command Prompt**"* → 与 `-RunLevel Limited` 的受限计划任务直接冲突。【官方 https://learn.microsoft.com/en-us/defender-endpoint/command-line-arguments-microsoft-defender-antivirus】

所以 **B 只能由人、或由 Codex 在一次交互会话里显式触发**。这不是保守，是这两条官方约束的直接后果。B 因此也是 C1 所说的「阶段一里需要人工做的一部分」的主体（另外三项在 §5 与 §9）。

推论：B 的入口**只接收无空格无歧义的 token**，一切路径由脚本自己从配置解析——理由是 Codex 的 Windows 原生 agent 跑的是 `powershell.exe -NoProfile`（Windows PowerShell 5.1），它会把参数数组重新拼成一个字符串再交给子进程（官方 `about_Parsing`）。【官方 + 仲裁裁定】**入口参数清单与退出码是 §8.2.3 的契约表**（`-BatchId` / `-ItemId` / `-Phase` / `-Retry` / `-Resume` / `-RetryAll` / `-ForceRetest` / `-Abandon` / `-Reason` 均已在那里登记），本节不再复述。

#### 6.1.2 一次运行的骨架（伪码级）

```
Invoke-Work.ps1 -BatchId <slug> [-ItemId <slug>] [-Phase all|extract|verify|scan|land|delete]
                                                  （入口与退出码契约：§8.2.3）

00  取**批次锁**：[System.IO.File]::Open(D:\GameLib\batches\<batch-id>.lock,
                                          Open|Create, ReadWrite, None)
      失败 -> 打印 LOCKED_BY_ANOTHER_RUN，退出，**不写任何状态**（运行期瞬时状况，不落盘）
01  读 D:\GameFlow\rules\*.json 与 D:\GameLib\batches\<batch-id>.json
      worklist = 批次条目中 abandoned != true 的全部（§3.2.5）
      **运行期状态一律取自 state.json / Replay(events.jsonl)，绝不读批次条目的字段**
    写事件 run.started（§3.3）
02  FOR EACH item IN worklist（**单线程、顺序、每个 item 独立**，I4）:
02a     取**条目锁**：games\<dirname>\.gameflow\.lock（FileShare.None，形状同 §5.1.2 第 4 步）
            失败 -> LOCKED_BY_ANOTHER_RUN（不落盘），continue
03      从 games\<dirname>\.gameflow\events.jsonl 重建 state（I2；重放按**文件行序**，§3.3.5）
04      state ∉ {PREFLIGHT_OK} ∪ 可续跑集合  ->  释放条目锁，跳过该 item
05      attempt_id := <UTC 紧凑 ISO>-<4hex>（格式见 §3.4.1 / §4.4.3）
        stg_id     := 8 位十六进制；建 D:\GameLib\_stg\<stg_id>\          （§6.4.1）
06      §6.12  磁盘余量预检                      不足 -> BLOCKED_RESOURCE
07      §6.3   解析密码链 P0..P6 + 等价展开       用尽 -> WAITING_FOR_PASSWORD
                                                 编码疑点 -> PASSWORD_ENCODING_SUSPECT
                                                 RAR4 歧义 -> EXTRACT_FAILED_AMBIGUOUS
08      §6.2   7z l -slt -TypeSpec none（**拿到密码后重做一次**；
               头加密包这是第一次能看见内容）
09      §6.12  条目预算 / 压缩比 / §6.6 路径穿越自查
                                                 -> BLOCKED_RESOURCE / WAITING_FOR_RULE
10      ==> 状态 EXTRACTING
11      §6.4   逐个源包：7z t  (D2,D3)  ->  7z x -t* 到 _stg\<stg_id>\  (D4,D5) -> 递归剥壳
12      §6.6   逐条目 exists+size 对账 (D6)、ZipCrypto 追加 CRC (D7)、
               嵌套残留扫描 (D8)、symlink/junction 扫描 (D9)
13      §6.7   content_files 判定 (D10)      == 0 -> readme 回喂 P3/P4 -> EXTRACTED_NO_CONTENT
14      §6.11  文件名编码修复（仅当批次条目显式声明了 codepage 规则）
15      ==> 状态 VERIFYING
16      §6.9   Defender 预检（对 _stg\<stg_id>\，**在落地之前**，需提权）
                                                 -> THREAT_CONFIRMED（硬停）/ THREAT_SUSPECTED（软停）
17      §6.8   D1..D11 全部成立？ 否 -> 落对应停机态，保留一切，跳到 24
18      §6.8   同卷 move  _stg\<stg_id>\*  ->  games\<dirname>\ ；体积对账 (D12 前半)
               写事件 staging.promoted
19      §6.8   写 manifest（含每个源包 SHA-256）+ 追加事件 + 重建 state
               （verified:true / consumed_by:<attempt_id>），Flush($true)   (D12 后半)
20      ==> 状态 EXTRACTED
21      §6.8   **重新从磁盘读 state.json**，按 verified+consumed_by gate 删源包（I3）
22      §6.10  Unblock-File 整树；写 manifest.stage1_snapshot 文件清单（§3.5）
23      ==> 状态 SCANNED       （§7 的前置条件在此刻全部就绪）
24      清空 D:\GameLib\_stg\<stg_id>\ 与 _stg\<stg_id>-p\（若存在）；释放条目锁
25  写事件 run.ended；释放批次锁
```

三条贯穿全程的不变量复述：

- **I1**：第 06、07、09、13、16、17 步的任何一个停机态，**都不删除任何文件**，也不移动源包。
- **I2**：状态只能由 `events.jsonl` 的追加事件推进（唯一改 state 的 kind 是 `state.entered`，§3.3.2）；`state.json` 随时可删掉重建。
- **I3**：第 21 步的删除**只读磁盘上已落盘的 `verified` + `consumed_by`**，不现场重新判断——这是幂等的唯一来源。重跑时若 `verified` 已是 `true`，第 21 步直接把剩下的源包删完，不会重新解压。

> **与 §4 的对齐点（契约，已由主控裁决为准）**：本节把 **Defender 预检钉死在 `VERIFYING` 之内、`staging.promoted`（落地）之前**（第 16 步）；`SCANNED` 的定义是「Defender 裁决已落盘为放行 **且** 整树 `Unblock-File` 已完成 **且** `manifest.stage1_snapshot` 已写」。
> **§4 已按本节改**（H20/H21 的 `from_state` 改 `VERIFYING`；M08 补「Defender 裁决已落盘为放行」；M09 只留「源包删除账已平 + `Unblock-File` 完毕 + `stage1_snapshot` 已写」；停机态手册里 `THREAT_*` 的解除后回到 `VERIFYING`）。
> 理由见 §6.9：`-DisableRemediation` 官方明写**忽略排除目录**，而我们只排除 `_stg`——扫描必须发生在文件还在 `_stg` 里的时候，这套组合才成立；反向改会让「A 级不删源包留证」不可能成立，并破坏 I1。

#### 6.1.3 互斥与可中断

- **两层锁**（与 §4.6.1 / §5.1.4 / §8.8.6 同一套）：`batches\<batch-id>.lock`（批次级，整次运行持有）+ `games\<dirname>\.gameflow\.lock`（条目级，**真正的并发单位**，每个 item 循环体内取/放）。两层都用 **`FileShare.None` 独占文件句柄**，OS 在进程死亡时自动释放。不用「lock 文件 + PID 存活检测」——Windows PID 复用快，且「超时清理陈旧锁」引入时间猜测，与确定性偏好冲突。【仲裁裁定；工程约定见 §8.8.6】
- 条目锁是 B 与 A 互斥的唯一支点：§4.5.3 的 `ReclaimStaging` 与 §4.5.1 的重建都要求「在 item 锁内做」，B 不取条目锁就满足不了，且会与 A 静默地不互斥。
- 抢锁失败统一停在运行期状况 `LOCKED_BY_ANOTHER_RUN`，**不落盘、不进状态机**。
- B 在任何一个 item 边界都可以被 Ctrl+C 打断（E1：这是主力机）。崩溃安全来自 I2，不来自单次文件替换的原子性——`ReplaceFile` 官方自称只是「把几步合进一个函数」并列举了三种部分失败中间态，`MoveFileEx` 全文无 atomic 字样。【官方；落盘协议见 §8.8.4】
- **崩溃恢复**：从 journal 重建走 §3.9.3 / §4.5.1；`*.tmp-*` 清理与孤儿 `_stg` 回收**只有 B 做**（R26），A 永不删除（I5）。

---

### 6.2 7-Zip 调用约定（**契约级；全 SPEC 唯一封装，§5 / §7 / §8.8.7 / §10 全部引用本小节**）

#### 6.2.1 唯一入口

项目内**禁止**在任何地方直接拼 7-Zip 命令行。所有调用经由唯一封装 `Invoke-SevenZip`（`lib\SevenZip.psm1`）。本签名已合并 §8.8.7 的那一套，**必须能表达 A 的只读调用**（`l -slt`、不传 `-t`）：

```
Invoke-SevenZip
    -Op         <'l' | 't' | 'x'>          # 只有这三个命令，永不用 a/u/d/rn
    -Archive    <绝对路径>                  # **单个**。永不传第二个位置归档参数（陷阱 T2）
    -Filter     <归档内条目路径>             # 可选，仅 §6.3 阶段 A 使用，用后必须校验 Files: 1
    -OutDir     <绝对路径>                  # 仅 Op='x'
    -Password   <string?>                   # $null 或 '' -> **完全不传 -p**（陷阱 T5）
    -TypeSpec   <'none' | '*' | '*:s16m' | '*:s64m' | '#:e'>   # 白名单，其它取值一律拒绝（T4）
    -TimeoutSec <int>
返回 [pscustomobject]@{
    ExitCode       # int，直接来自 Process.ExitCode
    TimedOut       # bool
    Stdout         # string[]，按 \r 与 \n 同时切分
    FilesReported  # int?，解析自 "Files: N"，未出现则 $null
    Warnings       # string[]，"WARNINGS:" 段与 "Open WARNING:" 行
    Entries        # 仅 Op='l'，解析后的归档级块 + 条目级块
}
```

**不提供 `-ExtraSwitch` 这类逃生口**（§8.8.7 原有的该参数取消）：任何新开关必须先进下面这张表，否则封装拒绝。

每次调用**固定附带**的开关：

| 开关 | 何时带 | 理由 |
|---|---|---|
| `-y` | 总是 | 无提示。注意：默认配 `-y` 就是**无提示覆盖全部**【实测，可外推】，所以 `-o` 目录必须是我们自己建的空目录。`l` 下无副作用 |
| `-bso1 -bse1` | 总是 | stdout 与 stderr 都定向到 stdout，得到**单一有序流**，解析器只需一个状态机【实测 macOS 25.01；可外推——流路由在跨平台公共 UI 代码】 |
| `-slt` | 仅 `l` | 机器可解析的键值块输出；`Entries` 的解析完全依赖它（A 的只读预检、G5 也用同一形状） |
| `-t<TypeSpec>` | **仅当 `-TypeSpec` ≠ `none`** | `none` = 不传 `-t`，走 7-Zip 默认的 `-t*:r`（内容嗅探 + 8 MB 签名扫描）。见 6.2.7 |
| `-aoa` | 仅 `x` | 覆盖模式显式化。目录本来就是空的，语义明确【实测】 |
| `-snz` | 仅 `x` | MOTW 传播，见 §6.10 |

`-scc` / `-sccUTF-8` 的去留**只在这里规定一次**（§8.8.7 不得自行规定）：
> **在 §11 的「重定向管道下 `-sccUTF-8` 是否生效」待测项裁定之前，封装一律不传 `-scc*`**，控制台编码统一靠 `StandardOutputEncoding` / `StandardErrorEncoding = UTF8Encoding($false)`（见 6.2.6）。实测确认它在重定向管道下确实生效且必要时，再在**本表**加一行固定附带。【官方：`-scc` = *Set charset for console input/output*，取值只有 `UTF-8|WIN|DOS`】

**永远禁止**出现的开关（封装里硬拒，不是靠纪律）：

| 禁止 | 理由 |
|---|---|
| 任何具体类型名 `-t7z` `-tzip` `-trar` `-tcab` … | 会**关闭内容嗅探**，把本可自动处理的情况变成 exit 2 硬失败。实测 `-tzip fake.jpg`（内容其实是 7z）→ exit 2；不给 `-t` → 正常打开【实测，可外推——嗅探在跨平台公共代码】 |
| `-snld*`（尤其 `-snld20`） | 官方描述即「bypass default security checks when creating symbolic links」。实测 `-snld20` **真的创建了逃逸符号链接且 exit 0**【实测】 |
| `-mcp*` | 官方 `method.htm` 明写 `-m` 的适用命令是 **a, h, d, rn, u**，不含 `x/l/t`；macOS 实测完全无效（连 `-mcp=99999` 这种非法值都不报错，说明根本没被消费）【官方 + 实测】。只有在 §11 #20 通过后、由配置显式开启时才允许，见 §6.11 |
| `-spe` | **不是 `tar --strip-components=1`**。它只在配合 `-o*` 时避免「归档名目录 / 归档内同名根目录」套娃；在普通 `-o<dir>` 下实测**完全无效**【实测，可外推】。写进方案只会误导 |
| `-r` / 通配归档名 | 我们永远显式枚举文件，不让 7-Zip 做文件系统匹配 |

#### 6.2.2 退出码完整语义表

官方只定义 6 个码，没有别的（`MANUAL/cmdline/exit_codes.htm`，随官方 tarball 分发，一手）：

| exit | 官方定义 | 本项目的解释 | 处置 |
|---|---|---|---|
| **0** | No error | **唯一的成功候选**，但还不够，见 6.2.3 | 继续走三条附加检查 |
| **1** | Warning (Non fatal error(s)) | 「完成了，但**有东西被静默跳过**」。官方给的例子正是「文件被别的程序锁住，所以没被压进去」 | **一律当失败**。见下方专段 |
| **2** | Fatal error | 密码错 / CRC 失败 / 截断 / 缺卷 / 非归档 / 不支持的方法 / 危险符号链接。**含义靠 stderr 文本区分** | 按 6.2.5 的文案映射表分流 |
| **7** | Command line error | **我们自己拼错了参数**，是程序缺陷不是数据问题 | 立即中止整个 B 运行，写 `events.jsonl`，非零退出。**不落任何停机态** |
| **8** | Not enough memory | 最常见成因：用大字典压的 RAR5。官方 `-smemx{size}g`：*"Default allowed limit for RAR unpacking is 4 GB"*【官方，未实测】 | 用 `-smemx8g` **重试一次**；再失败 → `WAITING_FOR_RULE`（配置问题，**不是** `CORRUPT`） |
| **255** | User stopped the process | **在本项目里真实含义是「需要密码」**——加密包在 stdin 不可用时返回 255。字面理解会判错 | 转 §6.3 的密码链；密码链已用尽则 `WAITING_FOR_PASSWORD` |
| *(超时)* | — | 我们自己 `Kill($true)` 掉的 | `TimedOut = true`，清空本层 staging，落 `BLOCKED_RESOURCE` |

**exit 1 为什么一律当失败。** 它的字面含义就是「操作完成了，但有东西被静默跳过」。实测复现（`a` 命令，一个不可读的源文件）：

```
Add new data to archive: 2 files, 10 bytes
Files read from disk: 1          <-- 说好的 2 个，只读进 1 个
WARNING: Cannot open 1 file
$? -> 1
```

归档声称成功、实际少了一个文件、退出码 1。映射到本项目就是「解压『成功』→ 删掉源包 → 游戏缺文件 → 源包已经没了」这条不可逆的数据丢失链。

> **契约 SZ-1：成功判据必须是 `ExitCode -eq 0` 严格相等。不是 `-ne 2`，不是 `-le 1`。**
> 【官方定义 + 实测，可外推——退出码在跨平台公共代码】

#### 6.2.3 `Warnings:` 与退出码是两条独立信号

反直觉但实测确凿：**7-Zip 可以报了 warning 而退出码仍是 0，不是 1。**

```
$ 7zz t tail-junk.7z
WARNINGS:
There are data after the end of archive
Tail Size = 5000
Everything is Ok
Archives with Warnings: 1
Warnings: 1
$ echo $?   ->  0
```

【实测，可外推】

> **契约 SZ-2：「有没有 Warnings」与「退出码是不是 0」必须**分别**检查，不能互相推导。**

Warning 分类表在 `rules\sevenzip-warnings.json`（**已登记进 §2.4 布局树与 §3.6 的 schema**；人是唯一写者，**默认拒绝未知 warning**）：

| 分类 | 串 | 处置 |
|---|---|---|
| 白名单（良性） | `Cannot open the file as [PE] archive` | 放行。成因：`.exe` 扩展名让 7-Zip 先试 PE handler 再回退【实测】 |
| 黑名单（必停） | `There are data after the end of archive` | 拼接 / 夹带，可能是图种或被塞了东西 → `WAITING_FOR_RULE` |
| 未知 warning | 其它任何 | **默认按黑名单处理** → `WAITING_FOR_RULE`，并把原文打进报告 |

错误文案会随 7-Zip 版本漂移，所以这张表必须是**可配置文件 + Preflight 用已知样本自检**，不能散落在代码里硬编码。【仲裁裁定】

#### 6.2.4 陷阱清单（每条都有对应的实现级防御）

| # | 陷阱 | 实测证据 | 防御（契约） |
|---|---|---|---|
| **T1** | `7z x plain.7z -oOUT "no-such.xyz"` → exit 0，`Files: 0`，OUT 为空 | 【实测】 | 见 T3 的三条检查 |
| **T2** | **`7z t a.7z b.7z` 不会测两个包**。第二个参数是 `a.7z` **内部**的文件名过滤器，返回 exit 0 而 `b.7z` 根本没被测 | 【实测】：`Testing archive: plain.7z / No files to process / Everything is Ok / Files: 0 / exit 0` | **每个包必须单独调用一次。** 封装层只接受单个 `-Archive`。唯一允许出现第二个位置参数的地方是 §6.3 阶段 A 的 `-Filter`，且**必须**校验 `Files: 1` |
| **T3** | exit 0 且零文件的四种情形：① 过滤器无匹配 ② 多归档参数误用（=T2）③ 归档里只有空目录 ④ 尾部垃圾 | 【实测，四种全复现】 | 见下方契约 SZ-3 |
| **T4** | 传具体类型名关闭嗅探 | 【实测】`-tzip fake.jpg` → exit 2 | `-TypeSpec` 白名单 |
| **T5** | 密码为空时 `-p$PW` 展开成**裸 `-p`**，7-Zip 切进交互模式并表现成「密码错」(exit 2) | 【实测】 | 密码为空 → **完全不传 `-p`**；探测阶段统一传非空哨兵值。实测对**不加密**的包传 `-p` 完全无害（`7zz t -p"__no_password__" plain.7z` → exit 0） |
| **T6** | 加密包在 stdin 是终端时**无限挂住** | 【实测 macOS】`</dev/null` 时得 exit 255 而不是挂死 | stdin 必须重定向 + **必须设超时**（见 6.2.6）。⚠ **Windows 的控制台密码读取可能走 `CONIN$` 而不是 stdin，那样重定向就挡不住挂起 → 【待测】§11 #5。超时是唯一不依赖这条实测的兜底。** |
| **T7** | exit 2 **不代表没有副作用**——磁盘上可能已经有残骸 | 【实测】不支持的压缩方法：exit 2，但 `weird.bin` 留了个 0 字节文件 | 必须解到 `_stg`，失败即整个 staging 子树删掉重来，**绝不就地解压** |
| **T8** | 路径穿越 7-Zip 确实拦截，但**完全静默**（exit 0、零 warning、零 stderr） | 【实测】7 种恶意路径全被剥干净，`/tmp/escaped-deep.txt` 没有产生 | 必须自己扫 `l -slt` 的 `Path`，见 §6.6 |
| **T9** | 缺中间卷 / 缺末卷 / 单文件截断，stderr **一字不差** | 【实测】三者都是 `Unexpected end of archive` | 卷集连续性与等大校验必须自己做（归组见 §5.5.2，重建见 §6.5） |

> **契约 SZ-3：`t` / `x` 判成功需要三条同时成立，缺一不可。**
> 1. `ExitCode -eq 0`（严格相等）
> 2. `Warnings` 为空，或全部命中白名单
> 3. **解析出的 `Files: N`，`N > 0`，且 `N` 等于同一 `TypeSpec` 下 `l -slt` 的非目录条目数**
>
> 第 3 条是 T1/T2/T3 的唯一解药。同一层的 `l` / `t` / `x` 必须用**同一个 `-TypeSpec`**，否则「列的是一层、解的是两层」，条目数永远对不上。

#### 6.2.5 exit 2 的文案分流表

| stderr/stdout 关键串 | 含义 | 落到哪 | 确定性 |
|---|---|---|---|
| `Wrong password : <path>` | ZIP AES 密码错 | 密码链下一候选；用尽 → `WAITING_FOR_PASSWORD` | **确定**（AES 有 2 字节 password verifier） |
| `Cannot open encrypted archive. Wrong password?` | 7z 头加密密码错 | 同上 | **确定**（头解不开就是密码错） |
| `Data Error in encrypted file. Wrong password? : <path>` | 7z 仅数据加密：密码错**或**密文损坏 | 见下 | **模糊**（7z 的 AES-256 数据流没有密码校验值，7-Zip 自己也分不清——文案里那个问号就是在承认这一点） |
| `CRC Failed : <path>` | 未加密数据损坏 | `CORRUPT` | 确定 |
| `Unexpected end of archive` | 截断**或**缺卷 | 卷集自检说了算：卷集不连续 → `WAITING_FOR_VOLUMES`；单文件 → `CORRUPT` | 模糊，靠自己数卷消歧 |
| `Is not archive` / `Cannot open the file as archive` | 不是归档 | 走 §6.5.1 的 `-t*:s64m` 重试；仍失败 → `WAITING_FOR_RULE` | 确定 |
| `Unsupported Method : <path>` | 7-Zip 不支持该压缩方法（如 ZIPX 的 method 96/97） | `WAITING_FOR_RULE` | 确定 |
| `Dangerous link path was ignored : <p> : <target>` | 危险符号链接被拒（**这个是响亮的，与 T8 的静默相反**） | `BLOCKED_RESOURCE`，只允许放弃或重下（§6.6） | 确定 |

对那条模糊项的消歧（确定性，不猜）：

- 密码库里**根本没有**该条目 → 直接 `WAITING_FOR_PASSWORD`，不需要消歧。
- 密码库里**有**条目且全部候选用尽仍报此错 → 两种可能并存 → 落 `EXTRACT_FAILED_AMBIGUOUS`，**不删任何源包**，报告里同时给出两种可能、已试候选数、卷集完整性结果。
- 「全部条目都报错更像密码错、只有个别条目报错更像局部损坏」——这是启发式，**只作为报告里给人看的提示，不作为自动判据**。

【全部实测，可外推——文案来自跨平台的 `CPP/7zip/UI/Common/` 与各 handler，不含平台分支；但文案会随版本漂移，见 6.2.3】

#### 6.2.6 PowerShell 调用形状

三条已被官方文档钉死的负面事实，合起来堵死了 5.1 下的所有传参路线：① `Start-Process -ArgumentList` **只用单空格拼接**、不逐参数加引号，把转义责任推回调用方；② `ProcessStartInfo.ArgumentList` 的 Applies-to moniker 列表里**没有任何 `netframework-*`** → **Windows PowerShell 5.1 根本没有这个属性**；③ `--%` 只展开 `%ENVVAR%`、**不展开 PowerShell 变量**，而本项目的路径与密码全部来自变量（把密码塞进环境变量再 `--%` 引用只是把明文从命令行挪到环境块，同用户进程照样读得到，不是安全改进）。【全部官方】

> **契约 SZ-4：B 的脚本本体必须在 `pwsh.exe` 7.4+ 下执行（`#Requires -Version 7.4`），并使用 `System.Diagnostics.ProcessStartInfo.ArgumentList` 逐参数投递。**
> 理由：5.1 下**不存在**任何安全的方式把含空格/引号/`$`/`` ` ``/`%` 的密码与路径传给 `7z.exe`。§9.11 的 Preflight 必须探测 `pwsh.exe` 存在；不存在则 B 拒绝运行（**不降级**）。Codex 那一层仍是 5.1，但它只传 `-BatchId <slug>` 这种受限字符集 token，不受影响（§6.1.1）。三层执行环境的完整分工见 §8.8.1。

规范形状：

```powershell
$psi = [System.Diagnostics.ProcessStartInfo]::new()
$psi.FileName               = $Cfg.SevenZipExe      # 绝对路径，来自 Preflight，不依赖 PATH
$psi.UseShellExecute        = $false                # 必须：否则拿不到重定向
$psi.RedirectStandardInput  = $true                 # 必须：T6
$psi.RedirectStandardOutput = $true
$psi.RedirectStandardError  = $true
$psi.StandardOutputEncoding = [System.Text.UTF8Encoding]::new($false)
$psi.StandardErrorEncoding  = [System.Text.UTF8Encoding]::new($false)

foreach ($a in $argv) { [void]$psi.ArgumentList.Add($a) }   # 逐个 Add，**永不拼字符串**

$p = [System.Diagnostics.Process]::Start($psi)
$p.StandardInput.Close()                            # 立刻关闭：给 7z 一个已关闭的管道
$so = $p.StandardOutput.ReadToEndAsync()            # 必须异步读，否则管道写满会双向死锁
$se = $p.StandardError.ReadToEndAsync()
if (-not $p.WaitForExit($TimeoutSec * 1000)) {
    $p.Kill($true)                                  # $true = 连同子进程树
    return @{ TimedOut = $true }
}
$exit = $p.ExitCode                                 # ← 从 Process 对象读，确定性最高
```

> **契约 SZ-5：必须显式检查退出码。** PowerShell **默认不会**因为外部命令返回非零而抛异常，`try/catch` **抓不到 7z 失败**。若因任何原因退回 `& $exe @argv` 形式，则必须在**紧接的下一行**捕获 `$LASTEXITCODE`（它是全局的，下一条命令就会被覆盖）。【官方；完整工程约定见 §8.8.8】

> **契约 SZ-6：每次调用必须设超时。**
> 默认值（**可配置参数，不是事实断言**）：`l` = 120 s；`t` / `x` = `600 + 120 × ceil(declared_total_GiB)` 秒，硬上限 6 h；头加密包拿不到 `declared_total`，用归档物理字节代入。
> 依据：`7z x` 解码时逐条目校验 CRC，10 GB 级包全量走一遍是分钟级【仲裁裁定】；超时只是兜底，**不是**正常路径的判据。超时触发 → `Kill($true)` → 清空本层 staging → `BLOCKED_RESOURCE`。

#### 6.2.7 `-TypeSpec` 的四种合法取值

| 取值 | 何时用 | 依据 |
|---|---|---|
| **`none`**（不传 `-t`） | **A 的顶层只读预检与 G5（§5.6.1 / §5.3.6）必须用它；B 的第 08/09 步顶层预算与路径穿越预检同样用它。** | 带 `-t*` 只能看到 Split 外壳、**拿不到内层的 `Size` 表**，体积预算与路径穿越预检会全部落空。默认模式是 `*:r` + 前 8 MB 签名扫描【官方 `type.htm` + 实测】 |
| `*` | **仅用于 §6.4 的递归计层**（同一层的 `l`/`t`/`x` 三者必须同用 `*`） | 官方 `type.htm`：`*` = *"Opens only one top level archive"*。强制每次只开一层，让 §6.4.2 的深度计数与现实一致。实测 `-t*` **不关闭嗅探**（`fake.jpg` 内容是 7z 时仍判 `Type = 7z`）【官方 + 实测】 |
| `*:s16m` / `*:s64m` | 前置垃圾字节超过默认 8 MB 扫描窗口；`N ∈ {16, 64}`，与 §3 的 `prefix_scan_mb ∈ {0,16,64}` 一一对应（`0` 即 `none`） | 官方 `type.htm`：*"Default scan size is 8 MBytes `*:s8m`"*。实测：9 MB 前缀 → `l` exit 2；加 `-t*:s16m` → exit 0，`Offset = 9000000`【官方 + 实测】 |
| `#:e` | **仅人工诊断**，不进自动流程 | 官方：*"Opens file in Parser mode and checks all byte positions as start of archive"*。全字节扫描，代价高 |

默认模式的直接后果：**伪装扩展名、前置垃圾字节（<8 MB）、图种、SFX 全部无需改名即可 `l`/`t`/`x`**。实测：`plain.7z` 复制成 `.dat/.txt/.jpg/.png/.pdf/无扩展名` 全部 `Type = 7z` 且 `x` 成功【官方 `type.htm` + 实测，可外推】。

> **C7「需要改后缀」因此被结构性地降级为例外分支。唯一的硬例外是分卷 `.NNN` 重建，见 §6.5。**

---

### 6.3 密码链 P0–P6 与等价展开

#### 6.3.1 优先级链（**顺序锁定，不得改序**）

```
P0  批次条目显式 password_ref                              最高，人工写死
P1  密码库按 (来源站点, 上传者/系列) 命中                    有序候选列表
P2  密码库按 (游戏标题) 命中                                有序候选列表
P3  同目录密码文本：密码.txt / password.txt / README* / *.nfo / *.url
        -> **只做正则精确抽取**（6.3.2）
P4  外层解出的密码文件                                      同 P3 的抽取规则
P5  资源页抓取（**仅执行体 C**）                            C 写进 vault 并标 confirmed:false；
                                                            **未确认的候选对 §6 等同不存在**
P6  停 WAITING_FOR_PASSWORD
```

vault 的键空间与条目 schema 见 §3.7.2（`site:<host>|<uploader>` / `title:<NFC 标题>` / `item:<item-id>` / …），本节不复述。

现实形态支撑这个设计（资源站一手素材）：真实密码里**绝大多数是「站点/上传者级统一密码」，不是每包一个**，同一上传者常有多个等价密码（如「密码是 `steamcl.com`、公众号：橙荔游戏、`chengleegame` 这三个」）→ 所以密码库条目必须是**有序候选列表**而不是单值，主键是 `(site, uploader)`。【社区，一手教程页】

#### 6.3.2 「什么叫猜、什么不叫猜」

> **判据：一个候选如果不能由「确定性规则 + 确定性输入」重放出来，它就是猜。**

| 不叫猜（允许） | 叫猜（硬性禁止） |
|---|---|
| 密码库里人工登记过的条目 | ❌ 从游戏名 / 站点域名 / 文件名「推导」 |
| **整行**命中固定正则的抽取结果 | ❌ 常见密码字典（`123456`、`www.xxx.com`） |
| 一个候选的 4 种编码等价展开（有限、可枚举） | ❌ 把 README 里「读懂」的一段话当密码 |
| 同一文本文件在 3 种编码下各解一次得到的字节串 | ❌ 超过 6.3.4 的尝试上限 |
| — | ❌ 失败后改写或删除原包 |

**P3/P4 的抽取正则集 `PW-RX-*` 定义在 `rules\source-profiles.json`（§3.6.1），本节不复述**，只规定它们的**使用语义**：

- **逐行匹配、必须整行命中**（`^…$`）。**整行锚定是「抽取」与「理解」的分界线**：一段自然语言里出现「密码」两个字不会命中；只有一行就是「密码：xxxx」才会。
- 允许「标签行 + 紧随其后的第一个非空行」这一种两行形式（该形式在 §3.6.1 的规则里标记出来），取整行并 trim 尾部 `\r`。
- 命中集合去重后 `|hits| == 0` → 视为没抽到。
- `|hits| > pw_extract_max`（默认 **3**）→ 也视为**没抽到**（文件里满是「密码」字样说明它不是密码文件）。
- `1 ≤ |hits| ≤ 3` → 按文件内出现顺序作为有序候选。

**文本文件本身的编码是个真实的坑。** 资源站的 `密码.txt` 极可能是 GBK 无 BOM，按 UTF-8 读会得到乱码，抽出来的密码字节就是错的。契约：

> **契约 PW-1：P3/P4 抽取器对每个候选文本文件做三次独立解码抽取——`utf-8`（有/无 BOM）、`gbk`(cp936)、`cp932`——三次的命中结果**全部**进入候选集（去重）。**
> 这不是猜：文本文件的编码本来就没有元数据，三个解码各自产生一个**确定的**字节串。上限由 6.3.4 的候选总数封顶。

#### 6.3.3 等价展开（≤4 个，**这不是暴力破解**）

```
expand(p) = dedup([
    p,                                     # 原样
    p.Trim(),                              # 去首尾空白（复制粘贴常带）
    Encoding.UTF8.GetBytes(p)  解码为 gbk    if 可解码 else skip,   # 正常中文 -> 乱码形
    Encoding.GBK .GetBytes(p)  解码为 utf-8  if 可解码 else skip,   # 乱码形 -> 正常中文
])
# **vault 条目带 byte_exact = true 时：expand(p) = [ 原样 ]，不做任何展开**（§3.7.2）
```

空间 ≤4，每个都有明确语义，全部失败即进下一候选。`byte_exact` 的语义是「这个密码已被人逐字节确认过」，对它做等价展开只会浪费 3 次尝试并污染失败判读。

#### 6.3.3b 候选的尝试顺序

候选列表的排序规则、使用统计的更新时点、以及四条硬护栏（尤其 **V1：绝不跨 key 借用高频密码**）见 **§3.7.4**，本节不重复。

这里只强调与本节其余部分的接口：排序**发生在候选集确定之后、等价展开之前**。也就是说 §6.3.1 的 P0→P6 先决定**有哪些候选**，§3.7.4 决定**按什么顺序试**，§6.3.3 的等价展开在每个候选内部各自进行。三者互不干扰，上限 `候选数 × 4`（§6.3.4）不受排序影响。

#### 6.3.4 硬性上限

```
candidates = dedup( P0 ∪ P1 ∪ P2 ∪ P3 ∪ P4 )        # P5 只在 §7 产生，且必须先被人确认
                                                     # 去重时每个候选记下它的最小 priority_tier；
                                                     # 尝试顺序按 §3.7.4 排序，tier 是第一排序键
                                                     # —— 合并的是「集合」，不是「优先级」
if |candidates| > pw_max_candidates (默认 8):
        **不做任何尝试**，直接 WAITING_FOR_PASSWORD，
        报告里列出全部候选（脱敏规则见 §9.1.4），
        由人挑一个写进批次条目的 password_ref
attempts_max = Σ over c in candidates of |expand(c)|      # byte_exact 候选只贡献 1
```

「候选多到 8 个以上就停下来问人」既守住了「不得超过候选数 × 4 次尝试」这条上限（`Σ|expand(c)| ≤ 4|candidates|`），也避免了静默截断候选列表这种更糟的做法。

#### 6.3.5 密码是**字节**不是字符

这是 C5 最大的坑，四条实测事实：

| 事实 | 实测 | 后果 |
|---|---|---|
| 同一个「看起来一样」的中文密码，在 GBK 系统和 UTF-8 系统上是**两个不同的密码** | `'密码测试'.encode('utf-8')` = `e5af86e7a081e6b58be8af95`；`.encode('gbk')` = `c3dcc2ebb2e2cad4`。交叉解压全部失败【实测（Python + Info-ZIP 造包，字节层事实，与工具无关，可外推）】 | 等价展开的第 3、4 项存在的理由 |
| **「乱码密码」是真实存在且可计算的** | 资源站同一页给出「密码 1 `鏌愪笉鐭ュ悕缃戝弸鍒嗕韩`（mp4 用）/ 密码 2 `某不知名网友分享`（文件夹用）」。实测 `'某不知名网友分享'.encode('utf-8').decode('gbk')` **恰好等于**那串乱码【实测 + 社区一手页面】 | 乱码密码 = 正常中文的 UTF-8 字节被按 GBK 显示，而包**确实**用那串乱码字节加密 |
| 可逆性是**有条件**的 | 只有**偶数字节长度**的中文串才能产生可显示的乱码密码；奇数长度（如 `灰原哀` 9 字节）GBK 解不了，显示成 `?`，**不可逆**【实测】 | 等价展开里「可解码则加入，否则 skip」不是偷懒，是这条事实的直接编码 |
| 尾随空格有意义，全角不等于半角 | `'pass '` ≠ `'pass'`（实测 rc 不同）；`ｐａｓｓ１２３`（7 个全角字符、UTF-8 21 字节）≠ `pass123`【实测】 | 展开里**原样与 Trim 都要留**；见下方契约 |

> **契约 PW-2：密码库存储必须字节精确。UI / 日志 / 报告 / JSON 序列化路径上**不得**做任何 Unicode 规范化。**
> `NFKC` 会把全角折成半角，直接破坏密码；`NFC/NFD` 会改动带浊点的日文假名。密码字段在 vault 里以 **Base64 编码的原始字节** 存储（§3.7.2），读出后直接交给 `ArgumentList`，中途不经过任何 `Normalize()`。

> **契约 PW-3：`events.jsonl` / `state.json` / `manifest.json` / 报告中**绝不**出现密码明文。**
> 只记 §3.3.2 的 `password.attempted` 那几个键：`source`（`P0..P5`）、`vault_key`、`candidate_index`、`variant_index`（0..3 对应 原样/Trim/u8→gbk/gbk→u8）、`exit`。
> 诚实局限（§9.1 复述）：密码**只能**经 `-p` 进命令行，这是 7-Zip 的硬限制（官方 `password.htm` 全表只有 `-p` 一项；实测证伪管道、文件重定向、4 个环境变量名）。**它因此会短暂出现在进程命令行里**，`Get-CimInstance Win32_Process | Select CommandLine`、Script Block Logging、Sysmon Event ID 1、EDR 遥测都看得见。这个暴露面**无法消除**，只能声明。【官方 + 实测】

#### 6.3.6 ACP 与 `PASSWORD_ENCODING_SUSPECT`

**本 SPEC 不断言系统 ACP 必须是 936。** 用户的 Windows 区域设置未知；若是日文 932，整套 GBK 换算根本不成立。【仲裁裁定】

契约：

- Preflight（§9.11.2）把 **ACP** 与 **Beta UTF-8 状态**记进 `env.snapshot` 事件与 `state.env`（§3.3.2 / §3.4.1），作为**解释失败的上下文**，不作为断言。
- 分流规则：

```
if 所有候选 × 所有展开 全部失败:
    if 任一候选含非 ASCII 字节:   ->  PASSWORD_ENCODING_SUSPECT
    else:                        ->  WAITING_FOR_PASSWORD
```

- `PASSWORD_ENCODING_SUSPECT` 的报告**必须**同时显示：当前 `env.acp`、`env.beta_utf8`、以及**这个候选的 4 种展开各自的十六进制字节串**（不显示可读明文，避免泄露与二次编码污染）。这样人一眼能看出「哦，站点给的是 UTF-8 形，我机器是 936」。
- 相关的社区事实（进报告的提示文案，**不作为自动动作**）：勾选 Windows「区域设置 → 管理 → Beta：使用 Unicode UTF-8 提供全球语言支持」会把 ACP 从 936 变成 65001，**用 GBK 环境打的中文密码包就打不开了**。资源站教程原文即「提示解压码错误的，自己检查一下有没有转区，语言里的区域关闭 beta 版本」。【社区，一手教程页 + 多篇旁证】

#### 6.3.7 探测顺序：为什么探测一律用 `t` 而不是 `x`

**ZipCrypto 有 1/256 的假通过。** 传统 ZIP 加密只用 1 字节校验头，错密码约 1/256 概率通过校验并**解出垃圾文件**。800 个小文件 + 错密码的实测：每次都有 1 个条目「通过」、若干条目 `bad CRC`。【实测（Info-ZIP/Python 造包；机理属 ZIP 格式层，与工具无关，可外推）】

> **「解出来几个文件」永远不能当密码正确的证据。**

处置（比「必须在 `_stg` 里做、失败后清空」这条纪律更强，**结构性地**消除这个风险）：

> **契约 PW-4：密码探测阶段一律使用 `7z t`（只读、不写盘），绝不使用 `7z x`。**
> `t` 会逐条目校验 CRC，ZipCrypto 的 1/256 假通过在 CRC 这一关必然暴露（exit 2）。因为探测不写盘，「垃圾产物被下游误当成内容」这个失效模式在设计上就不可能发生，而不是靠「记得清空 staging」的纪律。
> **唯一必须落盘的例外**是 §6.5 的分卷重建，它在 `_stg\<stg_id>-p\` 内做、用后即删。

两阶段探测（让「全量 `t` 太慢」这个问题消失）：

```
阶段 A（快速淘汰，仅对可列目录的包）
    probe := l -slt 里 Encrypted = '+' 且 Size > 0 的**最小**条目
    if probe 存在:
        for each cand in 展开后的候选序列:
            r := Invoke-SevenZip -Op t -Archive <包> -Filter <probe.Path> -Password cand
            if r.ExitCode -eq 0 AND r.FilesReported -eq 1:   -> 该候选进入阶段 B
            # ⚠ 这里**故意**使用第二位置参数作为内部过滤器（陷阱 T2 的正用）。
            #   `FilesReported -eq 1` 是必须的：否则 exit 0 毫无意义。
    else:
        跳过阶段 A（全是空文件/目录，或头加密包拿不到条目表）

阶段 B（确认）
    对阶段 A 唯一存活的候选（或跳过 A 时的逐个候选）跑一次**全量** `7z t`
    exit 0 + 无非白名单 Warnings  ->  密码确定，写 password_source
```

三个格式特例：

| 格式 | 探测代价 | 说明 |
|---|---|---|
| **7z 头加密（`-mhe`）** | **O(1)** | 无密码时 `l` 直接 exit 255，一个字段都拿不到；密码错在头解密阶段就失败（`Cannot open encrypted archive. Wrong password?`）→ 每个候选一次极快的调用【实测】 |
| **ZIP AES** | 阶段 A 生效 | AES 有 HMAC，**不存在** 1/256 假通过。判别见下 |
| **RAR4** | 阶段 A 生效，但**结论不可信** | 见 6.3.8 |

**AES vs ZipCrypto 的判别归 B，用与 §6.11 相同的纯字节 ZIP 中央目录解析器（确定性，不依赖 7-Zip 开关）：**

```
compression method == 99            ->  WinZip AES（有 HMAC，无 1/256 假通过，D7 不适用）
否则 general purpose bit 0 == 1     ->  传统 ZipCrypto（有 1/256 假通过，**D7 强制全量 CRC 重读**）
```

【实测（archive-reality 用 Python `zipfile` 逐条目确认 `compress_type == 99` 即 WinZip AES），可外推——这是 ZIP 格式规范的字节布局，与 OS 无关】

A 在只读预检里只记录 `encrypted: true` 与 `Method` **原文**作为上下文（§5.6.4），**不作判据**；判别结果写 `archives[].encryption_method`（§3.4.1）。
【待测】7-Zip `l -slt` 的条目级 `Method` 字段在 Windows 上对 ZIP AES 打印什么串——**不作为判据**，我们自己解析字节。

#### 6.3.8 RAR4：工具层不可区分 → `EXTRACT_FAILED_AMBIGUOUS`

WinRAR 官方退出码文档原文：

> *"Command line RAR returns the exit code **11** if it can detect that the user entered a wrong password. This code can be returned **only for RAR 5.0 archives**. It is **impossible to distinguish a wrong password and data damage for RAR 4.x archives**."*
> 【官方 https://chmlib.com/WinRAR/html/HELPExitCodes.htm】

即使我们用的是 7-Zip 而不是 `rar.exe`，这是**格式层**的缺陷（RAR4 没有密码校验值），换工具不会改变。RAR4 / RAR5 的魔数判别见 §5.4.4 的魔数表。

处置：

```
if 容器是 RAR4 且 所有候选全部失败:
        state -> EXTRACT_FAILED_AMBIGUOUS          # 不是 WAITING_FOR_PASSWORD，不是 CORRUPT
        报告必须同时给出：
          ① 「密码错」与「数据损坏/分卷不全」两种可能，明确说明工具层不可区分
          ② 已尝试的候选数与它们的来源（P0..P4）
          ③ §5.5.3 的卷集连续性 + 除末卷外等大的检查结果（这是**唯一**能给人提供的偏向性证据）
          ④ 每卷的 SHA-256（便于人拿去和资源站公布的校验值比对）
        不删除任何文件
```

【待测】§11 #11：本轮六份调研对 RAR 的**真实解码零验证**（造不出合法 RAR 样本）。RAR4/RAR5 加密与分卷的实际行为必须在目标机用 WinRAR 造样本复验。**不做这一项的后果**：资源站大量用 RAR，我们对这条分支的所有断言都停留在文档级。

---

### 6.4 解压与递归解包

#### 6.4.1 staging 布局

```
D:\GameLib\_stg\<stg_id>\          stg_id = 8 位十六进制，与 attempt_id 一一对应
                                   **解压产物直接落在这里，没有子层**
                                   前缀长度 25 字符（与 §2.5.3 的预算表完全一致）
D:\GameLib\_stg\<stg_id>-p\        **同级**目录，分卷重建（§6.5）等必须落盘的一次性产物，
                                   用后即删；内部只放 ASCII 短名 vol.NNN
```

两条硬约束：

1. **必须与游戏库同卷。** `File.Replace` 跨卷抛异常；staging→游戏目录的移动跨卷会退化成「复制 + 删除」，代价和风险都上升（且会让 §6.12 的余量公式要预留两倍）。【官方 + 仲裁裁定】
2. **不放在游戏目录下。** 上游设计的 `games\<名>\.gameflow\staging\<attempt-id>\` 单这一段就吃 39 字符，配合日文长名 + 多重嵌套会真的撞破 260。库级短中转区把这段压到 25 字符。【算术，§2.5.3】

#### 6.4.2 `-t*` 强制单层与深度计数口径

> **契约 EX-1：`max_depth = 5`（默认值登记在 §3.2.3）。计数口径是「**成功解开一个白名单容器 = depth + 1**」，不是「调用了几次 7z」。**

为什么不能用调用次数：`.tar.gz` 在不同工具下一步或两步（`7z x a.tar.gz` 只出 `a.tar`，`bsdtar -xf` 一步到底）【实测】。用 `-t*` 强制每次只开一层，让计数与现实一致。

```
depth 0  =  源包本身（尚未解开）
解开源包 -> depth 1
每成功解开一个白名单容器 -> depth + 1      # **Split 外壳同样 +1，没有例外**
depth > max_depth(5)  ->  BLOCKED_RESOURCE，保留一切，报告里画出已展开的层次树
```

【待测】§11（`-t*` 作用在 `.001` 上的行为）：本轮只在**默认模式**下实测过（`Type = Split, Volumes = 3, Files: 4`，即默认模式会穿透）。实现上两种行为都要能处理：若 `-t*` 只吐出内层归档**文件**，则 Split 外壳这一层照样记 depth+1，然后立即对吐出的文件继续 `peel`。**不做这一项的后果**：分卷包的层次树画得不对（但计数口径统一后不会再产生 A/B 分歧）。

#### 6.4.3 递归算法

容器白名单与扩展名排除表的 **schema 与内容以 §3.6.3 的 `container-types.json` 为唯一权威**（键名：`recurse_into_types` / `never_recurse_types` / `excluded_extensions` / `content_file_exclusions` / `manual_install_types`）。本节只给判定顺序：

```
peel(node, depth):
    # node 可以是「源包路径」或「_stg\<stg_id>\ 里的一个产物文件」
    if depth > max_depth:                                       -> BLOCKED_RESOURCE ; return
    if ext(node) ∈ excluded_extensions:
            # docx/xlsx/pptx/epub/apk/jar/msi/... —— 这是扩展名**唯一**真正有用的场合：
            # 不是识别要解什么，而是排除不该解什么
            -> 当作普通文件，return

    r := Invoke-SevenZip -Op l -TypeSpec '*' -Archive node -Password <已确定的密码 或 哨兵>
    if r.ExitCode == 2:                                         -> 不是归档，普通文件，return
    if r.ExitCode == 255:                                       -> 需要密码，回 §6.3
    if r.Entries.Archive.Type ∈ never_recurse_types:
            -> 不递归。若命中 §6.13 的 R1/R2/R3 -> 按 §6.13 落停机态；否则当普通文件
    if r.Entries.Archive.Type ∉ recurse_into_types:             -> 当普通文件，return
    Δdepth := 1                                                  # 无例外（含 Split）

    §6.12 护栏（条目数 / 压缩比 / 绝对上限 / 磁盘余量）
    §6.6  路径穿越自查（扫 r.Entries[*].Path）
    冲突自查：r.Entries[*].Path 与 <node 所在目录> 的现有条目求交，非空 -> WAITING_FOR_RULE

    Invoke-SevenZip -Op t -TypeSpec '*' -Archive node -Password …          (D2, D3)
    Invoke-SevenZip -Op x -TypeSpec '*' -Archive node -Password …
                    -OutDir <node 所在目录>                                 (D4, D5)
    # ↑ **就地解开到容器的同级目录**，保持上传者的目录形状。
    #   不解到 d<N>\ 这类平行目录：那会丢掉「这个内层包原本在 data\ 下」的信息。

    删除 node（它是解压产物或源包）：
        node 是**解压产物** -> 直接从 staging 删掉（中间层容器不是用户的文件）
        node 是**源包**     -> **绝不在这里删**，交给 §6.8 的独立删除阶段

    for each 新出现的文件 f in <node 所在目录> 的递归枚举:
            peel(f, depth + Δdepth)
```

**为什么递归终止必须靠内容嗅探而不是扩展名**——两条实测：

1. `7z x mystery.gz`（内容其实是 `bundle.tar.gz` 改名）→ 解出 `mystery`，**没有 `.tar` 扩展名**，但 `file` 认它就是 POSIX tar。gzip 单流不一定带原始文件名，7-Zip 就用「归档名去掉扩展名」当内层名。**靠扩展名做递归检测会在这里漏掉一整层。**【实测，可外推】
2. `L1.7z(加密) → L2_disguised.dat(实为 7z) → L3.zip → payload.txt` 三层链路，逐层用 `l -slt` 的 `Type` 判断，全部正确识别并在第 4 层正确终止。【实测】

**「7z 能打开它」≠「它是要递归解包的容器」**——`doc.docx` 的 `Type` 是 `zip`（照解会把文档拆成 XML 碎片，文档就毁了）、真 Mach-O 的 `Type` 是 `Mub`（会被拆成两个切片），而最要命的是 **Windows 上每一个 `.exe` / `.dll`**：`l` exit 0、`Type = PE`，7-Zip 会去解它的资源节与 section，而**一个游戏目录里少说几十个**。【实测（macOS 上是 `Mub`/`MachO`；`7zz i` 的 handler 表显示 Windows 是 `PE exe dll sys M Z`），机制可外推，数量级更大】

> **契约 EX-2：递归白名单、`never_recurse` 清单与扩展名排除表一律读 `rules\container-types.json`，schema 与取值见 §3.6.3。本节不复制这三张表。**
> **绝不能用「7z 能不能打开」当判据。** handler 名（`Type` 字符串）的大小写必须与目标机 `7z i` 的输出逐字一致——这条是 §11 的待测项，未核对前全 SPEC 三节必须写同一份拼写。

#### 6.4.4 `.tar.gz` 的两层与那个扩展名坑

```
$ 7zz l bundle.tar.gz      ->  Type = gzip,  一个条目：bundle.tar
$ 7zz x bundle.tar.gz -oOUT ->  OUT/bundle.tar     （只解出 tar，没有继续）
$ cp bundle.tar.gz mystery.gz ; 7zz x mystery.gz -oOUT3
                            ->  OUT3/mystery      （**没有 .tar 扩展名**，但它确实是 tar）
```
【实测，可外推】

后果：`.tar.gz` 恒定占 **2 层** depth；且第 2 层的探测**只能**靠对 `mystery` 这个无扩展名文件做 `l -slt` 嗅探。

#### 6.4.5 同名文件夹套娃与 `-spe`

**`-spe` 解决不了这个问题。** 它只在配合 `-o*` 时避免「归档名目录 / 归档内同名根目录」套娃；在我们使用的普通 `-o<dir>` 下实测**完全无效**（根目录 `GameRoot/` 照样保留）。【实测，可外推】

> **契约 EX-3：默认**不做**自动展平（`flatten.enabled = false`）。**
>
> 理由（取舍，不是技术限制）：
> 1. §7.6.2 已经把「游戏根目录」定义为**包含引擎标记文件的那一层**，不等于 `games\<dirname>\`。多出一层 `GameName\GameName_v1.02\` 对 MTool 流程**无害**。
> 2. C3 要求的是「源包与本体在同一个游戏条目目录下」，不是「在同一个物理层级」——源包躺在 `games\<dirname>\`，本体在其下一层，C3 成立。
> 3. 展平是**不可逆**的目录结构改动，而收益只是好看。用户品味明确反对为了流程而流程。
>
> 开关打开时的行为（确定性、可枚举）：
> - 仅当 `_stg\<stg_id>\` 下**恰好只有一个目录、零个文件**时触发；
> - **只展平一层**（展平多层可能吞掉游戏真实的目录结构）；
> - 用 move 不用 copy；目标存在同名项即**整个展平放弃、零改动**；
> - 展平动作连同原名按 §3.3 追加事件。

---

### 6.5 分卷 `.NNN` 重建（C7 的唯一硬例外）

#### 6.5.1 为什么这是唯一需要改名的场景

先把**不需要**改名的场景全部划掉（全部【实测】，机制可外推）：

| 场景 | 是否需要改名 | 依据 |
|---|---|---|
| 伪装扩展名（`.mp4`/`.jpg`/`.pdf`/`.txt`/无扩展名） | **不需要** | 默认 `-t*:r` 按内容嗅探（`-TypeSpec none`） |
| 前置垃圾字节 ≤ 8 MB / 图种 | **不需要** | 官方默认签名扫描窗口 8 MB。实测 1 KB、3 MB 前缀直接 `l`/`x` 成功，只是 `Offset` 变了 |
| 前置垃圾字节 > 8 MB | 不需要改名，**加开关** | **单次** `-t*:s64m` 重试（不做阶梯）。实测 9 MB 前缀 + `-t*:s16m` → exit 0，`Offset = 9000000`；`*:s16m` 仅在批次条目显式声明 `extraction.prefix_scan_mb = 16` 时使用 |
| SFX `.exe` | **不需要** | `Offset > 0` + 内层 `Type`，直接当归档解（§6.13） |
| **分卷组的文件名不成 `.NNN` 序列** | **必须改名** | 见下 |

实测（仲裁一手实验）：

```
decoy/ game_a.mp4 + game_b.mp4 + game_c.mp4   (其实是 parts.7z.001/002/003)
    7z t game_a.mp4                 ->  exit 2，完全打不开
half/  game.7z.001 + game_b.mp4 + game_c.mp4  (只改了第一卷)
    7z t game.7z.001                ->  exit 2  "Unexpected end of archive"
full/  anything.7z.001/.002/.003    ->  exit 0  Type = Split  Volumes = 3  Files: 4
d4/    game.mp4.001/.002/.003       ->  exit 0  Type = Split  Volumes = 3  Files: 4   ★
```

三条结论：

1. **7-Zip 靠 `basename.NNN` 的文件名序列去找同伴卷**，没有任何开关能「显式给我这一组文件」。名字不成序列就完全打不开。
2. **只改首卷不行**，必须整组补齐。
3. ★ **`.NNN` 之前的扩展名无所谓**（`game.mp4.001` 正常工作）。所以资源站教程里「mp4 先改 zip 再改 7z.001」中间那步是多余的。

#### 6.5.2 触发条件（三条与条件，缺一不进）

```
V1  该文件的 `l -slt` 返回 exit 2（含 `-t*:s64m` 重试失败）
V2  §5.5.2 的卷集归组判定它属于一个「疑似分卷组」：
        同目录、同前缀、≥2 个文件、**除排序最后一个外字节数完全相等**
V3  批次条目里有**显式声明**的重建规则（人工确认过，`WAITING_FOR_RULE` 的解除动作）：
        extraction.volume_rebuild = { enabled: true, base_name: …, members: [有序文件名] }
        （字段以 §3.2.6 为准）
```

`members` 必须是**人给出的有序文件名列表**，不是脚本按字典序猜的。`game_a/b/c` 碰巧字典序正确，但 `part2 / part10` 这种就不对，而「哪一卷在前」猜错的代价是一次必然失败的解压 + 一次误导性的 `CORRUPT`。

缺 V3 时：A 或 B 检出疑似分卷组 → 落 **`WAITING_FOR_RULE`**，报告里给出组内文件名、字节数、`除末卷外等大` 的检查结果，让人照着填 `members`。

#### 6.5.3 重建过程（**不动原文件**）

```
1  dst := D:\GameLib\_stg\<stg_id>-p\            (新建空目录，与 _stg\<stg_id>\ 同级)
2  for i, name in enumerate(members, start=1):
       Copy-Item -LiteralPath <源目录>\<name>  ->  dst\vol.{i:000}
       # **复制，不是移动、不是改名。** 原文件从头到尾没有被改名、移动或写入。
       # stem 固定用 'vol'：.NNN 之前的扩展名无所谓（★），用受限 ASCII 避免日文名再引入问题
3  验证：Invoke-SevenZip -Op l -TypeSpec '*' -Archive dst\vol.001
       exit 0  AND  Archive.Type == 'Split'  AND  Archive.Volumes == len(members)
       否则 -> **回滚**：Remove-Item -Recurse dst ; 落 WAITING_FOR_VOLUMES 或 WAITING_FOR_RULE
4  之后本 item 的所有 t / x 都对 dst\vol.001 做
5  按 §3.3 追加事件（kind 取 §3.3.2 的闭集），并写 manifest.archives[*].rebuilt_as = "vol.NNN"
   （`rebuilt_as` 字段见 §3.5）
6  attempt 结束时随 _stg\<stg_id>-p\ 一并清空
```

> **契约 VR-1：分卷重建的磁盘代价 = 整组源包再占一份。** 必须计入 §6.12.5 的余量公式（`volume_rebuild_bytes` 项），否则一个 40 GB 的分卷组会在余量检查通过之后把盘写满。

> **契约 VR-2：`D1`（卷集命名连续 + 除末卷外等大）在重建后**对重建产物**复检一次。** 重建只是改名，不会修复缺卷；`除末卷外等大` 在重建前后都必须成立。若末卷**恰好等于** `volume_size` → 高度怀疑后面还有一卷没下 → `WAITING_FOR_VOLUMES`。

---

### 6.6 逐条目校验

#### 6.6.1 exists + size 对账（D6，**必做，极廉价**）

```
expect := l -slt 的条目表里 Attributes 不含 'D' 的全部条目      # 即文件条目
for each e in expect:
    landed := 7zip_landing_path(e.Path, outDir)     # 见下：**不能**用 Path.GetFullPath/normpath
    assert Test-Path -LiteralPath landed
    assert (Get-Item -LiteralPath landed).Length -eq e.Size
```

**落地路径必须按 7-Zip 自己的规则算，不能用路径规范化函数**。实测 7-Zip 是**删除** `..` 分量而不是规范化解析：

| 归档里的 Path | 实际落地 |
|---|---|
| `../escaped-parent.txt` | `<out>/escaped-parent.txt`（`../` 被剥掉） |
| `sub/../../sneaky.txt` | `<out>/sub/sneaky.txt`（中间 `..` 被**删除**，不是解析——规范化应得 `../sneaky.txt`） |
| `/abs-path.txt` | `<out>/abs-path.txt`（前导 `/` 剥掉） |

【实测 macOS】；**Windows 上必然不同**：`\` 是路径分隔符、`C:` 是盘符，7-Zip 需要另外剥盘符 → 【待测】§11 #7（样本 `lab7z/evil/traversal.zip` 可直接拷过去）。**不做这一项的后果**：`7zip_landing_path()` 写错 → 对账在所有含 `\` 或盘符的条目上误判失败，或更糟，误判通过。

另一个 Windows 专有的落地名漂移：`-snt[-]` — *"Replace trail dots and spaces in file names for Extract operation"*。Windows 不允许文件名以点或空格结尾，7-Zip 会替换 → **落地文件名可能与归档里声明的不同**。【官方 switch 索引，未实测】对账函数必须把这条也建模，或在检出尾随点/空格条目时直接落 `WAITING_FOR_RULE`。

#### 6.6.2 CRC 全量重读：**默认关闭**

> **契约 VF-1：解压后重读全部文件算 CRC 默认关闭（`verify.full_crc = false`）。唯一强制场景是 D7：源包是 **ZipCrypto 加密的 ZIP**。**

四条理由：

1. **Windows 没有信号，也没有 `ulimit -f`。** 「工具报成功、落盘却被静默截断」这个场景是在 POSIX 下用 `ulimit -f` 造出来的（`exit 153 = 128+25 SIGXFSZ`，stderr 全空）。Windows 上磁盘写满是 `ERROR_DISK_FULL`，7-Zip 会把它当成写错误**显式报出来**并返回非 0。【仲裁裁定】
2. **`7z x` 解码时本就逐条目校验归档内 CRC。** 它校验的是归档内部的 CRC，不是已落盘文件的 CRC——两者不等价，但结合第 1 点，落盘侧的失败在 Windows 上是**会报错的**。【实测 + 仲裁裁定】
3. **10 GB 级包全量重读要几分钟**，而 B 是人在等的交互式运行（E1：主力机，会被打断）。
4. `exists + size` 对账**已经**能抓住实验里那个截断案例（`declared=20971520 actual=4194304`）。【实测】

D7 为什么是例外：ZipCrypto 的 1/256 假通过会产出**大小正确但内容是垃圾**的文件，size 对账抓不住它，只有 CRC 能。判别用 6.3.7 的字节解析（`compression method == 99` 即 AES → 不适用）。

配置项：`verify.full_crc ∈ {false(默认), true}`；`verify.full_crc_force_zipcrypto = true`（**不可关**）。

#### 6.6.3 symlink / junction / reparse point：一律拒绝（D9）

游戏资源包**正常不含**符号链接。真正的危险不在解压那一步，而在**之后的移动**：一个指向 `C:\Windows` 的 junction 会把移动操作引到目录树外面，或者把游戏目录变成系统目录的入口。

```
扫描（**不跟随链接**）：
  Get-ChildItem -LiteralPath <_stg\<stg_id>\> -Recurse -Force -ErrorAction Stop |
      Where-Object { $_.Attributes.HasFlag([IO.FileAttributes]::ReparsePoint) -or $_.LinkType }
  非空  ->  **整包判失败** -> BLOCKED_RESOURCE，不移动、不删除任何源包
移动阶段：
  必须使用不跟随链接的方式；移动前对每一层再确认一次 ReparsePoint 标志
```

落 `BLOCKED_RESOURCE` 而不是 `WAITING_FOR_RULE`：§4 的 `BLOCKED_RESOURCE` 说明已包含「产物含 reparse point、symlink、junction」。**唯一允许的人工动作是「放弃该条目（→ `ABANDONED`）」或「删除 staging 后重下」；不提供任何「放行」规则。**

7-Zip 侧的默认行为（实测 macOS）：相对逃逸链接**拒绝创建**且 exit 2（响亮）；绝对链接被**重写为解压目录内的相对路径**（安全但意外）。**Windows 上不可外推**：创建符号链接需要 `SeCreateSymbolicLinkPrivilege`（非管理员默认没有，除非开了开发者模式），而 Windows 上更该担心的是 **NTFS junction 与硬链接**——本轮完全没测。符号链接安全在 25.00 / 25.01 刚被连续加固过（CVE-2025-11001 / 11002 / 55188），低于 25.01 不要信任其防护；`-snld*` 已在 §6.2.1 硬拒。【实测 macOS + 明确不可外推；官方 history.txt】

#### 6.6.4 路径穿越自查（解压**前**）

7-Zip 确实拦截路径穿越——实测 7 种恶意路径一个都没逃出 `-o` 目录——但它**完全静默**：exit 0、零 warning、零 stderr。流水线拿不到「这个包试图穿越」的信号。【实测】

所以必须自己扫 `l -slt` 的 `Path`：

```
拒绝条件（任一命中即拒） ->  BLOCKED_RESOURCE
  - 含 ".." 分量
  - 以 "/" 或 "\" 开头
  - 匹配 ^[A-Za-z]:                       (盘符)
  - 以 "\\" 开头                          (UNC)
  - 含 NUL 或 C0/C1 控制字符
  - Symbolic Link / Hard Link 字段非空，且目标含 ".." 或为绝对路径
  - Windows 保留名（CON/PRN/AUX/NUL/COM1-9/LPT1-9，含「保留名+扩展名」如 NUL.txt）
  - 尾随点或空格
```

落点与 §5.6.5 统一为 `BLOCKED_RESOURCE`（§4 的说明已含「路径穿越」）。A 在只读预检里检出穿越时走 §4 新增的 `DOWNLOAD_STABLE → BLOCKED_RESOURCE`（执行体 A）。人工动作同 6.6.3：只允许放弃或重下，**不提供放行规则**。

> **契约 VF-2：头加密（`-mhe`）的 7z 包，路径穿越预检**只能由 B 在拿到密码后做**。**
> A 在无密码下 `l` 直接 exit 255，`Path` 一个都拿不到（§5.3.6 的 G5 分支）。所以 6.1.2 第 08 步「拿到密码后重做一次 `l -slt`」不是冗余，对头加密包它是**唯一**一次能看见内容。

#### 6.6.5 未处理嵌套残留（D8）

落地前对 `_stg\<stg_id>\` 里**每一个**文件重跑一次 §6.4.3 的白名单嗅探（`l -slt -t*` → `Type` → 白名单 → 扩展名排除表）。命中任何一个 → 说明递归没走完（通常是因为撞了 `max_depth` 或某层需要密码）→ **不落地、不删除**，落对应停机态。

**不能只看扩展名**：`mystery`（无扩展名的 tar）会被漏掉（6.4.4 实测）。

---

### 6.7 内容判定与 `EXTRACTED_NO_CONTENT`

#### 6.7.1 `content_files` 的定义（D10）

排除表（扩展名、`Thumbs.db` 等系统副产物、小图片阈值）**一律读 §3.6.3 的 `container-types.json → content_file_exclusions`**，本节不复制清单，只给判定语义：

```
content_files = { f ∈ _stg\<stg_id>\ 的全部普通文件 :
        f 不命中 content_file_exclusions 的任一条（扩展名 / 文件名 / 小图片尺寸）
    AND f 不是已被消费的中间容器
}
小图片阈值键路径：content_file_exclusions.small_image.max_bytes   默认 **524288**（512 KiB，§3.6.3）
```

`|content_files| == 0` → 这个包里没有游戏。

#### 6.7.2 那个 readme 很可能就是密码文件

实测：`nest/onlyreadme.zip` — `ONLY-A-README declared=39B ratio=1.0x entries=1`【实测】。而资源站**常把密码单独放一个包**，于是「解出来只有一个 readme」的真实含义往往是「这是密码，去开旁边那个包」。

> **契约 CT-1：`|content_files| == 0` 不得直接判失败，必须先走一轮密码回喂。**

```
if |content_files| == 0:
    1  对 _stg\<stg_id>\ 中所有说明类文件（*.txt / *.md / *.nfo / *.url / README*）跑
       §6.3.2 的 P3/P4 抽取语义（含 PW-1 的三编码解码）
    2  抽到的候选写进本 item 的候选池；事件按 §3.3.2 的 `password.attempted` 写
       （`source` = "P3"/"P4"）—— **不记密码本身**（PW-3）
    3  立即对本 item 中仍停在 WAITING_FOR_PASSWORD 的其它源包重试一轮
       （depth 不变；回喂上限 pw_refeed_max = 1 轮，防止环）
    4  若本 item 已无待解包：把候选**建议**（不是自动写入）打进报告，
       由人决定是否登记进 vault 的 (来源站点, 上传者) 条目。
       **跨 item 复用必须经人确认一次**——与 P5 同规格，因为「这个 readme 是给哪个包的」
       是一个语义判断，不是确定性规则。
    5  仍无内容  ->  EXTRACTED_NO_CONTENT，保留所有源包与 staging，不删任何文件
```

---

### 6.8 12 条删除判据 D1–D12（**本小节是 D1–D12 的唯一权威**）

> **删除不可撤销。** C3（源包与本体同目录）+ C4（解压后自动删除）意味着没有全局压缩包仓库可回退、没有回收站兜底。`Remove-Item` 在 PowerShell 里是**真删**，不进回收站。**判错一次即永久数据丢失。**
> 因此这 12 条是**与条件**，任一不满足 → **保留所有压缩包**，落对应停机态，写日志（I1）。

#### 6.8.1 判据表

| # | 判据 | 可编程检测点 | 它挡住的真实失效案例 |
|---|---|---|---|
| **D1** | 卷集命名连续，且除末卷外等大 | 枚举 `basename.NNN` / `.partN.rar` / `.rar+.rNN` / `.zNN+.zip`，要求序号无空洞；`除最后一卷外所有卷字节数完全相等`；末卷若**恰好等于** volume_size → 疑似还有一卷 | 缺中间卷 / 缺末卷 / 单文件截断三者 7-Zip 的 stderr **一字不差**（都是 `Unexpected end of archive`）【实测】。这条是**在调 7z 之前**就能抓出缺卷的廉价强校验 |
| **D2** | 每个源包**单独调用** `7z t`，`exit == 0` **严格相等** | `Invoke-SevenZip` 只接受单个 `-Archive`；调用后 `ExitCode -eq 0` | ① `7z t a.7z b.7z` 第二个参数是内部过滤器，返回 exit 0 而 `b.7z` 根本没被测【实测】 ② `exit 1` 的字面含义就是「有东西被静默跳过」（官方例子：文件被别的程序锁住）【官方+实测】 |
| **D3** | `t` 的输出无非白名单 Warnings | 解析 `WARNINGS:` 段与 `Open WARNING:` 行，逐条查 `rules\sevenzip-warnings.json`；未知 warning 默认拒绝 | 尾部追加垃圾数据 → `Warnings: 1` 但 **exit 0**【实测】。可能是拼接/夹带 |
| **D4** | `7z x` `exit == 0` 严格相等 | 同 D2 | exit 2 时磁盘上**已经有残骸**（实测：不支持的压缩方法留下 0 字节文件）【实测】 |
| **D5** | `x` 输出 `Files: N`，`N > 0`，且 `N == l -slt` 的文件条目数 | 解析 `Files: N`；与同一 `-TypeSpec` 下的 `l` 条目数比对 | exit 0 且零文件的**四种**情形：过滤器无匹配 / 多归档参数误用 / 只含空目录 / 尾部垃圾。四种全部 stdout 带 `Everything is Ok`【实测】 |
| **D6** | 逐条目 `exists + size` 对账全部通过 | §6.6.1，注意落地路径要按 7-Zip 规则算 | 「工具报成功、落盘却被截断」；实测能抓到 `declared=20971520 actual=4194304`【实测】 |
| **D7** | **仅当**源包是 ZipCrypto 加密的 ZIP 时，追加全量 CRC 重读 | 字节解析中央目录：`method != 99` 且 `bit0 == 1` → ZipCrypto | ZipCrypto 的 **1/256 假通过**会解出**大小正确但内容是垃圾**的文件，size 对账抓不住【实测】。AES（`method == 99`）有 HMAC，不受影响 |
| **D8** | 产物中不存在未处理的白名单嵌套容器（**按内容嗅探，非扩展名**） | §6.6.5 | `nest/layered.dat` 里的 `payload.bin` 其实是 ZIP，扩展名白名单漏判【实测】；`mystery.gz → mystery` 无扩展名的 tar 也会漏【实测】 |
| **D9** | 产物中不存在 symlink / junction / reparse point | §6.6.3，`-Force` 枚举 + `ReparsePoint` 标志，**不跟随** | 恶意 ZIP 里的符号链接**会被创建在 staging 里**；危险发生在之后的 move/copy 步骤【实测】 |
| **D10** | `content_files > 0` | §6.7.1 | 解出来只有一个 readme / 密码文件【实测】 |
| **D11** | 若批次声明了 `expected_parts`，实际卷数 == 声明卷数 | 与 `expected_parts` 比对（该字段由 §7 从资源页/网盘列表页写回；`null` 时 D11 真空成立，报告必须标注「无预期卷数，缺分卷不可自动检测」，见 §3.2.5） | 「A 分卷下完了、B 还没开始下」——**工具层面完全看不出来**，那个分卷根本就没下载下来。这是唯一可靠的自动检测点 |
| **D12** | staging→落地移动成功且体积一致；manifest / state 已 `Flush($true)` 落盘 | 移动后对 `games\<dirname>\` 的目标子树做递归体积求和，与移动前的 `_stg\<stg_id>\` 体积比对；然后写盘 + `Flush($true)` | 移动本身失败（同名冲突、权限、路径超长）；以及「删了源包但状态没落盘 → 重跑时以为还没解压」 |

#### 6.8.2 删除前必须写 SHA-256

> **契约 DEL-1：删除任何源包之前，必须把它的 SHA-256 写进 `manifest.json` 并 `Flush($true)`。**
> 成本近似为零（源包本来就要读一遍），收益是：**误删之后，人能确认「重新下载的是不是同一个包」**。
> 字段以 §3.5 为准：`manifest.archives[] = { name, sha256, bytes, volume_group, rebuilt_as, … }`（**`bytes` 不是 `size`**）。

#### 6.8.3 删除的执行顺序（I3 的实现）

```
15  D1..D11 全部成立？ 否 -> 落停机态，返回
16  同卷 move  _stg\<stg_id>\*  ->  games\<dirname>\                       (D12 前半)
    移动后体积对账；不一致 -> 回滚不可能（已移动），落 CORRUPT 并**保留所有源包**
    追加事件 staging.promoted
17  写 manifest.archives[*].sha256（字段名见 §3.5）
    追加事件 archive.verified（§3.3.2；payload: archive / sha256 / bytes / consumed_by）
        -Compress，整行一次 Write + Flush($true)
    重建 state.json: archives[i].verified = true ; archives[i].consumed_by = <attempt_id>
    写临时 -> [System.IO.File]::Replace(tmp, state.json, state.json.bak)
             # 保留 backup，不传 $null。**`.bak` 只作诊断/取证，绝不作恢复源**（§4.5.2）
                                                                          (D12 后半)
18  ==> EXTRACTED
19  **重新从磁盘读 state.json**（不用内存里的对象）
    for each archive a:
        if a.verified -eq $true AND a.consumed_by -eq <attempt_id> AND a.deleted_at -eq $null:
            $p = Join-Path <游戏根> a.name        # **§3.4.1 只有相对 name，没有 path 字段；
                                                 #   绝不自拼绝对路径（会破坏 I3）**
            if (Get-Item -LiteralPath $p).Length -ge retention.trash_max_bytes:
                    Remove-Item -LiteralPath $p -Force                  # 大包：直接真删
            else:
                    Move-Item  -LiteralPath $p -Destination
                               games\<dirname>\.gameflow\trash\         # 小包：同卷移动，O(1)
            追加事件 archive.deleted（payload: archive / sha256 / bytes）
            回写 archives[i].deleted_at
        else: 跳过
```

第 19 步的「从磁盘重读」是 **I3** 的实现方式，也是幂等的来源：B 被打断后重跑，`verified` 已经是 `true`，第 19 步直接把剩下的删完，**不会重新解压一遍**。

> **契约 DEL-2：删除阶段**不得**重新计算任何判据。** 它只读 `state.json` 里已落盘的 `verified` + `consumed_by`。现场重判会破坏幂等（例如源包已被删掉一半时重跑，`7z t` 必然失败，于是剩下一半永远删不掉）。

#### 6.8.4 两段式删除：尺寸门控（**仍列入 §12 待用户拍板**）

| 项 | 值 |
|---|---|
| 开关 | `retention.trash_days`、`retention.trash_max_bytes`（默认值登记在 §3.2.3） |
| **默认** | `trash_days = 3`、`trash_max_bytes = 2 GiB` |
| 语义 | 源包字节数 **< `trash_max_bytes`** → 先移入 `games\<dirname>\.gameflow\trash\`，`trash_days` 后由 **B** 在下一次运行开始时清理；**≥ 阈值** → 验证通过后直接真删 |
| 为什么按尺寸分段 | 误判概率最高的恰恰是**小包**：纯文本汉化补丁既是压缩比 133x 触 `ratio_warn` 的那一类，也最容易命中 `EXTRACTED_NO_CONTENT` 边界；而磁盘冲突只在大包成立。同卷移动是元数据操作，成本接近零，磁盘代价有硬上限（`trash_max_bytes × 待清理条目数`，并已计入 §6.12.5 的 `trash_pending_bytes`） |
| 与 C4 的关系 | **C4 仍满足**：源包在游戏目录里自动消失、无需任何人工动作，只是**小包的物理删除时刻推迟** `trash_days` 天。设 `trash_days = 0` 即回到字面 C4 |
| 落地位置 | `games\<dirname>\.gameflow\trash\`（与游戏库**同卷**、随条目走；条目被 `ABANDONED` 清理时一并消失，不需要库级 `_trash\` 目录） |
| **代价（必须在报告里显式签收）** | **误删无法恢复**，只能靠 `manifest.json` 的 SHA-256 确认重下的是不是同一个包，而**重下不保证还能下到**（资源站失效、网盘和谐）。两段式只把这个窗口推迟 `trash_days` 天，不消除它 |

---

### 6.9 Defender 预检

#### 6.9.1 位置与归属

- **归属 B**：`MpCmdRun` 官方明写需提权，与 `-RunLevel Limited` 的无人值守计划任务冲突。【官方 MS Learn】
- **时点**：`VERIFYING` 之内、`staging.promoted`（落地）**之前**（6.1.2 第 16 步）。**§4 已按此改**（见 6.1.2 的对齐点）。
- **对象**：`D:\GameLib\_stg\<stg_id>\` 整树。归档本身也可以扫（官方：`-DisableRemediation` 模式下 *"Archive files are scanned"*），但主判据是对解压产物扫。

#### 6.9.2 二选一：**选 `-DisableRemediation`**

官方对 `-DisableRemediation`（仅对 custom scan 有效）的完整语义：

> *"File exclusions are ignored. Archive files are scanned. **Actions aren't applied after detection. Event log entries aren't written after detection. Detections from the custom scan aren't displayed in the user interface. Detections from the custom scan are displayed in the command output.**"*
> 【官方 https://learn.microsoft.com/en-us/defender-endpoint/command-line-arguments-microsoft-defender-antivirus】

| 方案 | 优点 | 代价 |
|---|---|---|
| **A（本 SPEC 采用）：用 `-DisableRemediation`** | ① Defender **不会**在我们不知情的时候删掉游戏文件 ② **忽略排除目录** → 我们对 `_stg` 的排除不会让预检失明，这正是「只排 `_stg` + 显式预检」这套组合成立的关键 | **事件日志与 `Get-MpThreatDetection` 都看不到这次检出** → **stdout 是唯一信号**，而 stdout 格式是【待测】 |
| B（备选）：不用它 | 可以用 `Get-MpThreatDetection` 结构化查询 | Defender 会**真的动文件**——在 7-Zip 还在写的时候隔离/删掉解出来的文件，7-Zip 那边表现为「拒绝访问 / 找不到文件 / CRC 失败」等误导性错误【社区，长期用户报告模式】，并且会与「验证通过才删源包」直接打架，造成假失败 |

> ⚠ 仲裁已指出：`windows-download` 原稿里 `$before = (Get-MpThreatDetection).Count` + `-DisableRemediation` 的写法**自相矛盾**——该模式下这个计数永远不会变。**不要照抄那段代码。**

#### 6.9.3 把【待测】变成运行期可检测的条件

`-DisableRemediation` 的 stdout 格式（威胁名怎么打印、多威胁怎么分行）是【待测】§11 #6。**不实测就写不出解析器。** 但我们可以让「解析器是否可用」变成一个**每次运行都会被检查**的事实，而不是一个假设：

```
解析器配置：rules\defender-stdout.json（已登记进 §2.4 布局树与 §3.6 的 schema）
    { "threat_line_rx": "<正则，捕获组 name/path>",
      "no_threat_markers": [ "…" ],
      "probe_expect_threat": "EICAR_Test_File"        # 自检期望
    }

Preflight 自检（§9.11.2）：
    对一个 EICAR 测试样本跑一次 MpCmdRun -Scan -ScanType 3 -DisableRemediation -File <eicar>
    解析器必须恰好解出 1 条 threat，且 name 命中 probe_expect_threat
    自检不过  ->  B **拒绝进入 Defender 阶段**，该 item 直接落 THREAT_SUSPECTED（C 级）
                  **不静默放行**
```

#### 6.9.4 三级分流

**不要「扫到就停」。** 游戏破解补丁、汉化补丁、注册机、各种引擎的加密 DLL 与 SFX 被判为 `HackTool:*` / `Trojan:Win32/Wacatac.*` / `PUA:Win32/*` 是**高频事件**，且绝大多数是误报。【社区，业内共识级】「扫到就停」会让流水线在正常素材上频繁卡死；「扫到就忽略」会让真实恶意样本静默落地。

| 级 | 判据 | 状态 | 处置 |
|---|---|---|---|
| **A 级：明确恶意** | 威胁名前缀命中 `Ransom:` / `Backdoor:` / `Worm:` / `TrojanDownloader:` / `Trojan:Script/`，或严重级别 Severe/High | **`THREAT_CONFIRMED`** | **硬停**。不落地到游戏根目录、**不删任何源包**（留证）、staging 原样保留待人处置。报告给出：文件相对路径 + 威胁名 + SHA-256 + 大小 |
| **B 级：疑似误报** | 威胁名含 `HackTool:` / `PUA:` / `Crack` / `KeyGen` / `Wacatac` / `Zpevdo` / `Presenoker`，**或**命中文件位于 `crack/ 补丁/ 汉化/ patch/` 等目录、或是补丁型 `.dll`/`.exe` | **`THREAT_SUSPECTED`** | **软停**。产物**留在 `_stg`，不落地**；不删源包。等人一次性裁决（放行 / 丢弃） |
| **C 级：扫描本身失败** | `ExitCode == 2` 但 stdout 无威胁名（官方明写「Scanning errors」也返回 2）、`MpCmdRun` 不可用、或 6.9.3 的解析器自检未过 | **按 B 级处理** → `THREAT_SUSPECTED` | **不静默放行** |

「不删源包留证」之所以成立，正是因为删除阶段（6.1.2 第 21 步）排在 Defender 之后——这是把 Defender 钉在落地前的直接收益。

⚠ **返回码 0 有歧义**：官方原文「No malware found」与「Malware found and successfully remediated」**都是 0**。所以**不能只看 exit code**——这也是选方案 A 的另一个理由（`-DisableRemediation` 下不会发生「已修复」这种情况，0 就是干净）。

人工放行后写 `rules\threat-allowlist.json`（**按 SHA-256，不按路径**；阶段一产物的哈希是固定的）。该文件已登记进 §2.4 与 §3.6，并在 §3.8 的「谁写谁读」矩阵里标明它是**唯一由 B 在人工放行后追加**的规则文件。

#### 6.9.5 排除项

排除项清单、误报应急（`-Restore`）与回滚脚本的完整规定在 **§9.8.4**，本节只复述对 B 有直接约束的三条：

```
只排 D:\GameLib\_stg\ 这一个目录（避免实时保护在解压中途隔离/锁住文件）
games\ 与整个 D:\GameLib：**绝不排除**（库里装的正是最可能带毒的东西）
```

- B 每次进入 Defender 阶段前用 `MpCmdRun.exe -CheckExclusion -Path <路径>` 确认排除生效（输出 "is excluded. Exit code is 0."）。【官方】
- **人工放行按 SHA-256 的适用边界**：只对**阶段一的归档与解压产物**成立。**MTool 的 `version.dll` 是每次注入现生成的，哈希随游戏实例变化**（Kaspersky 版主确认「逐个加排除项无效」）→ SHA-256 白名单在原理上就不可能工作，**只能目录级排除（MTool 安装目录）**。这一条留给 **§7.6.11** 呼应，**不要**把它混进本节的 SHA-256 白名单。【官方/一手论坛 + 仲裁裁定】

【待测】§11 #13：受限权限计划任务下 Defender 预检的失败形态——要确认它是**干净报错**而不是静默跳过。§11 #12：实时保护是否会在解压中途隔离/锁住文件（会与 D2/D4 打架，造成假失败）。

---

### 6.10 MOTW（Mark of the Web）

#### 6.10.1 策略：默认传播 + 验收后 `Unblock-File`

```
解压：  Invoke-SevenZip -Op x …  **固定附带 -snz**（成本为零）
验收：  §6.8 落地 + §6.9 放行之后，对 games\<dirname>\ 整树一次性解除：
        Get-ChildItem -LiteralPath $GameRoot -Recurse -File -Force |
            Unblock-File -ErrorAction SilentlyContinue
        然后写事件 motw.cleared（§3.3.2）
```

`Unblock-File` 的官方保证：*"removes the **Zone.Identifier** alternate data stream"*、*"…on a file that is not blocked, the command **has no effect** … does not generate errors"*、*"introduced in Windows PowerShell 3.0"*（⇒ 5.1 可用）。【官方 Microsoft Learn · Unblock-File】即：**幂等、对未标记文件无副作用**，可以放心对整树跑。

【待测】§11（`Unblock-File` 是否会改 `LastWriteTime`）：若会，`manifest.stage1_snapshot` 的采样时点必须排在 `Unblock-File` **之前**，否则 §7 的差集判据会把整棵树都算成「阶段二新增」。

#### 6.10.2 「解除标记」是 §7 的**前置条件**，不是可选收尾

这是采纳「传播 + 统一解除」这条路线的**代价**，必须显式写死：

> **契约 MOTW-1：`SCANNED` 状态的定义之一是「`games\<dirname>\` 整树已 `Unblock-File` 完毕」**（另两项：Defender 裁决已落盘为放行、`manifest.stage1_snapshot` 已写）。
> **契约 MOTW-2：§7 的 preflight 必须复检**——`Get-ChildItem -Recurse -File | Get-Item -Stream Zone.Identifier -ErrorAction SilentlyContinue` **零命中**才允许启动 Computer Use。复检失败即拒绝启动。

为什么这么硬：带 `ZoneId=3` 的 exe 首次运行会弹 SmartScreen 的 *"Windows protected your PC"* 对话框，而**那是一个前台模态框**。Computer Use 在 Windows 上独占前台、无法与用户共用会话——**忘了解除标记就会让 §7 撞上模态框卡死**。【官方（SmartScreen 触发条件）+ 官方（Computer Use 前台独占）+ 仲裁裁定】

#### 6.10.3 `-snz` 的真实状态

| 命题 | 裁定 |
|---|---|
| `-snz` 存在，22.00（2022-06-15）引入，Windows-only | **【官方】** history.txt：*"New -snz switch to propagate Zone.Identifier stream to extracted files (Windows)"*；随附手册 switch 索引有条目 |
| CLI 默认**不**传播 | **【社区】+【待测】**。官方 changelog、switch 索引、以及独立 switch 页全都**没有一句话写默认值**（我复核过随附手册：`-snz` **没有独立的 `snz.htm` 页**）。网上「default is No」全部来自 BleepingComputer / SANS 等二手报道 → §11 #16 |
| 注册表 `HKCU\SOFTWARE\7-Zip\Options\WriteZoneIdExtract` 影响 CLI | **无任何证据。** 它是 File Manager（GUI）的选项存储位置。**不得写进 SPEC，不得依赖** |
| 24.09 修的 MOTW bug 是否影响本项目 | **不影响。** 官方原文的主语是 **7-Zip File Manager**，场景是「在 GUI 里打开的归档里再打开一个归档」。本项目是 CLI 逐层解到磁盘，**不落在这个 bug 的范围内**【官方原文复核 + 仲裁裁定】 |

因为默认值未知，**显式传 `-snz`** 是零成本的确定性做法：如果默认本来就开，多传一次无害；如果默认是关，我们拿到了想要的行为。

#### 6.10.4 MOTW **不得**作为安全边界

完整声明在 **§9.4**，本节只给 7-Zip 侧的三条成因：① CLI 默认值**未知**（上表）；② `-snz` 传播的是**归档自身的** Zone.Identifier，下载器没给归档打标记就什么都传不下去；③ MOTW 只在 **NTFS** 上存在（官方：*"…only applies to files saved on an NTFS file system, not files saved to FAT32 formatted devices."*）→ **库根若落在 exFAT 移动硬盘上，整个 MOTW 话题自动消失**（§9.11.2 的 Preflight 必须探测卷的文件系统并记进 `env.snapshot`）。另外这是个反复出问题的区域（24.09 的 CVE-2025-0411、26.03 的 CVE-2026-58052），且 CLI 与 GUI 行为不一致。【官方 + 官方 history.txt】

> **真正的护栏是 §6.9 的显式 Defender 预检**，它是主动的、不依赖 7-Zip 正确性的。MOTW 只是「中间态里多一层提示」。
> 顺带一条澄清：**7-Zip 的版本下限不以 MOTW 为理由**（既然不把它当安全边界，用它当理由就自相矛盾）。硬下限 **25.01**、建议 **≥ 26.02**，理由是**解析不可信归档的内存安全漏洞**——完整论证与准入矩阵见 **§9.9.3 / §9.11.4**。落后时在报告顶部长期提示，**但不拒绝运行**。

---

### 6.11 文件名编码修复

#### 6.11.1 范围：只有 ZIP（和少量 TAR）

| 格式 | 文件名编码 | 有乱码风险吗 |
|---|---|---|
| **7z** | 头里**强制 UTF-16LE** | ❌ 格式层面就是 Unicode |
| **RAR5** | 强制 UTF-8 | ❌ |
| RAR4 | 同时存 OEM 名和 Unicode 名（存在时优先） | ⚠ 极少数只有 OEM 名的老包 |
| **ZIP** | general purpose **bit 11** 决定，默认**不是** Unicode | ✅ **重灾区** |
| TAR | 无规范，纯字节流 | ⚠ 游戏资源少见 |

【实测（构造 bit11=0 的 CP932 包与 bit11=1 的 UTF-8 包对照，`l` 输出一对一正确/乱码）+ 格式规范】

> **所以编码逻辑只覆盖 ZIP 一条路径，不做通用机制。** 这符合「不要为了流程而流程」。

判定归 **§5.8**（纯字节解析中央目录的 bit 11，只读，不依赖任何 7-Zip 开关）：`bit11=1` 安全 / `bit11=0 且全 ASCII` 安全 / `bit11=0 且含 ≥0x80 字节` → **`FILENAME_ENCODING_RISK`** 停人工。
**B 只负责处置**，且只在批次条目显式声明了代码页规则之后才动手：`extraction.filename_codepage = 932 | 936 | …`。

#### 6.11.2 `-mcp=932` 是【待测】候选，**不是主路径**

三方证据全部不支持把它当主路径：

- **官方**：`method.htm` 结尾明写 `-m` 的适用命令是 **a (Add), h (Hash), d (Delete), rn (Rename), u (Update)** —— `x` / `e` / `t` / `l` **不在列表里**。`-m` 官方就是个「创建/更新归档」的开关族。【官方，随附手册一手】
- **实测（macOS 25.01）**：`-mcp=932` / `-mcp=65001` / `-sccUTF-8` 对 `l` 全部零效果；连 `-mcp=99999` 这种非法值都不报错 → 说明**根本没被消费**。【实测】
- **社区**：说法自相矛盾（有人说只对 `x` 生效不对 `l` 生效）。【社区】

顺带澄清一个常被误引的开关：`-scs` = *Set charset for **list files***（`@listfile.txt` 的编码，适用命令只有 `a`/`u`），**与归档内文件名解码无关**。（`-scc` 的去留见 §6.2.1，全 SPEC 只在那里规定一次。）【官方】

> **契约 FN-1：`-mcp*` 默认在 `Invoke-SevenZip` 里被硬拒。**
> 只有在 §11 #20 在目标机实测通过（样本 `lab7z/sjis/sjis-legacy.zip` 可直接拷过去）之后，才允许由配置 `extraction.mcp_verified = true` 打开。
> **这是一条「实测能实质简化设计」的分支**：若 Windows 上真有效，6.11.3 那套事后重命名就可以整块删掉。

#### 6.11.3 主路径：事后批量重命名（必须 dry-run + 人工确认）

```
1  按 7-Zip 默认行为解到 _stg\<stg_id>\
2  对每个落地文件名，反解回**原始字节**：
       macOS 25.01 的行为是 U+EF00 + 原字节 的私有区转义【实测，反解 100% 成功】
       **Windows 上大概率不同**（更可能直接用 ACP 解码，根本不走转义路径）→ 【待测】§11 #8
3  用批次声明的 filename_codepage 把原始字节解成正确名字
4  产出 rename_plan = [ {from, to, decoded_by, ok:bool} … ]
5  **dry_run 默认 true**：rename_plan 整表按 §3.3 写进 events.jsonl，
   由 §5.9 的对账报告呈现（报告文件只由 A / Get-BatchReport.ps1 新建，B 不写报告文件）
6  人工确认后，以 rename.apply = true 重跑同一 attempt：
       逐条 Rename-Item -LiteralPath
       目标已存在  ->  **整个计划中止，零改动**
7  只在 _stg 内做；落地之后**不再改名**
```

可逆性的量化边界（决定「能不能事后补救」）：

| 目标 ACP | CP932 双字节字符往返可还原 | 结论 |
|---|---|---|
| **GBK (936，简中)** | 9236 / 9604 = **96.17%**（丢失 368 个，主要是 F8xx 私用区/外字） | 事后修复**基本可行**，但那 3.83% 必须在 rename_plan 里标 `ok:false` 并**原样保留**，不能瞎改 |
| **Big5 (950，繁中)** | 3468 / 9604 = **36.11%** | **基本不可修复**（Windows 会把无法映射的字节替换成 `?`，信息永久丢失）→ 直接落 `FILENAME_ENCODING_RISK` 不做尝试 |

【实测（码表换算），**可外推**——这是 Unicode 码表的性质，与 OS 无关】

乱码指纹（**只作为报告里给人看的提示，不作为自动判据**）：CP932→GBK 的乱码里 `偟 偄 僼 僅 儖 僟 僉 儍 儔 乕 僞 僨 惡 壒 慺 嵽` 这类几乎不出现在正常中文里的罕见汉字会密集出现。【实测（逐字符换算表）】

#### 6.11.4 其它路径（都不是 B 的主路径，但要写明代价）

| 路径 | 状态 | 代价 / 风险 |
|---|---|---|
| 换工具（Bandizip / WinRAR 的「编码/代码页」选项手动指定日文） | 【社区】 | §9.11 的探测可能发现这台机器上**本来就只有** WinRAR/Bandizip（E3 没指明装的是哪个）→ 这条会从备选变成唯一选择，届时整套 7-Zip 调用约定要重写 |
| **运行期转区工具**（跟解压无关，是「能不能玩」的事，归 §7.7 的人工接力点） | 【社区】 | **AppLocale 微软已弃、Win10/11 不可用 —— 不要写进方案。** **Locale Emulator**（xupefei）原作者 2022-04 停止开发，社区有 fork，对**半角日文假名**有已知缺陷。**Ntleas / NTLEA** 社区评价对半角假名更好，是当前 Galgame 圈主流 |
| 改系统「非 Unicode 程序的语言」为日语 | 【社区】 | **全局生效 + 需重启**；社区报告对 RPG Maker 类比转区工具更可靠。**但代价必须写明：这会改变系统 ACP**，从而使 §6.3 里已登记的全部中文密码等价展开**失效**。改动后 `env.acp` 会变，报告必须在顶部提示「ACP 已从 X 变为 Y，此前登记的中文密码可能全部需要重新展开」 |

---

### 6.12 护栏（**本小节是护栏阈值语义的权威；默认值一律登记在 §3.2.3 的 `guards`**）

> 本节只写「每个阈值是什么意思、怎么求值、触线落哪个状态」。**所有数值、键名、取值域以 §3.2.3 为准**，正文不再重复写死默认值。

#### 6.12.1 深度

`extraction.max_depth`，计数口径见 §6.4.2。触顶 → **`BLOCKED_RESOURCE`**，报告里画出已展开的层次树（哪一层是什么 `Type`、多大、几个条目），让人一眼判断是「资源站规则比预期深」还是「递归跑飞了」。

#### 6.12.2 压缩比：`ratio` **绝不单独阻断**

```
expansion = sum(所有条目的 Size) / 归档级的 Physical Size
```

> ⚠ **不能用 `sum(Packed Size)`。** 7z 默认 solid，一个 block 里只有**第一个**条目带 `Packed Size`，其余为空。字段名就是 `Size`（解压后大小）与 `Packed Size`（压缩后大小），**不存在** `Uncompressed Size` / `Unpacked Size` 这种字段。【实测，可外推——字段名来自跨平台公共输出层】

| 条件 | 动作 |
|---|---|
| `expansion >= guards.ratio_warn` | **仅记录**（写 `state.guards.expansion_ratio` + 报告标注），**不阻断** |
| `expansion >= guards.ratio_block` **AND** `declared_total > guards.ratio_block_min_declared_bytes`（5 GiB） | 阻断 → `BLOCKED_RESOURCE` |

**为什么 ratio 是个很差的判别器（实测数据支撑）：**

| 载荷 | 算法 | ratio | 性质 |
|---|---|---|---|
| 纯文本 / JSON 汉化补丁 2.4 MB | lzma preset9e | **133x** | **合法** |
| 静音 WAV（VN 语音包里极常见的填充） | deflate-9 / lzma | **959x / 5088x** | **合法** |
| 真实语音 16bit PCM WAV | deflate-9 | 1.14x | 合法 |
| 混合「真实游戏」目录整体 | deflate-9 | 2.66x | 合法 |
| **200 MB 全零（真炸弹）** | deflate / lzma | **1029x / 6844x** | 恶意 |

【全部实测（本机构造载荷 + 标准算法），可外推——这是算法性质，与 OS 无关】

一个**完全正常**的纯文本汉化补丁 7z 就有 133x，已经超过常见的 100x 经验阈值；而真炸弹只有 1029x。**中间没有干净的分界线。** 这就是 `ratio` 只能作为「与绝对体积的 AND 条件」的原因。

（这条阈值的一个已知误伤，见本节末尾的遗留异议。）

#### 6.12.3 头部声明**会撒谎** → 运行期监控才是真护栏

实测：手工构造一个 ZIP，本地头与中央目录都声明 `uncompressed = 1024`，实际 deflate 流解出 **20 MB**；`unzip -t` 还**通过了**；而按声明值算出来的「压缩比」是 **0.1x** —— **撒谎的头部让它看起来比正常包还安全**。【实测（Info-ZIP/Python 造包，机理可外推；rc 数字不可当判据）】

> **契约 GD-1：运行期 staging 体积监控是唯一挡得住「撒谎头部」的东西，且对头加密包是**唯一**的护栏。**

```
解压开始前：free0 := 库所在卷的可用字节（GetDiskFreeSpaceEx，O(1)）
每 guards.poll_sec 秒：
    consumed := free0 - free_now                       # O(1)，但会把别的进程的写算进来
    if consumed > cap:
        精确复核：Get-ChildItem -Recurse _stg\<stg_id>\ | Measure Sum Length   # O(n)，只在触线时做一次
        if 精确值 > cap:  Kill($true) -> 清空 _stg\<stg_id>\ -> BLOCKED_RESOURCE
cap := max( declared_total × guards.runtime_factor , guards.abs_cap )
```

用「卷可用空间差值」做主轮询、只在触线时才做一次 O(n) 精确复核，是为了避免每几秒对一个几十 GB 的树做递归 `Get-ChildItem`。误差方向是**高估**（别的进程也在写）→ 后果只是多做一次精确复核，**永远不会**误删。

#### 6.12.4 绝对上限（比 ratio 可靠得多）

`guards.max_entries_per_archive`、`guards.max_total_uncompressed_bytes`（单个游戏条目）、`guards.max_single_file_bytes`，任一超限 → `BLOCKED_RESOURCE`。
【取值来自 archive-reality 的经验建议，是**可配置参数**、非实测事实；默认值见 §3.2.3】

#### 6.12.5 磁盘余量公式

```
required = declared_total × guards.space_safety_factor
         + guards.largest_single_file          # 本次待解包里最大的单个条目
         + volume_rebuild_bytes                # §6.5 启用时 = 该分卷组源包总字节；否则 0
         + trash_pending_bytes                 # .gameflow\trash\ 里未过期的总量（§6.8.4）
         + guards.min_free_headroom_bytes      # 默认 2 GiB（§3.2.3）

free_on_volume(库所在卷) - required < 0   ->   BLOCKED_RESOURCE
```

`space_safety_factor` / `largest_single_file` / `volume_rebuild_bytes` / `trash_pending_bytes` / `min_free_headroom_bytes` 全部登记在 §3.2.3 的 `guards`。

> **`_stg` 与 `games\` 同卷是这个公式成立的前提。** 同卷 move 是 O(1) 元数据操作，staging 与落地**占的是同一块空间**——所以**不需要**为「staging 一份 + 落地一份」预留两倍。这是 §2.6 那条同卷约束的额外好处，值得在实现时不要写反。

**头加密（`-mhe`）包的特例：**

```
declared_total 不可得（无密码时 l 直接 exit 255，Size/Path/条目数一个都拿不到）
        ->  粗筛用 declared_total := 归档物理字节 × guards.hdr_enc_factor
        ->  真正的护栏是 6.12.3 的运行期监控 + 6.12.4 的绝对上限
        ->  条目预算、路径穿越预检在拿到密码之前**全部失效**（§6.6.4 的契约 VF-2）
```
`hdr_enc_factor` 是**参数，不是事实断言**。【官方 + 实测：头加密包无密码 `l` 必然 exit 255】

---

### 6.13 不进自动流程的形态（**本小节是这一判定的唯一权威；§5.4.4 引用本小节**）

```
执行体 A / B 自动处理：zip / rar / 7z / tar(.gz|.bz2|.xz) / cab / 分卷 / SFX（**当归档解，绝不执行**）
停下来给人：NSIS 或 Inno 安装器 / setup.exe / .iso 等镜像 / 未知 MZ
        ->  WAITING_FOR_MANUAL_INSTALL
```

`container-types.json` 的 `manual_install_types` 是本判定的配置面（schema 见 §3.6.3）；**魔数表（含偏移）见 §5.4.4，本节不复制**。

#### 6.13.1 判别方法（**有序，先匹配先赢**）

先做 §5.5.2 的**分卷归组**，组内只把「首卷」送去判别——否则中间卷的魔数是数据碎片会被乱认（实测：`file` 把分卷 ZIP 的第 4 卷认成「DOS executable (COM)」）。【实测】若写成「凡嗅探出可执行文件就报警」，这类误报会把流程卡死。

```
R1  l -slt 的 Archive.Type == 'Nsis'              ->  WAITING_FOR_MANUAL_INSTALL
R2  Archive.Type ∈ {Iso, Udf}  或  offset 32769 命中 §5.4.4 的 ISO9660 签名
                                                  ->  WAITING_FOR_MANUAL_INSTALL
R3  offset 0 命中 §5.4.4 的 MZ 签名：
      R3a  Archive.Offset > 0  AND  Archive.Type ∈ recurse_into_types
                 ->  **SFX**：当归档解，depth+1，**绝不执行它**
      R3b  文件内含 "Inno Setup" 特征串
                 ->  WAITING_FOR_MANUAL_INSTALL
      R3c  Archive.Type == 'PE'（或 l exit 2）
                 ->  **未知 MZ**  ->  WAITING_FOR_MANUAL_INSTALL（保守）
R4  解压产物中出现 setup.exe / Setup.exe / インストール.exe
      AND 同层无引擎标记文件（引擎标记表见 §7.6.3 的 engine-rules.json）
                                                  ->  WAITING_FOR_MANUAL_INSTALL
```

#### 6.13.2 SFX：当归档解，**绝不执行**

实测（macOS，16334 字节 stub + 真 7z 负载）：

```
$ 7zz l -slt selfext.exe
Open WARNING: Cannot open the file as [PE] archive     <-- 良性，进 warning 白名单
Type = 7z
Offset = 16334                                         <-- stub 长度，SFX 的确定性标志 ★
$ 7zz t selfext.exe    -> exit 0
$ 7zz x selfext.exe    -> exit 0，4 个文件全部正确解出
```

两条结论：SFX **不需要改后缀**；**`Offset > 0` 是「这是 SFX / 带前缀容器」的确定性判据**，比看扩展名可靠。

> ⚠ **【待测】§11 #10（本节优先级最高的一条实测）**：我用的是**人造 stub**（不是合法 PE），所以走了 fallback 路径。**在 Windows 上真 SFX 就是合法 PE，PE handler 可能抢先成功打开**，此时 `Type` 会是 `PE` 而不是内层归档类型，7-Zip 会去解 PE 的资源节而不是尾随负载。
> **不做这一项的后果**：R3c 会把**每一个真 SFX** 都误停在 `WAITING_FOR_MANUAL_INSTALL`。资源站分发 SFX 不罕见，这会是一个高频的假阳性。
> 在实测结果出来之前，R3c 的保守选择是正确的取舍：**误停不会丢数据，误执行会**。

#### 6.13.3 为什么 NSIS / Inno / setup.exe 不硬解

7-Zip 对 NSIS 的支持不完整（安装器被压缩或改动过时需要专用 NSIS 提取器）【社区】；更重要的是这类包通常需要写注册表、装运行库，**硬解出来的目录未必能跑**。`setup.exe` 还可能需要选安装路径、可能捆绑、可能需要管理员权限。

`.iso` 虽然 `Mount-DiskImage` 是官方 cmdlet，但挂载会占用盘符、老游戏可能有 DRM 需要光盘在位、卸载失败会留下幽灵盘符 → 归 `WAITING_FOR_MANUAL_INSTALL`，由人在 §7 处理。【官方 cmdlet 存在 + 社区风险】

---

> ### ⚠️ 遗留异议（起草者对某条锁定决策的保留意见，主控未采纳）
>
> **异议 1（对 `ratio_block` 这条锁定阈值的**论证**，不是对阈值本身）。**
> 锁定决策给出的论证是「合法纯文本汉化补丁 133x vs 真炸弹 1029x，中间没有干净分界」。这个论证低估了重叠程度：本轮**同一份实测数据**里，**数字静音 WAV 用 LZMA 压到 5088x**，而静音填充在 VN 语音包里极常见，这类语音包又常常 > 5 GiB —— 也就是说，`ratio >= ratio_block AND declared_total > 5 GiB` 这条与条件**会在一个完全合法的 VN 语音包上同时成立**。
> 我照契约实现了阈值，并在 §6.12.2 保留了论证，但建议 §12 的决策台账把这条已知误伤记下来，并接受本节给出的缓解：该阻断落 **`BLOCKED_RESOURCE`（停机态，I1 保证不删任何文件）**，人看一眼后可在批次条目里声明一次性放行。误伤的代价因此是「停一下」而不是「丢数据」，符合用户「宁可多一点误报也要换取可控与可解释」的品味。
>
> **异议 3（`-DisableRemediation` 的选择建立在一条未实测的格式上）。**
> 锁定决策要求「`-DisableRemediation` 二选一必须显式选」，本节选了 `-DisableRemediation`，理由充分（它忽略排除目录，是「只排 `_stg` + 显式预检」这套组合成立的关键）。但这条选择的**全部可行性**押在一个【待测】项上——该模式下 stdout 是唯一信号，而 stdout 格式没人验证过。
> 我在 §6.9.3 用「Preflight 拿 EICAR 跑一次解析器自检，不过就落 `THREAT_SUSPECTED` 而不是静默放行」把这个未知变成了运行期可检测的条件，所以设计不会悄悄失效。但建议 §11 把 #6 的优先级提到**与 #1（环境快照）同级**：它不是「锦上添花」，它是本节唯一一条「不实测就写不出代码」的依赖。


---

## §7 执行体 C：前台（会话模型、三类网盘适配、MTool、人工接力点清单）

执行体 C 是整条流水线里**唯一会占用鼠标、键盘和屏幕**的部分。它对应用户约束 C1 的「阶段二」，也是环境事实 **E1**（Windows 是主力机、C 跑的时候用户没法用这台电脑）的全部代价所在。

本节的每一条设计都从同一个前提出发：**C 消耗的不是 CPU，是用户的时间**。因此 C 不追求吞吐，只追求「随时可停、停了不亏、下次接着跑」。所有能挪到 A（§5）和 B（§6）的工作，一律不放进 C。

### 7.0 下载这一步不归 C：主路径是人工排队（用户裁定，2026-09-14）

这条决定了本节的实际使用频率，必须放在最前面。

**`baidu` 与 `quark-ali` 两类网盘的「点下载」由人做，不由 Computer Use 做。** 具体形态：

```
你（一次性，约两分钟）                        A（无人值守，之后全自动）
──────────────────────────                 ──────────────────────────
把客户端下载目录设成 <root>\_inbox           轮询 _inbox
  （只设一次，不是每个游戏一次）              ↓
把本批次的游戏一次性排进下载队列               G1–G6 判定下载完成（§5.3）
  （已登录，转存 + 加入队列）                 ↓
—— 到此为止，你可以去干别的 ——                认领归位到 games\<dirname>\（§5.2）
                                            ↓
                                           B 接手解压、校验、删源包（§6）
```

**为什么是这样，而不是让 C 去点**：

| 理由 | 说明 |
|---|---|
| **E1 是硬约束** | C 跑的时候你用不了这台主力机。让 C 去客户端里点十个下载，就是拿你几十分钟的机器占用，换你两分钟的手动操作——**这笔交易在任何方向上都不划算** |
| **可靠性差一个量级** | 客户端界面会变、会弹限速提示、会要验证码、会因登录态过期跳登录页。这些对人是零成本的干扰，对 Computer Use 是随时可能卡住的失败点 |
| **SPEC 早已完整设计了这条路** | `_inbox` 统一落地 + G1–G6 完成判定 + 认领归位（§5.2、§5.3）本来就是为「文件从哪来我不管，落地了我负责」设计的。人工排队**不需要任何架构改动** |
| **它就是 C1 里那句「需要人工做一部分」** | 用户最初的需求原话是「不使用 computer use 的纯后台阶段一（**需要人工做一部分**）」。这一步正是那「一部分」，见 §12 的 D-14 |

**由此 C 的职责收窄为**：

- ✅ **MTool 加载翻译**（§7.6）——这是 C 唯一无法被替代的工作，因为 MTool 是 GUI-only
- ⚪ **资源页解析**（§7.4）——**当你直接提供链接与密码时，这一步整个跳过**。它只在「我只有一个资源页网址」时才需要
- ❌ **网盘转存与下载**（§7.5）——降级为**可选路径**，保留设计以备将来（例如换成专用机、或某个网盘出了可脚本化的通道），但**不是主路径**

> **`direct` 类（123云盘 / 蓝奏云 / 其他直链）是另一回事**：它们通常有可直接 HTTP 取的链接，**可以由 A/B 无人值守拉取**，连人工排队都不需要。这条留作演进项——当前版本仍走 `_inbox`，与人工排队同一条路径，不做特殊处理（少一条代码路径，少一类失败模式）。

**对 §7.5 各网盘小节的读法**：它们描述的仍然是正确的操作序列与已知坑，但**执行者默认是人，不是 Computer Use**。其中的「人工接力点」（R-01…R-12）因此大部分变成「你本来就在做的事」。

---

### 7.1 会话模型

#### 7.1.1 为什么 C 只能是短会话（事实依据）

| # | 事实 | 可信度 | 后果 |
|---|---|---|---|
| F1 | 「在 Windows 上，计算机使用在活动桌面运行。**您无法在它于后台运行的同时继续使用同一 Windows 会话**，因此请注意，任务运行期间，ChatGPT 会移动指针、输入内容并接管前台操作。」 | 【官方】<br>https://learn.chatgpt.com/zh-Hans/docs/computer-use | C 运行 = 用户失去这台机器。会话长度直接等于用户的损失 |
| F2 | 「**锁定状态下使用适用于 macOS。在 Windows 上，计算机使用在前台运行。**」；macOS 侧另有「在后台运行范围明确的任务，同时继续处理其他工作」 | 【官方】同上 | **「锁屏继续」是 macOS 专属能力，Windows 上不存在。** 不要在 SPEC 或运维手册里给用户这个指望 |
| F3 | 「在 Windows 上，请确保目标应用在任务运行期间**始终显示在活动桌面上**。」 | 【官方】同上 | 网盘客户端、MTool、游戏窗口必须一直可见；用户不能把它们最小化去干别的 |
| F4 | 该功能「**无法自动操作终端应用或 ChatGPT 本身**」，也「**无法以管理员身份进行身份验证**」 | 【官方】同上 | C **不能**用 Computer Use 去点 PowerShell 窗口跑解压（这本来就是 B 的活）；网盘客户端若弹 UAC，C 点不了，只能停下等人 |
| F5 | MTool 加载「耗时几秒到几十分钟」，且「必须保持游戏窗口在前台，不要最小化」 | 【官方】<br>https://mtool.app/tutorial.php | 单个 MTool 条目的占用时长**不可预测且可能极长**，必须与网盘工作分开成两类会话（见 7.1.4） |

> **F1–F3 合起来的结论**：C 不是「后台任务」，是**用户主动让出电脑的一段时间**。SPEC 里任何「让 C 挂着跑一晚上」的设计都与 E1 冲突。官方给的两条无人值守出路（保持解锁的物理机 + 手机远程查看进度；或在 Windows 虚拟机里跑）【官方】同上——本 SPEC **不假设**用户有第二台机器或虚拟机，只把它们写进 §10 的可选演进。

#### 7.1.2 会话的四条硬性质

```
S1  显式触发    C 只由人在交互会话里调用 §8 的 skill「阶段二处理」启动。
                没有定时器、没有 watcher、没有「A 发现有活就自动叫 C」。
S2  短会话      单次会话有明确的、事先声明的工作量预算（7.1.4），到点就收工，
                不管清单里还剩多少。
S3  可中断      任何时刻用户抢回鼠标 / 关掉 Codex 会话，都不会让任何条目进入
                「无法从落盘状态判断该怎么办」的中间态。
S4  可续跑      下一次 C 会话只看 events.jsonl / state.json 决定从哪继续，
                不依赖上一次会话的任何内存状态、不依赖会话之间的时间间隔。
```

S3 + S4 的实现方式只有一条，也是本节最硬的契约：

> **C-ATOM：C 按条目推进，且每个条目的每一个「接力段」结束后立即 append `events.jsonl` 并 `Flush($true)`，然后才允许开始下一个动作。**
> 中断的最大损失 = 当前正在做的那一个接力段，且该条目的落盘状态仍停在上一个已确认状态，下次会话原样重跑该段。

「接力段」的粒度在 7.2.3（网盘侧）与 7.6（MTool 侧）逐一钉死。落盘协议（JSONL `-Compress`、整行一次 `Write` + `Flush($true)` + 立即关闭）见 §8。

#### 7.1.3 会话主循环

```
   [人] 在 Codex 桌面 App 里调用 skill「阶段二处理」 -BatchId <slug>
        │
        ▼
   ┌──────────────────────────────────────────────────────────────┐
   │ 载入 D:\GameLib\batches\<batch-id>.json                        │
   │ 载入 D:\GameFlow\rules\netdisk-profiles.json / engine-rules.json│
   │ 计算 worklist（7.2.1），按批次清单里的**声明顺序**排序           │
   │   —— 不按体积排、不按预估耗时排、不做任何优先级评分              │
   └──────────────────────────────────────────────────────────────┘
        │
        ▼
   for item in worklist:                       ← 逐条，不并行
        ├─ 预算检查：已用条目数 / 已用时长 超过本次会话预算？ → break
        ├─ 执行该条目的下一个接力段（网盘段 或 MTool 段）
        │     ├─ 需要人工？ → 停下，把屏幕交给用户（7.7），等一次确认
        │     └─ 成功/失败 → append events.jsonl + Flush($true)   ← 落盘点
        └─ continue
        │
        ▼
   写 D:\GameLib\_reports\<batch-id>-<seq>.md（本次会话做了什么、还剩什么）
   明确告诉用户「机器还给你了」，会话结束
```

**为什么不并行**：并行只在有空闲资源时有意义，而 C 的稀缺资源是**唯一的前台桌面**，它天然是串行的（F1/F3）。并行只会制造「两个窗口抢焦点」的不确定行为。

**为什么按声明顺序**：用户批次清单里的顺序是他自己排的，是人可预测的。任何「智能排序」都会让「上次跑到哪了」变成需要查报告才知道的事，与 S4 冲突。

#### 7.1.4 一次 C 会话该做多少事

**两类会话必须分开跑，不要混在一次会话里。**

| 会话类型 | 处理什么 | 单次建议上限 | 目标时长 | 理由 |
|---|---|---|---|---|
| **C-net**（网盘会话） | 资源页解析、转存、起下载 | **≤ 6 个条目** | ≤ 20 分钟 | 每个条目 = 开页 + 抽字段 + 转存 + 起下载，动作数固定、耗时相对可预测；下载本身**不在会话内等待**（7.3.5），所以条目数才是唯一预算 |
| **C-mtool**（翻译会话） | MTool 注入与加载 | **1 个条目**（体积 < 2 GB 时可放宽到 2 个） | 不可预测 | F5：单个游戏的加载就可能几十分钟且必须前台。把它和网盘条目混在一起，会让本来 3 分钟能做完的转存被卡在后面几十分钟 |

> **单次会话的时长上限由人掌握，不由系统猜。** SPEC 不设「C 自动在 N 分钟后停止」——Computer Use 是否支持自我限时、以及超时后如何清理半完成的 GUI 状态，本轮**没有任何官方或社区资料**【待测】。
> **不实测的后果**：只能靠用户自己看着表叫停；若某次 MTool 加载真的卡死在几十分钟，用户除了强行中断会话没有别的手段，而强行中断的落盘后果由 C-ATOM 兜底（最多重跑一段）。这是可接受的降级，所以这条**不阻塞落地**。

#### 7.1.5 中断与重入语义

| 中断发生在 | 落盘状态 | 下次会话怎么做 |
|---|---|---|
| 资源页解析中 | 仍 `PLANNED` | 重新开页重抽。解析是纯读，天然幂等 |
| 转存已提交但未确认 | `PLANNED`，`cloud_links[i].transferred_at` 未写 | **重跑会在网盘里产生重复副本**——这是唯一一处非幂等动作。契约：C 在转存**成功确认后立即**写 `transferred_at`；重跑前先在网盘里按 `expected_files[].name` 查一次是否已存在，存在则跳过转存直接起下载 |
| 下载已在客户端排队 | `AWAITING_DOWNLOAD`，`cloud_links[i].dispatched = true` | 不重复起下载。下载完成与否由 A（§5）判定，与 C 无关 |
| MTool 注入中 | `TRANSLATING` | 见 7.6.9 的重入规则：先查 `version.dll` 是否已存在 |

---

### 7.2 C 的输入输出契约

#### 7.2.1 C 消费什么（worklist 选择器，写死）

**状态从哪里读（R9 硬规则）**：批次清单**没有** `state` 字段——它只有 `abandoned: true|false`（§3.2）。
条目的运行期状态**一律**取自 `games\<dirname>\.gameflow\state.json`；该文件不可解析时从 `events.jsonl` 重放重建（§3.9）。
**任何代码路径把批次清单当作状态来源，都是缺陷。**

```
STATE(item)    := ReadState(games\<item.dirname>\.gameflow\state.json)   // 派生快照，§3.4
                  ?? Replay(games\<item.dirname>\.gameflow\events.jsonl) // 快照缺失时重建，§3.9

NET_DONE(item) :=  (item.expected_files 非空)
                ∧ (∀ L ∈ item.cloud_links : L.dispatched == true)

C_WORKLIST(batch, kind) :=
  { item ∈ batch.items | item.abandoned == false } 再按 kind 过滤：
  kind == "net"   → STATE(item) == PLANNED
                  ∨ (STATE(item) == AWAITING_DOWNLOAD ∧ ¬NET_DONE(item))
  kind == "mtool" → STATE(item) ∈ { READY_FOR_TRANSLATION, TRANSLATING }
```

- **C 不看任何其它状态。** 处于 `WAITING_FOR_*` / `EXTRACT_FAILED_AMBIGUOUS` / `CORRUPT` / `THREAT_*` / `BLOCKED_RESOURCE` / `PATH_BUDGET_EXCEEDED` / `JOURNAL_CORRUPT` / `ABANDONED` 的条目，C 一律跳过并在报告里列出——它们的解除动作在 §4 的停机态手册里，属于 A/B 的领域。
- `translation.mode == none` 的条目根本不会进入 `READY_FOR_TRANSLATION`（§4 的主线：`SCANNED` 直接 → `COMPLETE`），所以 C-mtool 天然看不到它们。
- **契约（交给 §4 落实）**：`WAITING_FOR_MTOOL_RULE` 与 `MTOOL_FAILED` 的人工解除动作，**一律把条目送回 `READY_FOR_TRANSLATION`**，不引入新的入口状态。这样 C 的选择器永远只有两个状态名，S4 才成立。

#### 7.2.2 C 绝不做什么

```
❌ 不解压     任何 7z t / 7z x 都属于 B（§6）。C 连只读的 7z l 都不跑
❌ 不改名     包括分卷 .NNN 重建（C7 的唯一硬例外，归 §6）、
              包括百度网盘残留后缀的去除（7.5.1）
❌ 不删除     任何文件。删除受 §6 的 D1–D12 十二条判据 gate
❌ 不移动     _inbox → games\ 的认领归位是 A 的事（§5）
❌ 不提权     F4：Computer Use 无法以管理员身份验证，UAC 框点不了
❌ 不改配置   不动 Defender 排除项、不动 ExecutionPolicy、不动下载目录设置
              —— 这些是一次性人工前置（7.8 / R-12 / R-13）
```

这条边界不是洁癖：**C 是三个执行体里唯一无法用单元测试覆盖的部分**（GUI + 外部服务 + 视觉判定）。把所有可测的动作留在 A/B，C 的失败面才收得住。

#### 7.2.3 C 写回批次的字段

写回目标是 `D:\GameLib\batches\<batch-id>.json`（完整 schema 见 §3），落盘事件同时 append 到对应游戏的 `.gameflow\events.jsonl`。

| 字段 | 类型 | 接力段 | 语义与硬约束 |
|---|---|---|---|
| `cloud_links[]` | array&lt;object&gt; | 解析 | 元素 = `{ netdisk, url, code_ref, role, transferred_at, dispatched, channel, note }`（字段权威见 §3.2）。`netdisk` 取值必须是 `netdisk-profiles.json` 里已登记的 id，未登记的域名 → 停 `WAITING_FOR_RULE`，**不猜** |
| `cloud_links[].code_ref` | string｜null | 解析 | 网盘**提取码**的 **vault 键名**（`link:<site>\|<item-id>\|<index>`，§3.7.2）。与解压密码是两回事，**永不互相代入**。**提取码本身永不内联进批次文件**——C 抓到后写 vault、批次只回填键名（§3.2 硬规则；理由见 §9） |
| `expected_files[]` | array&lt;object&gt; | 转存后 | 元素 = `{ name, bytes, role, part_index }`（§3.2）。**取自转存后网盘里的文件列表，不是取自资源页正文**——资源页的描述经常与实际文件对不上 |
| `expected_parts` | int｜**null** | 转存后 | 该条目预期的分卷数，取值域 **≥ 1 或 null**（§3.2）。**明确无分卷写 `1`**（单卷即一卷），**未知写 `null`**；**`0` 不是合法值**。「明确 1 卷」与「没人告诉过我」在 §5.3 的 G6 与 §6.8 的 D11 里走完全不同的分支 |
| `password_ref` | string｜null | 解析（P5 命中时） | 只写**引用**，明文进 `%LOCALAPPDATA%\GameFlow\vault.clixml`（DPAPI）。见 7.4.3 |
| `translation.target_exe` | string | MTool 前 | 人工确认后写；同时同步进 `mtool-profiles\<item-id>.json`（7.6.6） |
| `translation.profile_ref` | string｜null | MTool 前 | 指向 `<item-id>.json`。profile 本体不写进批次 |

**`transferred_at` / `dispatched` 是 7.1.5 重入语义的落盘凭据**，不是给人看的时间戳——不允许被报告生成器改写或补齐。

#### 7.2.4 `expected_parts` 为什么是关键字段

7-Zip 对「缺中间卷 / 缺末卷 / 单文件截断」三种情况的 stderr **一字不差**（都是 `Unexpected end of archive`）【实测·macOS 官方 7zz 25.01；该输出串来自跨平台公共输出层，可外推 Windows，但仍列 §11 复验】。也就是说：

- 你下了 `part1..part3`，实际有 5 卷 → 卷集内部**连续且等大**，G6 的连续性检查全部通过；
- 直到 B 去解压才失败，而失败信息与「包损坏」不可区分 → 落 `EXTRACT_FAILED_AMBIGUOUS`（RAR4 更是无解，§6）；
- 而此时源包已经下完、可能已经过了几天，用户根本记不清资源页上到底写了几卷。

> **所以：`expected_parts` 是「批次里少下了分卷」这个失败模式唯一可靠的自动检测点。** 它只能由 C 在前台看着网盘文件列表时写回——A 和 B 都没有任何途径知道「应该有几卷」。
> 若 C 无法确定卷数（转存后的目录结构不清晰、链接指向的是一个网盘文件夹而非文件列表），必须写 `null` 并在报告里把该条目标为「**分卷数未声明：无法自动证明下载完整**」。宁可留白，不要猜一个数。

---

### 7.3 下载目标的现实

#### 7.3.1 「把下载目标定到游戏目录」这个能力不存在

- 官方原文：「浏览器下载的文件默认保存在系统的『下载』文件夹中。在**设置 > 浏览器**中，您可以选择其他下载位置、将下载位置恢复为系统默认位置，或开启**询问下载文件的保存位置**。」【官方】https://learn.chatgpt.com/zh-Hans/docs/browser
- 在官方 112 KB 的全量配置键参考 `config-reference` 里检索下载目录相关键：`download_dir` / `download_path` / `downloads_dir` **零命中**；所有 `download` 相关键都是 `allow｜deny` 权限开关（`browser_use.default_origin_policy.downloads`、`browser_use.origins.<pattern>.downloads`），不是路径。【实测·macOS 上对官方文档文本做 grep；被检索对象是平台无关的配置参考文档，结论可外推 Windows】

> 结论：内置浏览器只有**一个全局的、GUI 里设的、所有任务共享的**下载目录，或者「每次弹框问你存哪」（那就不是自动了）。上游设计里「Codex 将下载目标定到 `games\<游戏名>\`」**必须整段作废**。

**替代设计（D-02，写死）**：

```
全局下载目录 = D:\GameLib\_inbox        ← 唯一落地目录，一次性人工设定（7.8）
      │
      │  A（§5）按批次清单认领归位
      ▼
D:\GameLib\games\<dirname>\             ← C3「源包与本体同目录」在这里才成立
```

**C3 是终态保证，不是落地瞬间的保证。** 这一句必须在 §1、§2、§5、§7 保持完全一致的措辞。

#### 7.3.2 三条下载通道

| 通道 | 落地目录由谁决定 | 登录态从哪来 | 会话结束后下载是否继续 | 适用网盘 |
|---|---|---|---|---|
| **内置浏览器** | Codex 设置 > 浏览器（全局），人工设为 `_inbox` | 内置浏览器有**独立 profile**，不共享现有浏览器会话【官方】docs/browser；两条获取路径见 7.3.3 | 【待测】——不实测的后果：若关掉桌面 App 会中断下载，那 C 的「短会话」前提就不成立，必须改走客户端通道 | 直链型为主 |
| **@Chrome / @Edge 扩展** | **用户自己浏览器的**下载设置，人工设为 `_inbox` | 复用用户**已登录**的会话【官方】docs/chrome-extension.md：「ChatGPT can read or act on sites where you're already signed in」 | 浏览器是独立进程，大概率继续【待测】 | 三类都可 |
| **网盘桌面客户端** | 客户端自身的下载目录设置，人工设为 `_inbox` | 客户端自己的登录（通常扫码，R-01） | 客户端是独立进程，大概率继续【待测】 | 百度 / 夸克 / 阿里 |

> **扩展是官方路径，CDP 不是。** 「当 ChatGPT 需要在现有的 Chrome、Edge、Brave、Opera 或 Vivaldi 标签页中操作，或使用您常用的浏览器配置方案时，请改用**浏览器扩展程序**。」【官方】docs/browser。扩展安装后挂在**设置 > 计算机使用**下，用 `@Chrome` / `@Edge` 这类 @ 提及调用；它索要的权限里明确包含 **Manage your downloads**。【官方】docs/chrome-extension.md
> 完整 CDP 访问是**开发者模式下的受控检查通道**（设置 > 浏览器 > 开发者模式），用于看 DOM/控制台/网络【官方】docs/browser。**SPEC 不得写「用 CDP 连接用户已登录的 Chrome 的调试端口」——那不是官方支持的路径。**

> ⚠️ **扩展是否也占用前台指针，官方文档没有明确表态。**【待测】（对应 §11 清单第 4 项）
> 证据两面都有：一面是扩展权限里有 "Access the page debugger"（不需要移动鼠标）；另一面是它挂在「设置 > 计算机使用」下，且安全指南说「若要在 ChatGPT 工作期间继续使用您的浏览器，请让 ChatGPT 使用其他浏览器」。
> **不实测的后果**：C 与 A/B 的分界线画在哪里悬空；如果扩展其实不占前台，网盘部分本可以脱离 E1 的约束，会话模型能大幅放宽。**在实测出结论前，本 SPEC 一律按「占用前台」的保守假设设计**——保守假设错了只是浪费用户时间，反过来错了会让用户在毫无预期的时候被夺走鼠标。

#### 7.3.3 内置浏览器的登录态

两条路，**都不是实时共享**：

1. **手动登一次**：在内置浏览器里正常登录网盘，会话保持在它自己的 profile 里。【官方】docs/browser
2. **一次性导入**：「打开**设置 > 浏览器**，即可管理浏览器数据以及**您设备上可用的配置方案导入功能**。」【官方】docs/browser；管理侧开关 `in_app_browser.allow_external_browser_settings_import` 的存在也佐证了这条通道【官方】config-reference。
   **一次性导入具体能导什么（Cookie？已登录会话？）文档没说清**【待测】——不实测的后果：只能退回路径 1，用户对每个网盘手工登一次（R-01），成本可接受，所以这条也不阻塞落地。

#### 7.3.4 ChatGPT Work 的云端浏览器对本项目无用

「Work 使用自己的浏览器，该浏览器运行在**云端的一台独立计算机上**，并非您手机或笔记本电脑上的浏览器……即使您离开并合上电脑，ChatGPT 也能继续工作。」【官方】docs/browser

> 这段话很诱人，但它对本项目**没有任何用处**：云端浏览器下载的文件落在**云端机器**上，到不了 `D:\GameLib\_inbox`。不要把「合上电脑也能跑」这个卖点安到本地工作流头上，也不要在运维手册里让用户去试。

#### 7.3.5 下载不在 C 会话内等待

C 的职责在「**把下载排进队列**」那一刻就结束了：写 `cloud_links[i].dispatched = true`，落盘，下一个条目。

```
C 会话（前台，几分钟）        A 巡检（后台，计划任务，每 N 分钟）
─────────────────────        ────────────────────────────────
起下载 → dispatched=true  ⇢  扫 _inbox → G1..G6 六重判定
会话结束，机器还给用户         → DOWNLOAD_STABLE → 认领归位到 games\
                              → 出对账报告
```

这是 E1 与「下载动辄几十分钟甚至几小时」之间唯一自洽的安排，也是 A 必须是**无状态、扫一遍就退出**的批处理（而不是常驻 watcher）的原因之一：Windows 上 `features.unified_exec` 默认关闭（官方逐字：enabled by default except on Windows）【官方】config-reference，且 Computer Use 独占前台（F1）。详见 §5。

---

### 7.4 资源页解析

#### 7.4.1 抽取字段与去向

| 抽取项 | 方法 | 写到哪 | 抽不到时 |
|---|---|---|---|
| 游戏标题 | 页面主标题；仅当批次条目未显式给出 `title` 时才采用 | `item.title`（目录名生成规则见 §3） | 保留批次里原有值；两者都没有 → 停 `WAITING_FOR_RULE` |
| 网盘链接 | 按 `netdisk-profiles.json` 的 `page_rules.host_patterns` 做 **host 白名单匹配** | `cloud_links[].url` + `.netdisk` | host 不在白名单 → 停 `WAITING_FOR_RULE`，把 host 原样写进事件（用户决定要不要新增一个网盘 profile，R-12） |
| 提取码 | 固定正则，见 7.4.2 | `cloud_links[].code_ref` | 留 `null`；到网盘页时由人工输入（R-02） |
| 密码提示 | 固定正则，见 7.4.2 | 密码库（P5），批次里只留 `password_ref` | 留 `null`；§6 走 P0→P6 链，最终可能停 `WAITING_FOR_PASSWORD` |
| **链接或分卷数量** | 优先取**转存后网盘文件列表**的条目数；其次取资源页明确写出的分卷数 | `expected_parts` / `expected_files[]` | 写 `null`，报告标「分卷数未声明」（7.2.4） |

#### 7.4.2 抽取只做正则精确抽取

资源页是**不可信输入**。官方安全指南原文：「页面上的指令可能具有误导性或恶意。网站权限允许 ChatGPT 与该网站交互，但不代表网站内容可信。」【官方】docs/browser

因此本 SPEC 对资源页只允许一种消费方式：

```
允许：把页面文本喂给一组固定正则，把捕获组原样填进枚举字段。
禁止：❌ 让模型「读懂」页面自然语言后据此改变流程、跳过步骤、或合成一个密码
      ❌ 执行页面上出现的任何指令性文字
      ❌ 用页面上的说法覆盖 rules\ 里已登记的规则
```

密码抽取正则（与 §6 的 P3/P4 共用同一套，定义在 `source-profiles.json`，此处只列形状）：

```
/(?:解压)?密码\s*[：:＝=]\s*(\S+)/
/(?:提取|访问)码\s*[：:＝=]\s*([0-9A-Za-z]{4,8})/
/password\s*[：:=]\s*(\S+)/i
```

**抽取结果的处理是字节级的**（§6 的密码链已裁定，此处重申因为 C 是唯一从网页取值的执行体）：

- 抽不到、或抽到**多于一个互不相同**的结果 → 当作没抽到。绝不「选一个看起来最像的」。
- 抽到的串**原样保存字节**：不 trim（`'pass '` ≠ `'pass'`）、不做 NFC/NFD 转换、**绝不做 NFKC**（会把全角 `ｐａｓｓ１２３` 折成 `pass123`，直接破坏密码）。【实测·macOS：全角密码与半角密码在 7-Zip 下 rc 不同，是两个不同密码；密码是字节不是字符，此性质来自 7-Zip 对密码字节透明，可外推 Windows】
- 页面上显示为乱码的密码（如 `鏌愪笉鐭ュ悕缃戝弸鍒嗕韩`）**照原样存**——它很可能就是正常中文的 UTF-8 字节被按 GBK 显示，而包确实是用那串字节加密的。等价展开（≤4 个候选）由 §6 在解压时做，**C 不做任何展开、不做任何转码**。【实测·macOS】

#### 7.4.3 P5：抓到的密码需要人工确认一次

```
C 抓到密码候选
   ├─ 写 vault：{ ref, bytes(base64), source: "resource_page", url, confirmed: false }
   ├─ 屏幕上就摆着资源页 → **就地念给用户确认一次**（R-05）
   │     ├─ 用户确认 → confirmed: true，写 item.password_ref
   │     └─ 用户当时不想理 → 保持 confirmed: false，照常推进条目
   └─ §6 的 P1/P2 只接受 confirmed:true 的条目
         → 未确认的密码等于不存在，条目在 B 那里落 WAITING_FOR_PASSWORD
```

**为什么确认放在 C 会话内而不是单开一个确认环节**：C 跑的时候人本来就坐在机器前，且资源页正开着，确认成本接近零。为一次「是/否」再设计一个异步确认流程，就是为了流程而流程。

> 密码明文进 `%LOCALAPPDATA%\GameFlow\vault.clixml`（DPAPI，绑定运行身份），**不进批次 JSON、不进 `.gameflow\`、不进报告、不进 Git**（§3 / §9）。另注意：Computer Use 工作时**截图会进入上下文**【官方】docs/browser 隐私章节——所以密码在屏幕上出现是不可避免的，这条必须写进 §9 的「诚实局限」。

---

### 7.5 三类网盘适配器（E2）

#### 7.5.0 适配器契约

所有网盘差异集中在 `D:\GameFlow\rules\netdisk-profiles.json`，**代码里不得出现任何网盘名的硬编码分支**。每个 profile 的字段：

```json
{
  "id": "baidu",
  "display_name": "百度网盘",
  "_note": "字段含义见 SPEC §7.5.0",

  "page_rules": {
    "host_patterns": ["pan.baidu.com", "yun.baidu.com"],
    "access_code_in_url": true
  },

  "login": "client_qr",
  "requires_transfer": true,
  "download_channel": "desktop_client",
  "landing_dir": "D:\\GameLib\\_inbox",

  "temp_suffixes": [".baiduyun.p.downloading", ".baiduyun.downloading"],
  "temp_name_globs": [],
  "sidecar_pairs": [],
  "confidence": "community",
  "known_false_complete": [
    { "pattern": "*.baiduyun.p.downloading",
      "note": "已知会在下载完成后残留后缀，见 SPEC §7.5.1；不得自动去后缀" }
  ],
  "grace_seconds_after_final_name": 8,
  "fallback_quiet_seconds": 300,

  "manual_relay_points": ["R-01", "R-04", "R-05"]
}
```

**`confidence` 的取值与它对 §5.3 的强制含义**（字段与取值域的权威在 §3.6.2；本表只写 G1/G2 的执行语义）：

| 取值 | 含义 | §5 的 G1/G2 必须怎么办 |
|---|---|---|
| `official` | 有官方文档或源码证据（浏览器通道走这一档） | G1 正常生效 |
| `observed` | 已由人在本机实测登记（R-12 完成后从 `unregistered` 升级到这一档） | G1 正常生效 |
| `community` | 社区共识（百度） | G1 生效，但同时挂 `known_false_complete` 检查 |
| **`unregistered`** | **查不到任何资料，未人工登记** | **G1 只按通用后缀表判定，不得视为已充分覆盖**；且 G2 的静默窗口必须放大到 `fallback_quiet_seconds`；该条目在对账报告里打「临时物模式未登记」标记 |

这条设计的作用：**把「我不知道」显式编码成一个会影响判定的状态，而不是让它退化成「看起来没有临时文件所以下完了」。** 这正是 §5 的下载完成判定里那六种失败机理最爱钻的空子。

---

#### 7.5.1 (a) 百度网盘

| 项 | 内容 | 可信度 |
|---|---|---|
| 登录态 | 桌面客户端扫码登录（人工，R-01）；或走 @Chrome 扩展复用已登录网页 | 【官方】扩展路径见 docs/chrome-extension.md；扫码流程属通用常识 |
| 转存 | **需要**。分享链接 →（输入提取码）→「保存到我的网盘」→ 再从我的网盘下载 | 用户原始流程陈述（CONTEXT 一、步骤 3），非技术断言 |
| 下载通道 | **客户端为主** | 【社区】网页端下载限制与速度策略是社区共识且**政策数月一变**——这类断言只作背景解释，**不得**成为任何自动判定的输入 |
| 落地 | 客户端下载目录，一次性人工设为 `D:\GameLib\_inbox`（7.8） | 设计规定 |
| 临时物 | `*.baiduyun.p.downloading`（旧版也见 `*.baiduyun.downloading`） | 【社区】多篇中文技术文一致 |
| 人工接力点 | R-01 登录 · R-04 转存确认 · R-05 密码确认 · R-11 残留后缀 | — |

**已知坑（必须写进 SPEC，因为它直接击穿 G1）**：

> 多篇用户反馈一致指出：**下载已经完成、客户端任务列表显示成功，但 `.baiduyun.p.downloading` 后缀仍然残留**，需要手动去掉后缀。【社区】

因此对百度网盘：

```
「无临时后缀」既不是完成的必要条件，也不是充分条件。
```

处置规则（**C 不参与，写在这里是因为规则由 C 的 profile 提供**）：

- A（§5）发现 `_inbox` 里有匹配 `known_false_complete.pattern` 的文件、且尺寸与 mtime 已长时间静默 → **不自动去后缀**（去错了就毁掉一个未完成文件，且 I1 要求停机态不动文件），落 `WAITING_FOR_RULE`；
- 用户人工确认并去掉后缀（R-11）→ 文件以最终名出现 → 重新走 G1..G6。

**为什么不让 C 顺手改**：7.2.2 的「不改名」是一条无例外的边界。C 是最容易在错误时刻动手的执行体（它在前台、它有全部上下文、它看起来「顺手」），一旦开了口子，「C 只改这一处」会迅速变成「C 什么都能改」。

---

#### 7.5.2 (b) 夸克网盘 / 阿里云盘

| 项 | 内容 | 可信度 |
|---|---|---|
| 登录态 | 客户端或网页登录（人工，R-01） | — |
| 转存 | **需要**（同 (a) 的分享链接→转存→下载模式） | 用户原始流程陈述 |
| 下载通道 | 客户端或网页，二选一写进 profile 的 `download_channel`，**不在运行期自适应切换** | 设计规定 |
| 落地 | 同 (a)，人工设为 `_inbox` | 设计规定 |
| 临时物 | **查不到任何权威或可交叉验证的资料** → `temp_suffixes: []`，`confidence: "unregistered"` | 【待测】 |
| 人工接力点 | R-01 · R-04 · R-12（首次登记临时后缀） | — |
| 已知背景 | 「阿里云盘不支持常见压缩文件分享但支持 exe 分享」，故资源常打成 SFX 自解压 `.exe` | 【社区】单篇 2022 年 CSDN。**仅作为解释 C5/C6/C7 成因的背景**，**不得**成为自动判定输入。SFX 一律**当归档解，绝不执行**（见 §5 的「不进自动流程的形态」） |

> **【待测】不实测的后果**：这两家的下载判定只剩通用判据（尺寸+mtime 双静默 ∧ `FileShare::None` 独占打开 ∧ `7z l -slt` 头自校验），而通用判据在**预分配下载器**面前完全失效——文件从第 0 秒起就是最终尺寸【官方 aria2 手册：`--file-allocation` 默认 `prealloc`】。若夸克/阿里客户端也预分配，会稳定误判成「下载完成」，然后 B 去解压一个半截文件，落 `CORRUPT` 或 `EXTRACT_FAILED_AMBIGUOUS`，用户以为是密码错。
> **登记方法（R-12，五分钟的事）**：用该客户端下载**一个大文件**，在下载**进行中**执行 `Get-ChildItem -LiteralPath D:\GameLib\_inbox -Force`，记下所有不属于最终名的文件；下载完成后再执行一次，比较差集。把观察到的后缀填进 profile，把 `confidence` 改成 `observed`。

---

#### 7.5.3 (c) 123云盘 / 蓝奏云 / 其他直链型

| 项 | 内容 | 可信度 |
|---|---|---|
| 登录态 | **可能无需登录**（分享页 + 提取码即可下载） | 【社区】直链型网盘的通行形态 |
| 转存 | **通常不需要**——这是这一类与前两类最大的区别，也是它最省 C 会话时间的地方 | 【社区】 |
| 下载通道 | **网页直下**为主，走内置浏览器或 @Chrome 扩展 | 设计规定 |
| 落地 | 浏览器全局下载目录 = `_inbox` | 【官方】docs/browser（只有全局设置，7.3.1） |
| 临时物 | 走浏览器 ⇒ 临时物由**浏览器**决定，已知且有源码证据：Chromium 系 `<最终名>.crdownload`、危险下载走 `Unconfirmed <随机数>.crdownload`；Firefox `<最终名>.part` **且目标目录里同时有一个 0 字节的最终名占位文件** | 【官方】Chromium / Firefox 源码原文（见 §5 的临时物总表） |
| 已知坑 | Chromium **先重命名到最终名、再打 MOTW / 送 AV 扫描**，而 `IAttachmentExecute::Save()` 可能**直接把刚出现的文件删掉** ⇒ 最终名出现 ≠ 可以动它，必须过宽限期并在操作前复检存在性 | 【官方】Chromium 源码注释 |
| 人工接力点 | R-02 提取码 · R-03 人机验证 | — |
| 若改走该网盘的 PC 客户端 | 临时物**未知** → `confidence: "unregistered"` + R-12 | 【待测】 |

> 直链型是三类里**唯一可能完全不需要转存**的，因此在排 C-net 会话的条目时，把这一类排在前面能让单位会话时间产出更多——但这是**给用户的建议**，不是让 C 自动重排（7.1.3：按声明顺序）。

---

#### 7.5.4 临时物后缀表（进 `netdisk-profiles.json` 预填）

| 来源 | 临时物模式 | `confidence` | 可信度 |
|---|---|---|---|
| Chromium 系（Chrome/Edge/Brave/内置浏览器等） | `*.crdownload`；`Unconfirmed *.crdownload` | `official` | 【官方】Chromium 源码 `download_target_determiner.cc` |
| Firefox | `*.part` + **0 字节最终名占位** | `official` | 【官方】Firefox 源码 `DownloadCore.sys.mjs` |
| aria2 / Motrix | `*.aria2`（完成后删除该控制文件） | `official` | 【官方】aria2 手册 |
| **百度网盘** | `*.baiduyun.p.downloading`、`*.baiduyun.downloading` | `community` + `known_false_complete` | 【社区】 |
| **夸克网盘** | **（留空）** | **`unregistered`** | 【待测】R-12 |
| **阿里云盘** | **（留空）** | **`unregistered`** | 【待测】R-12 |
| **123云盘 / 蓝奏云（客户端通道）** | **（留空）** | **`unregistered`** | 【待测】R-12 |
| 迅雷 | `*.xltd`、`*.td`、`*.td.cfg`、`*.bt.td` | `community` | 【社区】 |
| IDM | 目标目录**无**临时物（分块在 `%APPDATA%\IDM\DwnlData\`），合并期文件缓慢增长 | `community` | 【社区】 |
| 通用兜底 | `*.tmp`、`*.partial`、`*.downloading`、`~*` | `community` | 【社区】 |

> 表里三个 `unregistered` 是**故意留白的**。查不到就是查不到；填一个「看起来合理」的后缀，会让 G1 从「不知道」变成「错误地知道」，后者危险得多。

#### 7.5.5 海外盘

MEGA / OneDrive / Google Drive 等**不在 E2 范围内，本 SPEC 不为它们写任何规则**。`netdisk-profiles.json` 的 schema 天然支持新增条目，新增一个网盘 = 填一份 profile + 走一次 R-12 登记。**扩展位仅此而已，不预置、不猜测、不写"大概是 `.partial`"。**

---

### 7.6 MTool

> MTool 是**闭源、纯 GUI、会自动更新且无版本号**的第三方工具，本轮调研在 macOS 上**完全无法实测它**。所以这里对「界面长什么样、产物叫什么、要多久」的每一条，只要没有官方页面或一手论坛发言支撑，一律标【待测】。**宁可让 C 停下来问人，也不要写一个看起来完整的假流程。**

#### 7.6.1 适用性判定：`TRANSLATION_NOT_APPLICABLE`

MTool 官网与作者 Patreon 页两个**互相独立**的引擎清单里，**都没有 Unity、Unreal、Godot、GameMaker**。【官方】https://mtool.app/ + https://www.patreon.com/AdventCirno/about

```
engine ∈ { Unity, Godot, Unreal }  →  TRANSLATION_NOT_APPLICABLE（终态，视同完成）
engine == UNKNOWN                  →  WAITING_FOR_MTOOL_RULE（不是失败！见 7.6.4）
其余（判据表命中且标 MTool 支持）    →  继续 7.6.3
```

**不要去试。** 对不支持的引擎跑一遍 MTool，代价是几十分钟的前台独占加一次可能失败的注入，收益为零。

#### 7.6.2 「游戏根目录」的定义与定位算法

**契约（写死）**：

> **游戏根目录 = 包含引擎标记文件的那一层目录**，它**不等于** `D:\GameLib\games\<dirname>\`。

两个原因，都是真实存在的：

1. 解压常常多出一层：`games\<dirname>\GameName_v1.02\` 才是真正的根【实测·macOS fixture `nested`：以 `games\<dirname>\` 为根时 exe 候选集为空】；
2. 按 C3，**源压缩包就躺在 `games\<dirname>\` 同级**，把它当根会把源包和游戏本体混在同一个扫描面里。

定位算法（有界、确定，无启发式）：

```
LOCATE_GAME_ROOT(item):
    base  := D:\GameLib\games\<item.dirname>\
    depth := engine-rules.json 的 root_search_depth（默认 2，静态可配）
    dirs  := { base } ∪ { base 下深度 1..depth 的所有子目录 }
    hits  := { d ∈ dirs | MATCH_ENGINE(d) ≠ UNKNOWN }      # 7.6.3

    |hits| == 1  →  game_root := 该目录，engine := 其判定结果
    |hits| == 0  →  engine := UNKNOWN  →  WAITING_FOR_MTOOL_RULE
    |hits| >  1  →  ambiguous          →  WAITING_FOR_MTOOL_RULE
                    （报告里列出全部命中目录及其判定，让人一眼选）
```

`game_root` 一旦人工或自动确定，**写进 `mtool-profiles\<item-id>.json` 并不再重算**。

#### 7.6.3 引擎判定表

**有序判定，先匹配先赢。** 判据是相对 `game_root` 的**文件/目录存在性**，大小写不敏感（NTFS 默认不区分大小写；跨平台差异见 §11）。

| 序 | 引擎 | 主判据 | 辅助判据 | MTool | 可信度 |
|---|---|---|---|---|---|
| 1 | RPG Maker **MZ** | `js/rmmz_core.js` | `index.html`、`data/System.json`、`effects/`、`package.json` | ✅ | 【实测·macOS fixture 命中】+【官方】mtool.app 引擎清单 |
| 2 | RPG Maker **MV** | `www/js/rpg_core.js`（无 `www` 时 `js/rpg_core.js`） | `www/data/*.json`、`package.json`、`.rpgmv*` 加密资源 | ✅ | 同上 |
| 3 | RPG Maker **VX Ace** | **`RGSS3*.dll`**（前缀匹配）、`*.rgss3a`、`Data/*.rvdata2` | `Game.ini` 的 `Library=` | ✅ | 同上 |
| 4 | RPG Maker **VX** | **`RGSS2*.dll`**（前缀匹配）、`*.rgss2a`、`Data/*.rvdata` | `Game.ini` 的 `Library=` | ✅ | 同上 |
| 5 | RPG Maker **XP** | **`RGSS1*.dll`**（前缀匹配）、`*.rgssad`、`Data/*.rxdata` | `Game.ini` 的 `Library=` | ✅ | 同上 |
| 6 | RPG Maker **2000/2003** | `RPG_RT.exe` + `RPG_RT.ini` + `RPG_RT.ldb` + `RPG_RT.lmt` | — | ✅ | 同上 |
| 7 | **Wolf RPG** | `Data.wolf`，或未打包时 `Data/BasicData/` | `Config.exe` 常伴随 `Game.exe` | ✅ | 同上 |
| 8 | **KiriKiri 2/Z** | `data.xp3` 或任意 `*.xp3` | `plugin/wuvorbis.dll`、`krflash.dll` | ✅ | 同上 |
| 9 | **SRPG Studio** | `data.dts` + `runtime.rts` | — | ✅ | 同上 |
| 10 | **TyranoBuilder** | `tyrano.js`、`_tyrano_data.sav` | `data/scenario/*.ks` | ✅ | 同上 |
| 11 | **SMILE GAME BUILDER** | `data.sgbpack` | — | ✅ | 同上 |
| 12 | **RPG Developer Bakin** | `bakinengine.dll` | — | ✅ | 同上 |
| 13 | **Visual Novel Maker** | `data/summaries.json.js` | — | ✅ | 同上 |
| 14 | **Ren'Py** | `renpy/` 目录、`*.rpyc`、`*.rpa` | `lib/`、`game/` | ✅ | 同上 |
| 15 | **Pixel Game Maker MV** | **判据未确认** | — | ✅（官网列出） | 【待测】 |
| 16 | **Unity** | `UnityPlayer.dll` / `UnityEngine.dll`、`<Name>_Data/globalgamemanagers` | `<Name>_Data/` | ❌ | 【实测·macOS fixture】+【官方】清单无 Unity |
| 17 | **Godot** | `project.godot`、`*.pck` | — | ❌ | 同上 |
| 18 | **Unreal** | **本轮调研未给出文件级判据** | — | ❌ | 【待测】 |
| 19 | `UNKNOWN` | 以上全不命中 | — | — | 兜底 |

**容器只作属性，不参与裁决**：`nw.dll` / `nw.pak` → 记 `container_attrs: ["nwjs"]`；`LICENSE.electron.txt` → `["electron"]`。它们只说明外壳是什么，还得看 `www/js` 才知道是 MV 还是 MZ。【实测·macOS：已下载 Valve 官方 `rules.ini` 逐行核对】

> **为什么不照抄 SteamDB 的正则**（`SteamDatabase/FileDetectionRuleSets/rules.ini`）：
> 1. 它的 RGSS 规则只枚举了 5 个具体 DLL 名（`rgss(?>104e|102j|102e|301|202e)\.dll`），**漏掉 `RGSS300.dll`、`RGSS202J.dll` 等**——所以本表改用**版本段前缀** `RGSS1*` / `RGSS2*` / `RGSS3*`；
> 2. 它把所有 RPG Maker 合并成一个 `RPGMaker` 标签，而 MTool 的支持面与注入行为按**子版本**不同；
> 3. 目的不同：SteamDB 是给商店页打标签，漏判只是少个标签；我们是决定「要不要往人家游戏目录里注入一个 DLL」，判错要人来兜底。
> 【实测·macOS：`curl` 下载 rules.ini 共 419 行，逐条 grep 核对上述三点属实。判据本身是纯文件名匹配，可外推 Windows；大小写敏感性差异需在 Windows 复验】

**表格本身的可判定性已验证**：13 种引擎的合成 fixture 全部命中、无冲突【实测·macOS，`detect_proto.py` + 17 组 fixture】。这只证明**规则集内部一致**，不证明它覆盖真实游戏——真实游戏目录的覆盖率属【待测】。

#### 7.6.4 主 exe 选择：不能自动选 `Game.exe`

**真实反例**：某游戏目录里同时有 `Game.exe`（需要 Steam）和 `開始游戏.exe`（能正常启动），**正确答案是后者**；选 `Game.exe` 会让 MTool 卡在加载界面。【社区】https://bbs.mtool.app/topic/1295/
而「按 `Game.exe` 优先」正是最自然的启发式——本轮原型检测器在同样构造的 fixture 上就选错了。【实测·macOS fixture `multi`】

**候选集生成规则（确定性，写在 `engine-rules.json`）**：

```
CANDIDATES(game_root) :=
    game_root 下**非递归**的所有 *.exe
  − exe_exclude 静态排除表（示例，实际清单在 engine-rules.json）：
        unins*.exe, uninstall*.exe, setup*.exe, install*.exe,
        vcredist*.exe, dxwebsetup.exe, oalinst.exe, *_setup.exe,
        Config.exe（Wolf RPG 的设置程序，不是游戏本体）

裁决：
  |CANDIDATES| == 1  →  target_exe := 唯一项，target_exe_decided_by := "auto"
  |CANDIDATES| == 0  →  ambiguous := true  →  WAITING_FOR_MTOOL_RULE
  |CANDIDATES| >  1  →  ambiguous := true  →  WAITING_FOR_MTOOL_RULE
                        报告里列出全部候选（含文件大小、修改时间），人选一次
```

人工选定后**写进 `mtool-profiles\<item-id>.json` 的 `target_exe` 与 `translation.target_exe`，此后不再询问**（R-08 是「每个歧义游戏一次」，不是「每次运行一次」）。

#### 7.6.5 `UNKNOWN` ≠ 不支持

**真实反例**：Enigma Virtual Box 之类工具把 RPG Maker MV 打包成**单个 exe**，目录里没有 `www/`、没有 `js/rpg_core.js` → 判据表判 `UNKNOWN`，但**MTool 实际能处理它**。【实测·macOS fixture `evb` 复现了判 UNKNOWN 的行为；「MTool 能处理」这半句来自】【社区】

```
engine == UNKNOWN  →  WAITING_FOR_MTOOL_RULE（停下来问人）
                   ✗  不得判 MTOOL_FAILED
                   ✗  不得判 TRANSLATION_NOT_APPLICABLE
```

人工裁决后把 `engine` 手工写进 profile 并标 `engine_decided_by: "human"`。**不把这次裁决反哺成新的自动规则**——判据表是静态资产，改它要人显式编辑 `engine-rules.json`，不由运行期自行生长（用户品味：拒绝自动重学）。

#### 7.6.6 `mtool-profiles\<item-id>.json` 完整 schema

```json
{
  "schema": 1,
  "item_id": "g0042",
  "_note": "由 SPEC §7.6 定义。人工确认一次后即为该条目的永久裁决记录。",

  "game_root": "D:\\GameLib\\games\\g0042\\SomeGame_v1.02",
  "game_root_decided_by": "auto",

  "engine": "RPGMaker_MV",
  "engine_evidence": ["www/js/rpg_core.js"],
  "engine_decided_by": "auto",
  "container_attrs": ["nwjs"],
  "mtool_supported": true,

  "target_exe": "開始游戏.exe",
  "target_exe_candidates": ["開始游戏.exe", "Game.exe"],
  "target_exe_decided_by": "human",
  "ambiguous": false,

  "mtool_mode": "default",
  "cli_template": "",
  "load_timeout_minutes": null,
  "translation_source": "mtool_online",

  "success_markers": {
    "files": ["version.dll"],
    "manifest_delta": true,
    "visual": ["Translation Complete!", "翻译完成"]
  },

  "mtool_build": null,
  "confirmed_at": null,
  "confirmed_by": null
}
```

字段契约与可信度：

| 字段 | 取值 | 可信度 |
|---|---|---|
| `engine` | 7.6.3 表中的枚举名，或 `UNKNOWN` | — |
| `target_exe` | `game_root` 下的相对文件名 | — |
| `mtool_mode` | `default` ｜ `passive_inject` ｜ `wait_external` —— 对应 MTool 的默认注入 / 被动注入 / 等待外部游戏并注入 | 【社区】https://bbs.mtool.app/topic/1295/ ；**三个模式在当前 MTool 界面上的确切名称与位置未经实测**【待测】 |
| `cli_template` | **默认空字符串 = 关闭**。启用时形如 `"{mtool}" --dp="{exe}"` | 见 7.6.7 |
| `load_timeout_minutes` | `null` = 用全局公式；显式数字 = 覆盖 | 【待测】，见 7.6.8 |
| `translation_source` | `mtool_online`（MTool 自己机翻）｜ `external_json`（导出 `ManualTransFile.json` → 外部翻译 → 加载回来） | 【官方】导出/加载两次点击的流程见 AiNiee wiki 与 JSON-translator-for-Mtool README |
| `success_markers` | 见 7.6.10 | 全部【待测】 |
| `mtool_build` | **运行时填写**的 MTool 构建标识（PC 发布页只有 build 标识、无版本号，且工具会自动更新） | 【官方】https://trs.mtool.app/release.php?lang=chs 。**SPEC 不写死任何 MTool 版本号**；实际使用的 build 同时记进 `state.json` |

#### 7.6.7 CLI 参数只作 best-effort 加速，默认关闭

MTool **开发者本人在官方论坛**回答「启动命令是否有参数」：**「有，但仅限『能正确自动检测引擎的前提下』」**，并给出三种参数形式：`--dp=` 开头的数据/引擎路径参数、`"xxx.exe"` 带引号的可执行文件路径、`D:\path\to\xxx.exe` 盘符开头的绝对路径。【官方】（官方论坛开发者发言）https://bbs.mtool.app/topic/850/

同一帖里的**用户实测反馈**：对一个 RPG Maker MV 游戏执行 `.\MTool.exe --dp="I:\my\game\path\Game.exe"`，**只启动了 MTool 本身，没有启动游戏**。【社区】

```
契约：
  cli_template 默认 ""（关闭）。
  启用时，CLI 只承担「把 MTool 拉起来并尽量预选 exe」这一件事。
  **成功与否一律由 Computer Use 在界面上确认**，绝不因为进程起来了就判定成功。
  ✗ 不得把 CLI 当成「无 GUI 批处理」的依据 —— 没有任何证据表明存在
    --translate / --load-file / --exit 之类的完整无人值守动作。
  参数名 --dp= 是未公开接口，随时可能变；它只能活在 profile 的可配置字段里，
  不得进入任何脚本的硬编码。
```

#### 7.6.8 加载耗时、超时与「前台被长期独占」

官方教程原文：加载「耗时几秒到几十分钟」，取决于游戏体积；期间「游戏和工具可能暂时无响应」；**必须保持游戏窗口在前台，不要最小化**。【官方】https://mtool.app/tutorial.php

超时按体积参数化（**静态公式 + 人可覆盖**，不做自适应）：

```
load_timeout_minutes
  = profile.load_timeout_minutes                    若显式给出
  = base_minutes + ceil(game_root_bytes / 1GB) × minutes_per_gb    否则

base_minutes    默认【待测】
minutes_per_gb  默认【待测】
```

> **【待测】不实测的后果**：填小了 → 在 MTool 正常工作时误判 `MTOOL_FAILED`，用户白等一场还得手工回滚；填大了 → 真卡死时前台被白白占用几十分钟，而 E1 下这段时间用户完全无法用电脑。
> **登记方法（一次即可）**：第一次跑 MTool 时用秒表记下「拖入 exe → 数据加载完成」的时长与 `game_root` 的体积，把两个默认值填进 `engine-rules.json`。**用两三个不同体积的游戏各测一次就够**，不需要拟合曲线。

**这段时间的性质必须在运维说明里写清楚**：从拖入 exe 到加载完成，Computer Use 独占前台（F1/F3/F5），用户既不能用鼠标也不能把窗口最小化。这就是 7.1.4 把 C-mtool 单独成一类会话、且单次只做 1 个条目的全部理由。

#### 7.6.9 幂等与重入

MTool 采用 **DLL Search Order Hijacking**：把 `version.dll` 放进**游戏根目录**，以此 hook 进游戏进程，**不改动被加密的游戏资源**。【社区】（Kaspersky 官方论坛误报申报帖，含版主确认）https://forum.kaspersky.com/topic/false-positive-mtool-translation-tool-components-versiondll-incorrectly-flagged-as-dllhijack-58061/

```
ENTER_MTOOL(item):
    if exists(game_root\version.dll):
        → 判定「已注入」，**不重复注入**
        → 若 state == TRANSLATING（上次会话被中断）：只走「确认翻译加载结果」的短流程
        → 若 state == READY_FOR_TRANSLATION：说明这个游戏此前已被处理过，
          落事件并交人裁决（R-08 的一个变体：重跑还是跳过）
    else:
        → 写 TRANSLATING（落盘）→ 正常注入流程
```

**不重复注入**是硬要求：叠加注入的后果本轮无任何资料【待测】，而 I1（停机态不删文件）意味着我们也没有干净的回滚手段。

#### 7.6.10 完成判据：**不要依赖产物文件**

新版 MTool 自己机翻的产物是 **`.bin` 而不是 `.json`**，而**具体落盘位置查不到**。【社区】https://bbs.mtool.app/topic/1206/ ｜ https://bbs.mtool.app/topic/433/

```
✗ 完成判据不得包含「某个 .bin 文件出现在某个路径」——那个路径没人知道。
✗ 完成判据不得硬编码中文文件名「与工具一同启动.bat」——它几乎肯定随
  MTool 的界面语言变化，把一个本地化字符串写进判据是脆的（本 SPEC 拒绝把本地化字符串写进机器判据）。
```

采用的三条判据：

| 记号 | 判据 | 机器可判定 | 可信度 |
|---|---|---|---|
| **M1** | `game_root\version.dll` 存在 | ✅ | 【社区】Kaspersky 帖（版主确认） |
| **M2** | 相对 §6 落盘的 manifest 文件快照，出现了新的 `*.dll` / `*.bat` / `*.json` | ✅ | 设计规定；快照来源见 §6 |
| **M3** | Computer Use 在界面上观察到「Translation Complete!」/「翻译完成」 | 视觉 | 【官方】tutorial.php 有这句提示；**其在当前版本界面上的确切文案与位置**【待测】 |

裁决：

```
M1 ∧ M2 ∧ M3                 →  COMPLETE
M1 ∧ M2 ∧ ¬M3（超时）         →  MTOOL_FAILED
¬M1（注入压根没发生）          →  MTOOL_FAILED
界面与 profile 描述对不上
（找不到按钮 / 引擎选项变了）   →  WAITING_FOR_MTOOL_RULE
```

> **【待测】M1/M2/M3 三条都需要 Windows 实测确认**（产物的实际文件名、是否随语言变化、成功提示的确切文案）。
> **不实测的后果**：C 只能靠 M3 的视觉判定单腿走路，而视觉判定是本流水线里唯一无法回放、无法测试的判据——一旦 MTool 换界面，失败会表现为「模型说成功了但其实没翻译」。**登记方法**：跑通第一个游戏后，对 `game_root` 做一次注入前/注入后的目录 diff，把实际出现的文件名填进 `success_markers.files`。

#### 7.6.11 Defender：SHA-256 白名单对 MTool 原理上无效

Kaspersky 版主确认的关键事实：MTool 的 `version.dll` **在不同游戏实例下哈希不同**，因此「逐个加排除项无效」。【社区】（同上 Kaspersky 帖）

```
阶段一（A/B）的归档与解压产物  →  人工放行按 **SHA-256** 白名单（哈希固定，可行）
阶段二（C）的 MTool 注入产物   →  SHA-256 白名单**原理上无效**
                              →  只能**目录级排除**：D:\GameLib\_stg\ + <MTool 安装目录>
游戏根目录                      →  **不排除**，保留实时保护
```

两条规则**必须在 SPEC 里分开写**，因为它们的适用对象不同，混写会让读者以为可以用一套办法解决。

**扫描时点钉死在 §6**：Defender 预检发生在**阶段一解压之后、阶段二之前**。阶段二产生的 `version.dll` / 启动器 / `ManualTransFile.json` 属于**已知豁免类别**，且**豁免必须是用户一次性明示同意**（R-13），写进 `rules\`，不由模型自作主张。`MpCmdRun` 官方明写**需要提权**【官方】Microsoft Learn，因此它属于 B 而非 A，C 完全不碰它。

#### 7.6.12 与 Translator++ / LinguaGacha 互斥

**被 MTool 注入过的游戏不能再用 LinguaGacha 处理**，且 MTool 导出的文本被按子句/短语切碎、翻译质量低于整句提取的路线。【社区】https://github.com/neavo/LinguaGacha/wiki/BestPracticeForRPGMaker

⇒ `translation.mode` 是**单选且事实上不可逆**。本 SPEC 只在 §10「未来演进」里提一句：若日后想走 Translator++ / LinguaGacha / RPGMakerTrans CLI 路线，必须在 MTool 注入**之前**决定；已注入的游戏只能重新解压一份干净副本。**正文不为这条路线设计任何分支。**

---

### 7.7 人工接力点清单

这张表是**给用户照着准备的**。每一项都写清「谁触发 / Codex 停在哪 / 你要做什么 / 做完怎么继续 / 多久遇到一次」。

| 编号 | 接力点 | 归属 | 触发时机 | 停在哪 | 你要做什么 | 做完怎么继续 | 频次 |
|---|---|---|---|---|---|---|---|
| **R-01** | 网盘登录 | C | 打开网盘客户端/网页，未登录 | 会话内暂停（不落停机态） | 扫码或输账号密码，登录完成 | 在会话里回一句「好了」，C 继续同一条目 | 每个网盘一次（会话保持期内） |
| **R-02** | 提取码 | C | 分享页要求输入提取码，而 该链接的 `code_ref` 为 null | 会话内暂停 | 从资源页找到提取码并填入 | 同上；C 顺手把它写进 `cloud_links[].code_ref` | 每条链接一次 |
| **R-03** | 人机验证 / CAPTCHA | C | 网站要求验证。官方：「有些网站会屏蔽自动化浏览器，或要求完成 CAPTCHA 验证」【官方】docs/browser | 会话内暂停 | 人工完成验证 | 同上 | 不定 |
| **R-04** | 转存确认 | C | 「保存到我的网盘」需确认目标目录/覆盖策略 | 会话内暂停 | 确认转存 | C 写 `transferred_at` 后继续 | 每条目一次（百度/夸克/阿里） |
| **R-05** | 资源页密码确认（P5） | C | 正则从资源页抽到密码候选 | 会话内暂停（几秒） | 看一眼、说「对」或「不对」 | 确认 → vault 里 `confirmed:true`；不确认 → 条目照常推进，将来在 B 落 `WAITING_FOR_PASSWORD` | 每个新密码一次 |
| **R-06** | 会员 / 付费 / 限速 | C | 网盘要求开通会员或提示限速 | 会话内暂停 | 自行决定是否付费；或改用另一条 `cloud_links` | 回话继续；若放弃该链接，标记后 C 换下一条链接 | 不定 |
| **R-07** | 覆盖已有文件 | C | 网盘或客户端提示目标已存在（多为 7.1.5 的转存重入） | 会话内暂停 | 判断是不是同一个包；**默认选「不覆盖」** | 已存在 → 跳过转存直接起下载 | 罕见 |
| **R-08** | 多 exe 歧义 / 找不到 exe | C | 候选 exe 数量 ≠ 1（7.6.4） | **`WAITING_FOR_MTOOL_RULE`** | 从报告里的候选表挑一个（注意真实反例：正确答案可能不是 `Game.exe`） | 把选择写进 `mtool-profiles\<item-id>.json` 的 `target_exe`，然后把条目送回 `READY_FOR_TRANSLATION` | 每个歧义游戏一次 |
| **R-09** | 新引擎 / `UNKNOWN` / 界面变化 | C | 判据表不命中、多目录命中、或 MTool 界面与 profile 对不上 | **`WAITING_FOR_MTOOL_RULE`** | 人工判断引擎；若需要长期支持，显式编辑 `engine-rules.json` | 同 R-08 | 每个新引擎一次 |
| **R-10** | 安装器 / 镜像 | B | 解出 NSIS / Inno / `setup.exe` / `.iso` 等 | **`WAITING_FOR_MANUAL_INSTALL`** | 手工安装或挂载，把游戏放进 `games\<dirname>\` | 人工把条目推到 `SCANNED`（§4 停机态手册） | 不定 |
| **R-11** | 百度残留后缀 | A→人 | `_inbox` 里出现 `*.baiduyun.p.downloading` 且长时间静默（7.5.1） | **`WAITING_FOR_RULE`** | 到客户端确认任务真的完成，然后**手工**去掉后缀 | 文件以最终名出现后，A 下一轮重新走 G1..G6 | 视百度客户端行为，可能常见 |
| **R-12** | 首次登记网盘临时后缀 | 人（一次性） | 首次使用夸克/阿里/123 客户端 | 无（前置准备） | 下一个大文件，下载中与下载后各 `Get-ChildItem -Force` 一次，比较差集；把后缀填进 `netdisk-profiles.json`，`confidence` 改 `observed` | 无需恢复；此后该网盘的 G1 才真正有效 | 每个网盘客户端一次 |
| **R-13** | 首次设置 Defender 排除 | 人（一次性） | 部署时 | 无（前置准备） | 只排 `D:\GameLib\_stg\` 与 `<MTool 安装目录>`，**不排整个库**；同时明示同意 MTool 注入产物的豁免类别 | 记录进 `rules\` 与部署日志，可回滚 | 一次 |
| **R-14** | Defender 裁决 | B | 扫描检出（B 级疑似误报 / C 级扫描失败） | **`THREAT_SUSPECTED`** | 看报告里的威胁名与文件，判定误报还是真威胁 | 误报 → 按 SHA-256 放行，回到主线；真威胁 → 落 `THREAT_CONFIRMED`（硬停）或 `ABANDONED` | 汉化/破解类资源上并不罕见 |
| **R-15** | 不明可执行文件 | B | 产物里出现未知 MZ 形态、且不属于已知引擎 | **`WAITING_FOR_MANUAL_INSTALL`** | 人工判断这是游戏本体、安装器还是别的 | 同 R-10 | 不定 |

**读表须知（三条）**：

1. **「会话内暂停」不落停机态**——它只在 C 会话活着的时候有意义，人一句话就过。**只有 `WAITING_FOR_*` 才是会跨会话保留的停机态**，它们的完整「触发条件 / 人工动作 / 恢复后回到哪 / 谁能解除」四列在 §4 的停机态手册里，本表只给操作视角。
2. **R-08 / R-09 是「每个游戏一次」，不是「每次运行一次」**——裁决结果落进 `mtool-profiles\<item-id>.json` 后永久生效。
3. **R-12 / R-13 是部署期的一次性动作**，它们与「录入并确认密码后启动解压」「Defender 预检的提权确认」一起，构成 C1 里那句「阶段一需要人工做的一部分」的确切内容（D-14，见 §12 决策台账）。

---

### 7.8 C 的前置条件

C 会话开始前必须全部满足。检查项由 `Preflight.ps1` 输出（§9），**不满足就不要启动 C**——C 撞上前台模态框的代价是整个会话作废，而用户已经把机器让出来了。

| # | 前置条件 | 为什么 | 可信度 | 不满足会怎样 |
|---|---|---|---|---|
| **PC-1** | 目标游戏目录已 `Unblock-File` | 带 ZoneId=3 的 exe 首次运行会弹 SmartScreen 的「Windows protected your PC」**前台模态框**，需要点「更多信息 → 仍要运行」 | 【官方】MOTW 触发 SmartScreen 额外检查（Microsoft）；`Unblock-File` **幂等、对未标记文件无副作用**，可整目录批量跑：https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.utility/unblock-file | Computer Use 撞上模态框卡死。**「解除标记」是 C 的前置条件，不是可选收尾** |
| **PC-2** | 库根不在 OneDrive 同步目录下 | 把 MTool 或游戏放在 OneDrive 里会触发 inject 失败 | 【社区】https://bbs.mtool.app/topic/949/ | 注入失败，且失败原因不会指向 OneDrive，排查成本极高 |
| **PC-3** | 库根不在 `Program Files` 下 | MTool 必须往游戏根目录写 `version.dll`；`Program Files` 需要提权，而 F4：Computer Use **无法以管理员身份验证** | 【社区】写入行为有 Kaspersky 帖佐证；权限推论 | 注入写不进去 |
| **PC-4** | MTool 已安装，且**可执行文件的签名状态已知** | Windows Computer Use 的应用白名单 `computer_use.windows.exes` 按「**已验证的发布者与签名版本信息，而不是路径或当前文件名**」匹配；**未签名**的可执行文件落 `computer_use.default_app_access` 兜底；**签名身份无法明确验证的直接被 block** | 【官方】config-reference | **MTool 的签名状态未知**【待测】。若属「签名无法明确验证」，C 根本操作不了 MTool，整个 7.6 落空。**这是本节风险最高的一条**，应在落地路线里最先验证 |
| **PC-5** | Codex **桌面 App**（不是 CLI、不是 IDE 扩展）；Computer Use 插件已安装并开启 | 「Codex CLI 和 Codex IDE 扩展均不支持浏览器功能。请打开 ChatGPT 桌面应用，使用内置浏览器。」；Computer Use 需安装插件 | 【官方】docs/browser、docs/computer-use | C 无法启动 |
| **PC-6** | 全局下载目录已设为 `D:\GameLib\_inbox`；**关闭**「询问下载文件的保存位置」 | 7.3.1：只有全局设置这一条路 | 【官方】docs/browser | 文件落到「下载」文件夹，A 认领不到，整批卡在 `AWAITING_DOWNLOAD` |
| **PC-7** | 若走 @Chrome/@Edge 扩展：扩展已装、已在「设置 > 计算机使用」里显示 **Manage**，且**该浏览器自身**的下载目录也已设为 `_inbox` | 扩展驱动的是用户自己的浏览器，用的是那个浏览器的下载设置 | 【官方】docs/chrome-extension.md | 同 PC-6 |
| **PC-8** | 网盘客户端的下载目录已设为 `_inbox`，且已登录 | 客户端有自己的下载目录设置，绕开浏览器 | 设计规定 | 同 PC-6 |
| **PC-9** | 该条目的 `mtool-profiles\<item-id>.json` 存在且 `ambiguous == false`（仅 C-mtool 会话） | 7.6.4 / 7.6.5 | 设计规定 | 条目本来就在 `WAITING_FOR_MTOOL_RULE`，不会进 worklist |
| **PC-10** | `D:` 卷是 NTFS | MOTW / `Zone.Identifier` 存放在 NTFS 的备用数据流里；exFAT 上整个 MOTW 话题消失（PC-1 随之失效，也失去 SmartScreen 这道防线） | 【官方】ADS 机制 + 【社区】exFAT 降级 | PC-1 的检查要能优雅降级而不是报错 |

> **PC-4 单独强调一次**：本 SPEC 的 §7.6 整节都建立在「Computer Use 能操作 MTool」这个尚未验证的前提上。如果实测发现 MTool 因签名问题被 block，7.6 需要整节改写（退路只剩「人手工跑 MTool，C 只负责判定与记账」）。**这一条必须排在 §10 落地路线的最前面验证，而不是等到写代码时才发现。**

---

> ### ⚠️ 遗留异议（起草者对某条锁定决策的保留意见，主控未采纳）
>
> **异议 1（位置）：`mtool-profiles\<item-id>.json` 不应放在 `D:\GameFlow\rules\` 下。**
> `rules\` 按 §2 的目录布局是**进 Git** 的代码/规则区，而 mtool profile 里含 `game_root` 绝对路径、`target_exe`（常是日文或中文游戏名）、以及 `item_id` 与游戏标题的对应关系——这些是**用户私人游戏库的内容**，进 Git 与 §9 的隐私边界冲突（`.gameflow\` 那条「不存密码、账号、token」的克制，在这里被从侧门绕过了）。
> 更根本的是**语义**：`engine-rules.json`、`container-types.json`、`netdisk-profiles.json` 是**跨条目复用的静态资产**，而 mtool profile 是**某一个游戏条目的一次性人工裁决记录**，它的生命周期与那个游戏完全一致（游戏删了 profile 就该没了）。
> **建议**：移到 `D:\GameLib\games\<dirname>\.gameflow\mtool-profile.json`，或至少移到 `D:\GameLib\` 下的不进 Git 区；`rules\` 里只保留跨游戏复用的**引擎默认值模板**。
> 本节已按契约原样写作 `D:\GameFlow\rules\mtool-profiles\<item-id>.json`，请主控裁决。
>
> **异议 2（职责悬空）：百度网盘残留后缀的「去后缀」动作，在当前契约下无人可执行。**
> 不变量 I5 规定「执行体 A 永不写 `games\<dirname>\` 以外的路径」，而残留文件在 `D:\GameLib\_inbox\`；7.2.2 规定 C 不改名；§6 的 B 的职责起点是 `PREFLIGHT_OK` 之后的归档处理。于是 R-11 只能由**用户手工**完成，这在百度是主力通道时可能相当频繁。
> 建议主控在 §5 或 §6 里显式指定一个承接方（我倾向 B：它本来就带密码、带提权、且是人触发的），或明确接受「这一步永远手工」并写进 §7.7 的频次列。本节已按后者写作。


---

## §8 Codex 操作面与 PowerShell 工程约定

本节定义两件不同的事，各自的失败后果也不同：

- **操作面**（§8.1–§8.7）：人和 Codex 按哪几个按钮、按钮背后是什么形状的进程调用、Codex 的沙盒/审批/指令层要怎么配。这一层配错了，**流水线跑不起来**（第一次写文件就被沙盒挡住、或者每解压一个包要人点一次审批）。
- **工程约定**（§8.8）：脚本内部怎么读写文件、怎么调外部进程、怎么落盘。这一层写错了，**流水线跑得起来但会损坏真实数据**（中文游戏名读成乱码 → 建出乱码目录 → 全盘错位；退出码判宽一档 → 删掉不该删的源包）。

两层的共同纪律是同一条：**形状固定、参数受限、失败显式**。凡是需要"当场判断一下"的地方，本节都把它换成一个写死的判据或一个具名的停机点。

---

### 8.1 五个固定操作（做成 Codex skills）

#### 8.1.1 为什么是 skills，不是 custom prompts，也不是 `.codex/` actions

- **custom prompts 已被官方废弃**。`docs/custom-prompts.md` 开篇：*"**Custom prompts are deprecated.** Use skills for reusable instructions that Codex can invoke explicitly or implicitly."* 并补一句它们"live in your local Codex home directory … so they're **not shared through your repository**"。【官方】https://learn.chatgpt.com/docs/custom-prompts.md
- **`.codex/` 不是 skills 目录**。`docs/environments/local-environment` 原文：*"Codex 会将此配置存储在项目根目录下的 `.codex` 文件夹中。"* 它装的是本地环境的 **setup 脚本**与 **actions**；而 actions *"会在应用的集成终端中运行"*，setup 脚本才*"在智能体环境中运行"*。【官方】https://learn.chatgpt.com/zh-Hans/docs/environments/local-environment
  → 两条路径的**权限模型不一样**：actions 跑在集成终端（不受 agent 沙盒管辖），skills 跑在 agent 沙盒里。把五个操作做成 actions 会得到一个"在按钮里能跑、在对话里跑不了"的分裂系统，且 actions 不进 Git、不可评审。**本项目一律用 skills。**
- skills 的加载位置（官方表格）：`$CWD/.agents/skills`、`$CWD/../.agents/skills`（在 Git 仓库内时）、`$REPO_ROOT/.agents/skills`、`$HOME/.agents/skills`、`/etc/codex/skills`。【官方】https://learn.chatgpt.com/docs/build-skills.md
  另有一个官方表格未列、但实际存在的位置 `$CODEX_HOME/skills`（Windows 上即 `%USERPROFILE%\.codex\skills\`）【实测：macOS 上 `ls ~/.codex/skills/` 有内容，且 codex 二进制 `strings` 命中 `Installs into $CODEX_HOME/skills/<skill-name>`；**能否外推到 Windows**：`%USERPROFILE%\.codex` 是官方指定的 Windows CODEX_HOME【官方 windows-app】，路径形式可外推，但**该位置在 Windows 版是否同样被扫描属【待测】**】。
  → **本项目钉死 `D:\GameFlow\.agents\skills\`**：随代码进 Git、可评审、可回滚；`%USERPROFILE%\.codex\skills\` 只作为"目标机上找不到 skill 时"的排查线索，不作为安装位置。

#### 8.1.2 命名、触发与目录形状

```
D:\GameFlow\.agents\skills\
  gf-new-batch\   SKILL.md   agents\openai.yaml
  gf-sweep\       SKILL.md   agents\openai.yaml
  gf-work\        SKILL.md   agents\openai.yaml
  gf-reconcile\   SKILL.md   agents\openai.yaml
  gf-foreground\  SKILL.md   agents\openai.yaml
```

`SKILL.md` 必须有 `name` + `description` 的 YAML front matter【官方 build-skills】。**五个 skill 一律关闭隐式触发**：

```yaml
# agents/openai.yaml
policy:
  allow_implicit_invocation: false
```

> `allow_implicit_invocation` (default: `true`): When `false`, Codex won't implicitly invoke the skill based on user prompt; explicit `$skill` invocation still works. 【官方】https://learn.chatgpt.com/docs/build-skills.md

理由是本项目的品味底线：`gf-work` **会删除文件**、`gf-foreground` **会独占前台鼠标键盘**。让模型"觉得任务匹配"就自己调起来，与"人在环内"直接冲突。关闭隐式触发后，唯一的触发方式是人打 `$gf-work`。

skill 的 `name` 用 ASCII slug（`gf-work`），正文与报告里一律用中文操作名（"作业"）。理由：显式触发要打 `$name`；**中文 skill 名能否被 `$` 正确触发属【待测】**，不验证的后果只是"打不出来这个 skill"，代价低但会当场卡住第一次使用。

`description` 要**前置触发词、写短**：官方说 skill 列表最多占模型上下文的 2%（未知上下文时 8000 字符），skill 多时 Codex *"shortens skill descriptions first"*，甚至 *"may omit some skills"*。【官方】五个 skill 远未到这个量级，但 `description` 里不要复述流程——流程写在 SKILL.md 正文。

#### 8.1.3 操作面全图

```
        人（在场）                                    Windows 任务计划（无人值守）
            │                                                    │
  ┌─────────┼──────────────┬───────────────┐                     │
  │         │              │               │                     │
$gf-new-batch  $gf-work  $gf-foreground  $gf-reconcile            │
 新建批次      作业(B)     前台处理(C)      批次对账               │
  │           │              │               │                   │
  ▼           ▼              ▼               ▼                   ▼
New-Batch   Invoke-Work   （无脚本：       Get-BatchReport    Invoke-Sweep
  .ps1        .ps1         Computer Use      .ps1              .ps1  巡检(A)
  │           │            + 内置浏览器）     │                   │
  │           │              │               │                   │
  └───────────┴──────┬───────┴───────────────┼───────────────────┘
                     ▼                       ▼
   games\<dirname>\.gameflow\events.jsonl   _reports\<batch-id>-<seq>.md
                （唯一真相 I2）              （派生物，随时可重生成）

                             $gf-sweep（手动催一遍巡检，与计划任务共用同一入口）
```

#### 8.1.4 五个操作的契约

| 操作 | skill | 脚本 | 输入 | 人必须在场 | 会删文件 | 会用密码 | 需提权 |
|---|---|---|---|---|---|---|---|
| 新建批次 | `gf-new-batch` | `New-Batch.ps1` | `-BatchId <slug>` | ✅ | ❌ | ❌ | ❌ |
| 巡检 | `gf-sweep` | `Invoke-Sweep.ps1` | `[-BatchId <slug>] [-Unattended]` | ❌ | ❌（I5） | ❌（I5） | ❌ |
| 作业 | `gf-work` | `Invoke-Work.ps1` | `-BatchId <slug> [-ItemId <slug>]` | ✅ | ✅ **唯一** | ✅ | ✅ |
| 批次对账 | `gf-reconcile` | `Get-BatchReport.ps1` | `-BatchId <slug>` | ❌ | ❌ | ❌ | ❌ |
| 前台处理 | `gf-foreground` | 无（GUI） | `-BatchId <slug>`（语义参数） | ✅ | ❌ | ❌ | ❌ |

##### （1）新建批次 `gf-new-batch`

- **干什么**：把人给的一组资源页 / 网盘链接整理成 `D:\GameLib\batches\<batch-id>.json`（模板见 `templates\batch.template.json`，字段见 §3），为每个条目生成 `item.id` slug。**只生成清单**：不建游戏目录、不访问网络、不下载。
- **输入**：命令行只有 `-BatchId <slug>`。**标题、URL、密码引用这些自由格式数据不走命令行**——由 skill 引导 Codex 直接把 JSON 写进 `batches\<batch-id>.json`，脚本负责校验（schema、slug 唯一性、`expected_parts` 类型）。这是 §8.2 R3 的第一处应用。
- **前置状态**：批次不存在。同名已存在 → **拒绝，不覆盖**（退出码 2）。
- **副作用**：写 1 个文件（`batches\<batch-id>.json`）。不碰 `games\`。
- **失败姿态**：slug 不匹配 §3.2.1 / §3.2.2 的正则、模板缺失、schema 校验失败 → 退出码 2/3，**不落任何状态、不留半个文件**（走 §8.8.4 的写临时→替换协议）。
- **人在场**：**必须**。人要提供链接、确认标题与 slug、确认 `translation.mode`。

##### （2）巡检 `gf-sweep`

- **干什么**：跑一次执行体 A（§5）：扫描 `_inbox` 与 `games\*`、按批次清单认领归位、下载完成判定 G1∧…∧G6、只读 `7z l -slt` 预检、卷集连续性、写状态、**生成对账报告**（I5 白名单第 ② 项）。
- **输入**：`[-BatchId <slug>]`（缺省 = 全部活跃批次）、`[-Unattended]`（计划任务传，影响的只有日志里的 `trigger` 字段与是否输出人读摘要，**不影响任何判据**）。
- **前置状态**：**无**。A 必须在任何状态下都可重跑（§8 的无状态批处理要求）。
- **副作用**：追加 `events.jsonl`、重写 `state.json` / `manifest.json`、把认领到的文件从 `_inbox` 移进 `games\<dirname>\`。写集合**恰为** I5 白名单的三处：① `games\<dirname>\`（含 `.gameflow\`）；② **新建** `_reports\<batch-id>-<seq>.md`（**不得覆盖已存在的报告**）；③ 把 `_inbox\` 中已认领的文件**移入** ①（同卷 rename，不构成删除语义）。**永不删除任何文件、永不提权、永不解析密码、不回收 `_stg\`（只在报告里列出孤儿）**（I1/I5）。
- **失败姿态**：拿不到条目锁 → 该条目跳过并记 `LOCKED_BY_ANOTHER_RUN`（运行期状况，不落盘、不进状态机，§4.2）；拿不到批次锁 → 整个进程退出码 75，**不等待、不重试**。单条目异常只影响该条目（I4）。
- **人在场**：**否**。这是五个操作里唯一无人也能跑的一个；作为 skill 触发时它只是"手动催一遍"，正常调度走 §8.6 的 Windows 任务计划，**两条路径共用同一个入口脚本**，互斥由 §8.8.6 的文件锁保证。

##### （3）作业 `gf-work`

- **干什么**：跑一次执行体 B（§6）：密码链 P0→P6、`7z t`、`7z x` 到 `_stg\<8hex>\`、递归解包（`max_depth=5`）、逐条目校验、Defender 预检、staging→落地、按 §6.8 的 12 条与条件删源包。
- **输入**：`-BatchId <slug>`，可选 `-ItemId <slug>` 只跑一个条目。
- **前置状态**：条目处于 `PREFLIGHT_OK`，或从某个停机态被人解除后回到的可推进状态（迁移表见 §4）。**不在可推进状态的条目直接跳过**，并在结束摘要里逐条列出跳过原因——不是静默忽略。
- **副作用**：本项目**唯一**会删除文件的操作；把密码明文经 `-p` 送上命令行（同机进程与系统审计可见，无法消除，见 §9 诚实局限）；Defender 预检需要提权（`MpCmdRun` 官方明写需提权）。
- **失败姿态**：12 条判据任一不满足 → **保留所有压缩包**，落对应停机态（`WAITING_FOR_PASSWORD` / `EXTRACT_FAILED_AMBIGUOUS` / `CORRUPT` / …），写日志，继续下一个条目。
- **人在场**：**必须**，两条硬理由：① DPAPI vault 只在**创建它的交互登录身份**下可解，无人值守的 S4U 任务解不开（§8.8.10）；② `MpCmdRun` 需提权，与 `-RunLevel Limited` 的计划任务冲突。
- **skill 正文的额外要求**（写进 `SKILL.md`）：调用脚本前必须先向人复述"本批次将处理 N 个条目、其中 M 个已满足删除前置、本次运行可能删除源包"，由人确认一次再执行。这不是礼貌，是 C3+C4 下**删除不可撤销、无回收站兜底**的唯一人工闸门。

##### （4）批次对账 `gf-reconcile`

- **干什么**：**只读**。**重新生成 / 回填**对账报告（常规报告由 A 在每轮巡检末尾产出，见（2））：从 `events.jsonl` / `state.json` 派生每条目当前状态、需要人做什么、本批次至今删了什么。用在「想立刻看一眼而不等下一轮巡检」或「A 那轮的报告需要重算」时。
- **输入**：`-BatchId <slug>`。
- **前置状态**：批次清单存在。
- **副作用**：只写 `D:\GameLib\_reports\<batch-id>-<seq>.md`。**不改任何状态、不追加任何事件。**
- **失败姿态**：批次不存在 → 退出码 2。**某个条目的 `state.json` 不可解析时，报告把该条目标注为"状态不可读"并给出 §4 的恢复指引，然后继续渲染其余条目**——对账绝不能因为一个条目坏了就整体失败（这不引入新状态名，只是报告里的一个单元格）。
- **人在场**：否（输出是给人看的，但生成过程不需要人）。

##### （5）前台处理 `gf-foreground`

- **干什么**：跑一次执行体 C（§7）的短会话：资源页解析、三类网盘转存与下载、MTool 加载翻译、最终验收。
- **脚本**：**无**。C 的动作是 GUI 操作（内置浏览器 + Computer Use），不是脚本。它读 `batches\<batch-id>.json` 与最近一份报告决定做什么，并把 `expected_parts`（资源页上一共几个下载链接 / 几个分卷）等信息**写回批次清单**——这是 §5.3 G6 唯一可靠的自动检测输入。
- **前置状态**：人在电脑前，且机器可以被独占。
- **副作用**：**独占前台鼠标键盘**。官方原文：*"在 Windows 上，计算机使用在活动桌面运行。**您无法在它于后台运行的同时继续使用同一 Windows 会话**"*；*"锁定状态下使用适用于 macOS。在 Windows 上，计算机使用在前台运行。"*【官方】https://learn.chatgpt.com/zh-Hans/docs/computer-use
  下载文件落到 `D:\GameLib\_inbox`（内置浏览器**没有按任务/按游戏的下载目录**，§8），由 A 认领归位。
- **失败姿态**：任何登录、验证码、人机验证、会员/付费环节 → 停下交给人（不绕过）；MTool 目标 exe 歧义或新引擎 → `WAITING_FOR_MTOOL_RULE`；加载失败 → `MTOOL_FAILED`。
- **人在场**：**必须**，且必须**可随时中断**（E1：这是主力机）。skill 正文要求把工作切成"一个资源页/一个游戏"的粒度，每完成一个就写回清单，使中断只损失当前一小步。

---

### 8.2 脚本入口约定

#### 8.2.1 事实：最外层永远是 Windows PowerShell 5.1

Codex 的 Windows 原生 agent 用 `powershell.exe -NoProfile` 执行命令【实测：对 codex 二进制 `strings -a … | grep -iE '^powershell\.exe$|-NoProfile'` 同时命中两者；这是在 macOS 上对同一份跨平台二进制做的，命中的是**编进二进制的 Windows 分支**，可外推到 Windows】，与官方表述一致：*"默认情况下，ChatGPT 桌面应用使用 Windows 原生 Codex 智能体，因此智能体会在 PowerShell 中运行命令。"*【官方】https://learn.chatgpt.com/zh-Hans/docs/windows/windows-app

`powershell.exe` 即 **Windows PowerShell 5.1**（7.x 的可执行名是 `pwsh.exe`）【官方】https://learn.microsoft.com/en-us/powershell/scripting/install/install-powershell-on-windows

5.1 的原生命令传参语义（官方 about_Parsing）：*"the arguments are **first parsed by PowerShell**. The parsed arguments are then **joined into a single string** with each parameter separated by a space."*【官方】https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.core/about/about_parsing

也就是说：**无论我们在脚本内部把 PowerShell 版本锁到多高，"Codex 决定跑什么命令"这一层永远按 5.1 的规则把参数拼成一个字符串。** 一个带空格的日文标题、一个含 `[` `]` 的路径，在这一层就可能被拆坏，而且拆坏之后脚本收到的是**一个语法合法但语义错误的参数**——最坏的一种失败。

调用链：

```
Codex agent
 └─ powershell.exe -NoProfile -Command "…"                    ← 5.1 解析规则，我们无法改变
     └─ pwsh.exe -NoProfile -File D:\GameFlow\scripts\Invoke-Work.ps1 -BatchId 2026-09-10-vn
         └─ 脚本内部：#Requires -Version 7.4，7.x 语义可用
             └─ Invoke-SevenZip → ProcessStartInfo.ArgumentList → 7z.exe
```

#### 8.2.2 入口契约（R1–R7，硬规则）

| # | 规则 | 理由 |
|---|---|---|
| **R1** | 入口只接收**无空格、无歧义的受限 token**。`BatchId` 必须匹配 §3.2.1 的 `batch_id` 正则、`ItemId` 必须匹配 §3.2.2 的 `item_id` 正则（**本节不另定正则**）。脚本第一件事就是正则校验，不匹配立即退出码 2 | 5.1 拼字符串的唯一安全解法是"参数里根本不存在需要引号的字符" |
| **R2** | **一切路径由脚本自己解析**。库根、代码根是 `scripts\lib\` 里的路径模块中的常量，是全项目**唯一**出现绝对盘符的地方；脚本自身位置用 `$PSScriptRoot` 反推 `D:\GameFlow` | 换盘符时只改一处；Codex 永远不需要在命令行里说出一个路径 |
| **R3** | **禁止**把标题、URL、密码、任意路径作为入口参数。自由格式数据一律经文件（批次清单 JSON）或 vault | 见 R1；密码另有 §9 的暴露面理由 |
| **R4** | 只用 `pwsh.exe -File`，**不用 `-Command`** | `-Command` 会让 pwsh 再做一次 PowerShell 解析，两层解析叠加；`-File` 后的参数按字面传给脚本【社区，理由自明】 |
| **R5** | 所有外部工具（`7z.exe`、`pwsh.exe`、`MpCmdRun.exe`）用**全路径**，或经 Preflight 确认在 `PATH` 里 | `-NoProfile` ⇒ `$PROFILE` 里的别名/函数在 Codex 面前**不存在**【官方 + 实测】 |
| **R6** | 布尔开关一律 `[switch]`，不接收值（写 `-Unattended`，不写 `-Unattended:$true`） | 5.1 拼串后 `:$true` 是另一个 token |
| **R7** | 入口脚本**不得依赖当前工作目录** | Codex 的 cwd 可能是 project 根，也可能是 worktree；计划任务的 cwd 又是另一个 |

#### 8.2.3 入口清单与退出码（契约）

| 脚本 | 允许的参数 | 备注 |
|---|---|---|
| `Preflight.ps1` | 无参数 | §9 |
| `New-Batch.ps1` | `-BatchId <slug>` | |
| `Invoke-Sweep.ps1` | `[-BatchId <slug>] [-Unattended]` | 计划任务与 `$gf-sweep` 共用 |
| `Invoke-Work.ps1` | `-BatchId <slug> [-ItemId <slug>] [-Phase <enum>] [-Retry] [-Resume] [-RetryAll] [-ForceRetest] [-Abandon] [-Reason <token>]` | `-Phase` ∈ `test`\|`extract`\|`verify`\|`scan`\|`promote`\|`delete`；重跑语义见 §4.7，`-Abandon` 见 §4.8 |
| `Get-BatchReport.ps1` | `-BatchId <slug>` | 只读 |

**本表是入口参数的封闭清单。** 任何脚本新增参数必须先改这张表。两条附加硬规则：

- **禁止把任意路径作为入口参数**（R2/R3）。测试用别的库根时走 `-Profile test`——由脚本从配置里解析出测试库根，**不接收 `-LibraryRoot` 这类自由路径**。理由与 R1 同源：最外层永远是 5.1 的解析规则，一个带空格或中文的路径在那里就已经被拆坏了。
- `-Reason <token>` 只接收**受限 token**（`^[a-z0-9][a-z0-9-]{0,31}$`），自由文本理由写进批次条目的 `_note`，不走命令行。

**进程退出码**（只表达"进程层结果"，与 §4 的状态机是两套东西；条目结果永远看状态与报告）：

| 码 | 含义 | 谁产生 |
|---|---|---|
| `0` | 本次调用正常结束。**不代表所有条目成功** | 全部 |
| `2` | 参数不合法（slug 不匹配、未知参数、批次不存在/已存在） | 全部 |
| `3` | 前置条件不满足（Preflight 失败：缺 `pwsh` ≥7.4、缺 `7z.exe`、`.gameflow` 与库不同卷、执行策略被 GPO 锁死） | 全部 |
| `70` | 内部错误（未捕获异常）。此时**必然**已写了一条 `level=error` 事件 | 全部 |
| `75` | 拿不到批次锁，立即退出（`LOCKED_BY_ANOTHER_RUN`） | Sweep / Work |

`75` 沿用 sysexits 的 `EX_TEMPFAIL` 习惯【社区】；选一个非 0/1/2 的固定值即可，重点是**它是写死的常量**，让计划任务与 skill 能确定地区分"别人在跑"和"真的错了"。

---

### 8.3 沙盒配置：双根问题

#### 8.3.1 问题

```
Codex project = D:\GameFlow        ← workspace-write 沙盒默认只允许写这里（代码、规则、skills）
        ✗ 写不进去
D:\GameLib\                        ← 所有真实数据：批次、状态、事件、游戏、报告
```

官方对 Windows 原生沙盒的表述：*"When you run Codex natively on Windows, agent mode uses a Windows sandbox to **block filesystem writes outside the working folder** and prevent network access without your explicit approval."*【官方】https://learn.chatgpt.com/docs/windows/windows-sandbox

→ 项目打开 `D:\GameFlow` 时，A/B 的**第一次写** `games\<dirname>\.gameflow\events.jsonl` 就会失败。

#### 8.3.2 建议配置

```toml
# %USERPROFILE%\.codex\config.toml
sandbox_mode = "workspace-write"

[sandbox_workspace_write]
writable_roots = ["D:\\GameLib"]

[windows]
sandbox = "elevated"          # 官方首选；需要管理员批准的一次性安装步骤
```

键的来源：`sandbox_mode`、`sandbox_workspace_write.writable_roots`、`[windows] sandbox`（取值 `elevated` / `unelevated`）三者均出自官方配置参考与 windows-sandbox 文档【官方】https://learn.chatgpt.com/docs/config-file/config-reference.md · https://learn.chatgpt.com/docs/windows/windows-sandbox
TOML 里的表写法是上述 key path 的机械展开；**在目标机上应以 Codex 实际读取的结果为准核对一次**【待测】。**不确定的配置键一个都不要写进 config.toml**——本节只列上面这三个，别的（`approval_policy`、`[[skills.config]]`、`computer_use.*`、`browser_use.*` 等）虽然在官方参考里存在，但本项目不需要它们参与沙盒决策。

三条必须知道的边界：

1. **`<writable_root>/.codex` 会被递归置为只读**：*"`<writable_root>/.codex` is protected as read-only when it exists as a directory. Protection is recursive."*【官方】https://learn.chatgpt.com/docs/agent-approvals-security
   我们的状态目录叫 `.gameflow`，**不冲突**。反过来这条也是一条禁令：**不要在 `D:\GameLib` 下建 `.codex` 目录**（例如"顺手放个项目级 rules"），否则整棵库瞬间只读。
2. **不要用 `danger-full-access` 绕过。** 沙盒是本项目"A 永不越界"（I5）的唯一机器级保障；用 full access 换方便，等于把 I5 降级成"脚本自觉"。
3. **`writable_roots` 在 Windows 原生沙盒下是否同样生效，属【待测】。** 官方 windows-sandbox 文档描述的是 Windows 专有的沙盒实现（低权限沙盒用户 / 受限 token + ACL 边界），而 `sandbox_workspace_write.*` 是通用配置族，**两者的关系文档里没有交代**。
   - 不验证的后果：A/B 在 Codex 会话里一步都跑不动，且**失败表现可能是一个普通的"拒绝访问"异常**，看起来像脚本 bug 而不是沙盒拦截，人会往错的方向排查很久。
   - 验证方法：配好后在 Codex 会话里让 agent 执行一次最小写入（`New-Item` 一个临时文件到 `D:\GameLib\_reports\`），看是被写入、被拒绝、还是弹审批。

#### 8.3.3 逃生路线（这条决定了整个设计的抗风险度）

如果 8.3.2 的第 3 条实测失败（`writable_roots` 在 Windows 原生沙盒下不生效），**执行体 A 仍然能跑**：A 的正常调度本来就走 Windows 任务计划（§8.6），那是一个**完全在 Codex 沙盒之外**的进程，不受 `sandbox_mode` 管辖。此时的降级形态是：

- A：照常由任务计划跑，不受影响。
- B：由人在普通终端里 `pwsh -File Invoke-Work.ps1 -BatchId …` 触发（B 本来就要求人在场）。
- Codex：退化为**只读角色**——读报告、帮人判断停机态、写批次清单（`D:\GameFlow` 内的操作不受影响）。

也就是说，**§8.3 的待测项失败只会削弱便利性，不会让流水线不可用**。这是刻意的：把不确定的能力放在便利层，把确定性放在任务计划层。

---

### 8.4 ExecutionPolicy

官方明写 Codex 生成的 `.ps1` 会撞上执行策略：

> 如果 Codex 为您创建 PowerShell 脚本，也可能发生这种情况。错误可能如下所示：`npm.ps1 cannot be loaded because running scripts is disabled on this system.` 常见的解决方法是将执行策略设置为 RemoteSigned：`Set-ExecutionPolicy -ExecutionPolicy RemoteSigned`
> 【官方】https://learn.chatgpt.com/zh-Hans/docs/windows/windows-app

但"默认策略下根本跑不起来"这句话说满了：Windows 客户端默认是 `Restricted`，可很多机器早已被设成 `RemoteSigned`；而计划任务侧我们用的 `-ExecutionPolicy Bypass` 是**进程级**的，本来就绕得开——**除非有 GPO**（`MachinePolicy` / `UserPolicy` 作用域优先级高于命令行参数）。

**契约**：Preflight 必须跑 `Get-ExecutionPolicy -List` 看**全部作用域**，按下表处置：

| Preflight 观测 | Codex 会话里 | 计划任务里（`-ExecutionPolicy Bypass`） | 处置 |
|---|---|---|---|
| `MachinePolicy`/`UserPolicy` = Undefined，有效策略 = `Restricted`/`AllSigned` | 跑不起来 | 可绕开 | 提示人执行 `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned` |
| 有效策略已是 `RemoteSigned` / `Bypass` | 正常 | 正常 | 无动作 |
| `MachinePolicy` 或 `UserPolicy` **非 Undefined**（GPO 强制） | 取决于 GPO 值 | **`Bypass` 无效** | Preflight 退出码 3，给人明确路径（联系 IT / 对脚本签名）。**本 SPEC 不提供任何绕过 GPO 的方案。** |

---

### 8.5 免审批：rules / prefix_rule 对 PowerShell 的语义空白

#### 8.5.1 已知的事实

- rules 用来控制 Codex **在沙盒外**能跑哪些命令，官方自述 *"Rules are experimental and may change."*；多条规则匹配时取**最严格**的决定（`forbidden` > `prompt` > `allow`）。【官方】https://learn.chatgpt.com/docs/agent-configuration/rules.md
- 规则写在 `~/.codex/rules/default.rules`（Starlark 语法），形如 `prefix_rule(pattern=["gh","pr","view"], decision="prompt", …)`。【官方】
- **复合命令的拆分只文档化了 bash 系**：官方明写 Codex 对 `bash -lc` / `bash -c` 及 `zsh`/`sh` 等价物做特殊处理，用 tree-sitter 拆成子命令；一旦脚本里出现重定向、替换、环境变量、通配、控制流，*"the entire invocation is treated as `["bash","-lc","<full script>"]`"*。【官方】
- **官方 rules 文档全文对 PowerShell 只字未提**：对 `en_agent-configuration_rules.md` 全文 grep `powershell|windows|pwsh|cmd\.exe` → **0 命中**【实测：在 macOS 上对官方 md 原文做的文本检索；检索对象是文档内容本身，与执行平台无关，**结论可外推**】。

#### 8.5.2 推论（标为推论，不是事实）与【待测】

Windows 上 agent 跑的是 `powershell.exe -NoProfile …`（§8.2.1），而拆分逻辑只覆盖 bash 系 ⇒ **极可能整条 PowerShell 调用被当成单一 invocation 来匹配 `prefix_rule`**，于是 `pattern = ["7z", "t"]` 这类写法**永远匹配不上**。【待测】

**不验证的后果**（这是本节最贵的一条）：一个批次十几个包，每个包 `l` / `t` / `x` 至少三次调用，几十次审批弹窗。执行体 A 的无人值守属性直接归零，执行体 B 从"人确认一次"退化成"人点几十次"——**A 存在的意义就没了**。

#### 8.5.3 验证方法（在 Windows 机器上做，写脚本之前做）

官方提供了本地验证工具（`codex execpolicy check`）【实测：本机 `codex execpolicy --help` 确有 `check  Check execpolicy files against a command` 子命令】。**按下面三条逐级收窄地跑**：

```
# ① 裸命令：确认规则语法本身是对的
codex execpolicy check --pretty --rules %USERPROFILE%\.codex\rules\default.rules -- 7z t D:\GameLib\_stg\demo\a.7z

# ② PowerShell 包一层：这是"rules 会不会拆 PowerShell"的核心问题
codex execpolicy check --pretty --rules %USERPROFILE%\.codex\rules\default.rules -- powershell.exe -NoProfile -Command "7z t D:\GameLib\_stg\demo\a.7z"

# ③ 我们真正会跑的形状：入口脚本
codex execpolicy check --pretty --rules %USERPROFILE%\.codex\rules\default.rules -- pwsh.exe -NoProfile -File D:\GameFlow\scripts\Invoke-Work.ps1 -BatchId demo
```

| 实测结果 | 结论 | 对策 |
|---|---|---|
| ② 能按 `["7z","t"]` 匹配上 | rules 会拆 PowerShell | 可以给 7z 写细粒度规则 |
| ② 匹配不上、③ 能按 `["pwsh.exe","-NoProfile","-File","D:\GameFlow\scripts\Invoke-Work.ps1"]` 匹配上 | 整条调用是单一 invocation | **正确的规则不是给 `7z` 写，是给入口脚本写** |
| ②③ 都匹配不上 | rules 对 Windows 不可用 | 走 §8.3.3 的逃生路线：A 归任务计划，B 由人在终端跑 |

第三行是本节与 §8.2 的交汇点，也是"入口只接固定形状"的**第二个理由**：一个形状固定、参数受限的入口（`pwsh.exe -NoProfile -File <固定路径> -BatchId <slug>`），比一条自由格式的 7z 命令行**更容易被 prefix_rule 精确放行，也更容易被人审计**——放行的是"这一个脚本"，不是"任意 7z 命令"。

#### 8.5.4 兜底设计：把审批面收敛到 1 次

B 的所有 7z 调用都发生在**同一个** `pwsh.exe -File` 子进程内部（§8.8.7 的 `Invoke-GfProcess` 起子进程）。若 Codex 只对它**直接**发起的进程做审批、不对脚本内部再起的子进程逐个审批，那么整个 B 对 Codex 而言就是 **1 次 invocation**，审批面从几十次收敛到 1 次。

**这条属【待测】**：沙盒/审批是进程级的、子进程通常继承，但官方**没有明说**脚本内部起的子进程是否会被重新拦截。若实测发现会逐个审批，B 只能由人在普通终端直接跑（B 本来就要求人在场，代价可接受）。

---

### 8.6 为什么阶段一调度用 Windows 任务计划，不用 Codex 的 Scheduled tasks

Codex 桌面 App 的计划任务能力**是真实存在的**（*"scheduled tasks can work with local projects and run in the project directory or an isolated worktree"*、*"Scheduled tasks use `approval_policy = \"never\"` when your organization policy allows it."*）【官方】https://learn.chatgpt.com/docs/automations.md 。我们不用它，**不是因为它不存在，而是四条理由**（照 §8 与仲裁裁定）：

| # | 理由 | 展开 |
|---|---|---|
| **1** | **确定性** | 执行体 A 是一个纯确定性的文件处理器。用一个 LLM turn 去驱动它，等于把确定性逻辑塞进不确定的载体，每次调度都要重新"理解"一遍该做什么，还要烧 token。这与本项目"引擎层确定性、静态、人在环内"的姿态正面冲突 |
| **2** | **依赖面** | Codex 计划任务的官方前提是：*"keep the machine powered on and the ChatGPT desktop app running. The selected project must still be available on disk"*——机器开机 **+ 桌面 App 常驻** + 项目目录在盘上 + 沙盒能写到库。Windows 任务计划只要求**用户登录着**（`LogonType Interactive`） |
| **3** | **E1：这是主力机** | 用户会关掉桌面 App、会重启、会被打断。任务计划是操作系统组件，不会因为关掉一个应用就停 |
| **4** | **沙盒** | Codex 计划任务 *"run unattended with your default sandbox settings"*，`workspace-write` 下**写不到 project 之外**，还要额外依赖 §8.3 那个【待测】的 `writable_roots`。Windows 任务计划根本没有这一层 |

**保留给 Codex 的两个角色**（这两条不受上面四条影响）：

- **五个 skill 作为人机接口**：`$gf-sweep` 手动催一遍、`$gf-reconcile` 出报告、`$gf-new-batch` 建清单。
- **B 与 C 的交互驱动**：需要密码、需要提权、需要 GUI 的那半，本来就必须人在场（§8.1.4）。

另记：桌面 App 计划任务的**最小间隔**官方未说明（只提到 chat 内任务可用 *"minute-based intervals"*）【待测，但因本裁决已降级为不影响设计】。

---

### 8.7 `AGENTS.md` 该写什么

#### 8.7.1 加载规则（决定了写在哪里才有用）

官方原文要点【官方】https://learn.chatgpt.com/docs/agent-configuration/agents-md.md ：

1. Codex 启动时构建一条指令链（*"once per run"*）。
2. **全局层**：`$CODEX_HOME`（Windows 上 `%USERPROFILE%\.codex`）里先读 `AGENTS.override.md`，否则 `AGENTS.md`，**只取第一个非空文件**。
3. **项目层**：从 **project root（typically the Git root）**一路走到 cwd，每个目录检查 `AGENTS.override.md` → `AGENTS.md` → `project_doc_fallback_filenames`，**每个目录至多取一个文件**；找不到 project root 时**只检查当前目录**。
4. **合并顺序**：从根往下拼接，**越靠近 cwd 的越优先**。
5. 跳过空文件；总量到 `project_doc_max_bytes`（**默认 32 KiB**）就停止追加。

对本项目的三条推论：

- `D:\GameLib` **不进 Git** ⇒ 没有 project root ⇒ 只检查 cwd 那一层 ⇒ `D:\GameLib\AGENTS.md` **只有当 cwd 恰好是它时才加载**。**不要把关键约束只放在那里。**
- `D:\GameFlow` 是 Git 仓库 ⇒ `D:\GameFlow\AGENTS.md` 会被加载。**它是本项目唯一权威的指令文件。**
- 32 KiB 上限 ⇒ AGENTS.md 只写**红线**，流程与操作细节进 skills（§8.1）与本 SPEC。往里塞教程会先撑爆预算，把后面的红线挤掉。

#### 8.7.2 `D:\GameFlow\AGENTS.md` 必须固化的红线

写成一条一条、可被人快速核对的禁令，**每条都指向 SPEC 的对应章节**：

| # | 红线 | 依据 |
|---|---|---|
| 1 | **绝不猜密码。** 禁止从游戏名/域名/文件名推导、禁止常见密码字典、禁止把 README 里"读懂"的一段话当密码、禁止超过「候选数×4」次尝试。密码只走 P0→P6，找不到就停 `WAITING_FOR_PASSWORD` | §6 / §6.3 |
| 2 | **绝不改原文件名。** 唯一硬例外是分卷 `.NNN` 序列重建，且必须先落 `WAITING_FOR_RULE` 由人显式声明规则、只对**副本**操作 | C7 / §6.5 |
| 3 | **绝不在停机态删除任何文件**（I1）。删除只发生在执行体 B、且 §6.8 的 12 条与条件全部满足 | I1 / §6.8 |
| 4 | **所有路径操作一律 `-LiteralPath`**，禁止 `-Path` | §8.8.9 / §3.2A |
| 5 | **7z 只能经 `scripts\lib\` 的 `Invoke-SevenZip` 调用**，禁止在会话里直接敲 `7z …` | §8.8.7 |
| 6 | **绝不把密码、账号、带 token 的下载 URL 写进** `.gameflow\`、日志、报告、批次清单或 Git | §9 / §3 |
| 7 | **不新增状态名。** §4 的状态集是封闭的；遇到没有对应状态的情况，停下问人，不要发明一个 | §4 |
| 8 | **不在 `D:\GameLib` 下创建 `.codex` 目录**（会让整棵库被沙盒置为递归只读） | §8.3.2 |
| 9 | **不修改 `events.jsonl` 的既有行**（append-only，唯一真相）。`state.json` 坏了就重建，不要手改 | I2 / §8.8.5 |

第 5 条的三个理由值得在 AGENTS.md 里直接写出来，否则模型会觉得"我直接敲一条 7z 更快"：**① stdin 必须关（否则加密包永久挂在 `Enter password:`）② 必须有超时 ③ `$LASTEXITCODE` 必须被立即捕获且判据是严格 `-eq 0`。** 手敲的命令这三条一条都不满足。

---

### 8.8 PowerShell 工程约定（契约级）

> 本小节的每一条都对应一种**已知会造成真实数据损坏或静默错误**的写法。它们不是风格偏好。

#### 8.8.1 版本策略：三层执行环境

| 层 | 谁决定 | 实际是什么 | 适用的解析规则 |
|---|---|---|---|
| **L1** Codex agent shell | Codex，我们改不了 | `powershell.exe -NoProfile` = Windows PowerShell **5.1**【官方 + 实测】 | 参数数组被**重新拼成一个字符串**【官方 about_Parsing】 |
| **L2** 入口进程 | 我们 | `pwsh.exe -NoProfile -File …` = PowerShell **7.4+** | 7.3+ 在 Windows 上默认 `Windows` 模式，逐参数正确转义 |
| **L3** 脚本内部 | 我们 | `#Requires -Version 7.4` | 7.x 语义可用 |

**硬规则**：

- 脚本内部**可以**用 7.4 语义，因为它由 `pwsh.exe` 执行。具体依赖三处：`ConvertFrom-Json -AsHashtable` 的**保序**行为需 7.3+【官方】；`ProcessStartInfo.ArgumentList` 的 Applies-to 列表里**没有任何 `netframework-*`**，5.1 用不了【官方】；`File.Move(src,dst,overwrite)` 三参重载需 .NET Core 3.0+【官方】。
- **但 L1 永远是 5.1 的规则** ⇒ §8.2 的 R1–R7 不可放松。这是本节最容易被"我们已经锁 7.4 了"这句话骗过去的地方。
- Preflight 必须显式检查 `pwsh.exe` 存在且 `$PSVersionTable.PSVersion ≥ 7.4`，**不满足就停在退出码 3，不降级跑**。
- 尽管如此，§8.8.2 的编码写法仍然刻意选用在 5.1 与 7.x 下**字节级一致**的形式。理由：万一有人用 `powershell.exe` 直接跑了某个 lib 函数（Codex 的 L1 就在手边），也不至于损坏数据。**这是廉价的纵深防御，不是兼容性承诺。**

#### 8.8.2 编码约定（本项目最容易造成真实数据损坏的一条）

5.1 的三个默认值**全是错的**，而且错得很安静【官方 about_Character_Encoding】https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.core/about/about_character_encoding ：

| 写法 | 5.1 实际编码 | 7.x 实际编码 | 后果 |
|---|---|---|---|
| `Out-File` / `>` / `>>` | **UTF-16LE** | utf8NoBOM | 同一个 `events.jsonl` 两次运行两种编码，解析器必坏 |
| `-Encoding utf8` | UTF-8 **带 BOM** | 无 BOM | BOM 混进 JSONL 行首，`ConvertFrom-Json` 报错 |
| `Set-Content` / `Add-Content`（新文件） | **ANSI 系统代码页** | utf8NoBOM | 中文游戏名直接损坏 |
| `Get-Content`（读无 BOM 文件） | **按 ANSI 解码** | UTF-8 | 读回来就是乱码 → 建出乱码目录 → 全盘错位 |

**统一读写入口（全项目唯一）**：`D:\GameFlow\scripts\lib\GameFlow.Io.psm1`

```powershell
$script:Utf8NoBom = [System.Text.UTF8Encoding]::new($false)   # $false = 不写 BOM

function Read-GfText  { param([Parameter(Mandatory)][string]$LiteralPath)
    [System.IO.File]::ReadAllText($LiteralPath, $script:Utf8NoBom) }

function Write-GfBytesRecoverable { param($LiteralPath, [byte[]]$Bytes) <# 见 §8.8.4 #> }
```

**硬禁清单**：任何**状态、事件日志、批次清单、manifest、报告**文件的读写，禁止使用
`Out-File` / `>` / `>>` / `Set-Content` / `Add-Content` / `Export-Csv` / 裸 `Get-Content`。
（写给人看的**终端输出**不受此限——它不落盘。）

两条容易混淆的补充：

- `$OutputEncoding` / `[Console]::OutputEncoding` 只管**与外部程序通信**的编码，官方明写 *"It has no effect on the encoding that the output redirection operators and PowerShell cmdlets use to save to files."*【官方】→ **捕获 7z 输出里的中文条目名是另一套问题**，见 §8.8.7，别和写文件混在一起。
- **脚本源文件本身**：若含中文字面量，存成 **UTF-8 with BOM**。官方：*"If you need to use non-Ascii characters in your scripts, save them as UTF-8 with BOM. Without the BOM, Windows PowerShell misinterprets your script as being encoded in the legacy ANSI codepage."*【官方】锁 7.4 后本无此问题，但 BOM 是零成本保险（同上纵深防御）。

#### 8.8.3 JSON 约定

- **一律显式 `-Depth 10`，禁止裸调用 `ConvertTo-Json`。** 默认 `-Depth 2`【官方，5.1 与 7.5 文档一致】；本项目清单的 `items[].extraction.extension_rule` 就在**第 4 层**，用默认深度写回状态时整块 `extraction` 会被序列化成类型名字符串。5.1 **不发警告**（官方 7.5 文档写 *"As of PowerShell 7.1, ConvertTo-Json emits a warning…"*，5.1 的 Description 里没有这句；两份文档在参数说明处互相矛盾 → §11）。
- **人类注释走 `"_note"` 字符串字段，不用 `//`。** `ConvertFrom-Json` 从 6.0 起吃得下 `//` 和 `/* */`，但 **`Test-Json` 从 7.4 起改用 `System.Text.Json`、拒绝带注释的 JSON**【官方】。我们要用 `Test-Json` 做入口 schema 校验，所以清单必须是严格 JSON。
- **读清单一律 `-AsHashtable`（7.3+ 保序）。** 不加它时，官方明写：大小写不同的两个键被当作**同一个键**且**只留最后一个**；出现空字符串键**直接抛错**【官方】。
  → **`item.id` 必须是显式生成的受限字符集 slug，绝不能用原始游戏标题派生。**
- **数组塌缩**：单元素数组写出会退化成对象（`[1]` → `1`）【官方】。写时 `@()` 包裹 + `-AsArray`（7.x 独有），读回一律 `@($obj.items)` 归一化。批次只剩一个游戏时这条就会咬人。
- **时间戳一律自己格式化**：`[DateTimeOffset]::UtcNow.ToString('o')`。**禁止序列化 `[datetime]` 对象**——5.1 用 `JavaScriptSerializer` 输出 `\/Date(-62135596800000)\/`，7.x 用 `Newtonsoft.Json` 输出 ISO 8601，**两版互相读不回来**【官方】。
- **大数用 `[long]`**（解压总字节数），不碰 `BigInteger`（7.5 才能序列化为 JSON 数字）【官方】。`[long]` 上限约 9.2×10^18 字节，远超任何硬盘。
- **写后回读校验**：每次写完 JSON 立即 `Read-GfText` + `ConvertFrom-Json` 一遍，失败即当作写失败并回滚（§8.8.4 第 4 步）。这是对"深度截断"与"编码错误"两类静默故障的确定性兜底，成本是一次小文件读。

#### 8.8.4 落盘协议：写「可恢复」，不写「原子」

**措辞纪律先行。** `ReplaceFile` 的官方 Remarks 自称 *"combines several steps within a single function"*，并逐条列出**三种部分失败中间态**（1175 `ERROR_UNABLE_TO_REMOVE_REPLACED` / 1176 `ERROR_UNABLE_TO_MOVE_REPLACEMENT` / 1177 `ERROR_UNABLE_TO_MOVE_REPLACEMENT_2`），每种都说明"哪些文件还在、叫什么名字"；`MoveFileEx` 全文**没有出现 atomic 字样**。【官方】https://learn.microsoft.com/en-us/windows/win32/api/winbase/nf-winbase-replacefilew · https://learn.microsoft.com/en-us/windows/win32/api/winbase/nf-winbase-movefileexw
→ **SPEC 全文不得出现"状态写入是原子的"。** 正确的说法是"**可恢复的**"。

```
Write-GfJsonRecoverable(dstLiteral, text):
  1. tmp = <dst 同目录>\<dst 文件名>.tmp-<run_id>            # 必须同目录、同卷
  2. fs = FileStream(tmp, Create, Write, FileShare.None)
     fs.Write(Utf8NoBom.GetBytes(text))
     fs.Flush($true)          # 唯一有断电保证的一步（对应 Win32 FlushFileBuffers）
     fs.Dispose()
  3. dst 不存在 → [System.IO.File]::Move(tmp, dst)
     dst 已存在 → [System.IO.File]::Replace(tmp, dst, dst + '.bak')     # 保留 backup！
  4. 回读 dst + ConvertFrom-Json 校验；失败 → 从 .bak 回滚，抛异常
```

四条约束：

- **`Flush($true)` 必须有。** 官方：*"Clears buffers for this stream and causes any buffered data to be written to the file, and also clears all intermediate file buffers."*，且 Applies to 含 `netframework-4.0` 起全部版本【官方】https://learn.microsoft.com/en-us/dotnet/api/system.io.filestream.flush 。而 `ReplaceFile` 的 `REPLACEFILE_WRITE_THROUGH` 官方写着 **"This value is not supported."** ⇒ **刷盘必须在替换之前自己做**。
- **保留 `.bak`，不要给 `destinationBackupFileName` 传 `$null`。** 理由不是数据冗余，是**恢复判据的确定性**：`ReplaceFile` 的三种失败态里，"有备份"与"无备份"的残留文件命名**完全不同**（1176 无备份时"被替换的文件已不存在，替换文件仍用原名"）。有备份才能让启动恢复走确定分支，而不是靠猜。
- **`File.Replace` 跨卷直接抛异常**（官方：*"If the sourceFileName and destinationFileName are on different volumes, this method raises an exception."*）【官方】https://learn.microsoft.com/en-us/dotnet/api/system.io.file.replace ⇒ **`.gameflow\` 必须与游戏库同卷**（§2 的目录布局已保证；Preflight 复核，不满足退出码 3）。
- **真正的崩溃安全来自 I2，不是来自单次替换的原子性**：`events.jsonl` 是唯一真相，`state.json` 是随时可重建的派生快照。`state.json` 写坏了不致命——它是缓存。**这比追求 `state.json` 的原子性有价值得多。**

**启动恢复算法**（确定性，无时间猜测；文件层判据，状态层见 §4）：

```
0  取条目锁（§8.8.6）——恢复必须在锁内做
1  state.json 存在且可解析        → 清理所有 *.tmp-*  → 结束
2  state.json 缺失或不可解析：
   a  从 events.jsonl **按文件行序**全量重放重建（§3.9、§4.5）
      —— **`.bak` 不作恢复源**：用它恢复会跳过重放、得到一个 last_event_seq
         落后于日志的快照，而删除 gate（I3）读的正是这个快照里的
         verified / consumed_by。`.bak` 只作诊断与取证（§4.5.2）
   b  events.jsonl 整体不可解析  → 落 `JOURNAL_CORRUPT`，停下等人，
      **不做任何破坏性动作**（I1）
3  清理孤儿：*.tmp-* ；以及 _stg\<8hex>\ 中 8hex 不出现在任何 state.json 里的目录
```

#### 8.8.5 JSONL 写读协议

**写**：

```
line  = (ConvertTo-Json $evt -Compress -Depth 10) + "`n"     # -Compress 必须有
bytes = Utf8NoBom.GetBytes(line)
fs    = FileStream(events.jsonl, Append, Write, FileShare.None)
fs.Write(bytes, 0, bytes.Length); fs.Flush($true); fs.Dispose()
```

- **`-Compress` 是硬要求**：默认带缩进的多行 JSON 会彻底毁掉"一行一条"，也就毁掉了整个可恢复模型。
- **整行一次 `Write`，写完立即关句柄。不保持长开的 `StreamWriter`**——进程被强杀时缓冲区内容全丢，而 JSONL 恰恰是唯一真相。

**读**：

- 逐行解析。**只有最后一行**解析失败时可以**丢弃并记警告**（崩溃可能留半行；追加写在 Windows 上**没有官方原子性保证**【社区】）。
- **中间行解析失败 = 真损坏** → 停下等人，不要"跳过继续"。这条区分很重要：容忍尾部半行是崩溃恢复，容忍中间坏行是掩盖数据损坏。
- 重放语义：重建 state = **按文件行序** fold（**不按 `seq` 排序**——封口后允许出现一次重复 `seq`，按 `seq` 排会让两条事件的先后不确定，重放结果就不确定了，I2 失效；权威见 §3.3）；`seq` 只用于人读与断裂检测。**所有事件必须幂等可重放**——记"进入状态 X"，不要记"计数 +1"。

#### 8.8.6 互斥

**主机制：`FileShare.None` 独占文件句柄。**

```powershell
$lock = [System.IO.File]::Open($lockLiteralPath, 'OpenOrCreate', 'ReadWrite', 'None')
```

**锁粒度**（与 I4「每个游戏条目独立推进」对齐）：

```
D:\GameLib\batches\<batch-id>.lock              批次级：整批的入口互斥
D:\GameLib\games\<dirname>\.gameflow\.lock      条目级：真正的并发单位
```

**为什么是它**：进程死亡时 OS 自动关闭句柄并释放锁（无陈旧锁问题）；句柄属于进程不属于线程（无线程亲和性问题）；**文件系统没有会话隔离**，所以计划任务与 Codex 交互会话天然互斥；5.1/7.x 行为一致、零依赖。

**为什么不用 lock 文件 + PID 存活检测**：Windows 的 PID 复用很快，`Get-Process -Id` 命中的可能是完全无关的新进程；而"超时清理陈旧锁"要引入时间猜测，与本项目的确定性偏好直接冲突。锁文件里可以写 `{run_id, host, started_at}` 供人排查，**但绝不用它判存活**。

**命名 Mutex 若要用，必须 `Global\` 前缀。** 官方：*"If its name begins with the prefix `Global\`, the mutex is visible in all terminal server sessions… **If you do not specify a prefix when you create a named mutex, it takes the prefix `Local\`.**"*【官方】https://learn.microsoft.com/en-us/dotnet/api/system.threading.mutex
默认的 `Local\` 只在当前终端服务会话内可见——而我们要防的**恰好**是"计划任务会话 vs 交互会话"这一对。本 SPEC 只把 `Global\GameFlow_Pipeline_v1` 用于捕获 `AbandonedMutexException`（"上次是被强杀的"这一信号），**不把它作为互斥主路径**。

**抢锁失败的姿态**：**不区分** `IOException`（被占用）与 `UnauthorizedAccessException`（权限/只读）的 HResult，统一停在运行期状况 `LOCKED_BY_ANOTHER_RUN`（不落盘、不进状态机，§4.2），批次级失败时进程退出码 75，**不等待、不重试**。第二道保险是计划任务的 `MultipleInstances = IgnoreNew`（§8.8.10）。

#### 8.8.7 外部命令传参约定

**四条已知的坑**（全部【官方】）：

1. **5.1 拼字符串**：参数数组被 *"joined into a single string with each parameter separated by a space"*，**不逐参数加引号** → `D:\GameLib\games\游戏 A\x.rar` 被 7z 当成两个参数。
2. **`Start-Process -ArgumentList` 数组形式是陷阱，不是解决方案**：官方自述 *"The cmdlet joins the array into a single string with each element of the array separated by a single space."*、*"If parameters or parameter values contain a space or quotes, they need to be surrounded with escaped double quotes."*，并建议 *"use a single ArgumentList value containing all the arguments and any needed quote characters."*——等于把转义责任推回给调用方。**全项目禁用。**
3. **`ProcessStartInfo.ArgumentList` 才是零歧义的**：*"Strings added to the list don't need to be previously escaped."*、*"ArgumentList escapes the provided arguments…"*。但它的 Applies-to 里**没有任何 `netframework-*`** ⇒ 5.1 用不了 ⇒ 这是"必须由 `pwsh.exe` 执行脚本"的最硬理由。
4. **`--%` 完全用不上**：它只展开 `%ENVVAR%`，*"Other than `%variable%` environment-variable references, you can't embed any other dynamic elements in the command."*，且遇到换行或管道符就失效。本项目的路径与密码都来自 PowerShell 变量。**全项目禁用。**

**契约：所有外部进程调用只允许经 `scripts\lib\GameFlow.Proc.psm1` 的 `Invoke-GfProcess`。**

```
Invoke-GfProcess
  -FilePath   <exe 全路径>
  -Argv       [string[]]      # 逐 token 构造。含空格的路径就是一个 token，不加任何引号
  -TimeoutSec <int>           # 必填，无默认值
  -WorkDir    <path>
→ [pscustomobject] @{ ExitCode; StdOut; StdErr; TimedOut; ElapsedMs }

内部不变量（一条都不能省）：
  UseShellExecute        = $false
  RedirectStandardInput  = $true，进程启动后**立即** Close()
       ← 加密包在 stdin 可用时会挂在 "Enter password:"，后台里就是一个永久僵尸进程
  RedirectStandardOutput = $true ; RedirectStandardError = $true
  StandardOutputEncoding = StandardErrorEncoding = UTF8Encoding($false)
  ArgumentList 逐元素 Add($token)，**绝不自己拼字符串**
  超时到 → Kill 整棵进程树 → TimedOut = $true，ExitCode 置 $null
       ← **不要把超时伪造成 0 或 1**，那会让 §6.8 的判据读到一个假的成功/警告
```

**7z 专用封装 `Invoke-SevenZip`**：签名、开关白名单、`-TypeSpec` 取值域（`none` / `*` / `*:s{N}m` / `#:e`）、退出码判据与超时语义的**唯一权威是 §6.2.1**，本小节不重复定义。

本节只补一条 PowerShell 实现层的约束：该封装**必须**经 `ProcessStartInfo.ArgumentList` 逐参数传递（§8.8.7 的调用约定），**不得**走 `Start-Process -ArgumentList`（官方明说它只用单空格拼接、不逐参数加引号），也**不得**依赖 `--%`（它只展开 `%ENVVAR%`、不展开 PowerShell 变量，而本项目的路径与密码全部来自变量）。`ArgumentList` 在 .NET Framework（5.1）上**不存在**，因此该封装只能由 `pwsh.exe`(≥7.4) 执行——三层执行环境的分工见 §8.8.1。

`-scc` / `-sccUTF-8` 的去留由 §11 的待测项裁定后**只写在 §6.2.1 一处**。

**脱敏**：任何 stdout/stderr 进日志前必过 `-p\S+ → -p***`；**`Argv` 数组本身绝不整体写日志**（§9）。

#### 8.8.8 `$LASTEXITCODE` 必须显式检查

- PowerShell **默认不会**因外部命令非零退出码抛异常（`$PSNativeCommandUseErrorActionPreference` 默认 `$false`）【官方】⇒ **`try/catch` 抓不到 7z 失败**。
- `$LASTEXITCODE` 是全局变量，**下一条命令就覆盖它** ⇒ 调用后**立刻**存入局部变量。
- 走 `Invoke-GfProcess` 时不读 `$LASTEXITCODE`，读返回对象的 `.ExitCode`。**这正是强制走封装的第三个理由：把"忘了判退出码"这类错误在结构上消掉，而不是靠 review 抓。**
- 判据一律 **`-eq 0` 严格相等**。**禁止 `-le 1`**——exit 1 的官方含义是 "Warning (Non fatal error(s))"，实际是**有文件被静默跳过**，本项目一律当失败（§6.2）。这条如果写宽一档，直接后果是删掉一个内容不全的包的源文件。

#### 8.8.9 `-LiteralPath` 强制

- **全项目禁止 `-Path`**（`Test-Path` / `Get-Item` / `Get-ChildItem` / `Copy-Item` / `Move-Item` / `Remove-Item` …），一律 `-LiteralPath`。
- 理由：§3.2A 的目录名规范化只把**非法字符**换成全角同形字，而 `[` `]` 在 Windows 上是**合法**文件名字符，会原样保留；`-Path` 把它当通配符 ⇒ 要么匹配不到（看起来像"文件不存在"），要么误匹配到别的目录（更糟）。
- `New-Item` 的参数名是 `-Path`、**没有 `-LiteralPath`** ⇒ **建目录一律走 `[System.IO.Directory]::CreateDirectory($literal)`**。【待测：Preflight 用 `(Get-Command New-Item).Parameters.Keys` 核对一次，成本一行；不核对的后果是这条禁令可能建立在一个记错的 cmdlet 签名上】
- 确实需要通配匹配时（例如扫 `_inbox`）：`Get-ChildItem -LiteralPath <目录>` 拿全量，**在内存里**用 `-like` / 正则过滤。**永远不要把模式塞进路径参数。**

#### 8.8.10 计划任务

**登录类型是整个设计的枢纽**【官方】https://learn.microsoft.com/en-us/windows/win32/taskschd/principal-logontype ：

| LogonType | 官方原文要点 | 本项目 |
|---|---|---|
| `S4U` (2) | *"no password is stored by the system and there is **no access to either the network or encrypted files**"* | ❌ **禁用**：解不开 DPAPI vault |
| `ServiceAccount` (5) | Local System / Local Service / Network Service | ❌ 同样解不开该用户的 DPAPI blob，且权限过大 |
| `InteractiveOrPassword` (6) | *"**This flag is not recommended for new tasks** because it is less reliable than TASK_LOGON_PASSWORD."* | ❌ 官方自己不推荐 |
| `Interactive` (3) | *"**User must already be logged on.**"* | ✅ **采用** |

`Interactive` 的前提（用户保持登录）本来就成立：E1 说这是主力机，且执行体 C 需要前台桌面。

**注册形状**（示意，不是完整实现）：

```powershell
$act = New-ScheduledTaskAction -Execute 'pwsh.exe' `
  -Argument '-NoProfile -NonInteractive -ExecutionPolicy Bypass -File "D:\GameFlow\scripts\Invoke-Sweep.ps1" -Unattended' `
  -WorkingDirectory 'D:\GameFlow'
$prn = New-ScheduledTaskPrincipal -UserId "$env:USERDOMAIN\$env:USERNAME" -LogonType Interactive -RunLevel Limited
$set = New-ScheduledTaskSettingsSet -MultipleInstances IgnoreNew `
         -ExecutionTimeLimit (New-TimeSpan -Hours 2) -StartWhenAvailable
```

逐条理由：

- **`-RunLevel Limited`**（最小权限）。A 不需要提权——提权只属于 B 的 Defender 预检（`MpCmdRun` 官方需提权）。
- **`-MultipleInstances IgnoreNew`**：§8.8.6 文件锁的第二道保险。
- **`-NoProfile`**：确定性（用户 profile 不得改变行为）。**`-NonInteractive`**：让任何意外的 `Read-Host` 立即失败，而不是永久挂起一个无人看见的进程。
- **`-ExecutionTimeLimit` 必设**：无限期挂着的任务会掩盖死锁；A 是"扫一遍就退出"的批处理，2 小时是极宽松的上界。
- **`-ExecutionPolicy Bypass` 只对该进程生效**，不改系统策略——但 GPO 例外，见 §8.4。
- ⚠️ 注意 `-Argument` 只接**一个字符串**，路径要自己带引号【社区】——这是 §8.8.7 那个"5.1 拼字符串"的坑在计划任务侧的同一副面孔。

**DPAPI 身份一致性**：vault（`%LOCALAPPDATA%\GameFlow\vault.clixml`）的**创建身份**必须与使用它的进程**运行身份**相同【社区共识；未找到微软对 `ConvertFrom-SecureString` 的正面声明 → §11】。
本项目里 A **根本不碰密码**（I5），所以这条真正约束的是 B——而 B 由人在交互会话触发，身份天然一致。**这条写出来是为了防止将来有人"顺手"把 B 也挂进计划任务**：那一刻 vault 就解不开了，而失败表现会是"所有条目都缺密码"，极具误导性。

**触发来源必须进日志**：每条事件的 `trigger` 字段取 `scheduled` / `manual` / `codex`，否则事后无法区分是谁动的（同一入口脚本三条触发路径）。

#### 8.8.11 日志字段与报告生成

**事件 schema 的唯一权威是 §3.3.1**（字段、`kind` 闭集、`payload` / `evidence` 形状、`state.entered` 作为唯一状态变更通道）。本小节**不重列字段表**，只规定 PowerShell 侧的落盘与脱敏纪律。

落盘纪律见 §8.8.5（`-Compress`、整行一次 `Write` + `Flush($true)` + 立即关闭、读取端丢弃尾部半行）。

**绝不允许出现**：密码明文、`password_ref` 的**值**（键名可以）、带 token 的下载 URL、绝对路径。

**报告生成**：永远从 `events.jsonl` / `state.json` **派生**，从不手工维护，**随时可重新生成**，落 `D:\GameLib\_reports\<batch-id>-<seq>.md`。呈现层是本项目唯一"可以不吝啬"的地方，四条要求：

1. **每个停机态都必须给出下一个人类动作**——一句可以照抄的命令，或一句明确的判断。"停在明确状态"如果没有可执行的下一步，人照样卡住。
2. **每份报告顶部显式声明本次的删除情况**：「本次未删除任何文件」或「本次删除了 N 个源包（清单如下，SHA-256 已记入 manifest）」。C3+C4 下这是用户最焦虑的点，不能靠翻日志确认。
3. **落盘的是纯 Markdown，不写 ANSI 色码进文件**；终端着色只在终端展示时用（`$PSStyle`，7.2+）。
4. 7-Zip 版本低于建议线（≥26.02）时，报告顶部**长期**提示，但**不拒绝运行**（§6.2）。

#### 8.8.12 测试

- **Pester 陷阱**：Windows 10/11 预装 **Pester 3.4.0**，与 Pester 5 语法不兼容（`Should Be` vs `Should -Be`、5 的 discovery/run 两阶段模型）。裸写 `Import-Module Pester` 会**静默加载 3.4.0**，测试会以**看起来合理的方式**误通过或误失败。【社区】
- **契约**：要么 `#Requires -Modules @{ ModuleName='Pester'; ModuleVersion='5.5.0' }`（或入口第一行 `Import-Module Pester -MinimumVersion 5.0 -ErrorAction Stop`），要么干脆零依赖自建 harness。**不允许裸 import。**
- Pester 是**开发期依赖**：放 `D:\GameFlow\tests\`，**计划任务跑的生产脚本绝不 import 它**。这样"运行时零外部依赖"的承诺才成立。
- **新机器第一次验证用零依赖冒烟脚本**（纯 `if`/`throw` + 计数）——那台机器上还没有 Pester 5，这是鸡生蛋问题的唯一解。
- **状态机黄金文件测试**（最高性价比的一类）：构造一段 `events.jsonl` → 重放 → 断言产出的 `state.json` **逐字节**等于 golden 文件。它覆盖 §8.8.4 / §8.8.5 的整个恢复模型，且**纯 JSON 处理、不碰 7z、可以先在开发机（macOS）上跑通**。
- **fixture 由 `tests\New-Fixtures.ps1` 现场生成，不进 Git**：加密压缩包进 Git 很蠢；二进制 fixture 会与目标机的 7-Zip 版本脱节；而生成脚本本身就是"我们假设 7z 会这样行为"的**可执行文档**——它跑不出预期结果，就说明假设变了。

---

### 8.9 本节产生的待实测项

全部汇入 §11，此处只给索引与"不验证会怎样"的一句话摘要：

| 待测 | 出处 | 不验证的后果 |
|---|---|---|
| Windows 原生沙盒下 `sandbox_workspace_write.writable_roots` 是否生效 | §8.3.2 | Codex 会话里 A/B 一步都跑不动，且失败长得像脚本 bug |
| `codex execpolicy check` 对 `powershell.exe -NoProfile -Command …` 的拆分语义 | §8.5.3 | 每次 7z 调用要人点一次，A 的无人值守属性归零 |
| Codex 是否对脚本内部起的**子进程**重新审批 | §8.5.4 | B 从"确认一次"退化成"点几十次" |
| `%USERPROFILE%\.codex\skills\` 在 Windows 版是否被扫描；中文 skill 名能否被 `$` 触发 | §8.1.1 / §8.1.2 | 第一次使用时 skill 找不到/打不出来 |
| 目标机 `Get-ExecutionPolicy -List` 全作用域快照 | §8.4 | GPO 锁死时才发现，且 `-ExecutionPolicy Bypass` 无声失效 |
| 重定向管道下 `-sccUTF-8` 是否影响 7z 输出编码 | §8.8.7 | 中日文条目名读成乱码 → D5/D6 在正常包上假失败 → 源包永远删不掉 |
| `New-Item` 是否真的没有 `-LiteralPath` | §8.8.9 | 一条禁令可能建立在记错的签名上（核对成本一行） |
| 5.1 的 `ConvertTo-Json` 超 `-Depth` 是否发警告 | §8.8.3 | 只影响"能否靠警告发现截断"；已用显式 `-Depth 10` + 写后回读消掉 |
| DPAPI vault 跨身份不可解密的实际表现 | §8.8.10 | 影响运维说明（重装/换机 = 密码全丢）与"别把 B 挂进计划任务"这条禁令的份量 |

---

> ### ⚠️ 遗留异议（§8.1.4 第 2 项「巡检」的形状）（主控未采纳）
>
> 契约要求把「巡检」做成一个直接调用 `Invoke-Sweep.ps1` 的 skill，本节已照此写。但我认为这个形状有一处可预见的故障源，建议主控考虑改动：
>
> 1. **它给执行体 A 造出了两条权限模型完全不同的执行路径。** 正常路径是 Windows 任务计划（§8.6），完全在 Codex 沙盒之外；skill 路径却把同一个 A 搬进了 agent 沙盒，于是 A 的可运行性又回头依赖 §8.3 那个【待测】的 `writable_roots`——而 §8.3.3 的逃生路线恰恰是"A 不走沙盒"。结果是：**同一个 A，在计划任务里跑得好好的，在 Codex 里跑就写不进去**，而人第一反应会是"A 坏了"，排查方向从一开始就错。
> 2. **更确定的形状是让 `gf-sweep` 不执行脚本**，只做两件不需要写权限的事：`Start-ScheduledTask -TaskName 'GameFlow-Sweep'` 触发那个已注册的任务，然后读最新的 `_reports\` 输出。这样 A 全程**只有一条**执行路径、只有一套权限模型，skill 退化为一个纯粹的遥控按钮——也更符合"引擎层确定性、Codex 只做人机接口"的分工。
> 3. 代价：`Start-ScheduledTask` 是异步的，skill 需要轮询任务状态或让人稍后再看报告。我认为这个代价远小于"两条路径"带来的排查成本。
>
> 这是形状之争，不是事实之争；契约怎么定我都能实现，但排查成本会记在第 1 条上。


---

## §9 安全、隐私与诚实局限；前置条件与 Preflight 探测

本节把两件看起来不相干的事放在一起，因为它们是同一件事的两面：

- **前半（9.1–9.10）诚实局限**——已经知道、且**无法消除**的东西。写出来，不粉饰，不给虚假安全感。
- **后半（9.11–9.12）Preflight**——**还不知道**的东西。E3 只说了"装了 7-Zip **或** WinRAR/Bandizip"，没说是哪个；Windows 版本、区域设置、文件系统、执行策略全部未知。这些不是可选的背景信息，它们是 §5/§6/§7/§8 每一条判据的地面。

两半共用一条纪律：**凡是做不到的，明写做不到；凡是没测的，明写没测，并说清楚"不测会怎样"。** 本节里所有"缓解"都只说它实际缓解了什么，不说它"解决"了什么。

---

### 9.1 密码的暴露面（诚实清单）

#### 9.1.1 这是硬限制，不是实现缺陷

7-Zip 把密码送进去的方式**只有一种**：命令行开关 `-p{password}`。

> 官方手册 `password.htm` 全文只有 `-p{password}`：「Specifies password.」示例 `7z x archive.zip -psecret`。唯一的变体是不带值时切进交互提示。
> 【官方】https://7zip.bugaco.com/7zip/MANUAL/cmdline/switches/password.htm

三条替代路径**已被逐一证伪**：管道（`echo pw | 7z ...`）、文件重定向（`7z ... < pwfile`）、环境变量（试了 4 个常见变量名）全部无效。
【实测】macOS + 官方 7zz 25.01。**可外推到 Windows**：这是 7-Zip 参数解析层的行为，跨平台是同一份代码，且官方手册本身就没有第二个开关可用。

社区侧有一条很能说明性质的旁证：SourceForge 官方讨论区里，一位管理约 800 台主机的管理员明确提出「`-p` 的明文进了审计日志」，社区给的全是 shell 变通，**Igor Pavlov 从未回应**，提问者最终放弃 7-Zip 改用 `tar` + OpenSSL（因为 OpenSSL 有 `-kfile`）。
【社区】https://sourceforge.net/p/sevenzip/discussion/45797/thread/fca20db3/

> **结论：只要用 `7z.exe`，密码进命令行就是不可避免的。本 SPEC 不承诺"密码不会以明文出现"。**

#### 9.1.2 完整暴露面

```
                        ┌──────────────────────────────────────┐
   vault.clixml         │  Invoke-Work.ps1（执行体 B）           │
   (DPAPI, §3)  ──解密─►│    $plain = <SecureString → BSTR>     │──► 进程内存 [E5]
                        │    & $7z x @args "-p$plain"           │
                        └───────────────┬──────────────────────┘
                                        │ CreateProcess
                                        ▼
                        ┌──────────────────────────────────────┐
                        │  7z.exe  命令行 = "... -p真实密码"      │  ◄── [E1] 无法消除
                        └───────────────┬──────────────────────┘
                                        │
        ┌───────────────────────────────┼────────────────────────────────┐
        ▼                               ▼                                ▼
  同机任意进程读               安全日志 4688（若开启                 Sysmon EID 1 /
  Win32_Process.CommandLine    「包含命令行」策略）                   EDR 采集
  Process Explorer / 任务管理器
```

| # | 暴露面 | 触发条件 | 可控？ | 缓解 | 可信度 |
|---|---|---|---|---|---|
| **E1** | **7z 进程的命令行** | 每一次带密码的 `7z t` / `7z x` | ❌ **不可控** | 无。只能缩短进程存活时间，读得到的窗口仍然存在 | 【官方】只有 `-p` 一条路 + 【社区】命令行对同机进程可见 |
| **E2** | 安全日志 **4688**（进程创建 + 命令行） | 该机开启了「审核进程创建」且启用了 `ProcessCreationIncludeCmdLine_Enabled` | ❌ 不可控 | 无（关审计不是我们该做的事） | 【社区】；**该机是否开启 = 【待测】PF-15** |
| **E3** | **Sysmon EID 1 / 第三方 EDR** | 该机装了 Sysmon 或 EDR | ❌ 不可控 | 同上 | 【社区】；**该机是否安装 = 【待测】PF-15** |
| **E4** | PowerShell **Script Block Logging (4104)** / **Module Logging (4103)** / Transcription | 相应策略开启 | ⚠️ **部分可控** | 见 9.1.3 的 M2：脚本文本里只出现 `"-p$plain"` 变量形式。4104 记录的是 **script block 文本**不是运行时值 → 4104 拿不到明文。**但 4103 记录管道参数值，仍会泄** | 【官方】微软自己的警告见下 |
| **E5** | 进程内存 / pagefile / hiberfil / 崩溃转储 | 始终 | ⚠️ 部分可控 | `Marshal::ZeroFreeBSTR` 用完归零，缩短驻留；进不了 pagefile 是做不到的 | 【社区】（SecureString 官方立场即"不应被当作安全边界"） |
| **E6** | **PSReadLine 历史文件**（明文） | **仅**交互式手敲命令，不影响脚本执行 | ✅ 可控 | 见 M5：**禁止在交互提示符下手敲带 `-p` 的 7z 命令** | 【社区】https://github.com/PowerShell/PSReadLine (#1058) |
| **E7** | 7z 自身的 stdout/stderr | `-bb3` 高 verbose，或命令行解析错（exit 7）时错误信息可能回显整条命令行 | ✅ 可控 | 见 M4 脱敏；且**禁止使用 `-bb3`** | 【待测】exit 7 是否真的回显整条命令行未在本轮验证；脱敏按最坏情况做 |
| **E8** | `Register-ScheduledTask -Password` | 用脚本注册"存密码"的计划任务 | ✅ 可控 | **禁止脚本化注册存密码的任务**；计划任务用图形界面注册一次（§8） | 【官方】该参数类型是 `[String]` 而非 SecureString：https://learn.microsoft.com/en-us/powershell/module/scheduledtasks/register-scheduledtask |

微软对 E4 的原文警告（值得逐字引）：

> "Increasing the level of logging on a system increases the possibility that logged content may contain sensitive data. For example, **with script logging enabled, credentials or other sensitive data used by a script can be written to the event log.**"
> 【官方】https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.core/about/about_logging_windows

E6 有一个专门针对本项目的陷阱：PSReadLine 有一条过滤，匹配 `password|asplaintext|token|key|secret` 的行不写历史——而 **`7z x foo.rar -pMyP@ss` 不匹配这个正则**，会被完整写进 `%APPDATA%\Microsoft\Windows\PowerShell\PSReadLine\ConsoleHost_history.txt`。【社区】

#### 9.1.3 可控 vs 不可控（这张表就是"诚实"本身）

| | 内容 | 由谁保证 |
|---|---|---|
| ✅ **可控** | **M1** 明文不进批次清单（`batches\<batch-id>.json` 只写 `password_ref` 键名） | §3 |
| ✅ | **M2** 明文不进任何日志：`events.jsonl` / `state.json` / `manifest.json` 里**不存在**密码字段；脚本文本里密码只以变量形式出现（`"-p$plain"`） | §3 + 本节 M4 |
| ✅ | **M3** 明文不进报告：`_reports\` 只写"已尝试候选数 / 命中的 `password_ref` 键名 / 当前 ACP"，**不写候选本身** | §6 + 本节 |
| ✅ | **M4** 明文不进 Git：`vault.clixml` 在 `%LOCALAPPDATA%\GameFlow\`，**不在** `D:\GameFlow`（Git 库）也**不在** `D:\GameLib` 内 | §2 |
| ✅ | **M5** 明文不进 shell 历史：禁止交互式手敲带 `-p` 的命令，一律经脚本 | 本节，写进 §8 的调用约定 |
| ✅ | **M6** 7z 输出脱敏后才落盘 | 本节 9.1.4 |
| ✅ | **M7** 用完归零（`ZeroFreeBSTR`），不把明文回传给调用方 | §6 |
| ❌ | **不可控 · 唯一的一条**：**密码在 7z 进程存活期间存在于该进程的命令行中；同机的其它进程与系统审计可见，无法消除。** | 无人能保证 |

**真正的、也是唯一的缓解是把这台机器当作信任边界。** 上游会话里 Codex 建议"专用闲置电脑或虚拟机"是从可用性角度提的（E1 主力机会被打断），这个建议在安全上同样成立——但用户明确说了这是主力机（E1），所以本 SPEC 的立场是：**接受这个暴露面，写清楚，不假装消除。**

#### 9.1.4 脱敏规则（契约级，写死）

任何 7z 的 stdout/stderr、任何要写进 `events.jsonl` / `_reports\` / 控制台的字符串，落盘前必须过一遍同一个函数：

```
SANITIZE_PW(text):
    # 规则 1：命令行形式。-p 后面接到第一个空白为止
    text := regex_replace(text, '(?<=(^|\s))-p\S+',  '-p***')
    # 规则 2：交互提示回显（7z 在某些路径会打印 "Enter password:" 后跟内容）
    text := regex_replace(text, '(?i)(Enter password[^\r\n]*:).*', '$1 ***')
    return text
```

- 应用点是**唯一的日志写入函数**（§8 的统一写盘入口），不是每个调用点各自记得脱敏。
- 规则 1 是**贪婪到空白为止**——因为密码可能含 `"`、`$`、`` ` ``、`|` 等；宁可多脱一点，不可漏。
- **脱敏是补救，不是防线**：它挡的是 E7 和"日志被别人看到"，挡不住 E1。不要因为有了脱敏就放松别的。
- 密码里的尾随空格有意义（§6.3：`'pass '` ≠ `'pass'`），所以规则 1 用 `\S+` 会在"密码含空格"时脱不干净——这种密码在命令行上本来就得靠参数数组传，日志里不会以 `-p<空格密码>` 的连续形态出现。**若 §6 未来允许含空格的密码，本规则必须同步复审。**【待测】含空格密码在 `powershell.exe` 参数拼接下的实际落地形态（并入 §11 的密码传递实测项）。

#### 9.1.5 本 SPEC 不允许出现的措辞

以下句子**不得**出现在 SPEC 正文、报告模板、README 或任何用户可见文案里，因为它们是假的：

- ❌「密码不会以明文出现」
- ❌「密码全程加密」
- ❌「密码只存在于内存中」
- ❌「日志中不含任何敏感信息」（脱敏管日志，管不了 4688）

正确的说法只有一种：**「密码在解压期间会短暂出现在 7z 进程的命令行里；同机进程与系统审计可以看到。GameFlow 保证它不进清单、日志、报告和 Git，仅此而已。」**

---

### 9.2 Computer Use 的截图会进上下文

9.1 那套约束是**文件层面**的：不写清单、不写日志、不进 Git。执行体 C（前台，§7）打开了一个 9.1 完全兜不住的维度。

> 「您的 ChatGPT 数据控制适用于通过 ChatGPT 处理的内容，**包括由计算机使用功能截取的屏幕截图**。」
> 「OpenAI stores browser activity only when it becomes part of the ChatGPT context, such as text ChatGPT reads from a page, **screenshots**, tool calls, summaries, messages…」
> 【官方】Codex / Computer Use 文档：https://learn.chatgpt.com/zh-Hans/docs/computer-use

**推论（这是本节要钉死的一句）：密码只要显示在屏幕上，它就在截图里。** YAML/JSON/日志层面的所有约束在这个维度上一条都不起作用。

本机 `~/.codex/config.toml` 里 `disable_response_storage = true`【实测·macOS 本机配置文件；**Windows 那台机器上是否也这么配 = 【待测】**】。要注意这个开关的语义边界：**它影响的是服务端是否存储，不改变"截图必须传给模型才能被看见"这一事实**——这是 Computer Use 的工作原理，不是配置能改的【社区】。

**操作规范（写进 §7 的执行体 C 会话前置清单，逐条可勾选）：**

| # | 规范 | 理由 |
|---|---|---|
| **CU-1** | 网盘页面的**密码输入框不得处于明文显示状态**（不点"显示密码"的眼睛图标） | 明文密码进截图 |
| **CU-2** | **不在 C 的会话期间打开 vault**、不打开任何密码管理器窗口、不打开存着密码的记事本/表格 | 整个 vault 进截图 |
| **CU-3** | 资源页上的**归档密码可以出现在截图里**（它本来就是公开页面上的内容）。C **可以**把抓到的候选写进 vault，但**必须**标 `confirmed: false`；**未确认的候选对 §6.3 的密码链等同于不存在**，人确认一次后才转 `true`（§7.4.3、P5）| 区分"网盘账号密码"（**绝不**持久化，见 CU-6）与"归档密码"（页面公开，可入 vault 但需确认） |
| **CU-4** | 启动 C 之前关闭无关的敏感应用（邮件、聊天、银行页、公司内网） | 官方安全指南同样这么说：「除非任务需要，否则请关闭敏感应用」【官方】 |
| **CU-5** | 登录、验证码、短信码、支付一律**人工接管**，接管期间 C 处于暂停 | 见 9.10 |
| **CU-6** | C 的会话必须短（E1：这是主力机）；会话结束即退出前台 | E1 + §7 |
| **CU-7** | **网盘账号密码 / 登录凭据**：C 不得读取、不得写进任何持久化文件、不得让其出现在截图中。登录一律人工接管（CU-5）。**这才是「不得持久化」禁令的真正对象**——它与 CU-3 的归档密码是两回事 | 账号一旦泄漏影响的是整个网盘，不只是一个包 |

> **CU-1..CU-7 是执行体 C 的前置条件，不是建议。** §7 必须在会话启动流程里把它们做成一次性勾选清单，勾不全就不开始。

---

### 9.3 DPAPI vault 的局限

密码库是 `%LOCALAPPDATA%\GameFlow\vault.clixml`，`Export-CliXml` 里的 SecureString 走 DPAPI（§3）。

> 「If an encryption key is specified by using the **Key** or **SecureKey** parameters, the Advanced Encryption Standard (AES) encryption algorithm is used. ... **If no key is specified, the Windows Data Protection API (DPAPI) is used** to encrypt the standard string representation.」
> 【官方】https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.security/convertfrom-securestring

⚠️ **这一页并没有明文写"只有同一用户同一机器能解密"。** 那是 DPAPI 众所周知的特性，但就本页而言属于【社区】而非【官方】。**跨用户/跨机不可解密的实际表现 = 【待测】**（§11；验证方法：A 账户加密，B 账户尝试解密）。

| 局限 | 后果 | 可信度 |
|---|---|---|
| **V1 绑定运行身份** | vault **必须由将来跑流水线的那个账户创建**。管理员建的，别的账户解不开 | 【社区】 |
| **V2 绑定机器** | 不可迁移到另一台机器 | 【社区】+【待测】 |
| **V3 重装 / 换机 = 密码全丢** | 这是一个**安静的数据丢失陷阱**：一切正常，直到某天不正常，且没有任何错误提示告诉你"你本该备份" | 【社区】 |
| **V4 S4U 计划任务解不开** | 官方：「When an S4U logon is used, **no password is stored** by the system and **there is no access to either the network or encrypted files**.」【官方】https://learn.microsoft.com/en-us/windows/win32/taskschd/principal-logontype ；社区侧机理：S4U 令牌创建时 lsass 里的凭据被清掉，DPAPI 改用空串的哈希，blob 就解不开了【社区】 | 【官方】+【社区】 |
| **V5 SYSTEM / LocalService 同样解不开** | 用服务账户跑计划任务同样无效 | 【社区】 |

**V4/V5 是 §2 三执行体切分的直接成因之一**：需要密码的步骤**不能**放进无人值守计划任务，所以执行体 A（计划任务）永不触碰密码，`7z t/x` 带密码归执行体 B（人/Codex 在交互会话触发）。

**必须写进用户可见文档的一条运维警告（不是脚注，是显式一页）：**

> **GameFlow 不是你的密码备份。** vault 只是一个绑定当前 Windows 账户和当前这台机器的本地缓存。系统重装、换机、换账户、账户 SID 变化都会让它**全部失效且不可恢复**。
> **请自行另外保存一份归档密码**（纸、离线密码管理器、任何你信任的地方）。GameFlow **不提供明文导出命令**——一条 `Export-VaultPlaintext` 会把 DPAPI 的保护意义整个抹平（§3 已锁定这条）。这意味着"另存一份"这件事**必须在录入密码的同时做**，事后补不了。

> **决策留待用户拍板（进 §12）**：要么接受"换机重填"，要么用户自己维护外部副本。本 SPEC 默认前者，并要求 Preflight（PF-14）在 vault 首次创建时打印这段警告一次。

---

### 9.4 MOTW 不是安全边界（明确声明）

**声明：本 SPEC 不把 Mark-of-the-Web 当作任何一道安全判据。** 没有任何状态迁移、任何删除判据、任何放行决定依赖 MOTW 的存在或缺失。

这个声明是必要的，因为 MOTW 看起来很像一道防线，而它不是：

| 事实 | 为什么它做不了边界 | 可信度 |
|---|---|---|
| MOTW 存在 NTFS 的备用数据流 `:Zone.Identifier` 里，**只在 NTFS 上存在** | 「Mark of the Web only applies to files saved on an NTFS file system, not files saved to FAT32 formatted devices.」→ **库落在 exFAT 移动盘上，MOTW 整个话题自动消失**。一道随文件系统蒸发的"防线"不是防线 | 【官方】https://learn.microsoft.com/en-us/deployoffice/security/internet-macros-blocked |
| 7-Zip CLI 是否默认传播 MOTW，**官方三处都没写** | switch 索引里 `-snz` 有条目、无默认值说明、无独立页面。网上的"default is No"全部是二手报道 | 【社区】+【待测】§11 |
| 注册表 `HKCU\SOFTWARE\7-Zip\Options\WriteZoneIdExtract` 影响 CLI | **无任何证据**，那是 GUI 的选项存储。**不得写进 SPEC** | 【裁定：证据不足】 |
| MOTW 在**嵌套解包**场景下的行为随版本变化 | 24.09 修过"嵌套包解出的文件不传播 MOTW"（限定主语是 File Manager）；26.03 又修了一个保留失效。这是**反复出问题的区域** | 【官方 changelog】https://www.7-zip.org/history.txt |
| MOTW **不阻止解压** | 它只影响运行时的 SmartScreen / Office 宏 | 【官方】 |

**那我们为什么还要传播 MOTW？** 因为它是一层**免费的纵深**，不是因为它可靠：带 ZoneId=3 的 exe 首次运行会触发 SmartScreen 的额外检查【官方】。我们保留它，同时**完全不依赖它**。

**真正的护栏只有一条：解压后、移入游戏目录前的显式 Defender 扫描**（9.8 / §6）。这条是主动的、有输出的、可判定的；MOTW 是被动的、默认值未知的、随文件系统消失的。

**MOTW 在本 SPEC 里的唯一硬性作用是可用性，不是安全**：如果解出来的 exe 带着 MOTW 进入执行体 C，Computer Use 启动游戏或 MTool 时会撞上 SmartScreen 的**前台模态框**并卡死（E1 下这是很难恢复的故障）。所以：

```
解压(可能带 MOTW) ──► Defender 显式扫描 ──► 人工验收 ──► Unblock-File（一次性、幂等）──► 执行体 C
                          ▲ 真正的护栏在这里          ▲ 「解除标记」是 C 的前置条件，不是可选收尾
```

`Unblock-File` 官方明文幂等、对未标记文件无副作用【官方】https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.utility/unblock-file 。
若卷是 exFAT（PF-04 探测），整段 MOTW 逻辑**优雅降级为 no-op**，并在报告里注记一句"本卷无 MOTW，SmartScreen 不介入"——**不报错、不阻断**，因为它本来就不是判据。

---

### 9.5 头加密包的护栏空洞（无法消除的盲区）

7z 的 `-mhe`（加密文件头）包，在拿到正确密码之前，`7z l` 直接 exit 255，**`Size` / `Path` / 条目数一个都拿不到**。
【官方 password.htm + 实测·macOS 25.01；**可外推到 Windows**：这是格式层面的性质，头本身就是密文，与平台无关】

这不是一个边角情况：资源站上带密码的 7z 包相当一部分开了 `-mhe`（这正是"看不到里面有什么"的卖点）。

```
        普通加密包（头明文）                       头加密包（-mhe）
   ┌───────────────────────────┐        ┌───────────────────────────┐
   │ 7z l -slt  → exit 0        │        │ 7z l -slt  → exit 255     │
   │  Path / Size / Encrypted  │        │  （什么都拿不到）           │
   └────────────┬──────────────┘        └────────────┬──────────────┘
                │                                     │
     ┌──────────┴──────────┐               ┌──────────┴──────────┐
     │ ✅ 体积预算           │               │ ❌ 失效              │
     │ ✅ 条目数预算         │               │ ❌ 失效              │
     │ ✅ 路径穿越预检       │               │ ❌ 失效              │
     │ ✅ 压缩比护栏         │               │ ❌ 失效              │
     │ ✅ 磁盘余量预留       │               │ ❌ 失效（无 declared）│
     └─────────────────────┘               └──────────┬──────────┘
                                                       │ 只剩下：
                                            ┌──────────┴──────────────────────┐
                                            │ 运行期 staging 体积监控（轮询）    │
                                            │ 绝对上限（§6 的总量/单文件硬顶）  │
                                            │ 磁盘余量硬顶（min_free_headroom） │
                                            └─────────────────────────────────┘
```

| 护栏 | 普通包 | 头加密包 | 说明 |
|---|---|---|---|
| 体积预算（`declared_total × 安全系数 + min_free_headroom`） | ✅ | ❌ | 拿不到 `Size` |
| 条目数预算 | ✅ | ❌ | 拿不到条目 |
| 路径穿越预检（扫 `l -slt` 的 `Path`） | ✅ | ❌ | 拿不到 `Path`。**7-Zip 自己会静默拦截穿越（exit 0、零 warning、零 stderr），我们拿不到信号**，所以预检是我们唯一的检测点——而它在这里不存在 |
| 压缩比护栏（`ratio_warn=200` / `ratio_block=1000` 且 >5 GB） | ✅ | ❌ | 分子分母都拿不到 |
| **运行期 staging 体积监控** | ✅ | ✅ | **唯一还活着的** |
| **绝对上限 + 磁盘余量硬顶** | ✅ | ✅ | **唯一还活着的** |
| 解压后的逐条目校验（D5/D6/D8/D9） | ✅ | ✅ | 解完之后一切恢复正常 |

**诚实结论**：对头加密包，GameFlow 在"解开之前"是**盲的**。我们不是靠预判保护自己，而是靠**边解边量 + 撞上限就停**。这意味着：

1. 一个精心构造的头加密解压炸弹**能真的开始往 staging 里写**，只有体积监控会截停它。截停的粒度取决于轮询间隔（§6 的参数），中间已经写下去的字节是真实占用磁盘的。
2. 路径穿越对头加密包**在解压前完全不可见**。缓解只有两条，都在解压之后：（a）staging 是库级的独立目录，穿越写出去的东西不会污染 `games\`；（b）落地前逐条目对账 + D9 的 symlink/junction/reparse point 检查会发现异常。**"缓解"不等于"不会发生"**——穿越如果落在 `_stg` 之外的路径上，那些文件就是已经写出去了。
3. `BLOCKED_RESOURCE` 在这个分支上会比普通包**更常出现**，且触发时**已经消耗了磁盘**。这是设计的代价，不是 bug。

> **这是本 SPEC 里最真实、也最无法消除的一块盲区。** 唯一能收窄它的做法是不接受头加密包——而那等于拒掉相当一部分真实素材，与 C5 冲突。所以我们接受它，并把它写在这里。

---

### 9.6 RAR4 的不可区分性

> 「Command line RAR returns the exit code **11** if it can detect that the user entered a **wrong password**. This code can be returned **only for RAR 5.0 archives**. It is **impossible to distinguish a wrong password and data damage for RAR 4.x archives**.」
> 【官方】WinRAR 退出码文档 https://chmlib.com/WinRAR/html/HELPExitCodes.htm

对 RAR4 包，"密码错"和"分卷损坏 / 下载不完整"在**工具层面不可区分**。这不是 7-Zip 的问题，也不是我们的问题——**是 RAR4 格式本身没有可校验的密码验证字段**。

后果与处置：

| 项 | 内容 |
|---|---|
| 状态 | 落 `EXTRACT_FAILED_AMBIGUOUS`（**不是** `WAITING_FOR_PASSWORD`，**不是** `CORRUPT`——那两个都是在断言我们其实不知道的事） |
| 格式判定 | 魔数：`52 61 72 21 1A 07 00` = RAR4；`52 61 72 21 1A 07 01 00` = RAR5（§5.4.4 的魔数表）【实测】 |
| 报告必须**同时**给出 | ① 两种可能各一句话；② 已试候选数（不是候选内容，见 M3）；③ 卷集完整性结果（D1/D11 的判定）；④ 建议的下一步：先核对分卷数与资源页，再考虑密码 |
| 不做的事 | ❌ 不猜哪种可能性更大 ❌ 不按"试了 N 次都失败所以八成是密码错"这类启发式分流 ❌ 不改写不删除任何文件（I1） |
| 影响面 | RAR5 不受影响（有 exit 11）；7z / ZIP-AES 不受影响（有 HMAC / MAC）；**ZipCrypto 的问题是另一种**：它不是不可区分，而是会**假通过**（1/256），见 §6.3 与 D7 |

**未验证的腿**：本轮六份调研对 RAR 的**真实解码零验证**（macOS 上造不出合法 RAR 样本）。上表的行为契约建立在 WinRAR 官方退出码文档 + 7-Zip 的 RAR handler 会复用同样的格式限制这个推论上。**【待测】真实 RAR5 / 加密 RAR / 分卷 RAR 在 `7z t` 下的退出码与 stderr**（§11）。不实测的后果：`EXTRACT_FAILED_AMBIGUOUS` 的触发条件写不准，可能把 RAR5 的明确密码错也误分流进来（那会让报告变得没用，因为它对每个 RAR 失败都说"两种可能"）。

---

### 9.7 删除的不可逆性

C3（源包与本体同目录）+ C4（解压后自动删除源包）合起来意味着：**删除没有回收站兜底，判错一次就是永久数据丢失**，而这些包往往是几十 GB、要重新走一遍网盘转存 + 下载才能拿回来。

§6.8 的 12 条与条件（D1–D12）就是为这件事设计的。本节的任务不是复述它们，而是说清楚**它们挡得住什么、挡不住什么**。

#### 9.7.1 挡得住（每条对应一个真实的失败模式）

| 判据 | 挡住的真实失败 | 依据 |
|---|---|---|
| D1 / D11 | 分卷缺失、末卷之外不等大、实际卷数 ≠ 声明卷数。**缺中间卷 / 缺末卷 / 单文件截断三种情况 stderr 一字不差**（都是 `Unexpected end of archive`），所以必须自己做连续性 + 等大校验 | 【实测】 |
| D2 | `7z t a.7z b.7z` 只测了一个包却 exit 0（第二个参数是内部过滤器）→ 每个包**单独调用一次** | 【实测】 |
| D2 / D4 | exit 1 = "完成了但有文件被静默跳过"，本项目一律当失败 → 判据是 `exit == 0` **严格相等** | 【官方+实测】 |
| D3 | exit 0 也可能带 `Warnings:`（尾部垃圾）——"有 Warnings"和"exit 1"是**两条独立信号** | 【实测】 |
| D5 | exit 0 且零文件的四种情形（过滤器无匹配 / 多归档参数误用 / 只含空目录 / 尾部垃圾）→ 必须核 `Files: N` 且 `N>0` 且与 `l -slt` 条目数一致 | 【实测】 |
| D6 | 落地缺文件或尺寸对不上 | 【实测】 |
| D7 | **ZipCrypto 的 1/256 假通过**：错密码也能"解出"一堆垃圾文件 →「解出来几个文件」永远不能当密码正确的证据。AES（`compress_type==99`）有 HMAC，不受影响 | 【实测】 |
| D8 | 还有没处理的白名单嵌套容器就删源包 | 【实测】 |
| D9 | 逃逸 symlink / junction / reparse point（**并且永不使用 `-snld20`**——实测它真的创建了逃逸符号链接且 exit 0） | 【实测】 |
| D10 | 只解出说明/密码类文件就当成功（`EXTRACTED_NO_CONTENT`） | 设计判据 |
| D12 | staging→落地移动半途失败、状态没落盘就删了 | 【官方】`File.Replace` 明列三种部分失败中间态 |

另有一条不属于 D1–D12 但同等重要的**留证**要求：**删除前必须把每个源包的 SHA-256 写进 `manifest.json`**（零成本；误删后至少能确认重下的是不是同一个包）。

#### 9.7.2 挡不住（诚实清单 → 显式人工验收项）

以下每一条，**12 条判据全部通过**，源包**会被删掉**，而结果**是错的**。它们在工具层完全看不出来，没有任何自动判据能覆盖，本 SPEC **不假装能自动判**。

| # | 挡不住的情况 | 为什么工具层看不见 | 处置 |
|---|---|---|---|
| **H1** | **缺 DLC / 缺补丁 / 缺附加分卷**：资源页给了"本体 + DLC + 汉化补丁"三个包，只下到本体。本体那个包 D1–D12 **全部通过**（它自己是完整的） | 归档完整性只能说明"这个包没坏"，说明不了"这批资源共有几个包" | **HV-1**（见下）。唯一的**半自动**检测点是批次条目的 `expected_parts` / `cloud_links[]` 数量——而那是**人填的**（§7 的执行体 C 从资源页写回，§5 的 A 用它做 G6/D11 对账）。**人填错，判据就一起错。** |
| **H2** | **版本不对**（下到 v1.00，资源页其实已更新到 v1.02） | 包本身完整 | HV-2 |
| **H3** | **试玩版 / 体验版 / 预告片当成完整版** | 完整的试玩版就是一个完整的归档 | HV-2 |
| **H4** | **缺运行时依赖**（RPG Maker RTP、VC++ Redist、DirectX）导致游戏起不来 | D10 的 `content_files > 0` 会通过——游戏文件确实都在 | HV-3 |
| **H5** | **上游打包时就打错了**（漏文件、放错版本） | CRC 正确、结构正确，错的是内容语义 | HV-2 / HV-4 |
| **H6** | **头加密包在拿到密码前的所有预检** | 见 9.5。注意：**拿到密码解开之后 D1–D12 全部恢复有效**，盲区只在"解开之前" | 9.5；`BLOCKED_RESOURCE` |
| **H7** | **RAR4 无法判定是密码错还是损坏** | 见 9.6 | 这一条**不会误删**（停在 `EXTRACT_FAILED_AMBIGUOUS`，I1 保证不删任何文件），但它意味着"我们无法判定" |
| **H8** | **Defender 没检出的真实恶意内容** | 签名库的固有局限 | 9.8 / 9.9 |

**人工验收项（契约级；§7 的最终验收环节必须逐条出现在界面上，§10 的验收清单必须包含它们）：**

| 编号 | 人工验收项 | 有无自动辅助 |
|---|---|---|
| **HV-1** | 包数/分卷数与资源页一致，DLC 与补丁齐全 | ⚠️ 半自动：`expected_parts` 与 `cloud_links[]` 数量对账（人填的） |
| **HV-2** | 版本号正确，且不是试玩/体验/预告版 | ❌ 纯人工 |
| **HV-3** | 游戏能启动（运行时依赖齐全） | ❌ 纯人工 |
| **HV-4** | 目录结构像一个完整游戏（不是"只有一个 readme 和一个安装器"） | ⚠️ 半自动：D10 的 `content_files > 0` 只是下限，不是充分条件 |
| **HV-5** | 对 `THREAT_SUSPECTED` 的一次性裁决 | ❌ 纯人工（见 9.8） |

> **报告契约**：`_reports\<batch-id>-<seq>.md` 中，任何进入 `COMPLETE` 或 `TRANSLATION_NOT_APPLICABLE` 的条目，**必须原样打印 HV-1..HV-5 这张清单**（哪怕全部未勾选），并在批次汇总顶部注明"以下项目工具无法验证，需人工确认"。**不要把它折叠、不要把它做成"高级选项"、不要在全绿的时候隐藏它。** 呈现层要好读，但好读不等于把坏消息藏起来。

#### 9.7.3 两段式删除

`retention` 默认是**尺寸门控的两段式删除**：`retention.trash_days = 3`、`retention.trash_max_bytes = 2 GiB`（§3.2.3 `guards`）。
**小于**阈值的源包先移入 `games\<dirname>\.gameflow\trash\`，3 天后由执行体 B 清理；**大于等于**阈值的**直接删除**。

这个默认值不是折衷，是按误判概率与代价的实际分布来的：**误判概率最高的恰恰是小包**——纯文本汉化补丁既是压缩比 133x 触 `ratio_warn` 的那一类，也最容易命中 `EXTRACTED_NO_CONTENT` 的边界；而磁盘冲突只在大包上成立。同卷移动是元数据操作，成本接近零，磁盘代价有硬上限（`trash_max_bytes` × 并发条目数）。
C4 的语义仍然满足：源包在游戏目录里**自动消失、无需任何人工动作**，只是物理删除时刻推迟。要字面 C4，设 `retention.trash_days = 0`。

**代价必须写明，且这条代价无法靠默认值消除**：

- **≥ `trash_max_bytes` 的大包**（游戏本体基本都是）走**直接删除**，D1–D12 判错 = **永久丢失，不可恢复**。唯一的补救是 `manifest.json` 里删除前写下的 SHA-256——它只能告诉你"重新下载的是不是同一个包"，**并不保证你还能下到**（资源站失效、分享链接过期、网盘和谐都很常见）。
- **< `trash_max_bytes` 的小包**有 3 天后悔窗口，过期即不可恢复。
- 把 `trash_days` 调大、`trash_max_bytes` 调高，就是拿磁盘换后悔窗口，**没有第三种可能**。

**这条仍列为用户拍板项（§12）**：SPEC 给的是一个有论据的默认值，不是替用户做的最终决定。

---

### 9.8 恶意内容与误报

#### 9.8.1 现实：误报是常态，不是异常

游戏破解补丁、汉化补丁、注册机、RPG Maker / Wolf 引擎的加密 DLL、自解压 SFX 被 Defender 判成 `HackTool:*` / `Trojan:Win32/Wacatac.*` / `PUA:Win32/*` 是**高频事件**，且**绝大多数是误报**【社区，业内共识级】。

所以两个直觉都是错的：

- ❌ **"扫到就停"** → 流水线在正常素材上频繁卡死，用户很快就会把整个扫描关掉（这才是最坏结果）。
- ❌ **"扫到就忽略"** → 真实恶意样本静默落地。

#### 9.8.2 三级分流（状态名严格按 §4）

| 级别 | 判据 | 状态 | 处置 |
|---|---|---|---|
| **A 级 · 明确恶意** | 威胁名含 `Ransom:` / `Backdoor:` / `Worm:` / `TrojanDownloader:` / `Trojan:Script/*`，或严重级别 Severe/High | **`THREAT_CONFIRMED`** | **硬停**。产物留在 `_stg` 不落地，**不删源包**（留证，I1），报告给出文件相对路径 + 威胁名 + SHA-256 + 大小 |
| **B 级 · 疑似误报** | 威胁名含 `HackTool:` / `PUA:` / `Crack` / `KeyGen` / `Wacatac` / `Zpevdo` / `Presenoker` 等历史高误报家族，且命中文件位于 `crack/ 补丁/ 汉化/ patch/` 类目录或是 `.dll`/`.exe` 补丁 | **`THREAT_SUSPECTED`** | **软停**等人工裁决（HV-5）。产物留 `_stg`，**不删源包**。人工放行后按 **SHA-256** 写进白名单，下次同一文件自动放行 |
| **C 级 · 扫描本身失败** | `MpCmdRun` 退出码 2 但输出无威胁名（官方明写 "Scanning errors" 也返回 2），或 `MpCmdRun` 不可用 | **按 B 级处理 → `THREAT_SUSPECTED`** | **不静默放行**。报告里必须区分"扫过了没事"与"没扫成" |

【官方】退出码语义与 `-DisableRemediation` 语义：https://learn.microsoft.com/en-us/defender-endpoint/command-line-arguments-microsoft-defender-antivirus

> **注意退出码 0 有歧义**：「没发现」和「发现了但已清除」都返回 0。所以**不能只看退出码**。

#### 9.8.3 `-DisableRemediation` 的二选一（必须显式选，SPEC 选了第一个）

官方原文：

> "Valid only for custom scans. · **File exclusions are ignored.** · Archive files are scanned. · **Actions aren't applied after detection.** · **Event log entries aren't written after detection.** · Detections from the custom scan aren't displayed in the user interface. · **Detections from the custom scan are displayed in the command output.**"

| 选项 | 得到 | 失去 |
|---|---|---|
| **✅ 采用：`-Scan -ScanType 3 -DisableRemediation -File <path>`** | Defender **不会在你不知情时删掉游戏文件**；忽略排除目录（所以 `_stg` 的排除项不会让预检瞎掉——**这正是整套组合成立的关键**）；归档文件本身也会被扫 | **事件日志和 `Get-MpThreatDetection` 都看不到这次检出**（官方明写）→ **stdout 是唯一信号** |
| ❌ 不采用：放弃 `-DisableRemediation` | 可事后 `Get-MpThreatDetection` 查询 | Defender 会**真的动文件**（隔离/删除），与"验证通过才删源包"的判据直接打架，制造假失败 |

**代价（必须写进 §11）**：**【待测】`MpCmdRun -Scan -ScanType 3 -DisableRemediation` 的 stdout 格式**——威胁名怎么打印、多个威胁怎么分行、无威胁时打印什么。**不实测就写不出解析器**，也就写不出 9.8.2 那张表的判据。验证方法：用 EICAR 样本跑一次。

配套的**已知副作用**：实时保护会在文件写入/关闭时扫描，**解压过程中命中签名的文件可能被 Defender 在 7-Zip 还在写的时候隔离掉**，7-Zip 那边表现为"拒绝访问"/"找不到文件"/"CRC 失败"等误导性错误【官方（实时保护扫描时机）+ 社区（表现形态）】。**【待测】§11：解压含 EICAR 串的样本，观察是假失败还是干净报错。** 这是 `_stg` 需要排除项的真实原因。

#### 9.8.4 排除项：只排两条，绝不排整个库

> **不要为了流程顺畅把整个 `D:\GameLib` 加进 Defender 排除项。** 那等于对**最可能带毒的目录**关掉实时保护，是一笔**净负面**的安全交易。官方自己的话是：「You usually don't need to define exclusions for Microsoft Defender Antivirus.」【官方】

允许的排除项**一共只有两条**，每条必须是用户一次性明示、写进日志、可回滚：

| # | 排除路径 | 理由 | 归属 |
|---|---|---|---|
| **X1** | `D:\GameLib\_stg\` | 解压中转区，避免实时保护在解压途中隔离文件造成假失败。**产物在移出 `_stg` 之前必须过一次 `-DisableRemediation` 显式扫描**（它忽略排除目录，所以这一步不受 X1 影响） | 阶段一（执行体 B） |
| **X2** | `<MTool 安装目录>` | MTool 的 `version.dll` **哈希随游戏实例变化**（Kaspersky 版主确认「文件在不同游戏实例下哈希不同，导致逐个加排除项无效」）→ **SHA-256 白名单在原理上就不可能工作**，只能目录级排除【官方论坛帖】https://forum.kaspersky.com/topic/false-positive-mtool-translation-tool-components-versiondll-incorrectly-flagged-as-dllhijack-58061/ | 阶段二（执行体 C） |

**明确不排除**：`D:\GameLib\`（库根）、`D:\GameLib\games\`（游戏目录）、`D:\GameLib\_inbox\`。游戏目录保留实时保护。

放行白名单分两套，**不能混**：

- **阶段一的归档与解压产物** → 按 **SHA-256** 白名单（这些文件哈希固定，白名单可行；按路径放行是错的）。
- **阶段二的 MTool 注入产物**（`version.dll` 等）→ **不进 SHA-256 白名单**（原理上不可能），走 X2 的目录级排除 + `manifest.json` 文件快照差集的"已知豁免类别"。豁免必须是用户一次性明示，写进 `rules/`，**不是模型自作主张**。

**误报应急（用户一定会遇到，必须写进用户文档）**：
- 验证排除是否生效：`MpCmdRun.exe -CheckExclusion -Path <路径>`（输出 "is excluded. Exit code is 0." / "is not excluded. Exit code is 1."）【官方】
- 被误删的文件可以找回：`MpCmdRun.exe -Restore -ListAll` / `-Restore -Name <threat> -Path <还原到哪>`【官方】

#### 9.8.5 SFX：当归档解，绝不执行

自解压包（SFX）在本流水线里**只被当作归档读取**（`7z l` / `7z t` / `7z x`），**任何情况下都不执行它**。这不是性能取舍，是安全取舍：执行一个来源不明的 exe 是我们整条流水线里唯一能做的、真正不可逆的危险动作，而 7-Zip 完全不需要执行它就能解开【实测：伪装扩展名、前置垃圾字节（<8 MB）、图种、SFX 全部无需改名即可 `l/t/x`】。

**【待测】§11：真实 SFX `.exe` 的 `7z l -slt` 输出（`Type` 与 `Offset`）。** Windows 上 `.exe` 是**合法 PE**，PE handler 可能抢先解资源节而不是尾随的归档负载——本轮用的是人造 stub，这条是最需要 Windows 复验的一项。不实测的后果：SFX 可能被识别成"PE 归档"从而解出一堆 PE 资源节碎片（而不是游戏文件），D10 的 `content_files` 判定会给出莫名其妙的结果。

对照：`NSIS` / `Inno` 安装器 / `setup.exe` / `.iso` 等镜像 / 未知 MZ **不进自动流程**，停 `WAITING_FOR_MANUAL_INSTALL`（§6.13）。它们与 SFX 的分界同样是"能不能当归档完整解开"，不是"是不是 exe"。

---

### 9.9 威胁模型与明确不防的

#### 9.9.1 本项目防什么

**GameFlow 防的是「意外损坏与误删」，不是「定向攻击」。**

| 防 | 手段 | 章节 |
|---|---|---|
| 下载不完整被当成完整 | `DOWNLOAD_STABLE` 的六重与条件 G1∧…∧G6 | §5 |
| 分卷不齐被当成齐 | D1 / D11 + 卷集连续性与等大校验 | §5 / §6 |
| 解压半成功被当成成功 | D2–D6（exit 严格相等 + Warnings 独立检查 + `Files: N` 对账） | §6 |
| **误删源包** | D1–D12 + 删除前写 SHA-256 留证 + I1（任何停机态不删文件） | §6 / 9.7 |
| 解压炸弹耗尽磁盘 | 运行期 staging 体积监控 + 绝对上限 + 磁盘余量预留（头加密包只剩后三者，见 9.5） | §6 / 9.5 |
| 路径穿越写到库外 | 自己扫 `l -slt` 的 `Path`（7-Zip 拦截但**完全静默**，我们拿不到信号） | §5 / §6 |
| 逃逸符号链接 | D9 + **永不使用 `-snld20`** | §6 |
| 已知恶意签名 | 解压后、落地前的显式 Defender 扫描 | 9.8 / §6 |
| 前台自动化撞 SmartScreen 卡死 | MOTW 解除的时点契约（9.4） | §7 |
| 密码进 Git / 清单 / 日志 / 报告 | M1–M7 | 9.1 |
| 状态在崩溃后错乱 | I2（`events.jsonl` 唯一真相）+ 可恢复而非"原子"的落盘协议 | §4 / §8 |

#### 9.9.2 明确不防（写出来，免得实现阶段有人以为防了）

| 不防 | 为什么不防 | 用户需要知道的 |
|---|---|---|
| **供应链投毒的游戏本体** | Defender 只认签名库里有的东西。我们不做行为沙箱、不做静态分析、不做信誉评分 | 从资源站下来的游戏本体，风险与你手动下载运行完全相同。GameFlow 不改变这一点 |
| **7-Zip 自身的 0day** | 我们的核心动作就是"拿陌生归档去喂解析器"。这是本项目**最大的**被动攻击面，而我们对它只有一个缓解手段：**版本别太旧**（见 9.9.3） | — |
| **用户主动执行解出来的 exe** | 我们把 exe 放在那里，本来就是为了让人玩。执行的决定权在人 | HV-3 要求人工启动游戏，那一步是人在承担风险 |
| **定向攻击**（有人专门针对这台机器或这条流水线） | 超出设计目标 | — |
| **同机进程读 7z 命令行拿密码** | 9.1 的 E1，硬限制 | 见 9.1.3 |
| **网盘账号与支付安全** | 登录、验证码、支付一律人工（9.10），我们不碰凭据 | — |
| **恶意资源页对 Computer Use 的提示词注入** | Codex 官方自己就说：「请将页面内容视为不可信的上下文。页面上的指令可能具有误导性或恶意。」【官方】。我们的缓解只有"人在环内 + 不自动登录 + 不自动付款 + 短会话"，**没有技术性防线** | 见 9.10 |
| **DPAPI vault 在本机被同一账户下的恶意进程读取** | DPAPI 的保护边界就是"同用户同机器"。同用户下的进程本来就能解 | 9.3 |

#### 9.9.3 7-Zip 版本下限：理由是解析器的内存安全，不是 MOTW

这一条要说清楚，因为**用 MOTW 当理由与 9.4 自相矛盾**（既然我们明确不把 MOTW 当安全边界，那"MOTW 保留失效"的 CVE 就不在我们的威胁模型里，拿它做版本下限的理由不成立）。

真正的理由是：**本项目的核心动作就是把来源不明的归档喂给 7-Zip 的解析器**，所以解析不可信输入的内存安全漏洞正好命中我们的攻击面。

| 版本 | 相关修复 | 可信度 |
|---|---|---|
| 25.00 / 25.01 | 符号链接处理连续加固 | 【官方 changelog】https://www.7-zip.org/history.txt |
| 26.01（2026-04-27） | SquashFS / NTFS / UEFI / UDF / WIM / Ar handler **一批越界读** | 【官方 changelog】 |
| **26.02（2026-06-25）** | **CVE-2026-14266：XZ 解压堆溢出 RCE** ← 这条才是致命的 | 【官方 changelog】 |
| 26.03（2026-09-03） | CVE-2026-58052：MOTW 保留失效 —— **与我们无关**（9.4） | 【官方 changelog】 |

**版本档位（契约级，Preflight PF-01 判定）：**

```
version < 25.01              → V-FAIL   Preflight FAIL，阻断执行体 B（不启动带解压的作业）
25.01 ≤ version < 26.02      → V-WARN   允许运行；报告顶部长期显示一条提示，不可关闭
version ≥ 26.02              → V-OK     正常
```

- **V-WARN 是"不拒绝运行"那一档**：落后于**建议线**（26.02）时只提示不阻断，因为 26.02 距本 SPEC 撰写只有两个多月，且 E3 还没告诉我们那台机器上装的是什么。
- **V-FAIL 是硬下限**：低于 25.01 不接受。低于这条线的构建，符号链接加固都还没有，而我们正在用 D9 防逃逸符号链接——那道判据在旧版本上等于在补一个上游还没修的洞。
- 实际版本号必须写进 `state.json.env.sevenzip_version` 并在**每一份**批次报告里显示（不只是 WARN 的时候）。

报告顶部的 V-WARN 提示文案（写死，避免每次实现自己发挥）：

> ⚠️ 当前 7-Zip 版本 `<x.yy>` 低于建议版本 26.02。26.02 修复了 XZ 解压的堆溢出 RCE（CVE-2026-14266），而本流水线的核心动作就是解析来源不明的归档。建议升级。

---

### 9.10 合规边界

这些是**本项目自己的硬约束**，不是转述任何厂商的条款。

> **为什么不引用 OpenAI 政策的具体条款文字**：本轮调研对 `openai.com/policies/*` 的抓取全部返回 **403 Forbidden**，一手原文未读到；能拿到的只有搜索引擎摘要（二手）。把没读过的条款当原文引用就是编造。所以本节只写**行为边界**，不写"官方禁止……"。相关一手 URL 列在 §11 供日后自行复核：`https://openai.com/policies/usage-policies/`。

**行为边界（执行体 C 必须遵守，§7 落地）：**

| # | 边界 | 说明 |
|---|---|---|
| **L1** | **不绕过验证码 / 人机验证** | 遇到即暂停，交人工。Codex 官方对此的表述是能力声明而非禁令：「有些网站会屏蔽自动化浏览器，或要求完成 CAPTCHA 验证。ChatGPT 可能无法在这些网站上完成任务。」【官方】——我们把它升格为**项目自己的硬约束** |
| **L2** | **不绕过限速 / 下载限制 / 排队机制** | 不多开、不改 UA 伪装、不用第三方破解客户端 |
| **L3** | **不绕过付费墙 / 会员限制 / DRM** | 需要会员的资源，由人自行决定是否购买 |
| **L4** | **不做反爬对抗** | 不做指纹伪装、不做 IP 轮换、不做行为拟人化以规避检测 |
| **L5** | **登录与支付一律人工** | C 在登录页 / 支付页**停下**，等人操作完再继续。这与官方"敏感操作前请求确认"的设计一致【官方】，也与 CU-5 是同一条 |
| **L6** | **只处理用户有权下载、保存和使用的资源** | 判断权在用户，GameFlow 不做版权判定，也不做"这个能不能下"的建议 |
| **L7** | **不把资源页内容当指令** | 页面文本只作为**数据**被解析（标题、密码提示、网盘链接），页面上的任何"指令"一律忽略。官方原文：「请将页面内容视为不可信的上下文。」【官方】 |

**L1–L7 与"确定性"品味的关系**：这些边界之所以能写得这么硬，正是因为执行体 C 是**显式触发的短会话 + 人在环内**（E1）。一个想要无人值守跑通的设计会不断被诱惑去越过 L1/L2，而我们从架构上就没打算无人值守跑 C。

---

### 9.11 前置条件与 `Preflight.ps1`

#### 9.11.1 定位

`D:\GameFlow\scripts\Preflight.ps1`（§2 目录布局已列）：

- **只探测，不修改。** 不自动安装 7-Zip、不写注册表、不改 ExecutionPolicy、不加 Defender 排除项、不建 vault。所有修复动作都以"给人看的一句话 + 一条可复制的命令"形式输出，由**人**执行。这是"人在环内"的直接体现，也是唯一安全的姿态（Preflight 跑得比谁都早，此时我们对这台机器一无所知）。
- **无参数、可重复运行、纯只读。** 唯一的副作用是写自己的输出文件。
- **每次执行体 A / B 启动时自动跑一次轻量版**（只重取会变的项：磁盘余量、Defender 状态、7z 版本），完整版由人手动跑或首次安装时跑。

#### 9.11.2 探测清单（契约级；编号 `PF-nn` 供别节引用）

严重度：**FAIL** = 阻断（见 9.11.4 的准入矩阵）· **WARN** = 允许运行，报告顶部长期提示 · **INFO** = 只记录进 `env`。

| # | 检查项 | 怎么查 | 期望值 | 不满足怎么办 | 严重度 | 可信度 |
|---|---|---|---|---|---|---|
| **PF-01** | `7z.exe` 在 PATH，且版本达标 | `Get-Command 7z.exe`；版本取 `7z` 无参数运行的 banner 前两行 | 存在 且 版本 ≥ 25.01（建议 ≥ 26.02） | 不存在 → 见 PF-02。版本 < 25.01 → **V-FAIL**，给出官方下载页与"升级后重跑 Preflight"。25.01–26.01 → V-WARN，报告顶部长期提示（文案见 9.9.3） | FAIL / WARN | 命令形状【社区】；**banner 的确切文本【待测】**，解析器必须容错（正则取第一个 `\d+\.\d+`），不要按固定列偏移切 |
| **PF-02** | **是否只装了 WinRAR / Bandizip 的 GUI**（E3 没指明装的是哪个） | ① 注册表卸载键枚举显示名含 `7-Zip` / `WinRAR` / `Bandizip`；② 探测已知安装路径下的可执行文件；③ `Get-Command` 探 `7z.exe` / `7za.exe` / `Rar.exe` / `UnRAR.exe` / `bz.exe` | **必须有 7-Zip 的命令行 `7z.exe`** | 输出补齐路径：从 https://www.7-zip.org 装官方版（默认会装 `7z.exe`），或下载 7-Zip Extra 拿独立控制台版；把安装目录加进 PATH，**或**把绝对路径填进配置（`sevenzip_path`，见 PF-03）。**在补齐之前 A 与 B 都不启动。** 已有的 WinRAR/Bandizip **不需要卸载**，也**不作为替代方案**——本 SPEC 的每一条判据（退出码、`-slt` 字段名、`-t*` 嗅探语义）都是按 7-Zip CLI 写的，换引擎等于整套判据作废 | **FAIL** | 判定逻辑是设计决策；**具体注册表键名与 Bandizip/WinRAR 的 CLI 可执行名【待测】**——不实测就只能靠 `Get-Command` 的宽松探测，可能把"装了 GUI"误报成"什么都没装"（后果是提示文案不准，不影响正确性） |
| **PF-03** | 7-Zip 的绝对路径可用 | 记录 `(Get-Command 7z.exe).Source` 或配置里的 `sevenzip_path` | 一个存在的绝对路径 | 无 → 同 PF-02 | FAIL | 【社区】。**必须记录绝对路径**：Codex 的 Windows 原生 agent 跑 `powershell.exe -NoProfile`，`$PROFILE` 里的别名/函数不可见，工具要么全路径要么在 PATH【官方 + 实测 strings】 |
| **PF-04** | 库所在卷的**文件系统** | `[System.IO.DriveInfo]::new('D:\').DriveFormat` | `NTFS` | exFAT / FAT32 → **MOTW 整个逻辑不存在**（9.4），降级为 no-op 并在报告注记；`File.Replace` 与 ADS 相关行为同样需要复审 | WARN | 【官方】MOTW 只在 NTFS 上存在；【官方】`DriveInfo` https://learn.microsoft.com/en-us/dotnet/api/system.io.driveinfo.availablefreespace |
| **PF-05** | `_stg` 与 `games\` **同卷** | 比较两者的 `DriveInfo.Name` | 相同 | 不同 → **FAIL**。`File.Replace` **跨卷直接抛异常**，staging→落地会退化成复制+删除【官方】。修复：把 `_stg` 移到与库同卷 | **FAIL** | 【官方】https://learn.microsoft.com/en-us/dotnet/api/system.io.file.replace |
| **PF-06** | `$PSVersionTable.PSVersion` | 直接读 | 记录 | INFO 记录。注意 Codex 那一层永远是 **Windows PowerShell 5.1**（`powershell.exe -NoProfile`），这条改变不了 | INFO | 【实测 strings·codex 二进制】 |
| **PF-07** | `pwsh.exe` 是否存在 | `Get-Command pwsh.exe` | 存在 | 不存在 → **FAIL**（脚本内部按 7.4 语义写，`#Requires -Version 7.4` 会拒绝在 5.1 下运行）。修复：装 PowerShell 7.4+，或由 §8 决定是否降级脚本内部语义 | **FAIL** | 【官方】https://learn.microsoft.com/en-us/powershell/scripting/install/install-powershell-on-windows |
| **PF-08** | **系统 ACP** | `Get-ItemProperty 'HKLM:\SYSTEM\CurrentControlSet\Control\Nls\CodePage' -Name ACP` | 记录数值，**不断言必须是 936** | 只记录。ACP 是解释密码失败的上下文（§6.3 的 `PASSWORD_ENCODING_SUSPECT`）与目录名 ACP 可表示性判定（§3.2A）的输入 | INFO | 【社区】注册表位置来自调研；**不得**写死期望值——用户的区域设置未知，若是日文 932 则整套 GBK 换算不成立 |
| **PF-09** | **Beta UTF-8** 状态 | 同 PF-08：`ACP == 65001` 即已勾选「Beta：使用 Unicode UTF-8 提供全球语言支持」 | 记录 | 只记录 + WARN。勾选后系统 ANSI 代码页从 GBK(936) 变成 UTF-8(65001)，**用 GBK 环境打的中文密码包就打不开**，含中文名的 WinRAR 包也会乱码。中文密码全败时报告里必须显示这一项 | WARN | 【社区】资源站教程原话「语言里的区域关闭 beta 版本」+ 编码机理【实测·macOS Python/Info-ZIP 造包，字节层事实可外推】 |
| **PF-10** | `LongPathsEnabled` | `Get-ItemProperty 'HKLM:\SYSTEM\CurrentControlSet\Control\FileSystem' -Name LongPathsEnabled` | 记录 | **只记录，不作为放宽 MAX_PATH 预算的依据**。§2 的预算表（`games\` 前缀 17 字符、建目录上限 248）照常执行 | INFO | 【官方】注册表位置；**【待测】** `LongPathsEnabled=1` 时 `powershell.exe`/`pwsh.exe` 对 >260 路径的实际行为（还要 exe manifest 声明 `longPathAware`，PS 7.x 曾缺这个 manifest）。不实测的后果：只能按"从源头压短路径"走——**这也正是本 SPEC 的默认取舍，所以这条不实测不阻塞任何设计** |
| **PF-11** | `Get-ExecutionPolicy -List` **全部作用域** | 直接读 | `MachinePolicy` / `UserPolicy` 为 `Undefined` | 若 `MachinePolicy` 或 `UserPolicy` **非** Undefined（GPO 强制）→ **FAIL**：`-ExecutionPolicy Bypass` 参数**无效**，必须给用户明确处置路径（联系域管，或改用签名脚本）。若只是 `LocalMachine=Restricted` → WARN，`powershell -ExecutionPolicy Bypass -File …` 在**进程级**可绕开 | FAIL / WARN | 【官方】cmdlet；GPO 优先级【社区】。**注意**：不要照抄"默认 Restricted 所以跑不起来"这种断言——很多机器早已被设成 `RemoteSigned` |
| **PF-12** | 库根**不在** OneDrive / `Program Files` 下 | 路径前缀比对 `$env:OneDrive`、`$env:ProgramFiles`、`$env:ProgramFiles(x86)`；并检查库根是否被 OneDrive"移动的文件夹"覆盖 | 都不在 | **FAIL**。OneDrive 有社区证据导致 MTool inject 失败；`Program Files` 写不进 `version.dll`。修复：把库根移到普通数据盘目录 | **FAIL** | 【社区】https://bbs.mtool.app/topic/949/ ；`Program Files` 的写权限限制【社区】 |
| **PF-13** | 磁盘余量 | `[System.IO.DriveInfo]::new('D:\').AvailableFreeSpace` | ≥ `min_free_headroom`（§6 参数） | 低于阈值 → **FAIL** 于执行体 B（不开新的解压），A 照常巡检并落 `BLOCKED_RESOURCE` | FAIL(B) | 【官方】DriveInfo |
| **PF-14** | **vault 存在性与身份可解性** | `Test-Path %LOCALAPPDATA%\GameFlow\vault.clixml`；存在则尝试解一条**哨兵条目** | 存在且当前身份能解开 | 不存在 → INFO（首次使用正常），并打印 9.3 的运维警告**一次**。存在但解不开 → **FAIL**：身份不匹配（换过账户/换过机器/被 S4U 任务创建过），给出"用当前账户重建 vault"的路径 | FAIL / INFO | 【社区】DPAPI 绑定；**【待测】§11 跨用户/跨机的实际表现** |
| **PF-15** | **命令行审计状态**（决定 9.1 的暴露程度） | `auditpol /get /subcategory:"Process Creation"`；注册表 `ProcessCreationIncludeCmdLine_Enabled`；`Get-Service Sysmon*`；枚举已知 EDR 服务名 | 记录 | **只记录，不阻断。** 若任一为开启 → 报告里显示一行："本机开启了进程命令行审计 / Sysmon，归档密码会进入审计记录（见 §9.1）"。**GameFlow 绝不为了降低暴露而去关闭审计**——那是拿系统安全换项目便利 | INFO / WARN | 【社区】探测方法来自调研；**该机实际状态【待测】** |
| **PF-16** | Defender 状态与现有排除项 | `Get-MpComputerStatus`（实时保护是否开启）；`Get-MpPreference` 的 `ExclusionPath`；`MpCmdRun.exe -CheckExclusion -Path <_stg>` | 实时保护开启；排除项**至多**是 X1/X2 两条 | 若发现整个 `D:\GameLib` 或 `games\` 在排除项里 → **WARN 并显著提示**这是净负面交易（9.8.4），给出 `Remove-MpPreference` 回滚命令。实时保护关闭 → WARN（我们的预检还能跑，但纵深没了） | WARN | 【官方】`-CheckExclusion` 输出语义；`Get-MpComputerStatus` 字段语义【社区】 |
| **PF-17** | `MpCmdRun.exe` 的**绝对路径** | 依次探 `%ProgramFiles%\Windows Defender\MpCmdRun.exe` 与 `%ProgramData%\Microsoft\Windows Defender\Platform\<版本>\MpCmdRun.exe`（后者更新，**默认不在 PATH**） | 找到一个存在的路径 | 找不到 → **FAIL** 于执行体 B（没有 Defender 预检就不允许把产物落地；C 级分流会让每个条目都停在 `THREAT_SUSPECTED`，那不是可用的状态） | FAIL(B) | 【官方】两个路径均见 MS Learn |
| **PF-18** | **提权能力** | 当前进程是否 elevated；若否，能否弹 UAC | 执行体 B 的会话可提权 | `MpCmdRun` **官方明写需要提权运行**。不可提权 → FAIL(B)，并说明这就是 Defender 预检归属 B 而非计划任务 A 的原因 | FAIL(B) | 【官方】「You need to run MpCmdRun in an elevated Command Prompt」https://learn.microsoft.com/en-us/defender-endpoint/command-line-arguments-microsoft-defender-antivirus |
| **PF-19** | **MTool 安装位置与签名状态** | 定位安装目录；`Get-AuthenticodeSignature <MTool 主程序>`；检查是否落在 OneDrive / `Program Files` 下 | 目录不在 OneDrive 下；签名状态**记录**下来 | 位置不合规 → WARN（会导致 inject 失败【社区】）。签名状态直接决定执行体 C 能不能自动操作 MTool：**未签名 → 落 `computer_use.default_app_access` 兜底；签名存在但无法明确验证 → 直接被 block**【官方 Codex 配置参考原文】。签名不明 → WARN + 提示 C 可能无法自动打开 MTool | WARN | 【官方】Codex 的 `computer_use.windows.exes` 匹配语义；**MTool 实际签名状态【待测】§11** |
| **PF-20** | **目录可写性** | 对 `_inbox` / `_stg` / `_reports` / `batches` / `games` 各建一个临时文件再删（**不是查 ACL**） | 全部成功 | 任一失败 → **FAIL**，报出具体目录 | **FAIL** | 设计决策：ACL 计算在 Windows 上不可靠，实测写入才算数 |
| **PF-21** | **计划任务的 LogonType** | `Get-ScheduledTask` → `Principal.LogonType` | **不是** `S4U`、不是 `ServiceAccount` | 是 S4U/SYSTEM → **FAIL**：那样的任务解不开 DPAPI vault（9.3 的 V4/V5）。不过按 §2，A 本来就不碰密码，所以这条只在有人把 B 也塞进计划任务时才会触发——**探测它就是为了防这个** | FAIL | 【官方】https://learn.microsoft.com/en-us/windows/win32/taskschd/principal-logontype |
| **PF-22** | **Codex 双根沙盒配置** | 读 Codex 配置里的 `sandbox_workspace_write.writable_roots` 是否包含 `D:\GameLib` | 包含 | 不包含 → **FAIL** 于 Codex 触发路径（project 是 `D:\GameFlow`、数据在 `D:\GameLib`，不配就第一步写文件就失败） | FAIL(Codex) | 【官方】配置键存在；**【待测】§11 沙盒的实际边界与 Windows 上的配置文件路径** |
| **PF-23** | Pester 版本 | `Get-Module Pester -ListAvailable` | 记录 | INFO。Windows 10/11 预装的是 **3.4.0**（语法不兼容 Pester 5）→ §10 必须显式钉最低版本或零依赖自建 harness | INFO | 【社区】 |

**探测清单里刻意**不**包含的项**：

- ❌ 不探测"是否装了杀软以外的安全软件"——超出范围，且探测本身容易误判。
- ❌ 不探测网盘客户端——它们的临时物模式由 `netdisk-profiles.json` 配置，**首次使用时人工观察一次并登记**（这是 §2 定义的"阶段一人工环节"之一），不是 Preflight 能自动推断的【待测】§11：阿里/夸克/123 三家的临时后缀与落盘目录**查不到任何权威资料**。
- ❌ 不探测游戏引擎——那是每个条目的运行时判定（§6/§7），不是环境属性。

#### 9.11.3 输出契约

Preflight 产出**两份**东西：

**（1）完整报告** → `%LOCALAPPDATA%\GameFlow\preflight.json`
与 vault 同目录，理由相同：这是**机器本地**的事实，既不该进 Git（`D:\GameFlow`），也不属于任何一个批次（`D:\GameLib\_reports\` 的命名契约是 `<batch-id>-<seq>.md`）。

```jsonc
{
  "schema": "gameflow.preflight/1",
  "captured_at": "2026-09-12T04:16:30.0000000Z",
  "machine": "…",                      // 计算机名，用于发现"vault 是别的机器建的"
  "user": "…",                         // 运行身份，DPAPI 绑定的那个
  "verdict": "WARN",                   // PASS | WARN | FAIL（= 所有检查项的最高严重度）
  "blocking": ["A", "B", "C", "CODEX"],// 空数组 = 什么都不阻断；见 9.11.4
  "checks": [
    { "id": "PF-01", "name": "7z.exe 与版本", "severity": "WARN",
      "observed": "25.01", "expected": ">= 26.02（硬下限 25.01）",
      "verdict": "WARN", "blocks": [],
      "remedy": "从 https://www.7-zip.org 升级到 26.02 或更高，然后重跑 Preflight",
      "evidence": "C:\\Program Files\\7-Zip\\7z.exe" }
    // … PF-02 … PF-23
  ]
}
```

**（2）`state.json.env` 投影**（这是别节真正消费的东西）
§3.4.1 已锁定 `env` 的字段集，Preflight 是它**唯一的**生产者。对应关系写死如下：

| `state.json.env` 字段 | 来源 | 备注 |
|---|---|---|
| `captured_at` | Preflight 运行时刻 | ISO 8601，UTC |
| `acp` | PF-08 | 整数，如 `932` / `936` / `65001` |
| `beta_utf8` | PF-09 | 布尔；`acp == 65001` 即 true |
| `sevenzip_version` | PF-01 | 字符串，如 `"26.02"` |
| `sevenzip_path` | PF-03 | **绝对路径** |
| `long_paths` | PF-10 | 布尔 |
| `fs_type` | PF-04 | `"NTFS"` / `"exFAT"` / … |
| `ps_version` | PF-06 | 字符串 |
| `mtool_build` | 阶段二（§7）运行时填 | Preflight 写 `null`；MTool 只有 build 标识且会自动更新，**不写死版本号** |

> **契约**：`env` 是**解释失败用的上下文，不是判据**。没有任何状态迁移可以读 `env` 来决定走哪条分支。它的唯一用途是：条目失败时，报告能说出"当时的环境长这样"。
> **写入时机**：每个条目在**进入 `EXTRACTING` 之前**把当时的 `env` 快照写进自己的 `state.json`。这样一批跑到一半用户升级了 7-Zip，前后两半条目各自记的是各自当时的值——不追求全局一致，追求**可解释**。

**（3）报告顶部的环境条**（呈现层，§6 的报告模板消费）
每份 `_reports\<batch-id>-<seq>.md` 顶部固定一行环境摘要，格式写死：

```
环境  7-Zip 25.01 ⚠️低于建议 26.02 · PS 7.4.6 · NTFS · ACP 932 · LongPaths ON · Defender RTP ON · 排除项 1/2
```

WARN 项用 ⚠️ 标出并**永远显示**（不折叠）。这是 9.9.3 说的"报告顶部长期提示"的落地形态。

#### 9.11.4 准入矩阵（谁被什么阻断）

```
                        PF-02  PF-05  PF-07  PF-11  PF-12  PF-13  PF-17  PF-18  PF-20  PF-21  PF-22
                        7z CLI 同卷   pwsh   GPO   位置   余量   MpCmd  提权   可写   S4U   沙盒
执行体 A（巡检）          ✖      ✖      ✖      ✖      ✖      ·      ·      ·      ✖      ✖      ·
执行体 B（作业）          ✖      ✖      ✖      ✖      ✖      ✖      ✖      ✖      ✖      ✖      ·
执行体 C（前台）          ·      ·      ✖      ✖      ✖      ·      ·      ·      ✖      ·      ·
Codex 触发路径            ·      ·      ·      ✖      ·      ·      ·      ·      ·      ·      ✖

✖ = 该项 FAIL 时此执行体不启动     · = 不阻断
```

- **阻断的表现形式**：脚本**立即退出并打印修复清单**，退出码非 0，**不落任何状态、不改任何文件**。不允许"降级运行"——一个在错误地基上跑起来的流水线比不跑更危险（它会写出看起来正常的状态）。
- PF-13（磁盘余量）只阻断 B：A 仍应巡检并把条目落到 `BLOCKED_RESOURCE`，因为"磁盘满了"是一个需要被**记录和报告**的状态，不是一个应该让巡检静默罢工的理由。
- WARN **从不**阻断任何执行体，只进报告。

#### 9.11.5 Preflight 明确不做的事

| 不做 | 为什么 |
|---|---|
| 自动安装 7-Zip / PowerShell 7 | 从网上拉二进制并静默安装，是本项目里比"解压陌生归档"风险更高的动作。给命令，让人做 |
| 自动改注册表（`LongPathsEnabled`、ACP、Beta UTF-8） | 全局影响，可能弄坏用户机器上别的东西。**尤其是 Beta UTF-8**：关掉它会改变所有程序的 ANSI 行为 |
| 自动改 ExecutionPolicy | 同上；且 GPO 强制时改也没用（PF-11） |
| 自动加 Defender 排除项 | 9.8.4 已定：排除项必须是用户**一次性明示**、写进日志、可回滚的动作 |
| 自动创建 vault | vault 的创建身份决定了它能不能被解开（9.3 V1），必须由人在正确的账户下明确地做一次 |
| "自动修复后重试" | 与"确定性、人在环内"直接冲突。Preflight 的输出是**一张清单**，不是一个修复流程 |

---

### 9.12 为什么这一节不能跳过

E3 给出的全部环境事实是一句话：「已装 **7-Zip 或 WinRAR/Bandizip**（没指明哪个）、已装 Windows 版 Codex、已在用 MTool」。

在这句话上：§5 的 `7z l -slt` 只读预检假设了 `7z.exe` 存在；§6 的退出码判据假设了引擎是 7-Zip 而不是 WinRAR；§3.2A 的目录名 ACP 可表示性判定假设我们知道 ACP 是多少；§8 的落盘协议假设 `.gameflow` 与库同卷且是 NTFS；§6 的 Defender 预检假设 `MpCmdRun.exe` 存在且会话能提权；§7 假设库根不在 OneDrive 下。**这六个假设，一个都还没被证实。**

> **不做 PF-01..PF-23 这张清单，本 SPEC 的每一节都建在沙上。**

---

> ### ⚠️ 遗留异议（起草者对某条锁定决策的保留意见，主控未采纳）
>
> **异议对象**：§6.2 的「7-Zip 硬下限 **25.01**，建议 ≥26.02」。
>
> **照契约已写**：9.9.3 严格按 25.01 = 硬下限、26.02 = 建议线实现（`V-FAIL` / `V-WARN` / `V-OK`）。
>
> **异议理由**：这条下限被设在了它自己引用的那个 CVE 的**下方**。契约给出的版本理由是"解析不可信归档的内存安全漏洞"，其中唯一一条明确的 RCE 是 **CVE-2026-14266（XZ 解压堆溢出，26.02 修复）**。而本项目的核心动作——执行体 A 对每一个下载完成的陌生归档跑 `7z l -slt`、执行体 B 跑 `7z t/x`——正是这个漏洞的触发路径，且执行体 A 是**计划任务里无人值守跑的**。也就是说：一台装着 26.01 的机器，在 GameFlow 下会被判为 V-WARN 正常运行，同时每隔 N 分钟自动把来路不明的归档喂给一个有已知 RCE 的解析器。仲裁把下限从 26.03 降下来的理由（"26.03 发布只有 9 天，硬卡最新版是很硬的运维要求"）我认同，但那个理由只支持**不卡 26.03**，不支持**降到 25.01**——26.02 已发布两个多月，且 7-Zip 的升级是一次覆盖安装，不涉及配置迁移【社区】。
>
> **建议改法**：硬下限提到 **26.02**；把 25.01–26.01 从"WARN 照常跑"改成"**允许运行，但每次执行体 B 的会话开始时需人工确认一次**"（与 9.8 的 `THREAT_SUSPECTED` 一样的人在环内姿态，成本一次点击）；低于 25.01 仍然 FAIL。这样既不强迫用户追最新版，也不让一个已知 RCE 在无人值守的计划任务里静默暴露。
>
> **若主控维持原判**：建议至少在 V-WARN 的报告文案里点名 CVE 编号与"执行体 A 是无人值守跑的"这个事实（9.9.3 的文案已经写了前者，未写后者），让用户自己知道他在承担什么。


---

## §10 落地路线、测试策略与验收

本节回答三个问题：**先做什么**（§10.1）、**怎么证明它没做错**（§10.2）、**凭什么说"能用了"**（§10.3），外加一节**阈值怎么定**（§10.4）。

贯穿本节的一条纪律：**本项目的失败代价不对称**。C3 + C4 决定了删除不可撤销、没有回收站兜底（见 §6.8 与 D-16），因此所有里程碑的验收判据都必须包含**反向判据**——不是"它能删/能解/能报"，而是"**在不该动手的时候它确实没动手**"。正向判据错了是功能缺失，反向判据错了是永久数据丢失。

---

### 10.1 里程碑 M0–M4

#### 10.1.1 路线全景

```
  M0             M1              M2              M3              M4
环境真相        只读巡检        受控解压        全自动          前台
──────────    ──────────    ──────────    ──────────    ──────────
Preflight.ps1  执行体 A       执行体 B       删除 gate      执行体 C
+ §11 P0 四条  不写不删       delete=false   + Defender     网盘 + MTool
                              3-5 个真包     + 完整状态机
    │              │              │              │              │
    ▼              ▼              ▼              ▼              ▼
 一份没有       对账报告        源包一个       该删的删了      端到端跑到
 "unknown"      每条目都有      不少地躺在     不该删的        终态 (※)
 的 JSON        具名状态        games\ 里      一个没少
    │              │              │              │              │
 破坏面: 0      破坏面: 0      破坏面: 写      破坏面: 删      破坏面: 前台
                (I5 保证)      (可撤销)       (不可撤销)      (占鼠标键盘)

 ※ M4 的终态 = COMPLETE 或 TRANSLATION_NOT_APPLICABLE
```

**破坏面随里程碑单调递增，这就是排序原则本身。** 每个里程碑只在前一个里程碑的反向判据全绿之后才允许开工。

#### 10.1.2 为什么是 A → B → C

这不是"从易到难"，是**从零破坏面到不可撤销破坏面**，同时也是**从确定性最高到未知最多**：

| 维度 | A（巡检） | B（作业） | C（前台） |
|---|---|---|---|
| 破坏面 | **零**。I5 明写永不写 `games\<dirname>\` 以外、永不删除 | 写 + **删**（不可撤销） | 占用主力机前台（E1 的硬成本） |
| 独立收益 | **即使 B/C 永不落地也成立**：一张"哪些下完了 / 哪些缺卷 / 哪些缺密码 / 哪些等规则"的对账报告，直接消掉当前人工流程里最耗神的一段 | 依赖 A 的状态与卷集分组作为输入 | 依赖 A+B 已经能把下载物变成游戏 |
| 未知量 | 低。判据全部是本地文件系统事实 + `7z l -slt` 只读输出 | 中。密码字节语义、RAR 真实解码、Defender stdout 三处仍是 `【待测】`（§11 #5/#6/#9/#11） | **高**。Computer Use 在 Windows 独占前台是 `【官方】`，但"浏览器扩展驱动已登录浏览器是否也占前台"是 `【待测】`（§11 #4）——**这条没有结论，C 的会话模型根本画不出来** |
| 契约依赖 | 无上游依赖 | **D-16**：删除必须由 A/B **已落盘**的 `verified:true` + `consumed_by` gate 住，不现场重判。A 的状态不稳，B 就绝不能打开删除 | 依赖 D-18：`Unblock-File` 是 C 的**前置条件**而非收尾，忘了就撞 SmartScreen 前台模态框 |

三条决定性理由，按权重：

1. **D-16 在结构上强制了 A 先于 B。** 删除由已落盘状态 gate，那么状态必须先正确；状态由 A 产出。顺序不是偏好，是数据依赖。
2. **A 的收益不依赖后续里程碑兑现。** 这符合"反感为了流程而流程"——M1 交付即可用，不是一个只有配上 M2 才有意义的半成品。
3. **C 的形态取决于一个尚未测量的量。** 把 C 放最后，等于把最大的未知留到信息最充分的时刻；而不是现在拍一个可能全错的会话模型。

> 上游 Codex 会话自己给出的顺序也是"先实现阶段一，先拿三五个已有压缩包做安全测试"（CONTEXT §四末）。本 SPEC 的三执行体切分（D-01）把"阶段一"进一步拆成 M1/M2/M3 三步，理由是破坏面在 A 与 B 之间有一个数量级的跳变，不该在同一个里程碑里跨过去。

#### 10.1.3 M0 · 环境真相

**没有 M0，后面每一节都建在沙上。** E3 只说了"装了 7-Zip **或** WinRAR/Bandizip"，没指明是哪个；Windows 版本、PowerShell 版本、系统 ACP、盘符、`LongPathsEnabled`、ExecutionPolicy 作用域全部未知（CONTEXT §五）。在这些量测出来之前，写下的任何命令行都是猜测。

| 项 | 内容 |
|---|---|
| **范围** | ① 实现 `Preflight.ps1`（§9 规定的全部探测项），一次运行输出一份 JSON 快照；② 打掉 §11 的 P0 四条（#1 环境快照 / #2 沙盒可写性 / #3 `prefix_rule` 拆分语义 / #4 浏览器扩展前台占用）；③ 把 macOS 上已造好的 fixture 库拷到 `D:\GameFlow\tests\fixtures\` 并跑一遍"能不能在 Windows 上复现"（§10.2.4）；④ 建 `D:\GameFlow` 骨架与 Git 仓库、`D:\GameTst\` 测试库根 |
| **完成判据** | 见 §10.3.1（可验证，逐条） |
| **依赖** | 无。这是唯一可以立刻开工的里程碑 |
| **风险** | ① 探测发现只有 WinRAR/Bandizip 的 GUI、没有 `7z.exe` 命令行 → 需要补齐路径（§9 必须给出），这会推迟 M1 但不改设计；② §11 #3 若判定 `prefix_rule` 对 PowerShell 语义空白（`【待测】`，官方 rules 文档全文零提及 powershell/pwsh/cmd.exe），则"7z 免审批"写不出来，每次解压都要人点一次——**这不阻塞 M1**（A 由 Windows 任务计划触发，不经 Codex 审批面，见 D-10），但会显著劣化 B 的体感，必须在 M0 就知道；③ §11 #2 若沙盒无法写 `D:\GameLib`，Codex 侧的所有 skill 都要重新设计权限模型 |

#### 10.1.4 M1 · 只读巡检（执行体 A）

| 项 | 内容 |
|---|---|
| **范围** | `Invoke-Sweep.ps1` 的**只读全集**：`_inbox` 扫描与按批次清单认领归位（D-02）、下载完成判定 G1∧G2∧G3∧G4∧G5∧G6（§5.3）、分卷分组与连续性/等大校验、`7z l -slt` 只读预检（**不带密码**，G5 降级形态，§5.3 与仲裁争议 13）、魔数判定与容器白名单初筛、状态落盘（`events.jsonl` 追加 + `state.json` 重建）、`Get-BatchReport.ps1` 对账报告、`New-Batch.ps1` 建批次。**不解压、不校验内容、不碰密码、不删除任何东西。** |
| **完成判据** | 见 §10.3.2。核心是**反向判据**：跑 A 前后对测试库做全量文件快照 diff，除 `.gameflow\` 外必须为空 |
| **依赖** | M0 全绿（尤其 #1：`7z.exe` 路径与版本、系统 ACP、卷/文件系统）。另需 §11 #7（`traversal.zip` 在 Windows 的落点）、#10（真实 SFX 的 `l -slt` 输出）、#17（三家网盘客户端的临时物后缀）三条有结论，否则 G1 的模式表和路径穿越预检只能写成空壳 |
| **风险** | ① §11 #17 三家网盘客户端的临时文件后缀**查不到任何权威资料**，只能首次使用时人工观察一次并登记（这正是 D-14 的人工环节之③）——在登记完成前，G1 恒为"未知"，A 只能保守地不推进；② 认领归位的匹配规则（`_inbox` 里的文件属于批次里哪个条目）是 A 的主要复杂度来源，日文长名 + 分卷 + 站点自带前缀会让匹配变脏，一旦匹配错就是把 X 的包归到 Y 的目录——**必须设计成匹配不唯一就停下来问，而不是取最像的那个**（品味：不猜）；③ 认领归位是 A 唯一的"移动文件"动作，与 I5"永不写 `games\<dirname>\` 以外"存在解释空间——需 §5 明确"从 `_inbox` 移入 `games\<dirname>\` 属于写目标目录，源侧的移出不算写 `_inbox`"，否则 I5 与 D-02 在字面上会打架 |

#### 10.1.5 M2 · 受控解压（执行体 B，删除关闭）

| 项 | 内容 |
|---|---|
| **范围** | `Invoke-Work.ps1` 的解压与校验：密码优先级链 P0→P6（§6.3）与等价展开 ≤4（D-20）、`7z t` 逐包单独调用、`7z x -t*` 到 `_stg\<8hex>\`、递归解包与深度计数（D-12）、逐条目 `exists`+`size` 对账、ZipCrypto 的强制全量 CRC（D-15 的唯一强制场景）、staging→落地移动、`manifest.json` 写入（含每个源包的 SHA-256）。**配置里 `delete_archives = false`，删除代码路径可以写但必须被这一个开关短路，且开关状态要出现在每一份报告的抬头。** |
| **完成判据** | 见 §10.3.3。核心反向判据：跑完之后 `games\<dirname>\` 下的源包**一个字节都不少** |
| **依赖** | M1 全绿（状态与卷集分组是 B 的输入）。另需 §11 #5（加密包 stdin 重定向到 `NUL` 是否真的不挂起）、#8（Shift-JIS ZIP 落地名形态）、#9（PowerShell 参数数组下的特殊字符密码 + 7-Zip 对 `-p` 用什么字节）、#11（真实 RAR5/加密 RAR/分卷 RAR 解码）四条有结论 |
| **风险** | ① §11 #5 若在 Windows 上 7z 走 `CONIN$` 而不是 stdin，`<NUL` 挡不住挂起——**后台挂死一个无输出的进程是本项目最难发现的故障模式**，所以脚本层的超时是无条件必需的（§8.8），不能等实测结论；② §11 #11：六份调研对 RAR 的**真实解码零验证**（造不出合法 RAR 样本），而资源站大量用 RAR，且 RAR4 的密码错与数据损坏在工具层不可区分（`EXTRACT_FAILED_AMBIGUOUS` 这个状态就是为它存在的）——M2 期间必须用 WinRAR 现造样本补上；③ ZipCrypto 的 1/256 假通过会解出垃圾文件（`【实测】`，见 §6.3），所以密码尝试必须在 `_stg` 里做、失败后清空——测试要显式覆盖"错密码解出了几个垃圾文件"这一情形，确认它没有被当成成功 |

#### 10.1.6 M3 · 打开删除 + Defender + 完整状态机

| 项 | 内容 |
|---|---|
| **范围** | ① `delete_archives = true`，D1–D12 十二条与条件全部实装并由已落盘状态 gate（D-16）；② Defender 预检与三级分流 A/B/C（§6.9、D-19），含 `-DisableRemediation` 的 stdout 解析器；③ 状态机补全：全部停机态的进入、恢复、幂等与崩溃恢复（I2）；④ `FileShare.None` 互斥（D-09）与 `LOCKED_BY_ANOTHER_RUN` 运行期处置；⑤ MOTW 传播 + 验收后 `Unblock-File`（D-18）；⑥ Windows 任务计划部署 A（D-10）；⑦ Codex 侧五个 skill（D-11） |
| **完成判据** | 见 §10.3.4。核心反向判据：**逐条造反例**，D1–D12 每条各一个用例，破坏其中任一条 → 源包必须还在 |
| **依赖** | M2 全绿。另需 §11 #6（`MpCmdRun -DisableRemediation` 的 stdout 格式——这是该模式下**唯一**的检出信号，事件日志和 `Get-MpThreatDetection` 都看不到，`【官方】`）、#12（Defender 实时保护是否会在解压中途隔离/锁住文件）、#13（受限权限计划任务下 Defender 预检的失败形态）、#16（`-snz` 默认值）四条有结论 |
| **风险** | ① §11 #6 不测就写不出解析器，而"扫描失败"按 C 级处理（软停）会让每一个条目都停下来，流水线等于停摆；② §11 #12 若实时保护会在解压中途隔离文件，会与"验证通过才删源包"打架并造成**假失败**（好在假失败的方向是保守的：源包保留）；③ 删除的第一次真跑必须用**可重新生成的合成包**，绝不能用用户手上的真实包——这是 §10.2.3 的硬护栏；④ D-18 的代价：忘了 `Unblock-File` 会让 M4 的 Computer Use 撞上 SmartScreen 前台模态框卡死，所以它必须进 M3 的验收而不是留给 M4 |

#### 10.1.7 M4 · 前台（执行体 C）

| 项 | 内容 |
|---|---|
| **范围** | 会话模型（显式触发 / 短会话 / 可中断续跑，E1）、三类网盘适配（百度 / 夸克·阿里 / 123·蓝奏·直链，E2）与 `netdisk-profiles.json`、资源页解析与 `expected_parts` 写回批次清单、密码 P5 抓取与人工确认一次、MTool 加载翻译与 `mtool-profiles\<item-id>.json`、`TRANSLATION_NOT_APPLICABLE` 分流、最终验收 |
| **完成判据** | 见 §10.3.5 |
| **依赖** | M3 全绿。**强依赖 §11 #4**（浏览器扩展驱动已登录浏览器是否占前台）——这条决定 C 是"必须独占主力机的短会话"还是"可以与用户共用"。另需 #14（内置浏览器下载目录只有全局设置）、#18（MTool 的签名状态 / 是否需管理员 / 注入产物实际文件名）、#21（桌面 App 计划任务最小间隔、`windows.sandbox_private_desktop` 与"活动桌面"要求是否冲突、多显示器/RDP 行为——官方**零表述**） |
| **风险** | ① Computer Use 在 Windows 上独占前台、"锁屏继续"是 macOS 专属（`【官方】` https://learn.chatgpt.com/zh-Hans/docs/computer-use ，转引 §8）——这是 E1 的直接冲突点，只能靠"短会话 + 显式触发"缓解，不能消除；② MTool 完成判据**不要依赖产物文件**（`.bin` 落盘位置未知，§7.6），也不要硬编码 `与工具一同启动.bat` 这个中文文件名（几乎肯定随界面语言变化，`【社区】`）——主判据用 `version.dll` 存在 + M3 结束时写下的 manifest 文件清单快照的**差集**；③ 网盘页面规则会失效（网盘几个月改一次版），必须设计成"页面变了就停 `WAITING_FOR_MTOOL_RULE` / 具名停机态"，而不是硬撑 |

---

### 10.2 测试策略

#### 10.2.1 三层分工

```
L1 纯函数单测   目录名规范化与 ACP 往返 / 卷集命名分组 / 魔数偏移判定 /
                ZIP bit11 解析 / 密码等价展开去重 / ratio 计算 /
                D1–D12 的求值器（喂结构化输入，不碰真实文件）
                → 毫秒级，无 IO、无 7z，每次改动都跑

L2 fixture 回归  对 tests\fixtures\ 里的**冻结样本**跑真实 7z.exe，
                比对 golden JSON（exit code / Files: N / 条目数 / Type /
                Encrypted / Offset / 本项目的判定结论）
                → 秒~分钟级，每次提交跑

L3 真机小样本    用户手上的 3-5 个真实包 + 独立测试库根，跑完整执行体
                → 分钟~小时级，每个里程碑出口跑一次
```

**golden 里只存判定量，不存 7-Zip 的输出原文。** 版本升级会改措辞（`【实测】` 依据：官方 History.txt 里输出格式条目多次变动），把原文当 golden 会造成大批假失败。存的是：退出码、`Files: N`、`l -slt` 条目数、`Type`、`Encrypted`、`Offset>0` 与否、`Warnings:` 有无、以及本项目对这些的**判定结论**。

#### 10.2.2 真实小样本回归（L3）

用用户手上已有的 3-5 个包。**覆盖矩阵必须显式列出来，没覆盖到的项要在报告里标明"这一项只有合成证据"**：

| 特性 | 为什么必须有真实样本 | 验的是哪条判据 |
|---|---|---|
| **带密码** | 合成样本的密码是我们自己设的，字节形态是"干净"的；资源站上的密码可能是"正常中文的 UTF-8 字节被按 GBK 显示"，带尾随空格、带全角（`【实测】`，§6.3） | P0→P6 优先级链、等价展开 ≤4（D-20）、`PASSWORD_ENCODING_SUSPECT` 分流 |
| **多重嵌套** | 真实层数与真实层间形态（外壳 zip → 分卷 rar → 7z → 同名文件夹）合成不出来 | `max_depth=5` 与"容器层数"计数口径（D-12）、容器白名单 vs 扩展名排除表 |
| **分卷** | 分卷是 C7 的唯一硬例外（D-04），且真实资源站的命名模式（`.NNN` / `.partN.rar` / `.rar+.rNN` / `.zNN+.zip`）只有真样本能覆盖全 | G6 卷集连续 + 除末卷外等大、D1、D11、`WAITING_FOR_VOLUMES` |
| **伪装扩展名** | 验证"主流程根本不需要改名"这条**简化依据**在用户的真实包上也成立 | D-03（不传具体 `-t`）、按内容嗅探 |
| **日文文件名** | 目录名的 ACP 可表示性往返（D-05）只有在**用户那台机器的真实 ACP** 下才有意义；ACP 是多少目前未知 | D-05 的两支（直接用规范化标题 / 降级 ASCII slug）、`FILENAME_ENCODING_RISK` |

一个包可以同时覆盖多项。**最小要求是 3 个包覆盖全部 5 项**；覆盖不到的用 §10.2.4 的合成 fixture 补，并在验收报告里明确标注证据等级。

#### 10.2.3 测试库隔离（硬护栏，不是纪律）

```
生产    library_root = D:\GameLib\     （§2/§3 写死，11 字符）
测试    library_root = D:\GameTst\     （本节命名，根名 10 字符 —— 与生产**等长**）
```

选 `D:\GameTst\` 而不是更短的名字，是因为**测试的 MAX_PATH 预算必须与生产完全等价**：库根短一个字符，测试就比生产宽松一个字符，路径超限的 bug 会在生产上首次暴露。`D:\GameTst\games\` = 17 字符，与 `D:\GameLib\games\` 逐字符相等。

> **已定**：`D:\GameTst\` 为测试专用库根，由 `-Profile test` 解析（§8.2.3）。§2.4 只持有「与生产库根等长」这条约束，具体名字由本节持有——库根是配置项，写进布局树会与此矛盾。生产路径不受影响。

三条护栏，写进 `scripts\lib\` 而不是靠人记住：

1. **入口拒绝**：所有测试入口一律传 **`-Profile test`**（§8.2.3；**不接收 `-LibraryRoot` 这类自由路径参数**——最外层永远是 5.1 的解析规则，带空格或中文的路径在那里就被拆坏了）。`test` profile 由脚本从配置解析出 `D:\GameTst`；若解析结果等于生产库根，**立即退出，不做任何 IO**（不是"警告后继续"）。
2. **同卷约束**：`D:\GameTst\_stg\` 必须与 `D:\GameTst\games\` 同卷——`File.Replace` 跨卷抛异常（`【官方】` `ReplaceFileW` 文档，转引 §8.8）。因此测试库根与生产库根同在 `D:` 盘，这也顺带让测试跑在真实的文件系统语义上。
3. **删除用例只用可再生成的合成包**：`delete_archives=true` 的用例**永不使用用户手上的真实包**。用例自己在 setup 阶段现造样本、teardown 阶段清理。理由直白——C3+C4 下删除不可撤销，一个测试 bug 就能吃掉用户的原始资源。

#### 10.2.4 fixture：macOS 已造好的可以直接搬，但要分清哪些能搬

调研阶段已在 macOS 上造好一批样本，**归档文件的字节与平台无关**（7z/ZIP/tar 容器就是字节流），所以样本本身可以整目录拷到 `D:\GameFlow\tests\fixtures\` 复用；**但观测到的行为不可复用**——落地文件名、路径穿越落点、符号链接处理都是 OS 相关的。

来源目录（整目录拷贝）：
- `scratchpad/research/lab7z/`（7-Zip 专项：`arc/ vol/ nest/ sjis/ evil/ pw/ bomb/ hazard/ magic/ mis/ zipspan/ rar/ sfx/ spe/ unsup/`）
- `scratchpad/research/lab/`（归档现实：`liar.zip trunc_*.zip cjk_*.zip jp_cp932.zip enc*.zip sp/ tuzhong.jpg game.{jpg,mp4,dat,bin,txt} sig_*`）
- `scratchpad/research/fixtures/`（引擎判定：`mv/ mz/ vx/ xp/ ace/ rm2k3/ wolf/ wolf2/ krkr/ renpy/ tyrano/ srpg/ unity/ evb/ multi/ nested/ done/`）

**可直接复用（字节层结论，可外推到 Windows）：**

| 样本 | 覆盖的危险场景 | 期望行为 |
|---|---|---|
| `lab7z/sjis/sjis-legacy.zip` + `utf8-modern.zip` | **文件名编码**：前者中央目录 bit11=0 且文件名含 ≥0x80 字节，后者 bit11=1 | 前者 → `FILENAME_ENCODING_RISK` 停人工；后者 → 直接放行。判定必须由**纯字节解析中央目录**得出，不依赖任何 7-Zip 开关（§5.8） |
| `lab7z/evil/traversal.zip` | **路径穿越**（含 `C:\...` 绝对路径与 `back\slash\...` 两种形态） | 预检必须自己扫 `l -slt` 的 `Path` 并检出。**7-Zip 确实拦截但完全静默**（exit 0、零 warning、零 stderr，`【实测·macOS】`），流水线拿不到任何信号 → 不自己判就等于没有这道护栏。**永远不要用 `-snld20`**（`【实测·macOS】`：真的创建了逃逸符号链接且 exit 0） |
| **伪装分卷组**：把 `lab7z/vol/parts.7z.001/.002/.003` 拷成 ① `game_a.mp4 / game_b.mp4 / game_c.mp4` ② `game.mp4.001/.002/.003` | **D-04 的双向证据** | ① `7z t` exit 2，**完全打不开**，只改首卷也不行；② exit 0、`Type = Split`、`Volumes = 3`、`Files: 4`。即"只需要补 `.NNN`，`.NNN` 之前的扩展名无所谓"（`【实测·macOS 7zz 25.01】`；属 7-Zip 卷集命名逻辑的跨平台公共层，**但仍需在 Windows 用 `7z.exe` 复跑一次确认**，见 §11 #1） |
| `lab7z/arc/pw-hdr.7z` | **头加密（`-mhe`）分支** | `l` 直接 exit 255，`Size`/`Path`/条目数一个都拿不到 → 体积预算、条目预算、路径穿越预检、压缩比护栏**全部失效**（§6.2）。A 在 G5 遇到它必须转 `WAITING_FOR_PASSWORD` 而不是 `CORRUPT` |
| `lab7z/arc/pw-data.7z`、`pw.zip`、`lab/enc.zip`、`enc_fw.zip`、`enc_sp.zip` | 数据加密（`l` 仍可列条目）、ZipCrypto vs AES | `Encrypted = +`；ZipCrypto（`compress_type != 99`）触发 D7 的强制全量 CRC，AES 不触发（D-15） |
| `lab/liar.zip` | **声明大小撒谎** | 头部 `Size` 与实际解出体积不符 → 证明"运行期 staging 体积监控 + 绝对上限"必须独立于头部预算生效，头部数字只能用于**事前**粗筛 |
| `lab/trunc_50.zip / trunc_90.zip / trunc_99.zip`、`lab7z/arc/truncated.7z` | **截断包** | stderr 是 `Unexpected end of archive`——**与"缺中间卷""缺末卷"一字不差**（`【实测·macOS】`）→ 三者只能靠自己做的卷集连续性 + 等大校验区分，不能靠 7-Zip 的错误文本 |
| `lab7z/vol/parts.7z.001+.003`（删掉 002）、`lab/sp/split.z01..z07+split.zip`（缺一卷） | **缺卷** | `WAITING_FOR_VOLUMES`，源包一个不动 |
| `lab7z/nest/L1.7z → L2_disguised.dat → L3.zip → payload.txt` | **多重嵌套 + 中间层伪装扩展名** | 每层都要**重新做内容嗅探**决定是否 depth+1，不看扩展名；`-t*` 强制每次只开一层让计数与现实一致（D-12） |
| `lab7z/bomb/bomb.7z` | **压缩比** | 实测约 1029x（`【实测】`，来自 archive-reality 的样本）。因体积远 < 5 GiB，按 D-13 只应 `ratio_warn` 记录、**不阻断**——这个用例的意义正是验证"ratio 绝不单独阻断" |
| `lab7z/arc/warn.7z`、`tail-junk.7z`、`dironly.7z`、`lab/many.zip` | **exit 0 的四个陷阱** | `tail-junk.7z` → exit 0 **伴随 `Warnings:`**；`dironly.7z` → exit 0 但零文件；再加一个"多归档参数误用"用例（`7z t a.7z b.7z`，第二个参数是过滤器不是第二个包，exit 0 而一个包都没测，`【实测】`）。判据必须是 `exit == 0` 严格相等 **且** `Files: N > 0` **且** N == `l -slt` 条目数 **且** 无非白名单 Warnings（§6.2、§6.8） |
| `lab7z/mis/a.{jpg,pdf,png,txt,exe,bin,dat}` + `a_noext`（同一份 7z 换 8 种扩展名）、`lab/tuzhong.jpg`（图种）、`lab/prepended.zip` | **伪装扩展名 / 前置垃圾字节** | 全部无需改名直接 `l/t/x`。前置垃圾 ≤8 MB 自动（默认扫描窗口 `*:s8m`，`【官方】` `MANUAL/cmdline/switches/type.htm`）；>8 MB 用 `-t*:s{N}m` |
| `lab7z/hazard/doc.docx`、`tiny.exe`、`zeros.bin` | **"7z 能打开它" ≠ "它是容器"** | `.docx` 是 `Type = zip`，MZ 文件是 PE"归档"。递归必须走容器白名单 + 扩展名排除表（`container-types.json`），**绝不能用"7z 能不能打开"当判据**（§6.2） |
| `lab7z/magic/*`、`lab/sig_*` | **魔数表**（含 offset 257 tar、offset 32769 ISO9660） | 逐条对 §5.4.4 的魔数表；同时验"**分卷必须先按命名分组再做类型判定**"——实测中间卷会被 `file` 误判成 DOS executable（`【实测·macOS】`） |
| `lab7z/pw/p1..p18.7z` + `dash.7z` | **特殊字符密码**（18 个，含中日文、全角、反斜杠、引号、以 `-` 开头） | 7-Zip 对密码字节透明，转义 100% 是调用方 shell 的问题（`【实测·macOS zsh】`）。**在 Windows 上必须用 `powershell.exe`(5.1) 与 `pwsh.exe` 各跑一遍**（§11 #9）——这批样本在 zsh 下验过，在 PowerShell 下是 `【待测】` |
| `lab7z/arc/mystery.gz`、`bundle.tar.gz`、`bundle.tgz` | **`.tar.gz` 只解一层** | 外层名不含 `.tar` 时内层文件不带扩展名（`mystery.gz → mystery`，实为 tar，`【实测】`）→ 递归检测不能靠扩展名 |
| `fixtures/*`（16 个引擎目录） | **引擎判定有序表**（D-21） | 逐个跑 `engine-rules.json`：`vx/RGSS202J.dll` 与 `xp/`、`ace/` 验"按版本段前缀匹配 `RGSS1*/2*/3*`"而不是照抄 SteamDB 的 5 个 DLL 名；`multi/`（`Game.exe` + `開始.exe` + `Config.exe` + `unins000.exe` + `vcredist_x86.exe`）验"多 exe → 候选集 + `ambiguous` + 人工确认一次"，**不得自动选 `Game.exe`**；`evb/`（Enigma Virtual Box 单 exe）验 `UNKNOWN ≠ 不支持`；`unity/` 验 `TRANSLATION_NOT_APPLICABLE`；`nested/`（`ゲーム.rar` + `Game_v1.02/`）验"游戏根目录 = 含引擎标记文件的那一层 ≠ `games\<dirname>\`"；`done/`（含 `version.dll` + `ManualTransFile.json` + `与工具一同启动.bat`）验幂等（`version.dll` 已存在 → 不重复注入）**且**验"不得把中文文件名写进判据" |

**必须在 Windows 重造的（macOS 造不出或造出来没意义）：**

| 缺口 | 为什么 | 怎么补 |
|---|---|---|
| `hazard/real-macho.bin` 是 Mach-O | Windows 上真正的问题是**每个 `.exe`/`.dll` 都是 PE"归档"**，游戏目录里少说几十个 | 拿一个真实的 Windows `.exe` 和 `.dll` 进 fixture，验扩展名排除表 |
| `evil/symlinks.7z` / `symlinks.tar` 是 POSIX symlink | Windows 上创建 symlink 需要特权或开发者模式，且真正要防的是 **junction / reparse point** | 在 Windows 上另造 junction 与 reparse point 样本，验 D9 |
| **真实 RAR** | 六份调研对 RAR 的真实解码**零验证**（`lab7z/rar/` 里只有魔数桩，不是合法 RAR），而资源站大量用 RAR | 用 WinRAR 造：RAR4/RAR5 × 加密/非加密 × 单卷/分卷 = 8 个样本，跑 `7z t`（§11 #11） |
| **真实 SFX `.exe`** | 现有 `sfx/selfext.exe` 是 macOS 7zz 造的；Windows 上 `.exe` 是**合法 PE**，PE handler 可能抢先解资源节而不是尾随负载 | 拿一个真 SFX 跑 `7z l -slt`，对比 `-t*` / `-t#`（§11 #10） |
| **MOTW 样本** | `Zone.Identifier` 是 NTFS 备用数据流，macOS 上不存在 | 从浏览器下一个包，`Get-Content -Stream Zone.Identifier` 确认有标记，再跑加/不加 `-snz` 的对照（§11 #16） |
| **EICAR 样本** | Defender 分流与实时保护干扰只能在 Windows 上验 | 造一个含 EICAR 串的包（§11 #6/#12） |

#### 10.2.5 Pester 版本陷阱

- Windows 10/11 预装的 `Pester` 是 **3.4.0**，语法与 Pester 5 **不兼容**（`【社区】`，转引 §8.8）。在 `powershell.exe`(5.1) 下裸 `Import-Module Pester` 会自动加载这个 3.4.0。而 Codex 的 Windows 原生 agent 跑的正是 `powershell.exe -NoProfile`（`【官方】`，本地留存的 `en_config-file_config-reference.md` 逐字复核 + `strings` 实测）——也就是说**默认路径上撞到 3.4.0 的概率最高**。
- 3.4.0 装在 `C:\Program Files\WindowsPowerShell\Modules\Pester` 且受系统文件保护，`Update-Module` 会失败；升级需 `Install-Module Pester -Force -SkipPublisherCheck -Scope CurrentUser`（`【社区】`，`【待测】`）。

**本节的处置：L1/L2 用零依赖 harness，不引入 Pester。** 理由：本项目的断言形态极窄——退出码、JSON 字段值、文件存在性/大小、状态名——一个 `Invoke-Tests.ps1` 加几个断言函数就够，而 Pester 带来的收益不抵它在 5.1 上的版本地雷 + 一个需要联网安装的依赖（符合"反感为了流程而流程"）。若后续确实要用 Pester，契约是：每个测试文件顶部 `#Requires -Modules @{ ModuleName='Pester'; ModuleVersion='5.5.0' }`，入口显式 `Import-Module Pester -MinimumVersion 5.0 -ErrorAction Stop`，**绝不裸 `Import-Module Pester`**。这条是可逆的工程选择，不是锁定决策。

#### 10.2.6 测试自身的编码与落盘约定

测试代码与被测代码适用**同一套**编码约定（§8.8）：显式 `[System.IO.File]::WriteAllText(path, text, UTF8Encoding($false))`；所有 `ConvertTo-Json` 一律 `-Depth 10`；golden 文件也按这套写。理由不是洁癖——如果 golden 是 5.1 的 `-Encoding utf8`（带 BOM）或裸 `>`（UTF-16LE）写出来的，含日文名的对拍会稳定假失败，而排查方向会全错。

---

### 10.3 验收清单

每条给出**命令形状**与**判定现象**。命令形状按 §8 的调用契约写：脚本入口只接收 `-BatchId <slug>` 这类无空格无歧义 token，路径由脚本从配置解析（D-01 相关，§8）。

#### 10.3.1 M0 验收

```
pwsh -NoProfile -File D:\GameFlow\scripts\Preflight.ps1 -Json
```

| # | 判定 | 现象 |
|---|---|---|
| M0-1 | 输出是**一份 JSON**，§9 规定的字段**一个都不是 `unknown`** | 包含至少：`sevenzip.path/version`、`archiver.alternatives`（WinRAR/Bandizip 是否只有 GUI）、`psversion`、`pwsh.present`、`library_volume` / `stg_volume` / `filesystem`、`acp`、`beta_utf8`、`longpaths_enabled`、`execution_policy`（**全部作用域**）、`library_root_in_onedrive_or_programfiles`（必须为 false，§7.6） |
| M0-2 | `sevenzip.version >= 25.01` | 低于此拒绝进入 M1（D-17 硬下限）。25.01 ≤ v < 26.02 时报告顶部出现长期提示，但不拒绝运行 |
| M0-3 | `library_volume == stg_volume` | 否则 `File.Replace` 跨卷抛异常（D-06 的前置条件） |
| M0-4 | `execution_policy` 的 `MachinePolicy` / `UserPolicy` 已判明 | 若非 `Undefined`，`-ExecutionPolicy Bypass` 无效，§9 必须给出处置路径 |
| M0-5 | §11 #1–#4 各有一条实测记录落在 `D:\GameLib\_reports\` 下 | 记录必须写"测了什么命令、看到什么、结论"，不是"应该可以" |
| M0-6 | fixture 库已在 Windows 上跑通一遍 L2 冒烟 | `lab7z/arc/plain.7z`、`mis/a.jpg`、`vol/parts.7z.001` 三个样本的 `7z l -slt` 输出与 macOS 结论一致（Type / 条目数 / Volumes）。**不一致就说明跨平台外推假设有问题，必须先查清再进 M1** |

#### 10.3.2 M1 验收（执行体 A）

```
pwsh -NoProfile -File D:\GameFlow\scripts\New-Batch.ps1      -BatchId 2026-01-01-t-m1
pwsh -NoProfile -File D:\GameFlow\scripts\Invoke-Sweep.ps1   -BatchId 2026-01-01-t-m1
pwsh -NoProfile -File D:\GameFlow\scripts\Get-BatchReport.ps1 -BatchId 2026-01-01-t-m1
```

| # | 判定 | 现象 |
|---|---|---|
| **M1-0** | **反向判据（不可协商）**：A 一个字节都没改、没删 | 跑 A 前后各做一次全量快照并比对：`Get-ChildItem -LiteralPath D:\GameTst -Recurse -Force \| Select-Object FullName,Length,LastWriteTimeUtc \| Sort-Object FullName \| ConvertTo-Json -Depth 10`。diff **排除 `\.gameflow\` 与 `_reports\`** 后必须为空。这一条不过，M1 直接不通过，其余判据不必看（I5） |
| M1-1 | 每个条目都有**具名状态**，报告里没有"未知/其它" | 状态名必须来自 §4 的封闭集合 |
| M1-2 | 完整目录 → `PREFLIGHT_OK` | 六重与条件 G1∧…∧G6 全过 |
| M1-3 | 人为删掉中间卷 → `WAITING_FOR_VOLUMES` | 用 `lab7z/vol/parts.7z.001+.003`。**同时用 `trunc_90.zip` 验反面**：截断单文件的 stderr 一字不差，但它不该落 `WAITING_FOR_VOLUMES` |
| M1-4 | 头加密包 → `WAITING_FOR_PASSWORD`（不是 `CORRUPT`） | 用 `lab7z/arc/pw-hdr.7z`。G5 在无人值守的 A 里不带密码，`l` exit 255 的正确解读是"需要密码"（§6.2、§5.3） |
| M1-5 | 伪装扩展名包 → 正常识别，**不改名** | 用 `mis/a.jpg`。报告里 `Type` 正确，且 `games\` 下的文件名原封不动 |
| M1-6 | 伪装分卷组 → 具名停机态，**不自动改名** | 用 `game_a.mp4 / game_b.mp4 / game_c.mp4`。A 只能报告"疑似分卷组"，规则必须由人显式声明（D-04 的例外分支 E1 在 B 里执行，A 不动手） |
| M1-7 | **幂等**：连跑两次 A | 第二次的 `events.jsonl` 不出现重复的状态迁移事件；`state.json` 删掉后重跑 A 能从 `events.jsonl` 完全重建（I2） |
| M1-8 | 认领归位正确 | `_inbox` 里的文件被移入正确的 `games\<dirname>\`；**匹配不唯一的条目必须停下来**，不得取"最像的那个" |
| M1-9 | 报告好读 | 一屏内能看清：每游戏一行状态、异常项单独成块、每个停机态附"该做什么"。这是品味里"呈现层不吝啬"的兑现点，验收方式是**用户看一眼说得出下一步做什么** |

#### 10.3.3 M2 验收（执行体 B，删除关闭）

```
pwsh -NoProfile -File D:\GameFlow\scripts\Invoke-Work.ps1 -BatchId 2026-01-01-t-m2
```

| # | 判定 | 现象 |
|---|---|---|
| **M2-0** | **反向判据**：源包一个不少 | 跑完后 `games\<dirname>\` 下每个源包的 **路径 + 大小 + SHA-256** 与跑前完全一致 |
| M2-1 | 配置守卫可见 | 报告抬头出现 `delete_archives = false`。若这行不在，视为验收失败（防止"以为关着其实开着"） |
| M2-2 | 3-5 个真实包全部到 `EXTRACTED` | 覆盖矩阵（§10.2.2）五项都有真实证据，或明确标注"仅合成证据" |
| M2-3 | `_stg\<8hex>\` 运行结束后为空 | 中转区是临时的，解完即清空（D-06 与 C3 不冲突的前提） |
| M2-4 | `manifest.json` 里每个源包都有 SHA-256 | M3 才开删除，但哈希必须在 M2 就写对（§6.8） |
| M2-5 | 逐条目对账可见 | 报告里能看到 `Files: N` 与 `l -slt` 文件条目数**相等**（D5），以及逐条目 exists+size 全过（D6） |
| M2-6 | 密码正路径 | 带密码的真实包：人录入密码后从 `WAITING_FOR_PASSWORD` 恢复并解开（这就是 D-14 的人工环节之①） |
| M2-7 | 密码反路径 | 故意给错密码：落 `WAITING_FOR_PASSWORD`（或 RAR4 的 `EXTRACT_FAILED_AMBIGUOUS`），**源包完好 + `_stg` 已清空**。ZipCrypto 样本必须验"解出了几个垃圾文件也不算成功"（1/256 假通过） |
| M2-8 | 空密码不传 `-p` | 无密码包的调用行里没有 `-p`。裸 `-p` 会切进交互模式并表现成"密码错"（`【实测】`），把"没提供密码"误分流成 `CORRUPT` |
| M2-9 | 深度计数正确 | `nest/L1.7z` 链：报告里的 depth 与实际容器层数一致（不是 7z 调用次数）；把 `max_depth` 临时调到 2 应触 `BLOCKED_RESOURCE` |
| M2-10 | 超时生效 | 人为构造一个长跑的 `7z t`（超大样本 + 临时把超时调到 5 秒）→ 进程被杀、落具名状态、**不留半个 staging** |

#### 10.3.4 M3 验收（打开删除 + Defender + 完整状态机）

| # | 判定 | 现象 |
|---|---|---|
| **M3-0** | **反向判据（本里程碑的全部重量）**：**逐条造反例，D1–D12 各一个用例** | 每个用例只破坏一条（例如手工从 staging 删掉一个文件让 D6 失败、给 `expected_parts` 填一个错数让 D11 失败、把 staging 移动目标设成只读让 D12 失败）→ **源包必须还在**，落对应停机态，`events.jsonl` 里能看到是哪一条判据否决的。12 条全绿才算过 |
| M3-1 | 正路径：合成包被删 | 用**可重新生成的合成包**（§10.2.3 护栏 3）走完 `PLANNED → … → SCANNED`，源包被删，`manifest.json` 里 SHA-256 在 |
| M3-2 | 删除由已落盘状态 gate | 把 `state.json` 里的 `verified` 手工改成 false 后重跑 → 不删除。证明 D-16"不现场重判" |
| M3-3 | Defender 三级分流 | EICAR 样本入包 → `THREAT_SUSPECTED` 或 `THREAT_CONFIRMED`（按 §6 的 A/B 级判据），源包不删。**扫描失败 → 按 C 级当 B 处理**，也要有一个用例 |
| M3-4 | Defender 排除项范围正确 | 只排 `D:\GameLib\_stg\`；`games\` 下保留实时保护（D-19）。用 `Get-MpPreference -ExclusionPath` 核对 |
| M3-5 | 互斥 | 同时起两个 B → 第二个停在 `LOCKED_BY_ANOTHER_RUN` 且 **不落盘、不进状态机**（§4.2）。用 `FileShare.None` 而非 lock+PID（D-09） |
| M3-6 | 崩溃恢复 | 解压中途 `Stop-Process -Force` → 删掉 `state.json` → 重跑：状态从 `events.jsonl` 完全重建到崩溃前**最后一个已落盘**状态；`events.jsonl` 末尾的半行被丢弃而不是让解析崩掉（§8.8） |
| M3-7 | MOTW | 解压产物带 `Zone.Identifier`；验收后 `Unblock-File` 跑过，`Get-Item -Stream Zone.Identifier` 在 `games\<dirname>\` 下零命中（D-18）。**这条必须在 M3 完成，不能留给 M4** |
| M3-8 | 计划任务 | A 由 Windows 任务计划触发，`MultipleInstances = IgnoreNew`，**非 S4U**；DPAPI vault 的创建身份与任务运行身份一致（§8.8）。验证：注销再登录后任务仍能跑通一轮 |
| M3-9 | Codex skill | 五个 skill 在 `.agents/skills` 下、`allow_implicit_invocation: false`，逐个手动触发一次能跑通（D-11） |

#### 10.3.5 M4 验收（执行体 C）

| # | 判定 | 现象 |
|---|---|---|
| M4-1 | 端到端 | 一个条目从资源页走到 `COMPLETE` 或 `TRANSLATION_NOT_APPLICABLE` |
| M4-2 | 落地路径 | 下载全部落在 `_inbox\`，由 A 认领归位到 `games\<dirname>\`（D-02）。C3 在**终态**成立：源包与本体同目录 |
| M4-3 | `expected_parts` 写回 | C 从资源页把"一共几个下载链接/几个分卷"写回批次清单——这是"A 下完了、B 还没开始下"这个失败模式**唯一可靠的自动检测点** |
| M4-4 | 可中断续跑 | Ctrl+C 掉 C 之后重跑：不重复转存、不重复注入（`version.dll` 已存在 → 判定已处理，§7.6）。这是 E1 的兑现点 |
| M4-5 | 引擎分流 | Unity/Godot/Unreal 条目落 `TRANSLATION_NOT_APPLICABLE`（终态，视同完成），**不是失败**；`evb/` 那类 `UNKNOWN` 落 `WAITING_FOR_MTOOL_RULE`，也不是失败 |
| M4-6 | 多 exe | `multi/` 那类 → 候选集 + 人工确认一次 → 结果写进 `mtool-profiles\<item-id>.json`；第二次跑不再问 |
| M4-7 | 完成判据不脆 | 判据是 `version.dll` 存在 + M3 manifest 快照的**差集**，报告里不出现硬编码的 `与工具一同启动.bat` |

---

### 10.4 阈值标定

#### 10.4.1 三条原则

1. **默认值有据可查，但那是别人的样本。** §6.12 的 `ratio_warn=200` / `ratio_block=1000 且 >5 GB` 来自实测（合法的纯文本汉化补丁 7z 就有 133x，真炸弹 1029x，中间没有干净分界，`【实测】`）。但用户的库可能完全不同：整包是 WAV/PNG 素材时 ratio 会低到接近 1，纯文本补丁会很高。**SPEC 给的是判据结构 + 一组有据可查的初值，不是"对你的库正确的值"。**
2. **标定只改配置里的数，不改判据结构。** 判据结构由 §6.8（D1–D12）与 §6.12 写死。标定不是重新设计。
3. **阈值只上调、不下调。** 下调等于让护栏更严，会在没有新证据的情况下制造假阳性。每次调整写一条标定台账（时间 / 样本 / 观测值 / 新值 / 谁改的）。

> **主控裁决（已定）**：**默认值是契约，标定后的实际值是配置。** 标定改的是配置文件里的数，不算改契约，因此不需要改 SPEC。约束有三条：① 只上调、不下调；② 每次调整写一条标定台账；③ 默认值本身要改，才算改契约，走 §12 的决策流程。

#### 10.4.2 需要标定的旋钮

| 旋钮 | 默认值 | 标定方法 | 不标定 / 标错的后果 |
|---|---|---|---|
| `ratio_warn` | 200（§6.12） | 对全部真实样本跑只读的比值测量：`expansion = sum(条目 Size) / 归档级 Physical Size`（**不是** `sum(Packed Size)`——7z 默认 solid，一个 block 只有第一个条目带 `Packed Size`，`【实测】`）。取观测最大值 M；若 M ≥ 200，上调到 `ceil(M × 1.5)` | 报告被 warn 刷屏。**只是噪声，不阻断**（D-13），代价最低 |
| `ratio_block` | 1000 **且** `declared_total > 5 GB` | 同上。只要有真实样本同时满足 ≥1000 且 >5 GB，就必须上调 | 合法大包被误阻断 → `BLOCKED_RESOURCE` 假阳性，流水线停在一个本该通过的条目上 |
| `max_total_uncompressed` / `max_single_file` | 待定（§6.12 只规定"绝对上限"存在） | 取真实样本的最大解压总量 × 3，向上取整到 10 GiB 的整数倍 | 大游戏被误阻断；或对头加密包（**头部信息全失效**，`【官方+实测】`）失去唯一的兜底 |
| `safety_factor` / `min_free_headroom` | 待定 | 在最大的真实样本上量 staging 的**峰值**占用（不是最终占用——递归解包中途会同时存在多层产物）÷ `declared_total`，得到真实系数；headroom 取"解压中途还能让系统正常工作"的余量 | 解压中途磁盘满。Windows 上是 `ERROR_DISK_FULL`，7-Zip 会显式报错并返回非 0（`【实测依据】`：Windows 没有信号也没有 `ulimit -f`，POSIX 那种"静默截断"不适用）——所以这是干净失败而非静默损坏，但仍会浪费一次完整解压 |
| **`G2` 采样次数与窗口** | ≥3 次 / 窗口可配置（§5.3） | **必须用用户自己的下载器实测**：在一次真实下载全程每秒采一次 `Length` + `LastWriteTimeUtc`，记录**最长静默间隔 L**（限速停顿、分片切换、合并阶段都会造成静默）。窗口取 `≥ 3L`，采样次数取 `≥ ceil(窗口 / 采样间隔)` | **本项目最贵的一类误判。** 半成品被当成下完的 → 送进 B → 浪费一次解压，且可能落 `CORRUPT` 掩盖真实原因。已知能击穿"大小连续 N 次不变"的机理有六种（aria2/Motrix 预分配、限速停顿 6 秒骗过 3×1 秒、Firefox 0 字节占位、IDM 无临时后缀缓慢合并、百度网盘完成后后缀残留、Chromium 先改名后扫描），所以窗口不是拍脑袋能定的 |
| **`G4` 宽限期** | 待定 | 量"最终名出现"到"`FileShare::None` 独占打开成功"的间隔（Chromium 先改名后扫描，`IAttachmentExecute::Save()` 可以直接把刚出现的文件删掉，`【调研·源码级】`）。取观测最大值 × 3；并且**操作前必须复检文件仍存在** | 同上；外加"文件刚出现就被杀软删掉，而流水线已经开始处理"这一竞态 |
| **7z 调用超时** | 待定（§8.8 要求必须有） | 在最大的真实样本上量 `7z t` 与 `7z x` 的墙钟时间 T，超时取 `max(10 × T, 30 min)`；`t` 与 `x` 分别设 | 后台任务里挂死一个无输出的进程（§11 #5 若判定 Windows 走 `CONIN$`，`<NUL` 挡不住加密包的交互提示）。**这是最难发现的故障模式**，超时是无条件必需的，不能等实测结论 |
| `max_depth` | 5（D-12） | 对真实样本数**容器层数**（不是 7z 调用次数）。只有观测到 5 层还没到底才上调 | 触上限只是停机不是破坏（`BLOCKED_RESOURCE`），代价最小。所以宁可偏小 |
| 密码尝试上限 | 候选数 × 4（D-20 的等价展开 ≤4） | 不标定，写死。**这是硬约束不是旋钮**（§6.3 硬性禁止超过"候选数×4"次尝试） | — |

#### 10.4.3 标定的诚实局限

- **3–5 个样本不构成分布。** 因此阈值不是"统计出来的"，是"保证不误伤已知样本 + 留一个明确倍数余量"。倍数（1.5× / 3× / 10×）本身是工程惯例，不是测量结果——照实标 `【社区】`。
- **标定会过期。** 用户换下载器、换资源站、换网盘客户端，G2/G4 的窗口就要重标。触发条件写死：Preflight 探测到下载器变化，或 `netdisk-profiles.json` 新增条目 → 报告里出现"标定可能已过期"提示（**提示，不阻断**）。
- **标定产物落在 `D:\GameLib\_reports\` 下**，每次标定追加一节：样本清单（含 SHA-256）、逐项观测值、结论值、调整理由。这是"人在环内"的实现方式——阈值为什么是这个数，永远查得到。

---

## §11 待 Windows 实测清单

本节汇总全部待实测项：仲裁给出的 21 条（#1–#21）+ 各章节自提并去重后收进来的 7 条（#22–#28），**按对设计的影响**排序（不是按调研方便程度）。每条给出"不测的后果"，因为这决定了它到底该在哪个里程碑清账。

**排序原则**：① 不测就没法开工的（环境与执行面）→ ② 决定架构分界线的 → ③ 决定某条判据能不能写出来的 → ④ 只影响某个分支体感的。

| # | 事项 | 怎么测 | 不测的后果 | 阻塞哪一节 |
|---|---|---|---|---|
| **1** | **目标机基础环境快照**：`7z.exe` 是否在 PATH、版本号、是否只有 WinRAR/Bandizip GUI；`$PSVersionTable`；`pwsh.exe` 是否存在；库所在卷的文件系统（NTFS/exFAT）；系统 ACP 与 Beta UTF-8 状态；`LongPathsEnabled`；`Get-ExecutionPolicy -List` 全作用域；库根是否落在 OneDrive / `Program Files` 下 | 一个 `Preflight.ps1` 一次性输出 JSON。E3 只说"装了 7-Zip **或** WinRAR/Bandizip"，**没指明是哪个** | **后面每一节都建在沙上。** 版本决定 D-17 能不能满足；ACP 决定 D-05 走哪一支、D-20 的 GBK 换算是否成立；`LongPathsEnabled` 决定 MAX_PATH 预算；ExecutionPolicy 的 MachinePolicy/UserPolicy 决定 `-ExecutionPolicy Bypass` 有没有用；OneDrive 目录会导致 MTool inject 失败（`【社区】`） | §9（前置探测本身）、§2（MAX_PATH）、§5、§6、§8 |
| **2** | **Codex 沙盒能否写 `D:\GameLib`**（project 打开 `D:\GameFlow` 时）；`sandbox_workspace_write.writable_roots` 的实际边界；`<writable_root>/.codex` 被递归置只读是否影响我们 | 配 `writable_roots` 后让 agent 往 `D:\GameLib\_reports\` 写一个文件；再试写 `games\<dirname>\.gameflow\` | Codex 侧第一步写文件就失败。要么整套 skill 权限模型重设计，要么被迫用 full access（**安全上不可接受**） | §8（Codex 操作面）、§2（双根部署） |
| **3** | **`prefix_rule` 对 PowerShell 的拆分语义**：`codex execpolicy check` 怎么看待 `powershell.exe -NoProfile -Command "…"` | `codex execpolicy check --pretty --rules ~/.codex/rules/default.rules -- powershell.exe -NoProfile -Command "7z t x.7z"`；再对比 `-File` 形式 | 官方 rules 文档**全文零提及 powershell/pwsh/cmd.exe**（`【官方】`：grep 0 命中），极可能整条调用被当成单一 invocation → `pattern=["7z","t"]` 失效 → 每次解压都要人点一次。**注意：这不阻塞 A**（A 由 Windows 任务计划触发，不经审批面，D-10），但会显著劣化 B 的体感 | §8 |
| **4** | **浏览器扩展驱动已登录浏览器时，是否占用前台指针** | 装扩展后跑一个 `@Chrome` 任务，全程观察鼠标指针与焦点是否被接管 | **这条决定阶段一/阶段二的分界线画在哪。** 官方文档两面都有暗示、没有明确表态。若扩展路径不占前台，则"用已登录浏览器转存网盘"可以留在阶段一（不占主力机，E1 的痛点大幅缓解）；若占前台，它必须整段划进 C，M4 的会话模型也随之改变 | §1（两阶段定性）、§7（C 的会话模型）、§10.1（M4 范围） |
| **5** | **加密包在 stdin 重定向自 `NUL` 时是否真的不挂起** | `7z t <加密包> <NUL`，看是否立即返回；`powershell.exe` 与 `pwsh.exe` 各一次 | macOS 上 `</dev/null` 得到 exit 255（`【实测】`），但 **Windows 的控制台密码读取可能走 `CONIN$` 而不是 stdin**，那样 `<NUL` 挡不住挂起。后台任务里挂死一个无输出的进程是**最难发现的故障模式**。（缓解已写进 §8.8：无论结论如何都必须设超时） | §6、§8 |
| **6** | **`MpCmdRun -Scan -ScanType 3 -DisableRemediation` 的 stdout 格式**：威胁名怎么打印、多威胁怎么分行、无威胁时打什么 | 用 EICAR 样本跑一次，把完整 stdout 存档 | 该模式下 **stdout 是唯一的检出信号**——事件日志和 `Get-MpThreatDetection` 都看不到这次检出（`【官方】` https://learn.microsoft.com/en-us/defender-endpoint/command-line-arguments-microsoft-defender-antivirus ）。格式不确定就写不出解析器，A/B/C 三级分流（D-19）全部落空 | §6（Defender 预检）、§10.1（M3） |
| **7** | **`traversal.zip` 里 `C:\...` 与 `back\slash\...` 的落地位置** | 拷 `tests\fixtures\lab7z\evil\traversal.zip` 过去解压到 staging，记录每个条目实际落在哪 | Windows 上 `\` 是路径分隔符、`C:` 是盘符，与 macOS **必然不同**。这是路径穿越护栏的正确性前提。7-Zip 拦截是**完全静默**的（exit 0、零 warning、零 stderr，`【实测】`）→ 不自己判就等于没有护栏 | §5（只读预检）、§6（解压护栏） |
| **8** | **Shift-JIS ZIP 在该机 ACP 下的落地文件名形态**（ACP 解码 / `U+EFxx` 转义 / `?` 替换） | 拷 `lab7z\sjis\sjis-legacy.zip` 过去，`7z l` + `7z x` 后看落地名的**码点**（不是看显示） | macOS 用的是 `U+EF00+原字节`，Windows 大概率不同。落地名形态决定 `FILENAME_ENCODING_RISK` 之后"人工修复"到底要做什么，也决定"96% 可逆的 GBK↔CP932 修复"这类说法在该机上成不成立 | §6.11（编码修复）、§5.8 |
| **9** | **PowerShell 参数数组下含 `` ` `` `$` `"` `%` `^` `&` `\|` 的密码传递**；以及 **7-Zip 把 `-p` 密码转成什么字节**（7z / ZipCrypto / RAR 三种容器可能不同） | 用 `tests\fixtures\lab7z\pw\p1..p18.7z` + `dash.7z` 在 `pwsh.exe` 与 `powershell.exe` 下各跑一遍 | 中文密码与特殊字符密码的成败。原 18 个样本是在 zsh 下验的（`【实测·macOS】`），PowerShell 的引号/转义规则完全不同；`Start-Process -ArgumentList` 官方明说只用**单空格拼接**、不逐参数加引号，`ProcessStartInfo.ArgumentList` 在 .NET Framework(5.1) 根本不存在（`【官方】`，转引 §8.8）。不测就没法确定 §8 的调用约定是否真的安全 | §6（密码链）、§8（调用约定） |
| **10** | **真实 SFX `.exe` 的 `7z l -slt` 输出**（`Type` 与 `Offset`） | 拿一个真 SFX 跑 `7z l -slt`；对比 `-t*` / `-t#` | Windows 上 `.exe` 是**合法 PE**，PE handler 可能抢先解资源节而不是尾随负载。现有 `sfx/selfext.exe` 是 macOS 7zz 造的桩，调研自己标注"这是最需要复验的一条"。判错会让 SFX 走进错误分支（要么当普通 exe 跳过，要么解出一堆 PE 资源当"内容"） | §5（归档识别、容器白名单） |
| **11** | **真实 RAR5 / 加密 RAR / 分卷 RAR 的解码** | 用 WinRAR 造 8 个样本（RAR4/RAR5 × 加密/非加密 × 单卷/分卷），逐个跑 `7z t` / `7z x` | 六份调研对 RAR 的**真实解码零验证**（造不出合法 RAR 样本），而资源站大量用 RAR。RAR4 的"密码错 vs 数据损坏"在工具层不可区分（WinRAR 官方：exit 11 仅 RAR5 有效，`【官方】`）——`EXTRACT_FAILED_AMBIGUOUS` 这个状态就是为它存在的，但它的**触发条件**目前是推断而非实测 | §6、§4（`EXTRACT_FAILED_AMBIGUOUS` 的进入条件） |
| **12** | **Defender 实时保护是否会在解压中途隔离/锁住文件** | 解压一个含 EICAR 串的样本，观察 staging 里的文件是否被移走/锁住，以及 `7z x` 的退出码与报错 | 会与"验证通过才删源包"打架，造成**假失败**（方向是保守的：源包保留，但流水线会莫名其妙停住且报错文本无关）。也决定 `_stg` 排除项到底是"优化"还是"必需" | §6（删除判据）、§10.1（M3） |
| **13** | **受限权限计划任务下 Defender 预检的失败形态** | 在 `-RunLevel Limited` 的任务里调一次 `MpCmdRun` | MS Learn 已确认 `MpCmdRun` **需提权运行**（`【官方】`，同 #6 的 URL），本 SPEC 据此把 Defender 预检裁给执行体 B（D-01）。这条要确认的是：失败是**干净报错**还是**静默跳过**——若是后者，A 的报告会显示"扫描通过"而其实根本没扫，这是一个静默的安全漏洞 | §6、§9 |
| **14** | **内置浏览器下载目录确实只有全局设置**（无按任务/按游戏目标） | 打开 设置 > 浏览器，逐项看有哪些选项 | D-02 的事实基础。112 KB 官方配置参考里 `download_dir` 类键 **0 命中**（`【官方】`），但配置文件里没有不等于 GUI 里没有。若 GUI 里其实有按任务的下载目录，`_inbox` + 认领归位这一整套可以简化 | §7、§5（认领归位） |
| **15** | **长路径实测**：`LongPathsEnabled=1` 时 `powershell.exe` / `pwsh.exe` 对 >260 字符路径的实际行为 | 造一个 270 字符路径做 `Get-Item` / `Remove-Item` / `[System.IO.File]::Replace` | 决定是"从源头压短路径"还是"可以放宽"。本 SPEC 已倾向前者（D-06、D-22），实测只是**确认代价**：如果长路径其实可用，D-05 里"MAX_PATH 预算超限触发 ASCII slug 降级"这条会显得过严 | §2（MAX_PATH 预算）、§3.2A（目录名降级） |
| **16** | **`-snz` 的默认状态**；以及 `7z.exe` CLI 是否读注册表 `HKCU\SOFTWARE\7-Zip\Options\WriteZoneIdExtract` | 给包打 MOTW → 分别加/不加 `-snz` 解压 → `Get-Content -Stream Zone.Identifier` 看产物 | 官方 changelog、switch 索引、独立页面**三处都没写默认值**（`【官方】`：我核对过随附手册的完整 switch 索引，`-snz` 有条目、无默认值说明、无独立页）。网上"default is No"全部是二手报道（`【社区】`）。不测则 D-18 的"默认传播"是一句没有依据的话；注册表那条**在有证据之前不得写进 SPEC** | §6（MOTW 策略）、§7（C 的前置条件） |
| **17** | **阿里云盘 / 夸克网盘 / 123 云盘 PC 客户端的临时文件后缀与落盘目录**（百度网盘的"完成后后缀残留"也要复核） | 各下一个文件，全程观察目录里出现过哪些文件名 | 这三家**查不到任何权威资料**。G1（无同组临时物）的模式表是空的，A 就没法判断"下完了没有"。SPEC 只能设计成"可配置的临时物模式表 + 首次使用时人工观察一次并登记"——这正是 D-14 的人工环节之③ | §5.3（G1）、§7（`netdisk-profiles.json`） |
| **18** | **MTool 三件事**：可执行文件的**签名状态**；是否需要管理员；注入产物的**实际文件名** | 看文件属性/数字签名；跑一次注入后 diff 目录 | 签名状态决定 `computer_use.windows.exes` 能不能写（未签名落 `default_app_access`，签名**无法明确验证**则直接被 block，`【官方】`）。注入产物文件名决定完成判据能不能机器判定——`与工具一同启动.bat` 是中文文件名，几乎肯定随界面语言变化（`【社区】`），不能写进判据 | §7（MTool） |
| **19** | **DPAPI vault 与运行身份的绑定**（跨用户/跨机不可解密的实际表现） | A 账户加密一个字符串，B 账户尝试解密；再试换机 | 只有社区共识，没有微软对 `ConvertFrom-SecureString` 的正面声明（`【社区】`）。影响 vault 的运维说明：重装/换机是否等于**密码全丢**。也决定计划任务运行身份必须与 vault 创建身份一致这条约束的严格程度 | §3（密码库）、§9（运维与诚实局限） |
| **20** | **`-mcp=932` 对 `x` / `l` 是否有效** | `7z l -mcp=932 sjis-legacy.zip`，对比不加的输出 | 三方矛盾：官方 `method.htm` 明写 `-m` 只适用 `a/h/d/rn/u`（不含 `x/l/t`，`【官方】`）；macOS 实测无效、连非法值都不报错（`【实测】`）；社区说法自相矛盾。**优先级低是因为主路径不依赖它**——ZIP 编码判定走纯字节解析 bit 11（§5.8），`-mcp` 只是一个可能的便利手段。不测的后果仅仅是少一个候选修复方式 | §6.11（编码修复的候选分支） |
| **21** | **桌面 App 计划任务的最小间隔**；`windows.sandbox_private_desktop=true`（默认）与 Computer Use"活动桌面"要求是否冲突；Computer Use 在多显示器/虚拟桌面/RDP 下的行为（官方**零表述**） | 实机试 | 本 SPEC 已把 A 的调度裁给 Windows 任务计划（D-10），所以这条对阶段一无影响，**已降级**。它只影响 C 的可用性细节（例如能不能把 C 赶到第二块屏幕上跑，从而部分缓解 E1） | §7 |
| **22** | **杀掉持锁进程后，另一进程能否立刻 `FileShare.None` 打开同一文件**（§4.9 S4-1） | `Start-Process` 起一个持锁进程 → `Stop-Process -Force` → 立即在另一进程抢同一句柄 | §4.6 互斥方案的**全部前提**是「OS 在进程死亡时自动释放句柄，因此不存在陈旧锁」。若释放有延迟，`--retry` 会随机撞 `LOCKED_BY_ANOTHER_RUN`——而我们又**明确拒绝**用超时清理兜底（那会引入时间猜测）。前提不成立就得重新设计互斥 | §4.6、§8.8.6 |
| **23** | **Codex 是否对脚本内部起的子进程重新审批** | 让一个已放行的 `.ps1` 内部 `Start-Process 7z.exe`，看是否再次弹审批 | 决定 §8.5.4「把审批面收敛到 1 次」的兜底设计是否成立。若子进程也逐个审批，A 的无人值守就是空话，必须改走任务计划以外的路径 | §8.5 |
| **24** | **重定向管道（非控制台）下 `-sccUTF-8` 是否生效** | `7z l -slt -sccUTF-8` 输出重定向到文件，看含日文条目名的包读出来是不是正确码点 | 不生效则 `l -slt` 阶段就把条目名读成乱码 → §6.8 的 D5（`Files: N` 与条目数一致）与 D6（逐条目 exists + size 对账）会在**完全正常的包**上假失败，源包永远删不掉，而人查不出原因 | §6.2.1、§6.8 |
| **25** | **`7z.exe i` 输出的 handler 名逐字拼写（含大小写）** | 目标机跑 `7z i`，与 `container-types.json` 的 `recurse_into_types` / `never_recurse_types` 逐字对照 | 拼写对不上 → 容器白名单匹配失败 → 每个包都落 `WAITING_FOR_RULE`，**流水线整体停摆**。三份草稿曾给出三套互相冲突的大小写，说明这条不能靠记忆 | §3.6.3、§5.4、§6.4 |
| **26** | **真实游戏包的最长内部条目路径**（RPG Maker MV/MZ 与 Unity 各取 3–5 个） | `7z l -slt` 求 `Path` 字段的最大字符长度与中位数 | 决定 §2.5 的 `E ≤ 222 − N` 闸门实际触发频率。若 E 中位数≈80，降级机制形同虚设；若≈180，多数日文长标题都会被强制降级成 `g0042-a1b2c3d4`，用户看到一库不可读的目录名——那属于设计失败，需要重新考虑库根还能不能再压 | §2.5、§3.2A |
| **27** | **`Unblock-File` 是否改动主数据流的 `LastWriteTime`** | 造一个带 MOTW 的文件，记 `LastWriteTimeUtc` → `Unblock-File` → 再读一次 | 决定 `manifest.stage1_snapshot` 拍在 `Unblock-File` **之前**还是之后。若会改，快照拍错时点会让阶段二的差集把**整库都算成「变了」**，MTool 产物识别彻底失效 | §3.5、§6.10、§7.6 |
| **28** | **单次巡检的实际耗时**（在含约 50 个游戏目录、`_inbox` 里约 30 个分卷文件的库上计时） | 真实库上跑一次 `Invoke-Sweep.ps1` 并计时 | 若超过巡检间隔（10 分钟），`MultipleInstances = IgnoreNew` 会把后续触发**静默丢弃**——没有报错、没有事件，任务计划历史里只有一行「已忽略」。巡检看似在跑、实则饿死，**是本项目最难发现的一类故障** | §5.10、§8.8.10 |


### 11.1 里程碑清账映射

| 里程碑 | 出口前必须有结论的条目 |
|---|---|
| **M0** | #1 #2 #3 #4 |
| **M1** | #7 #10 #17 |
| **M2** | #5 #8 #9 #11 |
| **M3** | #6 #12 #13 #16 |
| **M4** | #14 #18 #21 |
| 任意时点（不阻塞里程碑） | #15 #19 #20 |

**"有结论"的定义**：在 `D:\GameLib\_reports\` 下有一条记录，写明测了什么命令、看到什么输出、结论是什么。**"应该可以"不算结论。** 若某条实测下来结论是"做不到"，那也是结论——它会改变对应章节的设计，而不是被搁置。

### 11.2 已从清单中消解的三条

原始清单里的三条已被仲裁消解，**不再列入**：`7z l -slt` 解压后大小的字段名（已实测确定就是 `Size`，来自 7-Zip 跨平台公共输出层，与操作系统无关）、PowerShell 5.1 的 `ConvertTo-Json` 超 `-Depth` 是否告警（用"一律显式 `-Depth 10` + 写后回读校验"消掉，任何版本的告警行为都不再影响正确性，况且警告走 Warning 流、在 `-NonInteractive` 的计划任务里根本没人看得见）、`FileShare.None` 抢锁失败的 HResult 具体值（统一停在 `LOCKED_BY_ANOTHER_RUN` 不细分异常，比区分异常更符合确定性偏好）。

---

## §12 决策台账

本节把本 SPEC 的锁定决策与其**被否决的替代方案**并列存档，便于回溯"为什么是这样，而不是那样"。**落地阶段不得擅自更改**；唯一开放的是各条内标注"可配置"的数值旋钮（标定方法见 §10.4），以及 §12.2 的待用户拍板项。

可信度列的含义与 §0 一致：`【官方】` 官方文档/手册原文 · `【实测】` 本轮在 macOS 上跑出来的（并注明能否外推） · `【社区】` 论坛/博客共识 · `【待测】` 未核实，已进 §11。

### 12.1 决策表

| 编号 | 决策 | 选定方案 | 被否决的替代方案 | 理由与证据 | 可信度 |
|---|---|---|---|---|---|
| **D-01** | 执行体切分 | **三执行体**：A 巡检（Windows 任务计划，无密码、无提权、只读+归位）/ B 作业（人或 Codex 在交互会话触发，带密码、需提权）/ C 前台（Computer Use）。A+B = 用户要求的阶段一 | 上游的两分法：「阶段一 = 一个后台脚本」/「阶段二 = Computer Use」 | 需要密码的步骤和需要提权的步骤**都不能**放进无人值守计划任务：S4U 登录类型官方明写不存储密码、访问不了加密文件 → 解不开 DPAPI 密码库；`MpCmdRun` 官方明写**需提权运行**，与 `-RunLevel Limited` 直接冲突。切分后 A+B 仍完全满足 C1 | 【官方】https://learn.microsoft.com/en-us/defender-endpoint/command-line-arguments-microsoft-defender-antivirus （MpCmdRun 需提权，页首原文逐字复核）+ 【官方】任务计划 S4U 登录类型（转引 §8） |
| **D-02** | 下载落地 | 全局下载目录固定为 **`D:\GameLib\_inbox`**，执行体 A 按批次清单**认领归位**到 `games\<dirname>\`。**C3 是终态保证，不是落地瞬间的保证** | 上游设计的「Codex 将下载目标定到 `games\<游戏名>\`」 | 内置浏览器**没有**按任务/按游戏的下载目录——官方 112 KB 配置参考里 `download_dir` 类键 **0 命中**；只有一个 GUI 全局设置或「每次询问」。**这个能力不存在**，不是我们不用 | 【官方】本地留存的官方配置参考全文检索 + 【待测】§11 #14（GUI 侧复核） |
| **D-03** | 归档类型识别 | **永不传具体类型名**（`-t7z`/`-tzip`/`-trar`…）。只允许通配形式：`-t*`（强制只开一层，用于深度计数）、`-t*:s{N}m`（前缀垃圾 >8 MB）、`-t#:e`（人工诊断）。改后缀从主流程**降级为例外分支** | ① archive-reality 的「把 C7 实现为 `7z -t<type>` 显式指定类型」；② 整张「伪装扩展名 → 真实类型」映射表（`dat-as-rar`/`bin-as-zip`）；③ 「对 zip/rar/7z 三个候选各跑一次 `7z l -t<type>`」的受控探测 | 默认模式就是 `-t*:r` + 前 8 MB 签名扫描；扩展名只是「第一猜测 + 警告来源」。给**具体**类型会**关闭嗅探**：`-tzip fake.jpg`（内容其实是 7z）→ exit 2 硬失败，不给 `-t` → `Type = 7z` 正常打开。而资源站公告的规则本身经常是错的（「mp4 改 zip 或 rar 都行」这种表述说明上传者自己也不清楚真实类型）。**照着站点公告写死 `-t<type>`，会把本来自动能解的包变成硬失败** | 【官方】随 7-Zip 官方 tarball 分发的 `MANUAL/cmdline/switches/type.htm` 全文 + 【实测·macOS 7zz 25.01】（`-t` 语义属 7-Zip 跨平台公共层，可外推；仍需 §11 #1 确认 Windows 版本一致） |
| **D-04** | 改后缀的唯一硬例外 | **分卷 `.NNN` 重建**：伪装命名的分卷组必须**整组**补齐 `.NNN` 序列，且只在 `_stg` 里对副本做、原始文件永不改名、规则必须由批次条目**显式声明** | ① 「主流程完全不需要改名」（漏掉了这个真实存在的例外）；② 资源站教程的「先改 zip 再改 7z.001」三步法 | 实测：三个伪装成 `.mp4` 的分卷（实为 `.7z.001/.002/.003`）→ `7z t` **exit 2 完全打不开**，只改首卷也不行（报 `Unexpected end of archive`）；整组补 `.NNN` 后 → exit 0、`Type = Split`、`Volumes = 3`。且 **`.NNN` 之前的扩展名无所谓**——`game.mp4.001` 正常工作，所以站点教程中间那步「改成 zip/7z」纯属多余。C7 在真实资源站里出现的形态**恰恰以分卷为主** | 【实测·macOS 7zz 25.01】（卷集命名逻辑属跨平台公共层，可外推；§10.2.4 要求在 Windows 用 `7z.exe` 复跑确认） |
| **D-05** | 游戏目录名 | 完整规范化（NFC → 非法字符换**全角同形字** → 去控制字符 → 折叠空白 → 去尾部点与空格 → 保留设备名防御（含 `NUL.txt` 与 `COM¹/COM²/COM³` 上标变体）→ 长度裁剪 → SHA-1 短哈希防撞）**再加一道 ACP 可表示性往返检查**：能无损往返 → 用规范化标题；不能（或 MAX_PATH 预算超限）→ 降级 ASCII slug `g0042-<romaji-or-hash>`，真实标题写进 `manifest.json`。**无论走哪支都记录降级原因** | ① 一律保留日文标题（windows-download）；② 一律用 ASCII slug（archive-reality） | 两者的技术前提都不完整。真实约束**不是「非 ASCII」而是「必须能在系统 ANSI 代码页内无损表示」**——老引擎 KiriKiri/NScripter/RM2000-VX 用 ANSI API 打开自己的资源，在日文 Windows（ACP=932）上日文路径**完全正常**，在简中（936）上日文假名大部分能表示、部分汉字/记号不能。往返检查把一个口味之争变成了**可计算的确定性判定** | 【社区】（老引擎 ANSI API 行为）+ 【待测】§11 #1（该机真实 ACP 未知，是这条规则走哪一支的唯一输入） |
| **D-06** | 中转区位置 | **库级短路径** `D:\GameLib\_stg\<8hex>\`（25 字符），**必须与游戏库同卷** | 上游的 `games\<名>\.gameflow\staging\<attempt-id>\` | 上游设计单这一段就吃 39 字符预算，配合日文长名 + 多重嵌套会真的撞破。建目录实际上限是 **248**（MAX_PATH−12）不是 260。同卷是硬前置：`File.Replace` **跨卷抛异常**，staging→游戏目录跨卷移动会退化成复制+删除。附带收益：Defender 排除项从「每游戏一个通配路径」简化成一个固定目录（支撑 D-19）。**不违反 C3**——C3 约束的是原始压缩包的终态位置，中转区是临时的、解完即清空 | 【官方】`ReplaceFileW`（跨卷）+ 算术（字符数可验算）+ 【社区】（MAX_PATH−12 的建目录上限） |
| **D-07** | 配置格式 | **JSON**，人类注释走 `"_note"` 字符串字段。所有 `ConvertTo-Json` **一律显式 `-Depth 10`** | YAML（上游的 `batch.yaml`） | PowerShell 内置 JSON、零依赖；YAML 在 5.1 上需要装模块（`powershell-yaml`），而 Codex 的原生 agent 跑 `powershell.exe -NoProfile`，模块可用性不可控。`-Depth` 必须显式：默认 `-Depth 2`，本项目的批次清单第 4 层就会被**静默截断**。JSON 不能写 `//` 注释，所以约定 `"_note"` 字段 | 【官方】`ConvertTo-Json` 默认 `-Depth 2` + 【官方】Codex 配置参考（Windows agent 用 `powershell.exe -NoProfile`） |
| **D-08** | 状态持久化 | **`events.jsonl` 为唯一真相**（append-only），`state.json` 是**派生快照**，任何时候可删掉从 events 完全重建（I2）。JSONL 必须 `-Compress`、整行一次 `Write` + `Flush($true)` + 立即关闭；读取端逐行解析并**丢弃解析失败的最后一行** | 「以 `state.json` 为主、崩溃时靠原子替换保证一致」 | `File.Replace` **不是原子的**——官方自称「把几步合进一个函数」并列举三种部分失败中间态，`MoveFileEx` 全文无 atomic 字样。真正的崩溃安全来自「唯一真相是 append-only 日志 + 派生物可重建」，不是追求单次替换的原子性。不保持长开 StreamWriter：强杀会丢缓冲区。默认多行 JSON 会毁掉「一行一条」，所以 `-Compress` 是硬要求 | 【官方】`ReplaceFile` / `MoveFileEx` 文档（转引 §8.8）+ 【社区】（JSONL 半行恢复惯例） |
| **D-09** | 并发互斥 | **`FileShare.None` 独占文件句柄**（OS 在进程死亡时自动释放）。抢锁失败统一停在运行期状况 `LOCKED_BY_ANOTHER_RUN`，**不落盘、不进状态机**、不细分异常。若改用命名 Mutex 必须加 `Global\` 前缀 | ① lock 文件 + PID 存活检测 + 超时清理陈旧锁；② 默认（`Local\`）命名 Mutex | Windows 的 PID **复用很快**，PID 存活检测会误判；「超时清理陈旧锁」引入时间猜测，与确定性偏好直接冲突。`Local\` Mutex 只在当前终端服务会话内可见——**计划任务与交互会话互相看不见**，而这恰好是 A 与 B 的关系，等于没有互斥 | 【社区】（PID 复用）+ 【官方】（Mutex `Global\`/`Local\` 命名空间语义） |
| **D-10** | 阶段一调度 | **Windows 任务计划**（`MultipleInstances = IgnoreNew`，**非 S4U**，DPAPI vault 创建身份与运行身份一致） | Codex 桌面 App 的 Scheduled tasks（绑定 `D:\GameLib` 为 project，prompt 里触发 skill） | ① 品味：A 是纯确定性文件处理器，用一个 LLM turn 去驱动它等于把确定性逻辑塞进不确定的载体，还要烧 token；② 依赖面：Codex 计划任务要求「开机 **+ 桌面 App 常驻** + 项目目录在盘上 + 沙盒能写到库根」，任务计划只要求用户登录着；③ E1：这是主力机会被打断，桌面 App 可能被关掉，任务计划不会；④ 沙盒：Codex 计划任务用默认沙盒设置无人值守跑，workspace-write 写不到 project 外，还要额外配 `writable_roots` | 【官方】Codex 计划任务的前置条件原文 + CONTEXT §七（用户品味）+ E1 |
| **D-11** | Codex 操作面 | 五个固定操作做成 **skills**，放 `.agents/skills`，建议 `allow_implicit_invocation: false` | ① `custom prompts`；② 放 `.codex/` 目录 | `custom prompts` **官方已废弃**。`.codex/` **不是 skills 目录**——它是本地环境配置，其中的 actions 跑在集成终端而非 agent 沙盒，权限模型完全不同。`allow_implicit_invocation: false` 保证触发是显式的（确定性偏好） | 【官方】Codex 文档（custom prompts 废弃、skills 目录位置、`.codex` 语义） |
| **D-12** | 嵌套深度 | **`max_depth = 5`**（可配置）。计数口径：**「成功解开一个白名单容器 = depth+1」**，不是「调用了几次 7z」；用 `-t*` 强制每次只开一层让计数与现实一致 | ① `max_depth = 4`（CONTEXT 的取整）；② 按 7z 调用次数计数 | 真实资源站的形态证据：「改名 → 外壳 zip → 分卷 rar → 7z → 同名文件夹」就是 4~5 层，加上「改 2 次后缀解压 2 次」的站点规则会更深。计数口径必须钉死是因为 `.tar.gz` 有的工具一步到底、有的两步；官方手册还明写有些格式（如 VHD 里的 MBR）会**一步开两层**，不加 `-t*` 会让「深度计数」和「哪一层需要密码」对不上。代价可忽略：触上限只是停机（`BLOCKED_RESOURCE`）不是破坏 | 【实测】（`.tar.gz` 一步/两步差异）+ 【官方】`type.htm`（`*` 只开一层）+ 【社区】（资源站真实形态） |
| **D-13** | 压缩比护栏 | `ratio_warn = 200`（**仅记录**）、`ratio_block = 1000` **且** `declared_total > 5 GB` 才阻断。公式 = `sum(条目 Size) / 归档级 Physical Size`。**ratio 绝不单独阻断** | ① `ratio ≥ 100` 直接阻断（windows-download 的经验值）；② `sum(Size)/sum(Packed Size)` 的公式 | 实测合法的纯文本汉化补丁 7z 就有 **133x**，而真炸弹只有 **1029x**——中间没有干净分界，阈值 100 会在正常素材上频繁误伤。公式必须用归档级 `Physical Size`：7z 默认 solid，一个 block 只有第一个条目带 `Packed Size`，`sum(Packed Size)` 是错的。而且**头部声明的大小会撒谎**（`liar.zip` 实测），真正的护栏是运行期 staging 体积监控 + 绝对上限 + 磁盘余量。字段名就是 `Size`（不存在 `Uncompressed/Unpacked Size`） | 【实测】（133x / 1029x / solid 的 Packed Size 空缺 / 字段名）——归档元数据解析属跨平台公共层，可外推；阈值本身需按 §10.4 用用户样本标定 |
| **D-14** | 「阶段一的人工环节」的确切定义 | **五项**：**①把本批次排进网盘客户端的下载队列**（每批一次，客户端下载目录一次性设成 `<root>\_inbox`，§7.0）；**②录入并确认密码后启动解压**；**③Defender 预检的提权确认**；**④首次遇到新网盘客户端时观察并登记其临时后缀**；**⑤首次为 `_stg` 与 MTool 目录设置 Defender 排除** | ① 交给 Computer Use 去点（原设计）；②–⑤ 把这句话继续留空（CONTEXT §五的悬空项） | ②–⑤ 是 D-01 切分的**直接推论**：不能进无人值守任务的恰好是「要密码的」和「要提权的」两类，加两个只做一次的登记动作。① 是用户 2026-09-14 的裁定（D-32）：E1 下让 C 去点十个下载，是拿几十分钟机器占用换两分钟手动操作，且可靠性差一个量级。有了这五项，C1 里「阶段一（需要人工做一部分）」才是可验收的 | ②–⑤ 由 D-01 推导；① 用户裁定 + E1。④ 另见【待测】§11 #17（三家网盘客户端查不到任何权威资料，只能人工观察） |
| **D-15** | 全量 CRC 重读 | **默认关闭**，可配置。**唯一强制场景**：源包是 **ZipCrypto 加密的 ZIP**（`Encrypted = +` 且 `compress_type != 99`），此时追加全量 CRC 重读（D7） | 「解压后重读全部文件算 CRC」作为无条件的最高优先级删除判据 | 那条建议的实验依据是 POSIX 的 `ulimit -f` + `SIGXFSZ` 造出的静默截断——**Windows 没有信号也没有 `ulimit -f`**，磁盘满是 `ERROR_DISK_FULL` 会被 7-Zip 显式报成写错误并返回非 0；而且 `7z x` 解码时本就逐条目校验 CRC。10 GB 包全量重读要几分钟。**唯一真实存在的例外是 ZipCrypto 的 1/256 假通过**（实测），它会解出垃圾文件而工具层不报错——AES（`compress_type == 99`）有 HMAC 不受影响，所以范围可以精确限定 | 【实测】（ZipCrypto 1/256 假通过）+ 【官方】（Windows `ERROR_DISK_FULL` 语义）+ 保留的廉价判据（条目数对账、逐条目 exists+size）来自【实测】的「exit 0 但什么都没解出来」四种情形 |
| **D-16** | 删除的触发方式 | 删除由 `state.json` 里**已落盘**的 `verified:true` + `consumed_by:<attempt-id>` **gate 住，不现场重新判断**（I3）。删除前必须把每个源包的 SHA-256 写进 `manifest.json` | 「删除时现场再跑一遍 D1–D12 确认」 | 现场重判**破坏幂等**：重跑时环境已变（staging 已清空、产物已落地），同一套判据会得出不同结论，于是「重跑一次」可能删掉上次没删的、或漏删上次该删的。已落盘的 gate 让「删不删」成为一个可回溯的历史事实而不是一次现场推断。SHA-256 是零成本的后悔补偿：误删后能确认重下的是不是同一个包 | 【社区】（幂等设计惯例）+ 由 I2/I3 推导 |
| **D-17** | 7-Zip 版本下限 | **硬下限 25.01，建议 ≥ 26.02**。理由是**解析不可信归档的内存安全漏洞**，不是 MOTW。落后时报告顶部长期提示，**但不拒绝运行** | ① 「最低 ≥ 26.03，理由是 MOTW 保留漏洞 CVE-2026-58052」；② 不设下限 | 用 MOTW 当版本下限的理由**自相矛盾**：本 SPEC 明确不把 MOTW 当安全边界（D-18），既然不依赖它，它的 CVE 就不在威胁模型里。真正的理由更有力——本项目就是**拿陌生归档去喂解析器**：26.02 的 CVE-2026-14266（XZ 解压堆溢出 **RCE**）、26.01 一批 handler 越界读（SquashFS/NTFS/UEFI/UDF/WIM/Ar）、25.00/25.01 的符号链接连续加固。26.03 距调研只有 9 天，硬卡最新版是很硬的运维要求，而 E3 连装的是哪个软件都还没确定 | 【官方】7-Zip 官方 changelog / History.txt（本地留存） |
| **D-18** | MOTW 策略 | **默认传播**，验收 + Defender 扫描通过后再显式 `Unblock-File`。**「解除标记」是执行体 C 的前置条件而非可选收尾**。**MOTW 不得作为安全边界** | 「默认不加 `-snz`」（保可玩性） | 传播 + 最后统一解除，能在流水线**未完成的中间态**保留 SmartScreen 这道提示（用户中途手动去点 exe 时）；不传播则从第一刻起就没有。「可玩性」的顾虑被显式 `Unblock-File` 收尾解决（官方明文：幂等、对未标记文件无副作用、PS 3.0+ 可用）。**代价必须写明**：忘了在 C 之前跑 `Unblock-File`，Computer Use 会撞上 SmartScreen 的**前台模态框**并卡死。不作安全边界的理由：默认值未知 + CLI/GUI 行为不一致 + 历史上反复出漏洞；真正的护栏是显式 Defender 扫描 | 【官方】`Unblock-File`（幂等、无副作用）+ 【官方】`-snz` 开关存在（22.00 引入，Windows-only）+ **【社区】+【待测】§11 #16**（默认值官方三处都没写；注册表 `WriteZoneIdExtract` 影响 CLI **无任何证据，不得写进 SPEC**） |
| **D-19** | Defender 集成 | **三级分流**：A 级明确恶意 → `THREAT_CONFIRMED` 硬停；B 级疑似误报 → `THREAT_SUSPECTED` 软停等人工裁决；C 级扫描失败 → 按 B 处理。**排除项只排 `D:\GameLib\_stg\`，不排整个库**。人工放行**阶段一**产物按 **SHA-256**；**阶段二**的 MTool 产物只能**目录级排除** | ① 「扫到就停」；② 排除整个 `D:\GameLib`；③ 对 MTool 产物也用 SHA-256 白名单 | 游戏破解/汉化补丁的 Defender 误报是常态，「扫到就停」等于流水线停摆。排除整个库是净负面的安全交易。SHA-256 白名单对阶段一产物可行（哈希固定），但对 MTool 的 `version.dll` **原理上无效**——它是每次注入现生成的、**哈希随游戏实例变化**（Kaspersky 版主确认），逐个加排除项不可能工作。两者必须分开写。组合成立的关键：`MpCmdRun -Scan -ScanType 3` 官方明写**忽略排除目录**，所以「排除 `_stg` 免打断实时保护」与「显式预检仍然扫得到」不冲突 | 【官方】MS Learn MpCmdRun（同 D-01 的 URL：需提权、`-DisableRemediation` 语义、忽略排除项）+ 【社区·一手】（Kaspersky 版主关于 `version.dll` 哈希的确认）+ 【待测】§11 #6（stdout 格式是该模式下唯一的检出信号） |
| **D-20** | 密码策略 | 优先级链 **P0→P6**；**等价展开 ≤4 个**：`[原样, strip(), utf-8字节按gbk解, gbk字节按utf-8解]` 去重。**不断言系统 ACP 必须是 936**——把 ACP 与 Beta UTF-8 状态记进 `state.json` 作为解释失败的上下文，中文密码全败时分流 `PASSWORD_ENCODING_SUSPECT`。密码库存储**字节精确**，UI/日志**不得做任何 Unicode 规范化** | ① 从游戏名/域名/文件名推导密码；② 常见密码字典；③ 把 README 里「读懂」的一段话当密码；④ 「SPEC 写死 ACP == 936、要求关闭 Beta UTF-8」 | 等价展开有限、确定、可枚举——**这不是暴力破解**，是对「同一个中文密码在 GBK 与 UTF-8 系统上是两串不同字节」这一事实的确定性覆盖。断言 ACP=936 等于把用户的机器假定成简体中文 Windows，而 CONTEXT 明确「Windows 版本未知」；若那台机器是日文（932），legacy Shift-JIS 包反而原生正常、整套 GBK 换算不成立。NFKC 会折全角（`ｐａｓｓ１２３` → `pass123`），直接破坏密码；尾随空格有意义（`'pass '` ≠ `'pass'`，所以原样与 strip 都要留） | 【实测】（密码字节层事实：GBK/UTF-8 互不通用、全角、尾随空格）+ 【官方】`password.htm`（只有 `-p` 一种输入方式）+ 【待测】§11 #1（该机 ACP）、§11 #9（7-Zip 把 `-p` 转成什么字节，三种容器可能不同） |
| **D-21** | 引擎判定与 MTool | 引擎判定用**有序表、先匹配先赢**，按**版本段前缀**（`RGSS1*`/`RGSS2*`/`RGSS3*`）；NW.js / Electron 只作属性记录不参与裁决。**主 exe 需人工确认一次**（候选集 + `ambiguous` → 写进 `mtool-profiles\<item-id>.json`）。**`UNKNOWN` ≠ 不支持翻译** → 停 `WAITING_FOR_MTOOL_RULE`，不判失败。MTool 不支持 Unity/Godot/Unreal → `TRANSLATION_NOT_APPLICABLE` 终态 | ① 照抄 SteamDB 的引擎正则；② 「多 exe 时自动选 `Game.exe`」；③ 把 `UNKNOWN` 当作不支持 | SteamDB 的正则只枚举 5 个 DLL 名，**漏 `RGSS300.dll` / `RGSS202J.dll`**，还把所有 RPG Maker 合并成一个标签。「自动选 `Game.exe`」有论坛真实反例：正确答案是 `開始游戏.exe`，选 `Game.exe` 会卡在加载界面。`UNKNOWN` 的反例是 Enigma Virtual Box 单 exe 打包的 MV——判 UNKNOWN 但 MTool 能处理。另外「游戏根目录」= 包含引擎标记文件的那一层，**不等于** `games\<dirname>\`（解压常多出一层，且按 C3 源包就在同级） | 【官方】Valve `rules.ini`（本地核对，作为反例来源）+ 【社区·一手】（多 exe 论坛反例、EVB 反例）+ 【官方】MTool 官网与 Patreon 两个独立来源的引擎清单（均无 Unity/Godot/Unreal）+ 【待测】§11 #18 |
| **D-22** | 库根路径 | **`D:\GameLib\`**（10 字符根名）。**不得**放在 OneDrive 同步目录或 `Program Files` 下 | ① 上游的 `D:\GameLibrary\`；② 放在用户目录或 `Program Files` 下 | MAX_PATH 预算：`D:\GameLib\games\` = 17 字符，建目录实际上限是 **248**（MAX_PATH−12）不是 260；配合日文长名 + 多重嵌套，根名每多一个字符都是真金白银。OneDrive 有社区证据导致 MTool inject 失败；`Program Files` 写不进 `version.dll` | 算术（字符数可验算）+ 【社区】（OneDrive inject 失败、`Program Files` 权限）+ 【待测】§11 #1（确认库根实际不在这两处）、§11 #15（`LongPathsEnabled` 若可用，这条的严格程度可放宽） |
| **D-23** | 不变量 I5 的措辞 | **枚举式白名单**：A 的写集合恰为三处——① `games\<dirname>\`（含 `.gameflow\`）② **新建** `_reports\<batch-id>-<seq>.md`（不得覆盖既有报告）③ 把 `_inbox\` 中已认领的文件**移入** ①（同卷 rename，不构成删除语义）。除此之外不写、不删、不回收 `_stg\`（只报告孤儿） | 原措辞「A 永不写 `games\<dirname>\` 以外的路径，永不删除」 | 原措辞与分派给 A 的职责**自相矛盾**——A 必须写对账报告、必须把文件移出 `_inbox`；同时它又**禁不住**真正危险的操作（没说「不许覆盖既有报告」）。三位起草者独立提出同一异议。枚举式白名单更短、可断言、能直接写成一个 assert | 内部一致性（可验算） |
| **D-24** | 新增两个停机态 | **`PATH_BUDGET_EXCEEDED`**（路径预算超限且 dirname 已降级仍不够）与 **`JOURNAL_CORRUPT`**（`events.jsonl` 不可解析） | ① 把路径超限塞进 `WAITING_FOR_RULE`；② 把日志损坏当作不落盘的运行期状况 | ① 路径超限的人工动作是**改配置**（换更短库根/指定更短目录名），与 `WAITING_FOR_RULE` 的「补规则」语义不同，混在一起会让停机态手册的「人工动作」一列自相矛盾；② 日志损坏若只是运行期状况，条目会在报告里**永久静默停住**——抢锁失败下次就好了，日志损坏下次还是坏的，两者性质不同 | 内部一致性 |
| **D-25** | Defender 预检的时点 | 钉死在 **`VERIFYING` 之内、`staging.promoted`（落地）之前**。`THREAT_*` 的 `from_state` 是 `VERIFYING` | 「`D1–D12` 全过并落地删包后进 `EXTRACTED`，再做 Defender」 | 被否方案下**源包在裁决前就被删了**，于是「A 级留证、不删源包」不可能成立（直接破坏 I1）；且扫描对象变成已落地到 `games\` 的产物，而「只排 `_stg` + `-DisableRemediation` 忽略排除目录」这套组合成立的前提正是**文件还在 `_stg` 时扫** | 内部一致性 + §6.9/§9.8 的三方一致 |
| **D-26** | 事件的状态变更通道 | **唯一通道是 `state.entered`**，迁移信息进 `payload.{from_state, to_state, transition_id}`；§3.3.1 是事件 schema 的唯一权威 | 定义 14 个具名迁移 `kind`（如 `download.claimed`/`verify.result`）与 `state.entered` 并存 | 三份草稿各自宣称自己是权威，`kind` 名互不相交（`download.claimed` vs `item.claimed`、`reason_code` vs `reason`）。`events.jsonl` 是 I2 的唯一真相，schema 不唯一意味着**重放器、对账报告、删除 gate 三个消费者各写各的** | 内部一致性 |
| **D-27** | 崩溃恢复的来源 | **`.bak` 不作恢复源**，只作诊断与取证；恢复路径唯一——`state.json` 不可解析就从 `events.jsonl` 全量重放 | 「`.bak` 可解析就直接用它恢复」（两份草稿都写成了正常路径） | 用 `.bak` 恢复会**跳过重放**，得到一个 `last_event_seq` 落后于日志的快照，而删除 gate（I3）读的正是这个快照里的 `verified` / `consumed_by`。保留 `.bak` 的真实价值是让崩溃后的盘面布局**确定可判读**（`ReplaceFile` 有备份与无备份时的残留命名完全不同） | 内部一致性 + I2 |
| **D-28** | 事件重放顺序 | **按文件行序** fold，`seq` 只用于人读与断裂检测 | 「按 `seq` 排序后 fold」 | 封口逻辑**允许**出现一次重复 `seq`；按 `seq` 排序时这两条事件的先后不确定，重放结果就不确定，I2 失效。行序是磁盘上的客观事实，不需要任何排序假设 | 内部一致性 + I2 |
| **D-29** | G2 采样模型 | **混合**：未认领文件只能**运行内采样**（并写明这是 I5 白名单造成的结构性限制）；已认领文件可把 `download.gate` 事件作为跨运行采样源，G2 取「本轮采样 ∪ 最近 N 条 `download.gate`」 | 纯跨运行采样（每轮追加一条采样事件，明确否决运行内采样） | 纯跨运行方案在本 SPEC 里**结构上不可实现**：`_inbox` 中未认领的文件既没有 `item_id`（事件 schema 必填），也没有 A 被允许写入的目录（I5 白名单） | 内部一致性 |
| **D-30** | 批次条目的状态字段 | 批次清单**没有** `state` 字段，只有 **`abandoned: true\|false`**。运行期状态一律取自 `state.json`（不可解析时从 `events.jsonl` 重放） | 在批次条目里保留一个 `state` 字段（哪怕只用于声明初始值） | 只要字段还在，早晚会有人或某个 Codex turn 顺手把运行期状态写回去，**下一个读它的人就被骗了**。三份状态副本里必须只有一份是真相 | 内部一致性 + I2 |
| **D-31** | 7-Zip 封装的类型开关 | `-TypeSpec` 增加合法取值 **`none`** = 不传 `-t`，走默认 `-t*:r`。**A 的顶层预检与 G5 必须用 `none`**；`*` 只用于 §6.4 的递归计层 | 封装总是拼 `-t<TypeSpec>`（结构上发不出「不带 `-t`」的调用） | 带 `-t*` 只能看到 Split 外壳、**拿不到内层 `Size` 表**，于是体积预算、压缩比护栏与路径穿越预检全部落空。A 的只读预检恰恰全靠这张表 | 【实测·macOS，可外推】 |

| **D-32** | 网盘下载由谁执行 | **人工排队为主路径**：你把客户端下载目录一次性设成 `<root>\_inbox`，把本批次排进下载队列（每批约两分钟）；A 负责监视、完成判定与认领归位。Computer Use 驱动客户端降级为**可选路径**，保留设计但不是主路径（§7.0） | ① Computer Use 驱动客户端（原设计，`baidu` / `quark-ali` 走 §7.5）；② 全部改用第三方 API / 破解客户端 | E1 是硬约束：C 跑的时候用户用不了主力机。让 C 去点十个下载 = 拿几十分钟机器占用换两分钟手动操作，且客户端界面变化、限速弹窗、验证码、登录态过期对人是零成本干扰、对 Computer Use 是随时卡住的失败点。**且这条零架构改动**——`_inbox` + G1–G6 + 认领归位本来就是为「文件从哪来我不管」设计的。方案② 违反 §9.10 的合规边界 | 用户 2026-09-14 裁定 + E1 |
| **D-33** | 库根的数量 | **允许登记多个库根**（`gameflow.config.json` 的 `library_roots[]`），但**一个批次只能落在一个库根里**（LR2）。每个库根各有一份完整子树，彼此无共享状态；MAX_PATH 预算、同卷约束、Defender 排除项、NTFS 要求**逐库根**独立核验（§2.4.3） | ① 单一全局库根（原设计）；② 每个游戏任意路径、可跨盘 | 方案② 的代价是真实的：`_stg` 必须与游戏库同卷（`File.Replace` 跨卷抛异常），跨盘要每卷一个 `_stg`、预算表每卷一份、Defender 排除项变多个；而批内统一就能让同卷约束与预算表在批内仍然成立，只需把 `library_root` 从全局常量改成受登记集合约束的批次字段 | 用户 2026-09-14 裁定；同卷约束见【官方】`ReplaceFileW` |
| **D-34** | 密码候选的尝试顺序 | **按 `hit_count` 降序排序**（完整全序规则见 §3.7.4），vault 记 `hit_count` / `miss_count` / `last_used_at`。N7 相应改写为「禁止历史**改变判定结果**」，并新增 N7b 把本条列为唯一例外 | ① 恒按 `added_at` 升序（原设计，N7 原文明禁「按历史成功率排序候选」）；② 全局高频密码池优先试 | 它通过了 §1.2.5 的三问：结果不会不同（同一候选集穷举完结论一样）、可解释（计数器可读可 diff 可清零）、可复现（给定计数器顺序唯一）。省的是 `7z t` 的进程启动与归档打开开销，在大包上不可忽略。**方案② 被明确禁止**（护栏 V1）——跨 key 借用高频密码就是 §6.3.2 硬禁的字典爆破换了身外衣 | 用户 2026-09-14 要求 +【实测】资源站真实密码形态（站点/上传者级统一密码占绝大多数） |


### 12.2 待用户拍板项

目前只有一条。**它已有一个带论据的默认值并已在 §3/§6/§9 实装**——列在这里不是因为悬而未决，而是因为它是一条关于**你自己的数据**的策略选择，应该由你签收而不是由 SPEC 替你定。

#### 两段式删除：`retention.trash_days` / `retention.trash_max_bytes`

**当前默认：`trash_days = 3`、`trash_max_bytes = 2 GiB`（尺寸门控的两段式）。**
源包字节数 **< 2 GiB** → 先移入 `games\<dirname>\.gameflow\trash\`，3 天后由 B 清理；**≥ 2 GiB** → 验证通过后直接真删。

选这个默认值的理由是**误判概率与磁盘代价的分布不重合**：

- 误判概率最高的恰恰是**小包**——纯文本汉化补丁既是压缩比 133x 触 `ratio_warn` 的那一类（`【实测】`），也最容易命中 `EXTRACTED_NO_CONTENT` 的边界判定。
- 磁盘压力只在**大包**上成立——一个 40 GB 的游戏本体多占 3 天，下一批很可能因此落 `BLOCKED_RESOURCE`。
- 同卷移动是元数据操作，成本接近零；磁盘代价有硬上限（`trash_max_bytes` × 并发条目数），不是无界的。

**C4 仍然满足**：源包在游戏目录里自动消失、无需任何人工动作，只是小包的物理删除时刻推迟 3 天。

| 你可以怎么调 | 后果 |
|---|---|
| **保持默认**（`3` / `2 GiB`） | 小包有 3 天后悔窗口；大包仍是删了就没了 |
| **`trash_days = 0`** | 回到 **C4 的字面语义**：一律立即删除、不可撤销、无回收站兜底。D1–D12 判错一次即永久数据丢失 |
| **调大 `trash_max_bytes`** | 更多包进 trash，拿磁盘换后悔窗口。调到大于最大游戏包 = 全部两段式 |
| **调大 `trash_days`** | 后悔窗口更长，trash 占盘时间线性增长 |

**必须签收的代价（无论怎么调都消不掉）**：走直接删除的那部分包，判错就是**永久丢失**。唯一补救是 `manifest.json` 里删除前写下的 SHA-256——它只能告诉你"重新下载的是不是同一个包"，**并不保证你还能下到**（资源站失效、分享链接过期、网盘和谐都很常见）。

**实施建议**：M2 阶段 `delete_archives = false` 先跑若干真实批次、积累对账证据，确认 D1–D12 在你的实际资源上不误判，再在 M3 打开删除。

---

> ### ⚠️ 遗留异议（起草者对某条锁定决策的保留意见，主控未采纳）
>
> **一、D-14 的四项定义在用户视角下是不完整的，建议由 §4 补一句话。**
>
> D-14 把「阶段一需要人工做的一部分」定义为四项：①录入确认密码后启动解压 ②Defender 提权确认 ③首次登记网盘临时后缀 ④首次设置 Defender 目录级排除。这四项是 D-01 切分的直接推论，**作为「常规流程里必然出现的人工环节」的定义是准确的**，我照契约写了。
>
> 但 §4.2 的停机态本身**也是人工环节**，只是由异常触发而非必然出现：`THREAT_SUSPECTED` 要人裁决是不是误报、`WAITING_FOR_RULE` 要人显式声明分卷/前缀/未知容器的规则、`FILENAME_ENCODING_RISK` 要人判读、`WAITING_FOR_VOLUMES` 要人补卷、`EXTRACT_FAILED_AMBIGUOUS` 要人在「密码错」与「数据损坏」之间做判断、`ABANDONED` 要人决定放弃。用户读到「阶段一需要人工做的就是这四件事」时，会低估实际的人工负担——而这四件里有三件是**一次性**的（②③④），真正的日常人工负担恰恰在停机态上。
>
> 建议：D-14 的四项保持不变，但 §4 在停机态手册开头补一句「停机态的解除同属阶段一的人工环节，只是由异常触发而非常规必然」，并在对账报告里把「待人工处理条目数」作为一个显式指标。这不改任何契约，只补一个定义的边界。
>
> **二、阈值标定与契约的关系需要主控澄清（不是异议，是请裁）。**
>
> §6.12 说 `ratio_warn=200` / `ratio_block=1000` 是「写死为可配置默认值」，§10 又要求「阈值要用用户手上的真实样本标定，不能凭空定」。我按「默认值是契约、标定后的实际值是配置、标定只上调不下调、每次调整记台账」处理（§10.4.1）。若主控认为阈值本身也是契约，则 §10.4 的标定要求需降级为「只记录观测值，超限时在报告里提示人工修改契约」——两种处理方式对实现的影响不同（前者是配置文件里的数，后者是要改 SPEC 才能动的数），请主控明示。
>
> **三、`D:\GameTst\` 已由主控收编（异议已解决，保留于此仅作记录）。**
>
> §3 只定义了生产库根 `D:\GameLib\`，而 §10.2.3 的测试隔离硬护栏必须有一个独立的测试库根。我引入 `D:\GameTst\`，特意取与 `D:\GameLib\` **等长的根名（各 10 字符）**，使测试的 MAX_PATH 预算与生产逐字符相等（短一个字符，路径超限的 bug 就会在生产上首次暴露）。**主控裁决：由 §10 具名，§2.4 只保留约束。** 库根是配置项，把一个具体的测试路径写进 §2.4 的布局树会与「库根可配置」自相矛盾——所以 §2.4 只留「测试库根必须与生产库根等长」这条 MAX_PATH 等价约束，**具体名字 `D:\GameTst\` 由本节持有**。它由 `-Profile test` 解析得到，不出现在任何生产代码路径里。
